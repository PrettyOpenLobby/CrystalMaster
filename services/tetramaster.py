#!/usr/bin/env python3
"""Tetra Master (PlayOnline content id 2) world server.

STATUS (2026-08-26): the TM wire protocol IS reversed and IS served. This module
answers the in-match family on code 0x43 (`@StartData`, `@TurnData`, `@PutCard`,
`@BattleData`, `@ResultData`, `@Continue`, ...), the card shop and auction on
0xB2, and the card trade on 0xA2. Matches have run end to end live (2026-08-20,
2026-08-22) and a trade has completed live (2026-08-25).

WARNING: THE COMMAND TABLE BELOW IS THE AUTHORITY, NOT THE PROSE. Search for
`cmd  name             Recv=` before believing any sentence in this file about
what is or is not decoded. Prose ages; that table is read off the client's own
dispatch arms. Two comment blocks in this file (this docstring's STATUS, and the
fall-through arm at the end of `_handle_line`) sat ~17,000 lines out of date and
caused a reviewer to report the whole match protocol as unimplemented.

COVERAGE (audited 2026-09-04 against the client's command-table map and its 52-key
send list): every client send has a handler or a measured reason for silence.

- `@BattleSelect=`   handled (`3ad0700a`), the multi-defender picker.
- `@Break=`          (0x43 msgid 39, `/ID=0`) the in-match LEAVE notice, sent ~2 s
                     before `@GameExit=`. No reply is polled; the server takes
                     over the empty seat (`_seat_departed`).
- `@ExitCh=` (0x20), `@Viewer=` (0x41 msgid 0x11), `@Mode=` (0xB2): fire-and-forget
                     senders, read off their functions -- no store lookup, no
                     wait. Logged, not answered. `@Save=` likewise (0x87410 is a
                     formatter; its callers send through 0x111750 and poll
                     nothing), so `POL_TM_SAVE_ANS=0` is the measured default.
- 0x43 `@Data=`      msgid 37 is the watch request (handled); no other msgid has
                     ever been seen on a wire.

PRODUCED IN ONE MEASURED CASE (read off the client 2026-09-04):

- `@DataError=/No=` (0x43 cmd 40): the card-select scene (0xD7280) polls it
  beside the picks and, on a hit, sends `@Quit=` and leaves (0xD7317..0xD737C;
  `/No=` is parsed and discarded). Sent when a `@CardSelect=` arrives for a
  match this process has no roster for (a restart ate it) -- the only scene
  that polls cmd 40 is card select, so it is sent there and nowhere else.

STILL UNPRODUCED -- messages the client can PARSE that this server never sends,
because no wire capture shows what triggers them (do not invent one):

- `@ComList` (0x43 cmd 2/4): `/L=` count into obj+0x18C, `/D=` per row into
  the table at 0x524B630 (stride 0x14). Polled by the result scene (0x10D5ED)
  in a branch that sends `@Continue` cmd 17 with a third argument and sets
  0x5246586=1 first -- a "play again against a different opponent" path the
  live VS. COM rematch (09-03) never took (it went straight to `@CardSelect=`).
- `@GameKick=/Kick=/FN=/QN=` (0x41): popped by the reservation poll 0xA92D5
  (0x843A0); carries the kicker's names. No client send requests a kick.
- 0xB2 `@Select=/A=` (cmd 7): shop-scoped (checks the header's shop byte);
  polled by 0xFD220 only while a match context exists. Trigger unmeasured.
- `@ZoneList`: a debug/test-command path (0x1154E0 prints "ans0 = %s"), not
  a server message.
- the EVENT family, mapped off the client's event loader: the
  loader reads `b/g/TM0EventDataList`, sends `@EventTimeReq=` (0xD1), polls
  `@EventTimeReqCheck`, then waits for `@EventEn=/Ans=` (0xD0); `@ETelop`
  (0xD7, `/ID= /Lp= /Pg= /St=`) is the ticker; 0xD8 is a presence flag; the
  game half (`@EventGameInit` / `@EventCard` / `@EventResult`) shares the
  match arms. The scene has NO client entry in the US retail build
  (posting its command is a measured no-op), so it stays
  unbuilt. THE EVENT THE WIKI DOCUMENTS IS BUILT INSTEAD: the champion's
  `rank_1 Pack` in the ordinary Card Shop -- `@Init=/SN=<name>` (arm
  0x105201 formats "%s's Pack" for PackPrm record 5) and the save header's
  +0x104 name slot, both fed by `tm-champion.json` from the Sunday publish.
  See `_champion_name`.
- (0xA2, 24) `@Init` the trade-board opener is served from responders.py.

The client's send vocabulary is CLOSED at 52 `@Key=` literals (read off the
binary, fully enumerated), so the lists above are finite and known -- not a search.

What we know
------------
- Tetra Master was an online-only card game; the client is the PlayOnline Viewer's
  TM content (content id 2, name "TetraMaster"; see contentlist.py / pol-games-menu).
- The world address is handed to the client by the LOBBY (pp000), which dials it
  directly by raw IP (the POL-0008 boundary). In our stack OUR lobby emits that
  handoff -> POL_WORLD_IP:POL_WORLD_PORT, so the client dials our world stub.
- Session crypto: OPEN. The lobby/world MAY reuse the login session key (K=0 in our
  token0 flow) or re-key. The harness defaults to K=0 and is overridable, exactly
  like the lobby stub -- confirm from the first captured world frames.

Where the protocol RE comes from (PC-first; PS2 ELF is static-only, no DNAS)
---------------------------------------------------------------------------
- PC: TM.dll is a separate x86 rebuild, ASProtect-packed (see janhourou-format /
  pol-games-menu). Dynamic capture via this harness is the near-term route.
- PS2: the TM logic is in the Viewer monolith `SLPS_202.00`
  (unpacked MIPS); *reading* it touches no DNAS. Cross-index
  the PS2 name map for the TM module/handlers. This is the cleanest
  static reference for the match/board/turn message set.

Vestigial scaffolding -- do NOT read it as a status signal
----------------------------------------------------------
`frame()` / `parse()` / `build()` near the end of this file raise
`NotImplementedError` and have **zero callers** (verified 2026-08-26: no
`.frame(`/`.parse(`/`.build(` call site exists anywhere in `pol-server/`). They
are leftovers from the original capture-harness design. The live path is
`_handle_line` plus the per-command builders above it. They are kept only so a
stray caller fails loudly instead of sending fabricated bytes -- their existence
is not evidence that anything is unreversed.
"""
import inspect
import json
import os
import random
import shutil
import sys
import tempfile
import threading

#: WARNING: THIS MODULE'S DIAGNOSTICS WERE INVISIBLE IN A CONTAINER, and that is
#: why it exists. `responders.log()` prints with `flush=True`; a bare `print()`
#: does not, and a container's stdout is a PIPE, so Python block-buffers it.
#: Every "tm: ..." line in this file -- the money moves, the guild persist, the
#: save patches -- sat in a 8 KB buffer that a long-lived server never fills.
#: Measured 2026-08-20: `docker logs | grep "^tm:"` returned **0** across 4,000
#: lines while the handler that writes them was demonstrably running.
#:
#: WARNING: THAT IS RULE 5 FAILING SILENTLY. "The fix did not run" and "the fix ran
#: and logged nothing" are indistinguishable when the log cannot leave the
#: process, and this project has lost days to exactly that distinction.
#: `_say()` is defined FURTHER DOWN (search `def _say(*args`). A second, broken
#: definition used to sit right here and was removed 2026-08-26: its body called
#: `_say(msg, flush=True)` against its own one-positional-arg signature, so it
#: raised `TypeError` instead of logging -- and the `except UnicodeEncodeError`
#: wrapped around it could not catch that. It was harmless only by accident,
#: because the later definition rebinds the name at module scope; any
#: module-level call reaching it first would have raised. (Checked before
#: removal: no module-level `_say(` call precedes the real definition.)


import tm_cardprm
#: The board, the arrows and the battle arithmetic -- see `services/tmbattle.py`.
#: NOT optional: a match that cannot resolve a battle stops dead on (0x43, 12),
#: which is exactly the wall this module spent a long round in front of.
import tmbattle
try:
    # The auction's store, for what Check Out is owed. Optional so an import
    # failure degrades to "nothing to collect" -- an empty Check Out is a
    # legitimate state and a wrong one is a fabricated sale, which this screen
    # has already caused once.
    import tmauction
except ImportError:                                          # pragma: no cover
    tmauction = None
try:
    # The save writer. Optional so an import failure degrades to record-only
    # (what this module did before delivery was solved) rather than taking the
    # whole title down -- but it is a sibling in services/ and is mounted, so a
    # miss here means something is wrong with the image, and it says so.
    import tmsave
except ImportError as _e:                                    # pragma: no cover
    tmsave = None
    _say("tm: tmsave unavailable (%s) -- collections will be RECORDED but not "
          "delivered; every launch will start empty" % _e)
try:
    # PRIZE POINTS: the second currency, the weekly lucky cards, and the
    # `@ExcInit=` body the Prize Center opens on. Optional for the same reason
    # tmsave is -- a missing sibling must cost the Prize Center, not the title.
    import tmprize
except ImportError as _e:                                    # pragma: no cover
    tmprize = None
    _say("tm: tmprize unavailable (%s) -- the Prize Center will fall back to "
          "the CARD SHOP's opener and no lucky card will ever be counted" % _e)
try:
    # CARD ROLL + GROWTH: a card is rolled below its CardPrm ceiling at
    # acquisition and grows toward it through use. Optional for the same reason
    # as the siblings above -- a missing module must cost progression, not the
    # title (cards then fall back to nominal = the old all-maxed behaviour).
    import tmroll
except ImportError as _e:                                    # pragma: no cover
    tmroll = None
    _say("tm: tmroll unavailable (%s) -- cards will serve NOMINAL (all-maxed) "
          "stats; no roll or growth" % _e)
import re
import struct
import time
from enum import Enum

CONTENT_ID = 2
CONTENT_NAME = "TetraMaster"


# --- TM0 game-channel text protocol -----------------------------------------
#
# REVERSED 2026-08-13 from the decrypted PS2 module `TMaster.pex` (base
# 0x00280000), corroborated by the live capture in logs/authserv.log. Tetra
# Master rides the SAME auth-band IRC envelope Janhourou uses (see
# jan-world-protocol / responders._game_notice_reply):
#
#     NOTICE <peer> :G TM0 G <E-body> <4-char checksum> CRLF
#
# The class char after the 3-char tag is 'G' (same as Janhourou's binary class),
# but the body is NOT janwire's 'B' record -- it is TM's own text format:
#
#     <8 hex chars> <command...>
#     ^ message code, a u32 serialized LITTLE-ENDIAN then hex (upper). The
#       serializer is TMaster.pex 0x0029bd70 (nibble-hex via table 0x0043cd30);
#       the request builder at 0x00298b30 passes code 225 (0xE1), which is why
#       the captured opener reads `E1000000@TeachDV=`.
#
# THE OPENER -- "retrieving default data":
#   request  code 225  `E1000000@TeachDV=`     builder  TMaster.pex 0x00298b30
#   reply    code 226  `E2000000@TeachDVAns/D=<d>/V=<v>`   parser  0x00298dc0
#
# The parser (0x00298dc0) requires the message to present code 226 to the
# message reader (0x0029a880), then strstr's `@TeachDVAns` and extracts the
# integer fields `/D=` (stored as a BYTE at 0x005b7ca8) and `/V=` (a HALFWORD at
# 0x005b7cb8). Both are initialised to the sentinel 255 ("unknown") at
# 0x00298814 and overwritten by our answer. The client then uses D and V to
# version-check the default card/rule data files it reads over the file-transfer
# service (`sqMgCpReadFile` at 0x003aad50, filenames `b/g/TM0SML`, `b/g/TM0SML2`,
# `b/g/TM0IML` -- see 0x004864a8). Answering TeachDV clears the FIRST timeout
# ("trm-0-37312 timed out when retrieving default data"); the file read is the
# next layer and will now surface in the logs.

# --- THE SEQUENCE, as of 2026-08-15. Each step measured, not inferred. -------
#
#   @TeachDV=            -> @TeachDVAns/D=0/V=0        (D/V are read PARAMETERS,
#                                                       not a client switch; the
#                                                       "non-zero unlocks a fetch"
#                                                       hypothesis is REFUTED)
#   b/g/TM0IML           -> a manifest with count=1, entry[0].flag=1 and a
#                           NON-ZERO entry[0].u64. All three are required: a zero
#                           u64 never produced @Init. Proven by a sentinel u64
#                           surfacing at [0x005d15e0], which 0x004239cc writes
#                           only on a flag match.
#   @Init=/NN=.../GID=.. -> @InitAns/Ans=<positive>    (code 129; parser
#                                                       0x003a9240; caller blez's
#                                                       the returned /Ans=)
#   class P <CR>...      -> ??? THE CURRENT WALL
#
# THE WALL, named from the screen and from the module:
#
#   error 0-37246 = "player data timed out while SAVING"
#   (プレイヤーのデータを保存中にタイムアウトしました)
#
# So `<CR>` on the POLpro P channel is the player-profile SAVE, not a read. It
# carries GID, the content id, and card state ("Card Level 0"). The API is
# sqMgPfcSetCharacterProfile / ...Check; the Check is 0x003aa8f0 -> 0x00417518,
# which polls a state object at **0x004755e0**: `+0xb8` is the in-flight flag
# (0 = never started -> -8704 "pfc.c:424", 1 = still pending) and `+0xbc` is the
# result. Both are readable from a savestate -- that is the anchor for the next
# reading.
#
# Our reply is currently the polpro `*` wildcard, echo + <OK>(0). It goes out
# (confirmed: `polpro P ->` in authserv.log) and the client then sends NOTHING
# and times out. **This is the SAME failure Janhourou has on `<PG>`**
# (the Janhourou save-data reading: "our echo+<OK> reply reaches the sqMg gateway but
# does not satisfy it"). One protocol, one bug, now with two independent test
# cases -- worth solving jointly with whoever holds Jan.
#
# WARNING: THE ON-SCREEN NUMBER STILL CANNOT BE DECODED FROM THE 396-RECORD TABLE at
# 0x0072f0f4. 37088 and 37312 are in it; the codes actually displayed -- 37246
# and 37092 -- are not. Do not treat that table as the error list. The screen
# text is the reliable decode, and a screenshot gets it in one step.

# --- 2026-08-15 LIVE RESULT: <PR> and <CI> BOTH REJECTED. It is not the code. --
#
# WARNING: SUPERSEDED -- IT *WAS* THE CODE. See "THE ANSWER" at the end of this block:
# the id<->code table was misread by one, so <PR> and <CI> were never the codes
# this section believes they were. The live measurements below all stand; only
# the names attached to the numbers were wrong.
#
# Two replies tried against the real client, both delivered (confirmed
# `polpro P ->` in authserv.log), both leaving the pool state UNCHANGED at 1:
#
#   <PR>(0)   wrong sibling -- 82 is the PROFILE handler, and the savestate
#             showed profile state [+0xb8]=0 (never started) while pool
#             [+0xb0]=1 (pending). My error.
#   <CI>(0)   the POOL handler's own success code (75), read off
#             pfcCharaPoolResult 0x004175c8. Still [+0xb0]=1 afterwards.
#
# The code model itself checks out: 0x0040bd30 -- whose return the handler
# compares against 75/76 -- does read the 102-code table at 0x0045eb04, so it is
# returning a code id as assumed. The handler simply never took its branch.
#
# SO THE GAP IS DISPATCH, NOT CONTENT. Nothing has established HOW a class-P
# reply reaches pfcCharaPoolResult at all -- whether a callback is registered per
# request, whether the reply must carry a transaction/sequence field we are not
# echoing, or whether the target nick in the NOTICE matters (each TM0 message so
# far used a DIFFERENT peer id: TeachDV USH6MZJA7, @Init UOADRAHW1, <CR>
# UUIRT71SR). That is the next thing to reverse, and it is offline work.
#
# DO NOT try a third code without it. Three console launches have now been spent
# on reply CONTENT, and the pool state has never once moved off 1 -- which is the
# signature of a reply that is not being consumed at all, not of a wrong value.

# --- 2026-08-15: FOUR THEORIES TRIED, ALL REFUTED BY MEASUREMENT. STOP HERE. --
#
#   <OK>  (the polpro wildcard)  -> maps to type 44, served by no pfc handler
#   <PR>  (82)                   -> type 36 = the PROFILE handler, whose state is
#                                   0, so it errors -8704. Wrong sibling.
#   <CI>  (75)                   -> type 33 = the POOL handler. CORRECT code,
#                                   correct route. No effect.
#   <CI> delayed 1500 ms         -> no effect either.
#
# The delay tested a real race read off the client: 0x00417254 sets the pending
# flag AFTER the send returns, and pfcCharaPoolResult drops anything arriving
# while state != 1. The mechanism is real; it is not what is happening here.
# [0x004755e0+0xb0] stayed 1 across both runs.
#
# WARNING: AND A CORRECTION TO THE EARLIER CLAIM. The earlier reading said the reply was "delivered and
# consumed" because its bytes sit in the client's message ring at 0x0045a630.
# That was wrong. The ring reads **read == write == 3** with our `<CI>` sitting in
# slot 3 -- i.e. slot 3 is the NEXT slot to write, so the entry is NOT committed.
# Entries 0..2 (which include @TeachDVAns and @InitAns, both of which worked) were
# written AND committed. So our reply is staged and then backed out.
#
# The consumer does exactly that: on a gate failure it branches to 0x00417080 and
# the path through 0x00417078 writes the WRITE index back (`sw v0, 0xa4(s3)`),
# compacting the rejected entry out. Which means a gate DID fail --
#   gate 1  0x0040e020 : byte at msg+0x1d must be 'G'
#   gate 2  0x00418238 : byte at msg+0x21 must be 'P'
# -- even though the ring dump shows our entry as `.....GTM0P<CI>.0`, i.e. 'G' at
# +0x1d and 'P' at +0x21, which should pass both. That contradiction is the whole
# remaining question and it could not be resolved here.
#
# NEXT PERSON: do not try a fifth reply code. Work out why a gate rejects an entry
# that visibly satisfies both byte checks -- most likely the gates read a
# DIFFERENT buffer than the ring slot we are reading (the staging buffer, at a
# different offset), so the bytes we are inspecting are not the bytes being
# tested. Find the producer that fills the slot and confirm the offsets against
# a message that DID commit (@InitAns, entry[2]) rather than against ours.

# --- THE ANSWER, 2026-08-15: THE CODE TABLE WAS READ OFF BY ONE. -------------
#
# No gate ever rejected anything. The reply was accepted, dequeued, and thrown
# away because `<CI>` is not the code the handler wants -- `<CS>` is.
#
# THE MISREAD. The 102-code table is 8-byte records **[u32 id][2-char code][2
# pad]** -- id FIRST. The notes above read it as {code, pad, id} and so paired
# every code with the id of the record BELOW it. Every code we derived was +1:
#
#     believed        actual
#     CI = 75         CI = 74   (75 is CS)
#     PR = 82         PR = 81   (82 is GK)
#     OK = 89         OK = 88   (89 is OG)
#
# Read off the code three independent ways that agree -- not off a hex dump:
#   1. RESOLVER  0x0040bd30 walks the codes at 0x0045eb04 + i*8 and returns the
#      id with `lw v0, -4(v1)`, i.e. from code_ptr MINUS 4.
#   2. BUILDER   Janhourou's group builder 0x002f32e0 materialises the code
#      string as `0x003f146c + id*8` -- the same pairing, from the other side.
#   3. WIRE      TMaster's own request builder passes 73 (0x004171d4) and 74
#      (0x004171e4) for the two groups the captured request shows as <AN><CI>.
#
# And `polpro.py`'s TAGS/TAG_ID was GENERATED from the module and had CI=74 /
# CS=75 correct the whole time. The bug lived only in prose. Trust TAG_ID.
#
# WHY EVERY ATTEMPT FAILED, all four from the one cause. The type step
# (0x00418188) maps id-72 through a sparse 26-entry jump table at 0x0048da40;
# ids with no arm get no type and are dropped:
#
#     CI 74 -> NO ARM        consumed and discarded  <-- what we kept sending
#     PR 81 -> NO ARM        consumed and discarded
#     OK 88 -> type 43       a real handler, but not the pool one
#     CS 75 -> type 33 -> pfcCharaPoolResult 0x004175b8, whose 75 branch does
#                         [0x004755e0+0xb4] = old state; [+0xb0] = 2
#                         which IS the completion the poll is waiting for.
#
# THE CHAIN CLOSES, verified offline end to end. `sqMgPfcSetCharacterPoolCheck`
# (0x00417290 -- the POOL sibling; 0x00417518 is the PROFILE one and reads
# +0xb8/+0xbc, do not confuse them) polls exactly the two fields the handler
# writes:
#     s1 = [+0xb0]  state      s0 = [+0xb4]  result
#     s1 == 0  -> -8703 at pfc.c:286  "never started"
#     s1 == 1  -> return 0            still pending  <-- every run so far
#     s0 >= 0  -> return s0           SUCCESS
# Our <CS> leaves state 2 and result 1, so the poll returns 1 and the caller's
# blez takes the success path. Nothing after this is inferred.
#
# THE RING READING WAS WRONG TOO, and this is the reusable part. `read == write`
# does NOT mean "staged and backed out" -- it means the entry WAS dequeued:
#   * consume path: compacts the following entries down and DECREMENTS write
#     (0x0041707c), so a consumed last entry leaves read == write with its bytes
#     still in the slot (nothing is cleared);
#   * reject path (0x00417080): advances only a LOCAL cursor and leaves write
#     ahead -- it never stores the write index at all.
# So the observed read == write == 3 with our bytes in slot 3 was proof the
# message was ACCEPTED. Both gates passed, as the bytes said they should. The
# contradiction that closed the earlier reading was an artefact of reading the
# reject path as the writer of [+0xa4].
#
# Also measured while settling this: the ring is ONE shared queue with FOUR
# pumps, one per POLpro class byte -- 0x0040ebc8 'L', 0x00411810 'A',
# 0x00416460 'R', 0x00416e78 'P' -- so nothing competes for a class-P reply.
# And gate 1 has a second half nobody had noticed: msg+0x1e..0x20 must equal the
# RUNTIME tag at 0x0045a618 ('TM0'), not just the 'G' at +0x1d.
#
# FIX: config/polpro.json now answers <CR> with <CS>. It is mtime-reloaded, so
# it is live with no restart. WATCH [0x004755e0+0xb0]: 1 = still waiting,
# 2 = satisfied. Anything else means read this block again, not a new code.
#
# The same off-by-one had reached Janhourou -- its deployed <PG> answer <PR> is
# id 81 with no arm. Its handler's constants are 82/85 = <GK>/<GG>; corrected in
# polpro.json and flagged to that worker in STATUS.

MSG_TEACHDV = 225        # request  (client -> us)
MSG_TEACHDVANS = 226     # reply    (us -> client)
MSG_INIT = 128           # request  `@Init=/NN=.../CN=.../GID=.../L=0`
MSG_INITANS = 129        # reply    `@InitAns/Ans=<n>`; parser 0x003a9240
MSG_SHREQ = 176          # request  `@ShReq=/NN=.../CN=...`  (Player Data -> Cards)
MSG_SHENTER = 177        # reply    `@ShEnter=/EN=<n>`; handler TM.dll 0x88E06
MSG_OPT = 132            # request  `@Opt=/NN=.../HNSS=<n>`; builder TM.dll 0x88390
MSG_OPTANS = 133         # reply    `@OptAns=/Ans=<n>`;      parser  TM.dll 0x889B0

#: THE CARD SHOP'S OPENING MESSAGE -- and the reason every earlier attempt at it
#: looked like silence. Measured 2026-08-16 in the unpacked `TM.dll`.
#:
#: **THE 8-HEX HEADER CARRIES TWO FIELDS, NOT ONE.** The message store keeps each
#: line's text at `rec+0x25`, and `0xA9780(rec, off)` parses TWO ASCII hex chars
#: at `rec+0x25+off` into a byte. The matcher `0x81CB0` tests exactly two of them:
#:
#:     0xA9780(rec, 0) == code     <- header chars 0..1  (u32 byte 0)
#:     0xA9780(rec, 4) == msgid    <- header chars 4..5  (u32 byte 2)
#:
#: So a wait is on a (code, msgid) PAIR. Everything we have answered so far had
#: msgid 0 by accident, because `encode_code` packs a u32 and we only ever set
#: byte 0 -- which is why `encode_code(0x001300B2)` is all it takes to address
#: byte 2. Nothing new is needed on the wire.
#:
#: **This retires the earlier "0xB2 produced a screen indistinguishable from serving
#: nothing".** 0xB2 was never the wrong code; it was served with msgid 0 while
#: the client waits on msgid 0x13, so `0x81CB0` never matched it and the message
#: sat in the store unread.
#:
#: The waiter is `0x101A10(msgid)` -- a thiscall on `0x52463E0` that polls
#: (0x43, msgid), then (0xB2, msgid), then (0xA2, msgid), and dispatches through
#: three jump tables. **The card shop's three waits are the only `0x101A10` call
#: sites inside the shop module** (rva 0x117000..0x11C000):
#:
#:     0x11B5A9  msgid 0x13  -> arm 0x105201, logs `---->Recv=SHOPINIT`
#:     0x11A64E  msgid 0x14  -> arm 0x105E85, right after the screen prints
#:                              Cardsh.BIN record 62 `Transaction in progress...`
#:     0x11C012  msgid 0x17  -> arm 0x105A97
#:
#: and each resolves under code 0xB2 (their 0x43 table entries are the
#: unhandled-default target, 0x106CCD).
#:
#: Arm 0x105201 parses the body -- the key `@Init` (a map key, so it needs the
#: `=`, per the PC delimiter note above), then six integer fields:
#:
#:     /N=   -> byte at obj+0x30      /RA=  -> dword at obj+0x04
#:     /M=   -> ...                   /CP=  -> ...
#:     /S=   -> word at obj+0x00      /SP=  -> word at obj+0xEC
#:
#: WARNING: THE FIELD NAMES AND THE ORDER ARE MEASURED; THE VALUES ARE NOT. They are all
#: env-overridable so the meanings can be A/B'd without a rebuild. `/M=` is very
#: likely Money (Cardsh.BIN has `Money`, `Average Rank`, `Card Level`, `Page`),
#: and it defaults to a probe value rather than 0 so a pack is affordable and the
#: msgid-0x14 transaction path can be exercised on the same launch.
MSG_SHOPINIT = 0x001300B2   # code 0xB2 + msgid 0x13, packed for `encode_code`
#: The buy reply's own header, for pushing `@Card=` unsolicited. Taken from the
#: live wire (`B2001401`), so the trailing 0x01 is the SHOP NUMBER the handler's
#: second equality gate compares -- not padding.
MSG_SHOPCARD = 0x011400B2
MSG_CVREQ = 0x70         # request  `@CvReq=`  -- THE PRIZE CENTER, inside Rankings
MSG_CVENTER = 0x71       # reply    `@CvEnter=/EN=<n>`; parser TM.dll 0x8B490
MSG_CARDREQ = 0x90       # request  `@CardReq=`   (builder TM.dll 0x8B7C0)
MSG_CARDENT = 0x91       # reply    `@CardEnt=/Ans=<n>`; handler 0x8BA1E

#: THE EVENT CHAIN. All four are read off TM.dll and none has ever been seen on a
#: wire -- the event branch has never opened here, so
#: these are a specification, not a measurement. The two
#: that matter are that `@EInitReq=` is what the Event Shop's card machine sends
#: (0x9A1C8) and `@EventTimeReq=` is what BOTH event pumps block on.
#:
#: `@EventTimeReq=` is the one message in this family whose reply carries a msgid
#: rather than only a code: the sender registers its wait as `(0xD1, 5)` at
#: 0x8C1B6 and then sends `(0xD1, 4)` at 0x8C1CF, so the answer has to be packed
#: with msgid 5 or the client never sees it.
MSG_EVENTTIMEREQ = 0x000400D1   # request  `@EventTimeReq=/NN=...`  builder 0x8C190
MSG_EVENTTIMEANS = 0x000500D1   # reply    `@EventTimeReqCheck=/Start=../End=../Now=..`
MSG_EINITREQ = 0x98      # request  `@EInitReq=`   (builder TM.dll 0x8BBD8)
MSG_EINITENT = 0x99      # reply    `@EInitEnt=/Card=<n>/Ans=<n>`; handler 0x8BE34

#: THE TABLE'S MEMBER LIST -- the first message of the MATCH family (0x41) that
#: we can answer, and the first place the msgid rather than the code carries the
#: request/reply direction.
#:
#: Measured live 2026-08-17: standing at a table in #TM0R001 and choosing to view
#: the table's members sends
#:
#:     GTM0G41000300@GameM=/ID=AB12CEB20D9067C4
#:
#: -- code 0x41, **msgid 3** -- and the screen hangs, because `TM0: no answer for
#: this command`. An earlier reading took the family's convention as "reply = request code + 1",
#: which is right for 0xE1/0x80/0xB0/0x90 but WRONG here: the sender at TM.dll
#: `0x83730` builds `(@GameM=, code 0x41, msgid 3)` and the waiter three
#: instructions into the same scene's poll, `0x827F0(buf, 0x41, 4)`, looks for
#: **code 0x41 with msgid 4**. The code stays put and the msgid steps.
#:
#: THE REPLY, field for field, off the parser at `0x83870`:
#:
#:     0xAA920(msg, "@GameML")           the command name -- no '=' on this one
#:     0xAB470("/ID=", msg, 0, &u64)     must EQUAL our own player id, i.e. the
#:                                       `/ID=` the client just sent, or 0x838E3
#:                                       drops the whole message
#:     0xAB080("/Num=", msg, 0, &n)      the row count -> [obj+0xA8]
#:     for i in 0..n-1, each by OCCURRENCE INDEX, into a 0x38-byte row:
#:         /MLID=  u64    row+0x00      0xAB080 pair at 0x839A0/0x839AF
#:         /MLNN=  string row+0x08      0xAAD60, copied at 0x83975
#:         /Po=    int    row+0x18
#:         /Lv=    int    row+0x1C
#:         /Rk=    int    row+0x20
#:         /HN=    string row+0x24      0xAAD60, copied at 0x83A20
#:
#: Four numbers per row against a Member Info panel that shows VS. Rating, Title,
#: Card Level and Guild is a strong hint at the mapping, but it is a HINT: none of
#: the four has been watched on screen yet. `POL_TM_GAMEML_ROWS=1` serves one
#: synthetic row to settle it; the default is 0, which is the honest answer for
#: the empty table this was measured at and cannot mislead a later state machine
#: into thinking somebody is seated.
MSG_GAMEM = 0x00030041   # request  `@GameM=/ID=<u64 hex>`  (builder TM.dll 0x83730)
MSG_GAMEML = 0x00040041  # reply    `@GameML=/ID=.../Num=<n>...`; parser 0x83870
#: THE RESERVATION-LIST reply, same @GameML body at MSGID 0xC. The client has
#: TWO consumers of @GameML: the members parser 0x83830 reads only msgid 4 and
#: fills the members pane (+0x10c); the RESERVATION parser 0x83B80 reads msgid 4
#: FIRST but msgid **0xC** as its dedicated fallback and fills +0x108 -- the
#: count the Start-Game gate 0x5C0D0 requires (>= 2). The members branch runs
#: first (0xA918F before 0xA940C) and the msgid-4 reply never reaches +0x108
#: (proven by a live owner dump 2026-08-21: +0x10c=2 but +0x108=0 with the
#: reservation arm +0x41c set). msgid 0xC is the channel members never touches,
#: so a copy there fills the reservation slot without disturbing the pane.
#: The reservation parser has NO /ID gate (0x83B80 pushes no `/ID=`), so this
#: rides the SAME body; /MLID must be the POL-ID (same id-space as the room
#: member list [0x51DF120] the gate looks each row up in).
MSG_GAMEML_RESV = 0x000C0041


def _resv_gameml_code():
    """The code+msgid to carry the reservation @GameML re-feed.

    WARNING: ROOT-CAUSE CORRECTION 2026-08-22 (STATIC, UNPROVEN LIVE -- disasm of the
    dispatcher poll at 0xA9180, base 0x04F90000). The msgid-0xC channel CANNOT
    fill the owner's reservation slot +0x108, by construction:

      * The per-frame poll runs three arms in order:
          0xA918F  MEMBERS   (gated +0x414): 0x83830 POPS (0x41, msgid 4) -> +0x10c
          0xA92D5  @GameKick (gated on the selected-table id [0x52461d0:d4]):
                   0x84360 POPS (0x41, msgid 0xC) -- store lookups 0x827F0 are a
                   POP (0x81DB0 copies out then re-inits the slot), NOT a peek --
                   and only treats it as a kick if it says @GameKick; a @GameML
                   on 0xC is popped-then-discarded here.
          0xA940C  RESERV    (gated +0x41c): 0x83b80 reads (0x41,4) FIRST, then
                   (0x41,0xC); >0 -> copies blob into +0x108/+0x110.
      * For the OWNER of a reserved table the selected-table gate 0x52461d0:d4 is
        SET, so the @GameKick poll runs and eats the 0xC before the reservation
        arm can read it. msgid 0xC therefore only survives when that gate is 0
        (not at your own table) -- never for the owner.
      * The reservation arm's PRIMARY channel is msgid 4, which the kick poll
        never touches. It is contested only by the MEMBERS arm (+0x414), which is
        armed just for the frames right after "View Table Members" is clicked. On
        a plain @Pong re-feed +0x414 is NOT armed, so a msgid-4 @GameML survives
        the members arm and reaches the reservation arm -> +0x108 fills.

    This also explains the 2026-08-22 live finding (+0x10c filled, +0x108 stayed 0
    with +0x41c armed): the @GameM= reply arrived with +0x414 armed, the members
    arm popped the msgid-4 copy first, and both the members(4) and reservation(0xC)
    copies were then gone before the reservation arm ran.

    WARNING: NOT CONFIRMED LIVE. This is a disassembly hypothesis
    until +0x108 >= 2 and Start Game ungreys on a real owner's screen. Default is
    msgid 4 (the channel the kick poll cannot eat); POL_TM_RESV_MSGID=C restores
    the proven-broken 0xC for an A/B.
    """
    if (os.environ.get("POL_TM_RESV_MSGID", "4") or "4").strip().upper() == "C":
        return MSG_GAMEML_RESV
    return MSG_GAMEML

#: THE RESERVATION'S ANSWER, and the client never asks the question.
#:
#: Decompiled 2026-08-17 after four rounds of `b/g/PTL` experiments failed to
#: un-grey Reservation. The refusal a tester sees is `Lobby.BIN` 337, "You
#: cannot make a reservation with current table status", and it is raised at
#: EXACTLY ONE site in the whole module -- `0x4BD6F`, resolved by disassembling
#: backwards from every caller of the two message functions rather than scanning
#: for the id as an immediate (which produced three false positives: an
#: `new(424)` allocation, a colour component, and a table of card ids).
#:
#: That site shows 337 when `0x841E0` returns exactly 0, and `0x841E0` is:
#:
#:     0x84221  call 0x827F0(buf, 0x41, 6)   look for @GameQA in the message store
#:     0x8422A  jl   0x842B2                 not there -> fall to the tail
#:     0x8426A  push "/QT=" ; call 0xAB080   else read /QT= into [esp+0x10]
#:     0x84344  mov  eax, edi                edi = [esp+0x10], initialised to 0
#:
#: so it RETURNS THE `/QT=` VALUE out of an `@GameQA` message and returns 0 when
#: no such message exists. Its caller treats > 0 as success, < 0 as an error and
#: 0 as 337. The client has never sent `@GameQT=` in the entire log -- so this is
#: not a reply we are failing to give, it is an UNSOLICITED PUSH the server owes,
#: the same shape as SHOPINIT for the card shop and the Prize Center
#: where a scene likewise waits on a message it never requests.
#:
#: WARNING: UNPROVEN ON THE WIRE. The chain above is read, not measured: `@GameQA` has
#: never existed on this server, so "the client parses this" and "the client is
#: satisfied by this" remain different claims. `/QT=1` is the least-committal
#: value that is > 0; if the greying persists, the value is the next thing to
#: sweep, not the mechanism. `POL_TM_GAMEQA=0` disables it.
#: WARNING: AND IT IS NOT A PUSH AFTER ALL -- THE CLIENT DOES REQUEST IT (2026-08-18).
#: Everything above stays true about the CONSUMER; what was wrong was "unsolicited".
#: Once `@GameEN=` is answered (see `MSG_GAMEEA`) the client reaches the table
#: settings screen, and Confirm sends
#:
#:     GTM0G41000500@GameQT=
#:
#: -- code 0x41 **msgid 5**, no fields at all, from either of two builders
#: (`0x83FEF` and `0x84080`, both `0xA97C0("@GameQT=", 0x41, 5, 0, 0)`), each of
#: which drains `(0x41, 6)` immediately before sending. So `@GameQA` is an
#: ordinary reply on the msgid-steps-not-code rule, exactly like
#: `@GameM=`/`@GameML` and `@GameEN=`/`@GameEA`, and the whole reservation family
#: is now consistent.
#:
#: **Unanswered, this is the tester's "Reservations cannot be made with current
#: table restrictions"** -- `0x841E0` finds nothing, returns 0, and `0x4BD6F`
#: renders Lobby.BIN 337. Measured 2026-08-18 04:25:07Z: one `@GameQT=`, one
#: `TM0: no answer for this command`, and that dialog on screen.
#:
#: The push on `@GameM=` is KEPT. It costs nothing (the reply drains the slot,
#: and both carry the same value), and it is what put a `@GameQA` in the store
#: for the run that first sent `@GameEN=` -- so it may still matter to whatever
#: decides the button's greying, which is a different question from this dialog.
MSG_GAMEQA = 0x00060041  # push     `@GameQA=/QT=<n>`; consumer TM.dll 0x841E0

#: THE RESERVATION ITSELF -- `@GameEN=` finally arrives, and it is UNANSWERED.
#:
#: WARNING: THIS CLOSES THE EARLIER HARD FACT AND `MSG_GAMEQA`'s "the client never asks the
#: question". Both were true when written; they are not any more. Measured
#: 2026-08-18 off a second machine's shipped shim log
#: (`logs/uploads/FRAMEWORK-*.log`, `[tm] +821A1 <bf = 41000100@GameEN=...>`) and
#: the matching `authserv.log` lines -- twenty sends across the day, every one of
#: them met with `TM0: no answer for this command -- captured, silent`:
#:
#:     GTM0G41000100@GameEN=/NN=AB12CD56EB0F5932/HID=0/Dm=0/Vol=0
#:                          /CN=/HN=4C6170746F705465737432/L=1
#:
#: So Reservation is no longer greyed -- the unsolicited `@GameQA=/QT=1` above
#: did its job -- and the screen a tester is stuck on ("Retrieving table
#: info...", `tm-xlate` id 28) is the client polling for a reply that never comes.
#:
#: THE REPLY, off the waiter at TM.dll `0x83580` (the function the reservation
#: scene's poll calls; base 0x04F90000, so these are RVAs):
#:
#:     0x835C1  call 0x827F0(buf, 0x41, 2)    code 0x41, **msgid 2** -- and the
#:                                            sender at 0x832DE drains exactly
#:                                            that slot before it sends, which is
#:                                            the same msgid-steps-not-code rule
#:                                            `@GameM=`/`@GameML` follows
#:     0x835ED  0xAA920(msg, "@GameEA")       the command name, no '=' in the
#:                                            literal (as with `@GameML`)
#:     0x8360A  0xAB080("/EN=", msg, 0, &n)   the ONLY field the waiter reads
#:
#: and `/EN=` is a verdict, read three ways at `0x8361B`:
#:
#:     n  > 0   success -- global rva 0x298200 := 0xF, and the scene advances
#:              (`0x8F971 jle` is the failure branch, so > 0 is the whole gate)
#:     n == 0   the waiter substitutes **-99**, which `tm-errors.tsv` renders as
#:              37399 "Timed out while updating table data."
#:     n  < 0   passed through as that error code
#:
#: 1 is not an arbitrary pick above the threshold: `0x90114` compares the stored
#: value to **1** specifically and takes a different branch (posting 0x3000E79 or
#: 0x3000E7A) than any other positive, which reads as "you are the only entrant"
#: versus a countdown at 0x90145. So the number is a state, not a boolean, and
#: `POL_TM_GAMEEA_EN` exists to sweep it.
#:
#: WARNING: MEASURED OFF THE BINARY, NOT YET ON A WIRE. What is measured is that the
#: client asks and we say nothing; what follows a `/EN=1` has never been seen.
#: `POL_TM_GAMEEA=0` restores the silent control for an A/B.
MSG_GAMEEA = 0x00020041  # reply    `@GameEA=/EN=<n>`; waiter TM.dll 0x83580

#: VERIFIED: THE VS. COM SESSION'S FIRST REPLY, and the wall the gate fix uncovered.
#: With the VS. COM row finally reachable (room record `+0x29`, see
#: `tools/tmroomlist.py`), pressing it hangs on **"Preparing game"** and the
#: client re-sends this every ~2 s, measured live 2026-08-22:
#:
#:     NOTICE UGRAWNN3J :GTM0G41001200@GameENC=/NN=<guid>/HID=0/Dm=0/Vol=0
#:                       /CN=/HN=<name as hex ASCII>/L=1
#:
#: `@GameENC=` is the COM-game twin of `@GameEN=`: same field shape, code 0x41,
#: msgid **0x12** (vs 0x01), plus `/CN=`, `/HN=` and `/L=`. Note the PEER --
#: `UGRAWNN3J`, not the room peer -- which is the `'E'` row the picker at
#: `0x761F0` chose, and the first traffic that makes the shared `SERVICE_ID`
#: between our `'E'` and `'F'` rows matter.
#:
#: THE ANSWER IS `@GameECA`, and TM.dll's string pool at `0x2227B4` gives its
#: shape by neighbourhood -- the pool runs
#:     `@GameENC=` `@GameECA` `/TblId=` `/TblNo=` `@MuchMake` `/Ans=`
#:     `@MuchMakeAns=` `/Start=`
#: so `@GameECA` carries **`/TblId=` and `/TblNo=`** exactly as `@GameEA` carries
#: `/EN=`, and the session does NOT end here: `@MuchMake` follows.
#:
#: WARNING: THE MSGID IS INFERRED, NOT MEASURED. Every documented pair in this protocol
#: answers at request msgid + 1 (`@GameEN=` 0x01 -> `@GameEA` 0x02), so 0x12 ->
#: **0x13**. If the client ignores this reply, the msgid is the first thing to
#: sweep -- `POL_TM_GAMEECA_MSGID` does it without a redeploy. A wrong msgid is
#: indistinguishable from silence on this band.
MSG_GAMEECA = 0x00130041  # reply    `@GameECA=/TblId=<id>/TblNo=<n>`; see above

#: VERIFIED: THE JOIN HANDSHAKE, and the reply we have never sent. Decompiled
#: 2026-08-20 after live testing reported "after creating a reservation, the
#: table shows as setting up for everyone, but nothing else can be done".
#:
#: The client SENDS `@CheckJoinTable=` -- builder 0x86507,
#: `0xA97C0(buf, "@CheckJoinTable=", 0x2D, 0, ...)`, so code **0x2D** msgid 0 --
#: and waits on `0x825A0(buf, 0x2E, -1)`: code **0x2E**, the request+1 convention
#: this family follows everywhere (0xE1->0xE2, 0x80->0x81, 0x2D->0x2E). Its
#: name literal is `@CheckJoinTable_Ans` with NO trailing `=`, exactly like
#: `@GameML` and `@GameEA`; the `=` still goes on the wire (the family's delimiter).
#:
#: The parser at 0x865DA reads TWO integer fields and packs them into one word:
#:
#:     0x86601  0xAB080("/Join=", ..)   ->  result |= Join
#:     0x86620  0xAB080("/Mem=",  ..)   ->  result |= Mem << 1     (`lea eax,[edx+edx]`)
#:
#: so each is a BOOLEAN, `/Join=` at bit 0 and `/Mem=` at bit 1. The string pool
#: puts the pair immediately after `sqMgCpEnterTable` and `CpEnterTableCheck`,
#: which is what this is: the check a client runs before entering a table.
#:
#: WARNING: THE BIT MEANINGS ARE NOT MEASURED -- only their positions are. `1/1` is
#: the least-refusing answer and is what `POL_TM_JOIN` / `POL_TM_JOIN_MEM` exist
#: to sweep. What IS established is that an unanswered request is what kills this
#: client (see `@Quit=`), and this one has never been answered at all.
MSG_CHECKJOIN = 0x2D     # request  `@CheckJoinTable=`  (builder TM.dll 0x86507)
MSG_CHECKJOINANS = 0x2E  # reply    `@CheckJoinTable_Ans=/Join=<b>/Mem=<b>`

#: THE CARD-SERVICE ENDPOINT, found while chasing persistence and implemented
#: because an UNANSWERED request is what kills this client -- see `@Quit`.
#:
#: Same shape as `@ShReq`/`@ShEnter`: the handler at 0x8BA1E finds the key
#: `@CardEnt`, reads `/Ans=` and requires it **> 0** (`jle` skips), then stores
#: OUR message's sender id into `[0x5242958/0x524295C]` -- `tm_send`'s fallback
#: destination. So this teaches the client where to address card traffic.
#: The sender (0x8B7C0) drains codes 0x41, 0x42, 0x91 and 0xD0 first, which is
#: how the reply code 0x91 was identified rather than guessed.
#:
#: WARNING: NOT YET OBSERVED ON THE WIRE. `@CardReq=` has never appeared in
#: authserv.log, so what triggers it is unknown; this is here so that if the
#: client ever does ask, it gets an answer instead of hanging. Everything in the
#: reply is measured off the handler.

#: THE PURCHASE. `@Buy=/No=<pack>` arrives as `B2001401` -- **the client sets the
#: msgid field itself**, which is the live proof of the header layout above:
#: chars 0..1 `B2` = code, chars 4..5 `14` = msgid, chars 6..7 `01` = the shop
#: number, which is the `/N=` we sent in SHOPINIT.
#:
#: The answer is arm 0x105E85, `---->Recv=SHOPBUY`. Its gate is three equalities
#: before it will even log:
#:
#:     0xAA920("@Card")                       key must be present  -> `@Card=`
#:     0x10B6B0 = msg[+0x08]/[+0x0C]  == shop[+0x28]/[+0x2C]   sender id
#:     0x10B6C0 = msg[+0x3D0]         == shop[+0x30]           the `/N=` byte
#:
#: Both are satisfied for free by ECHOING THE REQUEST'S 8-HEX HEADER: same
#: service peer, same shop number. That is why `_shopbuy_body` reuses it rather
#: than rebuilding one.
#:
#: Then `/S=` = how many cards were drawn (must be > 0, else 0x105878), and for
#: each i in 0..S-1 a key `@N<i>=` carrying EIGHT `/D=` values read by index:
#:
#:     idx 0 -> word [rec+0x06]  CARD NUMBER, must be < the CardPrm.BIN table
#:                               (250 records), and becomes texture `cad%05d`
#:     idx 1 -> byte [rec+0x00]
#:     idx 2 -> byte [rec+0x01]  **must be < 4** -- the four Tetra Master card
#:                               types (P / M / X / A)
#:     idx 3 -> byte [rec+0x02]      idx 4 -> byte [rec+0x03]
#:     idx 5 -> byte [rec+0x04]      idx 6 -> byte [rec+0x05]
#:     idx 7 -> byte [rec+0x0E]
#:
#: A card that fails the number or type check is discarded (0x106093 ->
#: 0xB86B0) instead of added, so a bad value costs one card, not the screen.
#:
#: WARNING: Only the two RANGE checks are measured. Which of the remaining bytes is
#: attack / defence / arrows is NOT -- they go straight into the record and are
#: read back by the card renderer. `POL_TM_SHOP_CARDS` overrides the whole list
#: as `no:d1:d2:d3:d4:d5:d6:d7`, comma-separated, so it can be swept live.
CARDPRM_RECORDS = 250       # data/CardPrm.BIN slot count = the card-number ceiling

#: WARNING: MULTI-VALUE FIELDS ARE PIPE-SEPARATED, NOT REPEATED. `0xAB080(name, dest,
#: index, out)` finds the field, takes its value up to the next `/` or `@`
#: (char set `"/@"` at 0x51B3950), and then walks `index` occurrences of the
#: SEPARATOR `"|"` (0x51B2B78) INSIDE that one value. So the card block is
#:
#:     @N0=/D=<id>|<atk>|<type>|<pdef>|<mdef>|<b4>|<b5>|<b6>
#:
#: and NOT eight `/D=` fields in a row.
#:
#: THIS WAS THE BUG BEHIND "the client only accepts card ids 0..3", and it cost
#: four live purchases and two wrong theories (degenerate stats, a four-slot
#: cap). Writing `/D=a/D=b/D=c...` made every indexed read fall back to the
#: FIRST value -- the card id -- so the type check `cmp idx2, 4 ; jge discard`
#: was being applied to the CARD ID. Ids 0..3 passed it and everything else was
#: silently dropped, which looked exactly like an id ceiling of 4.
#:
#: The live dump killed that theory for good: `[0x528F4FC]` reads **250** and
#: the CardPrm rows in memory match the file byte for byte, so the id gate was
#: never the one firing. Measure before theorising (a live card-state probe).

#: LEAVING THE SHOP -- and NOT answering this is what dropped both clients.
#: Live 2026-08-16: after a purchase the player backs out, the client sends
#: `@Quit=/No=1` as `B2001701` (code 0xB2, **msgid 0x17**, shop 1) and blocks in
#: `0x101A10(0x17)`. We were silent, it waited, and ~75 s later the session went
#: `resumed channel closed` and the game errored out. The shop LOOKED finished
#: because the purchase had already succeeded; the failure is on the way out.
#:
#: Arm 0x105A97 handles msgid 0x17 and accepts two keys:
#:   `@Event`  -- the featured-pack event (Cardsh.BIN 71, "celebrate X's #1 rank
#:                by selling X's Pack"); fields /N= /RA= /M= /CP= /S= /RT=.
#:   `@EQuit`  -- 0x105BA3, the plain exit. Same two equality gates as SHOPBUY
#:                (sender id vs shop[+0x28/+0x2C], msg[+0x3D0] vs shop[+0x30]),
#:                logs `---->Recv=SHOPQUIT`, then reads ONE field: `/D=`, which
#:                **must be 0** -- 0x105C29 `cmp eax,ebx ; jne 0x105878` takes
#:                the failure path on anything else.
#: Echoing the request header satisfies both gates, as with SHOPBUY.

#: THE CARD SHOP ENTRY, and it is the same destination-id mechanism as `/Shm=`.
#: Measured 2026-08-16 in the unpacked `TM.dll`; the screen errors out with no
#: reply at all, which is what silence looks like from the client side.
#:
#:   handler 0x88E06:  find("@ShEnter")                <- a map KEY, so `=`
#:                     tm_field_get_int("/EN=", v, 0, &n)   <- the ONLY field
#:                     if (n > 0):
#:                         [0x5242958] = sender id lo
#:                         [0x524295c] = sender id hi
#:
#: `0x5242958/5c` is `tm_send`'s FALLBACK destination (0x820EC, the third branch
#: of the gate that silently ate `@Init=`). So
#: this exchange is how the client learns which peer to address card-shop traffic
#: to, and it takes the id from the SENDER of our reply -- nothing for us to put
#: in a field.
#:
#: WARNING: THE CODE IS 0xB1 (177), AND 0xB2 IS A DIFFERENT MESSAGE. The first cut served
#: 0xB2 by inferring it from `push 0xb2` at the send site; the client ignored it
#: and the screen sat loading, exactly as it does with no reply at all. Reading
#: the handler settled it -- it polls TWO codes in order:
#:
#:   0x88D95  push 0xb2 ; push 0x13 ; call 0x827f0   <- tried FIRST. If THAT is
#:            found it sets [0x5242958/5c] straight from the message and never
#:            looks at @ShEnter, so 0xB2 carries no text body.
#:   0x88DD4  push 0xb1 ; call 0x825a0               <- then the store lookup
#:            whose record is searched for "@ShEnter".
#:
#: So `@ShEnter=` rides on **0xB1**; the `push 0xb2` at the send site was this
#: first poll, not a drain. `POL_TM_SH_CODE` remains for A/B-ing 0xB2 (which
#: would take the other branch and may be the "shop ready" push).
def _sh_enabled():
    return os.environ.get("POL_TM_SH", "1") == "1"


def _say(*args, **kwargs):
    """`print()`, but FLUSHED -- and that word is the whole point of this.

    WARNING: EVERY DIAGNOSTIC THIS MODULE EMITS WAS INVISIBLE UNTIL 2026-08-20. The
    `authsess` container does NOT set `PYTHONUNBUFFERED` (the `jan` service does,
    which is why nobody noticed), so a bare `print()` from here sat in a block
    buffer and effectively never reached `docker logs` at all. `log("authserv")`
    lines were fine, so the log looked healthy while this module was mute.

    It cost three wrong diagnoses in one night: "the @Opt= writer never fires"
    (it fires -- nothing had moved), "member_id is None" (it is not), and
    "@Save= recorded nothing" (unknown -- the recorder's own line was buffered).
    Every one of those was reasoning from an absence of output that was never
    evidence of anything.

    Fixed HERE rather than in the compose env on purpose: `pol-git-sync` ignores
    docker-compose files, so an env fix lands on disk and recreates nothing,
    while a Python change deploys itself.
    """
    kwargs.setdefault("flush", True)
    # WARNING: STDOUT HAS NOW EATEN THIS MODULE'S DIAGNOSTICS TWICE. First the
    # unbuffered-print trap this banner describes; then, 2026-08-20T22:02, the
    # FLUSHED print -- verified on `jan`, which sets PYTHONUNBUFFERED -- fed a
    # stdout that reaches `docker logs` on no other container, so a reservation
    # failed with zero observable output while authserv.log looked healthy
    # (1,220 lines, not one of them ours). Absence of output was read as
    # evidence three times in one night on exactly this path.
    #
    # So: the same file every other diagnostic in this process uses, through
    # the core's log handed over by `titles.core` (no import of the core: a
    # title never imports responders). Outside the core (the selftests) the
    # handle's default prints, flushed.
    try:
        import titles
        titles.core.log("authserv",
                        " ".join(a if isinstance(a, str) else repr(a)
                                 for a in args))
        return
    except Exception:
        pass
    try:
        print(*args, **kwargs)      # the real builtin; _say is its only caller
    except UnicodeEncodeError:
        # WARNING: A LOG LINE MUST NEVER TAKE DOWN THE REQUEST THAT WROTE IT --
        # `responders.log` carries that rule verbatim and this function was
        # missing it. Measured 2026-08-20: a single emoji in a MATCH READY line
        # raised UnicodeEncodeError out of `_seat_at_table`, INSIDE a reply path,
        # on any stdout that is not UTF-8 (cp1252 on the Windows dev box; a
        # container that ever starts without a UTF-8 locale would do the same).
        # Half of what this module logs is client-supplied bytes, so the guard
        # is load-bearing, not decoration.
        enc = (getattr(sys.stdout, "encoding", None) or "ascii")
        safe = [a.encode(enc, "backslashreplace").decode(enc)
                if isinstance(a, str) else a for a in args]
        print(*safe, **kwargs)


def _sh_code():
    try:
        return int(os.environ.get("POL_TM_SH_CODE", str(MSG_SHENTER)), 0)
    except ValueError:
        return MSG_SHENTER


def _sh_en():
    """`/EN=` must be > 0; the handler bails at `jle` otherwise."""
    try:
        return int(os.environ.get("POL_TM_SH_EN", "1"), 0)
    except ValueError:
        return 1


def _cv_enabled():
    return os.environ.get("POL_TM_CV", "1") == "1"


def _cv_en():
    """THE PRIZE CENTER endpoint handshake -- `@CvReq=` -> `@CvEnter=/EN=<n>`.

    Observed live 2026-08-16 when a tester opened Rankings -> Prize Center:
    the client sends `@CvReq=/NN=.../HID=0/Dm=0/Vol=0/CN=...` on code 112 and we
    were silent, which is the same 75-second dead wait `@Quit=` was.

    NOT GUESSED. The parser at TM.dll rva 0x8B490 is `@ShEnter`'s (0x88DF0)
    instruction for instruction with one literal changed -- match `@CvEnter` in
    the body, read `/EN=` with the same extractor, bail at `jle` unless it is
    **> 0**, then take OUR message's sender id into [0x2B2958]/[0x2B295C] as the
    endpoint. So this is the shop handshake wearing a different name, and the
    same `/EN=1` that has been working there all day is the right answer here.

    WARNING: That global is shared: it is the same one the card-service directory
    search at 0x86044 compares entries against, so these endpoint handshakes
    move state the whole game reads. One at a time.
    """
    try:
        return int(os.environ.get("POL_TM_CV_EN", "1"), 0)
    except ValueError:
        return 1


def _shopinit_enabled():
    return os.environ.get("POL_TM_SHOPINIT", "1") == "1"


def _shopbuy_enabled():
    return os.environ.get("POL_TM_SHOPBUY", "1") == "1"


def _shopquit_enabled():
    return os.environ.get("POL_TM_SHOPQUIT", "1") == "1"


def _cardent_enabled():
    return os.environ.get("POL_TM_CARDENT", "1") == "1"


def _cardent_ans():
    """`/Ans=` must be > 0; 0x8BA52 `jle` skips the endpoint store otherwise."""
    try:
        return int(os.environ.get("POL_TM_CARDENT_ANS", "1"), 0)
    except ValueError:
        return 1


#: THE EVENT COUNTDOWN. `@EventTimeReq=` is sent by the event loader (0x96A22)
#: and by the member-list pump (0x975CF), and BOTH block on the answer -- so an
#: unanswered one is the same 75-second dead wait `@Quit=` and `@CvReq=` were.
#:
#: The three fields are stored to 0x2B5F9C/0x2B5FA0/0x2B5FA4 by the parser at
#: 0x8C2E9 and read back by the event main scene at 0x6F7A6, which tests exactly
#: one thing:
#:
#:     if Start == -1 and End == -1:  no event -- clear the clock, go to step 2
#:     else:                          [scene+0x1D8] = Now / 3600   (hours)
#:                                    [scene+0x1DC] = Now % 3600 / 60 (minutes)
#:
#: So **Start = End = -1 is the honest "no event is running" answer**, and it is
#: also the one that lets the scene move on rather than sit on a clock counting
#: down to something we do not serve. That is the default here on purpose: the
#: point of answering at all is to stop the hang, not to invent an event.
#: `Now` is a countdown in SECONDS, not a wall clock.
def _eventtime_enabled():
    return os.environ.get("POL_TM_EVENTTIME", "1") == "1"


def _eventtime_fields():
    def get(name, default):
        try:
            return int(os.environ.get("POL_TM_EVENT_" + name, str(default)), 0)
        except ValueError:
            return default
    return get("START", -1), get("END", -1), get("NOW", 0)


#: THE EVENT SHOP'S CARD DOOR. `@EInitReq=` is state 3 of the card machine
#: (0x9A1C2 -> 0x8B9B0); state 2 is the `@CardReq=` we already answer. The reply
#: parser is 0x8BE34 and it reads two fields:
#:
#:     /Card=  -> stored straight into the caller's out-param (an int, NOT a
#:                card list -- the `@Card=` family is a different message)
#:     /Ans=   -> if > 0, take OUR sender id as the endpoint (0x2B2958/0x2B295C),
#:                the same global `@ShEnter` and `@CvEnter` write
#:
#: WARNING: This can only ever fire once the Event Shop scene is OPEN, and the event-loader reading says
#: that needs us to place the player in the top three of an event ranking we do
#: not yet serve. It is here so the door is not the thing that blocks; do not
#: read silence on it as evidence about `/Card=`.
def _einitent_enabled():
    return os.environ.get("POL_TM_EINITENT", "1") == "1"


def _einitent_fields():
    def get(name, default):
        try:
            return int(os.environ.get("POL_TM_EINITENT_" + name, str(default)), 0)
        except ValueError:
            return default
    return get("CARD", 0), get("ANS", 1)


def _shopquit_d():
    """`/D=` -- 0 is the only value that continues; anything else fails out."""
    try:
        return int(os.environ.get("POL_TM_SHOP_QUIT_D", "0"), 0)
    except ValueError:
        return 0


#: A pack is a RANDOM DRAW, not a fixed list. SE's shop sells a permanent
#: catalogue -- `data/PackPrm.BIN` names Pauper's / Beginner's / Standard /
#: Collector's / Hero's / Starter's / COM's Lv.1-3 packs plus a `rank_1 Pack`
#: and per-character packs -- so a pack is a PRODUCT that stays on sale, and
#: what varies is the cards it yields. The frozen five-card list this replaced
#: was scaffolding to prove the wire format, never intended behaviour.
#:
#: `POL_TM_SHOP_CARDS` still pins an exact list (`no:d1..d7`, comma-separated)
#: for A/B testing; leave it unset for a real draw. `POL_TM_SHOP_SEED` makes a
#: draw reproducible when you are bisecting the unmeasured bytes.
_PACK_SIZE = 5              # cards per pack; also the minimum needed to play

#: WARNING: DIAGNOSTIC, 2026-08-16 -- REMOVE ONCE THE CEILING IS KNOWN.
#: Three live purchases say the client accepts a card id only below a small
#: ceiling: ids 1,2,3,4,5 -> it kept 1,2,3; two draws of random ids 34..226 ->
#: it kept NOTHING, while still printing "Acquired" for every card (the message
#: prints before the discard at 0x106093). The gate is
#: `cmp id, [0x528F4FC] ; jge discard`, and 0x528F4FC is written only by the
#: BIN loader's third argument when it loads CardPrm.BIN -- which ought to be
#: 250, so the static reading and the observed behaviour disagree.
#: `POL_TM_SHOP_LADDER=n` serves ids 0..n-1 with their real CardPrm stats;
#: however many come back IS the ceiling, in one purchase.
def _ladder():
    try:
        return max(0, int(os.environ.get("POL_TM_SHOP_LADDER", "0"), 0))
    except ValueError:
        return 0


def _ladder_base():
    """First id of the ladder. Rung 2 of the diagnostic: the first ladder (0..7)
    came back as ids 0,1,2,3, which reads as "card id must be < 4" -- but
    0x528F4FC is the CardPrm COUNT (0x128143 uses it as the loop bound over the
    array at 0x5246364), so an id ceiling of 4 is nonsense. The competing
    explanation is a TOTAL CARD CAP of four, which fits the same evidence.
    Serving a ladder that starts ABOVE 4 tells them apart in one purchase:
    4 cards back = a total cap, 0 back = a genuine id ceiling."""
    try:
        return max(0, int(os.environ.get("POL_TM_SHOP_LADDER_BASE", "10"), 0))
    except ValueError:
        return 0


#: VERIFIED: WHAT A PACK CONTAINS -- DECODED 2026-08-20 FROM `data/PackPrm.BIN`.
#:
#: Each 52-byte record carries FOUR draw rows at +0x0C, each four bytes:
#:
#:     (LEVEL, GROUP, CATEGORY, CUMULATIVE WEIGHT out of 255)
#:
#: One row is chosen per card: roll 0..254 and take the FIRST row whose
#: cumulative weight exceeds the roll, then pick uniformly from the cards that
#: row admits. The three filters are columns of `CardPrm.BIN`, and 0xFF (level)
#: or 0 (group, category) means "any" -- 0xFF never appears in the other two
#: slots in any of the 77 records, which is what makes the convention readable.
#:
#:     LEVEL     CardPrm byte 5, already the sell-price index (`_sell_price`)
#:     GROUP     CardPrm byte 6: 0..3 are broad classes, 4..29 are near-unique
#:               marker groups (22 = Mace of Zeus, 19 = Dragon's Hair, ...)
#:     CATEGORY  CardPrm byte 7: 1 monsters (109), 2 summons (19), 3 weapons
#:               (28), 4 ships (10), 5 mascots (9), 6 places (2), 7 characters
#:               (65), 8 oddities (8)
#:
#: WHY THIS IS THE READING AND NOT A SHAPE THAT HAPPENS TO FIT. The file names
#: its own records, and every name states what its rows resolve to:
#:
#:     Summoner's Pack     -> category 2, and the pool is the 12 summons
#:     Warrior's Pack       -> category 3 at levels 3/4/5, all weapons
#:     Voyager's Pack       -> category 4, the airships
#:     FFIX Celebrity Pack -> category 7 at levels 6/7, the characters
#:     FFX Blitzball Ed.   -> group 1, which is Datto/Letty/Jassu/Botta/Keepa
#:     Cid (COM deck)      -> category 4, the man who builds airships
#:     the weapon merchant -> category 3
#:     Steiner             -> group 23 = Excalibur II, group 5 = Beatrix
#:     Quina/Freya/Amarant -> groups 20/19/21 = Gastro Fork / Dragon's Hair /
#:                            Rune Claws, each that character's own weapon
#:     Beginner's/Hero's   -> plain level bands, 1/2/3/5 and 7/8/9/10
#:
#: Nine independent name-to-pool matches, and NO row in the whole file resolves
#: to an empty pool. The same record's +0x20 holds a card id for the 30 Prize
#: Center records, and those 30 rise monotonically in card LEVEL as their point
#: cost rises (13,13,13,14,15,15,15,16,16,16,16,...,22,23,23,24) -- an
#: independent confirmation of the level column, off a different field.
#:
#: WARNING: AND IT RETRACTS THE MASK. The 2026-08-20 pass read record +0x00 as
#: a 24-bit level mask and could not make the sanity check fit (the free pack
#: appeared to mask off a bit the 1000-gold pack allowed). It is not a pool
#: field at all: its low 20 bits are 0xFFFFF in every one of the 77 records,
#: and read as a level mask it has a counterexample -- Cid's deck (+0 =
#: 0x008FFFFF, bit 21 clear) has a draw row that asks for level 21 outright,
#: which is the Airship. The pool never needed it. What K = raw0 >> 20 (4..21)
#: means is still unknown; it is NOT price order, NOT pack size, and NOT the
#: highest level reachable. Nothing reads it here, and nothing should until it
#: is measured.
#:
#: WARNING: AND THE PLANNED MEASUREMENT IS RETIRED, NOT PASSED. The cheap route on
#: the table was "buy one pack repeatedly and record which levels appear". It
#: could never have worked from here: OUR server draws the cards, so the levels
#: a purchase yields are the levels we chose. The oracle was always the shipped
#: file. What a purchase CAN still falsify is this decode -- if a Beginner's Pack
#: ever hands over a card outside levels 1/2/3/5, the reading is wrong.
def _pack_pool(level, group, cat):
    """The card ids one draw row admits. Cached: 77 records x 4 rows at most."""
    key = (level, group, cat)
    pool = _POOL_CACHE.get(key)
    if pool is None:
        pool = []
        for no in range(tm_cardprm.CARD_COUNT):
            row = tm_cardprm.row(no)
            if level != 0xFF and row[5] != level:
                continue
            if group and row[6] != group:
                continue
            if cat and row[7] != cat:
                continue
            pool.append(no)
        _POOL_CACHE[key] = pool
    return pool


_POOL_CACHE = {}


def _pack_size(rec):
    """How many cards this pack yields -- the record's own +0x09 byte.

    The flat 5 this replaced was scaffolding; the field is the file's own,
    and it is 1 in all 31 single-card records, which is what identifies it.

    VERIFIED: AND THE CLIENT HAS ROOM FOR IT -- measured 2026-08-20 off the SHOPBUY
    parse loop at rva 0x105EF4, because raising a count SE never sent us is
    exactly the kind of unverified change that breaks a working screen:

        0x105EF4  eax = /S=          the count we send
        0x105F07  word [esi+0xF0] = 0        the staging count, zeroed
        0x105F50  edi = (staging + 0x4B9) << 4 + esi     the record slot
        0x10608A  inc word [esi+0xF0]        ...only for a card it KEPT
        0x10609F  cmp ebp, eax / jl          the loop runs exactly /S= times

    **There is no cap in that loop** -- it writes as many records as we send, so
    the only real bound is where the staging array (0x524AF70) runs into
    something else. The nearest global referenced above it is 0x524B0B0, at
    +0x140 = **20 records**, so ten is comfortably inside and five never tested
    the edge. `POL_TM_PACK_SIZE` still forces a count.

    WARNING: It changes what is DRAWN, not what is kept. Shop
    state 6 (0x17961D) copies ONE record, from staging slot 0, and zeroes the
    rest -- so `cards[0]` is the card the player actually gets, and the other
    nine are animation. That is also why the two range gates in the loop matter
    here: a card the client DISCARDS (id >= CardPrm count at 0x105F78, or type
    >= 4 at 0x105FBD) does not increment the staging count, so a discarded
    `cards[0]` would silently hand the player our second card while we recorded
    the first. Every id we draw comes out of CardPrm, so neither gate can fire.
    """
    env = os.environ.get("POL_TM_PACK_SIZE")
    if env:
        try:
            return max(1, min(64, int(env, 0)))
        except ValueError:
            pass
    if rec and 0 < rec["cards"] <= 64:
        return rec["cards"]
    return _PACK_SIZE


def _card_arrows(rnd, base):
    """Wire slot 6 for one newly-issued card: its ARROW MASK.

    WARNING: THIS SERVER WAS SENDING THE CARD'S LEVEL HERE, AND IT IS THE REASON A
    WHOLE TEN-TURN MATCH PRODUCED ONE BATTLE. Slot 6 is `rec+0x0D`, which the
    client bit-tests per direction in the renderer (0xB4D0D) and in the
    adjacency engine (0xD3760); we were filling it with `CardPrm` byte 5, the
    sell-price level. The two consequences are both visible on screen:

      * a level-0 card has NO arrows and can never take anything, and
      * levels cluster at 1-3 bits with a maximum of 5, against the client's
        own distribution of 0-8 centred on 3-4 (`ARROW_COUNT_WEIGHTS`), so
        every card we ever dealt was arrow-poor.

    WARNING: THIS CHANGES CARDS ALREADY IN PLAYERS' COLLECTIONS -- or rather, it does
    not: a collection stores the eight-value ROW, so cards granted before this
    keep the level in slot 6 until they are replaced. That is deliberate. The
    alternative is rewriting saved rows, and a migration that edits a player's
    cards to fix a server bug is exactly the class of change this project has
    been burned by. New cards are right; old ones stay as issued.

    `POL_TM_CARD_ARROWS=0` restores the old byte for an A/B.
    """
    if not _env_int("POL_TM_CARD_ARROWS", 1):
        return base[5]
    return tmbattle.draw_arrows(rnd)


def _draw_pack(no=None, n=None):
    """The cards pack `/No=<no>` yields, each carrying the stats the CLIENT'S
    OWN CardPrm row says that card should have.

    WARNING: REAL PACKS ALMOST CERTAINLY ROLLED, AND THAT ROLL IS NOT RECOVERABLE.
    Every card of a given id we hand out is byte-identical, because we mirror
    the CardPrm row exactly; SE's server presumably spread the three stat bytes
    around that baseline, which is what makes two copies of the same card worth
    different amounts. Checked 2026-08-18 (stopgap audit): the distribution is
    SERVER-SIDE by construction -- the client only ever RENDERS a record against
    the baseline (0xB9410 computes about ((2*v - base)/base + K1) * K2 per
    stat), it never generates one -- so no amount of reading TM.dll or
    TMaster.pex can produce it. There is no oracle for this in anything we hold.

    So this is a deliberate stop, not an unexamined one: `POL_TM_SHOP_JITTER`
    (percent, default 0) exists to spread the bytes, and it stays at 0 because
    picking a spread would be inventing a game rule and calling it recovery.
    Nominal stats are at least a defensible reading of the baseline. If a period
    source ever shows two same-id cards with different stats, the spread between
    them is the measurement, and this is the knob.

    TM.dll 0xB9410 renders a card by measuring the record we send AGAINST the
    CardPrm baseline for the same id -- about `((2*v - base)/base + K1) * K2`
    per stat. A record of placeholder 1s against a baseline of 6..90 is a
    degenerate card, which is why the first draws drew flat and skipped the
    acquire animation. Mirroring the row puts every stat at its nominal value.
    """
    rungs = _ladder()
    if rungs:
        # Ladder mode: ids 0..rungs-1 in order, real stats, nothing random --
        # except the arrows, which are a per-INSTANCE property and have no
        # nominal value to serve (see `_card_arrows`).
        out = []
        base_id = _ladder_base()
        lad = random.Random(os.environ.get("POL_TM_SHOP_SEED") or None)
        for card in range(base_id, min(base_id + rungs, tm_cardprm.CARD_COUNT)):
            b = tm_cardprm.row(card)
            out.append([card, b[0], b[1], b[2], b[3], b[4],
                        _card_arrows(lad, b), _new_card_slot(b[6])])
        return out

    rnd = random.Random(os.environ.get("POL_TM_SHOP_SEED") or None)
    try:
        jitter = max(0, min(100, int(os.environ.get("POL_TM_SHOP_JITTER", "0"), 0)))
    except ValueError:
        jitter = 0

    def vary(v):
        if not jitter or v <= 0:
            return v
        lo = max(1, v - v * jitter // 100)
        hi = min(255, v + v * jitter // 100)
        return rnd.randint(lo, hi)

    rec = _pack_rec(no)
    ids = []
    for _ in range(n or _pack_size(rec)):
        pool = None
        if rec:
            roll = rnd.randint(0, 254)
            for level, group, cat, cum in rec["draw"]:
                if cum > roll:
                    pool = _pack_pool(level, group, cat)
                    if not pool:
                        # WARNING: ONE ROW IN THE SHIPPED FILE ASKS FOR CARDS THAT DO
                        # NOT EXIST, and this is OUR repair of it, not SE's
                        # behaviour. Record 27 (Quina's COM deck, kind 0, never
                        # sold) wants level 1 + group 3 + category 5, and the
                        # four cards in that group and category are Chocobo,
                        # Fat Chocobo, Mog and Frog at levels 3/6/5/3. It is the
                        # ONLY empty row in all 308 -- `_selftest_packs` pins
                        # that -- so the level is what gives, and the group and
                        # category, which are what name the pack, are kept.
                        pool = _pack_pool(0xFF, group, cat)
                        _say("tm: pack /No=%s row (level %d, group %d, "
                             "category %d) selects no card that exists -- drew "
                             "on group+category alone (%d cards)"
                             % (no, level, group, cat, len(pool)))
                    break
            if not pool and rec["card"] < tm_cardprm.CARD_COUNT:
                # A Prize Center record: no live draw row, one named card.
                pool = [rec["card"]]
        if not pool:
            # No record, or one whose rows cover no roll. SAY SO -- drawing from
            # the whole table silently is exactly the bug this replaced.
            _say("tm: pack /No=%s -- no draw row covers this roll; falling back "
                 "to the whole card table" % (no,))
            pool = list(range(tm_cardprm.CARD_COUNT))
        ids.append(rnd.choice(pool))

    roll = tmroll is not None and _env_int("POL_TM_CARD_ROLL", 1)
    out = []
    for card in ids:
        base = tm_cardprm.row(card)
        # /D= order: idx1 attack, idx2 type, idx3 pdef, idx4 mdef, idx5 the
        # derived power byte, idx6 THE ARROWS, idx7 -> [rec+0x16], unread.
        if roll:
            # WARNING: THE MEASURED PER-INSTANCE ROLL -- the client's OWN (TM.dll
            # 0xB2CF0 / PS2 TMaster.pex 0x2b1c80, byte-identical): each combat
            # stat strictly below its CardPrm ceiling, so the card arrives with
            # room to GROW through use instead of pre-maxed. This ends the
            # universal-MAX bug (serving the ceiling made every card read MAX).
            # It REPLACES the old `POL_TM_SHOP_JITTER` stop, which predated
            # finding the roll and served nominal because "picking a spread would
            # be inventing a rule" -- the client's roll IS the rule now.
            # POL_TM_CARD_ROLL=0 restores nominal (all-maxed).
            atk = tmroll.roll_stat(rnd, base[0])
            pdf = tmroll.roll_stat(rnd, base[2])
            mdf = tmroll.roll_stat(rnd, base[3])
        else:
            atk, pdf, mdf = vary(base[0]), vary(base[2]), vary(base[3])
        out.append([card, atk, base[1], pdf, mdf,
                    tmbattle.power_byte(base[4], atk, pdf, mdf,
                                        base[0], base[2], base[3]),
                    _card_arrows(rnd, base), _new_card_slot(base[6])])
    return out


def _shopbuy_cards(no=None):
    """[(cardno, d1..d7)] for pack `/No=<no>`, filtered to what the client will
    accept. `no` is None only when a caller has no purchase in hand, and then
    the draw has no record to work from and says so."""
    pinned = os.environ.get("POL_TM_SHOP_CARDS")
    if not pinned:
        return _draw_pack(no)
    out = []
    for item in pinned.split(","):
        item = item.strip()
        if not item:
            continue
        try:
            vals = [int(x, 0) for x in item.split(":")]
        except ValueError:
            continue
        if len(vals) != 8:
            continue
        # The client drops a card whose number is >= the CardPrm table or whose
        # type byte is >= 4. Dropping it here too keeps the log honest about how
        # many cards the player should actually receive.
        # idx 0 is the card number, idx 2 the type -- the client's only two range
        # checks. (This read vals[3] until 2026-08-16; harmless with the old
        # hardcoded list, wrong for anything else.)
        if not (0 <= vals[0] < CARDPRM_RECORDS) or not (0 <= vals[2] < 4):
            continue
        out.append(vals)
    return out


def _card_body(header, cards):
    """`@Card=` for a list of 8-value card records, under an existing header."""
    body = bytearray(header + b"@Card=/S=%d" % len(cards))
    for i, v in enumerate(cards):
        # ONE `/D=` per card, values PIPE-SEPARATED -- see the banner below.
        body += b"@N%d=/D=%s" % (i, b"|".join(b"%d" % x for x in v))
    return bytes(body)


def _shopbuy_body(header, member_id=None, no=None):
    """The SHOPBUY answer. `header` is the request's 8 hex chars, echoed so the
    sender-id and shop-number equalities at 0x105EA9/0x105ECD both hold. `no` is
    the `/No=` the player bought -- it selects the pack's own draw table, so
    losing it here is what made every pack draw from all 250 cards."""
    cards = _shopbuy_cards(no)
    # RECORD WHAT WE GRANTED. This is the authoritative half of persistence --
    # these are our own bytes, not a reading of the client's. WARNING: The client keeps
    # only `cards[0]` and only if the player completes the acquire, so the pack
    # is held as an OFFER and credited when `@Get=` says it was taken. See
    # `_ACQUIRED_PER_PACK` for the disassembly that measures both halves.
    if _record_on_grant():
        _collection_add_cards(member_id, cards)
    else:
        _collection_offer(member_id, cards)
    return _card_body(header, cards)


#: Field defaults for the SHOPINIT body. Names measured, values NOT -- see the
#: banner above `MSG_SHOPINIT`. Each is `POL_TM_SHOP_<FIELD>`.
_SHOPINIT_FIELDS = (("N", 1), ("RA", 0), ("M", 10000), ("CP", 0), ("S", 1), ("SP", 0))


#: *** MONEY WAS A CONSTANT, AND THE SHOP RESET IT ON EVERY VISIT. ***
#: `/M=` in SHOPINIT is the Money the card shop displays (Cardsh.BIN names the
#: field). It was served from `_SHOPINIT_FIELDS` as a flat 10000 on every
#: `@ShReq=`, so a player who sold a card or bought a pack watched it snap back
#: to 10000 the moment they re-entered the shop. Nothing persisted it, and
#: nothing could: the client never reports its money.
#:
#: `POL_TM_START_MONEY` is what a member starts with.
#:
#: WARNING: IT WAS 10000, AND THAT NUMBER WAS NEVER MEASURED -- IT WAS THE UNMEASURED
#: `_SHOPINIT_FIELDS` CONSTANT, KEPT ONLY SO THE ACCOUNT-OPENING CHANGE WOULD
#: NOT LOOK LIKE A BALANCE CHANGE. Keeping it turned every new member into a
#: 10000-gold grant, and 2026-08-25 is when two players watched it land:
#:
#:   tester,   02:47Z  "fought a COM opponent, quit, lost a card, and was given
#:                      10000 gil"
#:   tester,   02:50Z  "another user backed out of the game and was given 10000
#:                      money as well upon logging back in"
#:
#: Neither is a prize and neither has anything to do with quitting -- the log
#: shows no payout on either match (`/R=0`, no wager, no `money` line between
#: `@ComGame=` and `@GameExit=`). It is THIS constant, materialised by `money_of`
#: at the member's first shop visit and then DELIVERED, late, by whichever of the
#: two channels the player touched first:
#:
#:   `@ComGameInit=/M=`  0x102DA9 `mov dword [esi+0xC8], eax` -- a plain
#:                       assignment into the LIVE WALLET. `esi` is the save
#:                       struct at 0x52463E0 (callers do `mov ecx, 0x52463E0 ;
#:                       call 0x101A10`, and 0x101A36 is `mov esi, ecx`), and
#:                       +0xC8 is the money field the save parser fills from
#:                       file +0x34 at 0x10123B. So whatever we serve here
#:                       BECOMES the player's on-screen gold.
#:   the authored save   `_collection_to_save` writes `money_of` at
#:                       `tmsave.MONEY_OFF`, which is what the client reads at
#:                       launch -- hence "given 10000 upon logging back in".
#:
#: The shop's own `/M=` is NOT a third channel: on `@Init` it is parsed and
#: discarded (money there is `[esi+0xCC]`, the `@EcmInit`/AUCMONEY field), which
#: is why the grant only became visible at a COM game or a relaunch.
#:
#: VERIFIED: ZERO IS WHAT SE SHIPPED FOR, and its evidence is in SE's own data: the
#: shop ladder in `PackPrm.BIN` opens with the **FREE Pauper's Pack** (No=0,
#: price 0) before 1000 / 1500 / 9000 / 40000 / 100000. A free starter pack is
#: only a design if a new player has nothing -- it IS the bootstrap, and a
#: 10000 stipend makes it pointless. Nothing anywhere measures a starting
#: balance; 0 is the only number here that is not invented.
#:
#: WARNING: THIS ONLY STOPS THE MINTING. A member with a balance already on record
#: keeps it -- `money_of` materialises once and never re-reads this. Undoing
#: grants already made is a deliberate act on somebody's save, not a default.
def _start_money():
    try:
        return max(0, int(os.environ.get("POL_TM_START_MONEY", "0"), 0))
    except ValueError:
        return 0


def _money_materialise_enabled():
    return os.environ.get("POL_TM_MONEY_OPEN_ACCOUNT", "1") == "1"


def money_of(member_id):
    """This member's money -- the stored value, opening an account if there is none.

    WARNING: THE FALLBACK USED TO BE A REFUND MACHINE, AND THAT IS WHY THE SHOP HANDED
    OUT FREE GOLD. `_start_money()` was returned WITHOUT being stored, so a member
    whose balance had never been written read 10000 on every single call, for
    ever. Nothing the client spent could stick, because there was no account for
    it to stick to -- the next read simply answered 10000 again.

    Reported live, 2026-08-20: "why does every shop visit give the player free money?
    I don't think that's how square handled it". It isn't. SE's server owned the
    balance; ours only owned it once something happened to write one.

    MEASURED, the same afternoon: that member bid 50 gold on an auction, their
    client went to 9950 (and proved it -- the wager default is 10% of gold and it
    sent `bm=995`), while `money_of` still answered **10000** because their
    collection carried no `money` key at all. The 50 would have been refunded on
    the next serve.

    So the fallback now OPENS THE ACCOUNT: the starting balance is written once,
    and from then on this is stored state that debits and credits accumulate
    against. Nothing else had to change -- `_collection_to_save` already writes
    our balance into the save at `tmsave.MONEY_OFF`, sales already credit and
    packs already debit. They were all accumulating against a number that reset.

    WARNING: AND THE VALUE IT OPENS AT IS NOW **0**, not the 10000 this docstring used
    to promise was unchanged. That 10000 was the grant two players watched arrive
    on 2026-08-25 -- see the banner above `_start_money` for the two delivery
    channels and why the free Pauper's Pack is the bootstrap SE actually shipped.
    Materialising is still ONCE: a member with a balance on record is untouched.

    `POL_TM_MONEY_OPEN_ACCOUNT=0` restores the non-storing fallback, which is the
    control for "did opening accounts change anything".
    """
    if member_id is None:
        return _start_money()
    data = _collection_load(member_id)
    v = data.get("money")
    try:
        return max(0, int(v))
    except (TypeError, ValueError):
        pass
    start = _start_money()
    if not _money_materialise_enabled():
        return start
    data["money"] = start
    if _collection_store(member_id, data):
        _say("tm: member %s had no balance on record -- opening the account at "
              "%d. Until now every read answered this and stored nothing, so "
              "anything the client spent came back on the next serve. A "
              "NON-ZERO figure here is a grant this server invented and will "
              "push into the client's wallet (@ComGameInit=/M= writes it "
              "straight to save-struct +0xC8) -- POL_TM_START_MONEY."
              % (member_id, start))
    return start


def _set_money(member_id, amount, why=""):
    """Persist a new balance. Clamped at zero -- a negative balance is a number
    the client has no way to display and we have no evidence it ever sees."""
    if member_id is None:
        return False
    data = _collection_load(member_id)
    was = money_of(member_id)
    data["money"] = max(0, int(amount))
    if not _collection_store(member_id, data):
        return False
    _say("tm: member %s money %d -> %d%s"
          % (member_id, was, data["money"], (" (%s)" % why) if why else ""))
    return True


#: WARNING: THE SALE PRICE IS **NOT MEASURED**, AND IT IS THE ONE NUMBER HERE THAT IS
#: NOT. `@Sell=` is fire-and-forget: TM.dll carries two command tables, one
#: space-terminated for messages it RECEIVES (`@EQuit`, `@Card`, `@CardSelect`)
#: and one `=`-terminated for what it SENDS (`@Buy=`, `@Quit=`, `@Sell=`), and
#: there is no `@ESell` anywhere in the image. So the client prices the card
#: itself, updates its own money, and only tells us WHICH card went. It never
#: reports the balance -- `@Save=` carries the table settings (`@Tab=`/`@Tet=`,
#: 11r) and `@PutData=` / `@Data=` have never been seen on a wire.
#:
#: So the server has to price it, and until somebody sells one card and reads
#: the money off the screen, any rule here is invented. A flat rate is the least
#: pretending: a formula would imply we know the shape.
#:
#: WARNING: PINNING IT TAKES ONE SALE. `_sell_cards` logs the card and the balance
#: either side; compare the on-screen money before and after, then set
#: `POL_TM_SELL_PRICE` -- or, if the delta varies by card, say so and this
#: becomes a function of the CardPrm row instead of a constant.
#:
#: NOTE: AND THERE IS A HYPOTHESIS WORTH TESTING FIRST, because if it holds the
#: price stops being ours to guess at all. SHOPINIT carries `/CP=` and `/SP=`,
#: two fields whose NAMES are measured and whose VALUES never were (see the
#: banner above `MSG_SHOPINIT`). **The server sends both.** If `SP` is the Sell
#: Price and `CP` the Card Price, then we DICTATE what the client pays and pays
#: out, and server and client agree by construction rather than by luck.
#:
#: The prediction is sharp and costs one sale: we currently send `/SP=0`. If SP
#: is the sell price, a card sells for NOTHING and the player's money does not
#: move. So --
#:
#:     money did NOT change on sale  ->  SP is (very likely) the price, and
#:                                       POL_TM_SHOP_SP is the real knob; set
#:                                       POL_TM_SELL_PRICE to the same number
#:     money went UP by X            ->  SP is not it; X is the price, and
#:                                       POL_TM_SELL_PRICE=X is exact
#:
#: Either answer closes this. Do not wire `@Buy=` to debit until it is settled:
#: pack prices are unknown, the Pauper's Pack is FREE, and a wrong debit desyncs
#: money in the direction the player notices most.
#: VERIFIED: THE SELL PRICE, SOLVED 2026-08-20 -- it is a LADDER LOOKUP, not a constant.
#:
#: A tester read three prices off the shop screen and the wire gave the cards:
#:
#:     15|14|0|10|12|**2**  ->  43        49|54|1|43|13|**5**  ->  437
#:     Cinna (level 4)      ->  218
#:
#: `data/CardPri.BIN` is a 25-entry u32 table after a 16-byte header:
#:
#:     0, 25, 50, 100, 250, 500, 750, 1000, 1500, 2500, 5000, 10000, | 2500, ...
#:
#: and the client's own pricing routine is at **0xB3840**: it loads the table
#: pointer from the global at rva 0x2B6368 (filled by the `CardPri.BIN` loader at
#: 0x188CBF), indexes it with `[ebx+5]` -- the card's LEVEL, which is exactly the
#: last field `@Sell=` sends -- and scales the result with float math built from
#: 1.0 (0x205DB8), 0.5 (0x2049F8) and 0.25 (0x204528):
#:
#:     0xB3877  mov al, [ebx+5]           the level
#:     0xB3892  mov edx, [ecx + eax*4]    base = ladder[level]
#:     0xB38DB  call 0x1E2790             float -> int
#:
#: All three prices are `floor(ladder[level] * 7/8)` exactly:
#:
#:     50 * 0.875 = 43.75  -> 43        250 * 0.875 = 218.75 -> 218
#:     500 * 0.875 = 437.5 -> 437
#:
#: VERIFIED: AND THE MULTIPLIER IS THE CARD **TYPE**, in eighths. Solved 2026-08-20
#: from six sold cards spanning all four types -- a tester read the prices
#: off the shop screen and selling put each card's full row on the wire:
#:
#:     id   atk type pdef mdef lvl   ladder   price    mult
#:     15    14   0    10   12   2       50      43    7/8
#:    102    65   0    56   40   6      750     656    7/8
#:     49    54   1    43   13   5      500     437    7/8
#:    234     5   1    50  100  15      500     437    7/8   [Magic Urn]
#:    161   100   2    90   99  21    10000   10000    8/8   [Braska]
#:    158    97   3    89   95  24    17500   19687    9/8   [Masked Rikku]
#:
#:     multiplier = (7 + max(0, type - 1)) / 8
#:
#: WARNING: AND THE STATS DO NOT MATTER, which is what makes this a rule rather than a
#: fit. Cards 15 (14/10/12) and 102 (65/56/40) have wildly different stats, are
#: both type 0, and are both 7/8; 49 (54/43/13) and 234 (5/50/100) likewise are
#: both type 1 and both 7/8. Only the type moves it. Those are Tetra Master's
#: P/M/X/A types, and the rarer X and A sell for more.
#:
#: WARNING: TYPES 4+ ARE UNSEEN. `@VsGameInit`'s parser bounds the type at 4
#: (tmsave's TYPE_MAX, `0x5091383 cmp byte [edi+1], 4 / jae`), so a type-4 card
#: would be 10/8 under this rule -- untested. POL_TM_SELL_NUM forces a numerator.
#:
#: WARNING: Three earlier readings were wrong before this: a flat 100, then "7/8
#: always" (fitted to four same-type cards), then "the stats are in the
#: multiplier". The type only became visible once cards of types 2 and 3 were
#: actually SOLD -- reading the price alone never carried the stats.
#: VERIFIED: THE CARD TABLE, DECODED 2026-08-20. `data/CardPrm.BIN` is 12-byte records
#: from offset 16, indexed by CARD ID:
#:
#:     byte 0 = attack   1 = TYPE   2 = phys def   3 = mag def   5 = LEVEL
#:
#: Validated against all six sold cards -- byte 5 reproduces their levels (2, 5,
#: 6, 15, 21, 24) and byte 1 their types exactly. 269 real records; the rest of
#: the 480 are padding with nonsense types.
#:
#: WARNING: AND IT SETTLES THE TYPE-4 QUESTION: the real records carry ONLY types
#: 0 (145), 1 (91), 2 (21) and 3 (12). There is no type 4, so
#: `(7 + max(0, type-1))/8` covers every card that exists.
#:
#: Why ship it: the wire row carries type and level, but a card that arrives
#: WITHOUT them used to fall back to a flat price and silently desync the
#: wallet. With this table an id is enough.
_CARDPRM = None


#: *** THE CARD LEVEL, PORTED FROM THE CLIENT'S OWN 0xB9040 (2026-09-08). ***
#:
#: WARNING: THIS IS THE NUMBER THE VS. COM OPPONENT UNLOCK IS GATED ON, and this
#: server was overwriting it with a ZERO on every VS. COM open -- see
#: `_comgame_body`. The client keeps it live: SEVEN sites call `0xB9040` and
#: store the result into `word [0x52463E0]` (struct +0x00) during play
#: (0xCB652, 0xDA648, 0xEAEFD, 0xEFFD3, 0xF0075, 0x117ABC, 0x12D046), and the
#: save parser derives it again at load (0x10147F). So "the client only knows
#: this at launch" is FALSE -- it recomputes constantly, and the only thing
#: that made it stale was our own message.
#:
#: The algorithm, read instruction for instruction at 0xB90E6..0xB9192 over the
#: collection's 16-byte memory rows (attack +0, type +1, pdef +2, mdef +3,
#: grade +4, arrows +5, id word +6, deck slot +0xE -- the save loader's own
#: copy order at 0x1013A3..0x1013CB):
#:
#:     total = 0
#:     for each row:
#:         if id >= CardPrm count:            skip entirely      (0xB90F6)
#:         if this id has not been seen yet:                     (0xB90FE)
#:             total += the HIGHEST grade among all copies of it (0xB9112 scans
#:                      the rest of the collection keeping the max, 0xB9131)
#:             mark the id seen
#:         if type > 1:  total += type - 1                       (0xB9146)
#:         if this ARROW MASK has not been seen yet:             (0xB915F)
#:             total += 5; mark the mask seen                    (0xB9171)
#:     return total as a WORD                                    (0xB9192)
#:
#: KEY: So it rewards BREADTH, not duplicates: one copy of each card (its best
#: grade), a bonus for the rarer types, and five per distinct arrow pattern.
#: The dedupe table is per CARD ID and the arrow table is 256 bytes indexed by
#: the mask byte (0xB90A5 zeroes 0x100 bytes), so both are exact.
#:
#: WARNING: The type bonus is per ROW, not per distinct id -- 0xB9143 sits AFTER the
#: dedupe block's join, so a second copy of a type-3 card still pays its 2.
def card_level(rows):
    """The player's CARD LEVEL from their collection -- the client's 0xB9040.

    `rows` are the stored 8-value rows `[id, atk, type, pdef, mdef, grade,
    arrows, deck slot]`. Returns the same u16 the client computes, so the two
    can be compared directly (and so `_comgame_body` can stop sending 0).
    """
    best = {}
    for r in rows or ():
        try:
            cid = int(r[0])
            grade = int(r[5])
        except (TypeError, ValueError, IndexError):
            continue
        if not 0 <= cid < CARDPRM_RECORDS:
            continue
        if grade > best.get(cid, -1):
            best[cid] = grade
    total = sum(best.values())
    seen_arrows = set()
    for r in rows or ():
        try:
            cid = int(r[0])
        except (TypeError, ValueError, IndexError):
            continue
        if not 0 <= cid < CARDPRM_RECORDS:
            continue
        try:
            ctype = int(r[2])
        except (TypeError, ValueError, IndexError):
            ctype = 0
        if ctype > 1:
            total += ctype - 1
        try:
            arrows = int(r[6]) & 0xFF
        except (TypeError, ValueError, IndexError):
            continue
        if arrows not in seen_arrows:
            seen_arrows.add(arrows)
            total += 5
    return max(0, min(0xFFFF, total))


def card_level_of(member_id):
    """`card_level` for one member's stored collection."""
    try:
        data = _collection_load(member_id)
        rows = data.get("cards")
        return card_level(rows if isinstance(rows, list) else [])
    except Exception as exc:
        _say("tm: card level unavailable for member %s (%r) -- sending 0, which "
             "LOCKS every VS. COM opponent above the first threshold"
             % (member_id, exc))
        return 0



def _cardprm(card_id):
    """(type, level) for a card id from `CardPrm.BIN`, or (None, None)."""
    global _CARDPRM
    if _CARDPRM is None:
        _CARDPRM = {}
        try:
            here = os.path.dirname(os.path.abspath(__file__))
            with open(os.path.join(here, "tmdata", "CardPrm.BIN"), "rb") as f:
                blob = f.read()
            H, R = 16, 12
            for cid in range((len(blob) - H) // R):
                row = blob[H + cid * R:H + cid * R + 6]
                if row[1] <= 3:                 # a real record, not padding
                    _CARDPRM[cid] = (row[1], row[5])
        except Exception as exc:
            _say("tm: CardPrm.BIN unreadable (%r) -- prices fall back to the "
                 "wire row alone" % (exc,))
    try:
        return _CARDPRM.get(int(card_id), (None, None))
    except (TypeError, ValueError):
        return (None, None)


#: card id -> ENGLISH name (0=Goblin, 21=Cerberus, 75=Cinna, 87=Viltgance).
#: The name ordinal IS the card id. WARNING: The shipped `CardPrm.BIN` is the JAPANESE
#: build (its stats feed `_cardprm`/`_sell_price` and are load-bearing, so it is
#: NOT swapped) -- its name pool is Japanese, wrong for the US Viewer. The US
#: English names ride alongside as `tmdata/card_names_en.txt`, extracted from the
#: US install tree's CardPrm.BIN; the BIN pool is only a last-ditch fallback.
_CARDNAMES = None


def card_name(card_id):
    """The card's US English display name, or "" if unknown -- callers must
    tolerate empty (a notice that cannot name the card still has to send)."""
    global _CARDNAMES
    if _CARDNAMES is None:
        _CARDNAMES = []
        here = os.path.dirname(os.path.abspath(__file__))
        try:
            with open(os.path.join(here, "tmdata", "card_names_en.txt"),
                      "r", encoding="utf-8") as f:
                _CARDNAMES = [ln.rstrip("\n") for ln in f]
        except OSError:
            # Last resort: the BIN's own pool (Japanese in the shipped file).
            try:
                with open(os.path.join(here, "tmdata", "CardPrm.BIN"), "rb") as f:
                    blob = f.read()
                pool = struct.unpack_from("<I", blob, 0)[0]
                if 0 < pool < len(blob):
                    _CARDNAMES = [s.decode("cp932", "replace")
                                  for s in blob[pool:].split(b"\x00")]
                _say("tm: card_names_en.txt missing -- names fall back to the "
                     "BIN pool (Japanese in the shipped build)")
            except Exception as exc:
                _say("tm: card names unreadable (%r) -- notices go unnamed"
                     % (exc,))
    try:
        cid = int(card_id)
    except (TypeError, ValueError):
        return ""
    if 0 <= cid < len(_CARDNAMES):
        name = _CARDNAMES[cid]
        return "" if name in ("(null)", "") else name
    return ""


#: VERIFIED: PACK PRICES, DECODED 2026-08-20 -- `data/PackPrm.BIN`.
#:
#:     header: u32 size(4020), u32 COUNT(77), u32 1026, u32 checksum
#:     77 records of **52 bytes** from offset 16
#:       record +0  read as a card mask in the first pass; it is not one
#:       record +4  the PRICE, u32
#:
#: 16 + 77*52 = 4020, exactly the size the header declares, which is what
#: confirms the stride. The first six records are the shop's ladder:
#:
#:     No=0      0   <- the FREE Pauper's Pack, and its own record says so
#:     No=1   1000       No=2   1500      No=3   9000
#:     No=4  40000       No=5 100000
#:
#: WARNING: AND `@Buy=/No=<n>` INDEXES THIS TABLE DIRECTLY, validated against the
#: a tester's own purchases: they bought `/No=3` and reported paying **9000**,
#: and `/No=0` (the free pack) is the one that costs nothing. Records beyond the
#: shop ladder are mostly 0 -- prize and event packs, not purchasable.
#:
#: WARNING: THE CLIENT DEBITS ITSELF, exactly as it prices sales itself, so our number
#: has to MATCH rather than lead. `_sell_price`'s banner explains why that
#: matters: `_collection_to_save` writes OUR balance into the save the client
#: loads, so a wrong debit eventually overwrites the right one.
#:
#: VERIFIED: THE REST OF THE RECORD WENT THE SAME WAY, 2026-08-20 -- see the banner
#: above `_draw_pack` for the decode and the evidence. The whole 52 bytes:
#:
#:     +0x00 u32   a packed pair, (K << 20) | 0xFFFFF, K = 4..21. NOT the pool
#:                 -- the low 20 bits are 0xFFFFF in all 77 records, and read
#:                 as a level mask it has a counterexample (see `_draw_pack`).
#:     +0x04 u32   PRICE (gold; the Prize Center's records price in points)
#:     +0x08 u8    KIND: 0 = not for sale (COM decks, Starter's), 1 = shop
#:                 pack, 2 = Pauper's, 3 = rank_1, 4 = Blitzball, 5 = Prize
#:                 Center card. Read off which records carry which name; the
#:                 grouping is exact but the labels are inference.
#:     +0x09 u8    CARDS the pack yields (1 for every single-card record)
#:     +0x0C..0x1B 4 x (level, group, category, cumulative-weight) -- THE POOL
#:     +0x1C u16   unread          +0x1E u16  unread (150 in 75 of 77 records)
#:     +0x20 u16   a CARD ID for the Prize Center's 30 records, else 9999
#:     +0x24..     five u32, 9999 each -- unread
_PACKPRM = None


def _packprm():
    """`PackPrm.BIN` parsed into one dict per record; [] if unreadable."""
    global _PACKPRM
    if _PACKPRM is None:
        _PACKPRM = []
        try:
            here = os.path.dirname(os.path.abspath(__file__))
            with open(os.path.join(here, "tmdata", "PackPrm.BIN"), "rb") as f:
                blob = f.read()
            n = struct.unpack_from("<I", blob, 4)[0]
            H, R = 16, 52
            if 0 < n <= 512 and len(blob) >= H + R * n:
                for i in range(n):
                    o = H + R * i
                    _PACKPRM.append({
                        "raw0": struct.unpack_from("<I", blob, o)[0],
                        "price": struct.unpack_from("<I", blob, o + 4)[0],
                        "kind": blob[o + 8],
                        "cards": blob[o + 9],
                        "draw": [tuple(blob[o + 12 + 4 * e:o + 16 + 4 * e])
                                 for e in range(4)],
                        "card": struct.unpack_from("<H", blob, o + 0x20)[0],
                    })
        except Exception as exc:
            _say("tm: PackPrm.BIN unreadable (%r) -- packs stay free and draw "
                 "from the whole table" % (exc,))
    return _PACKPRM


def _pack_rec(no):
    """The record `@Buy=/No=<no>` names, or None if there is no such pack."""
    try:
        i = int(no)
    except (TypeError, ValueError):
        return None
    table = _packprm()
    return table[i] if 0 <= i < len(table) else None


def _pack_price(no):
    """What pack `/No=<no>` costs, or None if the table has no such record."""
    rec = _pack_rec(no)
    return None if rec is None else rec["price"]


_CARDPRI = None


def _cardpri_table():
    """The 25-entry price ladder from `CardPri.BIN`, or [] if it is missing."""
    global _CARDPRI
    if _CARDPRI is None:
        _CARDPRI = []
        try:
            here = os.path.dirname(os.path.abspath(__file__))
            with open(os.path.join(here, "tmdata", "CardPri.BIN"), "rb") as f:
                blob = f.read()
            n = struct.unpack_from("<I", blob, 4)[0]
            if 0 < n <= 256 and len(blob) >= 16 + 4 * n:
                _CARDPRI = [struct.unpack_from("<I", blob, 16 + 4 * i)[0]
                            for i in range(n)]
        except Exception as exc:
            _say("tm: CardPri.BIN unreadable (%r) -- sell prices fall back to "
                 "POL_TM_SELL_PRICE" % (exc,))
    return _CARDPRI


def _sell_price(card=None):
    """What one card sells for. See the banner above: ladder[level] * 7/8."""
    forced = os.environ.get("POL_TM_SELL_PRICE")
    if forced:
        try:
            return max(0, int(forced, 0))
        except ValueError:
            pass
    tbl = _cardpri_table()
    # The level is the LAST field the client sends for the card -- index 5 on the
    # `@Sell=` wire row, which `_sell_cards` keeps in the row it hands us.
    lvl = typ = None
    if card is not None:
        # WARNING: THE LEVEL IS THE CARD TABLE'S, BY ID -- NOT THE WIRE'S SIXTH VALUE
        # (2026-09-07, 41i). The client prices at 0xB3840 with `ebx` = the
        # CardPrm ROW -- `[ebx]`, `[ebx+2]`, `[ebx+3]` are the baseline stats a
        # few lines up -- so `[ebx+5]` is CardPrm byte 5 looked up by the card's
        # ID. The sixth value the client SENDS is struct+5 = save rec+7 = what
        # this server stored in row slot 6: the table level until `422bc0c4`,
        # the ARROW MASK since. They were equal on every card of the 08-20
        # measurement, which is the only reason reading the wire ever worked;
        # a Fat Chocobo with arrows 20 then sold for ladder[20] * 7/8 = 6562
        # while the client's own shop showed ladder[6] * 7/8 = 656.
        # `POL_TM_SELL_TABLE_LEVEL=0` restores the wire read for an A/B.
        try:
            t2, l2 = _cardprm(card[0])
        except (TypeError, IndexError):
            t2 = l2 = None
        if _env_int("POL_TM_SELL_TABLE_LEVEL", 1) and l2 is not None:
            lvl = l2
        else:
            try:
                lvl = int(card[5])
            except (TypeError, ValueError, IndexError):
                lvl = l2
        try:
            typ = int(card[2])
        except (TypeError, ValueError, IndexError):
            typ = t2
        if typ is None:
            typ = t2
    if tbl and lvl is not None and 0 <= lvl < len(tbl):
        # THE MULTIPLIER IS THE CARD TYPE, in eighths. See the banner above.
        num = _env_int("POL_TM_SELL_NUM", 0) or (7 + max(0, (typ or 0) - 1))
        den = _env_int("POL_TM_SELL_DEN", 8) or 8
        return max(0, (tbl[lvl] * num) // den)
    # No level (or no table): say so rather than inventing a number silently.
    if card is not None:
        _say("tm:   WARNING: no level for %r -- falling back to a flat price, which "
             "WILL desync the wallet" % (card,))
    try:
        return max(0, int(os.environ.get("POL_TM_SELL_PRICE", "100"), 0))
    except ValueError:
        return 100


#: How many leading fields identify a card. The wire and the stored row agree on
#: id, attack, type, phys def and magic def and then DIVERGE -- measured
#: 2026-08-18 off one real sale:
#:
#:     stored  [220, 55, 0, 78, 80, 19, 18, 2]
#:     @Sell=   220| 55| 0| 78| 80|     18
#:
#: the wire's sixth value is the stored row's SEVENTH. So matching the whole row
#: would never hit. Five is what both sides mean the same way, and an id plus
#: four rolled stats is specific enough that a collision is two genuinely
#: identical cards, where removing either is the same answer.
_CARD_KEY_FIELDS = 5

#: One card entry on the wire: `@N0=/D=...`, `@0=/D=...` -- the prefix
#: differs between what `_card_body` builds and what `@Sell=` sends, so it
#: is matched loosely and only the pipe-separated values are taken.
_CARD_ENTRY_RE = re.compile(rb"@[A-Za-z]*\d+=/D=([0-9|]+)")


def _parse_card_list(cmd):
    """`@<i>=/D=a|b|c|...` -> a list of rows, each `[a, b, c, ...]`.

    The shape `_card_body` builds and both `@Sell=` and `@Decks=` send back. Any
    `/C=` count is deliberately not trusted: the entries are what we act on, and
    a count that disagrees is worth logging rather than obeying.
    """
    out = []
    for m in _CARD_ENTRY_RE.finditer(cmd):
        try:
            out.append([int(x) for x in m.group(1).split(b"|") if x != b""])
        except ValueError:
            continue
    return out


def _erase_cards(member_id, cmd):
    """Drop discarded cards from the collection. No money changes hands.

    The mirror of `_sell_cards` minus the credit -- which also makes the pair a
    useful control: a discard removes a card and moves nothing else, so if a
    balance ever shifts on a discard, the money path is wrong and not the card
    path. Returns None: nothing is waiting for an answer.
    """
    if member_id is None or not _env_int("POL_TM_ERASE", 1):
        return None
    gone = _parse_card_list(cmd)
    if not gone:
        _say("tm: member %s @Erase= carried no card list -- %r"
             % (member_id, cmd[:120]))
        return None
    mode = re.search(rb"/Mode=(-?\d+)", cmd or b"")
    data = _collection_load(member_id)
    have = data.get("cards")
    have = [list(c) for c in have] if isinstance(have, list) else []
    missed = []
    for card in gone:
        key = card[:_CARD_KEY_FIELDS]
        for i, row in enumerate(have):
            if row[:_CARD_KEY_FIELDS] == key:
                have.pop(i)
                break
        else:
            missed.append(key)
    data["cards"] = have
    _collection_store(member_id, data)
    _say("tm: member %s DISCARDED %d card(s) %s (Mode=%s) -- %d card(s) left, "
         "money unchanged at %d"
         % (member_id, len(gone), [list(c) for c in gone],
            mode.group(1).decode() if mode else "?", len(have),
            money_of(member_id)))
    if missed:
        _say("tm: member %s discarded %d card(s) we had NO record of: %s -- the "
             "collection had drifted from the client's"
             % (member_id, len(missed), missed))
    _collection_to_save(member_id, have)
    return None


def _sell_cards(member_id, cmd):
    """`@Sell=` -- the player sold cards. Drop them and credit the money.

    Returns None always: there is nothing to answer (see `_sell_price`). What
    matters is that the collection and the balance both move, because the client
    has ALREADY moved its own and will be shown ours on the next `@ShReq=`.
    """
    if member_id is None or os.environ.get("POL_TM_SELL", "1") != "1":
        return None
    sold = _parse_card_list(cmd)
    if not sold:
        _say("tm: member %s sent @Sell= with no /D= entries -- %r"
              % (member_id, cmd[:80]))
        return None
    data = _collection_load(member_id)
    have = data.get("cards")
    have = [list(c) for c in have] if isinstance(have, list) else []
    # WARNING: PRICE EACH CARD, FROM THE CARD. `_sell_price()` used to be called with
    # NO ARGUMENT here, so it could never reach the ladder and every sale paid
    # the flat fallback -- which is how member 3 ended up 57 gold ahead of its
    # own client while the pricing code was already correct.
    #
    # WARNING: And it must be the WIRE row, not the stored one: the level lives at
    # index 5 and `_CARD_KEY_FIELDS` is 5, so the identity prefix drops exactly
    # the field the price depends on. `SOLD ... [[102, 65, 0, 56, 40]]` in the
    # log was the truncated key, and that truncation was the bug.
    gained, missed, prices = 0, [], []
    for card in sold:
        key = card[:_CARD_KEY_FIELDS]
        for i, row in enumerate(have):
            if row[:_CARD_KEY_FIELDS] == key:
                have.pop(i)
                break
        else:
            # NOT AN ERROR TO SWALLOW. The client sold something we never
            # recorded -- a card from a save we did not write, or a grant we
            # missed. Credit it anyway (the player really did sell it) and say
            # so, because a collection drifting from the client's is exactly
            # what the save work exists to stop.
            missed.append(key)
        _p = _sell_price(card)
        prices.append(_p)
        gained += _p
    data["cards"] = have
    was = money_of(member_id)
    data["money"] = max(0, was + gained)
    _collection_store(member_id, data)
    _say("tm: member %s SOLD %d card(s) %s -- money %d -> %d (+%d: %s), "
          "%d card(s) left"
          % (member_id, len(sold), [list(c) for c in sold], was,
             data["money"], gained,
             " + ".join("lvl %s=%d" % (c[5] if len(c) > 5 else "?", pr)
                        for c, pr in zip(sold, prices)) or "nothing",
             len(have)))
    if missed:
        _say("tm: member %s sold %d card(s) we had NO record of: %s -- credited "
              "anyway; the collection had drifted from the client's"
              % (member_id, len(missed), missed))
    # The price is MEASURED now (four screen readings, one of them an
    # out-of-sample prediction): floor(CardPri[level] * 7/8). What is still
    # fitted is the 7/8 -- 0xB3840 folds card fields into that multiplier -- so
    # keep checking the screen, and a price that is NOT 7/8 of a CardPri entry
    # falsifies the multiplier alone. See the banner above `_sell_price`.
    if any(len(c) <= 5 for c in sold):
        _say("tm: WARNING: a sold card arrived with no level field -- it was priced by "
              "the flat fallback and WILL desync the wallet. Wire row: %r"
              % ([list(c) for c in sold],))
    return None


#: *** THE MONEY MESSAGE. WE HAD NEVER SENT IT, WHICH IS WHY MONEY READ 0. ***
#:
#: `AUCMONEY` is msgid 0x20 on code 0xB2, and its arm is TM.dll rva 0x1060B4:
#:
#:     050960BD  push 0x51c4728            "@Data" -- the body's command key
#:     050960C2  call 0x503a920            ...and the body must start with it
#:     050960D8  cmp  [esi+0x28] / +0x2c   the SENDER ID gate (as SHOPINIT)
#:     050960F9  cmp  [esi+0x30]           the SHOP NUMBER gate  <- this is /N=
#:     05096119  push 0x51c4b2c            "/M=" -- the SAME key SHOPINIT parses
#:     0509611E  call 0x503b080
#:     0509612A  mov  [esi+0xcc], ecx      -> scene+0xCC, a DWORD
#:
#: So `/M=` IS the money -- just not on SHOPINIT, where the very same key is
#: parsed and then thrown away (its value is overwritten by the `/CP=`
#: call before anything reads it). Two messages, one key, one live use.
#:
#: MEASURED SYMPTOM: a tester's money read 0 on BOTH the card shop and the
#: auction screen, through a save carrying 4321 at `tmsave.MONEY_OFF` and a
#: SHOPINIT carrying `/M=3333` and `/CP=1234`. None of them is the wallet,
#: because the wallet arrives here and we were silent.
#:
#: WARNING: THE SHOP NUMBER IN THE HEADER MUST EQUAL `/N=`. `[esi+0x30]` is where
#: SHOPINIT stores `/N=`, and this arm compares the incoming header's shop byte
#: against it -- so a mismatch is a silently dropped message, not an error. The
#: header is `code | msgid << 16 | shop << 24`, which is why this is built rather
#: than a constant. The client confirms the pairing from its own side: served
#: `/N=11`, its next `@Quit=` came back as `sh=11`.
MSG_AUCMONEY = 0x002000B2   #: code 0xB2 + msgid 0x20; the shop byte is OR'd in


def _shop_n():
    """The `/N=` we serve, which is the SHOP NUMBER every later message carries."""
    try:
        return int(os.environ.get("POL_TM_SHOP_N", "1"), 0) & 0xFF
    except ValueError:
        return 1


def _aucmoney_enabled():
    return os.environ.get("POL_TM_AUCMONEY", "1") == "1"


#: *** `@ShReq=` IS TWO DOORS, AND THE msgid IS WHICH ONE. ***
#:
#: Measured on prod 2026-08-19, twice, 28 seconds apart in one session:
#:
#:     20:41:54  GTM0GB0000000@ShReq=...   code 176 msgid 0x00   card shop
#:     20:42:22  GTM0GB0000100@ShReq=...   code 176 msgid 0x01   auction Check Out
#:
#: -- the header layout the banner above `MSG_SHOPINIT` documents (chars 0..1
#: code, 4..5 msgid, 6..7 shop), and authserv decoded the second one as
#: `msgid=0x01` on its own. The handler matched on the body text alone and
#: answered both identically, AUCMONEY included.
#:
#: WARNING: WHICH FABRICATED A SALE. `/M=` means DIFFERENT THINGS on the two scenes:
#: on the card shop it is the wallet (the measurement in the `_start_money`
#: banner: money read 0 on both screens until this message existed), but the
#: auction Check Out screen announces it as THE PROCEEDS OF A CARD SALE. Two
#: machines said so on the same evening, each quoting its OWN member's balance:
#:
#:     member 3   no stored balance -> _start_money() -> "/M=10000" -> sold for 10,000
#:     member 1   stored 4321 (the selftest's own value, `data/resources/
#:                1.tm_collection.json`)  -> "/M=4321"  -> sold for 4,321
#:
#: Two servers, two members, two numbers, each matching that member's wallet.
#: That is our message being read back, not a client-side computation.
#:
#: WARNING: AND WITHHOLDING THE MESSAGE IS **NOT** THE ANSWER -- MEASURED, 2026-08-19,
#: and it is the correction to this banner's own first version. Gating the push
#: off for msgid 0x01 made Check Out hang EARLIER: the screen no longer rendered
#: at all, the client went straight to `@Pong=/Cnt=0` and stayed there. So
#: AUCMONEY is a message the Check Out scene REQUIRES, exactly as SHOPINIT is the
#: one that advances the prize centre (see the `@CvReq=` banner) -- the scene
#: blocks until it arrives.
#:
#: It follows that `/M=` here is not "a wallet we should not have sent", it is
#: **the amount awaiting collection**, and the bug was only ever the VALUE. With
#: no completed auction that amount is 0, which is also what the all-zero
#: `u/g/TM0_DI` says (nothing to collect). So both doors get the message and the
#: AMOUNT is what differs:
#:
#:     msgid 0x00  card shop      /M= = money_of(member)   -- the wallet
#:     msgid 0x01  Check Out      /M= = POL_TM_CHECKOUT_M  -- awaiting collection
#:
#: `POL_TM_AUCMONEY_SHOP_ONLY=1` restores the withhold, which is kept ONLY so the
#: measurement above can be reproduced. It is not a fallback: it is the thing
#: that hung the load.
#:
#: WARNING: PARTLY RETRACTED, 2026-08-19 -- "the bug was only ever the VALUE" IS WRONG.
#: Everything above about the two doors and about AUCMONEY being required stands;
#: the conclusion that only `/M=` remained does not. The client's own log shows
#: the Check Out screen running the CARD SHOP's `@Init` arm, because that is the
#: SHOPINIT body we sent it. See the banner above `_INIT_ARM_FIELDS`: the arm was
#: wrong, which is why chasing this field never moved the screen.
SHREQ_MSGID_CHECKOUT = 0x01


def _aucmoney_shop_only():
    return os.environ.get("POL_TM_AUCMONEY_SHOP_ONLY", "0") == "1"


def _checkout_money():
    """`/M=` for the auction Check Out door -- the proceeds awaiting collection.

    0 until a real auction settles, which is the honest answer and the one that
    agrees with the empty `u/g/TM0_DI`. Overridable because the moment a sale
    DOES settle this stops being a constant, and because it is the field to A/B
    if the screen turns out to want something else here.
    """
    env = os.environ.get("POL_TM_CHECKOUT_M")
    if env is not None:
        # The A/B override still wins, so the field stays testable.
        try:
            return max(0, int(env, 0))
        except ValueError:
            return 0
    return 0


def _aucmoney_line(member_id, amount=None):
    """`AUCMONEY` carrying this member's balance, or None if it is switched off.

    `amount` overrides the wallet -- the Check Out door reads the same `/M=` as
    the proceeds awaiting collection, not as money held. See the banner above
    `SHREQ_MSGID_CHECKOUT`.

    Pushed UNSOLICITED right after SHOPINIT, for the same reason SHOPINIT itself
    is: once the client has the endpoint it stops asking and waits. The arm has
    no reply of its own -- it stores the value and sets a flag.
    """
    if not _aucmoney_enabled():
        return None
    code = MSG_AUCMONEY | (_shop_n() << 24)
    amount = money_of(member_id) if amount is None else amount
    return encode_code(code) + (b"@Data=/M=%d" % amount)


#: `AUCCARDS` -- code 0xB2 msgid 0x21, the arm at TM.dll 0x10613D. Decoded
#: 2026-08-20 by listing the Check Out scene's `0x101A10` waiters: that scene
#: waits on exactly TWO commands, 0x20 (AUCMONEY, the money) and 0x21 (this,
#: the cards). So Check Out's two halves are MESSAGES, which is why the
#: `u/g/TM0_DI` / `_BI` files are loaded and never read on the PC build.
#:
#: The body is the CARD SHOP'S OWN `@Card=` shape -- `0x106200` formats `"@N%d"`
#: in a loop over `/S=` and parses each one's `/D=` -- so nothing new had to be
#: reverse engineered for delivery.
#:
#: WARNING: `/E=` GATES THE WHOLE LIST: `0x1061BC` bails on `<= 0` before reading a
#: single card, so it must be positive or the message is a no-op that looks
#: exactly like one that never arrived.
MSG_AUCCARDS = 0x002100B2


#: EVENT SHOP (via @EInit down the Prize Center door) -- item list, PACED. Members
#: who just opened the event shop and owe an item push on their next @Pong(s).
#: In authsess only (both @CvReq and @Pong are auth-band), so a module dict is
#: safe. member_id -> pushes remaining.
_EVENTSHOP_PENDING = {}


def _eventshop_item_lines(member_id):
    """The event shop's item list -- delivered via @EcmInit (0xB2 msgid 0x13), the
    EVENT CARD-MACHINE init, NOT @Card/@Data.

    MEASURED 2026-08-22 (tmlog store-lookup trace): after @EInit the scene polls
    code 0xB2 with ONLY msgid 0x13 (SHOPINIT family) and 0x27 (@Dead) -- 5776 polls
    each, ZERO of 0x21(@Card)/0x20(@Data). So the item list arrives on the SAME
    0x13 arm as the opener, as @EcmInit, which carries the cards inline: after
    /N /RA /M /CP /S /RT /PM comes /C=<count> then @N<d>/D=<row> records (arm
    0x105788). @EcmInit MUST include /C= or the arm loops @N%d over
    UNINITIALISED STACK and freezes the client (the missing-/C= bug).
    Same 0x13 framing as @EInit (which the scene accepted), so the sender/shop gate
    that rejected @Card does not apply. POL_TM_EINIT_PM tunes /PM=."""
    if os.environ.get("POL_TM_EINIT_CARDS", "1") != "1":
        return []
    n  = _env_int("POL_TM_SHOP_N", 1)
    rt = _env_int("POL_TM_EINIT_RT", 1)
    # WARNING: THE MEMBER'S REAL BALANCE, NOT A CONSTANT. This was a flat 10000, the
    # same invented figure `_start_money` used to carry -- and a shop opener that
    # announces a wallet the member does not have is how the 2026-08-25 grant
    # reached players in the first place. An explicit POL_TM_EINIT_M still wins,
    # because this whole body is a live-bisection experiment.
    m  = _env_int("POL_TM_EINIT_M", money_of(member_id))
    pm = _env_int("POL_TM_EINIT_PM", 0)
    cards = _shopbuy_cards(_env_int("POL_TM_EINIT_PACK", 1))
    # /CP= IS THE CARD LEVEL SLOT (struct +0x00, stored at 0x105836 in this
    # arm) -- the number the VS. COM opponent unlock reads. A 0 here locks the
    # opponent list; see `_comgame_body` and `card_level`.
    body = (b"@EcmInit=/N=%d/RA=0/M=%d/CP=%d/S=1/RT=%d/PM=%d/C=%d"
            % (n, m, card_level_of(member_id), rt, pm, len(cards)))
    for i, v in enumerate(cards):
        body += b"@N%d=/D=" % i + b"|".join(b"%d" % x for x in v)
    return [encode_code(MSG_SHOPINIT) + body]


#: `@Dead` -- code 0xB2 msgid 39, arm TM.dll 0x105DB3. WARNING: THE SHOP SCENE'S
#: ERROR TERMINATOR, NOT AN END-OF-STREAM MARKER -- the 08-21 "stream over +
#: closing notice" double-@Dead model is RETRACTED (static decode 08-21, the
#: whole sequencer read end to end).
#:
#: EVERY state of the Check Out sequencer (0x1333D0, dispatch 0x133FDC /
#: 0x134024) polls 0x27 as an ABORT channel while it walks toward the exit,
#: and an answered @Dead in ANY of them pushes dialog id 221 via
#: 0x1952A0(scene, 0x142600A, 0xDD) and parks the scene in state 0x80,
#: which waits for the user to acknowledge a box that says -- descriptor
#: table 0x2373E8, entry 0xDD, its own inline strings -- "Failed to connect
#: to server." (en) / "the shop master does not exist" (jp), TRM string id
#: -37221. The 08-21T04:56Z "spinner forever, then failed-to-connect, on
#: both machines" was this path working exactly as coded: our @Dead was
#: eaten by state 9/0x0A's abort polls BEFORE the client reached state 0x0B,
#: where it SENDS `@Quit=` (0x101530) -- so it never asked to leave, nothing
#: ever answered msgid 23, and the scene died on its own 60 s timeouts
#: (dialog 343 = "Timed out while disconnecting from the server.").
#: WARNING: The earlier "Aucti.BIN 221 'You sold your card!'" label for this dialog
#: was numerology -- dialog-id space is TRM 37000+id, not Aucti rows.
#:
#: The CLEAN exit needs no push at all: after money B the client walks
#: states 9->0x0B on its own sub-scene events ([0x2EF344] is client-set),
#: sends `@Quit=/No=1` on (0xB2, 0x17), and our `@EQuit=/D=0` answer on the
#: echoed header advances 0x0C -> 0x0D -> 0x0E -> clean teardown (the @EQuit
#: arm 0x105BA3: /D= must be 0, /Stat= 0..3 optional, sender-id and /N= must
#: match the door's @Init -- all four /Stat= arms set result 1).
#:
#: Kept only for the legacy knob (POL_TM_CHECKOUT_STREAM=0) bisect shape.
MSG_AUCDEAD = 0x002700B2


def _aucdead_line():
    """`@Dead` -- "that is all the cards", which ends the Check Out delivery."""
    return encode_code(MSG_AUCDEAD | (_shop_n() << 24)) + b"@Dead="


def _pending_of(member_id):
    """What this member is owed, or an empty purse if the store is unreadable."""
    if tmauction is None:
        return {"money": 0, "cards": []}
    try:
        return tmauction.pending(member_id)
    except Exception:
        return {"money": 0, "cards": []}


def _checkout_money_for(member_id):
    """`/M=` for this member: the env override, else what they are actually owed."""
    env = os.environ.get("POL_TM_CHECKOUT_M")
    if env is not None:
        return _checkout_money()
    return int(_pending_of(member_id).get("money") or 0)


def _auccards_line(member_id, bucket="cards"):
    """`AUCCARDS` for this member -- ALWAYS a message, even with nothing owed.

    `bucket` picks which Check Out card section this fills: "cards" = RETURNED
    (unsold) cards on cards-A ("Returned Card"), "won" = cards WON at auction
    on cards-B ("Successful Bid"). Same wire shape, different pending list.

    WARNING: AN EMPTY LIST STILL HAS TO BE SENT, and getting this wrong hung the
    screen (measured 2026-08-20T21:48Z: Check Out served AUCMONEY alone and sat
    there). The first version returned None when there was nothing to deliver,
    reasoning that `/E=` <= 0 makes the arm bail before reading a field so the
    message is "indistinguishable from silence".

    That confuses two different mechanisms. The ARM bailing is about parsing.
    The WAIT at `0x134F97` is `0x101A10(0x21)` -- it needs command 0x21 to
    ARRIVE at all, and no amount of bailing happens if nothing turns up. The
    scene waits on 0x20 AND 0x21, so answering only the money answers half of
    it and blocks on the other half for ever.

    `/E=0` is therefore the correct way to say "nothing to collect": the
    command is answered, and the arm reads no cards. Answer the command the
    client asks, even when the answer is empty -- the same rule the chat roster
    needed.
    """
    if tmauction is None:
        return None
    pend = _pending_of(member_id)
    buckets = (bucket,) if isinstance(bucket, str) else tuple(bucket)
    cards = []
    for b in buckets:
        cards += pend.get(b) or []
    code = MSG_AUCCARDS | (_shop_n() << 24)
    if not cards:
        return encode_code(code) + b"@Card=/E=0/S=0/C=0"
    body = b"@Card=/E=1/S=%d/C=%d" % (len(cards), len(cards))
    for i, ii in enumerate(cards):
        row = tmauction.card_row_from_ii(ii)
        body += b"@N%d=/D=" % i + b"|".join(b"%d" % v for v in row)
    return encode_code(code) + body


#: *** THE CHECK OUT SCENE IS FOUR SECTIONS, AND THE WHOLE MACHINE IS NOW READ
#: OUT OF TM.dll (2026-08-21, static; rvas at image base 0x04F90000, flat dump).
#: The short version:
#:
#:   * The scene sequencer (inner object update, rva 0x1333D0, state at
#:     [obj+0x17C], jump table 0x133FDC) waits SHOPINIT (0x101A10(0x13)) and
#:     then runs FOUR sub-scenes in order, each with its own 0x101A10 wait and
#:     each OR-ing one bit into the global [0x2EF340] when it had nothing:
#:
#:         money A  rva 0x1348F6  waits cmd 0x20  bit 1   (the 08-19 'Payment:
#:                                                         777' notice was THIS)
#:         cards A  rva 0x134F06  waits cmd 0x21  bit 2
#:         cards B  rva 0x135416  waits cmd 0x21  bit 4
#:         money B  rva 0x135926  waits cmd 0x20  bit 8   (built only after the
#:                                                         @Quit answer, 0x133A3F)
#:
#:     [0x2EF340] == 0xF is what the outer scene (0x132A8A) tests to draw the
#:     "nothing to check out" outcome. Aucti.BIN 336..343 name the sections:
#:     Bid Refund / Collecting Payment / Returned Card / Successful Bid --
#:     WHICH section wears WHICH label is still unmeasured except money A
#:     (its dialog drew "Payment: 777" on 2026-08-19, so it is the collect side).
#:
#:   * So the door needs TWO 0x20 records and TWO 0x21 records queued -- the
#:     store is a queue and each section's wait consumes the next matching
#:     record, exactly the mechanism SHOPINIT already relies on. One AUCMONEY
#:     alone (the previous default) parks the client for ever in cards A's
#:     0x101A10(0x21): measured as the drain poll 0x2C/21/25/23/2A/43/B2/A2.
#:     THAT is why the screen "fails to load" -- this scene has NO base UI; it
#:     renders nothing but its per-section dialogs, so an unanswered section IS
#:     a black screen.
#:
#:   * @Dead (cmd 0x27) is NOT this scene's end-marker. The sequencer tests it
#:     at every section boundary (7 sites: 0x13353F, 0x133636, 0x13371B,
#:     0x133800, 0x1338E5, 0x133B07, 0x133BEB) and an ANSWERED 0x27 raises
#:     Aucti.BIN 221 -- "You sold your card! Collected closing bid amount." --
#:     and parks the scene in state 0x80. Sending @Dead in the entry batch
#:     therefore FABRICATES a sale notice, the AUCMONEY /M= trap in message
#:     form. Do not send it here.
#:
#:   * The 08-21T01:5x "falsification" of the @Dead theory is itself retracted:
#:     authserv.log shows those runs served @EcmInit + /M=0 WITH AUCCARDS and
#:     @Dead still on (fa4fbe60 was not yet deployed at 01:34/01:37Z), and the
#:     777 env never reached the container (/M=0 on the wire, wallet 39260
#:     logged beside it). No run has ever reproduced the 08-19 shape.
#:
#: `POL_TM_CHECKOUT_STREAM=0` restores the pre-stream behaviour (one AUCMONEY
#: plus the POL_TM_AUCCARDS gate) so the broken shape stays reproducible.
def _checkout_stream_enabled():
    return os.environ.get("POL_TM_CHECKOUT_STREAM", "1") == "1"


def _auccards_empty_line():
    """A well-formed EMPTY cards section -- answers a 0x21 wait, delivers 0."""
    code = MSG_AUCCARDS | (_shop_n() << 24)
    return encode_code(code) + b"@Card=/E=0/S=0/C=0"


#: WARNING: MEASURED LIVE 2026-08-21T03:29Z, AND IT RETRACTS THE PRE-QUEUE: a
#: section's wait DRAINS EVERY same-msgid record in the store at once (m3's
#: trace: cards B polling (0xB2,33) against store_count=5, "not found" -- the
#: second @Card had been drained together with the first by cards A; same
#: mechanism the 08-20T22:00Z "the cards HAD been consumed" measured). So the
#: later sections' records must arrive AFTER the earlier section drained, and
#: the client provides the pacing signal itself: **@Get=/No=1 (0xB2 msgid
#: 0x15, no arm -- table 0x106D98 sends 0x15 to the default) is sent when a
#: cards section completes** (sub-scene state 6, 0x106E30(0x15) at 0x13538C).
#: The flow is a PULL: door batch answers sections 1-2, the first @Get
#: releases cards B's record, the second releases money B's -- and that is
#: the whole stream. (The 08-21 note that 0x133A3F "builds money B's
#: sub-scene" was wrong: 0x133A3F is state 0x0C's msgid-23 arm building the
#: EXIT window after our @EQuit answer; money B was measured consumed with
#: no @Quit exchange at 03:42Z.) The exit is client-driven: @Quit= ->
#: @EQuit=/D=0.
_CHECKOUT_PULL = {}

#: WARNING: THE DRAIN RULE SETS THE PACING. Both card sections wait 0x21 and both
#: money sections wait 0x20, and a section's wait DRAINS EVERY same-msgid
#: record in the store at once (03:29Z, 20:28Z: an all-in-door batch left
#: cards B starving after cards A ate both 0x21). So each same-type record
#: must arrive AFTER its predecessor drained -- paced on the @Get a card
#: section sends when it COMPLETES (instantly when empty, after the player's OK
#: when it displayed a card -- confirmed 20:28Z, a card on cards-A displayed
#: and the OK sent the @Get). The synthesis of every measurement:
#:     door        -> money A + cards A (the card)
#:     @Get #1     -> cards B (empty)         [cards A drained, fresh 0x21]
#:     @Get #2     -> money B + @EQuit        [money A drained, fresh 0x20]
#: money B and cards B are pre-built at door time so they survive the clear.
#:
#: WARNING: RETURNED cards -> cards-A ("Returned Card"), WON cards -> cards-B
#: ("Successful Bid"). The earlier "all cards on cards-A" was because cards-B
#: hung -- but that was the SAME missing-terminator bug later found for cards-A
#: (19:37Z cards-B had a card with NO /E=0 terminator, so its receive loop
#: never closed). With the terminator, cards-B should display too. Each card
#: section is [<@Card list> , <@Card /E=0 terminator>] when non-empty; an empty
#: section is just its own /E=0.
_CHECKOUT_PULL = {}
_CHECKOUT_MONEYB = {}
_CHECKOUT_CARDSB = {}


def _auccards_section(member_id, bucket):
    """A card section's records: the @Card list plus a /E=0 TERMINATOR when it
    is non-empty (the receive loop needs it to close and display the card); an
    empty section is a single /E=0, which is its own terminator."""
    line = _auccards_line(member_id, bucket)
    if line and b"/E=1" in line:
        return [line, _auccards_empty_line()]
    return [line or _auccards_empty_line()]


def _checkout_stream_lines(member_id, amount):
    """The door batch: money A (proceeds) + cards A (RETURNED cards + its
    terminator). cards B (WON cards) and money B pace onto the follow-up @Gets
    -- see the banner above (the drain rule). Pre-builds cards B and money B
    before the caller's clear.
    """
    _CHECKOUT_PULL[member_id] = 1
    refund = int(_pending_of(member_id).get("refund") or 0)
    _CHECKOUT_MONEYB[member_id] = _aucmoney_line(member_id, refund)
    _CHECKOUT_CARDSB[member_id] = _auccards_section(member_id, "won")
    lines = []
    money_a = _aucmoney_line(member_id, amount)
    if money_a:
        lines.append(money_a)
    lines += _auccards_section(member_id, "cards")       # cards A = returned
    return lines


def _checkout_pull_lines(member_id, peer_nick=None):
    """@Get #1 (cards A done) -> cards B (WON cards + terminator, fresh 0x21).
    @Get #2 (cards B done) -> money B + @EQuit (fresh 0x20). Each same-type
    record lands after its predecessor drained (the drain rule)."""
    stage = _CHECKOUT_PULL.get(member_id)
    if stage == 1:
        _CHECKOUT_PULL[member_id] = 2
        return _CHECKOUT_CARDSB.pop(member_id, None) or [_auccards_empty_line()]
    if stage == 2:
        _CHECKOUT_PULL[member_id] = 3
        money_b = _CHECKOUT_MONEYB.pop(member_id, None) or _aucmoney_line(
            member_id, 0)
        equit = (encode_code(0xB2 | (0x17 << 16) | (_shop_n() << 24))
                 + (b"@EQuit=/D=0/N=%d" % _shop_n()))
        return [l for l in (money_b, equit) if l]
    return []


def _shopinit_body(member_id=None):
    """`@Init=` plus the six fields arm 0x105201 reads, in its own parse order.

    `/M=` is the member's REAL money now -- see the banner above `_start_money`.
    An explicit `POL_TM_SHOP_M` still wins, because that override is how this
    field's meaning was being A/B'd in the first place.
    """
    out = bytearray(b"@Init=")
    for name, default in _SHOPINIT_FIELDS:
        if name == "CP" and "POL_TM_SHOP_CP" not in os.environ:
            # /CP= IS THE CARD LEVEL SLOT (struct +0x00, this arm's store is
            # 0x1052B1), the number the VS. COM opponent unlock compares
            # against each PlPrm.BIN +0x02 threshold. The `_SHOPINIT_FIELDS`
            # default sent a flat 0, so opening the CARD SHOP zeroed it too.
            # See `card_level` and `_init_body`'s CP branch.
            out += b"/CP=%d" % card_level_of(member_id)
            continue
        if name == "M" and "POL_TM_SHOP_M" not in os.environ:
            out += b"/M=%d" % money_of(member_id)
            continue
        # WARNING: `/CP=` IS CARD POINTS, NOT MONEY -- and this line briefly carried
        # the balance because of that mistake. `/CP=` stores to struct +0x00
        # (0x1052B1 here, 0x102DCA in @ComGameInit), which the save parser fills
        # from file **+0x38**; MONEY is file +0x34 -> struct +0xC8, where `/M=`
        # lands. Putting the wallet in `/CP=` is the same off-by-four the save
        # writer had, in the other direction. See tmsave.MONEY_OFF.
        try:
            v = int(os.environ.get("POL_TM_SHOP_" + name, str(default)), 0)
        except ValueError:
            v = default
        out += b"/%s=%d" % (name.encode("ascii"), v)
    # WARNING: `/PM=` IS THE MONEY, BUT IT IS **NOT A FIELD OF `@Init`** -- CORRECTED.
    #
    # An earlier version of this comment claimed SHOPINIT zeroes the wallet when
    # `/PM=` is absent, and that omitting it was the bug. That was WRONG and the
    # live client said so: served `/PM=4321`, money still read 0.
    #
    # msgid 0x13 handles FOUR DIFFERENT BODY COMMANDS, not one message with
    # variants, and each has its own field set:
    #
    #     @Init     0x105201   /N= /RA= /M= /CP= /S= /SP= /SN= /CMP= /HD=
    #     @ExcInit  0x1054EE   /N= /PP= /LC=
    #     @EInit    0x10563A   /N= /RA= /M= /CP= /S= /RT= /Stat=
    #     @EcmInit  0x105788   /N= /RA= /M= /CP= /S= /RT= **/PM=**
    #
    # `/PM=` and the zeroing at 0x10589A both live in the `@EcmInit` block. We
    # send `@Init`, so neither runs -- the key below is simply IGNORED. It is
    # kept because it costs nothing and reads correctly in a log, NOT because it
    # is known to do anything.
    #
    # WHAT IS STILL TRUE: `[esi+0xcc]` is the money field, written by `@EcmInit`
    # and by `AUCMONEY`, and `/M=` on `@Init` is parsed and discarded.
    # WHAT IS NOT KNOWN: what puts money on the CARD SHOP screen, which is the
    # one the player is reading. Do not add another invented key here.
    #
    # SHOPINIT parses FOURTEEN distinct keys, not the six this file has been
    # serving since the message was first answered:
    #
    #   /N= /RA= /M= /CP= /S= /SP= /SN= /CMP= /HD= /PP= /LC= /RT= /Stat= /PM=
    #
    # The arm (rva 0x105201..0x105a97) is four alternative blocks, each
    # re-reading /N= /RA= /M= /CP= /S=, and the LAST one ends:
    #
    #   0509588E  push 0x51c4770         "/PM="
    #   05095893  mov byte [esi+0xea], 1
    #   0509589A  mov dword [esi+0xcc], 0        <- money ZEROED first
    #   050958A0  call 0x503b080                 <- then parsed
    #   050958AC  mov dword [esi+0xcc], eax      <- money := /PM=
    #
    # `[esi+0xcc]` is the same field AUCMONEY writes, so the two agree on where
    # money lives; `/M=` in this message is parsed and discarded, which is
    # the trap that cost several rounds. A DWORD here, unlike the
    # save's u16 at `tmsave.MONEY_OFF`.
    #
    # THE CHAMPION'S NAME. Read off arm 0x105201 on 2026-09-04: `/SN=` is
    # copied (0xAAD60, fixed 0x11 bytes) into the shop object at +0x148, then
    # formatted as "%s's Pack" (0x105368) and logged as `Now1stPackName=` --
    # it is the label of PackPrm record 5, `rank_1 Pack` (kind 3, 100,000
    # gold, three cards drawn from levels 9..11), Cardsh.BIN's "celebrate
    # <X>'s #1 rank" feature. This is the wiki-documented Tetra Master
    # "event": the top-ranked player's pack in the ordinary Card Shop. The
    # weekly publish names the champion (`tools/tmrank.py`, `tm-champion.json`);
    # with no champion the key is omitted and the shop shows "'s Pack", which
    # is what it has always shown.
    _sn = _champion_name()
    if _sn:
        out += b"/SN=" + _sn
    if os.environ.get("POL_TM_SHOP_PM", "1") != "0":
        out += b"/PM=%d" % money_of(member_id)
    return bytes(out)


#: THE CHAMPION -- last week's #1 by VS. Rating, named by the Sunday publish.
#: `<POL_DATA_DIR>/tm-champion.json` = {"member_id", "name", "week"}. The
#: card shop labels the rank_1 Pack "<name>'s Pack" (`/SN=`, above).
#: `POL_TM_CHAMPION_NAME` overrides the file for a test.
#:
#: WARNING: RETRACTED 2026-09-07: the save header's +0x104 string is NOT the shop's
#: pack label. The shop init at 0x1014EE does copy save +0x104 into the shop
#: object's +0x148 -- but that slot is the FIRST DECK SET NAME, and the first
#: Sunday publish (week 2957) put the champion's name on every
#: player's first deck tab (seen on a tester's card screen). The save stamp is now
#: behind `POL_TM_CHAMPION_SAVE_SLOT` (default 0); the slots are authored from
#: the player's own `@Decks=` name report -- see `SAVE_OFF_DECK_NAMES`.
_CHAMPION_FILE = "tm-champion.json"
_CHAMPION_CACHE = {"mtime": None, "data": None}
SAVE_OFF_CHAMPION = 0x104           #: = SAVE_OFF_DECK_NAMES[0]; stamp OFF by default
SAVE_CHAMPION_WIDTH = 17            #: 16 chars + NUL, the shape of every name


def _champion():
    """{"member_id", "name", "week"} for the current champion, or {}."""
    forced = os.environ.get("POL_TM_CHAMPION_NAME")
    if forced is not None:
        return {"name": forced} if forced.strip() else {}
    path = os.path.join(os.environ.get("POL_DATA_DIR", "/data"), _CHAMPION_FILE)
    try:
        mt = os.path.getmtime(path)
    except OSError:
        return {}
    if _CHAMPION_CACHE["mtime"] != mt:
        try:
            with open(path) as f:
                got = json.load(f)
            _CHAMPION_CACHE["data"] = got if isinstance(got, dict) else {}
        except (OSError, ValueError):
            _CHAMPION_CACHE["data"] = {}
        _CHAMPION_CACHE["mtime"] = mt
    return _CHAMPION_CACHE["data"] or {}


def _champion_name():
    """The champion's name as the client's own 16-byte cp932 field, or b""."""
    name = str((_champion() or {}).get("name") or "").strip()
    if not name:
        return b""
    raw = name.encode("cp932", "replace")[:SAVE_CHAMPION_WIDTH - 1]
    # never split a double-byte character at the cut
    return raw.decode("cp932", "ignore").encode("cp932", "ignore")


#: *** THE CHECK OUT DOOR WAS BEING SET UP AS A CARD SHOP, AND THE CLIENT SAID
#: SO IN ITS OWN LOG. *** Read 2026-08-19; the offsets are TM.dll RVAs and every
#: one of them lands in the table in `_shopinit_body` above:
#:
#:     [tm] +10520B  ---->Recv=SHOPINIT       0x105201 = the `@Init` arm entry
#:     [tm] +105390  Now1stPackName='s Pack   INSIDE `@Init`: the CARD SHOP's
#:                                            pack-name setup, printing a
#:                                            possessive with nothing in front
#:                                            of it -- there are no packs here
#:     [tm] +10610E  ---->Recv=AUCMONEY       ...and then `@Pong=` for ever
#:
#: So AUCMONEY is delivered AND read -- it just lands in a scene that has been
#: initialised as the card shop. That is why no value of `/M=` ever helped (the
#: banner above `SHREQ_MSGID_CHECKOUT` chased the value for a whole evening) and
#: why withholding the message hung it EARLIER rather than fixing it: the amount
#: was never the problem, the ARM was. msgid 0x13 dispatches on the BODY COMMAND,
#: and we sent the card shop's to both doors.
#:
#: WHICH arm Check Out actually wants is NOT measured. The field sets are the
#: ones RE'd in `_shopinit_body`, and the client's log is the oracle -- it names
#: the arm it ran, so one launch per candidate settles it. That is what the knob
#: below is for; it is A/B apparatus, not a fallback.
#: *** `@ExcInit`'s `/PP=` AND `/LC=` ARE MEASURED NOW -- IT IS THE PRIZE
#: CENTER'S OPENER, AND IT CARRIES THE PRIZE-POINT AWARDS. *** Read 2026-08-20
#: at rva 0x105504..0x1055EF. `esi` is the shop object (0x52463E0),
#: and each result is read out of the buffer pushed LAST, one call behind:
#:
#:     /PP= idx 0  -> [esi+0xB4]  u32  the prize-point BALANCE
#:     /PP= idx 1  -> [esi+0xBA]  u16  Top 30 award, PENDING
#:     /PP= idx 2  -> [esi+0xBE]  u16  lucky-card award, PENDING
#:     /PP= idx 3  -> [esi+0xBC]  u16  Best Rookie award, PENDING
#:     /LC= idx 0..2 -> [esi+0xC0/C2/C4] u16  the three LUCKY CARD IDS
#:
#: WARNING: Those indices are PIPE indices inside ONE value, not repeated keys -- the
#: multi-value banner above `CARDPRM_RECORDS` is the law here too. The body is
#: `@ExcInit=/N=1/PP=<bal>|<t30>|<lucky>|<rookie>/LC=<a>|<b>|<c>`, and
#: `tmprize.excinit_body` is what builds it.
#:
#: WARNING: THE THREE AWARD SLOTS ARE PENDING PAYMENTS, NOT HISTORY. The notice screen
#: (0x178DD0) credits them only on OK -- `bal += BA+BC` on its ranking page,
#: `bal += BE` on its lucky-card page -- and zeroes them in RAM as it does. The
#: client never writes `U/g/TM0DataFile`, so the paid/unpaid state is OURS to
#: keep: send a pending award twice and it is paid twice. The balance saturates
#: FLAT at 99,999 (0x1869F), which is NOT money's 99,999,999.
#:
#: WARNING: And the ranking page has no zero case: with both slots 0 it still draws
#: "You've won the Top 30 prize … awarded 0 points!". Open it only with an award
#: to pay. The lucky card IDS are announcement only -- the client has exactly one
#: reference to each (the draw) and picks nothing itself.
#:
#: WARNING: `/C=` IS NOT OPTIONAL ON @EcmInit -- OMITTING IT FROZE A CLIENT ON
#: UNINITIALISED STACK. Measured live 2026-08-21T16:11Z (m9/SYSADMIN1): the
#: client accepted our @EcmInit (`find("@EcmInit") -> hit`) and then spun
#: through `find("@N0")..find("@N20731")..` -- a de facto freeze, no @Pong,
#: watchdog teardown. Read at rva 0x1058D5: AFTER /PM= the arm parses
#: **`/C=` as an inline CARD COUNT** and loops `sprintf("@N%d") / find()`
#: over it, storing rows at `[esi + (0x4B9+idx)*0x10]` (30 slots, zeroed at
#: 0x1058B2, ids gated by [0x2FF4FC]=250). `0xAB080` leaves the out-slot
#: UNTOUCHED when the key is absent, so a missing /C= reads STACK GARBAGE:
#: <= 0 skips the loop (why m3/m6 never froze -- luck), huge spins for
#: minutes. So @EcmInit can DELIVER cards inline -- and until that is used
#: deliberately, /C=0 must be on every body. A find() miss does NOT break
#: the loop (0x105A7D increments and continues).
_INIT_ARM_FIELDS = {
    #  arm          rva        fields, in the arm's OWN parse order
    "Init":     (0x105201, ("N", "RA", "M", "CP", "S", "SP", "SN", "CMP", "HD")),
    "ExcInit":  (0x1054EE, ("N", "PP", "LC")),   # /PP= x4, /LC= x3 -- see above
    "EInit":    (0x10563A, ("N", "RA", "M", "CP", "S", "RT", "Stat")),
    "EcmInit":  (0x105788, ("N", "RA", "M", "CP", "S", "RT", "PM", "C")),
}

#: Defaults for the fields `_SHOPINIT_FIELDS` does not already carry. All zero,
#: and zero is the only value that cannot later be mistaken for a measured one.
#: `PP` and `LC` now HAVE meanings (see the banner above `_INIT_ARM_FIELDS`), and
#: zero is the right default for both anyway: no award pending, no lucky card.
#: `C` is @EcmInit's inline-card count and MUST default 0 -- see the banner
#: above `_INIT_ARM_FIELDS`; absent, the client reads stack garbage and can
#: spin for minutes. Each is `POL_TM_SHOP_<FIELD>`, same as the rest.
_INIT_ARM_DEFAULTS = {"SN": 0, "CMP": 0, "HD": 0, "PP": 0, "LC": 0, "RT": 0,
                      "Stat": 0, "C": 0}


def _checkout_init_arm():
    """Which of the four bodies the auction Check Out door is sent.

    `POL_TM_CHECKOUT_INIT=Init|ExcInit|EInit|EcmInit`, read per request, so both
    candidates can be tried against the live client without a redeploy each --
    the point being that the client's own `---->Recv=` line arbitrates, not us.

    The default is `EcmInit` and it is a REASONED GUESS, not a measurement: of
    the three non-shop arms it is the only one that writes `[esi+0xcc]` (`/PM=`,
    zeroed at 0x10589A and then parsed), and `[esi+0xcc]` is the field AUCMONEY
    writes -- so it is the arm whose story matches a screen whose entire job is
    an amount awaiting collection. `Init` stays selectable ONLY so the broken
    behaviour this replaces can be reproduced on demand.
    """
    name = os.environ.get("POL_TM_CHECKOUT_INIT", "EcmInit")
    if name not in _INIT_ARM_FIELDS:
        _say("tm: POL_TM_CHECKOUT_INIT=%r is not one of %s -- using EcmInit"
              % (name, "/".join(sorted(_INIT_ARM_FIELDS))))
        return "EcmInit"
    return name


#: *** THE ARM WAS NEVER THE GATE, AND THE CLIENT'S DRAIN LOOP SAYS SO. ***
#:
#: `@EcmInit` reached the Check Out scene and was ACCEPTED -- measured on CASPC,
#: 2026-08-20, the client's own trace, and it also shows the four-arm dispatch
#: working exactly as `_INIT_ARM_FIELDS` describes:
#:
#:     [tm] +10520B  ---->Recv=SHOPINIT
#:     [tm] find("@Init")    on obj 001AEE2C -> 0 (MISS -- caller bails)
#:     [tm] find("@EcmInit") on obj 001AEE2C -> 1 (hit)
#:     [tm] +10610E  ---->Recv=AUCMONEY
#:
#: `Now1stPackName=` is GONE, which is the card-shop pack-name setup no longer
#: running. So the arm change did what it was for -- and the screen still hangs,
#: because the scene then drops into a fixed poll and stays there for ever:
#:
#:     code=0x2C / 0x21 / 0x25 / 0x23 / 0x2A / 0x43 / 0xB2 / 0xA2  -> not found
#:
#: That list is the scene's ACCEPT SET (0xB2 is the entry that read FOUND when
#: SHOPINIT and AUCMONEY arrived), and 0xB2 is the only one of the eight we have
#: ever sent. No value of `/M=` and no choice of arm was ever going to satisfy
#: it: Check Out is waiting for a message we have never written.
#:
#: The command-table reading puts `@Tr=` at 0x21/0x25 and leaves 0x23/0x2A/0x2C unrecovered, with
#: `@TrAns=` `@TrCan=` `@TrCanAck=` still unaccounted for -- the trade family,
#: which is what a screen for collecting the results of trades should want.
#:
#: *** THE PROBE, AND WHY IT NEEDS NO GUESSWORK ABOUT BODIES. *** The store
#: lookup matches on the CODE ALONE, and when a message IS taken the client logs
#: every key it tries against it (`find("@Init") -> MISS`, `find("@EcmInit") ->
#: hit`, above). So one message per code with a deliberately unmatchable body
#: makes the client read out the vocabulary of each arm for us. That is the whole
#: trick: we do not have to know the body to learn what the body must be.
#:
#: WARNING: THIS SENDS MESSAGES WE DO NOT UNDERSTAND TO A LIVE CLIENT. The scene is
#: measured 100% stuck, so there is no working behaviour to lose, but a bad frame
#: could wedge or crash the title -- restart the game and it is gone. Set
#: `POL_TM_CHECKOUT_PROBE=` (empty) to switch it off; it is DELIBERATELY on by
#: default while Check Out is dead, because each run of it costs a deploy.
#: *** SOLVED, STATICALLY: `0x2C` TESTS THE MSGID, AND WE SENT ZERO. ***
#:
#: The store fills the message object's tail members with `0xA9780(off)`, which
#: reads TWO ASCII HEX DIGITS out of the raw text buffer at `this+0x25`:
#:
#:     mov edx, [esp+4]            ; the argument is a CHARACTER OFFSET
#:     mov al, [edx + ecx + 0x25]  ; nibble, 'A'-0x37 / '0'-0x30, shl 4
#:     mov al, [edx + ecx + 0x26]  ; ...or the second nibble
#:
#: and the pump stores offset 0 -> +0x3C8, offset 4 -> +0x3CC, offset 6 -> +0x3D0.
#: Those are the three fields of our own header, which decodes it beyond doubt:
#:
#:     B2001300@EcmInit=   off 0 "B2" code   off 4 "13" msgid   off 6 "00" shop
#:     B2002001@Data=/M=0  off 0 "B2"        off 4 "20"         off 6 "01"
#:
#: So `+0x3CC` IS THE MSGID, and the 0x2C arm at 0x08CC7F is:
#:
#:     cmp dword [esp+0x3e8], ebx   ; ebx = 0 -- the msgid, tested for zero
#:     je  0x8cca0
#:     mov byte [0x52136b0], cl     ; msgid != 0 -> flag := 1, return +1
#:     mov byte [0x52136b0], bl     ; msgid == 0 -> flag := 0, return -1
#:
#: Our first probe was `2C000000` -- msgid ZERO -- so it took the second branch
#: and told the scene NO. The message was never unheard; it was answered in the
#: negative. `0x2C:1` is the same message with the one field that matters set.
#: WARNING: AND IT CHANGES NOTHING, MEASURED -- so the default is OFF again. The flag
#: IS set (`out BODY@+0x25='2C000100@Probe=/P=0'` on every run) and the Check Out
#: drain list is byte-identical before and after it: 0x2C 0x21 0x25 0x23 0x2A
#: 0x43 0xB2 0xA2, 368 cycles of each in the last 4000 log lines, no new codes.
#:
#: An earlier reading of this file claimed the scene ADVANCED to wait on 0x41 and
#: 0x1A. That was taken from the TAIL of the shim log rather than from the lines
#: after that run's `Recv=AUCMONEY`, so it was a different screen entirely --
#: retracted. To attribute a
#: drain list to a scene, read the lookups that FOLLOW that scene's own `Recv=`.
#:
#: So the probe is off: it ships a frame that is measured to do nothing, on a
#: screen we do not understand. `POL_TM_CHECKOUT_PROBE=0x2C:1` restores it, and
#: the machinery is kept because the NEXT candidate wants exactly this shape.
_CHECKOUT_PROBE_DEFAULT = ""


def _checkout_probe_codes():
    """The codes to poke the stalled Check Out drain loop with, in order."""
    raw = os.environ.get("POL_TM_CHECKOUT_PROBE", _CHECKOUT_PROBE_DEFAULT)
    out = []
    for tok in raw.split(","):
        tok = tok.strip()
        if not tok:
            continue
        # `code[:msgid]` -- the msgid is NOT decoration here, it is the field the
        # 0x2C arm branches on. Default 1, because 0 is the measured "no".
        code_s, _, msgid_s = tok.partition(":")
        try:
            code = int(code_s, 0)
            msgid = int(msgid_s, 0) if msgid_s else 1
        except ValueError:
            _say("tm: POL_TM_CHECKOUT_PROBE: %r is not a number -- skipped" % tok)
            continue
        if 0 <= code <= 0xFF:
            out.append((code, msgid))
        else:
            _say("tm: POL_TM_CHECKOUT_PROBE: 0x%X is not a message code -- "
                  "skipped" % code)
    return out


def _checkout_probe_lines():
    """One message per probe code, each with a body no arm can match.

    `@Probe=` is chosen to MISS on purpose: a hit would run an arm we know
    nothing about, and a miss is what makes the client enumerate the keys it
    tried -- which is the measurement. The code is the only part that has to be
    right, because that is all the store lookup compares.
    """
    lines = []
    for code, msgid in _checkout_probe_codes():
        lines.append(encode_code(code | (msgid << 16)) + b"@Probe=/P=0")
    return lines


def _init_body(arm, member_id=None, amount=None):
    """`@<arm>=` plus exactly the fields that arm parses, in its parse order.

    `amount` is what `/M=` and `/PM=` carry when the caller knows better than the
    wallet -- the Check Out door's proceeds awaiting collection, the same number
    `_aucmoney_line` is handed, so the two messages cannot disagree about what
    the screen is being told.

    `@Init` DELEGATES to `_shopinit_body` unchanged. The card shop is the one
    door that is not in question here, and its body stays byte-for-byte what it
    has been -- including the `POL_TM_SHOP_M` / `POL_TM_SHOP_PM` knobs, which are
    deliberately NOT honoured on the other arms: `POL_TM_SHOP_PM` is a 0/1 toggle
    over there, and reading it as a VALUE here would quietly serve `/PM=1` to
    anyone who has it set. Use `POL_TM_CHECKOUT_M` for this door's amount.
    """
    if arm == "Init":
        return _shopinit_body(member_id)
    money = money_of(member_id) if amount is None else amount
    shop_defaults = dict(_SHOPINIT_FIELDS)
    out = bytearray(b"@" + arm.encode("ascii") + b"=")
    for name in _INIT_ARM_FIELDS[arm][1]:
        if name == "N":
            # MUST equal the AUCMONEY header's shop byte -- the arm at 0x1060F9
            # compares it against [esi+0x30], where this very field was stored.
            out += b"/N=%d" % _shop_n()
            continue
        if name == "M":
            # Parsed and discarded by the arm -- carry the amount.
            out += b"/M=%d" % money
            continue
        if name == "PM":
            # WARNING: `/PM=` IS THE WALLET BASE, NOT THE AMOUNT. The arm zeroes
            # [esi+0xcc] then sets it to /PM= (0x10589A/0x1058AC), and the
            # money A/B AUCMONEY messages ADD their /M= on top. So /PM= MUST be
            # the player's real wallet -- /PM=0 (the old value, = the proceeds
            # for a refund-only checkout) wiped the wallet, then money B added
            # the 200 refund -> the screen read 200 instead of 9800+200
            # (measured live 2026-08-22T00:05Z). money_of is read
            # BEFORE the collect credits the wallet, so it is the pre-collect
            # balance the AUCMONEY amounts correctly add to.
            out += b"/PM=%d" % money_of(member_id)
            continue
        if name == "CP":
            # WARNING: RETRACTED 2026-09-08: "/CP= LANDS ON THE MONEY FIELD".
            #
            # The store is real -- `0x10528F push "/CP=" / 0x1052B1 mov word
            # [esi], cx` -- but struct +0x00 is NOT the wallet. The wallet is
            # struct +0xC8 (`/M=`, written at 0x102DA9 and 0x105816, and the
            # global the VS. COM pot clamps against at 0xEC82C). Struct +0x00
            # is the CARD LEVEL: the save parser derives it there from the
            # collection (0x10146E -> 0x10147F), SEVEN sites recompute it
            # during play (0xCB652, 0xDA648, 0xEAEFD, 0xEFFD3, 0xF0075,
            # 0x117ABC, 0x12D046), and every reader compares it against a
            # `PlPrm.BIN` record's +0x02 unlock threshold -- 0xEC52C, 0xEC763,
            # 0xECAE0, 0xEBC4F and 0x110AE4, the VS. COM opponent gate. No
            # reader anywhere displays it as money.
            #
            # So the 2026-08-20 "both players show 0 money" fix was aimed at
            # the wrong field. What actually zeroed the shop's money that day
            # is named in this same file: `/M=` was the flat `_SHOPINIT_FIELDS`
            # constant, and `money_of` is what repaired it. Sending the BALANCE
            # here has since been writing the player's gil into their card
            # level -- 10,805 gil reads as card level 10805, over every
            # threshold in the ladder (max 2300), so a shop visit unlocked
            # every VS. COM opponent and the next VS. COM open (which sent
            # /CP=0) locked them all again.
            #
            # WARNING: The selftest below has asserted the corrected reading since
            # 2026-08-20 and never fired, because its fixture is a member with
            # no money: `0 == 0` skips the compare. An assertion whose fixture
            # cannot violate it is not a test.
            #
            # POL_TM_SHOP_CP still forces a number for an A/B.
            forced = os.environ.get("POL_TM_SHOP_CP")
            out += b"/CP=%d" % (_env_int("POL_TM_SHOP_CP", 0) if forced
                                else card_level_of(member_id))
            continue
        default = shop_defaults.get(name, _INIT_ARM_DEFAULTS.get(name, 0))
        try:
            v = int(os.environ.get("POL_TM_SHOP_" + name, str(default)), 0)
        except ValueError:
            v = default
        out += b"/%s=%d" % (name.encode("ascii"), v)
    return bytes(out)

#: `/Ans=` must be POSITIVE -- 0x003a92f0 returns it and its caller `blez`es on
#: the result. Env-overridable because the value may be an id rather than an OK.
#: `POL_TM_INIT=0` silences the reply again (back to the captured-and-silent
#: behaviour) without a code change, which is the clean way to A/B it.
#: CARD PERSISTENCE -- and the client hands us both halves unprompted.
#:
#: The problem: TM.dll has no save-write API. It only ever calls
#: `sqMgCpLoadPlayerSaveData`, so `U/g/TM0DataFile` is a file SE's server owned
#: and served; the collection itself lives at `0x5246F10` with its count at
#: `0x52464CC` and is never written back anywhere. A purchase therefore dies with
#: the process, which is exactly what live testing hit: five cards and a deck at
#: 05:03, gone by the next launch.
#:
#: What makes this tractable is that BOTH halves already cross our wire:
#:
#:     @Card=   OUR OWN reply to `@Buy=` -- the cards we granted, with stats
#:     @Decks=  the CLIENT reporting its deck back to us, unprompted, whenever
#:              it changes. Measured 2026-08-17:
#:                @Decks=/C=5@0=/D=220|55|0|78|80|18|0@1=/D=215|90|2|...
#:                @Decks=/C=0@DN1=/S=59454148        <- "YEAH", the deck's NAME
#:              (msgid 0x22, and it has no arm -- it is the
#:              client TELLING us, so it wants recording, not answering.)
#:
#: So we can record a player's collection exactly, with no RE of the save format
#: at all. That is what this does, and it is the half with no unknowns.
#:
#: VERIFIED: DELIVERY IS SOLVED, AND IT WAS NEVER A MESSAGE (2026-08-18). The paragraph
#: above is right that TM.dll has no save-WRITE api and wrong about what follows
#: from that: the client LOADS its collection from `U/g/TM0DataFile`, so the save
#: is the delivery channel in the only direction that exists. `services/tmsave.py`
#: carries the measured layout -- count u16 at +0x3C, 1000 records of 12 from
#: +0x148 -- and `_collection_to_save` writes it on every collection change.
#: `POL_TM_SAVE_WRITE=0` reverts to record-only.
#:
#: What made this hard to see: an earlier reading concluded "the collection is never loaded
#: from the save" from a scan for ABSOLUTE references into the save buffer, which
#: cannot see an indexed loop off a register -- and that is exactly how the card
#: region is walked. The check that broke the deadlock was not a better scan, it
#: was noticing that otherwise every retail player would have logged in with zero
#: cards until they visited the shop.
#:
#: WARNING: AND IT EXPLAINS THE INFLATED RECORDS. Serving a zero header re-cleared the
#: shop's "already bought" flags every launch, so the FREE Pauper's Pack was
#: buyable for ever and the recorder banked cards no real player could hold (30
#: on member 1). Preserving the header is not cosmetic -- it is what stops that.
#:
#: `POL_TM_CARD_RESTORE=1`'s unsolicited `@Card=` push is left in place but is no
#: longer the plan: it was the least-invented guess when the save looked closed,
#: and the save is not closed. Recording stays unconditional because it costs
#: nothing and cannot be wrong.
_COLLECTION_SUFFIX = ".tm_collection.json"


def _collection_dir():
    """POL_RESOURCE_DIR first, exactly as `_save_file` resolves it and for the
    same reason -- deriving it from POL_DATA_DIR alone agrees only until
    something sets the override."""
    root = os.environ.get("POL_RESOURCE_DIR")
    if not root:
        root = os.path.join(os.environ.get("POL_DATA_DIR", "/data"), "resources")
    return root


def _collection_file(member_id):
    """Where a member's collection is kept. Beside the resources, because that
    directory is already the bind-mounted per-member state we serve from, but
    under its own suffix so it can never collide with a POL resource path."""
    if member_id is None:
        return None
    return os.path.join(_collection_dir(), "%s%s" % (member_id, _COLLECTION_SUFFIX))


def _collection_load(member_id):
    path = _collection_file(member_id)
    if not path:
        return {}
    try:
        with open(path) as f:
            data = json.load(f)
    except (OSError, ValueError):
        return {}
    return data if isinstance(data, dict) else {}


def _save_resource_file(member_id):
    """The member's `U/g/TM0DataFile`, named the way `responders._resource_file`
    names it -- member-scoped, because a save is user-owned and is deliberately
    NOT in `_SUBJECT_KEYED_PATHS`."""
    if member_id is None:
        return None
    return os.path.join(_collection_dir(), "%s.U_g_TM0DataFile.bin" % member_id)


def _collection_to_save(member_id, cards):
    """Write the collection into the member's save, so it survives a relaunch.

    THIS IS THE DELIVERY HALF, and it is no longer an open question: the client
    loads its collection FROM the save (`services/tmsave.py` has the measured
    layout and the chain that proves it). Recording without this step is what
    made a purchase die with the process.

    WARNING: THE HEADER IS PRESERVED, NEVER REBUILT. Money, rank and the shop's
    "already bought" flags all live in +0x00..0x147, and the Pauper's Pack bit is
    among them. Dropping that header is what let the FREE pack be re-bought on
    every launch and inflated the recorded collections past anything a real
    player could hold. So an existing save is read and used as the base; if there
    is none, a zero header is the only honest starting point, and the first
    purchase is what fills it.
    """
    if tmsave is None or os.environ.get("POL_TM_SAVE_WRITE", "1") != "1":
        return False
    path = _save_resource_file(member_id)
    if not path:
        return False
    base = None
    try:
        with open(path, "rb") as f:
            base = f.read()
    except OSError:
        pass
    try:
        # MONEY GOES IN THE SAVE, not in SHOPINIT. `/M=` does not feed the shop's
        # Money display -- measured 2026-08-18 by serving `/M=4321` and watching
        # the screen stay at 87 across a shop exit and re-entry. The balance
        # comes from the save header at `tmsave.MONEY_OFF`, which we had been
        # writing as zeros, which is why every launch started the player at 0.
        if _deck_slots_on():
            # THE DECK SLOTS: byte 8 of every record is the card's position
            # (255 = none), never the CardPrm column -- see DECK_SLOT_COUNT.
            _data = _collection_load(member_id) or {}
            _slots = _deck_slot_bytes(_data, cards)
            cards = [(list(c) + [0] * 8)[:7] + [_slots[i]]
                     for i, c in enumerate(cards)]
        blob = tmsave.build(cards, base, money=money_of(member_id))
        _buf = bytearray(blob)
        # WARNING: OFF BY DEFAULT since 2026-09-07: +0x104 is the FIRST DECK TAB's
        # name, not the shop's pack label -- a tester's card screen read
        # the week's champion there. `POL_TM_CHAMPION_SAVE_SLOT=1` restores
        # the stamp; the shop's "<X>'s Pack" still rides `@Init=/SN=`.
        # THE DECK SET NAMES, from the player's own `@Decks=` name report.
        for _off, (_w, _val) in sorted(_deck_name_fields(member_id).items()):
            tmsave.write_field(_buf, _off, _w, _val)
        if _env_int("POL_TM_CHAMPION_SAVE_SLOT", 0):
            _cn = _champion_name()
            if _cn:
                tmsave.write_field(_buf, SAVE_OFF_CHAMPION, SAVE_CHAMPION_WIDTH,
                                   _cn)
        blob = bytes(_buf)
    except ValueError as e:
        # encode_card refuses rows the client would SILENTLY drop. Say which,
        # and leave the existing save alone rather than shipping a worse one.
        _say("tm: save NOT rewritten for member %s -- %s" % (member_id, e))
        return False
    try:
        os.makedirs(_collection_dir(), exist_ok=True)
        tmp = path + ".tmp"
        with open(tmp, "wb") as f:
            f.write(blob)
        os.replace(tmp, path)
    except OSError as e:
        _say("tm: could not write %s: %s" % (path, e))
        return False
    _say("tm: member %s save rewritten -- %d card(s) at +0x%X, count at +0x%X"
          % (member_id, len(cards), tmsave.CARDS_OFF, tmsave.COUNT_OFF))
    return True


def _collection_store(member_id, data, sync_save=True):
    path = _collection_file(member_id)
    if not path:
        return False
    try:
        os.makedirs(_collection_dir(), exist_ok=True)
        # tmp + os.replace, like `_collection_to_save` above and `_save_patch`.
        # This file is the wallet/collection OF RECORD (it also carries the
        # durable staked_wager), and the old truncate-in-place write meant a
        # crash mid-write -- or two of this member's threads interleaving --
        # left corrupt JSON; `_collection_load` then returns {} and the NEXT
        # write persists the empty record: cards, money, stats silently reset
        # (09-02 review M3). Per-writer tmp name for the same reason as the
        # live-match marker's.
        tmp = "%s.tmp.%d.%d" % (path, os.getpid(), threading.get_ident())
        with open(tmp, "w") as f:
            json.dump(data, f, indent=1, sort_keys=True)
        os.replace(tmp, path)
    except OSError as e:
        _say("tm: could not write %s: %s" % (path, e))
        return False
    # The JSON is our record; the SAVE is what the client actually reads. Keep
    # them in step here rather than at each call site, so nothing can record a
    # card without also delivering it. `sync_save=False` is for changes to
    # fields that do NOT live in the save (the `rank` stats block) -- rewriting
    # the save for those would be churn with no delivery in it.
    if sync_save:
        _collection_to_save(member_id, data.get("cards") or [])
    return True


#: HOW MANY OF A PACK THE CLIENT ACTUALLY KEEPS. **One.** Measured off the shop
#: scene's own state table (TM.dll rva 0x179690, eight states; image base
#: 0x04F90000) -- state 0 sends `@Buy=`, state 6 is the acquire:
#:
#:     0x17961D  mov  ecx, [0x52464CC]        the COLLECTION count
#:     0x179623  push 0x524AF70               src = STAGING SLOT 0, not a picked
#:                                            index -- there is no index here
#:     0x179637  call 0xB8180                 copies ONE 16-byte record
#:     0x17963C  inc  word [0x52464CC]        collection count += 1
#:     0x17964E  mov  word [0x52464D0], 0     and the rest of the pack is GONE
#:     0x179657  call 0x106E30(0x15)          only THEN is `@Get=` sent
#:
#: Two things fall out and both matter more than the arithmetic:
#:
#: 1. **`@Get=` is a NOTIFICATION, not a request.** The drain runs BEFORE the
#:    send, unconditionally, in the same basic block. Our silence on it costs
#:    nothing, which is why the client has always quit the shop cleanly. Nothing
#:    is waiting for a reply we have not written.
#: 2. **A pack grants exactly ONE card, and it is the FIRST in our `@Card=`
#:    list.** Recording all of `/S=5` therefore over-counted five-fold -- 18
#:    acquires across both logs had produced a 30-card file for a player who can
#:    hold at most 18. The wire agrees: every `@Buy=/No=0` in the logs is
#:    followed by exactly one `@Get=/No=1`, 18 times out of 19, and `/No=` never
#:    varies, so it is a count and not a chosen slot.
#:
#: WARNING: RETRACTED 2026-08-18 -- SCREEN-VERIFIED, AND IT WAS BACKWARDS. The check
#: named directly above was run: buy the Pauper's Pack, open the collection, and
#: the tester counts **FIVE** cards, not one. The whole pack is kept.
#:
#: So `@Get=/No=1` is ONE ACQUISITION EVENT, not one card, and every inference
#: built on reading it as a card count was wrong -- including "18 acquires can
#: hold at most 18 cards", which is what made the 30-card file look like 5x
#: over-counting when it was simply six packs. The static read of shop state 6
#: (rva 0x17961D, "copies staging slot 0 and zeroes the staging count") described
#: one iteration of a loop, not the whole transfer.
#:
#: WARNING: THE LESSON, because this cost a real debugging round: a static trace of one
#: state said one thing, the tester's screen said another, and the screen was
#: right both times it was consulted. `tetramaster.py` had ALREADY recorded the
#: contradicting observation -- "five cards and a deck at 05:03, gone by the next
#: launch" is in this file, above -- and it was read as a symptom of the loss bug
#: instead of as evidence about pack size.
#:
#: `POL_TM_COLLECTION_RECORD=acquire` restores the one-card behaviour.
_ACQUIRED_PER_PACK = 5


def _record_on_grant():
    return os.environ.get("POL_TM_COLLECTION_RECORD", "acquire").strip().lower() == "grant"


def _collection_add_cards(member_id, cards):
    """Record cards we just granted. Appends -- a pack is an acquisition, not a
    replacement, and the client's own collection grows the same way."""
    if member_id is None or not cards:
        return
    data = _collection_load(member_id)
    have = data.get("cards")
    data["cards"] = (have if isinstance(have, list) else []) + [list(c) for c in cards]
    if _collection_store(member_id, data):
        _say("tm: member %s collection += %d card(s), %d total"
              % (member_id, len(cards), len(data["cards"])))


def _live_matches_write():
    """`<POL_DATA_DIR>/tm-matches-live.json` -- the DEPLOY GATE's input.

    Deploy restarts wiped four live games' state in one afternoon
    (2026-08-22: a wedged board, two eaten stakes, a hung rematch vote).
    `pol-git-sync` / `pol-stale-check --restart` now DEFER recreating
    login/authsess while this marker says matches are live and fresh; the
    stale-check backstop retries every 3 minutes, so the deferred restart
    lands as soon as the last match ends. Touched by every match-flow
    handler -- INCLUDING `@CardSelect=`, so the pre-deal card-select phase is
    inside the window and not the blind spot it was until 2026-08-25 -- so the
    stamp stays fresh through the post-game screens; a
    wedged match stops touching it and the checkers' grace window expires.
    POL_TM_LIVE_MARKER=0 disables the writes (the checkers then never
    defer).
    """
    _watch_touch()                   # the web watch file rides the same beats
    if not _env_int("POL_TM_LIVE_MARKER", 1):
        return
    try:
        path = os.path.join(os.environ.get("POL_DATA_DIR", "/data"),
                            "tm-matches-live.json")
        # WARNING: THE WINDOW IS THE WHOLE MATCH, NOT JUST THE DEALT PART. This
        # counted `_MATCH_TURN`, which is not created until THE DEAL
        # (`@CardSelect=` from the last player) -- so a match sitting in CARD
        # SELECT reported 0 and both checkers restarted straight through it.
        # Measured 2026-08-25T03:01:05Z: pol-git-sync recreated login/authsess
        # for efbd5e6c->1658e3a9 while a 2-player match was picking cards; 8s
        # later the fresh process took member 16's `@CardSelect=` with no
        # roster and dealt a ONE-SIDED game, and the table's observer stayed
        # parked on "players are selecting their cards" for the rest of the
        # night. `_MATCH_STARTED` is the right window -- it opens on the host's
        # `@GameReady=` (Start Game) and closes on the room-return `@GameExit=`,
        # so it spans card select, the deal, play and the post-game screens.
        # `_CARD_READY` is belt-and-braces for a COM game, which never takes a
        # `_MATCH_STARTED` entry -- and `_COM_AT` closes the rest of that hole.
        # A COM game fills NONE of the three stores above until its first
        # `@CardSelect=`, so everything before that -- character select, the
        # rules dialog, the wager -- was still a blind spot after the
        # 2026-08-25 widening (measured on the wire: `@GameENC=` 02:43:49Z,
        # the `@ComGame=` roster 02:44:51Z -- over a minute of it). `_COM_AT`
        # is the whole window by construction: bound at `@GameENC=`, released
        # on the room-return `@GameExit=`. It also keeps a finished COM game
        # counted through the post-game screens, where the other three are
        # cleared for the rematch.
        # WARNING: A key present in two stores under different index TYPES would count
        # twice. That is deliberate: over-counting only DEFERS a restart, which
        # is the safe direction, and the stamp's own staleness bounds it.
        keys = set(k for k, v in _MATCH_STARTED.items() if v)
        keys |= set(_MATCH_TURN)
        keys |= set(_CARD_READY)
        keys |= set(_COM_AT)
        # THE ACCEPT WINDOW (Start Game -> deal). Measured 2026-09-03T22:57Z:
        # a restart between two `@GameOK=` wiped the quorum and the match never
        # dealt. `_MATCH_STARTED` covers it once written from `@GameReady=`;
        # a pending accept set is counted too, for a roster that reached
        # `@GameOK=` on a table this process never saw start.
        keys |= {("accept",) + k for k, v in _MATCH_ACCEPTS.items() if v}
        # WARNING: TRADES AND ARMED CHECKOUTS ARE LIVE SESSIONS TOO (09-02 review
        # H5). A recreate mid-trade used to sail through on "0 live matches",
        # wipe `_TRADE_PENDING`, and park both clients on the trade board
        # polling (0xA2,28) for a completion that no process remembers --
        # regression #25's shape, one subsystem over. Key sets are disjointly
        # prefixed because over-counting only DEFERS a restart (see above),
        # but under-counting eats somebody's evening.
        keys |= {("trade",) + (k if isinstance(k, tuple) else (k,))
                 for k in _TRADE_PENDING}
        keys |= {("checkout",) + (k if isinstance(k, tuple) else (k,))
                 for k in _CHECKOUT_PULL}
        keys |= {("eventshop",) + (k if isinstance(k, tuple) else (k,))
                 for k in _EVENTSHOP_PENDING}
        blob = json.dumps({"count": len(keys),
                           "stamp": time.time()})
        # Per-writer tmp name: every handler thread used to share `path +
        # ".tmp"`, so two concurrent writers could interleave into one file
        # and os.replace() could publish corrupt JSON -- which the external
        # checkers may read as "0 live" (the unsafe direction).
        tmp = "%s.tmp.%d.%d" % (path, os.getpid(), threading.get_ident())
        with open(tmp, "w") as f:
            f.write(blob)
        os.replace(tmp, path)
    except Exception as e:
        # Best-effort by design -- but a PERMANENTLY failing write (perms,
        # disk full) used to disable the deploy gate with no symptom at all.
        # Say so once per process.
        global _LIVE_MARKER_WHINED
        if not _LIVE_MARKER_WHINED:
            _LIVE_MARKER_WHINED = True
            _say("tm: WARNING: could not write the live-match marker (%r) -- the "
                 "deploy gate is BLIND until this is fixed (said once)" % (e,))


# ---------------------------------------------------------------------------
# THE WEB WATCH FILE -- `<POL_DATA_DIR>/tm-tables-live.json` (2026-09-13)
#
# The board service (services/boardtm.py: its own container, /data read-only)
# draws live matches at tm.example.com/watch, and it cannot see this process,
# so this process publishes them -- as the server owner chose on 2026-09-13:
#
#   * only tables whose creator allows observing: `@Tab=/in=` 0 "Possible"
#     (the client's own default) or 2 "No Comments Allowed". 1 "Impossible"
#     and any PASSWORD table never (the JongHoLow precedent). A table that may
#     not be shown is listed {"watchable": false} with NO state, so the board
#     drops it at once;
#   * HANDS HIDDEN -- counts only, as the game's own observer (@WatchInfo /H=);
#     a card's face is public once it is on the board;
#   * LIVE (no delay), and VS. COM games included.
#
# Shape: {"stamp": epoch, "tables": {"<room>-<table>": {"watchable": bool,
# "state": {...}}}}, written on change (at most once a second, and at once
# after a move) and at least every WATCH_EVERY_S, so a stale stamp means this
# process is gone. The state carries the MOVES of the game -- each
# placement's battles (the rolls the players were shown), the tiles that
# changed hands (after a won battle, the game's COMBO) and a chance block's
# effect -- recorded where every placement path ends (`_note_combo`), so the
# page can ANIMATE a move instead of redrawing a board.
#
# WARNING: IT MUST NEVER COST A MATCH: every entry point swallows every exception
# (logged once), reads the match dicts through copies, and writes nothing but
# this one file. And only a process that HOLDS a match writes it -- the login
# container imports this module too, and its empty dicts must never overwrite
# authsess's file. POL_TM_WATCH_FILE=0 turns it off (or names another file);
# POL_TM_WEB_WATCH=0 keeps writing it with every table unwatchable.
# ---------------------------------------------------------------------------
WATCH_EVERY_S = 5.0
WATCH_STEPS_MAX = 40
_WATCH = {"t": 0.0, "body": None, "owner": False, "whined": False}
#: match key -> {"rec": its _MATCH_TURN dict, "steps": [...], "seq": n, "began": t}
#: -- a new deal is a new dict, which is how a rematch starts a fresh list
_WATCH_GAMES = {}
#: match key -> {"battles": [...], "effects": [...]}: the placement in progress
_WATCH_PENDING = {}
_WATCH_LOCK = threading.RLock()


def _watch_file():
    name = (os.environ.get("POL_TM_WATCH_FILE") or "tm-tables-live.json").strip()
    if name in ("", "0", "off"):
        return None
    if os.path.isabs(name):
        return name
    return os.path.join(os.environ.get("POL_DATA_DIR", "/data"), name)


def _watch_whine(what, e):
    if not _WATCH["whined"]:
        _WATCH["whined"] = True
        _say("tm: WARNING: the web watch file's %s failed (%r) -- no match is affected; "
             "said once" % (what, e))


def _watch_touch():
    """A match message arrived. The first time this process is seen holding a
    match it becomes the file's writer (for good: an ended match must still be
    published as gone); then publish, throttled."""
    try:
        if not _WATCH["owner"] and (_MATCH_TURN or _COM_AT
                                    or any(v for v in list(_MATCH_STARTED.values()))):
            _WATCH["owner"] = True
    except Exception as e:                       # noqa: BLE001
        _watch_whine("owner check", e)
    _watch_publish()


def _watch_battle(chan, index, att, dfn, res, verdict):
    """One battle of the placement in progress, as the players were shown it."""
    try:
        with _WATCH_LOCK:
            p = _WATCH_PENDING.setdefault((chan, index), {"battles": [], "effects": []})
            p["battles"].append({
                "att": int(att), "def": int(dfn),
                "a_raw": int(res.get("a_raw", 0)), "a_roll": int(res.get("a_roll", 0)),
                "a_sel": int(res.get("a_sel", 0)),
                "d_raw": int(res.get("d_raw", 0)), "d_roll": int(res.get("d_roll", 0)),
                "d_sel": int(res.get("d_sel", 0)),
                "win": ("att" if verdict == tmbattle.ATTACKER else
                        "def" if verdict == tmbattle.DEFENDER else "draw")})
    except Exception as e:                       # noqa: BLE001
        _watch_whine("battle note", e)


def _watch_effect(chan, index, eff, target, hit, aim=None):
    """A chance block's effect in the placement in progress, or a rotating
    block's ray ("ray": `aim` = the tile it fires at)."""
    try:
        if not eff:
            return
        with _WATCH_LOCK:
            p = _WATCH_PENDING.setdefault((chan, index), {"battles": [], "effects": []})
            e = {"kind": str(eff), "tile": int(target),
                 "tiles": [int(t) for t in (hit or []) if isinstance(t, int)]}
            if aim is not None:
                e["to"] = int(aim)
            p["effects"].append(e)
    except Exception as e:                       # noqa: BLE001
        _watch_whine("effect note", e)


def _watch_card(card, code=0):
    """A card (or block) on a tile, as the page draws it."""
    row = card.row
    cid = int(row.get("id", 0))
    out = {"owner": card.owner, "placer": card.placer, "arrows": int(card.arrows)}
    if cid >= 0x8000:
        out["block"] = int(code or (cid - 0x8000))
    else:
        out.update(id=cid, atk=int(row.get("attack", 0)), type=int(row.get("type", 0)),
                   pdef=int(row.get("pdef", 0)), mdef=int(row.get("mdef", 0)),
                   # a special tile's ability, inherited by the card played on
                   # it: 1 attack up (spent by its first battle), 2 defense up,
                   # 3 all eight arrows (tmbattle SPECIAL_CODES)
                   ability=int(card.ability or 0) & 0xF,
                   # rec+0x26, what power up / power down moved: the card face
                   # adds it to each stat and colours the line (0xB500A..0xB505E)
                   mod=int(getattr(card, "modifier", 0) or 0))
    return out


def _watch_board(board, codes, turn=0):
    """{tile: card} as the page draws it, from a board dict. A rotating block
    also carries its CURRENT phase: its arrow turns a step every turn
    (`tmbattle.rotating_phase`; the ray fires at (phase - 4) & 7), and the
    dealt mask alone left the page's arrow frozen where the deal put it."""
    adv = int(turn or 0) * _env_int("POL_TM_ROTATING_STEP", 1)
    out = {}
    for t, c in sorted(dict(board).items()):
        e = _watch_card(c, codes[t] if t < len(codes) else 0)
        if tmbattle.is_rotating(c):
            ph = tmbattle.rotating_phase(c, adv)
            if ph is not None:
                e["phase"] = ph
        out[str(t)] = e
    return out


def _watch_game(key):
    rec = _MATCH_TURN.get(key)
    if rec is None:
        _WATCH_GAMES.pop(key, None)
        return None
    g = _WATCH_GAMES.get(key)
    if g is None or g["rec"] is not rec:
        g = _WATCH_GAMES[key] = {"rec": rec, "steps": [], "seq": 0, "began": time.time()}
    return g


def _watch_move(chan, index, actor, board, before, placed):
    """A placement has ended (every path ends in `_note_combo`): one step."""
    try:
        key = (chan, index)
        with _WATCH_LOCK:
            p = _WATCH_PENDING.pop(key, None) or {"battles": [], "effects": []}
            game = _watch_game(key)
            if game is None:
                return
            after = {int(t): c.owner for t, c in list(board.items())}
            placed = int(placed)
            beaten = {b["def"] for b in p["battles"] if b["win"] == "att"}
            changed = [{"tile": t, "from": before[t], "to": o,
                        "how": ("battle" if t in beaten
                                else "combo" if p["battles"] else "flip")}
                       for t, o in sorted(after.items())
                       if t != placed and t in before and before[t] != o]
            card = board.get(placed)
            game["seq"] += 1
            game["steps"].append({
                "seq": game["seq"], "t": round(time.time(), 2),
                "turn": int((_MATCH_TURN.get(key) or {}).get("turn") or 0),
                "seat": int(actor), "tile": placed,
                "card": _watch_card(card) if card is not None else None,
                # the placed card itself lost its battle and changed hands
                "lost": bool(card is not None and card.owner != actor),
                "battles": p["battles"], "changed": changed, "effects": p["effects"],
                "owners": {str(t): o for t, o in sorted(after.items())}})
            del game["steps"][:-WATCH_STEPS_MAX]
            # the shown board moves on WITH its step (see `_watch_state`)
            codes = list(_MATCH_OBJECTS.get(key) or [])
            game["board"] = _watch_board(board, codes,
                                         turn=(_MATCH_TURN.get(key) or {}).get("turn"))
            game["objects"] = codes
        _watch_publish(force=True)
    except Exception as e:                       # noqa: BLE001
        _watch_whine("move note", e)


def _watch_id(chan, index):
    """`<room>-<table>`: "#TM0R002" table 5 -> "2-5"."""
    try:
        room = int(str(chan)[-3:])
    except ValueError:
        room = 0
    return "%d-%d" % (room, int(index))


def _watch_allowed(chan, index):
    """May the web show this table? Its creator's own observe setting."""
    if not _env_int("POL_TM_WEB_WATCH", 1) or not chan or index is None:
        return False
    rules = _table_rules_get(chan, index) or {}

    def num(k):
        try:
            return int(rules.get(k, 0) or 0)
        except (TypeError, ValueError):
            return 0
    if num("pa") or str(rules.get("pw") or "").strip() not in ("", "0"):
        return False                              # a password table: never
    return num("in") != 1                         # 1 = "Impossible"


def _watch_name(mid, seat):
    try:
        import tmroom
        name = (tmroom.name_of(mid) or "").strip()
    except Exception:                            # noqa: BLE001
        name = ""
    return (name or "Player %d" % (seat + 1))[:15]


def _watch_seats(key):
    """([(name, is_com, hand, com_index, vote_key)], n, (chan, index)) for a
    match key. com_index = the client's `/Com=` value, the PlPrm.BIN record
    the page names and pictures the opponent by; vote_key = the seat's key in
    `_MATCH_CONTINUE[...]["votes"]` (None for a COM, which always plays again)."""
    chan, index = key
    hands = _MATCH_HANDS.get(key) or {}

    def count(h):
        return len(h) if h is not None else None
    if chan is None:                              # VS. COM: (None, push_key)
        got = _COM_GAME.get(index) or {}
        n = int(got.get("n") or 2)
        human = got.get("member")
        seats = [(_watch_name(human, 0), False, count(hands.get(_push_key(human))),
                  None, _push_key(human) if human is not None else None)]
        coms = got.get("coms") or []
        pattern = os.environ.get("POL_TM_COM_NAME", "COM %d")
        for s in range(1, n):
            # WARNING: NO `/Com=` YET = NO OPPONENT YET. Before the client's
            # character select sends @ComGame there is no index; falling back to
            # the seat number named every fresh game "Flower Girl Natasha"
            # (PlPrm record 1) on the page and in Discord (live testing, 2026-09-13)
            ci = coms[s - 1] if s - 1 < len(coms) else None
            try:
                ci = int(ci) if ci is not None else None
                nm = "COM" if ci is None else (pattern % ci) if "%d" in pattern else pattern
            except (TypeError, ValueError):
                nm, ci = "COM", None
            seats.append((nm[:15], True, count(hands.get(("com", s))), ci, None))
        return seats, n, tuple(got.get("table") or (None, None))
    roster = _roster_at(chan, index)
    return ([(_watch_name(m, i), False, count(hands.get(_push_key(m))), None, _push_key(m))
             for i, (m, _v) in enumerate(roster)], len(roster), (chan, index))


def _watch_state(key, phase):
    """(table id, {"watchable", "state"?}) for one match, or (None, None)."""
    seats, n, (chan, index) = _watch_seats(key)
    if chan is None or index is None:
        return None, None
    tid = _watch_id(chan, index)
    if not _watch_allowed(chan, index):
        return tid, {"watchable": False}
    rec = _MATCH_TURN.get(key) or {}
    n = int(rec.get("n") or n or 2)
    game = _watch_game(key) if phase == "play" else None
    # WARNING: THE BOARD AS OF THE LAST MOVE SHOWN, NOT THE LIVE ONE. A PvP placement
    # is RESUMABLE: `_begin_placement` puts the card down at once and
    # `_advance_placement` resolves its battles one at a time as the clients
    # ask (parking for a @BattleSelect), writing every capture to the live
    # board long before `_watch_move` writes the move's step. Publishing the
    # live board let the page show those results first, then replay the move
    # over them -- cards flipping before their battle, then flipping again
    # (live testing, 2026-09-13). While a placement is pending the board stays the
    # one the last step left; `_watch_move` moves it on with the step.
    if game is not None and key in _PENDING_BATTLE and game.get("board") is not None:
        shown, codes = game["board"], list(game.get("objects") or [])
    else:
        codes = list(_MATCH_OBJECTS.get(key) or [])
        shown = _watch_board(_MATCH_BOARD.get(key) or {}, codes, turn=rec.get("turn"))
        if game is not None:
            game["board"], game["objects"] = shown, codes
    scores = [0] * n
    for c in shown.values():
        o = c.get("owner")
        if o is not None and 0 <= o < n:
            scores[o] += 1
    over = bool(rec.get("result_sent"))
    winner = None
    if over and scores:
        top = [i for i, s in enumerate(scores) if s == max(scores)]
        winner = top[0] if len(top) == 1 else -1  # -1 = a draw
    seats = (seats + [("Player %d" % (i + 1), False, None, None, None)
                      for i in range(len(seats), n)])[:n]
    # the "Play again?" panel: who has answered (0 deciding, 1 again, 2 not)
    votes = ((_MATCH_CONTINUE.get(key) or {}).get("votes") or {}) if over else {}

    def vote(com, vk):
        if not over:
            return None
        if com:
            return CONTINUE_AGAIN
        v = int(votes.get(vk, CONTINUE_UNANSWERED)) if vk is not None else CONTINUE_UNANSWERED
        return v if v in (CONTINUE_AGAIN, CONTINUE_DECLINE) else CONTINUE_UNANSWERED
    return tid, {"watchable": True, "state": {
        "id": tid, "room": int(tid.split("-")[0]), "table": int(index), "n": n,
        "tiles": _board_tiles(n), "com": key[0] is None,
        "phase": "over" if over else phase,
        "turn": int(rec.get("turn") or 0), "active": int(rec.get("active") or 0),
        "limit": _env_int("POL_TM_TURN_LIMIT", 5 * n),
        # "mid" = the member id, for the board to find the player's PlayOnline
        # portrait in accounts.db; the board STRIPS it before serving anything
        "players": [{"name": nm, "com": com, "hand": h, "score": scores[i],
                     "ci": ci, "vote": vote(com, vk),
                     "mid": (int(vk) if isinstance(vk, int) or str(vk).isdigit() else None)}
                    for i, (nm, com, h, ci, vk) in enumerate(seats)],
        "board": shown,
        "objects": codes,
        "steps": list(game["steps"]) if game else [],
        "winner": winner,
        "began": round(game["began"], 2) if game else None}}


def _watch_tables():
    out = {}
    for key in list(_MATCH_TURN):
        tid, entry = _watch_state(key, "play")
        if tid:
            out[tid] = entry
    # card select: a started table not yet dealt, and a COM game before its deal
    for (chan, index), who in list(_MATCH_STARTED.items()):
        if who and (chan, index) not in _MATCH_TURN:
            tid, entry = _watch_state((chan, index), "select")
            if tid and tid not in out:
                out[tid] = entry
    for (chan, index), pk in list(_COM_AT.items()):
        if (None, pk) not in _MATCH_TURN:
            tid, entry = _watch_state((None, pk), "select")
            if tid and tid not in out:
                out[tid] = entry
    for k in [k for k in list(_WATCH_GAMES) if k not in _MATCH_TURN]:
        _WATCH_GAMES.pop(k, None)
    return out


def _watch_publish(force=False):
    """Write the file: on change (at most once a second, unless `force`) or
    every WATCH_EVERY_S. Never raises."""
    path = _watch_file()
    if not path or not _WATCH["owner"]:
        return
    now = time.time()
    if not force and now - _WATCH["t"] < 1.0:
        return
    try:
        with _WATCH_LOCK:
            tables = _watch_tables()
        body = json.dumps(tables, sort_keys=True, separators=(",", ":"))
        if body == _WATCH["body"] and now - _WATCH["t"] < WATCH_EVERY_S:
            return
        tmp = "%s.tmp.%d.%d" % (path, os.getpid(), threading.get_ident())
        with open(tmp, "w", encoding="utf-8") as fh:
            fh.write('{"stamp":%.3f,"tables":%s}' % (now, body))
        os.replace(tmp, path)
        _WATCH.update(t=now, body=body)
    except RuntimeError:
        return                     # a match dict changed under us: next beat
    except Exception as e:                       # noqa: BLE001
        _watch_whine("write", e)


def _stake_persist(member_id, wager):
    """Record an in-flight wager stake DURABLY, beside the money it came from.

    The in-memory `_COM_GAME[key]['staked']` dies with the process, and deploy
    restarts mid-match ate two stakes in one afternoon (2026-08-22: members 3's
    135 and 100, both refunded by hand). The collection copy survives; the
    orphan sweep in `_stake_recover` refunds it when no live game claims it."""
    if member_id is None:
        return
    try:
        data = _collection_load(member_id)
        data["staked_wager"] = int(wager)
        _collection_store(member_id, data, sync_save=False)
    except Exception as e:
        _say("tm: stake NOT persisted for member %s (%s)" % (member_id, e))


def _stake_clear(member_id):
    """The stake settled or refunded -- drop the durable copy."""
    if member_id is None:
        return
    try:
        data = _collection_load(member_id)
        if data.pop("staked_wager", None) is not None:
            _collection_store(member_id, data, sync_save=False)
    except Exception as e:
        _say("tm: stake record NOT cleared for member %s (%s)" % (member_id, e))


def _stake_recover(member_id, why=""):
    """Refund a stake the process no longer knows -- the restart orphan.

    Called where a NEW game begins (@GameENC=) and on @GameExit=: a durable
    stake with no in-memory match behind it can only be a mid-match restart's
    leftovers, and the client's own abort path (0xBC701) refunded its local
    wallet, so the server must follow or the save sync eats the difference."""
    if member_id is None or not _env_int("POL_TM_WAGER", 1):
        return
    try:
        entry = _COM_GAME.get(_push_key(member_id)) or {}
        if entry.get("staked"):
            return                      # a live game owns it; not an orphan
        if any(_push_key(member_id) in (_stk or {})
               for _stk in _PVP_STAKES.values()):
            # A PvP escrow still holds it -- including a LEAVER's stake, which
            # stays in the pot for the seats still playing (`_seat_departed`).
            # Refunding here AND paying the pot out later would mint money.
            return
        data = _collection_load(member_id)
        orphan = data.pop("staked_wager", None)
        if not orphan:
            return
        data["money"] = max(0, int(data.get("money") or 0) + int(orphan))
        _collection_store(member_id, data)
        _say("tm: VERIFIED: member %s had an ORPHANED stake of %d (a restart wiped "
             "the match that held it)%s -- refunded, balance %d"
             % (member_id, int(orphan), (" -- " + why) if why else "",
                data["money"]))
    except Exception as e:
        _say("tm: orphaned-stake check failed for member %s (%s)"
             % (member_id, e))


def _collection_remove_card(member_id, card_id, row=None):
    """Remove ONE card from the collection: exact-row match first, then by id.

    The take flow's deduction half: the loser's card goes to the
    winner. `row` is the 8-value wire row when the caller holds one -- an
    exact match removes the precise instance; a collection that has drifted
    from the client's still gives up its first copy of the id. Says loudly
    when nothing matches (nothing is then removed -- a take must never eat a
    different card).
    """
    if member_id is None:
        return False
    data = _collection_load(member_id)
    have = data.get("cards")
    if not isinstance(have, list) or not have:
        _say("tm: member %s take: no collection to remove card %s from"
             % (member_id, card_id))
        return False
    idx = None
    if row is not None:
        want = [int(v) for v in row.split(b"|")]
        idx = next((i for i, c in enumerate(have) if list(c) == want), None)
    if idx is None:
        idx = next((i for i, c in enumerate(have)
                    if c and int(c[0]) == int(card_id)), None)
    if idx is None:
        _say("tm: WARNING: member %s take: card %s is not in the collection "
             "(drifted?) -- nothing removed" % (member_id, card_id))
        return False
    gone = have.pop(idx)
    if _collection_store(member_id, data):
        _say("tm: member %s collection -= card %s (taken; %d left)"
             % (member_id, gone, len(have)))
    return True


def _collection_offer(member_id, cards):
    """Remember the pack we just put on offer, WITHOUT crediting it.

    The client keeps the WHOLE pack (see `_ACQUIRED_PER_PACK`), but only once the
    player goes through with it: `@Get=` is the acquisition event, and a pack the
    player walks away from is never acquired at all. So the offer is held here
    and committed there -- that part of the split was right even though the
    number of cards it committed was not.
    """
    if member_id is None or not cards:
        return
    data = _collection_load(member_id)
    data["pending_pack"] = [list(c) for c in cards]
    _collection_store(member_id, data)


def _collection_commit_offer(member_id):
    """`@Get=` arrived: credit what the client actually kept, and clear the offer."""
    if member_id is None:
        return
    data = _collection_load(member_id)
    pack = data.pop("pending_pack", None)
    if not isinstance(pack, list) or not pack:
        _say("tm: member %s sent @Get= with no pack on offer -- nothing credited"
              % member_id)
        _collection_store(member_id, data)
        return
    kept = [list(c) for c in pack[:_ACQUIRED_PER_PACK]]
    have = data.get("cards")
    data["cards"] = (have if isinstance(have, list) else []) + kept
    if _collection_store(member_id, data):
        _say("tm: member %s ACQUIRED %d of %d offered, %d total"
              % (member_id, len(kept), len(pack), len(data["cards"])))


def _merge_deck_stats_into(member_id, data, cmd):
    """Merge the CURRENT stats of a `@Decks=` report's cards into `data["cards"]`.

    Mutates `data` in place; returns True if any stored card changed. The caller
    owns the single load/store, so this cannot race its own read-modify-write.

    WARNING: THE UPGRADE-PERSISTENCE FIX. A card's combat stats (attack / type /
    physical-def / magic-def) and its arrows upgrade through play. The client
    RENDERS and RE-REPORTS them in `@Decks=` (msgid 0x22, builder 0x109D8D,
    emitted verbatim from the deck's own 16-byte records) -- but it never
    AUTHORS them via a formula: the stat bytes are server-supplied and the
    client only recomputes the DERIVED power/display from them (disassembly
    2026-09-02: writers 0x105E85, deriver 0xB9410/0xB9520). The save the client
    reloads its whole collection from is authored from `data["cards"]`
    (`_collection_to_save`), so unless the reported stats are merged back, every
    upgrade is overwritten by the stored pre-upgrade row on the next save sync --
    the measured "the boost reverts on relaunch".

    WARNING: NO UPGRADE RULE IS INVENTED: we persist exactly what the client reports.
    `@Decks=` carries only the <=5 DECK cards, so only those rows are touched;
    newly won cards still arrive by the take / `@Card=` path. There is NO
    per-instance id on the wire -- a card is identified only by (id + stats) --
    so a report row is matched to a stored row by ID, preferring a stored row
    that already carries the reported stats (an idempotent re-report) over
    upgrading a same-id sibling. When a player holds duplicates of an id this
    cannot be perfect (the client itself cannot tell the copies apart), but the
    multiset of (id -> stats) is kept as close as the wire allows.

    WARNING: FIELD ORDER DIFFERS between the two shapes -- never copy positionally:
        `@Decks=` /D= (7):  id | atk | type | pdef | mdef | arrows | flag
        stored row    (8):  id | atk | type | pdef | mdef | POWER  | arrows | flag
    the stored `power` (idx 5) is a value the client recomputes on load
    (0xB9520), so it is left untouched; arrows and flag are REMAPPED.

    WARNING: DEFAULT OFF as of 2026-09-02, and it must stay off while card GROWTH is
    on. This merge assumed the CLIENT is authoritative for card stats (persist
    what it reports). The decomp settled the opposite: the SERVER owns the stats
    (it rolls and grows them; the client only renders absolute records). So a
    `@Decks=` report is just the client echoing stats WE gave it -- taking them
    back is a no-op at best, and once `_grow_used_cards` has raised a card the
    client has not reloaded yet, the client reports the OLD (lower) stats and
    this merge would UNDO the growth. `POL_TM_DECK_STAT_SYNC=1` re-enables it for
    an A/B, but do not run it together with `POL_TM_CARD_GROW`.
    """
    if not _env_int("POL_TM_DECK_STAT_SYNC", 0):
        return False
    rows = _parse_card_list(cmd)          # a name-only /C=0 report parses to []
    if not rows:
        return False
    have = data.get("cards")
    have = [list(c) for c in have] if isinstance(have, list) else []
    consumed, changed, unknown = set(), [], []
    for r in rows:
        if len(r) < 6:                    # need at least id + 4 stats + arrows
            continue
        cid, stats, arrows = r[0], list(r[1:5]), r[5]
        # An exact same-(id, stats, arrows) stored row is this report row already
        # persisted -- consume it and change nothing (idempotent re-report).
        exact = next((i for i, row in enumerate(have)
                      if i not in consumed and row and row[0] == cid
                      and list(row[1:5]) == stats
                      and len(row) > 6 and row[6] == arrows), None)
        if exact is not None:
            consumed.add(exact)
            continue
        idx = next((i for i, row in enumerate(have)
                    if i not in consumed and row and row[0] == cid), None)
        if idx is None:
            unknown.append(cid)
            continue
        row = have[idx]
        while len(row) < 8:               # pad a short stored row out to 8 fields
            row.append(0)
        before = list(row)
        row[1], row[2], row[3], row[4] = stats     # the combat stats upgrade
        row[6] = arrows                            # arrows are per-instance too
        # row[5] (power, derived) and row[7] (flag, transient) left as stored.
        consumed.add(idx)
        if row != before:
            changed.append((cid, before[1:5] + [before[6]], stats + [arrows]))
    if changed:
        data["cards"] = have
        _say("tm: member %s deck stats synced -- %d card(s) changed: %s"
             % (member_id, len(changed),
                "; ".join("#%d %s->%s" % (c, b, a) for c, b, a in changed)))
    if unknown:
        _say("tm: member %s @Decks= reported %d id(s) not in the collection: "
             "%s -- deck/collection drift, stats not merged"
             % (member_id, len(unknown), unknown))
    return bool(changed)


#: WARNING: THE TWO 17-BYTE NAME SLOTS ARE THE DECK SET NAMES (2026-09-07, a tester's
#: screen). Save +0x104 renders as the card screen's FIRST deck tab and +0x115
#: as the second. The client reports a name it types as `@Decks=/C=0@DN<n>=
#: /S=<hex>` (stored under `deck_names`, never answered) and expects it back
#: in the save, which is why the tabs were blank (we wrote zeros) -- and why
#: they read the champion's name the Sunday the champion feature stamped the week's
#: #1 into +0x104 believing it the shop's "1st pack name" slot (RETRACTED: see
#: `SAVE_OFF_CHAMPION`). `POL_TM_DECK_NAMES=0` stops authoring them;
#: `POL_TM_DECK_NAME_BASE` is the `@DN<n>` index of the FIRST tab (default 1:
#: every report ever seen was `@DN1=`, and a player naming the first tab is the
#: likelier event -- unmeasured, flip it if the name lands on the wrong tab).
SAVE_OFF_DECK_NAMES = (0x104, 0x115)
SAVE_DECK_NAME_WIDTH = 17           #: 16 bytes + NUL


#: WARNING: THE 8TH CARD VALUE IS THE DECK SLOT (2026-09-07, a tester's deck screen).
#: Save record byte 8 (`tmsave` copy loop 0x10136D: rec+8 -> memory row +0xE;
#: the wire parser checks it `< 0x32` and indexes a word table) is the card's
#: position across BOTH deck sets: 0..24 = set 1 (5 decks x 5), 25..49 = set 2,
#: 255 = not in any deck. Measured: every card the tester "never put there"
#: sat at exactly the value we had been writing -- CardPrm field 6 (0, 1, 2, 3,
#: 6, 7, 12, 18, 21, 24, 29 on one tester's cards), which is NOT a slot. The client
#: reports a deck edit as `@Decks=/C=n@<i>=/D=id|atk|type|pdef|mdef|arrows|SLOT`
#: (the 7th value IS this byte, absolute) and reloads decks from the save, so the
#: server must (a) grant new cards with 255 and (b) write the reported slot
#: back. `POL_TM_DECK_SLOTS=0` restores the old column (the scatter).
#:
#: WARNING: UNMEASURED: whether the report's header `sh` names the deck (the name form
#: sends sh=1 for both tabs, so probably not). A report is per DECK (<=5 cards);
#: the decks it covers are inferred from its slots, and an EMPTY card report
#: (a deck cleared) therefore cannot say which deck -- logged, not applied.
DECK_SLOT_COUNT = 50
DECK_SLOT_NONE = 255


def _deck_slots_on():
    return _env_int("POL_TM_DECK_SLOTS", 1)


def _new_card_slot(legacy):
    """The 8th value of a card we GRANT: no deck slot (255), or the old column."""
    return DECK_SLOT_NONE if _deck_slots_on() else legacy


def _note_deck_slots(data, rows, sh=None):
    """Merge one card-form `@Decks=` report into `data["deck_slots"]`
    ({"<slot>": [id, atk, type, pdef, mdef, arrows]}). Returns (placed, decks)."""
    slots = data.get("deck_slots")
    if not isinstance(slots, dict):
        # FIRST MAP for this member: seed it from the verbatim `deck` report
        # kept before slots were understood, or the card it placed (a tester's Al
        # Bhed Primer at slot 4) is dropped by the first real edit -- which
        # is what happened live at 03:05Z.
        slots = {}
        text = data.get("deck")
        if text and rows is not None:
            data["deck_slots"] = {}
            _note_deck_slots(data, _parse_card_list(
                str(text).encode("latin1", "replace")), None)
            slots = data.get("deck_slots") or {}
    slots = {str(k): list(v) for k, v in slots.items()}
    placed, removed = {}, []
    for r in rows:
        if len(r) < 7:
            continue
        try:
            slot = int(r[6])
        except (TypeError, ValueError):
            continue
        card = [int(v) for v in r[:6]]
        if slot == DECK_SLOT_NONE:
            # VERIFIED: A REMOVAL, measured 2026-09-07T03:17:51Z: clearing two cards
            # sent `/C=2@0=...|255@1=...|255` -- the cards, slot 255. Drop
            # every map entry naming that card (id + stats, else id).
            hit = [k for k, v in slots.items() if v and v[0] == card[0]
                   and list(v[1:6]) == card[1:6]]                 or [k for k, v in slots.items() if v and v[0] == card[0]]
            for k in hit:
                slots.pop(k, None)
            removed.append((card[0], hit))
            continue
        if not 0 <= slot < DECK_SLOT_COUNT:
            continue
        placed[slot] = card
    # WARNING: A REPORT NEVER CLEARS A DECK. Measured 03:05Z/03:06Z: each placement
    # arrives ALONE (`/C=1`), so "a report replaces the deck it covers" would
    # drop the first card when a second joins the same deck. A placement
    # ADDS or MOVES: the card's previous entry (id + stats, else id) goes,
    # then the reported slot is set; a removal is a 255 row (above).
    decks = {s // 5 for s in placed}
    for s, v in placed.items():
        prev = [k for k, w in slots.items() if w and w[0] == v[0]
                and list(w[1:6]) == v[1:6]] \
            or [k for k, w in slots.items() if w and w[0] == v[0]]
        for k in prev:
            slots.pop(k, None)
        slots[str(s)] = v
    if removed:
        _say("tm:   ...deck removal: %s" % ", ".join(
            "card %d out of slot(s) %s" % (c, ",".join(h) if h else "(none held)")
            for c, h in removed))
    elif not placed and rows is not None:
        _say("tm:   ...a card-form @Decks= report with NO cards (sh=%r) -- nothing "
             "to place or remove" % (sh,))
    data["deck_slots"] = slots
    return placed, decks


def _deck_slot_bytes(data, cards):
    """Byte 8 for every stored card row, in order: its deck slot, else 255.

    A slot names a card by (id, stats); a stored row is matched by id,
    preferring one whose atk/pdef/mdef/arrows agree, each row used once.
    Falls back to the verbatim `deck` report when no slot map exists yet."""
    slots = data.get("deck_slots")
    if not isinstance(slots, dict):
        slots = {}
        text = data.get("deck")
        if text:
            _note_deck_slots(data, _parse_card_list(
                str(text).encode("latin1", "replace")), None)
            slots = data.get("deck_slots") or {}
    out = [DECK_SLOT_NONE] * len(cards)
    used = set()
    for s_str, v in sorted(slots.items(), key=lambda kv: int(kv[0])):
        try:
            slot = int(s_str)
        except ValueError:
            continue
        cid = int(v[0]) if v else -1
        cand = [i for i, row in enumerate(cards)
                if i not in used and row and int(row[0]) == cid]
        if not cand:
            continue
        best = next((i for i in cand if len(cards[i]) > 6 and len(v) >= 6
                     and [int(cards[i][1]), int(cards[i][3]), int(cards[i][4]),
                          int(cards[i][6])] == [v[1], v[3], v[4], v[5]]),
                    cand[0])
        used.add(best)
        out[best] = slot
    return out


def _deck_names(member_id):
    """{n: name bytes} from the member's stored `@DN<n>=/S=<hex>` report."""
    text = (_collection_load(member_id) or {}).get("deck_names") or ""
    out = {}
    for m in re.finditer(r"@DN(\d+)=/S=([0-9A-Fa-f]*)", str(text)):
        try:
            out[int(m.group(1))] = bytes.fromhex(m.group(2))
        except ValueError:
            pass
    return out


def _deck_name_fields(member_id):
    """{offset: (17, name)} for BOTH deck-set slots -- an unnamed tab gets
    zeros, so a stale string (the champion stamp) is cleared, not kept."""
    if member_id is None or not _env_int("POL_TM_DECK_NAMES", 1):
        return {}
    base = _env_int("POL_TM_DECK_NAME_BASE", 1)
    names = _deck_names(member_id)
    return {off: (SAVE_DECK_NAME_WIDTH,
                  (names.get(base + i) or b"")[:SAVE_DECK_NAME_WIDTH - 1])
            for i, off in enumerate(SAVE_OFF_DECK_NAMES)}


def _collection_set_deck(member_id, cmd, sh=None):
    """Record a `@Decks=` report: the verbatim body AND the cards' current stats.

    The raw body is kept as before (the client's own words for its own state;
    the two forms -- `/C=<n>` with `@<i>=/D=` cards, and `/C=0` with
    `@DN<n>=/S=<hex ascii>` names -- are stored under separate keys so a name
    report cannot erase the card list). NEW: the card form's stats are also
    merged into the collection so upgrades survive the save rewrite -- see
    `_merge_deck_stats_into`. One load/store covers both.
    """
    if member_id is None:
        return
    data = _collection_load(member_id)
    key = "deck_names" if b"/S=" in cmd and b"/D=" not in cmd else "deck"
    text = cmd.decode("latin1")
    slot_changed = False
    if key == "deck" and _deck_slots_on():
        _before_slots = dict(data.get("deck_slots") or {})
        _placed, _decks = _note_deck_slots(data, _parse_card_list(cmd), sh)
        slot_changed = (data.get("deck_slots") or {}) != _before_slots
        if _placed:
            _say("tm: member %s deck %s <- %s"
                 % (member_id, ",".join(str(d + 1) for d in sorted(_decks)),
                    ", ".join("slot %d: card %d" % (s, v[0])
                              for s, v in sorted(_placed.items()))))
    if key == "deck_names":
        # WARNING: A NAME REPORT CARRIES ONLY THE TAB JUST EDITED (measured
        # 2026-09-07T02:42Z: `@DN1=/S=TEST` then, 5 s later, `@DN2=/S=RAWR`
        # alone -- and replacing the stored text lost tab 1). MERGE per
        # index, rebuilt in the client's own form so `_deck_names` reads it.
        merged = {}
        for src in (data.get("deck_names") or "", text):
            for m in re.finditer(r"@DN(\d+)=/S=([0-9A-Fa-f]*)", str(src)):
                merged[int(m.group(1))] = m.group(2)
        text = "@Decks=/C=0" + "".join("@DN%d=/S=%s" % (n, hx)
                                       for n, hx in sorted(merged.items()))
    deck_changed = data.get(key) != text
    if deck_changed:
        data[key] = text
    # Merge the deck cards' current stats into `cards` (idempotent, so it is safe
    # to run even when the verbatim body repeats). Name-only reports carry no
    # `/D=` cards and merge to nothing.
    stat_changed = (_merge_deck_stats_into(member_id, data, cmd)
                    if key == "deck" else False)
    if not (deck_changed or stat_changed or slot_changed):
        return                                  # the client repeats itself
    if _collection_store(member_id, data):
        if deck_changed:
            _say("tm: member %s %s <- %s" % (member_id, key, text[:80]))


def _card_restore_enabled():
    """`restore=1` in the teach control file, else POL_TM_CARD_RESTORE.

    The control file is re-read per line and bind-mounted, so this experiment
    costs no restart and no session -- which matters, because whether an
    unsolicited `@Card=` reaches the collection depends on client-side timing we
    cannot see from here and will want to toggle mid-session. It is the same
    preference the class-L/class-R work landed on: express an experiment as a
    spec key, not a rebuild.

    WHY IT MIGHT NOT WORK, so the result is readable either way. `@Card=` is
    parsed at TM.dll 0x105E85 into a STAGING array (`0x524AF70`, slot
    `(count + 0x4B9)*16`, count at `0x52464D0`); the COLLECTION at `0x5246F10`
    is only filled by a separate arm at `0x16628`, which drains staging and
    zeroes it. So a push lands in staging immediately but shows up as "Owned"
    only once that arm runs. If Owned does not move, the answer is not a
    different `@Card=` -- it is the card service (`@CardReq=`/`@CardEnt=`).

    WARNING: AND THE PUSH CAN BE MIS-MATCHED. The parser gates on three equalities
    against the shop scene: sender id lo/hi (`[esi+0x28]`/`[esi+0x2c]`) and the
    shop number byte (`[esi+0x30]`). Ours satisfy all three, which also means a
    push left sitting in the queue could be picked up by the NEXT real `@Buy=`
    instead of its own reply. Turn it off once the question is answered.
    """
    opt = _teach_opts().get("restore")
    if opt is not None:
        return opt not in ("0", "", "off", "no")
    return os.environ.get("POL_TM_CARD_RESTORE", "0") == "1"


def _collection_cards(member_id):
    data = _collection_load(member_id)
    cards = data.get("cards")
    return [c for c in cards if isinstance(c, list) and len(c) == 8] \
        if isinstance(cards, list) else []


#: push_key -> {"t": accept stamp, "served": count}. Armed by an accepted
#: trade (`note_trade_accept`, called from the responders relay), consumed by
#: the `@Init=` arm. Auth-band only, like _EVENTSHOP_PENDING, so a module
#: dict is safe.
_TRADE_PENDING = {}


def note_trade_accept(*members):
    """Both sides of an accepted `@TrAns=`: serve the trade board ENTRY.

    Each entry is `(member_id, partner_member_id, partner_login_nick)` -- a
    bare member_id records the pair but authors nothing (selftest / callers
    without the pairing).

    VERIFIED: LIVE-CORRECTED 2026-08-22 by a tester's dump `pol (32).DMP` (sqMg recv ring).
    The from-the-MEMBERS-LIST trade does NOT use the
    @TrID/@TrEntAns/@Init(0xA2,24) handshake: that dump shows the accepter
    RECEIVED our @TrEntAns(0xA1) and @Init(0xA2,24) -- framed correctly from the
    partner, delivery/relay all working -- and IGNORED both, returning to the
    room heartbeat, never sending @TrID. So TM.dll scene 0x15590 (the @TrID
    handshake, which needs a 'D' table at state 0) is the SEATED/at-a-table
    trade, a different feature. The static-RE claim that the pushes were
    "guid-dropped" is also falsified -- they land fine.

    What the members-list accepter DOES poll after @TrAns=/Ans=1 is **(0xA2,1)**
    (measured live, b51b3422), and the 20:22Z run -- `(0xA2,1) @EventGameInit`
    + exactly ONE `(0xA2,26)` @Card list -- brought the board up polling 25/26
    without crashing. The crash (`__fastfail`, minidump-confirmed) was a stack
    buffer overflow in the client's sqMg formatter on the SECOND `(0xA2,26)`
    record (the /E=0 terminator or /P=1 partner list). So: serve (0xA2,1) + ONE
    (0xA2,26), never a second 26 (that is why responders relays 25/27 but NOT
    26). POL_TM_TRADE_SERVE=0 disables.

    WARNING: **DEFAULT FLIPPED TO OFF, 2026-08-24.** This authored serve is the
    artefact of the static model that has now been falsified twice over -- the
    game band (channel-2 watchdog flat at 0 across three live runs) and the seat
    ('D' row; seating both players changed nothing). Live, its records land
    AFTER the client has already torn the trade down: measured 12:08:13.769Z,
    the accepter's order is `@Tr=` filed -> gate set -> `@TrAns` sent ->
    RecvCLEAR -> CalcPlayerData -> **gate zeroed** -> only then our
    @EventGameInit + both @Card lists arrive. Nothing is left to receive them,
    and the static read says the board wants each client's OWN relayed list
    anyway, never a server-authored one.
    So the accept path now serves the VS-shaped go flag instead (responders.py,
    `POL_TM_TRADE_GO`), which is the one push ever measured to ADVANCE the
    scene. `POL_TM_TRADE_SERVE=1` restores this for an A/B; the two are
    deliberately separable so a run only ever changes one of them.
    """
    now = time.time()
    for entry in members:
        entry = entry if isinstance(entry, tuple) else (entry,)
        m, pm, partner, role = (tuple(entry) + (None, None, "acc"))[:4]
        k = _push_key(m)
        if k is None:
            continue
        # PARTIAL: RE-SERVE STATE (2026-08-24, measured with [tmlog] narration): the
        # client's @TrAns consumer runs a RecvCLEAR that PURGES (0xA2,*) from
        # the store -- probe-watched eating our pre-delivered @EventGameInit
        # (lookup #9951, wildcard FOUND -> deleted). The purge follows @TrAns
        # CONSUMPTION, which trails delivery by an unpredictable few seconds,
        # so no fixed serve delay can win the race. Instead the serve REPEATS
        # on the member's next few @Pongs (`_trade_reserve_tick`): copies that
        # land pre-purge die harmlessly, the first post-purge copy sticks.
        # The board polls its entry gate for minutes, so a late copy is fine.
        _TRADE_PENDING[k] = {"t": now, "served": 0, "last": now,
                             "left": _env_int("POL_TM_TRADE_RESERVE_N", 4),
                             "pm": pm, "partner": partner, "role": role}
        if partner is None or os.environ.get("POL_TM_TRADE_SERVE", "0") != "1":
            continue
        try:
            served = _trade_entry_lines(m, pm, role)
            for why, line in served:
                _queue_push(m, line, why=why, source=partner)
            _say("tm: member %s trade board queued (role=%s, %d line(s)), "
                 "source=partner %r; up to %d @Pong re-serves armed -- the "
                 "client's @TrAns RecvCLEAR purge eats pre-purge copies by "
                 "design, a post-purge copy must land for the board."
                 % (m, role, len(served), partner,
                    _env_int("POL_TM_TRADE_RESERVE_N", 4)))
        except Exception as exc:
            _say("tm: member %s trade board NOT queued (%r)" % (m, exc))


def _trade_entry_lines(m, pm, role="acc"):
    """The (why, line) serves for one member's trade-board entry.

    ROLE-AWARE (2026-08-24, [tmlog] narration): the ACCEPTER's post-accept
    scene polls (0xA2,1) -- it gets @EventGameInit + the card list. The
    INITIATOR consumes @TrAns and goes straight to the 25/26 waiter (42
    consecutive (0xA2,26) polls measured, ZERO (0xA2,1) polls), so an init
    would sit unconsumed in its ~24-slot ring; it gets the list ONLY.

    (0xA2,26) LINE LENGTH is load-bearing (2026-08-23/24, first live A/B):
    the 20-card 637-byte line fastfailed the client every time (dumps 933148/
    936252/942416); the 10-card ~340-byte line was served twice with ZERO
    crashes. Suspected mechanism: the client's sqMg logger second stage
    (vsnprintf 0x1E3E88 -> callback -> sprintf \"%s\") overruns a fixed buffer
    at ~630+ bytes and scribbles the caller's SEH node -- fastfail 0x15 on the
    ODS raise. Native client lists chunk at <=15 cards = always under. Do NOT
    raise POL_TM_TRADE_CARD_MAX past 15 before the static trace sizes the
    real threshold. POL_TM_TRADE_CARD=0 drops the list serve entirely.
    """
    lines = []
    if role != "ini":
        # (0xA2,1) @EventGameInit -- the accepter's measured board-entry gate.
        # VS-init family body (recipient-first: each client is player 0 in its
        # own copy), default rules, framed from the partner so the cmd-1 arm's
        # session-pair stamp matches subsequent peer traffic.
        body = _vsgame_body([(m, 0), (pm, 0)], 0, _rule_seven(None))
        name = os.environ.get("POL_TM_TRADE_INIT_NAME",
                              "EventGameInit").encode()
        body = b"@" + name + b"=" + body.split(b"=", 1)[1]
        lines.append(("trade board entry (0xA2,1) @%s -- the accepter's "
                      "measured post-accept poll" % name.decode(),
                      encode_code(0xA2 | (1 << 16)) + body))
    if _env_int("POL_TM_TRADE_CARD", 1):
        lines.append(("trade board card list (0xA2,26) /P=0, <=10 cards -- "
                      "the recipient's own collection",
                      _trade_card_line(m)))
        # BOTH SIDES (2026-08-24, [tmlog] 01:11Z run): consuming the complete
        # /P=0 list satisfied the 25-wait and left the scene polling for ONE
        # MORE 26 -- the PARTNER'S list. The 21:39Z "/P=1 partner list errors
        # the scene instantly" retraction was measured in the 637-byte era;
        # the length model retires it. Two ~340 B complete lists are safe.
        if pm is not None:
            lines.append(("trade board card list (0xA2,26) /P=1, <=10 cards "
                          "-- the PARTNER's collection",
                          _trade_card_line(pm, p=1)))
    return lines


def _trade_reserve_tick(member_id):
    """Re-serve the trade-board entry on this member's @Pong, while armed.

    The client's @TrAns consumer purges (0xA2,*) once, at a moment we cannot
    see; re-serving on the next few heartbeats guarantees a post-purge copy.
    Bounded: POL_TM_TRADE_RESERVE_N copies (default 4), >=3s apart, 30s TTL
    (POL_TM_TRADE_RESERVE_TTL). Ring pressure stays trivial next to the 24-slot
    ring, and no match runs during a trade.
    """
    k = _push_key(member_id)
    st = _TRADE_PENDING.get(k) if k is not None else None
    if not st or st.get("partner") is None:
        return
    if os.environ.get("POL_TM_TRADE_SERVE", "0") != "1":
        return
    now = time.time()
    if now - st["t"] > _env_int("POL_TM_TRADE_RESERVE_TTL", 45):
        _TRADE_PENDING.pop(k, None)
        return
    if st.get("left", 0) <= 0 or now - st.get("last", 0) < 3.0:
        return
    st["left"] -= 1
    st["last"] = now
    try:
        # Re-serves carry the CARD LIST only, whatever the role: [tmlog]
        # measured (2026-08-24) that NEITHER role polls (0xA2,1) -- both park
        # in the 25/26 waiter -- so re-serving the init would only stack
        # unconsumed copies in the client's ~24-slot ring.
        served = _trade_entry_lines(member_id, st["pm"], "ini")
        for why, line in served:
            _queue_push(member_id, line, why=why + " [re-serve]",
                        source=st["partner"])
        _say("tm: member %s trade board card list RE-SERVED (%d left) -- "
             "racing the client's one-shot @TrAns purge"
             % (member_id, st["left"]))
    except Exception as exc:
        _say("tm: member %s trade re-serve failed (%r)" % (member_id, exc))


#: THE TRADE ACTUALLY MOVES CARDS, AND UNTIL 2026-08-25 IT DID NOT.
#: The board completed live at 01:02:12Z and the tester confirmed minutes
#: later: *"can confirm the trade did not persist"*. Collections here are
#: SERVER-AUTHORITATIVE -- `_collection_load` reads the per-member JSON and
#: `_collection_to_save` writes it into the save the client loads from -- so a
#: swap that happens only on the two clients evaporates at the next launch.
#:
#: Both halves are on the wire and we already relay them, so nothing new has to
#: be asked for:
#:
#:   (0xA2,26) @Card=/P=n/E=<total>/S=<start>/C=<count>@N<i>=/D=a|b|c|d|e|f
#:                      each side's OFFER, chunked; complete when S+C == E
#:   (0xA2,27) @Data=/P=n/M=<mode>/D=<30 bits>
#:                      that side's SELECTION
#:
#: KEY: A SELECTION INDEXES THE **PARTNER'S** OFFER, NOT YOUR OWN -- so it marks
#: what you RECEIVE. Settled by elimination on the completed 01:02Z trade:
#:
#:   m6 (P=0) offered 4 cards, selected 1|0|0|0...   -> takes m3's card 0
#:   m3 (P=1) offered 1 card,  selected 1|1|1|1|0... -> takes m6's cards 0..3
#:
#: m3 published ONE card and selected FOUR. If `/D=` indexed your own offer that
#: is impossible, so it indexes the partner's. It also means uneven trades are
#: normal and intended -- that run was four cards for one, which is what the
#: tester saw on screen.
#:
#: A wire card is 6 fields and a stored row is 8; `tmsave.encode_card` pads
#: short records with zeros, and `8|9|0|9|5|26` clears all three of its
#: rejection gates (id fits u16, type < TYPE_MAX, attack+pdef+mdef > 0), so the
#: wire form is storable as-is once padded.
_TRADE_SELECT_SLOTS = 30


def _trade_state(member_id):
    k = _push_key(member_id)
    return _TRADE_PENDING.setdefault(k, {}) if k is not None else {}


def trade_partner_of(member_id):
    """This member's trade partner, from EITHER side of the binding.

    WARNING: A ONE-SIDED LOOKUP IS NOT ENOUGH, and a live run proved it (2026-08-25
    01:52:03Z). The pair was armed for members 3 and 6 at 01:51:51, and twelve
    seconds later the `@TrEnt=` handler logged **"partner member None"** -- so
    the partner copy of the grant never went out, and the role-1 client, which
    never sends `@TrEnt` itself, parked at "Now Loading" (board state 2,
    pct=80) exactly as it did before any of this was fixed. The `@Quit` disarm
    then printed "disarmed members 6 and None", confirming one entry had lost
    its `pm` while the other was fine.

    `note_trade_accept` writes BOTH directions, so the binding is redundant by
    construction -- there is no reason to fail when the other half is sitting
    right there. So: try our own entry first, and if it has no `pm`, scan for
    whoever names US. Empty entries (which `_trade_state` can create when a
    stray 0xA2 arrives outside a trade) are skipped rather than trusted.
    """
    k = _push_key(member_id)
    if k is not None:
        pm = (_TRADE_PENDING.get(k) or {}).get("pm")
        if pm is not None:
            return pm
    for other, st in _TRADE_PENDING.items():
        if other is None or not isinstance(st, dict):
            continue
        pm = st.get("pm")
        if pm is None:
            continue
        if _push_key(pm) == k and k is not None:
            # `other` names us as its partner -> `other` IS our partner.
            for cand in (st.get("pm_self"), other):
                if cand is not None:
                    return _unkey(cand)
    return None


def _unkey(k):
    """A push key back to a member id where possible (keys are ids today)."""
    try:
        return int(k)
    except (TypeError, ValueError):
        return k


def note_trade_offer(member_id, body):
    """Accumulate one `(0xA2,26) @Card=` chunk as this member's OFFER."""
    try:
        m = re.search(rb"@Card=/P=(\d+)/E=(\d+)/S=(\d+)/C=(\d+)", body)
        if not m:
            return
        total, start = int(m.group(2)), int(m.group(3))
        st = _trade_state(member_id)
        st["seat"] = int(m.group(1))        # this side's own /P= seat
        offer = st.setdefault("offer", {})
        if total == 0:                      # the /E=0 terminator
            st["offer_total"] = st.get("offer_total", 0)
            return
        st["offer_total"] = total
        for om in re.finditer(rb"@N(\d+)=/D=([0-9|]+)", body):
            idx = start + int(om.group(1))
            vals = [int(x) for x in om.group(2).split(b"|") if x != b""]
            if vals:
                offer[idx] = vals
    except Exception as e:
        _say("tm: trade offer parse failed for member %s: %r" % (member_id, e))


def note_trade_select(member_id, body):
    """Record one `(0xA2,27) @Data=` selection bitmap for this member."""
    try:
        m = re.search(rb"@Data=/P=(\d+)/M=(\d+)/D=([0-9|]+)", body)
        if not m:
            return
        bits = [int(x) for x in m.group(3).split(b"|") if x != b""]
        st = _trade_state(member_id)
        st["seat"] = int(m.group(1))
        st["sel"] = bits[:_TRADE_SELECT_SLOTS]
        st["sel_mode"] = int(m.group(2))
    except Exception as e:
        _say("tm: trade select parse failed for member %s: %r" % (member_id, e))


def _card_key(vals):
    """Identity for matching a WIRE card against a STORED row.

    WARNING: THE TWO SHAPES DO NOT LINE UP, and assuming they did is what made the
    first live run refuse with "m6 is missing a card it offered". Measured
    against member 6's real collection, all four offered cards agreeing:

        wire    [id, atk, type, pdef, mdef,     X]          6 fields
        stored  [id, atk, type, pdef, mdef, ?,  X,  ?]      8 fields
                                            5   6   7

    `wire[5] == stored[6]` in every case (26, 16, 14, 177) -- the wire DROPS
    stored[5] and stored[7]. Comparing the first six therefore never matches.

    WARNING: And the unit test did not catch it because the test built its collections
    by zero-padding wire cards, so both sides of the comparison shared the
    code's wrong assumption. A fixture derived from the code under test proves
    nothing; this one is now built from a real collection dump.

    Because the wire cannot carry stored[5]/[7], a received card is transferred
    as the GIVER'S STORED ROW rather than rebuilt from the wire -- lossless, and
    the reason `take()` returns the rows it popped.
    """
    v = list(vals)
    if len(v) >= 7:                      # a stored row
        return tuple(v[0:5]) + (v[6],)
    return tuple((v + [0] * 6)[:6])      # a wire card


def apply_trade_swap(a, b):
    """Move the agreed cards between two members' collections. Report a string.

    ALL-OR-NOTHING. If either side's offer is incomplete, or a card a member
    promised is not actually in their collection, NOTHING is written and the
    reason is returned. A half-applied trade would silently destroy cards, which
    is far worse than a trade that visibly did not take.
    """
    sa, sb = _trade_state(a), _trade_state(b)
    if sa.get("swapped") or sb.get("swapped"):
        return "already applied"
    oa, ob = sa.get("offer") or {}, sb.get("offer") or {}
    ta, tb = sa.get("offer_total"), sb.get("offer_total")
    if ta is None or tb is None:
        return "no offer recorded (m%s=%r m%s=%r)" % (a, ta, b, tb)
    if len(oa) != ta or len(ob) != tb:
        return ("offer INCOMPLETE -- m%s has %d/%s, m%s has %d/%s; refusing"
                % (a, len(oa), ta, b, len(ob), tb))
    ca, cb = sa.get("sel"), sb.get("sel")
    if ca is None or cb is None:
        return "no selection recorded (m%s=%s m%s=%s)" % (
            a, ca is not None, b, cb is not None)

    # a's selection indexes b's offer -> what a RECEIVES, and vice versa.
    a_gets = [ob[i] for i in range(min(len(ca), _TRADE_SELECT_SLOTS))
              if ca[i] and i in ob]
    b_gets = [oa[i] for i in range(min(len(cb), _TRADE_SELECT_SLOTS))
              if cb[i] and i in oa]
    if not a_gets and not b_gets:
        return "both selections empty -- nothing to move"

    da, db = _collection_load(a), _collection_load(b)
    la = [c for c in (da.get("cards") or []) if isinstance(c, list)]
    lb = [c for c in (db.get("cards") or []) if isinstance(c, list)]

    def take(pool, wanted, who):
        """Remove one instance of each wanted card; None if any is missing."""
        rest = list(pool)
        gone = []
        for w in wanted:
            key = _card_key(w)
            for j, have in enumerate(rest):
                if _card_key(have) == key:
                    gone.append(rest.pop(j))
                    break
            else:
                _say("tm: trade REFUSED -- member %s does not hold %r, which "
                     "it offered. Nothing written." % (who, w))
                return None, None
        return rest, gone

    la2, a_gives = take(la, b_gets, a)
    if la2 is None:
        return "m%s is missing a card it offered" % a
    lb2, b_gives = take(lb, a_gets, b)
    if lb2 is None:
        return "m%s is missing a card it offered" % b

    pad = lambda v: (list(v) + [0] * 8)[:8]
    la2 += [pad(c) for c in b_gives]
    lb2 += [pad(c) for c in a_gives]

    da["cards"], db["cards"] = la2, lb2
    if not _collection_store(a, da):
        return "m%s collection write FAILED -- m%s untouched" % (a, b)
    if not _collection_store(b, db):
        _say("tm: WARNING: m%s was written and m%s FAILED -- collections are now "
             "ASYMMETRIC, fix by hand" % (a, b))
        return "m%s collection write FAILED AFTER m%s succeeded" % (b, a)
    sa["swapped"] = sb["swapped"] = True
    return ("m%s gave %d and received %d; m%s gave %d and received %d"
            % (a, len(a_gives), len(b_gives), b, len(b_gives), len(a_gives)))


def note_trade_cancel(*member_ids):
    """A relayed `@TrCan=`/`@TrCanAck=` disarms the serve for both sides."""
    for m in member_ids:
        _TRADE_PENDING.pop(_push_key(m), None)


#: The board's card array is 30 slots. Arm 0x1067B5 writes card i to slot
#: (/S + i) with NO bounds check (`esi + (S+i+0x4d7)*16`), and the sibling
#: cleanup loop caps the valid index at 0x1e = 30. So /S + count MUST be <= 30
#: or the write runs past the array and corrupts the session object.
TRADE_BOARD_SLOTS = 30


def _trade_card_line(member_id, p=0):
    """One member's collection as the trade board's (0xA2, cmd 26) `@Card=`.

    Arm 0x1067B5 reads `@Card=/P=/E=/S=/C=` then `@N<i>=/D=<8 vals piped>` and
    writes card i to board slot **/S + i** (`esi + (S+i+0x4d7)*16`).

    WARNING: THE CRASH FIX (2026-08-22, minidump-root-caused). The old body was
    `/E=1/S=<n>/C=<n>` -- it put the card COUNT in /S=, the START index. For a
    20-card collection that told the client to write cards into slots 20..39,
    overflowing the 30-slot array (slots 30..39), corrupting the session object
    and __fastfailing pol.exe (0xC0000409). Size-dependent: <=10 cards stayed in
    bounds, which is why one early run "worked". Correct: **/S=0**, /E= the real
    total, and CAP at the 30 the board can hold (a bigger collection needs
    chunking -- /S=0/C=8, /S=8/C=8, ... -- not yet built). /S=0/C=n/E=n also
    satisfies the completion check (S+C == E -> list marked complete).

    `p` -> `/P=` player index (0 = recipient), UNMEASURED but non-fatal.
    """
    # PARTIAL: /E=<n>/S=0/C=<n> -- the COMPLETE form (2026-08-24). The incomplete
    # /E=1/S=n form was kept out of fear of a "completion crash" (dump 933148,
    # /E=20/S=0/C=20 fastfailed), but the length model retired that reading:
    # 933148's line was 637 BYTES -- the crash was length, not completion (the
    # incomplete /E=1/S=20 form at the same length ALSO crashed, dump 936252;
    # completion was never varied independently of length). And the incomplete
    # form is now MEASURED not to advance the board: [tmlog] narration
    # (2026-08-24 00:40:46Z) shows the 26-waiter consuming our /E=1/S=10/C=10
    # list and going straight back to polling 25/26 -- S+C != E reads as
    # "partial chunk, wait for the rest", forever. Native chunking: /E= total,
    # /S= start, /C= this chunk; complete when S+C == E. At the 10-card cap
    # (~340 B) the line is length-safe, so completion gets its first fair
    # test; if a genuine completion crash exists it will write a dump.
    # DEFAULT CAP 10 (length): the crash tracks the LOGGED LINE LENGTH (~630+
    # bytes = 20 cards). 10 cards ~= 340 bytes, safely under. The client's own
    # native list builder chunks at <=15/message -- the ceiling to respect if
    # this cap is ever raised.
    _cap = max(0, min(TRADE_BOARD_SLOTS, _env_int("POL_TM_TRADE_CARD_MAX", 10)))
    cards = (_collection_cards(member_id) or [])[:_cap]
    n = len(cards)
    body = encode_code(0xA2 | (26 << 16)) \
        + b"@Card=/P=%d/E=%d/S=0/C=%d" % (p, n, n)
    for i, v in enumerate(cards):
        body += b"@N%d=/D=%s" % (i, b"|".join(b"%d" % x for x in v))
    return body


def _trade_pending_serve(member_id):
    """The trade-board serve due on THIS `@Init=`, or []. TTL + serve cap.

    The cap exists because the game band REDIALS in a churn (~6-15s lives,
    measured 18:46Z: 20350 closed, 42159 closed, 45848 held) and every
    redial re-handshakes -- each fresh `@Init=` re-serves so the newest
    board still gets its list, but the client's receive store is one global
    ring of ~24 slots (ring pressure once ate a delivered @PutCard),
    so the re-serve cannot be unbounded.
    """
    # WARNING: RETRACTED MODEL, DEFAULT OFF. This served the trade card list on the
    # game-band @Init= (code 0x80). Static RE 2026-08-22: a trade NEVER opens
    # the game band (clients handshake it only for real game launches), and the
    # board takes each client's OWN relayed list, not a server-authored one. So
    # this path is dead for a trade; kept behind POL_TM_TRADE_INIT_CARDSERVE=1
    # only for a deliberate A/B. POL_TM_TRADE_SERVE now gates the 0xA1/0xA2,24
    # HANDSHAKE (note_trade_accept), a different thing.
    if os.environ.get("POL_TM_TRADE_INIT_CARDSERVE", "0") != "1":
        return []
    k = _push_key(member_id)
    st = _TRADE_PENDING.get(k) if k is not None else None
    if st is None or os.environ.get("POL_TM_TRADE_SERVE", "0") != "1":
        return []
    if time.time() - st["t"] > _env_int("POL_TM_TRADE_SERVE_TTL", 120):
        _TRADE_PENDING.pop(k, None)
        return []
    cap = _env_int("POL_TM_TRADE_SERVE_MAX", 4)
    if st["served"] >= cap:
        _say("tm: member %s trade-board serve SUPPRESSED (cap %d reached "
             "within the TTL) -- game band churning; the board never held "
             "long enough to consume a serve" % (member_id, cap))
        return []
    st["served"] += 1
    line = _trade_card_line(member_id)
    _say("tm: member %s @Init= answered WITH the trade board card list "
         "(serve %d/%d, %d card(s)) -- riding the game-band @InitAns batch "
         "so it lands before the ~15s idle-close"
         % (member_id, st["served"], cap, line.count(b"@N")))
    return [line]


def _gameqa_enabled():
    return os.environ.get("POL_TM_GAMEQA", "1") != "0"


def _gameqa_qt():
    """`/QT=`, which `0x841E0` RETURNS and whose caller reads as: > 0 success,
    0 -> Lobby.BIN 337, < 0 -> an error message. 1 is the least-committal value
    above the threshold. Tunable because the number may well mean something (a
    seat index, a table state) rather than a plain yes."""
    try:
        return int(os.environ.get("POL_TM_GAMEQA_QT", "1"), 0)
    except ValueError:
        return 1


def _gameqt_enabled():
    return os.environ.get("POL_TM_GAMEQT", "1") != "0"


def _gameea_enabled():
    return os.environ.get("POL_TM_GAMEEA", "1") != "0"


def _gameea_en():
    """`/EN=`, the reservation verdict. > 0 advances the scene, 0 becomes the
    client's own -99 ("Timed out while updating table data"), < 0 is an error
    code. 1 is the value TM.dll `0x90114` singles out; see `MSG_GAMEEA`."""
    try:
        return int(os.environ.get("POL_TM_GAMEEA_EN", "1"), 0)
    except ValueError:
        return 1


def _gameml_enabled():
    return os.environ.get("POL_TM_GAMEML", "1") != "0"


def _gameml_rows():
    """How many members to claim are at the table. 0 -- an empty table -- is the
    truth at the table this was measured at, and the only answer that invents
    nothing. Set 1 to serve a probe row and find out which of `/Po=`, `/Lv=` and
    `/Rk=` is VS. Rating, Title, Card Level or Guild on the Member Info panel."""
    try:
        return max(0, min(16, int(os.environ.get("POL_TM_GAMEML_ROWS", "0"))))
    except ValueError:
        return 0


def _is_seated_here(member_id, peer_nick=None):
    """Is `member_id` seated at the table this `@GameM=` is about? Only a seated
    requester may have their reservation slot (+0x108) filled -- a looker's must
    stay empty or their Reserve button greys. See the @GameM= handler.

    WARNING: THIS GATE FEEDS +0x108 (the 'Table Members' sidebar AND the Start-Game
    gate), and BOTH feeders die when it wrongly returns False for a seated
    member (the @Pong re-feed at `_reservation_reply` and the @GameM= reply
    share it). Measured live 2026-08-21 with tm_store_probe: +0x10c=2 (View pane
    fine), +0x41c=1 (armed), store held ONLY @GameQA (0x41/6) and NO 0x41/0xC --
    so the reservation reply was never SENT, i.e. this returned False. The
    three-outcome log below is the observable: it prints member_id, the resolved
    room, and the actual seated ids on every FALSE, so an id-space or
    room-resolution mismatch is visible instead of silent (`POL_TM_SEATED_LOG=0`
    to quiet it)."""
    if member_id is None:
        return False
    reason = "room_of=None"
    try:
        import tmroom
        chan = tmroom.room_of(member_id)
        if chan:
            for _idx, seats in (tmroom.seats(chan) or {}).items():
                if any(int(m) == int(member_id) for m, _i in seats):
                    return True
            reason = ("not in seats: room=%s seated ids=%s"
                      % (chan, [int(m) for _i, s in (tmroom.seats(chan) or {})
                                .items() for m, _x in s]))
    except Exception as exc:
        reason = "raised %r" % (exc,)
    if os.environ.get("POL_TM_SEATED_LOG", "1") == "1":
        # Rate-limited so the @Pong re-feed (~1/2s) does not flood -- one line
        # per (member, reason) until the reason changes.
        key = (int(member_id) if member_id is not None else None, reason[:40])
        if _SEATED_LOG_SEEN.get(key) != reason:
            _SEATED_LOG_SEEN[key] = reason
            _say("tm: _is_seated_here(member %s) = FALSE -- %s. No reservation "
                 "reply (+0x108) will be sent for this member." % (member_id, reason))
    return False


_SEATED_LOG_SEEN = {}


def _gameml_live_rows(member_id, peer_nick=None):
    """`[(member_id, name_bytes), ...]` -- the SEATED members of ONE TABLE.

    WARNING: THIS LISTED THE WHOLE ROOM UNTIL 2026-08-21 ~13:38Z, and a tester's
    report is what it did on screen: "when player two joins the room, it
    immediately pseudo-adds them to table 1... they appear in the table members
    list and it doesn't let them place a reservation." The pane is *View TABLE
    Members*; a room-joiner is not a table member. The table is resolved from
    the PEER the `@GameM=` was addressed to (the same `table_index_for_peer`
    every table-scoped command uses), falling back to the asker's own seated
    table; an empty seat list answers `/Num=0`, which is the truth."""
    if member_id is None or os.environ.get("POL_TM_GAMEML_LIVE", "1") != "1":
        return None
    try:
        import tmroom
    except ImportError:
        return None
    try:
        chan = tmroom.room_of(member_id)
        if not chan:
            return None
        seats_map = tmroom.seats(chan) or {}
        index = None
        if peer_nick:
            index, _why = table_index_for_peer(
                peer_nick, tmroom.room_peer(chan), chan=chan)
        if index is None:
            for i, s in seats_map.items():
                if any(int(m) == int(member_id) for m, _x in s):
                    index = i
                    break
        names = {}
        for mid, vals in tmroom.members(chan):
            # v10 is name[15] + the 16-letter block; the name is the first 15,
            # space-padded, and `0x66A39` reads its length from the block's
            # first letter. Here we only need the text.
            names[int(mid)] = ((vals[10] or "")[:15].strip()
                               .encode("latin1", "replace"))
        out = []
        for mid, _ident in (seats_map.get(index) or []):
            nm = names.get(int(mid))
            if not nm:
                # A seat can outlive its member's ROOM presence (the silent-
                # seat expiry frees seats for members who crashed out), and a
                # row served nameless is the "Since  has cancelled their
                # reservation" popup with the blank name (Lobby.BIN 50 --
                # live testing, 2026-08-22). The durable name store keeps what
                # note_name taught even after they leave the channel.
                try:
                    nm = (tmroom._live_names().get(str(mid)) or "")[:15] \
                        .strip().encode("latin1", "replace")
                except Exception:
                    nm = b""
            out.append((int(mid), nm or b"?"))
        return out
    except Exception as exc:
        _say("tm: @GameML live roster unavailable (%r) -- fixed count" % (exc,))
        return None


def _gameml_body(cmd, member_id=None, peer_nick=None):
    """`@GameML=` for a `@GameM=` request. COLUMNAR, PIPE-delimited, HEX names.

    WARNING: THE FORMAT WAS WRONG SINCE THE FIRST SESSION, AND IT IS THE WHOLE OF THE
    EMPTY "View Table Members" PANE (RE + minidump `pol (29).dmp`, 2026-08-21).
    The reply was ACCEPTED and drained
    into the members slot every time -- what was wrong was the ROW LAYOUT:

      * The client's field extractors (0xAAD60 string, 0xAB470 u64, 0xAB080 int)
        are COLUMNAR, not grouped. Each key appears ONCE and its value is a
        `|`-separated list, one element per member; the parse loop's index picks
        the i-th `|` element -- the exact shape of `@Decks=/D=220|55|0|...`. The
        old "repeat `/MLID=/MLNN=...` per row, read by occurrence index" made the
        client, for member index >= 1, find no `|`, fall back to element 0, and
        return every row as member 0.
      * `/MLNN=` and `/HN=` are UPPERCASE HEX of the name bytes. 0xAAD60 reads
        the value TWO hex chars at a time into one byte (`hi<<4|lo`), so plaintext
        `"Fox"` decoded to the single byte `0xC0+0x2A = 0xEA` -- the sentinel the
        dump showed for every name. The client's own `@GameEN=/HN=4C61...7432`
        (an 11-character name) already sends names this way.

    Each column is length `Num`, same member order, so MLID[i]/MLNN[i]/Po[i]/...
    line up by index. `/ID=` still echoes the request's `/ID=` (the recipient's
    local POL-ID, checked at 0x838E3) and `/Num=` is decimal.
    """
    m = re.search(rb"/ID=([0-9A-Fa-f]+)", cmd)
    if not m:
        _say("tm: @GameM= with no /ID= -- cannot echo it, staying silent: %r" % cmd)
        return None
    ident = m.group(1)

    def _hexname(b):
        # UPPERCASE hex, 2 chars/byte -- 0xAAD60 hex-decodes; lowercase a-f and
        # an odd nibble both decode wrong, so bytes.hex().upper() is the contract.
        return b.hex().upper().encode("ascii")

    rows = _gameml_live_rows(member_id, peer_nick)
    if rows is not None:
        import tmroom as _tr2
        mlids, names = [], []
        for mid, name in rows:
            # WARNING: `/MLID=` IS A POL-ID, NOT OUR DATABASE ID (three-ids banner):
            # a db id like 0000000000000003 renders an empty pane. pol_id_of
            # returns a HEX STRING; base-10 int() on it hung the pane once
            # @Init taught a POL-ID, so parse it base 16.
            pid = None
            try:
                pid = _tr2.pol_id_of(mid)
            except Exception:
                pid = None
            if pid:
                try:
                    mlid = int(str(pid).replace("0x", "").replace("0X", ""), 16)
                except ValueError:
                    mlid = int(mid)
            else:
                _say("tm:   @GameML row for member %s has NO recorded POL-ID "
                     "-- serving the db id, which the client may drop" % mid)
                mlid = int(mid)
            mlids.append(b"%016X" % (mlid & 0xFFFFFFFFFFFFFFFF))
            names.append(_hexname(name))
        n = len(rows)
        zeros = b"|".join([b"0"] * n)
        # `/Po=` `/Lv=` `/Rk=` are served 0 (VS.Rating/Title/CardLevel HINTS,
        # so a wrong column shows a 0, not an invented number).
        return (b"@GameML=/ID=%s/Num=%d/MLID=%s/MLNN=%s"
                b"/Po=%s/Lv=%s/Rk=%s/HN=%s"
                % (ident, n, b"|".join(mlids), b"|".join(names),
                   zeros, zeros, zeros, b"|".join(names)))

    # FALLBACK PROBE (no live roster, POL_TM_GAMEML_LIVE=0): same columnar shape,
    # names hex-encoded so the pane can render the probe at all.
    n = _gameml_rows()
    rng = range(n)
    return (b"@GameML=/ID=%s/Num=%d/MLID=%s/MLNN=%s/Po=%s/Lv=%s/Rk=%s/HN=%s"
            % (ident, n,
               b"|".join(b"%016X" % (0x2000000010 + i) for i in rng),
               b"|".join(_hexname(b"MLNN%d" % i) for i in rng),
               b"|".join(b"%d" % (10 + i) for i in rng),
               b"|".join(b"%d" % (20 + i) for i in rng),
               b"|".join(b"%d" % (30 + i) for i in rng),
               b"|".join(_hexname(b"HN%d" % i) for i in rng)))


#: ROOM CHAT IS THREE ARMS OF ONE CODE, AND THE MSGID PICKS WHICH.
#: TM.dll's chat manager (the object at `parent+0x1A0`) dispatches every code
#: 0x42 message in the store on the header's msgid field -- rva 0x07E3FF,
#: `a9780(4)`:
#:
#:     cmp eax, 4 / je  0x07E42F    arm A -- the MEMBER LIST, rebuilt wholesale
#:     cmp eax, 6 / jle bail        5, 6 and anything below 4 are dropped
#:     cmp eax, 8 / jle 0x07E8A9    arm B -- a chat LINE; appends it and lights
#:                                  the new-message indicator
#:
#: and msgid **1** is what the client SENDS to ask for arm A's data. The sender
#: is 0x07F540: it stamps `[esp+0x3d0]=0x42`, `[esp+0x3d4]=1` and writes
#: `@Chat=/NN=<POL-ID>/CN=<name>/HID=/Dm=/Vol=` -- with `/NN=` and `/CN=` read
#: from globals ([0x5224BD8]/[0x5224BDC] and [0x5224BE0]) that NOTHING in the
#: image ever writes, which is why every one of them on the wire reads
#: `/NN=0000000000000000/CN=`. The server is meant to fill them in, exactly as
#: it fills `<DE>`'s name and `@Init=/Shm=`. Twenty went unanswered in the
#: 2026-08-20 session.
#:
#: WARNING: AND THE ANSWER IS POLLED FOR, NOT PUSHED AT. The screen pump at 0x0ACD0 is
#: a four-state machine over `[obj+0x64F0]`; state 0 sends the msgid-1 request
#: (0x0AD1C, the arm taken when `[obj+0x18C] == 1`) and states 2 and 3 ask the
#: message store `0x07DFD0(0x42, 4)` on every tick before dispatching. So the
#: answer may arrive whenever -- it sits in the store until the pump looks --
#: but it is only ever LOOKED for while that screen is up. That is why the
#: "rebroadcast a chat line as msgid 4" experiment logged no `find("@Chat")`
#: at all: it was answered into a state that does
#: not poll for msgid 4, not dropped by the chat manager.
MSG_CHAT = 0x42
CHAT_MSGID_WHO = 1              # request  `@Chat=/NN=../CN=../HID=../Dm=../Vol=..`
CHAT_MSGID_ROSTER = 4           # reply    `@Chat=/Num=..|..(/C=)(/NN=)(/CN=)`
CHAT_MSGID_LINE = 8             # a chat line, both directions


def _chat_roster_enabled():
    """OFF by default. WARNING: **A MSGID-4 THAT FAILS ARM A'S GATE IS NEVER ERASED,
    AND IT BLOCKS EVERY LATER CODE-0x42 MESSAGE.**

    Measured 2026-08-20, live, and it cost a tester a working chat indicator:

        13:37  roster served to members 3 and 6
        14:12  chat lines relayed, NO INDICATOR on the receiver
        14:18  roster served again
        15:47  member 6 relaunches Tetra Master -- the store dies with the process
        15:55  indicator WORKS again

    The mechanism is in the bail paths. `0x07E852` (the gate at 0x07E4B0 failing)
    and `0x07E828` (the gather loop finding no part) both `inc esi ; jmp` OUT
    **without calling the erase at 0x81F60** -- only the "@Chat not found" path
    (0x07E855) erases. The dispatcher at 0x07E36C takes the FIRST code-0x42
    message in the store on every tick and arm A ends the function, so one
    un-erased msgid-4 parks permanently in front of the msgid-8 chat lines. The
    store is a TM.dll global (0x5228250) that survives room changes and dies only
    with the process, which is why a relaunch is what cleared it.

    So this stays off until the gate is PROVEN to pass. The suspect is the one
    thing the chat reading flags as never verified: which of `0xAB080`'s
    output pointers takes the value and which takes the found-flag. If `/Num=`
    does not land in the slot 0x07E4A9 reads, the gate compares the shop byte
    against stack garbage and fails on a number we never sent.
    """
    if _chat_control_says() is not None:
        return _chat_control_says()
    return os.environ.get("POL_TM_CHAT_ROSTER", "0") != "0"


#: The live toggle for the roster, RE-READ ON EVERY REQUEST.
#:
#: VERIFIED: THE POINT IS THAT TURNING IT OFF COSTS ONE COMMAND, NOT A DEPLOY. This
#: reply carries a measured hazard -- a msgid-4 that fails arm A's gate is never
#: erased and parks in front of every later code-0x42 message, taking the chat
#: indicator with it until the player relaunches (see `_chat_roster_enabled`).
#: An env knob cannot be changed without recreating the container, which is far
#: too slow to abort a live test with somebody sitting in a room. The teach
#: control file already established this pattern in this module ("re-read per
#: line, so this is tunable to zero without a restart"); this is the same thing
#: for the same reason.
#:
#:     echo 1 > /data/tm_chat_roster.txt      # answer the next request
#:     echo 0 > /data/tm_chat_roster.txt      # stop, takes effect immediately
#:     rm      /data/tm_chat_roster.txt       # fall back to POL_TM_CHAT_ROSTER
#:
#: Absent or unreadable means "no opinion", so the env default still governs and
#: a missing file can never turn the feature ON.
_CHAT_CONTROL_FILE = os.environ.get(
    "POL_TM_CHAT_ROSTER_FILE",
    os.path.join(os.environ.get("POL_DATA_DIR", "/data"), "tm_chat_roster.txt"))


def _chat_control_says():
    """True/False from the control file, or None if it has nothing to say."""
    try:
        with open(_CHAT_CONTROL_FILE) as f:
            text = f.read().strip()
    except OSError:
        return None
    if not text:
        return None
    return text.split()[0].strip().lower() not in ("0", "off", "no", "false")


#: member_id -> the last `/C=` we served them. See `_chat_next_serial`.
_CHAT_SERIAL = {}


def _chat_next_serial(member_id):
    """The next `/C=` for this member -- a serial that only ever goes UP.

    Arm A treats `/C=` as the identity of one roster SNAPSHOT. Having taken the
    first code-0x42 message out of the store, it walks the store for the rest of
    that snapshot's parts (0x07E5A9 parses each candidate's `/C=`) and:

        equal    -> a part of my snapshot: keep it, erase the original
        greater  -> a NEWER snapshot: skip it, it is not mine
        lesser   -> STALE: erase it unread   (0x07E5C4)

    so a serial that rises per snapshot is what lets a client that fell behind
    throw away what it missed instead of drawing it. A constant would do, right
    up until two snapshots sit in the store at once.
    """
    key = _push_key(member_id)
    nxt = (_CHAT_SERIAL.get(key, 0) % 0x7FFFFFFE) + 1
    _CHAT_SERIAL[key] = nxt
    return nxt


def _chat_name(mid, vals):
    """The name to draw on one roster row, as arm A's 16-byte slot wants it.

    The consumer at 0x0AFF3 copies at most 0x10 bytes out of the node
    (`0x07ED50(i, 0, 0x10)`), so 15 characters plus a terminator is the budget.
    `/`, `@` and `|` are the field grammar's own delimiters -- a name carrying
    one would end the value early and shift every later row by one -- so they
    are dropped rather than escaped: this grammar has no escape.
    """
    name = ""
    if vals is not None and len(vals) > 10:
        name = (vals[10] or "")[:15].strip()
    if not name and vals is not None and len(vals) > 4:
        name = (vals[4] or "").strip()
    if not name:
        try:
            import tmroom
            name = tmroom.name_of(mid)
        except Exception:
            name = ""
    name = "".join(c for c in str(name)
                   if c not in "/@|" and 0x20 <= ord(c) < 0x7F)
    return name.strip()[:15].encode("latin1", "replace")


def _chat_roster_rows(member_id):
    """`[(mid, pol_id_bytes, name_bytes), ...]` for this player's room, or None
    if we cannot say which room they are in.

    WARNING: A MEMBER WITH NO POL-ID IS SKIPPED, NOT INVENTED. `/NN=` is matched by
    every receiving client against its own id (0x0AFE3, and see
    `tmroom.note_pol_id`), and it is the id the row carries into whatever the
    player does with that row next. A placeholder would be a row nobody can
    resolve and that its own owner does not recognise as itself -- so it is left
    out, and said out loud instead of being papered over.
    """
    try:
        import tmroom
    except ImportError:
        return None
    try:
        chan = tmroom.room_of(member_id)
    except Exception as exc:
        _say("tm: chat roster -- no room for member %s (%r)" % (member_id, exc))
        return None
    if not chan:
        return None
    rows, missing = [], []
    for mid, vals in tmroom.members(chan):
        ident = tmroom.pol_id_of(mid)
        if not ident:
            missing.append(mid)
            continue
        rows.append((mid, ident.encode("ascii"), _chat_name(mid, vals)))
    if missing:
        _say("tm: chat roster for %s omits member(s) %s -- no POL-ID yet "
             "(it is learned from @Init=/NN=; a client that has not sent one "
             "this session cannot be put on a row anyone could match)"
             % (chan, ", ".join(str(m) for m in missing)))
    return rows


def _chat_roster_body(member_id):
    """The `@Chat=` member list for arm A, or None to stay silent.

    Read off the builder at 0x07E5C0-0x07E71B rather than invented:

        0x07E620  erase the second list (the container at `+0x62AC`)
        0x07E674  find "@Chat"
        0x07E68F  `/Num=` occurrence **1** -- the ENTRY COUNT in this message
        0x07E6A1  loop that many times:
        0x07E6B1      `/NN=` occurrence i  (0xAB470: up to 16 hex digits into a
                                            u64 -- low half to node+0x18, high
                                            half to node+0x1C)
        0x07E6C6      `/CN=` occurrence i  (0xAAD60: the text, node+0x0C)
        0x07E6DE      append the node
        0x07E709  `mov eax,[ebp+0x62b4] ; mov [ebp+0x62b8],eax` -- PUBLISH the
                  count, which is the bound every accessor checks

    Two things about the shape are load-bearing and neither is obvious:

    * **`/Num=` occurrence 0 is a different number from occurrence 1.**
      Occurrence 0 is the highest PART index of a multi-message roster (the
      gather loop at 0x07E4E8 runs `0..occ0` INCLUSIVE, into a 17-slot array of
      0x3E8-byte buffers); occurrence 1 is how many entries this part carries.
      We send one message, so occurrence 0 is 0.
    * **the header's shop byte must EQUAL `/Num=` occurrence 0** -- 0x07E4B0
      compares `a9780(6)` against it and bails on `jne`. It is the "this is the
      last part, reassemble now" trigger, so with a single message both are 0.

    And occurrences are PIPE-separated under one key, not repeated keys: the
    ordinal indexes into a `|` list (see `_vsgame_body`'s banner).
    """
    rows = _chat_roster_rows(member_id)
    if rows is None:
        _say("tm: member %s asked who is in the chat and we do not know which "
             "room they are in -- silent" % (member_id,))
        return None
    parts = 0                                   # one message: parts 0..0
    body = b"@Chat=/Num=%d|%d/C=%d" % (parts, len(rows),
                                       _chat_next_serial(member_id))
    if rows:
        body += b"/NN=" + b"|".join(r[1] for r in rows)
        # NOTE: DELIBERATELY NOT CHANGED, AND FLAGGED. This `/CN=` is parsed by
        # **0xAAD60** (this docstring says so, at 0x07E6C6) -- the same function
        # measured 2026-08-20 to be a HEX DECODER, not a text extractor; see
        # `_str_field`, where the in-match `/CN=` was fixed after a tester
        # photographed a name rendering as `z` followed by squares. If the chat
        # roster's names are squares too, `_str_field(...)` around each row here
        # is the whole fix. It is left alone because this screen is another
        # track's active work and nobody has reported it broken -- shipping an
        # unmeasured change to a screen nobody has seen is the habit this
        # subsystem's ledger exists to stop.
        body += b"/CN=" + b"|".join(r[2] for r in rows)
    header = encode_code(MSG_CHAT | (CHAT_MSGID_ROSTER << 16)
                         | ((parts & 0xFF) << 24))
    _say("tm: member %s chat roster -- %d row(s): %s"
         % (member_id, len(rows),
            ", ".join("%s=%s" % (r[2].decode("latin1"), r[1].decode("ascii"))
                      for r in rows) or "(none)"))
    return header + body


def _init_enabled():
    return os.environ.get("POL_TM_INIT", "1") == "1"


def _init_ans():
    try:
        return int(os.environ.get("POL_TM_INIT_ANS", "1"), 0)
    except ValueError:
        return 1

#: `b/g/TM0IML` IS THE GATE, and its layout is measured, not guessed
#: (TMaster.pex, 2026-08-14). The blob is read to 0x005c64a0, 400 bytes
#: (0x003aaf18 passes a3 = 400), by the read at 0x003aaec0.
#:
#:   +0x00  u32 count      0x00423950 `bne s2, zero` -- ZERO takes the dead
#:                         branch, which is what we have been serving and what
#:                         ends in the 0-37092 timeout. 0x0042398c then loops
#:                         i = 0..count-1.
#:   +0x04  u8  array      chain key, indexed by cursor [0x00450d08]/[0x00450d18]
#:   +0x08  u16 array      chain key, indexed by cursor [0x00450d0c]/[0x00450d1c]
#:   +0x10  entry[i]       48 bytes each. TWO accessors pin the base, and the
#:                         second corrects the first reading of it:
#:                           0x00423dc0 -> u32 [0x005c64bc + i*48]  = entry+0x0c
#:                                         (the FLAG the selection loop matches)
#:                           0x003b7a90 -> u64 [0x005c64b0 + i*48]  = entry+0x00
#:                                         (read on a match, at 0x004239bc)
#:                         0x005c64b0 - 0x005c64a0 = 0x10, so the entry BASE is
#:                         +0x10 and the flag sits at entry+0x0c (= manifest
#:                         +0x1c for entry 0). An earlier note here said "entries
#:                         at +0x1c"; that was the flag offset, not the base.
#:
#: This is the SAME record shape as `_TM0SML_INIT` in responders.py -- 48-byte
#: records, u32 flag at record+0x0c, u64 payload at record+0x00 -- which is a good
#: sign the reading is right, since that one was reversed independently.
#:
#: Minimal manifest: count=1, entry[0].flag=1 -- 400 bytes, everything else zero.
#: Written per-member to `/data/resources/<member>.b_g_TM0IML.bin`, which
#: `_resource_blob` prefers over `RESOURCE_INIT`, so it takes effect with NO
#: restart and NO dropped session. Delete those files to revert, also live.
#:
#: WARNING: THE ERROR-CODE DECODE THIS WORKSTREAM RELIES ON IS UNSOURCED. A full-tree
#: search for `37088` returns exactly two hits, both in the Janhourou save-data notes,
#: and both merely repeat "TM's error table decodes 37088/37092/37312 as *timed
#: out while reading default data*" as a group. There is no per-code decode
#: anywhere in the repo, no such table in the PS2 `tm-xlate` string table (1638 rows), and
#: TMaster.pex materialises none of the three as an immediate. So the family
#: reading is an assertion carried forward, not a measurement, and **which code is
#: "further along" cannot currently be answered**. Anyone using the on-screen
#: number as a progress signal should find or rebuild that table first.
#:
#: MEASURED 2026-08-14 from a savestate at the error (PS2 savestate probe):
#:
#: * **the manifest LANDS.** `0x005c64a0` reads count=1 with entry[0].flag=1 at
#:   +0x1c, exactly as written. The stored-file delivery route
#:   (`/data/resources/<member>.b_g_TM0IML.bin`, no restart) works, and the layout
#:   reversal above is confirmed against the client's own copy.
#: * **the chain still does not run.** Both destination buffers (`0x005c6ef0`,
#:   `0x005c6ed0`) are entirely zero and no `b/g/TM0RkData` request has ever
#:   appeared on the wire. The chain cursors are non-zero (3 and 2) but there is
#:   no baseline for them, so do not read that as "it iterated".
#: * the matched entry's **u64 at entry+0x00 is zero**, and `0x004239bc` reads it
#:   the instant the flag matches. Still the best candidate for the next field.
#:
#: WARNING: **AND A CORRECTION: "count=1 changed the error" is NOT established.** It was
#: reported as 0-37092 before and 0-37088 after, but the tester was unsure the
#: second was new, and **37092 does not exist in the client's own error table**
#: (396 records, codes 30024..39912, live at `0x0072f0f4..0x00730378`, records
#: `{u32 code, u32 category, u32 extra}`; 37088 -> {1542, 32} and 37312 ->
#: {1541, 32} are both present). So the safe reading is that we have ONE confirmed
#: code, 37088, and no reliable evidence the manifest moved it at all. Do not
#: build on "the error responds to manifest content" until a controlled
#: before/after pair says so.
#:
#: WARNING: CORRECTION 2026-08-14: `b/g/TM0RkData` is **RANKING** data
#: ("Rk" -- cf. the POLpro class 'R' = RANKING / sqMgRkcp* in
#: the Janhourou save-data reading), fetched by a DIFFERENT subsystem. Its only caller
#: is 0x003fc0f4, inside an s4-based state machine with fields around +0x5bb0,
#: not the default-data path at 0x004238xx. The manifest's u8/u16 arrays are read
#: BY that ranking reader when ranking runs -- which it does not during startup.
#: So "no b/g/TM0RkData request appeared" was never evidence the manifest failed,
#: and it was chosen as the pass/fail signal in error.
#:
#: THE MANIFEST WORKS, PROVEN: a traceable sentinel u64 (0x5a5a5a5a00000001)
#: written to entry[0]+0x00 turned up at **[0x005d15e0]**, and 0x004239cc stores
#: there ONLY on a flag match. So the selection loop runs, matches entry[0], and
#: reads our u64. Everything from the fetch through the match is confirmed
#: end-to-end against the client's own memory. The failure is strictly AFTER the
#: match -- 0x00423a18 onward, and whatever consumes [0x005d15e0] (readers at
#: 0x0037726c, 0x003b7438, 0x003b7810, 0x003e6cb4).
#:
#: WHAT THE CHAIN THEN DOES, and why this is staged: a non-zero count makes the
#: client run chained sub-reads (`sqMgCpReadFile` 0x003ab910) of
#: **`b/g/TM0RkData`** (0x00486470, 24-byte records, buffer 0x005c6ef0,
#: 0x003aabd0) and a second 20-byte reader at 0x003ab740. Neither path is in
#: `_FETCH_PATHLEN`, so they would currently fall back to the opcode default --
#: the same class of wrong-length read that stalled `U/g/TM0DataFile` at 664.
#: **First launch answers only "does the chain start at all?"** If `b/g/TM0RkData`
#: appears in lobby.log, the model is confirmed and the lengths get added next.
TM0IML_LAYOUT = {"count": 0x00, "u8_array": 0x04, "u16_array": 0x08,
                 "entries": 0x1c, "entry_size": 48, "entry_flag": 0x00,
                 "total": 400}

#: The default-data version we teach the client. Re-read per line, from a CONTROL
#: FILE first (bind-mounted /logs, so it can be retuned with NO container recreate
#: and NO dropped connection), falling back to env. Format: one line `D V`
#: (decimal). D is a byte, V a halfword.
#:
#: ~~HYPOTHESIS (2026-08-13): D/V=0/0 reads as "default data version 0 = nothing
#: to fetch" ... Non-zero D/V may make it FETCH.~~ **REFUTED LIVE 2026-08-14 on
#: the PS2 client.** Served `D=1/V=1` (confirmed on the wire in authserv.log) and
#: the client fetched exactly the same two paths as with 0/0 -- `b/g/TM0IML` and
#: `U/g/TM0DataFile`, never `TM0SML`/`TM0SML2` -- and raised the same 0-37092.
#: **D and V do not select which files the client asks for.** They are passed as
#: PARAMETERS of the read itself (`0x005b7ca8` byte and `0x005b7cb8` halfword are
#: both loaded into the `sqMgCpReadFile` argument list at 0x003aaef8/0x003aaf10),
#: so they are a version the SERVER is meant to key its answer on, not a client
#: switch. Left at 0/0; do not spend another run toggling them.
#:
#: What actually gates the default-data path is the TM0IML manifest -- see
#: TM0IML_LAYOUT below.
_TEACH_FILE = os.environ.get("POL_TM_TEACH_FILE", "/logs/tm_teach.txt")


def _teach_dv():
    try:
        with open(_TEACH_FILE) as f:
            d, v = f.read().split()[:2]
            return int(d) & 0xFF, int(v) & 0xFFFF
    except (OSError, ValueError):
        return (int(os.environ.get("POL_TM_TEACH_D", "0")) & 0xFF,
                int(os.environ.get("POL_TM_TEACH_V", "0")) & 0xFFFF)


def _teach_d():
    return _teach_dv()[0]


def _teach_v():
    return _teach_dv()[1]


def _teach_enabled():
    return os.environ.get("POL_TM_TEACH", "1") == "1"


#: THE PC BUILD WANTS FOUR VERSION SETS, NOT ONE -- and this is why Tetra Master
#: on the PC died at `TRM-0-37088 "Timed out while reading default data."` while
#: the PS2 sailed past on the same reply. Measured 2026-08-15 in the UNPACKED
#: `TM.dll` (the disk copy has .text rawsize=0; these come from a full-memory dump
#: of a live pol.exe, TM.dll based at 0x05340000 in that process).
#:
#: The handler at 0x053c134d parses `@TeachDVAns` field by field, and the field
#: extractor 0x53eb080 takes an OCCURRENCE INDEX as its second argument -- so a
#: name repeated in the message yields successive values:
#:
#:   /D=   x1  -> byte [0x55cbb68]          | debug 'LN D,V = %d %d'
#:   /V=   x1  -> word [0x55f2950]          |
#:   /AC=  x2  -> D byte [0x55c36cc], V word [0x55c36a8]   'AC D,V = %d %d'
#:   /RK=  x2  -> D byte [0x55b95e0], V word [0x55ef4c0]   'RK D,V = %d %d'
#:   /Shm= x3  -> occurrence 0 goes to a DIFFERENT parser (0x53eb470) that fills
#:                two dwords at [0x558f590]/[0x558f594] -- the `%08x%08x` of
#:                'SH = %08x%08x, D,V = %d %d' -- then D byte [0x55d8248] and
#:                V word [0x55ba8f0].
#:
#: PROOF IT IS THE PAYLOAD AND NOT A TIMING RACE: our own reply
#: `GTM0GE2000000@TeachDVAns/D=1/V=1` was found verbatim in the client's receive
#: buffer at 0x0565fa85 in that dump. It arrived and was parsed; it simply had
#: nothing for the other three sets, so the client kept waiting. (The `<CI>`
#: 1500 ms delay note above therefore still stands -- do not re-run it for this.)
#:
#: AC/RK/Shm are the AUCTION, RANKING and Shm data sets, matching the PC-only
#: paths `b/g/TM0AucData` and `b/g/TM0RkData`. The PC build has NO `b/g/TM0IML`
#: string at all, so the PS2's manifest step does not exist here.
#:
#: All zero = "version 0 of everything", the same least-committal answer `D=0/V=0`
#: already makes for LN. Tunable live -- the control file is re-read per line, so
#: no restart:
#:
#:     0 0                     <- D V, as before
#:     ac=0,0                  <- AC D,V
#:     rk=0,0                  <- RK D,V
#:     shm=0000000000000000,0,0  <- Shm 64-bit hex, D, V
#:     ext=0                   <- omit AC/RK/Shm entirely (the old PS2-only reply)
#:
#: `ext` defaults to 1. Sending the extra fields to a PS2 client is expected to be
#: harmless -- its parser (TMaster.pex 0x00298dc0) strstr's for the names it wants
#: and ignores the rest -- but `ext=0` reverts instantly if that turns out wrong.
def _teach_delay():
    """Hold a TM0 reply back `delay=<ms>` before sending it.

    Default 1500 ms, matching POL_POLPRO_DELAY_MS -- the same race, on the same
    client, one channel over. `delay=0` restores the instant reply, which is what
    the PS2 has always had and been fine with; if this turns out not to be the
    fault, set it back to 0 rather than leaving an unexplained pause in the path.
    """
    try:
        ms = int(_teach_opts().get("delay", "1500"), 0)
    except ValueError:
        return
    if ms > 0:
        time.sleep(min(ms, 30000) / 1000.0)


def _teach_opts():
    """`key=value` tokens from the teach control file; {} if absent/unreadable."""
    out = {}
    try:
        with open(_TEACH_FILE) as f:
            text = f.read()
    except OSError:
        return out
    for tok in text.split():
        key, sep, val = tok.partition("=")
        if sep:
            out[key.strip().lower()] = val.strip()
    return out


#: `/Shm=` IS A PEER ID, NOT A VERSION (2026-08-16). `tm_init_build` (TM.dll rva
#: 0x87F10) writes the parsed `/Shm=` u64 straight into the outgoing @Init
#: record's id field at `+0x10`/`+0x14` (`mov [esp+0x24],esi` at 0x881CF) and
#: then calls the send, 0x82060 -- which REFUSES to transmit a message with no
#: destination: `if ([rec+0x10]|[rec+0x14]) == 0 and rec+0x18 (the nick) == ""
#: and the global at 0x242958 == 0: return 0`, silently, nothing on the wire.
#: Serving sixteen zeros is why `@Init=` has never once appeared in a capture.
#:
#: The id is computable from the peer's NICK, with no lookup and no state:
#:
#:     nick --polnick.polid_for_nick--> POL ID --base-36 over polnick.ALPHA-->
#:     client_guid
#:
#: verified against `accounts.db`: one test account (POL ID EFGH5678, nick
#: UH5GRSV86) has the stored `client_guid` 0x162E92CDC54, exactly b36(ALPHA).
#: The TM0 peer the client addresses is nick USH6MZJA7 = POL ID `AAAC0001`, a
#: SYNTHETIC service id the client makes up locally -- it is not a member of
#: ours, which is why nothing server-side has ever known about it.
#:
#: WARNING: ONE OPEN VARIABLE. In client memory these ids appear as `client_guid XOR K`
#: (K = 0xAB12CDD0E4BCBB90 on the measured install, constant across both peers --
#: the client-local guid key). Whether the value we put in
#: `/Shm=` is expected in RAW or XORed form is NOT yet established. We serve RAW,
#: because raw is the form the server can compute for anyone; if the launch shows
#: `@Init=` addressed to a garbage nick instead of the peer, the XOR is needed and
#: that wrong nick hands us K directly. Either outcome is one launch.
#: VERIFIED: THE PEER NICK IS THE TABLE, AND IT IS THE FIELD THAT WAS MISSING.
#:
#: Measured 2026-08-20T00:59-01:00Z with a tester narrating the flow: they
#: reserved TABLE 1 with default settings, saved, then switched to TABLE 2 and
#: reserved that. The wire:
#:
#:   00:59:30  -> UE7QN1N9G   @Save=/Tbl=1/Game=0 ...   <- names table 1
#:   00:59:34  -> UE7QN1N9G   @Tet=/../tl=3 @Tab=/..    <- the custom rules
#:   01:00:37  -> UE7QN1N9N   @Tet=/.. @Tab=/..         <- table 2
#:
#: and the nicks decode (`polnick` + the base-36 fold below) to
#:
#:   UE7QN1N9G  ->  0xD0E4BCBB91     table 1
#:   UE7QN1N9N  ->  0xD0E4BCBB92     table 2
#:   UKXDBA266  ->  0xF0E4BCBB91     the ROOM's roster peer -- the nick every
#:                                   class-L `<DR>` is addressed to
#:
#: So a table peer is `0xD0` over the room's key and the room itself is `0xF0`
#: over the same key, one CLASS NIBBLE apart. `@Save=/Tbl=1` landing on the
#: `...91` peer is the cross-check, and it is why `Tbl` is parsed and compared
#: rather than trusted on its own.
#:
#: VERIFIED: AND IT CLOSES A STANDING UNKNOWN. `responders`'s `_SUBJECT_KEYED_PATHS`
#: note says "`b/g/PTL`'s 0xf0e4bcbb91 is unexplained -- it is NOT the room id the
#: client reports in its own `<DE>`". It is the ROOM PEER'S GUID. Same value,
#: arrived at from the other end.
#:
#: WARNING: THE INDEX FORMULA IS INFERRED FROM ONE ROOM. Two tables in `#TM0R001` fit
#: `index = table_low32 - room_low32 + 1`; a second room would either confirm it
#: or show the base is per-room in a way one room cannot reveal. That is why
#: `table_index_for_peer` returns None rather than a guess when it has no room
#: peer to measure against, and why every resolution logs what it used.
TABLE_PEER_CLASS = 0xD0
ROOM_PEER_CLASS = 0xF0


def peer_guid(peer_nick):
    """The u64 behind a TM0 peer nick, or None. The fold is `_teach_shm`'s, which
    was confirmed live by `@Init` reaching the card shop."""
    if not peer_nick:
        return None
    try:
        import polnick
        nick = (peer_nick.decode("ascii", "ignore")
                if isinstance(peer_nick, bytes) else peer_nick)
        pid = polnick.polid_for_nick(nick)
        if not pid:
            return None
        guid = 0
        for ch in pid:
            guid = guid * 36 + polnick.ALPHA.index(ch)
        return guid & 0xFFFFFFFFFFFFFFFF
    except Exception:
        return None


def peer_class(guid):
    """The class nibble-pair: 0xD0 for a table, 0xF0 for a room."""
    return None if guid is None else (guid >> 32) & 0xFF


def _is_table_class(cls, room_cls, room_no=None):
    """Is `cls` a TABLE peer class? ROOM- and era-invariant.

    WARNING: THE LOW NIBBLE IS THE ROOM NUMBER, NOT A FIXED "1" -- the fourth gate
    era, measured live 2026-08-21T16:24: room 3's table peer arrived class
    **0xD3** and was dropped because the rule wanted low nibble 1. Every
    measured table class fits one shape: high nibble = the re-allocation ERA
    (drifts F->E->D per restart), **low nibble = the ROOM NUMBER**:
        room 1 -> 0xF1 / 0xE1 / 0xD1   (low 1)
        room 3 -> 0xD3                 (low 3)
    and the ROOM peer is that minus one in the low nibble (room 1 -> 0xF0).
    So a table peer's class low nibble equals `room_no`; that is the stable
    fact across every era AND room. The old "low nibble == 1" only worked in
    room 1 by coincidence. Legacy 0xD0 and the room+1 relation are kept as
    fallbacks for environments where `room_no` is unknown.
    """
    if cls is None:
        return False
    if cls == TABLE_PEER_CLASS:
        return True                       # the legacy absolute (0xD0)
    # The ERA is the high nibble and drifts F -> E -> D per re-allocation; a
    # real table/room peer is in that band, which is what separates it from a
    # foreign family (e.g. 0xA2). Extend the band if it ever drifts below 0xD.
    hi = (cls >> 4) & 0x0F
    era_band = hi in (0x0D, 0x0E, 0x0F)
    if room_no is not None and room_no >= 1 and era_band \
            and (cls & 0x0F) == (room_no & 0x0F):
        return True                       # ERA band + low nibble == room number
    if room_no is None and era_band and (cls & 0x0F) == 0x01:
        return True                       # room unknown: the old family-bit
    if room_cls is not None and cls == ((room_cls + 1) & 0xFF):
        return True                       # the 2026-08-20 room+1 relation, kept
    return False


def _exact_table_index(guid, chan):
    """`(index, why)` from the id key, or None if this peer is not one of ours.

    `tmroom.app_id_for_guid` undoes the wire truncation exactly, so this is a
    LOOKUP, not an inference: the reconstructed id either equals a table id we
    published in this room or it does not. Returns None (never a guess) so the
    caller's heuristic still gets its turn -- which matters while `K` is a
    default rather than a value derived at runtime.
    """
    if not chan:
        return None
    try:
        import tmroom
        room_no = tmroom._room_no(chan)
        if not room_no:
            return None
        app = tmroom.app_id_for_guid(guid)
        if app == tmroom.room_id_for(chan):
            return None                   # the ROOM peer, not a table
        for i in range(64):
            if app == tmroom.canonical_table_id(i, room_no):
                return i + 1, ("peer %#x ^ K -> id %#018x = table %d (exact)"
                               % (guid, app, i + 1))
    except Exception:
        return None
    return None


def table_index_for_peer(peer_nick, room_peer_guid, tbl_hint=None, chan=None):
    """`(index, why)` -- which table this peer is, and how we decided.

    `index` is None when we cannot say. `tbl_hint` is `@Save=`'s own `/Tbl=`,
    which is GROUND TRUTH when present: it is used, and any disagreement with
    the computed value is reported rather than quietly preferred either way.
    """
    guid = peer_guid(peer_nick)
    if guid is None:
        return (tbl_hint, "no peer nick; /Tbl= only") if tbl_hint else (None, "no peer nick")
    # VERIFIED: THE EXACT PATH (2026-08-22). A peer nick carries the low 40 bits of
    # `app_id ^ K`, so `guid ^ K` IS the id we published -- no eras, no block
    # bases, no class nibbles. Try that first and fall through to the measured
    # heuristic below when it does not land, so nothing that used to resolve
    # stops resolving. See `tmroom.CLIENT_KEY_DEFAULT` for the derivation.
    exact = _exact_table_index(guid, chan)
    if exact is not None:
        idx, why = exact
        if tbl_hint is not None and tbl_hint != idx:
            return (tbl_hint, "%s but /Tbl= says %d -- TAKING /Tbl="
                    % (why, tbl_hint))
        return idx, why + (" (/Tbl= agrees)" if tbl_hint is not None else "")
    # WARNING: THE CLASS DRIFTS WITH THE ALLOCATION ERA -- the third gate era in two
    # days, each measured live off a dropped reservation:
    #   0xD0            the legacy absolute (earlier environment)
    #   0xF1            2026-08-20T22:48 -- room 0xF0 + 1, the RELATION fix
    #   0xD1            2026-08-21T12:33:09 -- room peer STILL 0xF0, so the
    #                   room+1 relation ALSO broke: the client re-allocates its
    #                   TABLE peers into a new era (high nibble F->E->D, one
    #                   step per re-allocation/restart -- two deploys that day)
    #                   while it keeps ADDRESSING the room by its ORIGINAL
    #                   peer. Same whack-a-mole as the k*0x1000 base drift the
    #                   block below already survives, one field over.
    # What is stable across every measured value is the LOW NIBBLE: rooms are
    # x0, tables x1. `_is_table_class` holds the whole rule; the constants are
    # pinned in `_selftest_peer_class`, including 0xD1 (the live reject).
    cls = peer_class(guid)
    room_cls = peer_class(room_peer_guid) if room_peer_guid is not None else None
    _rn = None
    try:
        cn = chan.decode("latin1") if isinstance(chan, bytes) else (chan or "")
        if cn.startswith("#TM0R"):
            _rn = int(cn[5:])
    except (ValueError, AttributeError):
        _rn = None
    if not _is_table_class(cls, room_cls, _rn):
        return (None, "peer %#x is class %#04x, not a table (low nibble %#x != "
                "room %s, not %#04x%s)"
                % (guid, cls or 0, (cls or 0) & 0x0F, _rn, TABLE_PEER_CLASS,
                   ", not room+1 %#04x" % ((room_cls + 1) & 0xFF)
                   if room_cls is not None else ""))
    if room_peer_guid is None:
        return (tbl_hint,
                "peer %#x is a table but no room peer is known to index it "
                "against%s" % (guid, "; /Tbl= used" if tbl_hint else ""))
    # WARNING: TWO BASES, ONE MEASURED LIVE. The original formula indexed tables from
    # the room peer's own low half (base +0). On 2026-08-20T22:57 a tester
    # clicked TABLE 1 and the peer arrived as 0xF1E4BCAB91 against a fresh room
    # peer 0xF0E4BCBB91 -- the table block sits 0x1000 BELOW the room peer in
    # this allocation era, and the old base computed -4095. Both bases are tried
    # and at most one can land in 1..64 (they are 0x1000 apart), so this cannot
    # mis-index a peer the old scheme handled. Which base matched is in the
    # `why`, so the day a third era appears it names itself.
    low, room_low = guid & 0xFFFFFFFF, room_peer_guid & 0xFFFFFFFF
    # VERIFIED: RESTART-RESILIENT INDEXING (2026-08-21). SEATING MUST SURVIVE AN
    # authsess RESTART -- the server owner's standing requirement, and the bug that
    # broke it: the table-peer block sits a WHOLE number of 0x1000 blocks below
    # room 1's peer, and that offset DRIFTS by another 0x1000 every time the
    # client re-allocates its peers, which is exactly what a restart forces (the
    # client resumes on a NEW allocation while we kept the OLD room peer). The
    # table peers STEP per-table within a block; the block itself moves. Two
    # rooms measured 2026-08-21T00:02: room peers step with the room number
    # (room 1 low ...BB91, room 2 ...BB92) while table 1 is ...AB91 in BOTH, so
    # the block is GLOBAL at room1_low - k*0x1000.
    #
    # WARNING: THE OLD CODE HARD-CODED k = 1 (two bases) AND A THIRD ERA FELL THROUGH:
    # 2026-08-21T04:13, room 3 peer ...BB93, TABLE 2 arrived as ...9B92 (block at
    # -0x2000, k = 2) and dropped as "both out of range" -- every reservation of
    # the night. Hard-coding eras is whack-a-mole and re-breaks on the next
    # restart.
    #
    # THE INDEX DOES NOT DEPEND ON k. Only the position WITHIN the 0x1000 block
    # matters, and that is stable across any number of re-allocations. Normalise
    # room N's peer back to room 1's origin (undo the per-room +(N-1) step) and
    # reduce mod 0x1000: the k*0x1000 drift cancels, so seating survives every
    # restart. `/Tbl=` stays ground truth; an in-block offset past the table
    # count is still rejected as not-a-table-here. Verified vs every measured
    # peer: room1 t1 AB91->1, room1 t3 8B93->3, room3 t2 9B92->2 (the failing one).
    room_no = 0
    try:
        cn = chan.decode("latin1") if isinstance(chan, bytes) else (chan or "")
        if cn.startswith("#TM0R"):
            room_no = int(cn[5:])
    except (ValueError, AttributeError):
        room_no = 0
    base_used = (room_low - (room_no - 1 if room_no >= 1 else 0)) & 0xFFFFFFFF
    offset = (low - base_used) & 0xFFF          # position within the 0x1000 block
    idx = offset + 1
    if not (1 <= idx <= 64):
        return (tbl_hint, "peer %#x in-block offset %d against room %#x (norm "
                          "base %#x) is out of 1..64 -- not a table in this room"
                % (guid, offset, room_peer_guid, base_used))
    if tbl_hint is not None and tbl_hint != idx:
        # BOTH SOURCES SPOKE AND THEY DISAGREE. Say so loudly and take the
        # client's own number -- the formula is the inferred half.
        return (tbl_hint, "peer %#x indexes to %d but /Tbl= says %d -- TAKING "
                          "/Tbl=, the formula is what is inferred here"
                          % (guid, idx, tbl_hint))
    return idx, "peer %#x - base %#x -> table %d%s" % (
        guid, base_used, idx, " (/Tbl= agrees)" if tbl_hint is not None else "")


def _teach_shm(opts, peer_nick):
    """The `/Shm=` u64: the peer id, derived from `peer_nick`. `shm=` overrides."""
    raw = opts.get("shm")
    if raw is None and peer_nick:
        try:
            import polnick
            pid = polnick.polid_for_nick(
                peer_nick.decode("ascii", "ignore")
                if isinstance(peer_nick, bytes) else peer_nick)
            guid = 0
            for ch in pid:
                guid = guid * 36 + polnick.ALPHA.index(ch)
            return "%016X" % (guid & 0xFFFFFFFFFFFFFFFF), 0, 0
        except Exception:
            pass                       # unknown nick shape -> fall through to zeros
    shm = (raw or "0000000000000000,0,0").split(",")
    shm_hex = (shm[0] if shm and shm[0] else "0").strip()
    try:
        shm_hex = "%016X" % (int(shm_hex, 16) & 0xFFFFFFFFFFFFFFFF)
    except ValueError:
        shm_hex = "0" * 16
    try:
        return shm_hex, int(shm[1], 0) & 0xFF, int(shm[2], 0) & 0xFFFF
    except (IndexError, ValueError):
        return shm_hex, 0, 0


def _teach_pair(opts, key, default=(0, 0)):
    """`key=D,V` -> (D & 0xFF, V & 0xFFFF)."""
    raw = opts.get(key)
    if not raw:
        return default
    parts = raw.split(",")
    try:
        return int(parts[0], 0) & 0xFF, int(parts[1], 0) & 0xFFFF
    except (IndexError, ValueError):
        return default


#: WARNING: THE `=` AFTER THE COMMAND NAME IS LOAD BEARING ON THE PC (2026-08-16).
#: The PS2 parser strstr's `@TeachDVAns` out of the raw body, so `@TeachDVAns/D=0`
#: parses there. The PC does NOT: `TM.dll` rva 0xAA3C0 tokenises the whole body
#: into an associative container keyed on the text from `@` up to the NEXT `=`,
#: and rva 0xAA920 (`@TeachDVAns` / `@InitAns`) is a lookup in THAT container --
#: not a substring search. With `@TeachDVAns/D=0` the key stored is
#: `@TeachDVAns/D`, every lookup misses, and no field is ever parsed. The value
#: is taken from the first `/` after the `=` up to the next `@` or end of body,
#: which is why the fields still follow the `=` unchanged. TM's own outgoing
#: commands all carry it (`@TeachDV=`, `@Init=`, `@Opt=`, `@Save=` ... rva
#: 0x22241C ff), and the `Ans` literals do not -- because they are keys.
#: Adding it is safe for the PS2: `@TeachDVAns=` still contains `@TeachDVAns`.


def _teachdvans_body(d, v, peer_nick=None):
    """The `@TeachDVAns` payload, PC field set included unless `ext=0`.

    Field ORDER here is the order the PC handler reads them in, which costs
    nothing and keeps the line comparable to a real capture if one ever turns up.
    Occurrence count is what matters to the parser, not position.
    """
    body = b"@TeachDVAns=/D=%d/V=%d" % (d, v)
    opts = _teach_opts()
    if opts.get("ext", "1") != "1":
        return body
    ac_d, ac_v = _teach_pair(opts, "ac")
    rk_d, rk_v = _teach_pair(opts, "rk")
    shm_hex, shm_d, shm_v = _teach_shm(opts, peer_nick)
    return body + (
        b"/AC=%d/AC=%d/RK=%d/RK=%d/Shm=%s/Shm=%d/Shm=%d"
        % (ac_d, ac_v, rk_d, rk_v, shm_hex.encode("ascii"), shm_d, shm_v))


#: WRITING THE PLAYER SAVE. The client has NO save-write API -- only
#: `sqMgCpLoadPlayerSaveData(+Check)`; SE's SERVER owned `U/g/TM0DataFile` and
#: built it from game events. So persistence is our job,
#: and it is done by patching the member's stored blob, which `_resource_blob`
#: then serves verbatim on the next 3:0 read -- no rebuild, no restart.
#:
#: Offsets are read off the gates that consume them:
#:     +0x3B  u8  GUILD   0 unset · 1 Warriors · 2 Voyagers · 3 Summoners
#:                        gate TM.dll rva 0x14C866: guild==0 -> draw the guild
#:                        screen, else skip to state 0x15.
#:     +0x126 u8  the "skip handle linking" checkbox (@Opt=/HNSS=)
_SAVE_LEN = 12328                       # what sqMgCpReadFile asks for
_SAVE_FILE_LEN = _SAVE_LEN + 4          # + the 4-byte trailer the signer fills
SAVE_OFF_GUILD = 0x3B


def _save_file(member_id):
    """The stored save blob for one member -- the name `_resource_file` builds.

    WARNING: Resolve the directory the way `responders.RESOURCE_DIR` does, POL_RESOURCE_DIR
    FIRST. Deriving it from POL_DATA_DIR alone agrees today only because nothing
    sets the override; the day something does, we would write a save the lobby
    never reads and the guild screen would come back with nothing in the log to
    say why.
    """
    root = os.environ.get("POL_RESOURCE_DIR")
    if not root:
        root = os.path.join(os.environ.get("POL_DATA_DIR", "/data"), "resources")
    return os.path.join(root, "%s.U_g_TM0DataFile.bin" % member_id)


def _save_patch(member_id, off, value):
    """Set one byte of a member's save, creating the blob if it does not exist.

    Returns (old, new) or None if nothing was written. Atomic: the replace is
    what the lobby's next read sees, so a torn file can never be served.
    """
    if member_id is None:
        return None
    path = _save_file(member_id)
    try:
        with open(path, "rb") as f:
            buf = bytearray(f.read())
    except OSError:
        buf = bytearray(_SAVE_FILE_LEN)      # a fresh player: all zeros
    if len(buf) < _SAVE_FILE_LEN:
        buf.extend(b"\x00" * (_SAVE_FILE_LEN - len(buf)))
    old = buf[off]
    if old == value:
        return None                          # already right; do not churn the file
    buf[off] = value & 0xFF
    try:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        tmp = path + ".tmp"
        with open(tmp, "wb") as f:
            f.write(buf)
        os.replace(tmp, path)                # atomic
    except OSError:
        return None
    return old, value & 0xFF


def _guild_persist_enabled():
    return os.environ.get("POL_TM_GUILD_PERSIST", "1") == "1"


def _save_patch_many(member_id, changes):
    """`_save_patch_fields` for a map of single BYTES -- `{offset: value}`.

    The `@Opt=` shape, kept because that is what every option field is. Anything
    with a dword in it (the table-settings preset) goes through
    `_save_patch_fields` directly.
    """
    return _save_patch_fields(member_id,
                              {off: (1, val) for off, val in changes.items()})


def _save_patch_fields(member_id, changes):
    """Set SEVERAL fields of a member's save in one rewrite.

    `changes` is {offset: (width, value)}, width 1 or 4. Returns
    {offset: (old, new)} for the fields that actually moved, or `{}`.

    WARNING: WIDTH IS NOT COSMETIC. The options block is all bytes, but the table
    preset's `bm`/`lu`/`ll`/`au`/`al` are DWORDS -- `0x14DB61` reads +0xE0 with
    `mov eax, dword ptr [...]`. Writing `au=300` as a byte stores 44, and a rank
    band of 0..44 refuses everybody exactly the way 0..0 did.

    Same file, same atomic replace as `_save_patch` --
    that is the one-byte case of this, and the two should fold together when
    whoever owns this file next touches it; kept separate today only because
    `_save_patch` was in flight uncommitted when this landed.

    `@Opt=` carries TWELVE option bytes, so doing it a byte at a time would
    rewrite 12,332 bytes twelve times and leave eleven windows in which the
    lobby could serve a half-applied save.
    """
    # THREE WAYS THIS COULD FAIL WITHOUT SAYING SO -- all now audible. None of
    # them is what prod was doing, and that took a wrong diagnosis to establish:
    # twelve `@Opt=` arrived and produced no save patch, which reads like a lost
    # write and is not one. The client ECHOES its whole option set every time, so
    # with a zero header it sends zeros back and NOTHING MOVES -- `moved` is
    # empty and there is correctly nothing to log. The proof is member 6's own
    # header: the only non-zero option bytes it had were +0x103 and +0x126, which
    # are exactly the two the client sent as non-zero (`CT=60`, `HNSS=1`). This
    # writer put them there. It works.
    #
    # The three below are real latent traps, not the observed behaviour, and the
    # READ one is the dangerous member of the set: falling back to zeros on a
    # read error would write a ZERO HEADER over a good save and manufacture the
    # very bug `tmsave.DEFAULT_HEADER` exists to prevent.
    if not changes:
        return {}
    if tmsave is None:
        # The one hard dependency: `write_field` owns the width encoding, and
        # open-coding it here is how the two would drift. Say so rather than
        # writing a partial header.
        _say("tm: %d setting(s) cannot be saved -- tmsave did not import"
              % len(changes))
        return {}
    if member_id is None:
        _say("tm: @Opt= carried %d setting(s) but this session has NO MEMBER -- "
              "DISCARDED. The client was told /Ans=1, so it believes they were "
              "saved and will not resend them." % len(changes))
        return {}
    path = _save_file(member_id)
    try:
        with open(path, "rb") as f:
            buf = bytearray(f.read())
    except FileNotFoundError:
        # A player with no save yet. Mint one the way `tmsave.build` does, so a
        # first-ever settings write does not create the zero header that
        # `tmsave.DEFAULT_HEADER` exists to prevent.
        buf = bytearray(_SAVE_FILE_LEN)
        if tmsave is not None:
            for off, (width, val) in tmsave.default_header_writes().items():
                tmsave.write_field(buf, off, width, val)
        _say("tm: member %s has no save; minting one with the default header "
              "before applying %d setting(s)" % (member_id, len(changes)))
    except OSError as exc:
        # WARNING: NEVER FALL BACK TO ZEROS ON A READ ERROR. The old code did, so a
        # permissions or I/O fault would have rebuilt the save from nothing and
        # written a ZERO HEADER over a good one -- turning a transient error into
        # the exact bug `tmsave.DEFAULT_HEADER` documents.
        _say("tm: member %s save is UNREADABLE (%r) -- refusing to write, "
              "because rebuilding it from zeros is how settings get destroyed"
              % (member_id, exc))
        return {}
    if len(buf) < _SAVE_FILE_LEN:
        buf.extend(b"\x00" * (_SAVE_FILE_LEN - len(buf)))
    moved = {}
    for off, (width, value) in sorted(changes.items()):
        if not (0 <= off and off + width <= _SAVE_FILE_LEN):
            continue
        got = tmsave.write_field(buf, off, width, value)
        if got:
            moved[off] = got
    if not moved:
        return {}                            # already right; do not churn the file
    try:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        tmp = path + ".tmp"
        with open(tmp, "wb") as f:
            f.write(buf)
        os.replace(tmp, path)                # atomic
    except OSError as exc:
        # The resources directory is root-owned on prod; a container that cannot
        # write it silently threw away every setting the player changed.
        _say("tm: member %s save WRITE FAILED (%r) -- %d setting(s) lost. The "
              "client was told /Ans=1 and will not resend them."
              % (member_id, exc, len(moved)))
        return {}
    return moved


#: `@Opt=` IS THE PROFILE WRITE-BACK CHANNEL -- the general case of the guild
#: write above, and the thing that closes the "already linked to handle X, start
#: game?" prompt. Read off `TM.dll.unpacked`:
#:
#:   * the sender is `0x13F800` / `0x140ACF`, both `push 0x5245E8C ; call
#:     0x5018370`, and `0x5245E8C` is the base of the option-globals block that
#:     `apply_options_from_save` (`0x14D9F0`) fills STRAIGHT OUT OF THE SAVE.
#:     So the twelve dwords of that struct are the twelve save bytes below, and
#:     `@Opt=` is the client telling us which of them it just changed.
#:   * `0x140AAD` sends it **only when the value actually moved**
#:     (`if ([0x5245EB8] == [scene+0x150]) skip`), and `[0x5245EB8]` is the
#:     "skip handle linking from now on" checkbox on that very prompt. Tick the
#:     box -> `@Opt=/…/HNSS=1` -> this write -> the prompt stops drawing.
#:
#: The client CLAMPS three of these on load, so a value we cannot make sense of
#: is not dangerous: `+0x100`/`+0x101` are masked `& 1`, `+0x102` folds anything
#: above 0x14 to 8, and `+0x103` outside 0x1E..0x46 becomes 0x3C. Store what the
#: client sent; it will normalise its own value back.
#:
#: WARNING: MEASURED: the offsets, the order, and which global each field reads.
#:
#: VERIFIED: AND SIX OF THE MEANINGS ARE NOW MEASURED TOO (2026-08-20), from the client's
#: own Options screen under its `Default` button plus a live confirmation -- three
#: bytes served, three values read back off the screen:
#:
#:     /Se=  +0x0B3   SE Volume              1=Low 2=Medium 3=High
#:     /Bgm= +0x0B4   BGM Volume             (same list; default High)
#:     /CMD= +0x100   Auto Member Display    boolean -- the `& 1` mask
#:     /CAD= +0x101   Auto Chat Display      boolean
#:     /CL=  +0x102   Chat Window Size       LINES; >0x14 folds to 8
#:     /CT=  +0x103   Chat Window Transp.    PERCENT; outside 0x1E..0x46 -> 0x3C
#:
#: The clamps above are what made this readable: a marker pass wrote 147 into
#: +0x102 and the screen said "8", which is the fold, and that is how `CL` was
#: identified. See `tmsave.DEFAULT_HEADER`, which now carries the values a MINTED
#: save gets -- a zero header is the bug this whole class of "settings ignore
#: their defaults" reports comes from.
#:
#: STILL UNMEASURED: `/Ar=` (+0x0B0, Card Placement -> "Normal"), `/Cu=` (+0x0B1),
#: `/Vi=` (+0x0B2), `/Per=` (+0x0B5), `/Ran=` (+0x0B6, Rankings Display ->
#: "Display name"). The LABELS are known from the Default screen; the index each
#: list uses for them is not. `/HNSS=` has its meaning from its call site.
SAVE_OFF_HNSS = 0x126
OPT_FIELD_OFFSETS = (
    (b"Ar",   0x0B0),      # struct +0x00
    (b"Cu",   0x0B1),      # struct +0x04
    (b"Se",   0x0B3),      # struct +0x08
    (b"Bgm",  0x0B4),      # struct +0x0C
    (b"Per",  0x0B5),      # struct +0x10
    (b"Ran",  0x0B6),      # struct +0x14
    (b"Vi",   0x0B2),      # struct +0x18  (out of offset order on purpose)
    (b"CMD",  0x100),      # struct +0x1C
    (b"CAD",  0x101),      # struct +0x20
    (b"CL",   0x102),      # struct +0x24
    (b"CT",   0x103),      # struct +0x28
    (b"HNSS", SAVE_OFF_HNSS),   # struct +0x2C
)


def _opt_enabled():
    return os.environ.get("POL_TM_OPT", "1") == "1"


def _opt_persist_enabled():
    return os.environ.get("POL_TM_OPT_PERSIST", "1") == "1"


def _opt_ans():
    """`/Ans=` for `@OptAns=`. Must be > 0.

    The poll at `0x889B0` is byte-for-byte `@InitAns`'s: look up code 133, find
    the key `@OptAns`, return `/Ans=`. Its caller `0x13F83A` does `cmp edi,0 ;
    jg <success>` -- so 0 reads as "still pending" and the scene keeps polling,
    and negative raises the error dialog. 1 is the least-committal positive.
    """
    try:
        return int(os.environ.get("POL_TM_OPT_ANS", "1"), 0)
    except ValueError:
        return 1


def _opt_fields(cmd):
    """{save offset: value} for every `@Opt=` field present in `cmd`.

    Values may be NEGATIVE on the wire -- the builder emits a `-` and negates
    (`0x18933`) rather than printing an unsigned -- so the sign is parsed and
    then truncated to the byte the client will read back.
    """
    out = {}
    for name, off in OPT_FIELD_OFFSETS:
        m = re.search(rb"/%s=(-?\d+)" % name, cmd)
        if m:
            out[off] = int(m.group(1)) & 0xFF
    return out


#: The `@Tet=` and `@Tab=` field names, in the order the client writes them.
#: Measured off the wire, not off the binary -- these arrive as plaintext with
#: their own names, which is why they could be read at all.
#:
#: WARNING: ONLY `lu`/`ll` HAVE A HOME IN THE 104-BYTE RECORD, as `block[44..48]` and
#: `block[39..43]`. `au`/`al` do NOT -- checked, and `block[38]` is the only
#: rank-shaped field there and `0x6708C` bounds it to 0..8, so it cannot hold 100
#: or 300. That is the open question to settle BEFORE authoring: either
#: the band lives somewhere else in the record, or it only ever reaches the
#: client over `@Tab=` -- and `0x87220` being a PARSER says the message route
#: exists. Storing the whole set here costs nothing and is what either answer
#: needs.
TABLE_SETTING_KEYS = (b"bm", b"du", b"st", b"cb", b"ca", b"gs", b"tl",
                      b"in", b"lu", b"ll", b"au", b"al", b"co", b"pa")


#: WARNING: `pw` IS THE PASSWORD TEXT, NOT A NUMBER -- measured 2026-08-22. The
#: tester turned Password on and typed HIHIHI; the client sent
#: `/pa=1/pw=4849484948492A2A`, which is `H I H I H I * *` in hex, i.e. eight
#: characters padded with `*`. So `pa` is the on/off flag and `pw` is the
#: secret itself.
#:
#: WARNING: AND WE HAD BEEN DESTROYING IT TWO WAYS AT ONCE. `pw` was not in
#: `TABLE_SETTING_KEYS`, so it was never parsed and `table_info_body` emitted
#: the absent-key default -- every reply said `/pw=0`, i.e. "the password is 0".
#: Adding it to the numeric list would have been WORSE, not better: the
#: `(-?\d+)` capture stops at the first hex letter, so `4849484948492A2A` would
#: have been stored as 4849484948492 and echoed as that -- a plausible-looking
#: number that is a different password. It is parsed as TEXT and echoed
#: VERBATIM, which is also the only form that cannot be wrong: the client's own
#: characters go back to it unaltered.
TABLE_SETTING_TEXT_KEYS = (b"pw",)


def parse_table_settings(cmd):
    """`{name: int|str}` for every `@Tet=`/`@Tab=` field present, plus
    `Tbl`/`Game` when the carrier is `@Save=`. Absent keys are simply absent --
    a missing field and a zero one are different facts and the caller may care.

    `TABLE_SETTING_TEXT_KEYS` come back as STRINGS, verbatim."""
    out = {}
    for key in TABLE_SETTING_KEYS + (b"Tbl", b"Game"):
        m = re.search(b"/" + key + rb"=(-?\d+)", cmd)
        if m:
            out[key.decode("ascii")] = int(m.group(1))
    for key in TABLE_SETTING_TEXT_KEYS:
        m = re.search(b"/" + key + rb"=([0-9A-Fa-f]*)", cmd)
        if m:
            out[key.decode("ascii")] = m.group(1).decode("ascii")
    return out


def _preset_persist_enabled():
    return os.environ.get("POL_TM_SAVE_PRESET", "1") == "1"


def table_preset_fields(cmd):
    """`({offset: (width, value)}, which_half)` for one `@Save=` (code 0x24).

    VERIFIED: THE PERSISTENCE ROUTE, AND IT IS THE SAME ONE `@Opt=` ALREADY USES. See
    the `TABLE_PRESET` banner in `tmsave`: `apply_options_from_save` (`0x14D9F0`)
    seeds the settings dialog's two pages out of save +0xB8..+0xF0, and `@Save=`
    is the client handing back what the player just changed. Writing it there is
    the whole of "the table settings do not persist".

    WARNING: ONE HALF AT A TIME, AND THAT IS NOT AN OPTIMISATION. `@Save=` always
    carries all fourteen fields, but the dialog only fills in the half whose PAGE
    sent it; the other half is whatever the globals held, which on a zero save is
    zeros. Persisting the whole message would therefore blank the half the player
    was not looking at -- turning a bug that loses settings between sessions into
    one that loses them between PAGES.

    The discriminator is the builder's own two arguments (`0x87410` args 5 and 6,
    emitted as `/Tbl=` and `/Game=`, and it sends `@Save=` when EITHER is
    non-zero):

        `0x512ED`  passes Game=1  -> the RULES page      -> the `@Tet=` half
        `0x53674`  passes Tbl=1   -> the RESTRICTIONS pg -> the `@Tab=` half

    WARNING: A message naming NEITHER page is not a preset write at all -- `0x87410`
    would have built the bare code 0x14 pair for it. Return nothing rather than
    guess a half.
    """
    fields = parse_table_settings(cmd)
    if int(fields.get("Game", 0)):
        half, names = "@Tet= (the rules page)", tmsave.TABLE_PRESET_TET
    elif int(fields.get("Tbl", 0)):
        half, names = "@Tab= (the restrictions page)", tmsave.TABLE_PRESET_TAB
    else:
        return {}, None
    out = {}
    for name in names:
        if name.decode("ascii") not in fields:
            continue                # absent and zero are different facts
        off, width = tmsave.TABLE_PRESET_BY_NAME[name]
        out[off] = (width, fields[name.decode("ascii")])
    return out, half


def _persist_table_preset(cmd, member_id):
    """Write an `@Save=`'s half of the preset into the member's save file."""
    if not _preset_persist_enabled() or tmsave is None:
        return
    try:
        changes, half = table_preset_fields(cmd)
    except Exception as exc:
        _say("tm:   ...preset parse failed (%r)" % (exc,))
        return
    if not changes:
        _say("tm:   ...this @Save= names neither page (/Tbl=0 and /Game=0), so "
              "there is no half to store -- captured, not written")
        return
    moved = _save_patch_fields(member_id, changes)
    if not moved:
        # NOT a lost write. The client echoes its whole half every time, so a
        # re-send of values already stored moves nothing -- exactly the
        # false-alarm `_save_patch_fields` documents for `@Opt=`.
        _say("tm:   %s preset already matches the save -- nothing to write"
              % half)
        return
    _say("tm:   %s preset STORED for member %s: %s" % (half, member_id,
         " ".join("+0x%03X %d->%d" % (o, a, b)
                  for o, (a, b) in sorted(moved.items()))))


def _note_table_settings(cmd, member_id, code=None, peer_nick=None):
    """Record one `@Tet=`/`@Tab=` set and publish the table it belongs to.

    Returns `(fields, index)`, so the selftest can assert on both.
    """
    fields = parse_table_settings(cmd)
    where = "code %#06x" % code if code is not None else "?"
    _say("tm: table settings from member %s (%s, peer %s): %s"
         % (member_id, where, (peer_nick or b"-"),
            " ".join("%s=%s" % kv for kv in sorted(fields.items()))
            or "NO RECOGNISED FIELDS"))

    chan = room = None
    try:
        import tmroom
        chan = tmroom.room_of(member_id) if member_id is not None else None
        room = tmroom.room_peer(chan) if chan else None
    except Exception as exc:
        _say("tm:   ...roster unavailable (%r)" % (exc,))
    # WARNING: `/Tbl=` IS NOT A TABLE NUMBER, SO IT IS NOT A HINT. Measured off
    # `0x87410`: the builder takes `/Tbl=` and `/Game=`
    # as its last two ARGUMENTS and emits `@Save=` when either is non-zero --
    # `0x512ED` passes Game=1 for the rules page and `0x53674` passes Tbl=1 for
    # the restrictions page. They name WHICH PAGE saved, nothing else.
    #
    # Feeding it in as `tbl_hint` made it beat the peer, which IS the table:
    #
    #     table -> UNRESOLVED  [peer 0xd0e4bcbb92 indexes to 2 but /Tbl= says 0
    #                           -- TAKING /Tbl=, the formula is what is inferred]
    #     ...table 0 is not in the shipped fixture -- nothing to update
    #
    # measured on prod 2026-08-20T19:41:47Z, from a real save at table 2. Table 0
    # does not exist, so every `@Save=` at a table above 0 resolved to nothing.
    # The peer nick is the table (and `_note_table_settings`'s own peer
    # arithmetic), and on the one carrier that has a `/Tbl=` at all it is the
    # page id -- so there is never a second opinion worth taking here.
    index, why = table_index_for_peer(peer_nick, room, None, chan=chan)
    _say("tm:   table -> %s  [%s]" % (index if index else "UNRESOLVED", why))
    if index is None or not chan:
        return fields, None

    # PUBLISH IT. This is the whole point: the row the other players in the room
    # read out of `b/g/PTL` is the one that has to move, and `note_table` both
    # writes it and queues the `TD` delta that tells a client already holding
    # the snapshot.
    #
    # WARNING: WHAT IS PUBLISHED IS THE SETTINGS BLOCK AND NOTHING ELSE. Seats and the
    # state byte are NOT touched here, because "the player sent the settings for
    # this table" and "the player is now sitting at it" are different facts and
    # only the first one is measured. `@GameEN=` is the reservation; when its
    # `/EN=` verdict is understood, that is where seating belongs.
    try:
        import tmroom
        row, _authored = _table_rows(tmroom, chan, index)
        if row is None:
            _say("tm:   ...table %d is not in the shipped fixture -- nothing to "
                 "update" % index)
            return fields, index
        # WARNING: THE SETTINGS BLOCK ONLY. `row[3]` (the state) and the occupancy id
        # inside `row[6]` belong to whoever is seated, and `_settings_block`
        # writes only the card-level letters -- so publishing settings can no
        # longer un-seat anybody. See `_table_rows`.
        _table_rules_put(chan, index, fields)
        # WARNING: AND WHETHER THE OWNER HAS FINISHED. Only code 0x14 -- the bare
        # `@Tet=`/`@Tab=` pair the client sends on **Confirm** -- means "done".
        # 0x24 (`@Save=`) is the preset write and arrives repeatedly WHILE the
        # dialog is open. See `_announce_match` for what this gates.
        if code == TABLE_SETTINGS_CONFIRM:
            if (chan, index) not in _TABLE_CONFIRMED:
                _say("tm:   %s table %d CONFIRMED by its owner -- a match may "
                     "start here now" % (chan, index))
            _TABLE_CONFIRMED.add((chan, index))
            # A FRESH GAME is being set up: clear the @GameReady= "started" set so
            # the coming start sequence's first @GameReady= (the host's) is treated
            # as the initiator. Confirm happens once, before any @GameReady=, and
            # unlike the seat path it does not fire on the PART churn that the
            # start sequence itself causes -- so the guard survives that window.
            _MATCH_STARTED.pop((chan, index), None)
            _MATCH_BEGAN.pop((chan, index), None)
            try:
                tmroom.note_table_confirmed(chan, index)   # survives the deploy
            except Exception as exc:
                _say("tm:   confirm not persisted (%r) -- a restart before the "
                     "match starts will re-brick this table" % (exc,))
        row[6] = _settings_block(fields, row[6])
        # THE CONFIRM FLIPS recruiting(1) -> ready(2) so the owner's tile
        # becomes the clickable two-pawns (reads the count, not +0x108) and
        # Start Game shows -- the flow that worked live before this was wrongly
        # reverted. Joins should be done by now; the owner confirms once
        # everyone is seated. See _occupancy_state.
        _seated_now = _seats_of(chan).get(index) or []
        _authored = tmroom.fixture_table(index)
        row[3] = _occupancy_state(_seated_now, (_authored or row)[3],
                                  confirmed=_confirmed_flag(tmroom, chan, index),
                                  inplay=True if _match_running(chan, index)
                                  else None)
        _push_table_info(tmroom, chan, index, why="the owner just set them",
                         skip=member_id)
        # ...and if the table filled up WHILE the dialog was open, the confirm
        # is the moment the match becomes startable. Without this the players
        # sit there: the seat that would have announced it has already been
        # taken and `@GameEN=` will not arrive again.
        if code == TABLE_SETTINGS_CONFIRM:
            seated = _seats_of(chan).get(index) or []
            if seated:
                # Refresh the reservation slot on confirm too -- the gate reads
                # the confirm-set greyed flag, so a re-push after confirm lets
                # the tile flip to the "start enabled" code.
                _push_reservation_list(tmroom, chan, index, seated)
                _announce_match(tmroom, chan, index, seated)
        if tmroom.note_table(chan, row):
            _say("tm:   %s table %d published, sequence %d -- every client in "
                 "the room will see it" % (chan, index, tmroom.sequence(chan)))
    except Exception as exc:
        _say("tm:   ...could not publish table %d (%r)" % (index, exc))
    return fields, index


#: Tables whose owner has pressed Confirm -- `{(chan, index)}`.
#:
#: WARNING: A MATCH WAS BEING ANNOUNCED WHILE THE OWNER WAS STILL IN THE RULES DIALOG.
#: Reported live, 2026-08-20: "Player 2 was able to join before I finished setting the
#: table rules, and immediately started loading into the game while I was still
#: setting the rules... now the host is just sitting in the lobby with the table
#: at setting up while player 2 is at the black screen waiting to start."
#: The wire shows exactly that, and the host's refusal is the client's, not a
#: choice:
#:
#:     12:43:04  m6 -> @GameEA=/EN=2                 guest joins, 2 seated
#:     12:43:08  m6 -> @GameStart=                   wake-up to the guest
#:     12:43:12  m3 <- @Save=/Tbl=0/Game=1@Tet=…     host STILL configuring
#:     12:43:18  m3 -> @GameStart=                   wake-up to the host, mid-dialog
#:     12:43:23  m6 <- @GameOK=                      guest accepts -> black screen
#:     12:43:26  m3 <- @GameNG=                      HOST DECLINES
#:     12:43:30  m3 <- (code 20) @Tet=/@Tab=         …and only NOW confirms
#:
#: `_announce_match` fired on SEAT COUNT alone. `@GameEN=` is sent when the host
#: STARTS reserving, before the dialog, so two seats can be reached while the
#: owner has chosen nothing -- and a wake-up delivered into an open dialog is
#: answered `@GameNG=`, which strands the guest on a game nobody will start.
#:
#: WARNING: 0x24 (`@Save=`) is NOT confirmation. It is the preset write and the client
#: sends it repeatedly while the dialog is open -- twice in the trace above.
#: Only code 0x14, the bare pair sent on Confirm, means the owner is done.
_TABLE_CONFIRMED = set()

#: Who is sitting where: `{room: {table index: [member_id, ...]}}`. In this
#: process only -- it is derived state, rebuilt by the next reservation, and the
#: thing that MUST be shared (the published row) already goes through `tmroom`.
_SEATED = {}

#: Rooms whose seat list we have already read back off disk this process.
_SEATED_ADOPTED = set()


def _seats_of(chan):
    """`_SEATED[chan]`, seeded ONCE from the published file if this process is new.

    WARNING: WITHOUT THIS A RESTART LEAVES A RESERVATION NOBODY OWNS. The table ROW is
    persisted by `tmroom` and this list was not, so a fresh `authsess` came up
    serving `state 1, seated 1, <occupancy id>` with an empty seat list behind
    it -- reported 2026-08-20T19:25 as a table still showing reserved with "no
    one has done anything with the table since the clients restarted", and it can
    never clear, because every path that would free it iterates THIS dict. See
    `tmroom.note_seats`.

    `setdefault`-shaped on purpose: anything this process has already learned is
    newer than the file, exactly as `tmroom._adopt` argues.
    """
    rooms = _SEATED.setdefault(chan, {})
    if chan not in _SEATED_ADOPTED:
        _SEATED_ADOPTED.add(chan)
        try:
            import tmroom
            for idx, seats in (tmroom.seats(chan) or {}).items():
                if idx not in rooms:
                    rooms[idx] = list(seats)
            if rooms:
                _say("tm: adopted %s's seat list from the roster file -- %s"
                     % (chan, ", ".join("table %d: %d seated" % (i, len(v))
                                        for i, v in sorted(rooms.items()))))
        except Exception as exc:
            _say("tm:   seat list not adopted for %s (%r) -- a reservation made "
                 "before this process started cannot be released" % (chan, exc))
    return rooms


def _save_seats(chan):
    """Write `chan`'s seat list back, so it outlives this process."""
    try:
        import tmroom
        tmroom.note_seats(chan, _SEATED.get(chan) or {})
    except Exception as exc:
        _say("tm:   seat list not persisted for %s (%r) -- it will not survive a "
             "restart" % (chan, exc))


def _seat_at_table(member_id, peer_nick, seated, cmd=b""):
    """Add/remove this member at the table `peer_nick` names; publish the count.

    Returns the new seat count, or None if we could not place the message --
    which is not a failure, it is "this did not come from a table peer".
    """
    if member_id is None or os.environ.get("POL_TM_SEATS", "1") != "1":
        return None
    try:
        import tmroom
        chan = tmroom.room_of(member_id)
        if not chan:
            # WARNING: THIS USED TO RETURN SILENTLY, and that is the whole reason two
            # live reservation faults could not be told apart. Measured
            # 2026-08-20T17:48:15Z: member 3's `@GameEN=` produced NO publish and
            # NO line, while member 9's on the same table seconds earlier
            # published fine -- and the difference is invisible without this.
            # The client meanwhile believes it HAS a reservation (its own local
            # state), so the tile keeps its authored "open" label with a
            # highlight under it, and the next player to join is offered the
            # settings dialog as though the table were free.
            _say("tm: WARNING: member %s tried to take a seat and we cannot place "
                 "them -- %s. The seat is NOT recorded, so no other client will "
                 "see this reservation and the next joiner will be offered a "
                 "fresh one."
                 % (member_id, tmroom.why_no_room(member_id)))
            return None
        index, why = table_index_for_peer(peer_nick, tmroom.room_peer(chan),
                                          chan=chan)
        if index is None:
            _say("tm:   seat NOT placed [%s]" % why)
            return None
        # THE ID THE CLIENT COMPARES AGAINST IS ITS OWN `@GameEN=/NN=`, AND IT IS
        # IN THIS VERY MESSAGE. `tmroom.guid_of` is the wrong source and says so
        # -- it refuses a 64-bit TM id outright, because the member record's
        # +0x00 is the LOBBY-band guid and the two must not be conflated.
        m = re.search(rb"/NN=([0-9A-Fa-f]{1,16})", cmd or b"")
        ident = int(m.group(1), 16) if m else 0
        # WARNING: ONE PLAYER, ONE SEAT, AND ONLY WHILE THEY ARE IN THE ROOM.
        # Reported live, 2026-08-20: "if I have a game reserved and I back out and go
        # back in it's still reserved". The client sends **no `@GameExit=` at
        # all** -- checked across a whole session's `TM0 <- code=` lines: two
        # `@GameEN=`, two `@Tet=`, one `@GameQT=`, and not one exit. So a seat
        # can never be released by a message and has to be released by STATE:
        # taking this member off every other table here, and dropping anybody
        # the room roster no longer holds. Otherwise the first reservation of a
        # session pins that table for ever, which is worse than not showing it.
        rooms = _seats_of(chan)
        # ONE PLAYER, ONE SEAT -- and since 2026-08-20 pm, one seat GLOBALLY,
        # because the client has exactly one "my table" slot: a new reservation
        # anywhere replaces whatever it held. The presence half of the old sweep
        # ("drop anybody the room roster no longer holds") is GONE -- that was
        # release-on-room-exit by the back door, the same policy the PART site
        # just retired. Departed members are released by the reconcile's
        # no-live-session rule, not by whoever happens to sit down next.
        for other in [c for c in list(_SEATED) if c != chan]:
            other_rooms = _seats_of(other)
            other_touched = []
            for oidx, oseats in other_rooms.items():
                obefore = list(oseats)
                oseats[:] = [(m, i) for (m, i) in oseats if m != member_id]
                if oseats != obefore:
                    other_touched.append((oidx, obefore))
            for oidx, obefore in other_touched:
                _republish_table(tmroom, other, oidx, other_rooms.get(oidx) or [])
                gone2 = _owner_changed(obefore, other_rooms.get(oidx) or [])
                if gone2 is not None:
                    _push_owner_change(tmroom, other, oidx, gone2,
                                       list(other_rooms.get(oidx) or []))
                _say("tm:   %s table %d: seat moved -- member %s reserved in %s "
                     "(one seat per player, the client has one my-table slot)"
                     % (other, oidx, member_id, chan))
            if other_touched:
                _save_seats(other)
        touched = set()
        vacated = {}
        for idx, seats in rooms.items():
            before = vacated[idx] = list(seats)
            seats[:] = [(mid, i) for (mid, i) in seats if mid != member_id]
            if seats != before:
                touched.add(idx)
        who = rooms.setdefault(index, [])
        if seated:
            who.append((member_id, ident))
        touched.discard(index)
        for idx in sorted(touched):
            _republish_table(tmroom, chan, idx, rooms.get(idx) or [])
            # THE SAME DEPARTURE, ONE TABLE OVER. This loop vacates seats for two
            # reasons -- this member sat down somewhere else, or somebody it
            # holds is no longer in the room roster -- and either can take an
            # owner off a table that still has people at it. `release_seats` is
            # the PART path only, so without this the owner walking to the next
            # table leaves their guest behind with a table nobody is told they
            # now own. Same helper, so the two cannot drift.
            gone = _owner_changed(vacated.get(idx) or [], rooms.get(idx) or [])
            if gone is not None:
                _push_owner_change(tmroom, chan, idx, gone,
                                   list(rooms.get(idx) or []))
        row, authored = _table_rows(tmroom, chan, index)
        if row is None:
            return len(who)
        # THE ROW's view of who is there, which a VS. COM game joins without a
        # seat (`_COM_AT`). `who` itself is NOT reassigned: it is the seat
        # list, and this function's return value is the `/EN=` seat count.
        pub = who or _com_who(chan, index)
        cap = int(row[4] or 0)
        # `0x76338` refuses a table whose seated >= 8, so the count is bounded by
        # the capacity the row itself declares rather than by a literal.
        row[5] = str(min(len(pub), cap) if cap else len(pub))
        # VERIFIED: AND THE FIELD THE SCREEN ACTUALLY BELIEVES. block[14..29] is one
        # 64-bit id, most-significant nibble first, and the tile loop's state-1
        # arm opens with `or ebp, edx; je` on it: a ZERO id short-circuits to
        # display code 2, the empty tile. Every table this server has served has
        # been all-'A' = 0, which is why a reservation changed nothing.
        row[6] = _occupancy_block(row[6], pub)
        # The AUTHORED state is what an empty table goes back to -- taking it
        # from `row` would leave the table stuck on the last occupant's state.
        # Recruiting (state 1) until the owner confirms -> ready (state 2).
        # inplay: a seat change during a RUNNING match (the start sequence's
        # own PART churn, a mid-game release) must not demote "Playing" to
        # recruiting -- see _match_running.
        row[3] = _occupancy_state(pub, (authored or row)[3],
                                  confirmed=_confirmed_flag(tmroom, chan, index),
                                  inplay=True if _match_running(chan, index)
                                  else None)
        if tmroom.note_table(chan, row):
            _say("tm:   %s table %d -> %s seated, sequence %d -- pushed to every "
                 "client in the room" % (chan, index, row[5],
                                         tmroom.sequence(chan)))
        _save_seats(chan)
        # STATE 3 ON THE SEATED MEMBER, NOW. The owner's "Start Game" gate
        # (0x5C0D0) reads each reserved player's status block; re-marking here
        # emits the PD that carries block[8]='D' without a re-fetch. See
        # tmroom.remark_seated / _mark_status.
        try:
            if member_id is not None:
                tmroom.remark_seated(member_id)
        except Exception as exc:
            _say("tm:   seated status not re-marked (%r)" % (exc,))
        # FILL THE RESERVATION SLOT so the owner's tile unlocks and Start Game
        # can evaluate -- pushed on every seat change, both directions, since a
        # departure also changes who is reserved. See _push_reservation_list.
        _push_reservation_list(tmroom, chan, index, who)
        # CAPTURE THE MATCH ROSTER on every seat change, independent of
        # _announce_match (which POL_TM_MATCH_AT=0 disables). The guest wake-up on
        # the host's @GameReady= needs to know who was seated with the host AFTER
        # the host PARTs the room, and this seat-change is the last moment both are
        # in `who`. _remember_match only resets pre-match state (accepts/hands/
        # turn), which is empty before a game starts, so calling it here is safe.
        if who:
            _remember_match(chan, index, who)
        if seated:
            _announce_match(tmroom, chan, index, who)
        return len(who)
    except Exception as exc:
        _say("tm:   seat not published (%r)" % (exc,))
        return None


def _block_fit(current, need):
    """The block, long enough to write index `need-1`, and NEVER LONGER.

    WARNING: THIS IS THE CRASH. Both writers used to do `ljust(64, 'A')`, which
    filled the whole 64-byte field and destroyed the NUL that has to follow the
    letters: `tools/tmptl.py` records `T_SET_END = 49` as "block[49] -- and NUL
    from here (0x86074)", and the record is 104 bytes with +0x28 running to the
    very end, so a 64-character block leaves the client's `mgStrCopyLim(rec+0x28,
    src, 0x40)` an UNTERMINATED string. Both players' clients died the instant the
    first such row was published (live testing, 2026-08-20).

    WARNING: It was DORMANT until the commit that fixed the `tools/` import: while
    those writers were silently no-oping in the container, nothing ever padded.
    Fixing one bug is what armed the other.
    """
    cur = current or ""
    return cur.ljust(max(len(cur), need), "A")


#: VERIFIED: MATCHMAKING, MEASURED 2026-08-20 AND SERVER-INITIATED.
#:
#: Live: player 1 reserves and sticks on "Setting Up"; player 2 sees the
#: table, can only "Reserve" or "View table members", reserves, and sticks too.
#: Both got what they should have: `@GameEN=` -> `/EN=1` then `/EN=2`, and the
#: `/EN=` consumer at `0x90114` takes the != 1 branch into a COUNTDOWN at
#: `0x90145` (`now - [0x52461E0]`, formatted). So the wait was correct and
#: nothing ever ended it.
#:
#: What ends it is the one command in the family whose direction is inverted
#: (the command table: "`@MuchMakeAns=` is an ANSWER, so matchmaking is server-initiated").
#: Read off the receiver at `0x85521`:
#:
#:     0x825A0(buf, 0xD2, 0)          waits for code **0xD2**, msgid 0
#:     0xAA920(msg, "@MuchMake")      the name, no '=' in the literal
#:     0xAB080("/TblNo=", ..)  int    -> [0x52223A8]
#:     0xAB470("/TblId=", ..)  u64    -> [0x521BB58]/[0x521BB5C]
#:
#: and the client then answers `@MuchMakeAns=/Ans=` - builder `0x8563F`,
#: `0xA97C0(buf, "@MuchMakeAns=", 0xD3, 0, ..)`, so **code 0xD3**.
#:
#: WARNING: `/TblId=` IS A CHOICE AND IT IS FLAGGED. The client stores it at a
#: standalone global, not into a record, so nothing in the parser says whether it
#: wants the table RECORD's `+0x00` id or the table PEER's guid. We send the peer
#: guid because that is the value the client itself demonstrably uses to address
#: that table (it is the nick every `@GameEN=` for it went to), and
#: `POL_TM_MATCH_TBLID=record` switches it. If matchmaking fires and the client
#: goes nowhere, this is the first thing to flip.
#: WARNING: AND IT IS **TWO** PUSHES, NOT ONE -- which is why the first attempt was
#: answered with silence. The push went out eight times (authserv.log) and
#: `@MuchMakeAns=` never came back, because there are TWO receivers and the
#: answer belongs to the other one:
#:
#:     0x85521   0x825A0(buf, 0xD2, 0)   msgid 0 -- reads /TblNo= and /TblId=,
#:                                       stores them, transitions
#:     0x85803   0x825A0(buf, 0xD2, 1)   msgid 1 -- reads **/Start=**, and
#:     0x85626   0x828E0(0xD2, 1)        is the slot the ANSWER drains before
#:                                       building `@MuchMakeAns=`
#:
#: So msgid 0 is "you are matched, here is the table" and msgid 1 is "start",
#: and only the second is ever answered. We were sending the first alone.
MATCH_PUSH_CODE = 0x000000D2    # msgid 0: `@MuchMake=/TblNo=<n>/TblId=<hex>`
MATCH_START_CODE = 0x000100D2   # msgid 1: `@MuchMake=/Start=<n>`  <- answered
MATCH_ANS_CODE = 0xD3           # `@MuchMakeAns=/Ans=<n>`, the client's reply

#: VERIFIED:VERIFIED: THE SIGNAL THE RESERVATION SCENE IS ACTUALLY WAITING ON, and the
#: shipped SHIM LOGS are what found it. The client logs its own message-store
#: lookups, and across ~26,000 of them it polls exactly two groups:
#:
#:     15927x  0x2C 0x2A 0x25 0x23 0x21      (the trade family)
#:     10521x  0x41 0x1A                     (the game / reservation family)
#:
#: **It never looks up 0x13 or 0xD2 -- not once.** So `@MuchMake` and the table-
#: info push were being delivered into slots nothing reads, which is why both
#: were met with silence no matter how they were framed. Every lookup in the log
#: is "not found" while `store_count=3` sits unread.
#:
#: Within 0x41 the polled msgids are 2 (`@GameEA`), 4 (`@GameML`), 6 (`@GameQA`)
#: -- all of which we answer -- plus **7** and **0x10**, which we have never sent.
#: And msgid 7's handler at 0x8477F parses NOTHING:
#:
#:     0x8477F  0x825A0(buf, 0x41, 7)     poll
#:     0x84788  0x828E0(0x41, 9)          drain
#:     0x84793  0x828E0(0x41, 0x10)       drain
#:     0x8479E  flag = 1
#:
#: no `0xAA920` name match, no field reads -- **ARRIVAL ALONE IS THE EVENT**, and
#: it clears the two pending slots behind it. That is the shape of a "stop
#: waiting, it is happening" notification.
#:
#: WARNING: The body is therefore free. A name is sent anyway so the wire stays
#: readable; POL_TM_MATCH41_BODY overrides it.
MATCH_41_MSGID = 0x00070041     # code 0x41, msgid 7 -- the reservation wake-up

#: VERIFIED: IT WORKED, AND HERE IS WHAT CAME BACK. Pushing (0x41, 7) on a match
#: moved the HOST straight through a sequence never seen before on this server
#: (authserv.log 2026-08-20T04:02):
#:
#:     m3 <- @CheckJoinTable=   (code 0x2D)   <- the FIRST EVER, and we answered
#:     m3 <- @GameReady=        (0x41, 0x0A)
#:     m3 <- @Req=              (code 0x01)
#:     m6 <- @GameNG=           (0x41, 0x0F)  <- the GUEST DECLINED
#:
#: and the tester saw "setting up game" on the host only. So msgid 7 is right,
#: and the two players need DIFFERENT treatment: the host proceeds, the guest is
#: being asked something and answered NO.
#:
#: `@GameNG=` is msgid 0x0F, and the `request+1` rule this family keeps makes its
#: answer **msgid 0x10** -- which is one of the two polled-but-never-filled slots,
#: and its waiter at 0x84B22 names it: **`@GameWa`** with an **`/Error=`** field.
#:
#: WARNING: `/Error=0` is the least-committal value and is a CHOICE. The field is
#: named, its slot is measured, and the client polls it constantly; what a
#: non-zero value does is not measured. POL_TM_GAMEWA_ERROR moves it,
#: POL_TM_GAMEWA=0 stops sending it.
#: VERIFIED: `@GameWa=/Error=<n>` ACKNOWLEDGES BOTH ANSWERS, ON DIFFERENT MSGIDS.
#: The complete polled map for code 0x41, read off every `0x825A0` call site:
#:
#:     msgid 2    0x835C1   `@GameEA`     answered
#:     msgid 4    0x83870   `@GameML`     answered
#:     msgid 6    0x84221   `@GameQA`     answered
#:     msgid 7    0x8477F   the wake-up   answered (bare, no fields)
#:     msgid 9    0x84B01   **`@GameWa` + `/Error=`**   <- the `@GameOK=` reply
#:     msgid 0x10 0x84B22   **`@GameWa` + `/Error=`**   <- the `@GameNG=` reply
#:     msgid 0xC  0x843AA   not sent
#:     msgid 0xE  0x84601   not sent
#:     msgid 0x13 0x85361   not sent
#:
#: `@GameOK=` is msgid 8 and `@GameNG=` is 0xF, so request+1 puts their answers
#: at 9 and 0x10 -- the same message, two slots. The msgid-9 handler reads
#: `/Error=` (ABSENT defaults it to -1, 0x84B78) and then DRAINS (0x41, 7),
#: retiring the wake-up it is acknowledging.
#:
#: WARNING: AND `@GameReady=` HAS NO REPLY SLOT. It is msgid 0x0A, request+1 would
#: be 0x0B, and 0x0B IS NOT POLLED ANYWHERE. It was being answered with 0x10 --
#: `@GameNG=`'s slot -- which was the error: a decline acknowledgement delivered
#: for a readiness announcement. It is fire-and-forget; we now say nothing.
GAMEWA_OK_MSGID = 0x00090041    # reply to `@GameOK=`  (msgid 8)
GAMEWA_NG_MSGID = 0x00100041    # reply to `@GameNG=`  (msgid 0xF)

#: VERIFIED: `@GameOwner` -- AND THIS ONE THE RESERVATION SCENE POLLS ITSELF.
#:
#: Found 2026-08-20 by enumerating every code the PC client waits on (all 26
#: `0x825A0`/`0x827F0` sites). The reservation family lives at 0x83xxx-0x84xxx
#: (0x841E0 is the reservation call, 0x83580 the `@GameEA` waiter), and inside it
#: sits `0x84D69: 0x825A0(buf, 0x1A, -1)` -- **code 0x1A, any msgid** -- reading
#:
#:     0xAA920(msg, "@GameOwner")     the name, no '=' in the literal
#:     0xAB470("/NN=", ..)     u64    a player guid
#:     0xAAD60("/CN=", ..)     str    -> [ebp+8]
#:     0xAAD60("/QN=", ..)     str    -> [ebp+0xC]
#:
#: That matters more than `@MuchMake` does: this is polled BY THE SCENE THE
#: PLAYERS ARE STUCK IN, whereas `@MuchMake`'s two receivers are reached from
#: functions we have not identified and one of them sits behind a state-machine
#: jump table.
#:
#: WARNING: AND IT IS AN OWNERSHIP **TRANSFER**, NOT AN ANNOUNCEMENT -- MEASURED
#: ON SCREEN 2026-08-20, BY BREAKING IT. Pushed on a match, the client rendered
#:
#:     "Since <blank> has cancelled their game you are now the owner"
#:
#: So the client DOES consume this message and the field map above is right --
#: but its meaning is the opposite of what it was used for. `@GameOwner` says
#: THE OWNER LEFT AND YOU INHERIT THE TABLE, which told player 2 that player 1
#: had cancelled, and left the table stuck. The blank is our empty `/CN=`, so
#: that field is the DISPLAY NAME of the player who left.
#:
#: **DEFAULT OFF.** It is kept, wired and documented because it is now a measured
#: message we will need the day a table owner actually leaves -- but pushing it
#: to announce a match is wrong, and it made things worse than sending nothing.
#: `POL_TM_OWNER_PUSH=1` re-enables it.
#:
#: WARNING: `/QN=` is still unmeasured; `/CN=` is now known to be a display name. `@Init=` uses `/CN=` for a content id
#: (hex ASCII) and `@GameEN=` sends `/CN=` EMPTY, so empty is what the client
#: itself does with it here. They are sent empty unless POL_TM_OWNER_CN /
#: POL_TM_OWNER_QN say otherwise, rather than filled with a plausible guess.
OWNER_PUSH_CODE = 0x1A          # `@GameOwner=/NN=<hex>/CN=<s>/QN=<s>`

#: VERIFIED: `@Req=` -> `@PLAYACK`, AND THIS IS WHERE BOTH PLAYERS NOW HANG.
#:
#: With the wake-up delivered correctly, both clients walk
#: `@GameEN=` -> `@CheckJoinTable=` -> `@GameReady=` -> **`@Req=`** and stop dead,
#: because we answer nothing. The trace says why it is answerable: after `@Req=`
#: they poll only the ambient group, so the reply had to be a slot they read --
#: and it is, one the earlier scan MISSED because the code is pushed as decimal
#: `2` rather than `0x2`:
#:
#:     0x86730  the `@Req=` sender: `0x828E0(msgid=-1, code=2)` FIRST -- the
#:              drain-before-you-wait pattern this family uses everywhere --
#:              then `0xA97C0(buf, "@Req=", 1, 0)`.  So `@Req=` is code 1 and
#:              its answer is code **2**, the request+1 rule again.
#:     0x86A25  `0x825A0(buf, 2, -1)` -- the waiter
#:     0x86A4D  `0xAA920(msg, "@PLAYACK")`  the name, no '=' in the literal
#:     0x86A6E  `0xAB080("/EN=", ..)`       the ONLY field it reads
#:
#: THE GATE, off 0x86A7F, and it is the same shape as `@GameEA` and `@InitAns`:
#:
#:     /EN= == 0   ->  esi = 0xFFFF7FFD and the scene FAILS
#:     /EN=  < 0   ->  passed through as that error
#:     /EN=  > 0   ->  0x86AC9, which reads the table count at [0x524290C] and
#:                     walks the table array -- i.e. it proceeds
#:
#: so `/EN=` must be > 0. 1 is the least-committal positive, as everywhere else
#: in this file; `POL_TM_PLAYACK_EN` sweeps it and `POL_TM_PLAYACK=0` restores
#: the silence for an A/B.
MSG_PLAYACK = 0x02              # reply `@PLAYACK=/EN=<n>` to `@Req=` (code 1)

#: `@GameExit=` is (0x41, 0x0D) and its answer is request+1 -- see the handler.
GAMEEXIT_ANS_MSGID = 0x000E0041

#: VERIFIED: CODE 0x13 -- THE TABLE'S SETTINGS, SERVER -> CLIENT, AND THE MIRROR OF
#: THE 0x14 THE CLIENT SENDS US. Decompiled 2026-08-20 while chasing "the other
#: player can only Reserve or View Table Members".
#:
#:     0x86ED0  0x825A0(buf, 0x13, -1)   code 0x13, any msgid
#:              then it takes the message's u64, LINEAR-SCANS the table array at
#:              0x521BB70 (stride 0x68) for a record whose +0x00 matches, and on
#:              a hit calls 0x87090 and sets that table's info record
#:              (0x521EF78 + i*0x34) `+0x30 = 2` -- "info received".
#:
#:     0x87090  parses SEVENTEEN fields, and they are the client's own 0x14 set:
#:              @Tet= /bm= /du= /st= /cb= /ca= /gs= /tl=
#:              @Tab= /in= /lu= /ll= /au= /al= /co= /pa=  **/pw=**
#:
#: `/pw=` is the one field the client never SENDS and only ever receives, which
#: is consistent with it being the table password.
#:
#: So a player looking at somebody else's table learns nothing about it until we
#: push this, and its info record stays at 0 instead of 2. We have the settings
#: already -- the owner handed them to us on 0x14 -- so this is an ECHO, not an
#: invention, which is why it is the first candidate in this whole chase that
#: needs no guessed values at all.
#:
#: WARNING: THE TABLE IS IDENTIFIED BY THE MESSAGE'S u64, NOT BY A FIELD. 0x86ED0
#: reads it straight out of the message rather than through `0xAB080`/`0xAB470`,
#: so it is the envelope's sender -- i.e. the TABLE PEER the reply goes out on.
#: That is why this rides a reply to something the client addressed to that
#: table's peer, and why the log line below records which peer it went out on.
#: VERIFIED: AND THE CLIENT ASKS FOR IT: `@Rule=`, code **0x12**. Read at 0x86DF0 --
#: it drains the 0x13 slot, sets the table's info flag to 0, builds
#: `0xA97C0(buf, "@Rule=", 0x12, ..)` carrying that table's u64, sends it, and
#: sets the flag to 1 ("asked, waiting"). Our 0x13 sets it to 2 ("received") at
#: 0x86F37 -- and it does that on ARRIVAL, unconditionally, so an unsolicited
#: push works as well as an answer. The command table already had `@Rule=`
#: at 0x12; nobody had connected it to the info record.
#:
#: WARNING: THE CLIENT HAS NEVER SENT IT -- 0 occurrences in the whole log. So this
#: arm is a completion of the pair, not the fix; the push is what does the work.
TABLE_INFO_REQ = 0x12           # `@Rule=` -- the client asking for a table's rules
#: The client's CONFIRM. `0x14` is the bare `@Tet=`/`@Tab=` pair; `0x24` is
#: `@Save=`, the preset write, which arrives repeatedly while the dialog is open
#: and must NOT be read as "the owner is done" -- see `_TABLE_CONFIRMED`.
TABLE_SETTINGS_CONFIRM = 0x14
#: `@Save=`, the PRESET write -- the player's own default table values, which
#: `0x14D9F0` reads back out of the save on the next login. See
#: `table_preset_fields` and `tmsave.TABLE_PRESET`.
TABLE_SETTINGS_PRESET = 0x24

TABLE_INFO_CODE = 0x13
TABLE_INFO_TET = (b"bm", b"du", b"st", b"cb", b"ca", b"gs", b"tl")
TABLE_INFO_TAB = (b"in", b"lu", b"ll", b"au", b"al", b"co", b"pa", b"pw")


def table_info_body(fields):
    """`@Tet=`/`@Tab=` for one table, in the order 0x87090 parses them.

    Absent keys are sent as 0 rather than omitted: the parser reads all
    seventeen by name, and a table that answers "no value" for a rule is not a
    thing the client has any way to draw.
    """
    def _one(k):
        # TEXT KEYS GO BACK VERBATIM. `pw` is the password's own characters
        # (see `TABLE_SETTING_TEXT_KEYS`); `%d` on it either raises or, worse,
        # silently ships a different password.
        v = fields.get(k.decode(), None)
        if k in TABLE_SETTING_TEXT_KEYS:
            text = "0" if v in (None, "") else str(v)
            return b"/%s=%s" % (k, text.encode("ascii", "replace"))
        try:
            return b"/%s=%d" % (k, int(v or 0))
        except (TypeError, ValueError):
            return b"/%s=0" % (k,)

    tet = b"".join(_one(k) for k in TABLE_INFO_TET)
    tab = b"".join(_one(k) for k in TABLE_INFO_TAB)
    return b"@Tet=" + tet + b"@Tab=" + tab

#: member_id -> [[after, E-body], ...] waiting to ride a later reply.
_PUSHES = {}

#: WARNING: ONE LOCK FOR EVERY `_PUSHES` TOUCH. A member holds at least TWO live
#: connections (room band + game band, plus a zombie after a relaunch), each in
#: its own thread, and both the reply path (`_pending_pushes`) and the idle
#: tick (`idle_pushes`) do read-modify-write on the same list -- `q` -> `keep`
#: -> reassign. Unlocked, one thread's reassignment overwrites the other's pop:
#: a push delivered twice at best, LOST at worst -- and this band never
#: resends, so a lost @StartData/@TurnData is a player parked mid-match until
#: they kill the client (the 09-02 review's H4).
_PUSH_LOCK = threading.RLock()

#: (room, table index) -> the last settings set its owner sent us, so the 0x13
#: push is an ECHO of the owner's own values rather than anything we made up.
#:
#: WARNING: A CACHE NOW, NOT THE STORE. `tmroom.note_table_rules` is the store, because
#: this dict is per-PROCESS and the rules have to outlive a restart -- see
#: `_table_rules_put`. Kept because the selftest and any caller without a live
#: roster still need somewhere to put them.
_TABLE_SETTINGS = {}


def _table_rules_put(chan, index, fields):
    """Record a table's rule set, in shared state as well as in memory."""
    _TABLE_SETTINGS[(chan, index)] = dict(fields)
    try:
        import tmroom
        tmroom.note_table_rules(chan, index, fields)
    except Exception as exc:
        # NOT fatal: the in-memory copy still serves this process, which is
        # exactly the behaviour we had before. Say so, because a table whose
        # rules quietly stop surviving restarts is invisible until somebody is
        # stuck in front of it.
        _say("tm:   ...could not persist table %s rules (%r) -- they will be "
              "lost on the next restart" % (index, exc))


def _table_rules_get(chan, index):
    """A table's rule set: SHARED STATE FIRST, then this process's cache.

    WARNING: THE ORDER MATTERS. The shared copy is the one that survived the restart
    and the one the other container can also see; the in-memory dict is only
    ever a same-process shortcut. Reading memory first would make a freshly
    started container answer "no rules" for a table the file knows about --
    which is the bug this pair exists to fix.
    """
    if index is None or not chan:
        return None
    try:
        import tmroom
        got = tmroom.table_rules(chan, index)
        if got:
            return got
    except Exception:
        pass
    return _TABLE_SETTINGS.get((chan, index))


def _push_key(member_id):
    """A stable queue key. WARNING: NOT `int()` -- `member_id` is whatever the caller
    has, and the selftest passes the string 'selftest'. A push queue must never
    be the thing that raises inside a reply path."""
    if member_id is None:
        return None
    try:
        return int(member_id)
    except (TypeError, ValueError):
        return str(member_id)


def _queue_push(member_id, body, why="", after=0, peer=None, source=None):
    """Hand one E-body to a later reply this member draws.

    `after` is how many replies to let pass first. 0 = the very next one.

    WARNING: `peer` AND `source` ARE TWO DIFFERENT QUESTIONS AND CONFLATING THEM COST
    A LIVE ROUND (2026-08-22, table-settings propagation). `peer` is a SOCKET
    pin: which of the member's connections may carry this, per the band law
    below. `source` is the ENVELOPE: which nick the NOTICE is framed FROM, and
    therefore -- through the id key -- which object the client thinks the
    message is ABOUT.
    They coincide for a push that answers a conversation already happening on
    that peer, which is every caller that predates this. They do NOT coincide
    for a push about an object the recipient has never addressed: pinning
    `_push_table_info` to the table's peer made the envelope right and then held
    the message forever, because a player who has never touched that table has
    no connection carrying its peer. `source` alone gives the right envelope on
    any game-carrying connection -- which is sound here because TM's receive
    store is ONE global ring (`0x5228250`, fed by `0x81A00` from the single
    `0x52af848` queue), not per-socket. `source` defaults to `peer`, so no
    existing caller changes behaviour.

    WARNING: THE STAGGER IS NOT COSMETIC. `@MuchMake` msgid 0 and msgid 1 are read
    by DIFFERENT scenes -- msgid 0 from 0x854B0 (called at 0x6D780 and 0x7B770)
    and msgid 1 from 0x85790, which is reached through a STATE-MACHINE JUMP TABLE
    at `0x985F3: jmp [eax*4 + 0x5028F54]`. So the state that polls for the start
    only exists AFTER the client has acted on the table it was handed. Sending
    both in one batch puts the start in the store before anything is looking for
    it, and a scene change is exactly where a message store gets drained.
    """
    key = _push_key(member_id)
    if key is None:
        return
    with _PUSH_LOCK:
        q = _PUSHES.setdefault(key, [])
        if any(e[1] == body for e in q):
            return                      # never announce the same match twice
        # The 4th slot is WHEN, for `idle_pushes`: an entry has to be able to
        # say how long it has been held, and keying that on id(entry) was wrong
        # twice over -- ids are reused after GC, and a missing stamp read as
        # 0.0, which made "held back one reply" expire on the very first tick.
        q.append([int(after), body, (peer or None), time.time(),
                  (source or peer or None)])
    _say("tm:   queued a push for member %s%s%s%s%s"
         % (member_id, (" -- " + why) if why else "",
            "" if not after else " (holding it back %d reply/replies)" % after,
            "" if not peer else " [only on a reply to %s]"
            % peer.decode("latin1", "replace"),
            "" if not source or source == peer else " [framed from %s]"
            % source.decode("latin1", "replace")))


def _pending_pushes(member_id, peer_nick=None):
    """The bodies due now; anything still waiting has its counter decremented.

    WARNING: A PUSH MUST RIDE A REPLY ON THE BAND ITS READER IS LISTENING TO, and that
    is not automatic -- "the member's next reply" can be on a different peer.
    Measured 2026-08-20, the same message to the same member, twice:

        11:22:40  m6 [UE7QN1N9G]  @VsGameInit  -> the client READ it
                                                  (`---->Recv=PLGAMEINIT`)
        11:30:54  m6 [UKXDBA266]  @VsGameInit  -> never seen; 130,000 further
                                                  trace lines, no Recv at all

    `UE7QN1N9G` is the TABLE peer and `UKXDBA266` is the ROOM peer. The second
    board carried the correct GAME-IDs and was still invisible, because it rode
    a reply on the room band while the match scene reads the table band. So a
    correct message on the wrong peer is exactly as unread as no message --
    the same class of fault as sending it to the wrong (code, msgid) slot.

    An entry with `peer=None` rides anything, as before.
    """
    key = _push_key(member_id)
    if key is None:
        return []
    with _PUSH_LOCK:
        q = _PUSHES.get(key)
        if not q:
            return []
        due, keep = [], []
        for entry in q:
            want = entry[2] if len(entry) > 2 else None
            if want is not None and peer_nick is not None and want != peer_nick:
                keep.append(entry)      # right member, wrong band -- wait
                continue
            if entry[0] <= 0:
                due.append(entry[1])
            else:
                entry[0] -= 1
                keep.append(entry)
        if keep:
            _PUSHES[key] = keep
        else:
            _PUSHES.pop(key, None)
        return due


#: *** A PUSH USED TO NEED THE CLIENT TO SPEAK FIRST, AND IN A MATCH IT BARELY
#: SPEAKS AT ALL. ***
#:
#: Measured 2026-08-20T16:32..16:37Z, the first two-player match ever played on
#: this server. In a match the client's ONLY unprompted traffic is `@Pong=`,
#: **every 15 seconds**:
#:
#:     16:32:36  m6 <- @PutData=/P=0/H=0/F=3     the move
#:     16:32:36  m6 -> @PutCard=...              answered instantly (a REPLY)
#:     16:32:48  m9 -> @PutCard=...              the other player: +12 s
#:     16:33:02  m6 -> @TurnData= turn 1         +26 s
#:
#: Every one of those pushes was correct and sitting in the queue the whole
#: time; `_pending_pushes` can only hand one over when a reply is being built,
#: so each held reply costs a full Pong interval. The live report was
#: "it's happening but it's happening veeeery slowly", and this is all of it --
#: not a turn timer, not the client thinking.
#:
#: The auth session loop already wakes every **2 s** (`POL_GMCHAT_POLL`; the
#: socket timeout was lowered from the 60 s ping interval for exactly this
#: reason on the GM chat track) and can `chat_sess.send()` from that tick. So a
#: push does not have to wait to be asked for. `responders._auth_session_hop`
#: calls this on each idle tick.
#:
#: WARNING: THE HOLD-BACK LAW STILL APPLIES, and it is why this is not just "flush
#: everything". `after=N` exists because a message must not ride -- or closely
#: follow -- the reply that CREATES the scene meant to read it (see
#: `POL_TM_MATCH41_GAP` and `@MuchMake` before it). A tick is a genuinely
#: separate transmission, so it counts as one of the N; but the ticks are 2 s
#: apart and a reply-gap was worth ~15 s, so each step also has to be worth at
#: least `POL_TM_IDLE_MIN_S` seconds of wall clock or the staging that was
#: measured with 15 s gaps stops being staging at all.
def idle_pushes(member_id, peers=None, now=None):
    """(peer_nick, body) pairs due for this member RIGHT NOW, unprompted.

    Returns [] for everything else, including a member with nothing queued, so
    the caller can invoke it on every tick of every session for free.

    WARNING: `peers` IS NOT OPTIONAL IN PRACTICE, AND SKIPPING IT COST A LIVE MATCH.
    A member has MORE THAN ONE auth connection -- the room band and the table
    band are separate sockets -- and the first version of this drained the queue
    on whichever of them ticked first. Measured 2026-08-20T17:10..17:11Z:
    member 9's `@Tet=` and `@VsGameInit` went out on 127.0.0.1:**50478** while
    its `@GameStart=` went out on :**50512**, and the board was never read --
    the live report was "one of the players is still at a black screen
    instead of getting to selecting cards", which is precisely the wrong-peer symptom
    one axis over.

    The wrong-peer bug established that a push must ride the right PEER; this is the same law
    on the socket. `peers` is the set of peer nicks THIS connection has actually
    carried game traffic for, so a pinned push only leaves on the connection
    that peer lives on. An unpinned push needs the connection to have carried
    SOME game traffic -- otherwise it would go out on the login socket.
    """
    _watch_publish()                 # the web watch file's heartbeat (throttled)
    if _env_int("POL_TM_IDLE_PUSH", 1) != 1:
        return []
    key = _push_key(member_id)
    q = _PUSHES.get(key)
    if not q:
        return []
    if peers is not None and not peers:
        return []                       # no game traffic here: not our band
    now = time.time() if now is None else now
    # 4 SECONDS IS MEASURED, NOT PICKED. Every match that started cleanly had
    # the wake-up land ~4 s after the second player's own `@GameEN=`
    # (+4.0 / +4.0 / +4.7 s on 2026-08-20 at 14:17, 16:30 and 16:47); the one
    # that answered `@GameNG=` had it land 2 ms after. A reply-gap used to be
    # worth a 15 s Pong interval, so "one step" has to keep meaning something
    # on that scale rather than one 2 s poll.
    min_s = float(os.environ.get("POL_TM_IDLE_MIN_S", "4") or 4)
    with _PUSH_LOCK:
        return _idle_pushes_locked(member_id, key, peers, now, min_s)


def _idle_pushes_locked(member_id, key, peers, now, min_s):
    q = _PUSHES.get(key)
    if not q:
        return []
    due, keep = [], []
    for entry in q:
        peer = entry[2] if len(entry) > 2 else None
        if peers is not None and peer is not None and peer not in peers:
            # WARNING: WAIT, BUT NOT FOREVER. A pinned push only leaves on the
            # connection its peer lives on, so one pinned to a peer this member
            # never carries would sit in the queue for the life of the process.
            # That was harmless while almost nothing pinned; `_push_table_info`
            # pins every table-settings push, and a member who is in the room
            # but has never touched that table is exactly the case. Drop it
            # loudly rather than leak it. POL_TM_PUSH_TTL_S=0 waits forever.
            ttl = float(os.environ.get("POL_TM_PUSH_TTL_S", "120") or 0)
            held = now - (entry[3] if len(entry) > 3 else now)
            if ttl and held > ttl:
                _say("tm:   dropping a push for member %s held %.0fs for peer "
                     "%s, which this connection never carries: %s"
                     % (member_id, held,
                        peer.decode("latin1", "replace"), describe_line(entry[1])))
                continue
            keep.append(entry)          # right member, wrong SOCKET -- wait
            continue
        if entry[0] <= 0:
            # FRAME from `source`, which is `peer` unless the caller split them.
            due.append((entry[4] if len(entry) > 4 else peer, entry[1]))
            continue
        # Not due yet. One tick counts as one step, but only once the step has
        # actually been worth some wall clock -- see the banner.
        last = entry[3] if len(entry) > 3 else 0.0
        if now - last >= min_s:
            entry[0] -= 1
            if len(entry) > 3:
                entry[3] = now
        keep.append(entry)
    if keep:
        _PUSHES[key] = keep
    else:
        _PUSHES.pop(key, None)
    for peer, body in due:
        _say("tm:   ...pushing to member %s unprompted%s: %s"
             % (member_id,
                "" if not peer else " [on %s]" % peer.decode("latin1", "replace"),
                describe_line(body)))
    return due


def requeue_pushes(member_id, items, why="delivery failed"):
    """Put undelivered (peer, body) pairs BACK at the head of the queue.

    WARNING: A POP IS NOT A DELIVERY. `idle_pushes` hands entries to the caller and
    forgets them, and the caller's send can fail (peer gone mid-write, an
    exception in the encode) -- the old arm logged "push failed" and dropped
    the bodies on the floor. This band never resends, so every dropped
    @StartData/@TurnData was a player parked mid-match forever (09-02 review
    H3). The consumer now hands back what it could not deliver; entries go to
    the FRONT with after=0 so the next tick or reply retries them, and
    `queue_push`'s own dedup keeps a retry from doubling anything that did
    land. POL_TM_PUSH_REQUEUE=0 restores drop-on-failure.
    """
    if _env_int("POL_TM_PUSH_REQUEUE", 1) != 1:
        return
    key = _push_key(member_id)
    if key is None or not items:
        return
    now = time.time()
    with _PUSH_LOCK:
        q = _PUSHES.setdefault(key, [])
        have = {e[1] for e in q}
        back = [[0, body, (peer or None), now, (peer or None)]
                for peer, body in reversed(list(items))
                if body not in have]
        for entry in back:
            q.insert(0, entry)
    if back:
        _say("tm:   requeued %d undelivered push(es) for member %s (%s)"
             % (len(back), member_id, why))


def _push_reservation_list(tmroom, chan, index, who):
    """Push the reserved-player list (msgid 0xC @GameML) to each seated member.

    WARNING: THE OWNER'S OWN TILE LOCKS ON AN EMPTY RESERVATION SLOT, AND SO DOES
    START GAME -- ONE mechanism (disasm 2026-08-21). The owner's state-1 tile
    arm at `0x672E4` reads the reservation count `[0x51def2c]` (mirror of
    `screenobj+0x108`): 0 -> **display code 2 = LOCKED**, 1 -> 0x11, >=2 ->
    (n<<4)|5 (clickable, with Start Game enabled when `0x51defa0`==0). The gate
    `0x5C0D0` reads the same `+0x108`. That slot fills ONLY from a msgid-0xC
    `@GameML` (parser `0x83B80`); the members pane (msgid 4) fills a different
    slot. The client arms the reservation request (`+0x41c`) when the owner
    opens the table menu, but that request is not the plain `@GameM=` we answer,
    so `+0x108` stayed 0 -- and a 2-seated owner's tile then renders code 2, so
    the owner cannot re-open the menu to retrigger it. Measured live: dump with
    `+0x108`=0, `+0x41c` ARMED, tile code 2, both clients frozen.

    `+0x41c` is armed continuously while at the table, so a msgid-0xC reply
    sitting in the queue is consumed on the client's next copy-loop frame and
    `+0x108` fills WITHOUT any click -- breaking the deadlock. Pushed on every
    seat change (the same unpinned `_queue_push` path `_push_table_info` uses,
    proven to reach seated/idle members). `POL_TM_RESV_PUSH=0` disables.
    """
    if os.environ.get("POL_TM_RESV_PUSH", "1") != "1":
        return
    if not who:
        return
    # The reservation parser has NO /ID gate (0x83B80), so one body serves every
    # recipient; the /ID is cosmetic. Build it for the table's seated list.
    owner = next((m for m, _i in who), None)
    if owner is None:
        return
    try:
        pid = tmroom.pol_id_of(owner)
        idhex = (str(pid) if pid else ("%X" % owner)).replace(
            "0x", "").replace("0X", "").encode("ascii")
        body = _gameml_body(b"@GameM=/ID=" + idhex, member_id=owner)
    except Exception as exc:
        _say("tm:   reservation list not built (%r)" % (exc,))
        return
    if not body:
        return
    line = encode_code(_resv_gameml_code()) + body
    _say("tm:   reservation list -> %d seated member(s) at table %d (msgid %s, "
         "fills +0x108: unlocks the owner tile AND Start Game)"
         % (len(who), index,
            "0xC" if _resv_gameml_code() == MSG_GAMEML_RESV else "4"))
    for mid, _ident in who:
        _queue_push(mid, line, "reservation list for table %d" % index)


def _reservation_reply(member_id):
    """The msgid-0xC `@GameML` reservation line for `member_id`'s seated table,
    or None -- what feeds the client's reservation list (+0x108/+0x110), the
    "table members" sidebar, and the Start-Game gate. RE 2026-08-21: the client
    polls (code 0x41, msgid 0xC) EVERY FRAME while +0x41c is armed, and its
    message store holds only a few entries, so a one-shot reply is evicted
    before the reservation branch (0xA940C -> 0x83B80) reads it. Re-emitting it
    keeps a fresh copy present. Only for SEATED members -- a looker's slot must
    stay empty or their Reserve greys."""
    if os.environ.get("POL_TM_RESV_PUSH", "1") != "1":
        return None
    # WARNING: NEVER INTO A RUNNING MATCH. The re-feed exists for the TABLE screen
    # (+0x41c armed); the match scene never reads (0x41, 4), and the client's
    # receive store is ONE GLOBAL RING -- so during a match every re-feed is a
    # ring slot that never frees. Measured live 2026-08-22T16:16Z (FRAMEWORK
    # probe, mid-COM-game): store_count parked at 24, the ring full of unread
    # re-feeds, and the COM's turn-7 @PutCard VANISHED ON ARRIVAL -- the
    # tester's "com seems to have stopped", ~3 minutes into a slow game at
    # one re-feed per @Pong. Fast games finished before the ring filled,
    # which is why four earlier full games never showed it.
    # The gates are the two precise "a match is RUNNING" signals -- NOT
    # `_match_of` (its roster snapshot includes pre-match seating, and
    # suppressing the re-feed at the table would resurrect the +0x108 bug):
    #   * a VS. COM match key exists (cleared on @GameExit=), or
    #   * this member is in `_MATCH_STARTED` for their table (set on
    #     @GameReady=, reset on CONFIRM, and now also cleared on @GameExit=).
    _k = _push_key(member_id)
    if (None, _k) in _MATCH_TURN:
        return None
    _mc, _mi, _ms = _match_of(member_id)
    if _mi is not None and _k in (_MATCH_STARTED.get((_mc, _mi)) or set()):
        return None
    if not _is_seated_here(member_id):
        return None
    try:
        import tmroom
        pid = tmroom.pol_id_of(member_id)
        idhex = (str(pid) if pid else ("%X" % member_id)).replace(
            "0x", "").replace("0X", "").encode("ascii")
        body = _gameml_body(b"@GameM=/ID=" + idhex, member_id=member_id)
    except Exception:
        return None
    if not body:
        return None
    # OBSERVABLE (2026-08-21): the @Pong re-feed is the ONLY thing keeping a
    # msgid-0xC resident between seat changes, and it was SILENT -- so live, "the
    # reply stopped" and "the reply is firing but not landing" were
    # indistinguishable (the shim [resvdiag] saw no 0x41/0xC in the store while
    # armed). Log each fire, rate-limited per member (@Pong is ~15s, so a few
    # lines/min). `POL_TM_RESV_LOG=0` quiets it.
    if os.environ.get("POL_TM_RESV_LOG", "1") == "1":
        now = time.time()
        key = int(member_id)
        if now - _RESV_REPLY_LOG.get(key, 0.0) >= 8.0:
            _RESV_REPLY_LOG[key] = now
            _say("tm: @Pong reservation re-feed -> member %s (msgid %s @GameML, "
                 "%d-byte body) -- meant to fill +0x108 (kick poll cannot eat "
                 "msgid 4; +0x414 not armed on a @Pong frame)"
                 % (member_id,
                    "0xC" if _resv_gameml_code() == MSG_GAMEML_RESV else "4",
                    len(body)))
    return encode_code(_resv_gameml_code()) + body


_RESV_REPLY_LOG = {}


def table_peer_nick(tmroom, chan, index):
    """The NICK the client addresses table `index` by, or None.

    VERIFIED: THE SERVER CAN COMPUTE THIS NOW, and could not before 2026-08-22. A
    table's peer is not something the client tells us and not something we
    allocate -- it is our own published table id run through the client's id
    key: `wire = (id ^ K) & 0xFF_FFFF_FFFF`, rendered as 8 base-36 digits and
    scrambled into a login nick. See `tmroom.CLIENT_KEY_DEFAULT`.

    VERIFIED AGAINST THE LIVE WIRE: table 1 of `#TM0R001` synthesises to
    `UGRAWG3GT`, which is exactly the nick the client addressed its `@Tet=`
    confirm to at 05:15:20Z. Same value, arrived at from the other end.
    """
    try:
        import polnick
        room_no = tmroom._room_no(chan)
        if not room_no:
            return None
        guid = tmroom.wire_guid(tmroom.canonical_table_id(int(index) - 1, room_no))
        nick = polnick.nick_for_polid(polnick._digits_out(guid))
        return nick.encode("ascii") if nick else None
    except Exception:
        return None


def _push_table_info(tmroom, chan, index, why="", skip=None):
    """Tell the room what table `index` is configured as (code 0x13).

    Sent to members of the room, not just the table's occupants: the point is
    that somebody LOOKING at the table can see what it is, and 0x86ED0 keys the
    info record off the message rather than off who asked.

    VERIFIED: BACK ON BY DEFAULT 2026-08-22, AND THE REASON IT WAS OFF IS RETRACTED.
    The note this replaces said the pushes "CLOGGED the client's small receive
    store and STARVED the reservation list", measured as a seated owner holding
    5 unconsumed `@Tet=` pushes while `+0x108` stayed 0. The starvation was
    real; the CAUSE was not. `+0x108` stayed 0 because the reservation
    `@GameML` rode msgid **0xC**, which the `@GameKick` poll (`0x84360` at
    `0xA92D5`) POPs before the reservation arm runs whenever the selected-table
    gate is set -- always, at your own table. Fixed in `2246e005` by moving it
    to msgid 4. So this push was disabled for something it did not do.
    WARNING: THE ONE TRUE OBSERVATION IN THAT NOTE IS KEPT: the pushes that piled up
    were the ones ECHOED BACK TO THE OWNER WHO SET THEM, with the settings scene
    already closed so nothing ever consumed them. `skip` drops that echo, which
    removes the queue pressure without removing the feature. The owner does not
    need to be told what they just typed.
    WARNING: AND WITHOUT THIS THE SECOND PLAYER NEVER SEES A CHANGE. `0x873A0` gates
    on `record+0x30 == 2` and returns 0 otherwise; once a viewer's record is at
    2 it holds the values it was given, so a table reconfigured after they
    looked stays stale on their screen until something pushes the new set. Only
    `lu`/`ll` have a home in the `b/g/PTL` row (`_settings_block`), so the
    `TD` delta cannot carry the other twelve fields.
    `POL_TM_TABLE_INFO_PUSH=0` disables it again -- its OWN knob, deliberately:
    it used to share `POL_TM_TABLE_INFO` with the `@Rule=` answer arm while the
    two carried DIFFERENT compiled defaults (1 there, 0 here). Setting that one
    variable to reach this behaviour would silently have switched off the
    `@Rule=` reply that "Change Table Settings" depends on -- the
    two-defaults-per-setting trap, one subsystem over.
    """
    if not _env_int("POL_TM_TABLE_INFO_PUSH", 1):
        return
    fields = _table_rules_get(chan, index)
    if not fields:
        return                          # nothing measured to echo yet
    body = encode_code(TABLE_INFO_CODE) + table_info_body(fields)
    try:
        who = [mid for mid, _v in tmroom.members(chan)]
    except Exception:
        return
    sent = [mid for mid in who if skip is None or str(mid) != str(skip)]
    # WARNING: THE PUSH MUST RIDE THE TABLE'S OWN PEER -- the wrong-peer law, and skipping it
    # is why the first cut of this changed nothing on the other player's screen
    # (measured live 2026-08-22, 05:15Z). An unpinned push is framed FROM THE
    # RECIPIENT'S OWN NICK (`_game_notice_line(body, _peer or nick, ...)`), so
    # the client reconstructs `own_guid ^ K` -- its own id -- and `0x86ED0`
    # compares that against every table record, misses, and drops the message.
    # Byte-identical payload, delivered, consumed, and invisible. Exactly the
    # id-space failure the `@Rule=` reply had, one path over: there the peer
    # came for free because we answered the message it arrived on, and here
    # there is no arriving message to answer.
    peer = table_peer_nick(tmroom, chan, index)
    if peer is None:
        _say("tm:   table %d info NOT pushed -- cannot synthesise the table's "
             "peer nick for %s, and an unpinned 0x13 is framed from the "
             "recipient's own nick, which the client discards" % (index, chan))
        return
    _say("tm:   table %d info -> %d of %d member(s) as code 0x13 on %s%s%s"
         % (index, len(sent), len(who), peer.decode("latin1"),
            (" (%s)" % why) if why else "",
            "" if skip is None else
            " [not echoed to member %s, who set them]" % skip))
    for mid in sent:
        # SOURCE, NOT A SOCKET PIN. The envelope has to be the table's peer or
        # the client resolves the message to the wrong object and drops it; the
        # SOCKET must stay unpinned, because the whole point is to reach someone
        # who has NOT been addressing this table and therefore has no connection
        # carrying its peer. Measured live: pinning both queued the push and
        # never sent it ("[only on a reply to UGRAWGL2Q]", no delivery line).
        _queue_push(mid, body, "table %d info" % index, source=peer)


def _announce_match(tmroom, chan, index, who):
    """Push `@MuchMake` to everyone seated once a table has enough of them.

    WARNING: "TETRA MASTER IS A TWO-PLAYER GAME" WAS WRONG, AND IT IS WHY THREE CAN
    NEVER SIT DOWN. That sentence stood here as the justification for the
    default, and live testing corrected it 2026-08-20: **the game also supports
    three players.** The number was never measured -- it was inferred from "a
    card game has two sides" and then reasoned from.

    The consequence is not merely a wrong default. Announcing the moment the
    SECOND player sits is what FORECLOSES the third: the match fires, both
    players leave the room for the board (`_MATCH_ROSTER`'s banner: entering a
    match means leaving the room), and the seat a third person was walking
    toward is gone. So a headcount trigger cannot express a three-player game at
    all, whatever number is in it.

    WARNING: WHAT THE RIGHT TRIGGER IS, IS NOT KNOWN, and this is where to start
    looking rather than where to guess. It is almost certainly an OWNER ACTION --
    the person who reserved the table saying "go" once the people they want are
    seated -- because that is the only thing that can distinguish "two is all I
    wanted" from "still waiting for a third". Candidates already decoded but not
    connected to this: `@GameQA=/QT=` (seen live in the class-A ring), `@GameOK=`
    / `@GameNG=` (the ACCEPT half, which runs after the announce, not before),
    and the settings pair `@Tet=`/`@Tab=`. None of the seven `/R=` rule values
    carries a seat count (`VSGAME_RULE_KEYS`: bm du st cb ca gs tl -- `tl` is a
    time limit), so the count is NOT part of the table's declared rules and has
    to arrive some other way.

    Until that is measured, this stays a count WE choose and the dial is
    `POL_TM_MATCH_AT`: 2 to keep today's behaviour, 3 to hold the table open for
    a third (a pair will then never start, so it is a test setting, not a fix),
    0 to disable the push entirely -- the control for "did this change
    anything". The client's own floor is the only hard number here:
    `0x1024D5` refuses `/N=` <= 1 with `StartAloneERROR`.
    """
    need = _env_int("POL_TM_MATCH_AT", 2)
    if need <= 1:
        # 0 disables; 1 would be a table of one, which the client refuses at
        # 0x1024D5 -- raised here rather than paid for as a StartAloneERROR.
        return
    if len(who) < need:
        return
    # THE OWNER MUST HAVE FINISHED. See `_TABLE_CONFIRMED`: announcing into an
    # open rules dialog gets `@GameNG=` from the host and strands the guest.
    # THE CONFIRM SURVIVES A DEPLOY VIA tmroom.table_confirmed -- the shared
    # flag, written where the process-local set is written. Measured 23:52:31:
    # the hold fired for a confirm sent at 23:48:13, across one restart, and
    # the "Setting up" freeze locks every control on every seated client, so a
    # wiped flag bricks the table with the players inside it.
    # WARNING: NOT inferred from stored rules -- @Save= (0x24) also stores rules, and
    # the selftest's "no match before the owner confirms" caught that shortcut
    # before it shipped. The flag is its own fact.
    if (_env_int("POL_TM_MATCH_NEED_CONFIRM", 1)
            and (chan, index) not in _TABLE_CONFIRMED
            and not tmroom.table_confirmed(chan, index)):
        _say("tm:   %s table %d has %d seated but its owner has NOT confirmed "
             "the rules yet -- holding the match. A wake-up delivered into an "
             "open settings dialog is answered @GameNG=." % (chan, index, len(who)))
        return
    which = (os.environ.get("POL_TM_MATCH_TBLID", "peer") or "peer").lower()
    room_peer = tmroom.room_peer(chan)
    if which == "record":
        row = dict(tmroom.tables(chan)).get("#TM0T%03d" % index)             or tmroom.fixture_table(index) or []
        tblid = int((row[2] if len(row) > 2 else "0") or "0", 16)
    else:
        # The table peer's guid -- room peer with the table class nibble and the
        # table's own index, i.e. exactly what `table_index_for_peer` inverts.
        tblid = ((TABLE_PEER_CLASS << 32)
                 | (((room_peer or 0) & 0xFFFFFFFF) - 1 + index)) if room_peer else 0
    start = _env_int("POL_TM_MATCH_START", 1)
    bodies = []
    # THE ONE SLOT THE SCENE POLLS. First, because it is the only push here with
    # live evidence that the client is listening for it at all.
    if _env_int("POL_TM_MATCH41", 1):
        # WARNING: NEVER IN THE SAME BATCH AS THE REPLY THAT TRIGGERED IT. Measured
        # 2026-08-20 with the trace on both clients, and the two differ by exactly
        # this:
        #
        #   m6 (guest)  @GameEA=/EN=2 + 0x13 + @GameStart= in ONE reply
        #               -> store_count=3, polls (0x41,7), **not found**, and 8 s
        #                  later tears down with @GameNG=
        #   m3 (host)   @GameStart= alone on a LATER exchange
        #               -> store_count=2, polls (0x41,7), **FOUND**
        #
        # The second player's wake-up arrives before the scene that polls for it
        # exists -- the reply it rides is the one that CREATES that scene. So it
        # waits a reply, which on this client is the ~2 s `@Pong=` heartbeat.
        # POL_TM_MATCH41_GAP=0 restores the same-batch behaviour for an A/B.
        bodies.append(encode_code(MATCH_41_MSGID)
                      + os.environ.get("POL_TM_MATCH41_BODY",
                                       "@GameStart=").encode("latin1", "replace"))
    # WARNING: DEFAULT OFF: the shim logs prove the client never polls 0xD2, so these
    # only fill a store it never drains. Kept, documented and switchable because
    # the receivers are real -- some scene we have not identified does read them.
    if _env_int("POL_TM_MUCHMAKE_PUSH", 0):
        bodies.append(encode_code(MATCH_PUSH_CODE)
                      + b"@MuchMake=/TblNo=%d/TblId=%016X" % (index, tblid))
    if _env_int("POL_TM_MUCHMAKE_PUSH", 0) and _env_int("POL_TM_MATCH_START_PUSH", 1):
        # WARNING: THE ORDER MATTERS AND IT IS THE READ, NOT A GUESS: msgid 0 is what
        # gives the client the table, msgid 1 is what it answers. Sending the
        # start first would announce a match for a table it has not been told
        # about yet.
        bodies.append(encode_code(MATCH_START_CODE)
                      + b"@MuchMake=/Start=%d" % start)
    # THE ROSTER, CAPTURED WHILE IT STILL EXISTS. See `_MATCH_ROSTER`: the
    # players PART the room on their way into the match, so this is the last
    # moment anything knows who is playing whom.
    _remember_match(chan, index, who)
    _say("tm: MATCH READY at %s table %d (%d seated) -- pushing %s to %s"
         % (chan, index, len(who),
            ", ".join(b[:8].decode("ascii", "replace") for b in bodies) or "NOTHING",
            ", ".join(str(m) for m, _ in who)))
    if _env_int("POL_TM_OWNER_PUSH", 0):
        # THE OWNER IS THE FIRST PLAYER SEATED -- the one who reserved the table
        # and whose id is already in the row's occupancy block.
        owner = next((i for _m, i in who if i), 0)
        bodies.insert(0, encode_code(OWNER_PUSH_CODE)
                      + b"@GameOwner=/NN=%016X/CN=%s/QN=%s"
                      % (owner,
                         os.environ.get("POL_TM_OWNER_CN", "").encode("latin1", "replace"),
                         os.environ.get("POL_TM_OWNER_QN", "").encode("latin1", "replace")))
    # WARNING: ONE PUSH PER REPLY, IN ORDER. Not "the first now, the rest later" -- an
    # earlier version did that and put both `@MuchMake` halves back in the same
    # batch the moment a third message joined the list, which is the exact fault
    # the stagger exists to prevent. Each body waits for its own reply.
    gap = _env_int("POL_TM_MATCH_START_GAP", 1)
    lead = _env_int("POL_TM_MATCH41_GAP", 1)      # see the banner above
    for mid, _ident in who:
        for n, b in enumerate(bodies):
            _queue_push(mid, b, "match at table %d" % index,
                        after=lead + n * gap)


#: VERIFIED: THE IN-MATCH COMMAND TABLE -- CODE 0x43, AND THE `msgid` IS A **COMMAND
#: ID**, NOT A SEQUENCE. Decompiled 2026-08-20 from `TM.dll.unpacked`.
#:
#: An earlier reading took the live trace line
#:
#:     [tm] store lookup: code=0x43 msgid=1 store_count=9 -> not found
#:
#: as "the board is a numbered stream -- expect to send 1, 2, 3...". It is not.
#: `0x101A86` is three instructions inside **`0x101A10`**, the client's shared
#: *wait for the answer to command N* helper -- the very one the card shop has
#: been using all along (the banner above `MSG_SHOPINIT` already names it):
#:
#:     0x101A10(this=0x52463E0, cmd)          __thiscall, one arg, `ret 4`
#:       if [rva 0x2B624C] == 0:  return 1    no session -> everything allowed
#:       edi = cmd & 0xFF                     <- the ARGUMENT, not a counter
#:       0x825A0(buf, 0x43, edi)              <- the line the trace prints
#:       found:   idx = [0x106D6C + cmd-1];  jmp [0x106D14 + idx*4]
#:       else:    0x825A0(buf, 0xB2, edi) -> [0x106DBC + cmd-7] / [0x106D98]
#:       the unhandled-default arm 0x106CCD returns **0**
#:
#: and every one of its 90 call sites does `cmp eax, 1 / jne <refuse>`. So the
#: polled msgid is the command being waited on, `msgid=1` means command 1, and
#: the answer belongs in `(0x43, 1)` -- not at a position in a stream.
#:
#: WARNING: AND THE SAME READING NAMES THE WRONG PAYLOAD FOR THAT SLOT. `@Test` +
#: `/Count=` and `@WatchInfo` + `/N=` + `/CN=` are simply the two parses that sit
#: next after the dispatch in ADDRESS order, so a linear read attributes them to
#: msgid 1. They are arms **0x101ACD (command 43)** and **0x101B15 (command
#: 37)**; the `jmp [edx*4 + ...]` jumps over both. Command 1 is `@VsGameInit`.
#:
#: THE WHOLE TABLE, arm by arm, with the client's own `---->Recv=` debug label --
#: which matters more than it looks: `0x93260` IS the sink `[tmlog] enable`
#: hooks (pol-shim/src/tmlog.cpp, `TMLOG_RVA`), so every label below appears in
#: `logs/shim-<HOST>.log` the moment the client READS our message. Fields are in
#: parse order; `str` is 0xAAD60, everything else 0xAB080 (int).
#:
#:   cmd  name             Recv=          fields
#:     1  @VsGameInit      PLGAMEINIT     /N= /ID=* /CN=* /R=x7 then @No<i>=
#:     1  @EventGameInit   EVGAMEINIT     (same arm, second name, 0x102964)
#:     2  @ComGameInit     COMGAMEINIT    /RA= /M= /CP= /S= /R=x7
#:     2  @ComList         COMLIST        /L= then /D= per row (same arm)
#:     4  @ComList         COMLIST        /L= /D=            (arm 0x102F74)
#:     5  @Quit            QUIT           /No= /B=
#:     7  @CardSelect      CARDSELECT     /Ans= /Ok=   (also @EventCard)
#:     8  @StartData       STARTDATA      /S= /B= /E= /F= /L=
#:     9  @TurnData        TURNINFO       /A= /S= /R=
#:    10  @PutCard         PUTCARD        /A= /H= /F= /D=
#:    12  @BattleData      BATTLESET      /A= /D= /R= /CBC=
#:    13  @ResultData      RESULTINFO     /R= /P= /D= /T= /RN=(str)
#:                                        (also @EventResult)
#:    14  @Ready           READY          /Go=
#:    15  @GetSelect       GETSELECT      /E= /H=
#:    17  @Continue        CONTINUE       /Ans= /B=
#:    26  @TCList          TRADELIST      /H= /D=      (also @TradeCard)
#:    35  @GetAway         GETAWAY        /P=
#:    36  @Change          PLCHANGE       /P= /C=
#:    37  @WatchInfo       WATCHINFO      /N= /CN=(str) /R= /Q= /C= /G= /H= /T=
#:                                        /S= /A= /F= /CH= /L=
#:    38  @WFCard          WATCHCARD      /T= /D=      (also @WatchCard)
#:    39  @Dead            DEAD           /N=
#:    40  @DataError       ERROR          /No=
#:    43  @Test            TEST:Count%d   /Count=
#:
#: Commands 3, 6, 11, 16, 18..25, 27..34, 41 and 42 have NO 0x43 arm; they fall
#: through to the 0xB2 table -- the card shop / auction set this file already
#: answers (7 @Select `/A=`, 19 @Init SHOPINIT, 20 @Card SHOPBUY, 23 @Event
#: SHOPQUIT, 32 @Data AUCMONEY, 33 @Card AUCCARDS, 39 @Dead, 40 @DataError) --
#: and on a second miss to a THIRD table at `0x1064BD` keyed `cmd - 0x18`, bound
#: 0x10, arms at `0x106DF0`: **code 0xA2 is CARD TRADE** (24 @Init TRADEINIT,
#: 25 @Quit TRADEQUIT, 26 @Card TRADELIST, 27 @Data TRADESELECT, 28 @Data
#: TRADEDECIDE, 30 @Quit TRADECLOSE, 39 @Dead, 40 @DataError). All three are
#: reproducible from the client's command table.
#:
#: VERIFIED: AND IT RETIRES THE "VS. COM IS A UI GATE" THREAD, TWO WAYS.
#: That reading asks for the site that pushes `tm-xlate` **string id 40**. There
#: is no such id: column 2 of the PS2 `tm-xlate` table is `jp_bytes`, the JAPANESE
#: BYTE LENGTH, and 40 is simply twice the 20 characters of that sentence (44 is
#: the two-character-longer "...now." variant). The real Lobby.BIN id is **428**,
#: which pol-shim's own `probe_msg` banner already records. Scanning all 312
#: callers of the two message-display functions (0x195260 / 0x1952A0) and reading
#: the id out of the third argument slot -- the technique that banner recommends
#: over hooking -- finds **exactly one** site pushing 428: `0xFCE55`, whose scene
#: waits on commands 0x13 / 0x17 / 0x27, i.e. SHOPINIT / SHOPQUIT / DEAD. That is
#: the CARD SHOP, not vs-COM. And the refusal is not a UI predicate at all: it is
#: the generic arm taken when `0x101A10` returns != 1, i.e. "the server never
#: answered that command". vs-COM's own waiter is `0xBADDA`, which asks for
#: command **2** and on success sets the match scene's state `[esi+0x16F] = 5`.
#: So that reading's instinct was right for the wrong reason: vs-COM IS
#: server-initiated, and the way to un-grey it is to answer `(0x43, 2)` with
#: `@ComGameInit` -- which needs no second player, no table and no room.
IN_MATCH_CODE = 0x43            # the in-match / vs-COM command family

#: WARNING: EVERYTHING FROM HERE DOWN IS READ OFF THE PARSER AND HAS NEVER BEEN SEEN
#: TO LAND. The client tells us itself, in one line, whether it did: turn
#: `[tmlog] enable` on and look for `---->Recv=PLGAMEINIT` (it read the message)
#: followed by either `ID=<n>` (it found itself, n = its seat) or `ID=NOT_FOUND`
#: / `StartAloneERROR` (it read the message and rejected it). Those three strings
#: are pushed at 0x102530, 0x102574 and 0x102943 and all three go to the traced
#: sink. Do not mark this fixed on anything less.
VSGAME_MSGID = 0x00010043       # code 0x43, command 1  -- `@VsGameInit`
COMGAME_MSGID = 0x00020043      # code 0x43, command 2  -- `@ComGameInit`
#: `@Quit` -- code 0x43, command 5. The console's LEAVE ACK, and it is a
#: PUSH: `TMaster.pex` only ever parses `@Quit` (receive arm 0x003eedd8),
#: never builds one, so nothing arrives for `_handle_line` to answer.
COMQUIT_MSGID = 0x00050043      # code 0x43, command 5  -- `@Quit` (leave ack)
STARTDATA_CMD = 8               # code 0x43, command 8  -- `@StartData` (the deal)
TURNDATA_CMD = 9                # code 0x43, command 9  -- `@TurnData` (whose turn)

#: The seven `/R=` occurrences, in parse order.
#:
#: WARNING: INFERRED, NOT MEASURED, and the one mapping here a live run should check
#: first. `@Tet=` carries exactly seven rule fields (`TABLE_INFO_TET`, the order
#: 0x87090 parses the client's own settings in) and both GameInit messages carry
#: exactly seven `/R=`; that one-to-one count is the whole of the argument. Their
#: destinations differ in WIDTH, which is the corroboration: occurrence 0 lands
#: as a DWORD at [obj+0xDC] and 1..5 as BYTES at [obj+0xE0..0xE4], so the first
#: rule is the wide one -- `bm` in `@Tet=`'s order. Occurrence 6 goes to
#: [obj+0xE9] in `@VsGameInit` and [obj+0xD9] in `@ComGameInit`.
#: `POL_TM_VSGAME_RULES` overrides the lot with a comma-separated list.
VSGAME_RULE_KEYS = TABLE_INFO_TET

#: VERIFIED: THE CLIENT'S OWN DEFAULTS, TAKEN OFF THE WIRE -- not invented, and not the
#: zeros we used to send for a table nobody had configured.
#:
#: The server owner, 2026-08-20: "at some point we'll have to fix the default rules for
#: these rooms so they match the game defaults". They already told us what those
#: are. In one session the SAME client sent both halves:
#:
#:   code 0x24 `@Save=` (the stored preset)  Game=1 Tbl=0 al=0 au=0 lu=0 ll=0
#:                                           bm=0 ca=1 cb=1 co=0 du=0 gs=1
#:                                           in=0 pa=0 st=1 tl=3
#:   code 0x14 (Confirm)                     al=100 au=300 lu=99999 ll=0
#:                                           bm=0 ca=1 cb=1 co=0 du=0 gs=1
#:                                           in=0 pa=0 st=1 tl=3
#:
#: The `@Tet=` seven are **byte-identical in both**, so the player never touched
#: them -- they are what the client seeded itself with. Only the `@Tab=` numeric
#: ranges differ (the preset had never been stored, hence its zeros).
#:
#: WARNING: Corroborated independently, which is why this is not just "what one player
#: happened to pick": an earlier wire reading measured `au=300/al=100` ACCEPTED and
#: `au=0/al=0` ALWAYS REFUSED. Zeros are not merely un-default here, they are
#: rejected.
#:
#: WARNING: SCOPE: this is the fallback for a table whose owner has set nothing, and it
#: only reaches `/R=` on the GameInit. The authored `b/g/PTL` fixture still
#: carries its own rule block, and the open question -- whether `au`/`al` live
#: anywhere in the 104-byte table record at all -- is untouched by this.
TET_DEFAULTS = {"bm": 0, "du": 0, "st": 1, "cb": 1, "ca": 1, "gs": 1, "tl": 3}

#: The `@Tab=` half, same measurement. Not used by `/R=` (which is `@Tet=` only);
#: recorded here because it is the other half of the same answer and the next
#: person to serve `@Save=` back will need it.
TAB_DEFAULTS = {"in": 0, "lu": 99999, "ll": 0, "au": 300, "al": 100,
                "co": 0, "pa": 0, "pw": 0}


#: VERIFIED: RETRACTED 2026-08-25, THE SAME NIGHT, BY READING THE BUILDER. The stake
#: is BACK ON: `@ComGame=/Rule=` occurrence 0 **is** the pot, and the banner
#: below (kept for the record) reasoned its way to the wrong answer from
#: position and coincidence.
#:
#: `0x106F87`..`0x106FE4`, the `@ComGame=` builder, emits occurrence 0 from
#: `[ebp+0xDC]` and clamps it first DOWN to **0x7D0 = 2000** and then UP to 0
#: before printing it -- and `[obj+0xDC]` is the POT (`0x52464BC`), with 2000
#: the same cap `POL_TM_WAGER_CAP` already carried. The `@ComGameInit=` parser
#: agrees from the other side: `0x102E05` stores `/R=` occurrence 0 to
#: `[esi+0xDC]`. Two functions, one slot, and a clamp that only makes sense
#: for money.
#:
#: WARNING: WHAT THE "BOTH PLAYERS SENT 135" EVIDENCE WAS WORTH: nothing, on its own.
#: It is a real observation with an innocent explanation (two players on the
#: same screen picking the same figure), and it was allowed to outweigh a
#: function nobody had opened. That is exactly the
#: open-the-function-not-just-the-address trap, and it cost a live feature for
#: an hour. The saved `bm` (2000 / 995) is the PRESET's own wager field, a
#: different tier from the live pot -- "looks more like a wager" was pattern
#: matching, not measurement.
#:
#: `POL_TM_WAGER_COM=0` still turns charging off if it ever needs an A/B.
#:
#: ---- the original, WRONG banner, kept so the reasoning stays auditable ----
#: WARNING: THE VS. COM STAKE IS OFF BY DEFAULT, AND IT IS OFF BECAUSE THE FIELD WE
#: WERE CHARGING IS NOT MEASURED TO BE A STAKE.
#:
#: `@ComGame=/Rule=` occurrence 0 was read as "the wager the player chose on
#: the COM select screen" and `_start`/settle arithmetic was built on it --
#: real gold, moved on every COM game. The wire does not support the reading.
#: Every `@ComGame=/Rule=` this server has ever logged opens with the SAME
#: number:
#:
#:     @ComGame=/Rule=135|1|1|1|3|0|0|0     member 3, wallet 39360
#:     @ComGame=/Rule=135|0|1|1|1|0|0|0     member 6, wallet  9600
#:     @ComGame=/Rule=0|1|1|1|3|0|0         member 3, the bare (no /Com=) form
#:
#: Two different players, wallets 4x apart, both sending 135. A per-player
#: stake cannot do that, and a money-DERIVED default cannot either -- the
#: tester's own measurement of the client's default is "10% of however much
#: you have in gold" (see `tmsave.DEFAULT_TABLE_PRESET_DERIVED`), which would
#: be 3936 and 960. Meanwhile the same two players' SAVED `bm` (tmsave +0xB8)
#: is 2000 and 995 -- and 995 is exactly 10% of the 9950 that client held.
#: **`bm` in the save behaves like the wager; `/Rule=` occ 0 does not.**
#:
#: So we were deducting an unverified constant and then telling the client its
#: pot was that constant, which is the live report: "at the end, it said
#: the wager was 0 even though it wasn't". Charging stops until the field is
#: read out of the `@ComGame=` builder (0x106F52 -> 0xA97C0) rather than
#: guessed from position. `POL_TM_WAGER_COM=1` re-enables it for that A/B.
#:
#: WARNING: SCOPED TO THE COM PATH ON PURPOSE. The PvP wager comes from somewhere
#: else entirely (`_PVP_STAKES`, filled at the accept quorum) and no evidence
#: here touches it -- `POL_TM_WAGER` / `POL_TM_WAGER_PVP` are unchanged. And
#: the `@GameExit=` REFUND stays live whatever this says, so a stake taken
#: before the flip is still given back.
def _com_wager_enabled():
    """Is the VS. COM stake allowed to move gold? Default NO -- see above."""
    return bool(_env_int("POL_TM_WAGER", 1)
                and _env_int("POL_TM_WAGER_COM", 1))


def _rule_seven(fields):
    """The seven `/R=` values for a table, from the settings its owner chose."""
    over = os.environ.get("POL_TM_VSGAME_RULES")
    if over:
        out = []
        for part in over.split(","):
            try:
                out.append(int(part.strip(), 0))
            except ValueError:
                out.append(0)
        return (out + [0] * 7)[:7]
    fields = fields or {}
    return [int(fields.get(k.decode(), TET_DEFAULTS.get(k.decode(), 0)) or 0)
            for k in VSGAME_RULE_KEYS]


#: The COM `@ComGameInit=` `/R=` field order the CLIENT PARSER (0x102D21) reads,
#: which is NOT the @Tet order `VSGAME_RULE_KEYS` uses. Measured 2026-09-02:
#: occ0->[obj+0xDC]=bm, occ1->[0xE0]=st, occ2->[0xE1]=cb, occ3->[0xE2]=ca,
#: occ4->[0xE3]=tl, occ5->[0xE4]=du (occ6->[0xD9]=d9 is written, not rendered).
#: `gs` is DROPPED on the COM path. Sending @Tet order here MANGLED the VS. COM
#: rules screen: `/R=0|0|1|1|1|1|3` (@Tet: bm,du,st,cb,ca,gs,tl) is read as
#: bm=0, st=0 (Special Tile OFF), cb=1, ca=1, tl=1 (Time Limit 0:30), du=1 --
#: the tester's exact wrong-defaults screen (Special Tile Off / 0:30 vs the
#: factory On / 3:00), 2026-09-02. In COM order the same defaults are
#: `/R=0|1|1|1|3|0|0` = Special Tile On, Time Limit 3:00.
COM_RULE_KEYS = (b"bm", b"st", b"cb", b"ca", b"tl", b"du")


def _com_rule_seven(fields):
    """The seven `@ComGameInit=` `/R=` values in the CLIENT PARSER's order.

    A COM game's rules land on a DIFFERENT slot order than `@Tet=`/`_rule_seven`
    (see `COM_RULE_KEYS`): six named fields plus a trailing d9=0 (occ6, written
    to [obj+0xD9] but not rendered). Use this -- never `_rule_seven` -- for any
    `@ComGameInit=` `/R=` we build from defaults. `POL_TM_VSGAME_RULES` still
    overrides the lot (given in this same COM order).
    """
    over = os.environ.get("POL_TM_VSGAME_RULES")
    if over:
        out = []
        for part in over.split(","):
            try:
                out.append(int(part.strip(), 0))
            except ValueError:
                out.append(0)
        return (out + [0] * 7)[:7]
    fields = fields or {}
    vals = [int(fields.get(k.decode(), TET_DEFAULTS.get(k.decode(), 0)) or 0)
            for k in COM_RULE_KEYS]
    return (vals + [0])[:7]      # + d9 (occ6), keeping seven values


#: The `/R=` field order the @VsGameInit / @WatchInfo client PARSER reads
#: (arm 0x102476), which is NOT the @Tet order `VSGAME_RULE_KEYS` aliased.
#:
#: WARNING: THIS IS THE PvP "the turn timer is always 30 s / Special Tile is off"
#: BUG, 2026-09-03. `_rule_seven` emitted `/R=` in `@Tet=` order
#: (bm,du,st,cb,ca,gs,tl), but @VsGameInit lands its seven on the SAME match
#: object as @ComGameInit -- occ0->[0xDC], occ1..5->[0xE0..0xE4] -- and the
#: game engine reads the TURN TIMER from [0xE3] = occ 4 (measured on the COM
#: path 2026-09-02; same engine, same struct). @Tet order put `tl` at occ 6,
#: so the client read `ca` (=1) at occ4 as the timer -> **0:30 every game**,
#: and `du` (=0) at occ1 as Special Tile -> **OFF**. The bug's very existence
#: proves the timer is NOT at occ6 ([0xE9]), or the old order would have shown
#: 3:00. So the fix is the SAME reorder that repaired VS. COM: `tl` at occ4.
#:
#: The first six positions are exactly `COM_RULE_KEYS`; they differ only at
#: occ6, which @VsGameInit writes to [0xE9] (COM writes [0xD9]). `gs` is the
#: remaining @Tet field and the likely occupant -- but [0xE9]'s match-meaning
#: is UNMEASURED and does NOT affect the timer. `_vsgame_rule_seven(None)` is
#: [0,1,1,1,3,0,1] = Special Tile On, Time Limit 3:00, the factory screen.
#: `POL_TM_VSGAME_RULES` overrides the lot, given in THIS order.
VSGAME_RULE_KEYS_PARSED = (b"bm", b"st", b"cb", b"ca", b"tl", b"du", b"gs")


def _vsgame_rule_seven(fields):
    """The seven `@VsGameInit=`/`@WatchInfo=` `/R=` values in the CLIENT
    PARSER's slot order (`VSGAME_RULE_KEYS_PARSED`) -- never `_rule_seven`'s
    `@Tet=` order, which mis-slots the timer and Special-Tile rules."""
    over = os.environ.get("POL_TM_VSGAME_RULES")
    if over:
        out = []
        for part in over.split(","):
            try:
                out.append(int(part.strip(), 0))
            except ValueError:
                out.append(0)
        return (out + [0] * 7)[:7]
    fields = fields or {}
    return [int(fields.get(k.decode(), TET_DEFAULTS.get(k.decode(), 0)) or 0)
            for k in VSGAME_RULE_KEYS_PARSED]


#: member_id -> (chan, table index, seats) as they were when the match was
#: ANNOUNCED.
#:
#: WARNING: BECAUSE `_SEATED` IS ALREADY EMPTY BY THE TIME `@GameOK=` ARRIVES, AND
#: THAT IS CORRECT BEHAVIOUR, NOT A BUG TO FIX THERE. Measured live
#: 2026-08-20T10:43Z, both players:
#:
#:     tm:   #TM0R001 table 1 released -> 1 seated
#:     tm: member 6 left #TM0R001 -- released table(s) 1
#:     tm: member 6 sent @GameOK= -- answering (0x41, 0x9) @GameWa=/Error=0
#:     tm:   no @VsGameInit -- member 6 is seated at no table
#:
#: **Entering the match means LEAVING the room**, so the PART path runs
#: `release_seats` in the middle of the accept chain -- between
#: `@CheckJoinTable=` and `@GameOK=`. Deriving the roster at `@GameOK=` time
#: therefore always finds nobody. Snapshot it at the one moment it is complete
#: and true: when the table reached its seat count and the match was announced.
#:
#: WARNING: Keyed per MEMBER, not per table, so each player keeps their own view --
#: `_vsgame_ids` needs to know which seat the RECIPIENT holds.
_MATCH_ROSTER = {}


#: (chan, table index) -> the set of members who have sent `@GameOK=`.
#:
#: WARNING: THE GUEST'S BOARD NEVER OPENED, AND THE REASON IS THIS FILE'S OLDEST LAW
#: ONE LEVEL DOWN. Measured live 2026-08-20T10:55Z, the two players differing in
#: exactly one thing -- how long after accepting the message arrived:
#:
#:     m6 guest  accepted 10:55:01, @VsGameInit at 10:55:03  (2 s)  -> nothing,
#:                                            then @GameExit= at 10:55:41
#:     m3 host   accepted 10:55:24, @VsGameInit at 10:55:36  (12 s) -> FOUND,
#:                                            ---->Recv=PLGAMEINIT, ID=0, and it
#:                                            went on to send @CardSelect=
#:
#: The guest accepted FIRST, so its board was pushed while the host had not
#: accepted yet -- into a scene that did not exist, in a store the transition
#: then drained. Same fault as `POL_TM_MATCH41_GAP`, same fault as `@MuchMake`'s
#: two halves: **when a message arrives is as load-bearing as what it says.**
#:
#: A reply-count hold-back cannot fix it, because the thing to wait for is not a
#: number of replies -- it is the OTHER PLAYER. A game starts when everyone is
#: in, so the push waits for the last accept and then goes to all of them.
#: `POL_TM_VSGAME_QUORUM=0` restores the push-on-own-accept behaviour for an A/B.
_MATCH_ACCEPTS = {}

#: (chan, table index) -> set of push keys whose player LEFT a running match
#: (`@Break=` / `@GameExit=` mid-game). The server plays those seats itself --
#: see `_seat_departed` and `_bot_move`. Cleared with the match.
_MATCH_BOTS = {}
#: (chan, table index) of matches where a departed seat got a SYNTHETIC hand
#: (left during card select) -- the take flow is off for those.
_BOT_SYNTH = set()

#: WARNING: THE ACCEPT QUORUM MUST OUTLIVE THE PROCESS. Measured 2026-09-03T22:57Z on
#: prod: member 11 `@GameOK=` at 22:57:13 ("1 of 2"), a hand `docker compose`
#: recreated authsess at 22:57:07-18 EDT (18 s, NOT the git-sync timer -- its
#: 18:57:32 run touched only `jan`), member 6 `@GameOK=` at 22:57:24 -> "1 of
#: 2, waiting for 11". The first accept lived only in `_MATCH_ACCEPTS`, so the
#: fresh process could never reach quorum; both players backed out (`@Break=`
#: + `@GameExit=`) and re-did Start Game. The deploy gate could not help
#: either: `tm-matches-live.json` was first written at `@CardSelect=`, so the
#: whole Start Game -> deal window reported count 0 (`_live_matches_write` now
#: runs at `@GameReady=` / `@GameOK=` too, and counts pending accepts).
#:
#: So each accept is ALSO written here, keyed by table, with the roster it was
#: given against and a stamp; `_note_accept` adopts a fresh entry when this
#: process holds nothing for that table and the file's roster still matches.
#: A stale entry (older than POL_TM_ACCEPT_TTL_S, default 600 s) is ignored --
#: an accept from yesterday's game must not start today's. Cleared on quorum,
#: on a fresh announcement (`_remember_match`) and on a rematch reset.
#: `POL_TM_ACCEPT_PERSIST=0` disables both the write and the adopt.
_ACCEPTS_FILE = "tm-match-accepts.json"


def _accepts_path():
    return os.path.join(os.environ.get("POL_DATA_DIR", "/data"), _ACCEPTS_FILE)


def _accepts_key(chan, index):
    return "%s|%s" % (chan, index)


def _accepts_load():
    try:
        with open(_accepts_path()) as f:
            got = json.load(f)
        return got if isinstance(got, dict) else {}
    except (OSError, ValueError):
        return {}


def _accepts_store(data):
    path = _accepts_path()
    try:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        tmp = "%s.tmp.%d.%d" % (path, os.getpid(), threading.get_ident())
        with open(tmp, "w") as f:
            json.dump(data, f)
        os.replace(tmp, path)
    except OSError as exc:
        _say("tm:   accept quorum NOT persisted (%r) -- a restart before the "
             "deal loses it" % (exc,))


def _accepts_persist(chan, index, got, need):
    """Write one table's accept set beside the roster it was taken against."""
    if not _env_int("POL_TM_ACCEPT_PERSIST", 1):
        return
    data = _accepts_load()
    data[_accepts_key(chan, index)] = {
        "stamp": time.time(),
        "got": sorted(str(k) for k in got),
        "need": sorted(str(k) for k in need),
    }
    _accepts_store(data)


def _accepts_forget(chan, index):
    if not _env_int("POL_TM_ACCEPT_PERSIST", 1):
        return
    data = _accepts_load()
    if data.pop(_accepts_key(chan, index), None) is not None:
        _accepts_store(data)


def _accepts_adopt(chan, index, need):
    """The persisted accept set for this table, if fresh and for THIS roster."""
    if not _env_int("POL_TM_ACCEPT_PERSIST", 1):
        return set()
    entry = _accepts_load().get(_accepts_key(chan, index))
    if not isinstance(entry, dict):
        return set()
    age = time.time() - float(entry.get("stamp") or 0)
    ttl = _env_int("POL_TM_ACCEPT_TTL_S", 600)
    if not 0 <= age <= ttl:
        _say("tm:   persisted accept set for %s table %s is %ds old (> %ds) "
             "-- ignored" % (chan, index, int(age), ttl))
        return set()
    if sorted(str(k) for k in need) != list(entry.get("need") or []):
        _say("tm:   persisted accept set for %s table %s was taken against a "
             "different roster (%s vs %s) -- ignored"
             % (chan, index, entry.get("need"), sorted(str(k) for k in need)))
        return set()
    by_str = {str(k): k for k in need}
    got = {by_str[s] for s in (entry.get("got") or []) if s in by_str}
    if got:
        _say("tm: VERIFIED: adopted %d persisted accept(s) for %s table %s from a "
             "previous process (%s) -- the quorum survives the restart"
             % (len(got), chan, index, ", ".join(str(k) for k in sorted(got, key=str))))
    return got


def _remember_match(chan, index, who):
    """Snapshot the roster for everyone in it. See `_MATCH_ROSTER`."""
    seats = list(who)
    cur = _roster_at(chan, index)
    if (_MATCH_BOTS.get((chan, index)) and cur
            and {_push_key(m) for m, _v in seats}
            <= {_push_key(m) for m, _v in cur}):
        # A leaver being unseated from a match the server is now playing a
        # seat of: the announced roster stands (seat numbers, hands, board),
        # and resetting here would wipe the game the others are still in.
        _say("tm:   ...%s table %d roster shrank to %d while a departed seat "
             "is being played -- keeping the running match"
             % (chan, index, len(seats)))
        return
    for mid, _ident in seats:
        # SEATED HERE MEANS OUT OF THERE. A member still on another
        # table's roster (a crash left it) is retired from it first, and a
        # live VS. COM binding at another table -- a crashed COM game keeps
        # its table Playing for POL_TM_MATCH_HOLD_S -- is released, since
        # `_in_com_game` would otherwise read this PvP seat as a COM game.
        # See `_match_abandoned`; the same knob gates both.
        _k = _push_key(mid)
        _old = _MATCH_ROSTER.get(_k)
        if _old and (_old[0], _old[1]) != (chan, index):
            _match_abandoned(mid, "seated at %s table %s now" % (chan, index))
        _cc, _ci = _com_table_of(mid)
        if (_ci is not None and (_cc, _ci) != (chan, index)
                and _env_int("POL_TM_ABANDON_CLEANUP", 1)):
            _say("tm: 🧹 member %s is seated at %s table %s but still bound to "
                 "a VS. COM game at %s table %s -- releasing the stale binding"
                 % (mid, chan, index, _cc, _ci))
            _release_com_table(_cc, _ci, why="member %s is seated in a PvP "
                               "match" % mid)
            _reset_for_rematch(None, _k)
    for mid, _ident in seats:
        _MATCH_ROSTER[_push_key(mid)] = (chan, index, seats)
    # A fresh announcement is a fresh game: nobody has accepted it yet.
    _MATCH_ACCEPTS.pop((chan, index), None)
    _MATCH_BOTS.pop((chan, index), None)
    _BOT_SYNTH.discard((chan, index))
    _accepts_forget(chan, index)
    _CARD_READY.pop((chan, index), None)
    # ...and a fresh hand and a fresh turn counter. Leaving the previous match's
    # hands behind would relay the WRONG CARD -- `/H=` is an index into this
    # list and nothing on the wire would contradict it.
    _MATCH_HANDS.pop((chan, index), None)
    _MATCH_TURN.pop((chan, index), None)
    _MATCH_BOARD.pop((chan, index), None)
    _MATCH_OBJECTS.pop((chan, index), None)
    _MATCH_COMBO.pop((chan, index), None)
    _TURN_RAND.pop((chan, index), None)
    _MATCH_CONTINUE.pop((chan, index), None)
    # ...and the Double Up escalation. A fresh announcement is a fresh table
    # session; the escalator only compounds ACROSS REMATCHES, which is what
    # makes it a rematch stake and not a wager multiplier.
    _TABLE_POT.pop((chan, index), None)


#: (chan, table index) -> set of members who have sent `@GameReady=` this game.
#: Guards the guest wake-up against a ping-pong: only players who have NOT started
#: are woken, so the guest's own @GameReady= (sent after it is woken) does not push
#: @GameStart= back at the already-playing host. Reset on the rules CONFIRM (0x14),
#: which is the once-per-game signal that survives the start sequence's PART churn.
_MATCH_STARTED = {}

#: *** THE RESERVATION IS A HEARTBEAT, AND ITS SILENCE IS THE CANCEL. ***
#:
#: Measured live 2026-08-22 (live testing):
#: the client opens a dedicated `@Pong=/Cnt=N/GM=0` conversation with the TABLE's
#: OWN PEER the moment `@GameEN=` is confirmed -- counter from 0, one beat every
#: ~15 s -- and that heartbeat is owned by the RESERVATION, not the room scene:
#:
#:   * rules-Confirm + parking in the wait scene: beats continue (Cnt 0..7+);
#:   * PART, the rooms list, standing in ANOTHER room: beats continue
#:     (Cnt 16..19 measured after the PART) -- reservation persistence is
#:     literally "keep ponging your table from wherever you are";
#:   * the tile-menu Cancel Reservation: **zero bytes on the wire; the beats
#:     just stop.** The cancel has no message (the bare `@GameQT=` earlier
#:     measurements attributed to it is the settings dialog-confirm).
#:
#: So SE's server can only have freed a cancelled seat by TIMEOUT, and that one
#: rule also covers every ghost-seat producer on record: dead sockets,
#: crashed clients, and the post-match leak (a finished match's players never
#: resume the table heartbeat). `_SEAT_ALIVE` stamps each (chan, member) on
#: every table-peer @Pong; `expire_silent_seats` frees a seat whose stamp is
#: older than POL_TM_SEAT_TTL_S (default 45 s = three missed beats + slack).
#:
#: WARNING: THE MATCH GATE: players in a live match CONSUME the reservation (the
#: client-side clear at 0xA9520) and stop ponging the table -- expiring their
#: seats mid-game would tear the table down under a running match. A table
#: whose match began less than POL_TM_MATCH_HOLD_S ago (default 2700 = 45 min,
#: a generous full-game bound) is exempt; after that the TTL reclaims it, which
#: is deliberate -- nothing else ever ends a match server-side (the result
#: screen is a client-local park; see tm-vs-com-gate).
#:
#: WARNING: RESTART SAFETY: stamps are process-local, so a fresh process sees seats
#: with no stamp. The sweep's first sight of such a seat STARTS its clock
#: rather than freeing it -- a live holder re-stamps within ~15 s, and only a
#: genuinely silent one ages out. (The d1bd5100 lesson: a bounce must never
#: free a live player's seat.)
_SEAT_ALIVE = {}     #: (chan, member int) -> last table-peer @Pong epoch seconds
_LIVE_MARKER_WHINED = False

_MATCH_BEGAN = {}    #: (chan, table index) -> epoch of the game's first @GameReady=
_SEAT_SWEEP = [0.0]  #: last expiry sweep, rate limit


def _match_running(chan, index):
    """Is a game actually RUNNING at this table -- somebody sent `@GameReady=`
    (the host's Start Game) and has not yet sent the room-return `@GameExit=`.

    THE ROOM'S "Playing" TRUTH. While this is True the row publishes at state 2
    (arm 0x67344 -- the observable-in-play tile: "Playing" label, Observe on
    the menu, VAR4 = no join for foreign players). Before this the row sat at
    state 1 (recruiting) for the whole match, so the room kept offering Join
    on a table mid-game (live testing, 2026-08-22).

    WARNING: THIS STATE MUST NOT OUTLIVE THE MATCH. `_MATCH_STARTED` empties on
    the last `@GameExit=` (republished there), is popped when the table empties
    (`_republish_table`), and `_occupancy_state` returns the authored state for
    an empty seat list BEFORE it ever looks at inplay -- so a crashed match
    degrades to the TTL sweep freeing the seats, never to a stuck "Playing".
    Process-local, deliberately: a restart forgets the flag and the row falls
    back to recruiting, which is recoverable; persisting it would be a new
    producer of exactly the phantom-seat problem.

    `POL_TM_INPLAY_STATE=0` disables (seat count >= POL_TM_INPLAY_AT stays the
    only way to state 2)."""
    if not _env_int("POL_TM_INPLAY_STATE", 1):
        return False
    # ...AND A VS. COM GAME COUNTS. It is a real game at a real table (see
    # `_COM_AT`, and the peer decode that proves which one), so the room shows
    # Playing and offers Observe for it exactly as it does for a PvP match.
    # Before this, a COM game left the tile free: the reported bug.
    if _com_key_at(chan, index) is not None:
        return True
    try:
        return bool(_MATCH_STARTED.get((chan, int(index))))
    except (TypeError, ValueError):
        return False


def _alive_key(chan, member, index=None):
    """The heartbeat key for one member's hold on one TABLE.

    WARNING: `index` IS NOT OPTIONAL DECORATION -- keying by room alone let one
    table's beat keep ANOTHER table's seat alive. The reservation heartbeat is
    addressed to the TABLE's own peer (see the `_SEAT_ALIVE` banner), so a
    player reserved at table 1 who walks into a VS. COM game at table 2 pongs
    table 2 for the whole game -- and under a `(chan, member)` key every one of
    those beats re-stamped the table-1 reservation, so the TTL could never
    reap it. That is a phantom-tile producer with an engine behind it:
    the stale seat is refreshed forever, by design, by a different table.

    Measured 2026-08-25 (member 6, reserved table 1 at 04:04:05Z, COM game at
    table 2 from 04:04:25Z, ponging table 2's peer every ~15 s).

    `index=None` is still accepted so a caller that genuinely means "this
    member's hold on this room" (there is one: the pre-table-resolution path)
    does not have to invent a number, and it keys distinctly from any real
    table so the two can never alias.
    """
    try:
        member = int(member)
    except (TypeError, ValueError):
        pass
    try:
        index = int(index) if index is not None else None
    except (TypeError, ValueError):
        index = None
    return (chan, index, member)


def _peer_table(peer_nick):
    """(chan, index) when this peer nick IS a table's id, else (None, None).

    A published table id is `(K_hi ^ room) << 32 | (n << 12 | n)` (see
    `tmroom.canonical_table_id`), and a nick carries `(id ^ K) & 40 bits`, so
    `peer_guid ^ K` restores the id exactly. The low half's shape (the table
    number twice) is the discriminator -- member guids do not fold to it.
    """
    if not peer_nick:
        return None, None
    try:
        import tmroom
        tid = peer_guid(peer_nick) ^ tmroom.client_key()
    except Exception:
        return None, None
    low = tid & 0xFFFFFFFF
    n = low & 0xFFF
    if not (1 <= n <= 16) or low != ((n << 12) | n):
        return None, None
    room = ((tid >> 32) ^ ((tmroom.client_key() >> 32) & 0xFFFFFFFF)) & 0xFFFFFFFF
    if not (1 <= room <= 999):
        return None, None
    return "#TM0R%03d" % room, n


def expire_silent_seats(now=None):
    """Free every seat whose holder's table heartbeat has gone silent.

    See the `_SEAT_ALIVE` banner: the client's cancel (and every crash/ghost
    variant) is expressed ONLY as the table-peer @Pong stopping. Runs from the
    @Pong arm (rate-limited) and from `reconcile_room_tables`, sweeps the
    SHARED seat map so a seat this process has not touched is still covered.
    Returns the number of seats freed. `POL_TM_SEAT_TTL_S=0` disables.
    """
    ttl = _env_int("POL_TM_SEAT_TTL_S", 45)
    if ttl <= 0:
        return 0
    now = now if now is not None else time.time()
    if now - _SEAT_SWEEP[0] < min(ttl / 3.0, 15.0):
        return 0
    _SEAT_SWEEP[0] = now
    freed = 0
    try:
        import tmroom
        hold = _env_int("POL_TM_MATCH_HOLD_S", 2700)
        for room_hex, tbls in (tmroom._live_seats() or {}).items():
            try:
                rid = int(room_hex, 16)
            except (TypeError, ValueError):
                continue
            chan = "#TM0R%03d" % (rid & 0xFFFFFFFF)
            for idx_s, rows in (tbls or {}).items():
                try:
                    idx = int(idx_s)
                except (TypeError, ValueError):
                    continue
                began = _MATCH_BEGAN.get((chan, idx))
                if (_MATCH_STARTED.get((chan, idx))
                        and (began is None or now - began < hold)):
                    continue        # a match is (or may be) running here
                for m, _i in list(rows or []):
                    key = _alive_key(chan, m, idx)
                    stamp = _SEAT_ALIVE.get(key)
                    if stamp is None:
                        # First sight (deploy, restart, adoption): start the
                        # clock instead of freeing -- a live holder re-stamps
                        # within one beat (~15 s).
                        _SEAT_ALIVE[key] = now
                    elif now - stamp > ttl:
                        _say("tm: member %s's seat at %s table %d: table "
                             "heartbeat silent %ds (> TTL %ds) -- the client "
                             "cancelled or is gone; releasing"
                             % (m, chan, idx, int(now - stamp), ttl))
                        release_seats(m, chan)
                        _SEAT_ALIVE.pop(key, None)
                        freed += 1
    except Exception as exc:
        _say("tm:   heartbeat expiry sweep failed (%r) -- seats stand" % (exc,))
    return freed


#: member_id -> the peer NICK that member's `@GameOK=` arrived on.
#: That is the band its match scene reads; see `_pending_pushes`.
#: WARNING: A VS. COM game never sends `@GameOK=`, so the COM path fills this at
#: `@GameENC=` time instead -- the game peer that request arrives on is the
#: peer whose guid becomes the match object's SESSION PAIR ([obj+0x28]/[0x2C],
#: stored by the `@ComGameInit` arm at 0x102D3C from the delivering record's
#: sender u64). Every in-match push must be FRAMED from that nick or the
#: equality gates (0x1034FD for @StartData, 0x103054 for @Quit, ...) discard
#: it silently -- which is what "the deal was pushed and nothing happened" was.
_MATCH_PEER = {}


#: push_key -> {"n": total players, "coms": [character indexes], "peer": nick}.
#:
#: THE VS. COM ROSTER, AND THE CLIENT TELLS US IT EXACTLY ONCE. The COM
#: character-select screen sends (live 2026-08-22T13:57:26Z)
#:
#:     @ComGame=/Rule=100|0|1|1|1|0|0|0/Com=1|2       (0x43, msgid 6)
#:
#: where `/Com=` is a PIPE LIST of the chosen COM character indexes -- built by
#: the client at 0x107239 from [obj+0xE5] (count) over the per-entry table at
#: [obj+0x1CA]. One entry per opponent, so the PLAYER COUNT of the match is
#: `1 + len(/Com=)`: `/Com=2` = 2-player, `/Com=1|2` = 3-player. Discarding it
#: hardcoded every COM game to N=2 -- a 3-player game got `/Ans=1|1` and
#: `/L=0|0` ("1 of 2 have chosen" on a three-seat board, log 13:57:58Z).
_COM_GAME = {}


def _com_n(member_id):
    """Player count of this member's VS. COM game, or None outside one."""
    got = _COM_GAME.get(_push_key(member_id))
    return got.get("n") if got else None


#: (chan, table index) -> push_key of the member playing the COM there.
#:
#: VERIFIED: A VS. COM GAME IS AT A REAL TABLE, AND THE CLIENT NAMES IT -- measured
#: live 2026-08-25 on prod. `@GameENC=` carries no table field, so the COM
#: path never looked for one; but it ARRIVES ON A TABLE PEER, and
#: `_peer_table` decodes that exactly (peer `UGRAWGL2Q` -> `#TM0R001` table 2,
#: `UGRAWGI78` -> table 3; three sessions, perfect correlation). The client
#: then PARTs the room and `JOIN`s that table's own channel `#TM0T002` --
#: byte for byte the observer's own flow.
#:
#: So the room's tile CAN say "Playing" for a COM game and the Observe menu
#: CAN reach it; nothing was missing but this binding. Two consumers:
#:
#:   * `_match_running` -- the tile's state-2 ("Playing"/Observe) truth, with
#:     `_com_who` supplying the occupancy the row needs to render it;
#:   * the `@Data=/Watch=` arm -- a watcher of a COM table is registered under
#:     the COM MATCH KEY `(None, push_key)`, which is what every COM
#:     `_watch_push` call already uses, so the relays flow unchanged.
#:
#: WARNING: THIS MUST NOT OUTLIVE THE GAME. Cleared on `@GameExit=`, on the
#: next `@GameENC=` from the same member (a rematch rebinds), and by
#: `expire_com_tables` when the COM player's table-peer heartbeat goes silent
#: -- the same signal, TTL and first-sight grace as a reservation.
_COM_AT = {}


def _in_com_game(member_id):
    """Is this member INSIDE a VS. COM game right now?

    WARNING: THE QUESTION `seats` CANNOT ANSWER, and getting it from `seats` is what
    made a COM game unplayable. `_match_of` reads `_MATCH_ROSTER`, which
    SURVIVES the PART into a match on purpose (it is the only thing that still
    knows the roster once the room is gone) -- so a player who RESERVES a table
    and then walks into VS. COM instead arrives at the COM handlers with a
    one-entry `seats` list, and every `None if seats else _com_n(...)` in this
    file reads their COM game as a one-player PvP match.

    Measured live 2026-08-25 (member 6): `@GameEN=` 04:04:05Z reserved table 1,
    `@GameENC=` 04:04:25Z started the COM game at table 2. Consequences, all
    from that one stale seat list:
      * `n_eff = len(seats) = 1` -- "1 of 1 have chosen", and the `if com_n:`
        arm that DEALS THE COM ITS CARDS never ran;
      * `_queue_next_turn` instead of `_queue_com_turns`, with n = 1, so
        `(actor + 1) % 1` is 0 for ever: the COM was announced the human's turn
        every time and never played a card (reported live: "it just skips");
      * the match state keyed by the stale PvP `(chan, index)` while the
        watchers sat under the COM key, so the deal's sh=1 @WatchInfo refresh
        went to a key with no watchers and the observer stayed parked on
        "players are selecting their cards".

    `_COM_AT` is the honest answer: bound at `@GameENC=`, released on the
    room-return `@GameExit=` and by the heartbeat TTL, so it is true for
    exactly the life of the game. `_COM_GAME` is NOT usable here -- nothing
    pops it, so it would go on claiming a later PvP match is a COM game.
    """
    return _com_table_of(member_id)[1] is not None


def _com_table_of(member_id):
    """(chan, index) of this member's VS. COM game, or (None, None)."""
    got = _COM_GAME.get(_push_key(member_id)) or {}
    tbl = got.get("table")
    return tbl if tbl else (None, None)


def _com_key_at(chan, index):
    """The COM match key `(None, push_key)` for a game at this table, else None.

    The key, not the member: it is what `_MATCH_BOARD`/`_MATCH_TURN`/
    `_MATCH_HANDS` and every COM `_watch_push` are already keyed by.
    """
    try:
        key = _COM_AT.get((chan, int(index)))
    except (TypeError, ValueError):
        return None
    return (None, key) if key is not None else None


def _com_who(chan, index):
    """`[(member_id, ident)]` for the COM player at this table, else [].

    The tile's occupancy list, in the shape `_occupancy_block` wants. `ident`
    is the `/NN=` off that member's own `@GameENC=` -- the client compares
    block[14..29] against its OWN guid, so a fabricated one is worse than none
    (see `_occupancy_block`).
    """
    try:
        key = _COM_AT.get((chan, int(index)))
    except (TypeError, ValueError):
        return []
    got = _COM_GAME.get(key) or {}
    mid = got.get("member")
    if mid is None:
        return []
    return [(mid, got.get("ident") or 0)]


def _bind_com_table(member_id, peer_nick, ident=None):
    """Bind this member's VS. COM game to the table its peer names, and publish
    that row as Playing so the room can SEE it -- and observe it.

    Returns (chan, index), or (None, None) when the peer names no table.
    `POL_TM_COM_TABLE=0` restores the pre-2026-08-25 behaviour, where a COM
    game was invisible to the room.
    """
    if not _env_int("POL_TM_COM_TABLE", 1):
        return None, None
    chan, index = _peer_table(peer_nick)
    if index is None:
        _say("tm: 🔭 member %s VS. COM -- peer %r decodes to no table, so the "
             "room cannot be told and nobody can observe this game. (Every "
             "measured COM request arrived on the picked table's own peer; if "
             "this line appears, that changed.)" % (member_id, peer_nick))
        return None, None
    key = _push_key(member_id)
    # A rematch, or a second game at another table: drop the old binding first,
    # so one member can never hold two tables Playing.
    for k, v in list(_COM_AT.items()):
        if v == key and k != (chan, index):
            # NOT popped here: `_release_com_table` pops it AND republishes the
            # row. Popping first makes it a no-op and strands the old tile at
            # Playing -- exactly the phantom-tile shape this feature must not add.
            _release_com_table(k[0], k[1],
                               why="the same member started another COM game")
    # WARNING: AND THE LOBBY SEAT GOES. One seat per player is already this file's
    # rule -- `_seat_at_table` enforces it when a player reserves somewhere
    # else, because the client has ONE my-table slot -- and a COM game is
    # "somewhere else". Without this, reserving table 1 and then playing the
    # computer at table 2 leaves table 1 reserved by somebody who is demonstrably
    # not there, which is the phantom tile arriving by the front door.
    # `POL_TM_COM_RELEASES_SEAT=0` keeps the reservation.
    if _env_int("POL_TM_COM_RELEASES_SEAT", 1) and member_id is not None:
        try:
            _held = [i for i, ss in (_seats_of(chan) or {}).items()
                     if any(m == member_id for m, _v in ss) and i != index]
            if _held:
                _say("tm: 🔭 member %s starts VS. COM at %s table %d but still "
                     "holds table(s) %s in that room -- releasing. One seat per "
                     "player; the client has one my-table slot."
                     % (member_id, chan, index,
                        ", ".join(str(i) for i in _held)))
                release_seats(member_id, chan)
        except Exception as exc:
            _say("tm:   ...could not release member %s's lobby seat(s) for the "
                 "COM game (%r) -- a phantom reservation may remain"
                 % (member_id, exc))
    _COM_AT[(chan, index)] = key
    ent = _COM_GAME.setdefault(key, {})
    ent["table"] = (chan, index)
    ent["member"] = member_id
    if ident:
        ent["ident"] = ident
    # Start the heartbeat clock HERE, exactly as `@GameEN=` does for a
    # reservation: the first table-peer @Pong lands ~4 s later (measured), and
    # an unstamped binding would ride the sweep's first-sight grace instead of
    # the real TTL.
    if member_id is not None:
        _SEAT_ALIVE[_alive_key(chan, member_id, index)] = time.time()
    try:
        import tmroom
        _republish_table(tmroom, chan, index, _com_who(chan, index),
                         why="VS. COM started, now Playing")
    except Exception as exc:
        _say("tm:   %s table %s not flipped to Playing for the COM game (%r) "
             "-- the room keeps seeing a free tile" % (chan, index, exc))
    # ...AND OPEN THE DEPLOY WINDOW. `_live_matches_write` counts `_COM_AT`
    # now, but the gate reads the STAMP's freshness too -- so write one here
    # rather than inherit whatever the last match-flow handler left.
    _live_matches_write()
    _say("tm: 🔭 member %s VS. COM is at %s table %d (decoded from peer %r) -- "
         "row published Playing; the room can observe it"
         % (member_id, chan, index, peer_nick))
    return chan, index


def _release_com_table(chan, index, why="VS. COM over"):
    """Unbind a COM table and put its row back to whatever the seats justify."""
    if index is None:
        return
    key = _COM_AT.pop((chan, index), None)
    if key is None:
        return
    ent = _COM_GAME.get(key)
    if ent:
        ent.pop("table", None)
    # A departing COM game takes its watchers with it -- they are registered
    # under the COM match key, and the next game must not inherit them.
    _MATCH_WATCHERS.pop((None, key), None)
    try:
        import tmroom
        _republish_table(tmroom, chan, index,
                         _seats_of(chan).get(index) or [], why=why)
    except Exception as exc:
        _say("tm:   %s table %s not returned from Playing (%r)"
             % (chan, index, exc))
    _live_matches_write()
    _say("tm: 🔭 %s table %s: %s -- the COM binding is released"
         % (chan, index, why))


def expire_com_tables(now=None):
    """Free a COM table whose player's table heartbeat has gone silent.

    The no-phantom-tile rule, applied to the producer this feature adds: a crash, a kill or a
    relaunched client leaves no `@GameExit=`, and without this the row would
    say "Playing" for ever on a table nobody is at. Same signal and TTL as a
    reservation (`_SEAT_ALIVE`, stamped by the @Pong arm off the table peer --
    a COM client pongs its table peer every ~15 s, measured 2026-08-25), and
    the same first-sight grace so a restart starts the clock instead of
    reaping. `POL_TM_COM_TTL_S=0` disables.
    """
    ttl = _env_int("POL_TM_COM_TTL_S", _env_int("POL_TM_SEAT_TTL_S", 45))
    if ttl <= 0 or not _COM_AT:
        return 0
    now = now if now is not None else time.time()
    freed = 0
    for (chan, index), key in list(_COM_AT.items()):
        got = _COM_GAME.get(key) or {}
        mid = got.get("member")
        if mid is None:
            continue
        # WARNING: THE MATCH GATE, and it is the same one `expire_silent_seats`
        # needs: a client in the match scene may stop ponging its table (the
        # reservation is consumed client-side -- see the `_SEAT_ALIVE`
        # banner), and reaping mid-game would clear the tile under a running
        # COM game. A game with a live turn gets the generous match hold
        # instead of the beat TTL.
        _st = _MATCH_TURN.get((None, key)) or {}
        _running = (_st.get("turn") is not None
                    and not _st.get("result_sent"))
        eff = _env_int("POL_TM_MATCH_HOLD_S", 2700) if _running else ttl
        akey = _alive_key(chan, mid, index)
        stamp = _SEAT_ALIVE.get(akey)
        if stamp is None:
            _SEAT_ALIVE[akey] = now
        elif now - stamp > eff:
            _say("tm: member %s's VS. COM game at %s table %d: table heartbeat "
                 "silent %ds (> %ds) -- the client is gone; releasing the "
                 "tile" % (mid, chan, index, int(now - stamp), eff))
            _release_com_table(chan, index, why="VS. COM heartbeat expired")
            _SEAT_ALIVE.pop(akey, None)
            freed += 1
    return freed


def _note_accept(chan, index, member_id, seats):
    """Record one `@GameOK=`; return the members to hand the board to.

    Empty until the last player accepts, then every seat at once. See
    `_MATCH_ACCEPTS`.
    """
    if not _env_int("POL_TM_VSGAME_QUORUM", 1):
        return [member_id]
    need = {_push_key(m) for m, _i in seats}
    if (chan, index) not in _MATCH_ACCEPTS:
        # Nothing in THIS process for the table: a restart may have eaten an
        # earlier accept (measured 2026-09-03T22:57Z, see `_ACCEPTS_FILE`).
        _MATCH_ACCEPTS[(chan, index)] = _accepts_adopt(chan, index, need)
    got = _MATCH_ACCEPTS[(chan, index)]
    got.add(_push_key(member_id))
    waiting = need - got
    if waiting:
        _accepts_persist(chan, index, got & need, need)
        _live_matches_write()
        _say("tm:   %s table %d: %d of %d accepted -- holding the board until "
             "%s does too (a board pushed before the last accept lands in a "
             "scene that does not exist yet)"
             % (chan, index, len(got & need), len(need),
                ", ".join(str(w) for w in sorted(waiting, key=str))))
        return []
    _accepts_forget(chan, index)
    _say("tm:   %s table %d: ALL %d accepted -- the game starts now"
         % (chan, index, len(need)))
    return [m for m, _i in seats]


#: (chan, table index) -> the set of members who have sent `@CardSelect=`.
#: See the `@CardSelect=` handler: the `/Ans=` vector has to be true, and this
#: is what makes it true.
_CARD_READY = {}


def _cardselect_body(seats, ready, msgid, me=0, n_solo=None):
    """`@CardSelect=/Ans=<per seat>/Ok=0`, addressed to (0x43, msgid).

    `/Ans=` occurrence i is ABSOLUTE seat i; arm 0x1030C9 rotates it to the
    display slot itself. 1 means that seat has chosen -- 0x10317E gives a seat
    whose byte goes 0 -> 1 the extra `[esi+0x19F+48k] = 5`, which is why 1 is
    the "has confirmed" value and not merely non-zero.

    `n_solo` is the VS. COM player count (`_com_n`); it only matters when
    `seats` is empty, where 2 used to be hardcoded and a 3-player COM game got
    a two-entry vector.
    """
    n = len(seats) or int(n_solo or 2)
    yes = _env_int("POL_TM_CARDSELECT_ANS", 1)
    # WARNING: `/Ok=` IS "EVERYONE IS READY, GO" -- NOT A SUCCESS FLAG, AND NOT A
    # CONSTANT. Measured 2026-08-20T13:24Z: both clients read the answer
    # (`---->Recv=CARDSELECT`), both showed cards selected, and both then sat
    # for ever polling 7/35/36/40 and never asking for 8.
    #
    # The match scene is a state machine on `[esi+0x16F]` dispatched at 0xC2827
    # (`add eax, -0x17`, bound 8, table 0x5052F18), and the card-select state is
    # **0x17**:
    #
    #     0xC2835  0x101A10(7)
    #     0xC283A  cmp eax, 2        <- TWO, not 1
    #     0xC283D  jne 0xC2884       <- otherwise stay here for ever
    #     0xC287D  [esi+0x16F] = 0x18
    #
    # and 0x1031BC computes that return as **`/Ok=` + 1**. So `/Ok=0` returns 1,
    # which satisfies every `cmp eax, 1` caller but leaves THIS state parked.
    # `/Ok=1` returns 2 and releases it: 0x18 falls straight to 0x19, which
    # drains 7 (0xC28B5's `jg` loop) and then asks for **8 = @StartData**.
    #
    # So the value is a function of the vector, not a knob: 0 while anyone is
    # still choosing, 1 once they all have. `POL_TM_CARDSELECT_OK` forces it.
    forced = os.environ.get("POL_TM_CARDSELECT_OK")
    # WARNING: NO SEATS = A VS. COM GAME, AND IT IS READY THE MOMENT THE HUMAN PICKS.
    # `bool(seats)` made `everyone` False FOREVER for a COM game -- there are no
    # lobby seats to be ready -- so `/Ok=` stayed 0, 0x1031BC returned 1, and the
    # card-select state `[esi+0x16F]=0x17` parked exactly as this banner warns.
    # Live 2026-08-22: the board came up, the player chose five cards, and the
    # COM opponent "selected cards" for ever.
    #
    # This is the THIRD time a PvP seat gate has blocked the COM path -- the same
    # shape as `_push_muchmake` (seat count) and `_queue_vsgameinit` (`/N=` > 1).
    # When a COM feature stalls, look for a seat count before looking for a
    # missing message. The human's own submission is the whole quorum here: the
    # opponent is the server. `POL_TM_CARDSELECT_OK` still forces the field, and
    # `POL_TM_COM_CARDSELECT_OK=0` restores the old behaviour for an A/B.
    solo = not seats and _env_int("POL_TM_COM_CARDSELECT_OK", 1)
    everyone = solo or (bool(seats)
                        and all(_push_key(m) in ready for m, _i in seats))
    okv = _env_int("POL_TM_CARDSELECT_OK", 0) if forced else (1 if everyone else 0)
    # WARNING: RECIPIENT FIRST, for the same reason the board is -- and this is what
    # "player 2 was pushed into selecting cards, the game just auto selected"
    # was. Measured 2026-08-20T11:47:33Z: m6 had just picked and we answered it
    #
    #     @CardSelect=/Ans=0/Ans=1
    #
    # because the vector was in TABLE order and m6 sits at table seat 1. The
    # client reads occurrence 0 and nothing else (see `_vsgame_body`), so it read
    # its OWN state as `0` -- not chosen -- immediately after choosing.
    if seats:
        vec = [yes if _push_key(m) in ready else 0 for m, _i in seats]
        if _env_int("POL_TM_VSGAME_SEAT0", 1) and me:
            vec = vec[me:] + vec[:me]
    else:
        vec = [yes] * n
    return (encode_code(IN_MATCH_CODE | ((msgid & 0xFF) << 16))
            + b"@CardSelect=/Ans=" + b"|".join(b"%d" % v for v in vec)
            + b"/Ok=%d" % okv)


def _match_of(member_id):
    """(chan, index, seats) for the match this member is in, or (None, None, []).

    In-match messages arrive AFTER the player has left the room, so `_SEATED`
    and `room_of()` are both empty by then -- the announced-match snapshot is
    the only thing that still knows. See `_MATCH_ROSTER`.
    """
    got = _MATCH_ROSTER.get(_push_key(member_id))
    return got if got else (None, None, [])


def _table_of_member(chan, member_id):
    """(table index, seats) for wherever this member is sitting, or (None, []).

    The live seat list first -- it is authoritative while they are still in the
    room -- then the announced-match snapshot, which is the only source that
    survives the PART that entering a match performs.
    """
    for idx, seats in sorted(_seats_of(chan).items()):
        if any(m == member_id for m, _i in seats):
            return idx, list(seats)
    remembered = _MATCH_ROSTER.get(_push_key(member_id))
    if remembered:
        r_chan, idx, seats = remembered
        if chan is None or r_chan == chan:
            return idx, list(seats)
    return None, []


#: *** EVERY `:str` FIELD IN THIS PROTOCOL IS HEX-ENCODED ASCII. ***
#:
#: Measured 2026-08-20 and confirmed byte for byte against a live client's own
#: trace. `0xAAD60` is the parser behind every field `tmcmds.py map` marks
#: `:str` -- `@VsGameInit`'s `/CN=`, the chat roster's `/CN=`, SHOPINIT's
#: `/SN=`, `@ResultData`'s `/RN=` -- and it is not a text extractor at all. Its
#: tail, 0xAAFDA..0xAB005, walks the value TWO CHARACTERS AT A TIME:
#:
#:     hi = c > '9' ? c - 0x37 : c - 0x30      ; 0xAAFE3
#:     lo = same for the next character        ; 0xAAFF4
#:     append (hi << 4) + lo                   ; 0xAB002, 0xAB03B
#:     esi += 2 ; edi += 2 ; while edi+1 <= end
#:
#: i.e. `4C6170746F70` -> `Laptop`. 0xAAF33's `start + 2 > end -> return empty`
#: is the "not even one byte" guard, and 0xAAF3E's `esi + 1` is the second
#: character of the first pair.
#:
#: WARNING: THE PROOF IS THE TESTER'S OWN SCREEN. Reported live: "the player names
#: seem to still be garbage text". We sent `/CN=<name1>|<name2>` in plain text -- correct
#: on the wire -- and the client's `%s` print of the parsed slots (rva 0x10271A)
#: emitted `7A CD B9 FE FD` and `9C FE FD`. Feed the two plain-text names to
#: the decoder above and you get exactly those bytes: 'L'(0x4C) - 0x37 = 0x15,
#: << 4 = 0x50, + 'a'(0x61) - 0x37 = 0x2A -> **0x7A**, which is the `z` the log
#: opens with. Not a hypothesis -- an identity.
#:
#: WARNING: UPPERCASE ONLY. The decoder's `c - 0x37` branch is taken for anything above
#: '9', so 'A'..'F' land on 10..15 and lowercase 'a'..'f' land on 0x2A..0x2F --
#: silently wrong, no error anywhere.
#:
#: WARNING: AND THE PROJECT HAD ALREADY CAPTURED THIS, in this file. The `@GameEN=`
#: banner records the client's OWN send as
#: `/CN=/HN=4C6170746F705465737432/L=1` -- `HN` is the same tester's name in exactly this
#: encoding -- and the `OWNER_PUSH_CODE` note says `@Init=` uses `/CN=` "for a
#: content id (hex ASCII)". The convention was on the wire in both directions
#: and only the ints had been re-derived.
#:
#: `POL_TM_HEXSTR=0` sends the raw text again, for an A/B without a rebuild.
def _str_field(value):
    """One `:str` value, in the encoding `0xAAD60` actually decodes."""
    if isinstance(value, str):
        try:
            raw = value.encode("cp932")
        except UnicodeEncodeError:
            raw = value.encode("cp932", "replace")
    else:
        raw = bytes(value or b"")
    if _env_int("POL_TM_HEXSTR", 1) != 1:
        return raw
    return raw.hex().upper().encode("ascii")


def _com_player_name(char_index, seat):
    """The `/CN=` for a COM seat, HEX-ENCODED -- see `_str_field`.

    WARNING: UNMEASURED, AND DELIBERATELY GENERIC. The COM opponents' real names live
    in the client's own character-select data; nothing on the wire has ever
    carried one (`@ComGame=/Com=` is a list of INDEXES, and `@ComGameInit=`
    carries no names at all), so there is nothing to reproduce here -- only a
    watcher needs them, and a blank label is a blank label on the board. The
    pattern is a knob: `POL_TM_COM_NAME` takes one `%d`, the character index.
    If a capture ever shows the real names, that table replaces this.
    """
    pattern = os.environ.get("POL_TM_COM_NAME", "COM %d")
    try:
        name = (pattern % int(char_index)) if "%d" in pattern else pattern
    except (TypeError, ValueError):
        name = pattern.replace("%d", str(seat))
    return _str_field(name[:15])


def _vsgame_player_name(member_id, seat):
    """The `/CN=` for one seat, HEX-ENCODED -- see `_str_field`.

    Never empty: the client copies it into a 16-byte slot at rva
    0x2B6218 + i*0x10 and prints it with `%s`, so an empty one is a blank label
    on the board. The 15-character budget is on the DECODED text; the wire form
    is twice that.

    WARNING: A tester's screenshot of the first real match is the measurement that
    forced the encoding: the second player's label read a literal `z` followed
    by squares, and 0x7A `z` is precisely `('L'-0x37)<<4 + ('a'-0x37)` -- the
    client hex-decoding the plain-text name we had been sending."""
    name = ""
    try:
        import tmroom
        name = tmroom.name_of(member_id) or ""
    except Exception:
        name = ""
    name = name.strip()
    if not name:
        pattern = os.environ.get("POL_TM_VSGAME_NAME", "Player%d")
        name = (pattern % (seat + 1)) if "%d" in pattern else pattern
    return _str_field(name[:15])


#: member_id -> the GAME-ID that client compares `@VsGameInit`'s `/ID=` against.
#:
#: WARNING: THIS REPLACES A WORKAROUND BUILT ON A WRONG MEASUREMENT, and the client is
#: what corrected it. `_vsgame_ids` used to put a chosen value at the
#: recipient's own seat because `CalcPlayerData` printed `GAME-ID = 0` in one
#: 2026-08-19 log, so no real id was thought to exist. Measured 2026-08-20T11:22Z
#: with the trace on both machines, the guest answered:
#:
#:     [tm] +1024A9  ---->Recv=PLGAMEINIT     it read the board
#:     [tm] +10257E  ID=NOT_FOUND             and could not find itself
#:
#: and its own debug line one screen earlier says why:
#:
#:     [tm] +80E87  gameID = 1000001602
#:
#: 1000001602 is 0x3B9AD042 -- **exactly the `/GID=` that same client sends in
#: `@Init=`** (`/GID=000000003B9AD042`). So GAME-ID is the player's content id,
#: every client reports its own at login, and the two players' ids differ. The
#: zero was a stale reading of a field that had not been populated yet.
#:
#: So there is nothing to work around: send the REAL ids, the same list to
#: everyone, and each client finds itself at its own seat.
_GAME_IDS = {}


def _note_game_id(member_id, cmd):
    """Remember this member's `/GID=` off `@Init=`. Hex ASCII on the wire."""
    if member_id is None:
        return
    m = re.search(rb"/GID=([0-9A-Fa-f]{1,16})", cmd or b"")
    if not m:
        return
    try:
        gid = int(m.group(1), 16)
    except ValueError:
        return
    if _GAME_IDS.get(_push_key(member_id)) == gid:
        return
    _GAME_IDS[_push_key(member_id)] = gid
    _say("tm: member %s GAME-ID = %d (0x%X) -- from @Init= /GID=. This is what "
         "@VsGameInit's /ID= is matched against (the client prints the same "
         "number as `gameID = %d`)." % (member_id, gid, gid, gid))


def _game_id_of(member_id):
    """This member's GAME-ID, or None if they have not sent `@Init=` here."""
    return _GAME_IDS.get(_push_key(member_id))


def _note_pol_id(member_id, cmd):
    """Remember this member's `@Init=/NN=` -- Tetra Master's POL-ID -- verbatim.

    Hex ASCII on the wire and hex ASCII in the store: the client writes it with
    the 16-digit hex formatter at 0xACDA0 and reads it back with the accumulator
    at 0xAB72E, so echoing its own characters is the one representation that
    cannot get the nibble order wrong. `tmroom.note_pol_id` is where it lands
    and why; `_chat_roster_body` is what spends it.
    """
    if member_id is None:
        return
    m = re.search(rb"/NN=([0-9A-Fa-f]{1,16})", cmd or b"")
    if not m:
        return
    try:
        import tmroom
    except ImportError:
        return
    try:
        if tmroom.note_pol_id(member_id, m.group(1)):
            _say("tm: member %s POL-ID = %s -- from @Init= /NN=. This is what "
                 "the chat roster's /NN= is matched against; a client skips the "
                 "row carrying its own (TM.dll 0x0AFE3)."
                 % (member_id, m.group(1).decode("ascii").upper()))
    except Exception as exc:
        _say("tm: member %s POL-ID %r not stored (%r)"
             % (member_id, m.group(1), exc))


def _vsgame_ids(seats, me):
    """The `/ID=` list, one per seat, FROM THIS RECIPIENT'S POINT OF VIEW.

    WARNING: THE CLIENT LOOKS FOR ITS OWN ID AND NOTHING ELSE. 0x1024F9 walks the
    `/ID=` occurrences comparing each against the global at TM.dll rva
    **0x294BD4**; the first that matches is that player's seat index, and if none
    matches it prints `ID=NOT_FOUND` and returns -1, which is a refusal.
    Everything after is derived from that index -- the seat rotation at
    [obj+0x19B+48i], which `@No<i>=` section is read for money, which name goes
    on which side of the board.

    0x294BD4 is what `CalcPlayerData` prints as **`GAME-ID`** (0x818B1 loads the
    trio [0x294BD8]/[0x294BDC]/[0x294BD4]; 0x81930..0x8194E prints them as
    POL-ID / GAME-ID / HAND-ID), and in the only trace that carries the line --
    `logs/shim-FRAMEWORK.log`, twice -- it reads **0**. Nothing in the image
    writes it with an absolute store, so we have never assigned one and both
    clients are holding the same value.

    That is survivable because a push is PER MEMBER: each recipient gets its own
    copy of this message, so the recipient's own id goes at THEIR seat and a
    value they cannot match goes everywhere else. `POL_TM_VSGAME_SELFID` is what
    we believe their GAME-ID to be (0, measured) and `POL_TM_VSGAME_PEERID_BASE`
    is the first of the deliberately-not-that values.
    """
    # WARNING: NOT `/GID=`, AND THAT WAS A RETRACTION. `@VsGameInit` compares against
    # the global at rva **0x294BD4**, which `CalcPlayerData` prints as
    # `GAME-ID` and which measures **0**. The client ALSO prints
    # `gameID = 1000001602` at 0x80E82 -- but that reads [0x2B52A0/A4], the
    # pair that feeds `/GID=` in `@Init=`, a DIFFERENT variable. Sending the
    # content id put a number in the list that nothing compares against, and the
    # guest answered ID=NOT_FOUND with its own id sitting right there.
    self_id = _env_int("POL_TM_VSGAME_SELFID", 0)
    if _env_int("POL_TM_VSGAME_SEAT0", 1):
        # The recipient is seat 0 by construction, so its own value goes first
        # and every peer gets something it cannot match.
        return [self_id] + [self_id + 1 + i for i in range(len(seats) - 1)]
    real = [_game_id_of(m) for m, _i in seats]
    if all(v is not None for v in real):
        return real
    # FALLBACK, and it should never be needed once every player has sent
    # `@Init=` on this connection. Kept because a board built from a guessed id
    # at least lets the RECIPIENT find itself, which is strictly better than a
    # message every client refuses -- and it logs, so a run that uses it says so.
    self_id = _env_int("POL_TM_VSGAME_SELFID", 0)
    base = _env_int("POL_TM_VSGAME_PEERID_BASE", 1)
    out = []
    for i, (mid, _ident) in enumerate(seats):
        known = real[i]
        if known is not None:
            out.append(known)
            continue
        if i == me:
            out.append(self_id)
            continue
        v = base + i
        if v in out or v == self_id:
            v = self_id + len(seats) + i + 1
        out.append(v)
    _say("tm:   WARNING: no GAME-ID for seat(s) %s -- falling back to synthesised ids "
         "%r. That client never sent @Init= here, and it will answer "
         "ID=NOT_FOUND unless its own id is at its own seat."
         % (", ".join(str(i) for i, v in enumerate(real) if v is None), out))
    return out


def _vsgame_body(seats, me, rules):
    """`@VsGameInit=` for the player sitting at seat `me`.

    Field order and widths off arm 0x102476, in the order it parses them:

        /N=          -> [obj+0xE5] and [obj+0xC7]; [obj+0xE6] = N-2.
                        **Must be > 1** -- 0x1024D5 sends `cl <= 1` to 0x102943,
                        which prints `StartAloneERROR` and fails the scene.
        /ID= x N     -> see `_vsgame_ids`: the seat search, then the rotation.
        /CN= x N     -> the 16-byte name slots, in ABSOLUTE seat order (the
                        per-player record's name pointer is assigned from the
                        rotated index at 0x102701, not the other way round).
        /R=  x 7     -> the ruleset; see `VSGAME_RULE_KEYS`.
        @No<i>=      -> one section per ABSOLUTE seat i, matched by name
                        (sprintf "@No%d", 0x10282F). Inside it:
                          /RA= -> dword [obj+0x180+48k]   average rank
                          /M=  -> dword [obj+0xC8], **only when k == 0**
                                  (0x1028BE), i.e. only out of the recipient's
                                  OWN section -- money is private
                          /CP= -> word  [obj+0x17C+48k]   card points
                        where k = (i - me) mod N is the display slot.
    """
    # WARNING: THE RECIPIENT GOES FIRST, AND THE CLIENT CANNOT BE TOLD OTHERWISE.
    # Measured 2026-08-20 across three runs, and the rule is exceptionless:
    #
    #   host  11:22  /ID=0/ID=2                    its id at occurrence 0  -> ID=0
    #   guest 11:22  /ID=1/ID=0                    its id at occurrence 1  -> NOT_FOUND
    #   guest 11:37  /ID=1000000302/ID=1000001602  its id nowhere          -> NOT_FOUND
    #
    # Every success matched at occurrence 0; every failure needed a later one.
    # The reason is in the parser: `0xAB080`'s third argument is NOT an
    # occurrence index -- `0xAB098` reads it as a BYTE and builds a string from
    # it, and `0x51B3944` (the token it searches for) is `"="`. This is the
    # map-keyed-on-`name=` parser this file's header already describes, so a
    # REPEATED `/ID=` collapses: the loop at 0x1024F9 re-reads the same value
    # every iteration and can only ever match on the first.
    #
    # So the seat numbering is PER RECIPIENT: each client is player 0 in its own
    # copy, which is what a card game wants anyway -- you are always the near
    # side of the board. Everything downstream already agrees: the rotation
    # `[obj+0x19B+48i] = (i + me) % N` is the identity at me=0, and `@No<i>=`'s
    # display slot `k = (i - me) mod N` becomes i.
    #
    # `POL_TM_VSGAME_SEAT0=0` restores absolute seat order for an A/B.
    if _env_int("POL_TM_VSGAME_SEAT0", 1) and me:
        seats = seats[me:] + seats[:me]
        me = 0
    n = len(seats)
    # VERIFIED: REPEATED VALUES ARE PIPE-SEPARATED UNDER ONE KEY, NOT A REPEATED KEY.
    # `0xAB080(name, buf, index, &out)`: after finding `name=` it runs the loop
    # at 0xAB15A -- `index` iterations of "find the next separator and step past
    # it" -- where the separator (0x51B2B78) is **`|`** and the value ends at the
    # next `/` or `@` (0x51B3950, a find-first-of set). So the third argument IS
    # an occurrence ordinal; it just indexes into a pipe list.
    #
    # We were emitting `/ID=0/ID=1`, so occurrence >= 1 had no `|` to step to and
    # every one of those reads failed. That single mistake produced all three
    # of the symptoms on screen, because none of the call sites checks the
    # return value before storing:
    #   * `/ID=`  -- only the first id readable  -> ID=NOT_FOUND for the guest
    #   * `/CN=`  -- 0x102635 SKIPS the copy on failure, leaving the 16-byte
    #                name slot holding whatever was there -> a row of squares
    #   * `/R=`   -- 0x10276C reads `[esp+0x20]` regardless of the result, so
    #                six of the seven rules came from stack garbage. One of them
    #                is the time limit, which is why a match nobody touched
    #                "auto-selected" the cards immediately.
    #
    # The client had been showing us the format the whole time: its own
    # `@CardSelect=/C=5@N0=/D=161|100|2|90|99|31|21|255` is eight `|`-separated
    # values under one key.
    body = b"@VsGameInit=/N=%d" % n
    body += b"/ID=" + b"|".join(b"%d" % v for v in _vsgame_ids(seats, me))
    body += b"/CN=" + b"|".join(_vsgame_player_name(m, i)
                                for i, (m, _ident) in enumerate(seats))
    body += b"/R=" + b"|".join(b"%d" % v for v in rules)
    for i, (mid, _ident) in enumerate(seats):
        # `/M=` only carries a real balance in the RECIPIENT's own section.
        # 0x1028BE reads it for `k == 0` and discards every other section's, so
        # putting another player's money on this wire would be data the client
        # cannot use and we have no evidence the game ever shares.
        body += b"@No%d=/RA=%d/M=%d/CP=%d" % (
            i, _env_int("POL_TM_VSGAME_RA", 0),
            money_of(mid) if i == me else 0,
            _env_int("POL_TM_VSGAME_CP", 0))
    return body


#: How many `/F=` values `@StartData` carries, indexed by `[obj+0xE6]` (= N-2).
#: Read straight out of the image at rva 0x23359E: {16, 25, 233, ...}. For two
#: players that is **16** -- a 4x4 board, which is Tetra Master's. The third
#: entry is nonsense, so only the 2- and 3-player counts are real.
TILES_BY_PLAYERS = (16, 25)


def _startdata_body(seats, me, starter=0, n_solo=None, codes=None):
    """`@StartData=` -- the deal. Arm 0x1034E2, `Recv=STARTDATA`.

    WARNING: THIS IS WHAT THE SCENE IS WAITING FOR. Measured 2026-08-20T13:38Z: once
    `/Ok=1` released card select, both clients moved to state 0x19 and began
    polling **7 then 8** (`0xC28AE`'s drain, then `0xC28C8`'s `0x101A10(8)`).
    Command 8 is this message.

    Field for field, and note it is gated like every other in-match message on
    the session pair `@VsGameInit` established ([obj+0x28]/[obj+0x2C], 0x1034FD):

        /S=      -> 0x10354E: the STARTING PLAYER, run through the same seat
                    rotation as everything else (`(N - me + S) % N`) and stored
                    at [0x2B63C4]+0x1B0 and +0x178. Because our copies are
                    recipient-first (me = 0), this is already in the recipient's
                    own frame -- so it must be encoded PER RECIPIENT, or both
                    players believe they lead.
        /B=      -> byte [0x2B63C4]+0x174
        /E=      -> byte [0x2B63C4]+0x175
        /F= xM   -> M = TILES_BY_PLAYERS[N-2]; [0x2B63C4]+0x1F5+i. The BOARD:
                    one value per tile, 16 of them for two players.
        /L= xN   -> per player, [obj+0x1A8+48k] at the rotated slot; a failed
                    parse writes 0 (0x103689), so the count must be exact.

    VERIFIED: `/F=`, `/B=` AND `/E=` ARE THE BOARD, MEASURED 2026-09-06 -- the three
    fields this docstring used to call unknown. `/F=` is one OBJECT CODE per
    tile (blocks, chance blocks, rotating blocks, special tiles), `/B=` is how
    many of those codes are block-family and `/E=` how many are special tiles.
    `codes` comes from `_match_objects`; the whole mechanism is in
    `tmbattle.roll_board`.

    WARNING: THE TWO COUNTS ARE DERIVED FROM `codes`, NEVER ROLLED SEPARATELY. The
    client's reveal loop divides by the number of tiles still carrying a code,
    so a count that outruns the array is a DIVIDE BY ZERO in the client.
    `POL_TM_START_B` / `POL_TM_START_E` still force them for an A/B and will
    happily crash a client if you force them too high -- that is what they are
    for. `POL_TM_START_TILE` fills every tile with one code (the old flat
    board; =0 is the pre-2026-09-06 behaviour).

    WARNING: `/L=` IS STILL UNKNOWN, and is still zero.

    The client says whether it read the message at all: `---->Recv=STARTDATA`
    (0x103520) is in the traced sink.
    """
    n = len(seats) or int(n_solo or 2)
    tiles = TILES_BY_PLAYERS[min(max(n - 2, 0), len(TILES_BY_PLAYERS) - 1)]
    tiles = _env_int("POL_TM_START_TILES", tiles)
    # The starter, moved into THIS recipient's frame. `_vsgame_body` rotated the
    # seats so the recipient is 0; the same rotation has to be applied here or
    # the two clients disagree about who leads.
    s_val = (int(starter) - int(me)) % n
    # THE BOARD. Absolute -- unlike `/S=` it is NOT rotated into the
    # recipient's frame, because a tile index is a tile index for everybody.
    # WARNING: THIS ONLY SENDS. `codes` is whatever `_match_objects` decided for this
    # match -- rolled, or forced by POL_TM_START_F/POL_TM_START_TILE -- and it
    # is the SAME array that seeded `_MATCH_BOARD`. Reading an override here
    # instead is what made the server and the client play different boards on
    # 2026-09-07T00:16Z; see the banner in `_match_objects`.
    board_codes = (list(codes or []) + [0] * tiles)[:tiles]
    b_val, e_val = tmbattle.board_counts(board_codes)
    b_val = _env_int("POL_TM_START_B", b_val)
    e_val = _env_int("POL_TM_START_E", e_val)
    body = (b"@StartData=/S=%d/B=%d/E=%d" % (s_val, b_val, e_val))
    body += b"/F=" + b"|".join(b"%d" % c for c in board_codes)
    body += b"/L=" + b"|".join(b"%d" % _env_int("POL_TM_START_L", 0)
                               for _ in range(n))
    return body


#: The per-match random table. WARNING: BOTH CLIENTS MUST GET THE SAME EIGHT VALUES --
#: `@TurnData`'s `/R=` is pre-rolled randomness the client stores and uses
#: locally (it prints them itself as `RandTbl=` then `%d,` per value), so two
#: different tables would have the two players computing different outcomes for
#: the same battle. Generated once per (chan, table) and reused for every
#: recipient of that turn.
_TURN_RAND = {}


def _turn_rand(key, fresh=False):
    """Eight values for `/R=`, identical for everyone in this match."""
    if fresh or key not in _TURN_RAND:
        import random as _r
        lo = _env_int("POL_TM_RAND_LO", 0)
        hi = _env_int("POL_TM_RAND_HI", 255)
        _TURN_RAND[key] = [_r.randint(lo, hi) for _ in range(8)]
    return _TURN_RAND[key]


def _turndata_body(seats, me, active, rand, scores=None, n_solo=None):
    """`@TurnData=` -- whose turn it is. Arm 0x1036A3, `Recv=TURNINFO`.

    Measured 2026-08-20T14:19Z: once `@StartData` was read (both clients printed
    `---->Recv=STARTDATA`, and the tester got the turn-order roulette on
    screen) both began polling **command 9**, which is this.

    Off the parser, and gated on the same session pair as everything else
    (0x1036BE):

        /A=     the ACTIVE player -- whose turn it is. Run through the same
                rotation as `@StartData`'s `/S=` (0x103713) and stored in the
                SAME fields, [0x2B63C4]+0x1B0 and +0x178, so it is the "current
                player" the board draws from. Per recipient, like /S=.
        /S= xN  per player -- THE SCORE. The store at 0x103799 is
                `[0x5246580 + 48k]` (esi in the arm is the parser's `this`
                0x52463E0, +0x1A0 = 0x5246580): the SAME per-seat field the
                client's own turn-end tally rewrites by walking the board and
                counting TILES per owner (0xC73EF zeroes it, 0xC7440
                increments per tile; no hand component). So /S= must carry
                the per-seat BOARD TILE COUNT, in the recipient's frame
                (occ 0 = the recipient; the client's own-seat global
                0x524657B is always 0 in its own frame).

                WARNING: Serving anything else FIGHTS the client's own tally over
                one display: the hand-remaining counts served until
                2026-08-22 made the score panel drop to the hand count on
                every announcement and snap back to tiles at each turn end
                -- the live report "score fluctuated, 1 point with 6 cards on
                the field", live that evening. The earlier "COM hand
                refilled" report against constant /S=5 was the same field.
        /R= x8  the RANDOM TABLE, [0x2B63C4]+0x1A8+i, exactly eight
                (0x1037F2 `cmp edi, 8`). The client logs them itself as
                `RandTbl=` / `%d,`.

    The random range is unmeasured; POL_TM_RAND_LO/HI sweep it.
    POL_TM_TURN_S is the constant fallback when no scores are passed.
    """
    n = len(seats) or int(n_solo or 2)
    a_val = (int(active) - int(me)) % n
    sc = scores or [_env_int("POL_TM_TURN_S", 5)] * n
    return (b"@TurnData=/A=%d" % a_val
            + b"/S=" + b"|".join(b"%d" % v for v in sc[:n])
            + b"/R=" + b"|".join(b"%d" % v for v in rand[:8]))


#: *** THE THIRD ENVELOPE BYTE IS THE TURN NUMBER, AND IT IS A GATE. ***
#:
#: Read off the parser 2026-08-20, and it explains the shape of every in-match
#: message at once. `describe_line` already calls this byte `sh` because the
#: card shop uses it for the shop number (`@Quit=` came back `sh=11` after we
#: served `/N=11`); in the 0x43 match family it is the TURN COUNTER.
#:
#:   0x1036EC  @TurnData  : `[obj+0x18D] = sh`   -- the SERVER sets the turn
#:   0x103536  @StartData : the same store, so the deal seeds it
#:   0x103876  @PutCard   : `cmp [obj+0x18D], sh` -> mismatch prints
#:             **`Errot!!Turn%d!=%d`** and the arm bails to the error path
#:   0x103C0D  @BattleData: the same compare, the same bail
#:
#: and the client's own senders put its current turn in the same slot:
#: `@TurnData=` (0x107954), `@PutData=` (0x1079F8), `@BattleSelect=` (0x107B3B)
#: and `@Ready=` (0x107BD1) all push `byte [0x2B63C4]+0x18D`. `@GetSelect=`
#: (0x107C6F) and `@Continue=` (0x107CD9) push the player's SEAT there instead
#: -- different scene, different meaning for the same byte.
#:
#: WARNING: THE CLIENT OWNS THE COUNTER BETWEEN TURNS. 0xC76A2 (state 0x28, the arm
#: that prints `GamePlayEnd:Turn=%d`) does `inc al; mov [esi+0x18D], al` and
#: then advances the active player `[esi+0x178] = ([esi+0x178]+1) % N`, skipping
#: anyone out of cards. So the server does not get to choose freely: the next
#: `@TurnData` must carry the turn the client has already counted to, or the
#: first `@PutCard` of that turn is refused with `Errot!!Turn`.
#:
#: The match is exactly `5 * N` turns -- 0xC7701 loads N from [0x2B64C5],
#: `lea eax,[eax+eax*4]` makes 5N, and `cmp [esi+0x18D], eax / jge` prints
#: `End` instead of `Next`. Ten turns for two players, five cards each.
PUTCARD_CMD = 10                #: code 0x43 command 10 -- `@PutCard`, arm 0x103820
PUTDATA_CMD = 10                #: ...and the slot the client's `@PutData=` rides
TURNDATA_ACK_CMD = 9            #: `@TurnData=` comes back on the slot it was sent on


def _turn_code(cmd, turn):
    """The wire header for an in-match message: code, command, turn."""
    return encode_code(IN_MATCH_CODE | ((cmd & 0xFF) << 16)
                       | ((int(turn) & 0xFF) << 24))


#: (chan, index) -> {push_key(member): [b"161|100|2|90|99|31|21|255", ...]}
#:
#: THE HANDS, CAPTURED FROM `@CardSelect=`. The client sends its whole hand as
#: `@CardSelect=/C=5@N0=/D=<row>@N1=/D=<row>...`, one eight-value row per card,
#: and that is the ONLY place the server ever learns what a player is holding.
#: `@PutData=` then names a card by its HAND INDEX only (`/H=`), so without this
#: there is nothing to put in `@PutCard`'s `/D=`.
_MATCH_HANDS = {}

#: (chan, index) -> {"turn": int, "active": absolute seat, "n": players}
_MATCH_TURN = {}

#: (chan, index) -> {tile: tmbattle.Card}. THE BOARD, and it is the thing this
#: server did not have.
#:
#: WARNING: EVERY INPUT ALREADY ARRIVED; NOTHING WAS BEING KEPT. `@PutData=` names
#: the tile (`/F=`) and the hand slot (`/H=`), `_MATCH_HANDS` turns the slot
#: into a row, and the actor is the connection -- so which card sits on which
#: tile and whose it is has been derivable since the first relayed placement.
#: Without it the server can answer `@PutCard` and nothing else: it cannot say
#: whether a placement STARTS a battle, cannot resolve one, and cannot count
#: the tiles at the end to know who won.
#:
#: Reset in `_remember_match` with the hands and the turn, for the same reason
#: they are: a stale board relays a battle that is not happening.
_MATCH_BOARD = {}

#: (chan, index) -> one OBJECT CODE PER TILE, exactly as `@StartData`'s `/F=`
#: sent it. See `tmbattle.roll_board` for what a code is.
#:
#: Kept past the deal because two different things need it afterwards: a
#: SPECIAL TILE has to hand its ability to whatever is played on it (the
#: client does this at 0xC7015/0xC703C and so must we -- `_tile_ability`), and
#: an observer arriving mid-match needs the same array in `@WatchInfo`.
#: Cleared wherever `_MATCH_BOARD` is.
_MATCH_OBJECTS = {}

BATTLEDATA_CMD = 12             #: code 0x43 command 12 -- `@BattleData`, arm 0x103BB7

#: The eight `|`-separated fields of a card row, named from the two independent
#: measurements that fixed them: `@PutCard`'s parser (0x103951..0x103AAA) and
#: `CardPrm.BIN`. occ0 is a WORD (`[obj+0x33A]`, and the client refuses
#: an id >= [0x2FF4FC], the card-count limit); the rest are bytes. occ2 is the
#: TYPE and the client rejects >= 4 with `Error:CardNum=%d` -- which is exactly
#: CardPrm's "no type 4 exists".
#:
#: WARNING: SLOTS 5 AND 6 WERE BOTH NAMED WRONG HERE UNTIL 2026-08-20, AND SLOT 6 IS
#: THE ONE THAT MATTERED: it is the **ARROW MASK**, not the level, and the
#: whole of the battle mechanic hangs off it. The reasoning that fixed it is in
#: `tmbattle.ARROW_COUNT_WEIGHTS`; the short version is that the client
#: GENERATES the byte with a random arrow count (0xB2DF4), bit-tests it per
#: direction in both the renderer (0xB4D0D) and the adjacency engine (0xD3760),
#: and that no column of a static card table can be any of those things.
#:
#: WARNING: AND THE CAPTURE THAT SEEMED TO SAY OTHERWISE WAS OUR OWN DATA COMING BACK.
#: The one measured hand, `161|100|2|90|99|31|21|255`, has 21 in slot 6, and
#: 21 is card 161's level -- because `_draw_pack` PUT it there. A round-trip
#: through the client is not a measurement of the client. `tmbattle` is the
#: authority for these names now; this tuple is kept in its order.
CARD_ROW_FIELDS = tmbattle.ROW_FIELDS


def _hands_from_cardselect(cmd):
    """The card rows out of one `@CardSelect=` body, in hand order.

    WARNING: Keyed by the `@N<i>=` number, not by order of appearance: `/H=` in
    `@PutData=` is an index into THIS list and an off-by-one here puts the
    wrong card on the board with no error anywhere.
    """
    rows = {}
    for m in re.finditer(rb"@N(\d+)=/D=([0-9|]+)", cmd or b""):
        rows[int(m.group(1))] = m.group(2)
    return [rows[i] for i in sorted(rows)]


def _putcard_body(seats, me, actor, hand_idx, tile, row, n_solo=None):
    """`@PutCard=` -- one card lands on the board. Arm 0x103820, `Recv=PUTCARD`.

    Field for field, off the parser:

        /A=   the ACTING player, rotated into the recipient's frame like every
              other seat number here. 0x103951 tests it against **255** and
              treats that as "nothing was placed", so a real seat must be sent.
        /H=   -> byte [0x2B63C4]+0x1B2, the hand slot the card came from
        /F=   -> byte [0x2B63C4]+0x1B1, the BOARD TILE. 0xC6FD7 prints it as
              `GamePlayPutCard=%d` and 0xC6FF1 treats 0xFF as "no placement".
        /D= x8  the card, one `|` list:
              occ0 word [obj+0x33A] (the id; `cad%05d` names its texture),
              occ1..6 bytes [obj+0x334..0x339], occ7 byte [obj+0x342].

    The recipient is always player 0 in its own copy, so `me` is 0 for
    everyone and `/A=` is the actor's DISTANCE from the recipient.
    """
    n = len(seats) or int(n_solo or 2)
    a_val = (int(actor) - int(me)) % n
    return (b"@PutCard=/A=%d/H=%d/F=%d/D=%s"
            % (a_val, int(hand_idx), int(tile), row))


def _battledata_body(res, att_tile, def_tile, rand):
    """`@BattleData=` -- one card contests another. Arm 0x103BB7,
    `Recv=BATTLESET`.

    VERIFIED: THIS IS THE MESSAGE A MATCH STOPPED ON. Measured live
    2026-08-20T17:34Z: a placement landed next to an enemy card, both clients
    printed `-BattleInit / -BattleCheck / -Battle:Select=1 / -BattleDecide:7 /
    -BattleStart:8` and then parked in `0x101A10(0xC)` at 0xCF107 for ever.

    Ten values, five per side -- `/A=` the ATTACKER (the card just placed),
    `/D=` the DEFENDER -- each a pipe list read by occurrence:

        occ0  byte  the board TILE      [obj+0x1B1] / [obj+0x1B2]
        occ1  word  the raw STAT        record +0x8E
        occ2  word  the ROLLED value    record +0x8C
        occ3  byte  the stat SELECTOR   record +0x29
        occ4  byte  the ability MULT    record +0x2A

    then `/R=` x8, the match's own random table again (0x103E38 writes it into
    the SAME [obj+0x1A8+i] slots `@TurnData` fills, and the client re-prints it
    as `RandTbl=`).

    WARNING: `/CBC=` IS DELIBERATELY OMITTED. It is optional -- 0x103E7B is
    `test eax,eax / je` straight to the arm's normal end -- and it carries a
    whole card row in `@PutCard`'s eight-field shape (0x103E85 applies the same
    card-id ceiling). INFERRED, from the name, that it is the "Combo Battle
    Card". Sending a card row for a mechanic nobody has watched happen is
    exactly the kind of invention this file exists to stop.

    WARNING: `occ0` of `/A=` is confirmatory only. 0xCF1D6 takes the ATTACKER tile
    from [obj+0x179], which the client set itself when it placed the card
    (0xC6FF9); only the DEFENDER tile is read out of the message, and only when
    the network flag is set (0xCF1A5, gated on [0x2B64CA]). Both are sent
    because both are parsed.
    """
    return (b"@BattleData=/A=%d|%d|%d|%d|%d/D=%d|%d|%d|%d|%d"
            % (int(att_tile) & 0xFF, res["a_raw"] & 0xFFFF,
               res["a_roll"] & 0xFFFF, res["a_sel"], res["a_mult"],
               int(def_tile) & 0xFF, res["d_raw"] & 0xFFFF,
               res["d_roll"] & 0xFFFF, res["d_sel"], res["d_mult"])
            + b"/R=" + b"|".join(b"%d" % v for v in rand[:8]))


def _board_tiles(n):
    """How many tiles an n-player board has. 0x23359E, and `TILES_BY_PLAYERS`."""
    idx = min(max(int(n) - 2, 0), len(TILES_BY_PLAYERS) - 1)
    return _env_int("POL_TM_START_TILES", TILES_BY_PLAYERS[idx])


def _dealt_chance_codes():
    """The chance codes this server may DEAL, after the server owner's knobs.

    `tmbattle.MODELLED_CHANCE_CODES` is the truth about what is MODELLED; this
    is the shorter list a server owner can ask for without a deploy. Only 17 has
    a switch, and it has one because it is the only effect that MOVES cards:
    get its candidate list wrong and the two boards disagree about which tile
    holds which card, which parks the next placement for ever (04:20Z). Every
    other code changes a card in place, so a divergence there plays wrong
    rather than hanging.

    `POL_TM_SCRAMBLE=0` -> no scramble is dealt and a forced board carrying one
    is blanked, the same way an unmodelled code always was.
    """
    codes = tuple(tmbattle.MODELLED_CHANCE_CODES)
    if not _env_int("POL_TM_SCRAMBLE", 1):
        codes = tuple(c for c in codes if c != 17)
    return codes


def _unplayed_specials(chan, index):
    """The tiles still carrying a SPECIAL nobody has played on -- `card+0x1C`.

    WARNING: THE SCRAMBLE'S CANDIDATE LIST NEEDS THIS AND NOTHING ELSE DOES. The
    client collects its redeal candidates with 0xD3A60's arg2=1 arm, which
    tests `card+0x1C`; an unplayed special tile is stamped **4** there
    (0xC1976) while its owner byte stays 8, so it is the one square this
    server holds no card on that the client will never redeal onto. Leaving it
    in made our list longer than theirs and the modulus picked a different
    tile -- the 2026-09-07T04:20Z hang. See `tmbattle.apply_scramble`.

    A special that has been PLAYED on is not in here: `_consume_tile_special`
    clears the deal entry when the card lands, which is the same thing the
    client does to the tile record (0xC7098 overwrites the 4 with the owner).
    """
    codes = _MATCH_OBJECTS.get((chan, index)) or []
    return [t for t, c in enumerate(codes)
            if int(c) in tmbattle.SPECIAL_CODES]


def _match_objects(chan, index, n, fresh=False):
    """This match's board objects, rolled ONCE and remembered.

    WARNING: ONCE PER MATCH, NOT ONCE PER RECIPIENT. `@StartData` is built per
    player because `/S=` is in the recipient's own seat frame -- but the BOARD
    is absolute, tiles are not rotated, so rolling inside `_startdata_body`
    would deal each player a different board and the two clients would
    disagree about every tile. Same law as `_TURN_RAND`.

    Rolling also SEEDS `_MATCH_BOARD` with the blocks, and that one line is
    what makes every consumer downstream correct without touching it: the
    COM's `empty` list is already `t not in board`, `_board_scores` already
    skips an owner outside the seat range, and `tmbattle.contest` now has the
    neutral arm the blocks need.

    WARNING: SPECIAL TILES ARE DELIBERATELY NOT PUT ON THE BOARD. A special tile is a
    property of a FREE tile, not an occupant (`tmbattle.special_ability`);
    putting one in the board dict would make it unplayable and cost the player
    a square. That asymmetry is the client's, not a shortcut.

    `POL_TM_BOARD_OBJECTS=0` deals the old empty board for an A/B.
    """
    key = (chan, index)
    if not fresh and key in _MATCH_OBJECTS:
        return _MATCH_OBJECTS[key]
    tiles = _board_tiles(n)
    if (not _env_int("POL_TM_BOARD_OBJECTS", 1)
            or tiles not in tmbattle.BLOCK_COUNT_WEIGHTS):
        if tiles not in tmbattle.BLOCK_COUNT_WEIGHTS:
            _say("tm: WARNING: no shipped block weights for a %d-tile board -- "
                 "dealing it empty" % tiles)
        _MATCH_OBJECTS[key] = [0] * tiles
        return _MATCH_OBJECTS[key]
    # WARNING: THE OVERRIDES LIVE HERE, NOT IN THE DEAL BUILDER, AND THAT COST A LIVE
    # GAME (2026-09-07T00:16Z, the first run with objects on). `POL_TM_START_F`
    # used to be read only by `_startdata_body`, so `/F=` went out as the
    # explicitly authored board while THIS function rolled a different one and
    # seeded `_MATCH_BOARD` from it: the client saw blocks on tiles 1/4/6/9/11/14
    # and the server believed 4/8/9/11/13. Every downstream consumer -- the free
    # list, `_tile_ability`, the arrow engine -- was then answering about a board
    # nobody was looking at. One source, or none.
    _explicit = os.environ.get("POL_TM_START_F")
    if _explicit:
        codes = []
        for _p in _explicit.replace(",", "|").split("|"):
            try:
                codes.append(int(_p.strip(), 0))
            except ValueError:
                codes.append(0)
        codes = (codes + [0] * tiles)[:tiles]
    elif os.environ.get("POL_TM_START_TILE") is not None:
        codes = [_env_int("POL_TM_START_TILE", 0)] * tiles
    else:
        codes = None
    if codes is not None:
        # WARNING: AN OVERRIDE IS NOT A LICENCE TO DEAL AN UNMODELLED EFFECT. The
        # forced board is a debugging convenience; the rule it must not escape
        # is `tmbattle.MODELLED_CHANCE_CODES`, because a chance code the client
        # applies and this server does not is a board divergence, and a board
        # divergence hangs the match. That is how 2026-09-07T04:20Z ended: the
        # env still pinned a 17 on tile 3 after `roll_board` had stopped dealing
        # one, so withdrawing the effect protected nobody. Blanked, loudly --
        # the deal is the one place this can still be caught.
        # WARNING: NOT just `UNMODELLED_CHANCE_CODES` (which is empty again now):
        # anything the DEAL is not allowed to produce must not arrive this way
        # either, or `POL_TM_SCRAMBLE=0` would withdraw 17 from the roll and
        # leave the override still pinning one -- which is exactly what made
        # the first withdrawal protect nobody.
        _dealable = set(_dealt_chance_codes())
        _unmodelled = set(tmbattle.UNMODELLED_CHANCE_CODES) | {
            c for c, eff in tmbattle.CHANCE_EFFECTS.items()
            if eff is not None and c not in _dealable}
        _dropped = [(t, c) for t, c in enumerate(codes) if c in _unmodelled]
        if _dropped:
            for t, _c in _dropped:
                codes[t] = 0
            _say("tm:   WARNING: forced board for %s table %s asked for %s -- BLANKED. "
                 "%s not on this server's deal list, and dealing one diverges "
                 "the board from the client's on the first battle against it"
                 % (chan, index,
                    ", ".join("code %d on tile %d" % (c, t) for t, c in _dropped),
                    "They are" if len(set(c for _t, c in _dropped)) > 1
                    else "It is"))
        _MATCH_OBJECTS[key] = codes
        board = _MATCH_BOARD.setdefault(key, {})
        for t, c in enumerate(codes):
            card = tmbattle.object_card(c)
            if card is not None:
                board[t] = card
        b, e = tmbattle.board_counts(codes)
        _say("tm:   ...board objects for %s table %s: FORCED by the environment "
             "-- /B=%d /E=%d (%s)"
             % (chan, index, b, e,
                ", ".join("tile %d %s" % (t, tmbattle.object_kind(c))
                          for t, c in enumerate(codes) if c) or "none"))
        return codes
    fields = _table_rules_get(chan, index) or {}

    def _on(k):
        """One rule byte, table first then the client's own defaults."""
        return int(fields.get(k, TET_DEFAULTS.get(k, 0)) or 0) != 0

    # `POL_TM_BOARD_SEED` deals the SAME board every match, which is the knob
    # the live proof wants: two runs that differ only in the thing under test.
    _seed = os.environ.get("POL_TM_BOARD_SEED")
    _rnd = random
    if _seed:
        try:
            _rnd = random.Random(int(_seed, 0))
        except ValueError:
            _say("tm: WARNING: POL_TM_BOARD_SEED=%r is not a number -- ignoring"
                 % (_seed,))
    codes = tmbattle.roll_board(tiles, _rnd,
                                special=_on("st"), chance=_on("cb"),
                                rotating=_on("ca"),
                                chance_codes=_dealt_chance_codes())
    _MATCH_OBJECTS[key] = codes
    board = _MATCH_BOARD.setdefault(key, {})
    for t, c in enumerate(codes):
        card = tmbattle.object_card(c)
        if card is not None:
            board[t] = card
    b, e = tmbattle.board_counts(codes)
    _say("tm:   ...board objects for %s table %s: /B=%d /E=%d (%s) -- rules "
         "st=%d cb=%d ca=%d. %s"
         % (chan, index, b, e,
            ", ".join("tile %d %s" % (t, tmbattle.object_kind(c))
                      for t, c in enumerate(codes) if c) or "none",
            _on("st"), _on("cb"), _on("ca"),
            "%d tile(s) left to play on" % (tiles - b)))
    return codes


def _tile_code(chan, index, tile):
    """The object code this match dealt onto `tile`, or 0. See `_match_objects`."""
    codes = _MATCH_OBJECTS.get((chan, index)) or []
    tile = int(tile)
    return int(codes[tile]) if 0 <= tile < len(codes) else 0


def _chance_after_battle(chan, index, n, actor, target, board):
    """Run a CHANCE BLOCK's effect after a battle resolves against `target`.

    The client does this at scene state 0x11 (`0xD07EA`): it reads the ability
    byte off `[obj+0x17A]` -- the battle TARGET -- and shifts it right by four.
    Nothing between the battle and that shift tests who won, so the effect
    belongs to the battle RESOLVING, not to the attacker winning it.

    WARNING: A code whose effect this server cannot reproduce must never have been
    dealt (`tmbattle.MODELLED_CHANCE_CODES`). If one turns up here anyway it is
    logged loudly and NOT applied, because a half-applied effect is the same
    divergence as none: the client changes its board, we do not, and the next
    placement parks on a battle we will never fight.
    """
    code = _tile_code(chan, index, target)
    if not code or code not in tmbattle.CHANCE_EFFECTS:
        return False
    # WARNING: THE DEAL ARRAY IS NOT THE BOARD. `_MATCH_OBJECTS` records what was
    # DEALT onto each tile and never changes; the board does. A consumed block
    # frees its square, a card gets played there, and keying the effect off the
    # deal array alone re-fires it against that CARD -- and the consumption
    # below then DELETES it. Caught by the whole-match selftest 2026-09-07:
    # "BEFORE 4:('0x9', 1) ... AFTER tile 4 is gone", a card played on a former
    # chance-block tile vanishing on the next battle.
    #
    # So the tile must still be holding the object itself. Pseudo ids are all
    # >= 0x8000 and no real card can reach that (`@PutCard` refuses >= 250).
    _occ = board.get(int(target))
    if _occ is None or int(_occ.row["id"]) < 0x8000:
        return False
    if code in tmbattle.UNMODELLED_CHANCE_CODES:
        _say("tm: WARNING: tile %s is chance block %d (%s) and this server does NOT "
             "model its effect -- the client is about to change its board and "
             "we are not. This code should not have been dealt; expect the "
             "match to park on the next battle."
             % (target, code, tmbattle.chance_effect(code)))
        return False
    # `rand` is the match's own `/R=`; `take_owner` reads index 1 of it, which
    # is what makes a code-8 draw reproducible on both sides with no message.
    # WARNING: READ BEFORE THE EFFECT. A scramble refills the board as it runs, so a
    # candidate count taken afterwards is the LEFTOVER empties, not the list
    # the redeal indexed -- and a log line that reads as a measurement and is
    # not one is how the last two of these went wrong.
    _specials = _unplayed_specials(chan, index)
    # ...and the list a SCRAMBLE indexes is taken after phase 1 has lifted every
    # real card (0xD1974..0xD197C write all three bytes back to 8), so an
    # occupied tile IS a candidate and only the blocks and the unplayed
    # specials are not.
    _cands = [t for t in range(_board_tiles(n))
              if t not in set(_specials)
              and int((board.get(t).row["id"] if board.get(t) else 0)) < 0x8000]
    eff, hit = tmbattle.apply_chance(
        board, code, actor, players=n, tiles=_board_tiles(n), target=target,
        rand=_turn_rand((chan, index)),
        # WARNING: THE SCRAMBLE'S CANDIDATES ARE `card+0x1C`, NOT "no card here".
        # An unplayed special tile is stamped 4 there and is not a candidate;
        # leaving it in is what put card 33 on tile 7 while the client put it
        # on tile 2 (04:20Z). The block being fought is still ON the board at
        # this point -- it is consumed below -- so it is excluded already.
        specials=_specials)
    _watch_effect(chan, index, eff, target, hit)
    if eff == "scramble":
        _say("tm:   ...CHANCE BLOCK on tile %s (code 17): SCRAMBLE -- every "
             "card lifted and redealt from /R=. The FIRST candidate list "
             "(card+0x1C == 8, rebuilt per card at 0xD19DF) was %r; the "
             "unplayed special tile(s) %r are NOT in it, which is the whole "
             "of the 04:20Z fix. %d moved: %r"
             % (target, _cands, _specials, len(hit), hit))
    if eff in ("power_up", "power_down") and hit:
        _say("tm:   ...CHANCE BLOCK on tile %s (code %d): %s -- seat %s's "
             "card(s) on tile(s) %r move by %+d (rec+0x26)"
             % (target, code, eff.upper().replace("_", " "), actor, hit,
                tmbattle.POWER_STEP if eff == "power_up"
                else -tmbattle.POWER_STEP))
    elif eff == "take":
        _who = tmbattle.take_owner(_turn_rand((chan, index))[1], actor, n)
        _say("tm:   ...CHANCE BLOCK on tile %s (code 8): TAKE -- /R= occ 1 = %d "
             "sends every adjacent card to seat %s%s; converted tile(s) %r"
             % (target, _turn_rand((chan, index))[1], _who,
                " (the attacker)" if _who == actor else " (NOT the attacker)",
                hit))
    _scrambled = (eff == "scramble")
    if code in tmbattle.CONSUMED_CHANCE_CODES and int(target) in board:
        # WARNING: THE BLOCK IS CONSUMED, NOT CAPTURED. State 0x1D rebuilds the target
        # as card id 0xFFFF -- the empty-tile constructor -- so the square goes
        # back to being playable and belongs to nobody. Holding it as a captured
        # card is what put THREE PHANTOM TILES in our score on 2026-09-07T01:07Z:
        # we served /S=2|7 while the tester's screen held six cards, and the
        # client dutifully displayed our wrong number.
        del board[int(target)]
        # ...and the deal array forgets it too, the way the client clears the
        # tile's own code (0xC1BCF) and ability (0xD2526). Without this the
        # square keeps claiming to be a block for the rest of the match.
        _codes = _MATCH_OBJECTS.get((chan, index))
        if _codes and 0 <= int(target) < len(_codes):
            _codes[int(target)] = 0
        _say("tm:   ...and tile %s is CONSUMED -- the block is spent, the square "
             "is empty and playable again (0xD2543, card id 0xFFFF)" % (target,))
    elif eff is None:
        _say("tm:   ...chance block on tile %s (code %d) has NO effect -- "
             "nibble 5 lands on state 0x1B, the ordinary post-battle state"
             % (target, code))
    return _scrambled


def _rotating_after_battle(chan, index, n, actor, target, board):
    """Fire a battled ROTATING BLOCK's ray. Returns True if `target` was one.

    WARNING: A TRUE RETURN MEANS THE CALLER MUST NOT CAPTURE THE TILE. The client's
    rotating branch (0xCF6C7 -> state 0x1E) replaces the ordinary post-battle
    path outright, and nothing in states 0x1E..0x22 writes the block's own
    owner. Treating it as an ordinary defender is what desynced the boards and
    hung the match on 2026-09-07T02:37Z.

    What fires instead is one tile's worth of ray -- see
    `tmbattle.apply_rotating`.
    """
    blk = board.get(int(target))
    if not tmbattle.is_rotating(blk):
        return False
    # THE BLOCK HAS TURNED SINCE THE DEAL: one step per turn (measured
    # 2026-09-07T14:23Z). `POL_TM_ROTATING_STEP=0` = the static block
    # that froze that game.
    _turn = int((_MATCH_TURN.get((chan, index)) or {}).get("turn") or 0)
    _adv = _turn * _env_int("POL_TM_ROTATING_STEP", 1)
    hit = tmbattle.apply_rotating(board, _board_tiles(n), int(target), actor,
                                  players=n, advance=_adv)
    _phase = tmbattle.rotating_phase(blk, _adv)
    _aim = (tmbattle.ray_target(_board_tiles(n), int(target), _phase)
            if _phase is not None else None)
    _watch_effect(chan, index, "ray", target, hit, aim=_aim)
    _say("tm:   ...ROTATING BLOCK on tile %s: it is NOT captured -- it fires "
         "one tile %s (deal phase %s + turn %d -> phase %s, opposite its arrow "
         "bit) at tile %s, and converted %r for seat %s"
         % (target, tmbattle.DIR_NAMES[(( _phase or 0) - 4) & 7],
            tmbattle.rotating_phase(blk, 0), _turn, _phase, _aim, hit, actor))
    return True


def _burn_attack_boost(board, tile):
    """An ATTACK-UP special tile is spent on the battle it powered. 0xD30EE.

    KEY: THE THREE SPECIAL TILES ARE NOT SYMMETRIC. At `-BattleEnd` (0xD3104
    prints the string) the client reads the ability of the tile it just played
    on -- `[obj+0x179]`, written from the chosen tile at 0xC6FF9 and the same
    index 0xC703C hands the inherited ability to -- and zeroes it **only when
    it is exactly 1**:

        0xD30EE  mov al, [ebp + tile*156 + 0x368]
        0xD30F5  cmp al, 1
        0xD30FE  jne  0xD3104          ; 2 and 3 survive untouched
        0xD3100  mov byte [ebp+...], 0

    So ability 1 (the ATTACKER multiplier) fires ONCE and is gone, while
    ability 2 (the defender multiplier) and ability 3 (all eight arrows) stay
    on the card for the rest of the match. That asymmetry is the rule, not an
    accident: 1 only ever applies to the card's own attack, so a card that has
    attacked has used it.

    WARNING: The compare is against the WHOLE byte, so a chance block's high-nibble
    ability (0x10..0x50) never matches -- those are consumed by their own
    states (0xD249B / 0xD2526) instead.

    Called on every path out of the battle chain, and only when a battle
    actually happened: a placement that only flips never reaches -BattleEnd.
    """
    card = board.get(int(tile))
    if card is not None and int(card.ability) == 1:
        card.ability = 0
        return True
    return False


def _consume_tile_special(chan, index, tile):
    """`_tile_ability`, and the tile stops being a special the moment it pays.

    WARNING: THE DEAL ARRAY IS NOT THE BOARD -- the same law the chance blocks
    earned, applied to the other object that outlives its own reveal. The
    client keeps NOTHING in its object-code array after the opening animation
    (0xC1BCF / 0xC2E33 zero each entry as it is materialised); the truth lives
    in the tile record, and a placement overwrites it: 0xC7015 lifts the
    ability out, 0xB3200 builds the played card over the top, 0xC703C writes
    the ability onto the CARD and 0xC7098 replaces the special's `+0x1C` = 4
    with the owner. From then on the square is an ordinary occupied tile.

    Holding the code forever made this server answer "unplayed special" about
    a square with a card on it, which the scramble's candidate list reads
    (`_unplayed_specials`) -- and it would have handed the same ability out
    twice if a scramble ever lifted the card off again.
    """
    ability = _tile_ability(chan, index, tile)
    codes = _MATCH_OBJECTS.get((chan, index))
    if codes and 0 <= int(tile) < len(codes)             and int(codes[int(tile)]) in tmbattle.SPECIAL_CODES:
        codes[int(tile)] = 0
    return ability


def _tile_ability(chan, index, tile):
    """The ability a SPECIAL TILE hands the card played on it.

    The client's own placement does this in three instructions: 0xC7015 saves
    `[tile.card+0x24]`, 0xB3200 builds the played card over the top (zeroing
    it like every card constructor), 0xC703C writes it straight back. So the
    tile's ability outlives the placement and belongs to the new card, which
    is what makes a special tile do anything at all.
    """
    codes = _MATCH_OBJECTS.get((chan, index)) or []
    tile = int(tile)
    return tmbattle.special_ability(codes[tile]) if 0 <= tile < len(codes) else 0


#: THE COMBO RULE -- `POL_TM_COMBO`, and the 3-player freeze that pinned it.
#:
#: WARNING: MEASURED 2026-09-06T23:29Z (replayed turn by
#: turn in `_selftest_replay_3p`): seats 18/3/11 froze on turn 13 because the
#: CLIENTS' boards and this server's had diverged at turn 7 -- every client
#: ran `-BattleCheck` -> `-Battle:Select=1` -> `-BattleStart:8` on tile 5 and
#: parked for a `@BattleData` this server never sent, because on THEIR board
#: tile 11 was seat 1's (taken by the turn-8 combo) and on ours it was still
#: seat 0's. The rule the clients play, and the one this knob defaults to:
#:
#:   defeated  (default)  the chain runs off the DEFEATED card. Every card its
#:                        arrows point at that belongs to the LOSER changes
#:                        hands with it -- no battle, one level, whether or not
#:                        it points back (the client's post-battle wave is
#:                        state 0x23, flips only). On a WIN the defeated card
#:                        is the one on the TARGET tile; on a LOSS it is the
#:                        card you played. A third seat's card is never taken.
#:   placed               the pre-2026-09-07 model: re-contest from the PLACED
#:                        tile with its post-battle owner, taking anything not
#:                        the winner's. IDENTICAL to `defeated` in every
#:                        2-player LOSS (the placed card IS the defeated card,
#:                        and "not the winner's" IS the loser's), which is why
#:                        the 2026-08-22 loss measurement could not tell them
#:                        apart -- and WRONG on every WIN (the chain must run
#:                        off the card you TOOK, not the one you played) and in
#:                        every 3-player loss (a third seat's card is not the
#:                        loser's). Kept for an A/B; it reproduces the frozen
#:                        game's own server log to the tile.
#:
#: `POL_TM_LOSS_COMBO=0` still restores the stop-dead loss arm under either.
def _combo_mode():
    return (os.environ.get("POL_TM_COMBO") or "defeated").strip().lower()


def _combo_capture(board, tiles, n, defeated, loser, winner):
    """Chain one battle's outcome off the DEFEATED card. Returns the tiles taken.

    For each arrow of the card on `defeated`, the neighbour it points at is
    captured for `winner` when it is the LOSER's; anyone else's is left alone,
    and a card that points back is taken all the same (a combo is a flip, not
    a fight). One level: a card taken here does not chain further. A loser
    that is not a seat (a BLOCK, owner >= n) has no cards and chains nothing.
    `placed` mode returns [] and leaves the old arms to do their re-contest.
    """
    if _combo_mode() == "placed":
        return []
    took = []
    card = board.get(defeated)
    if card is None or loser is None or winner is None or loser == winner \
            or not 0 <= loser < n:
        return took
    for d in range(8):
        if not (card.arrows >> d) & 1:
            continue
        u = tmbattle.neighbours(tiles, defeated)[d]
        if u is None:
            continue
        occ = board.get(u)
        if occ is None or occ.owner != loser:
            continue
        if not tmbattle.flippable(occ):
            continue                 # the 0xD301B ceiling: a flip wave skips it
        occ.owner = winner
        took.append(u)
    return took


def _apply_placement(chan, index, n, actor, tile, row, rnd=None):
    """Play one card onto the board and resolve everything it touches.

    Returns `(battles, marks)` where `battles` is the ordered list of
    `(attacker tile, defender tile, resolve() result, verdict)` this placement
    owes the clients as `@BattleData`, and `marks` is what the placement
    touched, for the log.

    The order is the client's own, not a convenient one:

      1. 0xCE2B8 runs the arrow engine on the placed tile, flagging each
         reachable enemy 1 (battle) or 2 (free flip).
      2. 0xCE470 (`-BattleCheck`) takes the battles first, one at a time,
         auto-selecting when there is exactly one.
      3. On a win the defender's tile changes hands and 0xD24AA re-runs the
         arrow engine FROM THE PLACED TILE -- so a combo chains off the card
         you played, never off the card you just took.
      4. On a loss the placed card itself changes hands (0xCFAB4) -- **and the
         WINNER then re-contests from the placed tile too**: the arrow engine
         runs again with the tile's NEW owner, capturing the former placer's
         pointed-at cards. That is the game's COMBO, seen from the loss side.
      5. Only when no battles are left do the flips apply (0xCE53D -> state
         0x23), each setting the tile's owner to the current player (0xD2B22).

    VERIFIED: THE LOSS ARM WAS MEASURED LIVE 2026-08-22T21:14Z AND THE OLD "the
    placement is over" MODEL WAS WRONG -- this was THE board divergence
    (every wrong wager/take verdict rode it). Two-sided board dump, 2P game:
    the human placed tile 5 (card 10, arrows 0x7E) into three back-pointing
    enemies, the server fought tile 10 and the placer LOST. The client then
    flipped tile 4 -- the human's OWN other card, reached by the beaten
    card's W arrow -- to the defender (`board3p.log` 17:14:13, tile 4
    client-owner 0 -> 1) while this server left it, and the endgame scored
    5|5 draw here vs 4-6 loss on screen. The static read "0xD25B3 refuses to
    re-scan" was the open-the-function-not-just-the-address trap
    again: the re-scan happens, with the new owner. The unified rule -- ONE
    re-contest from the placed tile with its post-battle owner, win or lose --
    also reproduces every previously-matched game (3P turn 8 win-combo; 3P
    turn 11 loss where the re-contest touched only the winner's own cards, so
    nothing visibly moved and the boards agreed by luck).

    WARNING: Still unmeasured on the loss side: a re-contest mark that POINTS BACK
    (a would-be battle). The client's post-battle wave is state 0x23, "arrow
    flips only", so it is applied as a CAPTURE here like every other combo
    mark -- but it is logged loudly, and the first live game that shows one
    should be read against this choice. `POL_TM_LOSS_COMBO=0` restores the
    old stop-dead arm.

    WARNING: The pending flips of the ORIGINAL marks still die with the loss --
    measured (3P turn 11: abs2's pending flip on tile 6 never applied). Only
    the re-contest from the NEW owner runs.
    """
    board = _MATCH_BOARD.setdefault((chan, index), {})
    tiles = _board_tiles(n)
    rnd = rnd or random
    if not 0 <= int(tile) < tiles:
        # 0xC6FF1 treats 0xFF as "no placement"; anything else off the board is
        # ours to refuse, loudly, rather than index a dict with it.
        _say("tm: WARNING: tile %s is not on a %d-tile board -- nothing placed"
             % (tile, tiles))
        return [], {}
    # WARNING: ...AND THE TILE MUST BE FREE. Until 2026-09-06 nothing on a board
    # could occupy a tile the placer had not chosen, so an occupied tile was
    # a client bug and overwriting it was harmless. BLOCKS CHANGE THAT: they
    # hold tiles from the deal, the client's own cursor will not offer one,
    # and overwriting one would DELETE a block the other client can still see.
    # Refuse, loudly -- a `@PutData` naming an occupied tile is a desync, and
    # the log line is how it gets found.
    if int(tile) in board:
        _occ = board[int(tile)]
        _say("tm: WARNING: tile %s already holds %r (owner %s) -- nothing placed"
             % (tile, _occ, _occ.owner))
        return [], {}
    # Owners BEFORE the placement resolves -- the only way to count what this
    # one card took without threading a counter through every arm below.
    _before = {t: c.owner for t, c in board.items()}
    # ...INHERITING THE TILE'S ABILITY, which is only ever non-zero on a
    # SPECIAL TILE (0xC7015/0xC703C -- see `_tile_ability`). Every `resolve`
    # below already threads `att_ability`/`dfn_ability`; this is the first
    # thing that has ever made one of them non-zero.
    board[int(tile)] = tmbattle.Card(
        row, actor, ability=_consume_tile_special(chan, index, tile))
    battles, guard = [], 0
    marks = tmbattle.contest(board, tiles, int(tile), actor, players=n)
    ties = _env_int("POL_TM_BATTLE_TIES", 0)
    while guard < 64:
        guard += 1
        target = tmbattle.next_defender(marks)
        if target is None:
            break
        att, dfn = board[int(tile)], board[target]
        res = tmbattle.resolve(att.row, dfn.row, rnd,
                               att_ability=att.ability, dfn_ability=dfn.ability,
                               att_mod=att.modifier, dfn_mod=dfn.modifier)
        verdict = tmbattle.winner(res)
        if verdict == tmbattle.DRAW and not ties:
            # WARNING: A DRAW IS LEGAL AND THE CLIENT HANDLES IT -- by fighting the
            # SAME battle again (0xCFD63 -> state 0xC -> state 7) and asking
            # for a SECOND `@BattleData` on the same (0x43, 12). We re-roll
            # here instead, and the reason is delivery, not squeamishness:
            # `_queue_push` de-duplicates identical bodies and a second message
            # in one slot has never been watched being drained, so shipping a
            # draw is shipping a hang we cannot see. `POL_TM_BATTLE_TIES=1`
            # sends it anyway, which is how that gets measured.
            continue
        battles.append((int(tile), target, res, verdict))
        _watch_battle(chan, index, int(tile), target, res, verdict)
        # The ray fires on the battle RESOLVING, like the chance effects, and
        # a true return means the block is NOT captured below.
        _rot = _rotating_after_battle(chan, index, n, actor, target, board)
        if _chance_after_battle(chan, index, n, actor, target, board):
            # WARNING: A SCRAMBLE ENDS THE PLACEMENT. Every card has just been lifted
            # and redealt, so `tile` and every entry in `marks` are stale
            # indices -- continuing to resolve from them read a tile that no
            # longer held the placed card and raised KeyError. The client's own
            # flow agrees: the effect runs at the END of the battle chain and
            # returns to the idle state, not to more resolution.
            if battles:
                _burn_attack_boost(board, tile)
            _board_map_log(board, tiles, tile, row, battles)
            _note_combo(chan, index, actor, board, _before, tile)
            return battles, {}
        if _rot:
            # WARNING: NEITHER ARM APPLIES TO A ROTATING BLOCK. It is not captured
            # and the placed card is not lost -- states 0x1E..0x22 write no
            # owner at all. Written as `verdict == ATTACKER and not _rot` this
            # fell into the `else`, which is the LOSS path, and turned the
            # player's own WINNING card into a block (owner 4) live on
            # 2026-09-07T03:19Z: "board map after tile 10 ... 10:4", score 0-0.
            #
            # WARNING: AND THE TARGET MUST BE DROPPED. `contest` is stateless, and a
            # rotating block is the one object that survives its battle still
            # NEUTRAL and still carrying an arrow -- so it would be re-selected
            # every pass until the 64-step guard. The client stops this with
            # its per-tile mark; we do it by taking the target off the list.
            marks.pop(target, None)
        elif verdict == tmbattle.ATTACKER:
            # WARNING: A CONSUMED CHANCE BLOCK LEAVES NOTHING TO TAKE. Its tile was
            # emptied above, so there is no card to change hands and none for
            # the win-side combo to chain off. The re-contest below still runs.
            if target in board:
                _loser = board[target].owner
                board[target].owner = actor
                # THE WIN-SIDE COMBO: off the card just TAKEN (`POL_TM_COMBO`).
                for _ct in _combo_capture(board, tiles, n, target, _loser,
                                          actor):
                    _say("tm:   ...combo: tile %d (seat %s's, reached by the "
                         "beaten card on tile %d) taken by seat %s"
                         % (_ct, _loser, target, actor))
            marks = tmbattle.contest(board, tiles, int(tile), actor, players=n)
        else:
            # The placed card is taken. 0xCFAB4 writes the DEFENDER'S owner
            # onto the attacker's tile -- and the WINNER combos through it:
            # the same re-contest the win arm runs, from the placed tile,
            # with its new owner (measured 2026-08-22T21:14Z, tile 4; see the
            # docstring). Marks that point back are captured too, flips-only
            # (state 0x23), logged loudly because that half is unmeasured.
            board[int(tile)].owner = dfn.owner
            if dfn.owner >= n:
                # WARNING: A BLOCK TOOK THE CARD, AND A BLOCK DOES NOT PLAY. 0xCFAB4
                # still writes the winner's owner onto the placed tile, so the
                # card really is lost to owner 4 -- but the re-contest is the
                # WINNER'S move, and inventing a block-driven combo would be
                # inventing a rule. Skipping it is the conservative reading.
                _say("tm:   ...tile %s was taken by a BLOCK (owner %s) -- no "
                     "loss combo; a block has no turn" % (tile, dfn.owner))
            elif _env_int("POL_TM_LOSS_COMBO", 1):
                if _combo_mode() == "placed":
                    for t, kind in sorted(tmbattle.contest(
                            board, tiles, int(tile), dfn.owner,
                            players=n).items()):
                        if kind == tmbattle.BATTLE:
                            _say("tm: WARNING: loss-combo capture of a BACK-POINTING "
                                 "card on tile %d -- applied as a flip; if the "
                                 "client fought a battle here instead, this is "
                                 "the divergence" % t)
                        board[t].owner = dfn.owner
                        _say("tm:   ...loss combo: tile %d captured by seat %s "
                             "through the beaten card" % (t, dfn.owner))
                else:
                    # Off the beaten card, the LOSER's cards only.
                    for _ct in _combo_capture(board, tiles, n, int(tile),
                                              actor, dfn.owner):
                        _say("tm:   ...loss combo: tile %d captured by seat %s "
                             "through the beaten card" % (_ct, dfn.owner))
            if battles:
                _burn_attack_boost(board, tile)
            _board_map_log(board, tiles, tile, row, battles)
            _note_combo(chan, index, actor, board, _before, tile)
            return battles, marks
    for t, kind in sorted(marks.items()):
        # 0xD301B: the flip loop SKIPS a tile whose card id is >= 0x8009, so a
        # captured rotating block or high chance block can be taken in battle
        # but never flipped. See `tmbattle.FLIP_ID_CEILING`.
        if kind == tmbattle.FLIP and tmbattle.flippable(board[t]):
            board[t].owner = actor
    if battles:
        _burn_attack_boost(board, tile)
    _board_map_log(board, tiles, tile, row, battles)
    _note_combo(chan, index, actor, board, _before, tile)
    return battles, marks


#: (chan, index) -> a placement mid-resolution, PARKED waiting for the player's
#: `@BattleSelect=/B=<tile>`. The board (`_MATCH_BOARD`) already holds the placed
#: card; this holds what the resolver needs to resume and to finish the turn.
#:
#: WARNING: THE MULTI-DEFENDER PATH, measured live 2026-09-03 (the first time it ever
#: fired -- and it HUNG, because the server auto-resolved every battle at
#: `@PutData` time and never handled `@BattleSelect`). The client scene 0xCE270
#: is a state machine: -BattleCheck (state 2, 0xCE453) counts battle-eligible
#: defenders and branches -- 0 -> flips only, 1 -> auto-select (NO
#: `@BattleSelect`), >1 -> the interactive picker (state 3) which sends ONE
#: `@BattleSelect=/B=<tile>` (0x43, cmd 11) then parks in state 8 polling (0x43,
#: 12) @BattleData. Combos are CLIENT-DRIVEN: after each @BattleData the client
#: re-contests from the placed tile and loops back to -BattleCheck, so a fresh
#: >1 sends another `@BattleSelect`. `/B=` is the ABSOLUTE tile index, unrotated.
_PENDING_BATTLE = {}


def _battle_push(chan, index, turn, att_tile, def_tile, res, verdict, roster):
    """Push one resolved battle's `@BattleData` to every player and watcher.

    Extracted from the `@PutData` path so the resumable resolver pushes each
    battle the same way -- held back one reply and framed from each
    recipient's own match peer."""
    _watch_battle(chan, index, att_tile, def_tile, res, verdict)
    _rnd8 = _turn_rand((chan, index))
    _body = _battledata_body(res, att_tile, def_tile, _rnd8)
    _watch_push(chan, index, _turn_code(BATTLEDATA_CMD, turn) + _body,
                "the battle on tile %d" % def_tile,
                after=_env_int("POL_TM_BATTLE_GAP", 1))
    for mid, _v in roster:
        if _is_bot(chan, index, mid):
            continue
        _queue_push(mid, _turn_code(BATTLEDATA_CMD, turn) + _body,
                    "the battle on tile %d" % def_tile,
                    after=_env_int("POL_TM_BATTLE_GAP", 1),
                    peer=_MATCH_PEER.get(_push_key(mid)))
    _say("tm:   ...battle: tile %d (%d/%d) vs tile %d (%d/%d) -- %s takes it. "
         "(0x43, %d) @BattleData=, turn %d. Watch for '---->Recv=BATTLESET' then "
         "'-BattleSet:OK!'."
         % (att_tile, res["a_roll"], res["a_raw"], def_tile,
            res["d_roll"], res["d_raw"],
            {tmbattle.ATTACKER: "the ATTACKER", tmbattle.DEFENDER: "the DEFENDER",
             tmbattle.DRAW: "NOBODY (a draw)"}[verdict], BATTLEDATA_CMD, turn))


def _begin_placement(chan, index, n, actor, tile, row, turn, seats, com_n,
                     member_id, roster, rnd=None):
    """Put the card on the board and arm the resumable battle resolver.

    Returns True if a placement is now pending (call `_advance_placement`), or
    False if the tile was off the board (nothing placed)."""
    board = _MATCH_BOARD.setdefault((chan, index), {})
    tiles = _board_tiles(n)
    if not 0 <= int(tile) < tiles:
        _say("tm: WARNING: tile %s is not on a %d-tile board -- nothing placed"
             % (tile, n))
        return False
    if int(tile) in board:
        # The same free-tile guard as `_apply_placement` -- see the banner
        # there. A block holds its tile from the deal.
        _occ = board[int(tile)]
        _say("tm: WARNING: tile %s already holds %r (owner %s) -- nothing placed"
             % (tile, _occ, _occ.owner))
        return False
    _before = {t: c.owner for t, c in board.items()}
    # The same special-tile inheritance as `_apply_placement`; the resumable
    # resolver must not be the path where a special tile quietly does nothing.
    board[int(tile)] = tmbattle.Card(
        row, actor, ability=_consume_tile_special(chan, index, tile))
    _PENDING_BATTLE[(chan, index)] = {
        "n": n, "actor": actor, "tile": int(tile), "turn": turn,
        "seats": seats, "com_n": com_n, "member_id": member_id,
        "roster": roster, "before": _before, "row": row, "rnd": rnd or random,
    }
    return True


def _advance_placement(chan, index, chosen=None):
    """Resolve the pending placement, pausing at each multi-target choice.

    Mirrors the client scene 0xCE270 exactly: -BattleCheck counts battle-
    eligible defenders of the placed tile; 0 -> apply flips and finish the turn,
    1 -> auto-resolve, >1 -> park for the player's `@BattleSelect` (return
    without finishing). `chosen` is the `/B=` tile from an arriving
    `@BattleSelect`; it resolves that one battle and continues the loop.

    Returns True if the placement fully resolved (turn advanced), False if it
    parked awaiting a `@BattleSelect`."""
    st = _PENDING_BATTLE.get((chan, index))
    if st is None:
        return True
    n, actor, tile, turn = st["n"], st["actor"], st["tile"], st["turn"]
    seats, com_n, member_id = st["seats"], st["com_n"], st["member_id"]
    roster, before, row, rnd = (st["roster"], st["before"], st["row"], st["rnd"])
    board = _MATCH_BOARD.setdefault((chan, index), {})
    tiles = _board_tiles(n)
    ties = _env_int("POL_TM_BATTLE_TIES", 0)
    # Rotating blocks already fought in THIS placement. They survive their
    # battle still neutral and still carrying an arrow, so `contest` -- which
    # is stateless -- would offer the same one every pass.
    _fought_rot = st.setdefault("fought_rot", set())

    def _finish():
        # 0xD30EE, the same -BattleEnd burn `_apply_placement` does. This
        # resolver is only ever entered with a battle already pending, so every
        # path through here has fought one.
        _burn_attack_boost(board, tile)
        _board_map_log(board, tiles, tile, row, [])
        _note_combo(chan, index, actor, board, before, tile)
        _PENDING_BATTLE.pop((chan, index), None)
        # THE WATCHER'S LIVE BOARD. The played card is the one new face this
        # placement puts down; push it as @WFCard (the only channel the watch
        # scene reads). Flips change OWNER, which @WFCard cannot carry, so they
        # do not get a message here -- a known limitation until the owner
        # channel is found. See `_watch_wfcard`.
        _watch_wfcard(chan, index, turn, [tile], board)
        _sc = _board_scores(chan, index, n)
        _say("tm:   ...board after turn %d: %s"
             % (turn, " / ".join("seat %d: %d tile(s)" % (i, v)
                                 for i, v in enumerate(_sc))))
        if _env_int("POL_TM_TURNDATA", 1):
            if com_n:
                _queue_com_turns(chan, index, member_id, n, turn + 1,
                                 (actor + 1) % n)
            else:
                _queue_next_turn(chan, index, seats, turn + 1, (actor + 1) % n,
                                 roster=roster, n_solo=com_n)

    guard = 0
    while guard < 64:
        guard += 1
        owner = board[tile].owner
        marks = tmbattle.contest(board, tiles, tile, owner, players=n)
        battles = sorted(t for t, k in marks.items()
                         if k == tmbattle.BATTLE and t not in _fought_rot)
        if chosen is not None:
            if int(chosen) in battles:
                target = int(chosen)
            else:
                _say("tm: WARNING: @BattleSelect=/B=%s is not a battle-eligible tile "
                     "(options %r) -- auto-selecting the client's own default"
                     % (chosen, battles))
                target = battles[-1] if battles else None
            chosen = None
        elif not battles:
            target = None
        elif len(battles) == 1:
            target = battles[0]
        elif st.get("bot"):
            # A departed seat has no picker to park for: take the client's
            # own default (the last eligible tile, as the fallback above).
            target = battles[-1]
            _say("tm:   ...tile %d can attack %d defenders %r -- departed seat, "
                 "auto-selecting tile %d" % (tile, len(battles), battles, target))
        else:
            # >1: the client is in its picker (state 3); park until its
            # @BattleSelect names a target. The board and this record stand.
            _say("tm:   ...tile %d can attack %d defenders %r -- holding for the "
                 "player's @BattleSelect (0x43, 11, /B=). The single-target and "
                 "0-target paths never send it; only this one does."
                 % (tile, len(battles), battles))
            return False
        if target is None:
            # No battles remain: apply the free flips and end the placement.
            for t, kind in sorted(marks.items()):
                # The same 0xD301B ceiling as `_apply_placement`.
                if kind == tmbattle.FLIP and tmbattle.flippable(board[t]):
                    board[t].owner = owner
            _finish()
            return True
        # Resolve `target`, re-rolling a draw (the client fights it again and
        # asks for a second @BattleData; we re-roll rather than ship a hang).
        while True:
            att, dfn = board[tile], board[target]
            res = tmbattle.resolve(att.row, dfn.row, rnd,
                                   att_ability=att.ability, dfn_ability=dfn.ability,
                                   att_mod=att.modifier, dfn_mod=dfn.modifier)
            verdict = tmbattle.winner(res)
            if verdict == tmbattle.DRAW and not ties:
                continue
            break
        _battle_push(chan, index, turn, tile, target, res, verdict, roster)
        _rot = _rotating_after_battle(chan, index, n, owner, target, board)
        if _chance_after_battle(chan, index, n, owner, target, board):
            # The same rule as `_apply_placement`: a scramble ends it.
            _finish()
            return True
        if _rot:
            # The same two rules as `_apply_placement`: neither arm applies to
            # a rotating block, and it must not be fought again. Here `marks`
            # is recomputed from the board every pass, so dropping it from a
            # dict would not stick -- the set on the pending record does.
            _fought_rot.add(target)
            continue
        if verdict == tmbattle.ATTACKER:
            # WARNING: The same consumed-block guard as `_apply_placement`.
            if target in board:
                _loser = board[target].owner
                board[target].owner = owner
                # THE WIN-SIDE COMBO: off the card just TAKEN (`POL_TM_COMBO`).
                for _ct in _combo_capture(board, tiles, n, target, _loser,
                                          owner):
                    _say("tm:   ...combo: tile %d (seat %s's, reached by the "
                         "beaten card on tile %d) taken by seat %s"
                         % (_ct, _loser, target, owner))
            continue                      # re-contest from the placed tile
        # A loss: the placed card is taken, and the winner combos through it
        # (the same loss-side rule `_apply_placement` measured 2026-08-22T21:14Z
        # -- flips only, back-pointers logged loudly). The original marks' flips
        # die with the loss. This ends the placement.
        board[tile].owner = dfn.owner
        if dfn.owner >= n:
            # A block won it -- see the same guard in `_apply_placement`.
            _say("tm:   ...tile %s was taken by a BLOCK (owner %s) -- no loss "
                 "combo; a block has no turn" % (tile, dfn.owner))
        elif _env_int("POL_TM_LOSS_COMBO", 1):
            if _combo_mode() == "placed":
                for t, kind in sorted(tmbattle.contest(
                        board, tiles, tile, dfn.owner, players=n).items()):
                    if kind == tmbattle.BATTLE:
                        _say("tm: WARNING: loss-combo capture of a BACK-POINTING card "
                             "on tile %d -- applied as a flip; if the client "
                             "fought a battle here instead, this is the "
                             "divergence" % t)
                    board[t].owner = dfn.owner
                    _say("tm:   ...loss combo: tile %d captured by seat %s "
                         "through the beaten card" % (t, dfn.owner))
            else:
                for _ct in _combo_capture(board, tiles, n, tile, owner,
                                          dfn.owner):
                    _say("tm:   ...loss combo: tile %d captured by seat %s "
                         "through the beaten card" % (_ct, dfn.owner))
        _finish()
        return True
    _say("tm: WARNING: battle resolver hit its 64-step guard at %s table %s -- "
         "finishing the placement to avoid a hang" % (chan, index))
    _finish()
    return True


#: (chan, index) -> {actor: the largest chain that seat has taken this match}.
#: Cleared with the rest of the match state; see `_remember_match`.
_MATCH_COMBO = {}


def _note_combo(chan, index, actor, board, before, placed):
    """Record the size of the chain one placement just took.

    WARNING: **THIS IS AN INFERENCE, AND IT IS WHY THE WRITE IS OFF BY DEFAULT.**
    An earlier reading identified the save field (`+0x7C` -> struct `+0xAC`, a career MAX kept
    at `0xD029B` over a per-seat byte at `48*seat + struct+0x19C`), and
    `0xD003E` shows what that byte MEANS in outline -- the client saves it,
    calls the battle routine `0xD3590`, re-reads it, and fires only when it
    GREW, which is the shape of "did this action take anything". What was never
    read is the exact unit: whether the client counts battles won, cards
    flipped, or both, and whether the placed card itself is in the total.

    So this counts **cards whose owner became `actor`, excluding the placed
    card** -- the most defensible reading -- and `POL_TM_MOST_COMBOS` gates the
    save write, defaulting to **0**. The tally accumulates either way, so the
    day the unit is measured the history is already there and only the gate
    moves. Putting a plausible wrong number on the Status screen is the failure
    this project keeps paying for; keeping an unused correct-shaped tally costs
    nothing.
    """
    # every placement path ends here, with the owners from before it: the web
    # watch file's one step per move
    _watch_move(chan, index, actor, board, before, placed)
    try:
        taken = sum(1 for t, c in board.items()
                    if t != int(placed) and t in before
                    and before[t] != actor and c.owner == actor)
        if taken <= 0:
            return
        seat = _MATCH_COMBO.setdefault((chan, index), {})
        if taken > int(seat.get(actor) or 0):
            seat[actor] = taken
    except Exception:
        pass                     # a stat must never cost a placement


def _board_map_log(board, tiles, placed, row, battles):
    """One line per placement: the full owner map, for diffing against the
    client's board when verdicts disagree.

    2026-08-22 18:36Z: the server called a 6|4-looking endgame 5|5 (draw,
    stake returned, no take flow -> the client parked on (0x43,35)) while the
    client showed a LOSS -- its `-BattleCheck` found ZERO battles on the
    final placement where we found one, so the boards had already diverged
    on some earlier flip. A per-placement owner map on both sides is the
    instrument that names the first divergent placement and therefore the
    rule that is wrong. POL_TM_BOARD_LOG=0 silences it.
    """
    if not _env_int("POL_TM_BOARD_LOG", 1):
        return
    _say("tm:   ...board map after tile %s (card %s, arrows %s%s): %s"
         % (placed, row.split(b"|")[0].decode(),
            row.split(b"|")[6].decode() if row.count(b"|") >= 6 else "?",
            (", %d battle(s)" % len(battles)) if battles else "",
            " ".join("%d:%s" % (t, board[t].owner)
                     for t in sorted(board))))


def _board_scores(chan, index, n):
    """Tiles held per absolute seat. 0xD23E5's own count, and THE SCORE.

    The client tallies exactly this at the end of a match: walk every tile,
    read its owner from rec+0x1D, and increment `[0x2B6580 + 48*owner]`. With
    a board model the server can finally answer "who won" without a new
    message -- which is what the battle plan asked for.

    WARNING: It does NOT yet decide what a win PAYS. `@ResultData`'s per-player
    sections now carry this count as the per-game score (`_bump_result_stats`
    accumulates it -- the sections became MANDATORY when `/P=0` turned out to
    be the result-screen divide-by-zero, see `_resultdata_body`), but the
    transfer-or-faucet question the take flow raised is still open; a tile count is
    not a prize.
    """
    board = _MATCH_BOARD.get((chan, index)) or {}
    out = [0] * (n or 2)
    for card in board.values():
        if card.owner is not None and 0 <= card.owner < len(out):
            out[card.owner] += 1
    return out


def _board_takes(chan, index, n):
    """Per seat, the card ids that seat TOOK -- owns now, did not play.

    `Card.placer` is set once when the card is put down and never moves;
    `Card.owner` moves every time a battle is lost. So `owner != placer` is
    precisely the client's own idea of a card changing hands, and reading it
    off the finished board needs no new message.

    WARNING: This is NOT a transfer. Nothing here puts a card in anybody's collection
    -- what a win PAYS is still the open question the take flow raised, and a tile is
    not a card until that is answered. It is a TALLY, and the only thing that
    consumes it is the lucky-card counter, which counts takes by the client's
    own statistic ("Lucky Cards Won This Week", Playdat.BIN 83).
    """
    board = _MATCH_BOARD.get((chan, index)) or {}
    out = [[] for _ in range(n or 2)]
    for card in board.values():
        if card.owner is None or not 0 <= card.owner < len(out):
            continue
        if card.placer == tmbattle.OWNER_BLOCK:
            # A CAPTURED BLOCK IS A TILE, NOT A CARD. Its `placer` is 4 and
            # its `owner` is now a seat, so the `owner != placer` test below
            # would read it as a take and feed a pseudo id (0x8000+) into the
            # lucky-card counter.
            continue
        if card.owner != card.placer:
            out[card.owner].append(card.row["id"])
    return out


def _tally_lucky(chan, index, seats, n=None):
    """Credit every seat's takes against this week's lucky cards.

    Called once, at the end of the match, from the same place `@ResultData` is
    queued -- which is the only moment the board is final and still in memory
    (`_MATCH_BOARD` is popped when the table breaks up). `n` is the real
    player count when `seats` is the VS. COM roster (one human on an N-seat
    board).
    """
    if tmprize is None or not _env_int("POL_TM_LUCKY", 1):
        return
    takes = _board_takes(chan, index, n or len(seats) or 2)
    lucky = tmprize.pick_lucky()
    _say("tm:   ...this week's lucky cards: %s"
         % ", ".join("#%d %s" % (c, tm_cardprm.name(c)) for c in lucky))
    for seat, (mid, _v) in enumerate(seats):
        if seat < len(takes) and takes[seat]:
            tmprize.record_takes(mid, takes[seat], say=_say)


RESULTDATA_CMD = 13             #: code 0x43 command 13 -- `@ResultData`, arm 0x104141
TCLIST_CMD = 26                 #: code 0x43 command 26 -- `@TCList`, arm 0x104648
GETAWAY_CMD = 35                #: code 0x43 command 35 -- `@GetAway`, arm 0x104FF6
GETSELECT_CMD = 15              #: code 0x43 command 15 -- `@GetSelect`, arm 0x104E5B
WATCHINFO_CMD = 37              #: code 0x43 command 37 -- `@WatchInfo`, arm 0x101B15
WFCARD_CMD = 38                 #: code 0x43 command 38 -- `@WFCard`, arm 0x1020F4
CHANGE_CMD = 36                 #: code 0x43 command 36 -- `@Change`, arm 0x1050A2

#: (chan, index) -> {push_key: (member_id, table peer nick)} -- THE WATCHERS.
#: An observer JOINs the table's own channel and sends `@Data=/Watch=` on
#: (0x43, 37) (measured live 2026-08-22T20:07Z);
#: after the @WatchInfo snapshot it consumes the
#: same in-match pushes the players get, pinned to the table peer it @Pongs.
_MATCH_WATCHERS = {}

#: (chan, index) -> {push_key: (member_id, amount actually staked)} -- the
#: PvP wager ESCROW, filled at the accept quorum, settled at the result,
#: refunded by @GameExit/_stake_recover when a game never finishes. Its own
#: dict because the deal REASSIGNS `_MATCH_TURN[(chan, index)]`.
_PVP_STAKES = {}

#: (chan, index) -> the pot the table's NEXT game is played for. Seeded from
#: the table's `bm` rule and ESCALATED by Double Up -- see `_double_up_pot`.
_TABLE_POT = {}


def _wager_cap():
    """The hard ceiling on a pot. 0x10BF69 clamps to 0x7D0 and so does the
    `@ComGame=` builder (0x106F87), which is where `POL_TM_WAGER_CAP` came
    from; Double Up is the only thing that can climb to it from below."""
    return _env_int("POL_TM_WAGER_CAP", 2000)


def _table_pot(chan, index, fields=None, reset=False):
    """The pot this table's next game is played for, clamped to the cap.

    Seeded from the table's own `bm` rule the first time it is asked for, then
    OWNED HERE, because Double Up moves it between games and nothing on the
    wire reports the new figure back (`@Continue=` has no pot field).
    """
    key = (chan, index)
    if reset:
        _TABLE_POT.pop(key, None)
    if key in _TABLE_POT:
        return _TABLE_POT[key]
    if fields is None:
        fields = _table_rules_get(chan, index) or {}
    pot = max(0, min(int((fields or {}).get("bm", TET_DEFAULTS["bm"]) or 0),
                     _wager_cap()))
    _TABLE_POT[key] = pot
    return pot


def _double_up_pot(chan, index, seats):
    """Escalate the pot for a REMATCH when the table's `du` rule is on.

    KEY: DOUBLE UP IS A POST-GAME STAKE ESCALATOR, NOT A PRE-GAME MULTIPLIER,
    which is the answer to "why not just wager double". Both `du` read sites
    (0x10BC4B and 0x10BF5C, two LAYOUTS of one panel) live inside `0x10B780`
    -- the **"Play again?" panel constructor**, `Playmes.BIN` 93, whose three
    callers are take-scene states 0x35/0x41/0x45 and whose accessors are all
    index 0x11 = `@Continue`. The transitions into 0x41 print their own names,
    `----GameDrawOver----` (0xBE713) and `----GameTradeOver----` (0xBEA5B), so
    the panel is reached AFTER the draw and AFTER the card trade.

    What it does there, exactly:

        0x10BF5C   if du == 1:  pot += pot        [0x52464BC] = pot * 2
                   if pot > 0x7D0: pot = 2000     the same cap we clamp to
        0x10CF91   eax = [0x52464A8]              the wallet
        0x10D9C9   if wallet < pot: du = 0        the rule switches ITSELF off
        0x10D191   ...and raises the "can't afford it" dialog on that compare

    KEY: THE 2000 CLAMP IS THE POINT OF THE RULE. The pot is hard-capped at 2000
    everywhere, so doubling is the only way to climb to the ceiling from a
    table set below it -- and it does nothing at all for a wager above 1000.

    WARNING: THE CLIENT'S CHECK IS AGAINST ITS OWN WALLET, so two players with
    different balances could disagree about whether the rule is still on. This
    server is the authority, and it takes the strict reading: the pot doubles
    only when EVERY seat can cover the doubled stake. Anything else would
    stake a player into an overdraft the client already refused to offer them.
    """
    key = (chan, index)
    fields = _table_rules_get(chan, index) or {}
    pot = _table_pot(chan, index, fields)
    if not _env_int("POL_TM_WAGER_DOUBLEUP", 1):
        return pot
    if not int(fields.get("du", TET_DEFAULTS["du"]) or 0):
        return pot
    if pot <= 0:
        _say("tm:   ...Double Up is on but the table's pot is 0 -- nothing to "
             "double (0x10BF5C doubles the pot, it does not invent one)")
        return pot
    want = min(pot * 2, _wager_cap())
    if want == pot:
        _say("tm:   ...Double Up: the pot is already at the %d cap (0x10BF69) "
             "-- a rematch is played for the same %d" % (_wager_cap(), pot))
        return pot
    short = [(m, money_of(m)) for m, _v in seats if money_of(m) < want]
    if short:
        # 0x10D9C9's own arm, applied to the table rather than to one wallet.
        _say("tm:   ...Double Up would take the pot %d -> %d, but %s cannot "
             "cover it -- the rule switches ITSELF off on that compare "
             "(0x10D9C9) and the rematch is played for %d"
             % (pot, want, "; ".join("member %s holds only %d" % (m, b)
                                     for m, b in short), pot))
        return pot
    _TABLE_POT[key] = want
    _say("tm:   ...DOUBLE UP (rule du=1): the rematch pot doubles %d -> %d "
         "(cap %d, 0x10BF5C) -- the client's own panel has already done this "
         "arithmetic and nothing on the wire reports it back, so this is the "
         "half that has to agree" % (pot, want, _wager_cap()))
    return want


def _stake_pvp_pot(chan, index, seats, pot, why):
    """Take `pot` off every seat into the table's escrow. Returns the escrow.

    One place, because the accept quorum and the rematch must stake the same
    way or a rematch's refund path (`@GameExit` / `_stake_recover`) would not
    recognise what it is refunding.
    """
    if pot <= 0:
        return None
    stakes = {}
    for m2, _v2 in seats:
        put_in = min(pot, money_of(m2))
        if put_in <= 0:
            continue
        _set_money(m2, money_of(m2) - put_in,
                   "PvP stake in (table %s %s %d)" % (index, why, pot))
        _stake_persist(m2, put_in)
        stakes[_push_key(m2)] = (m2, put_in)
    if stakes:
        _PVP_STAKES[(chan, index)] = stakes
    return stakes or None


def _watchers_of(chan, index):
    return list((_MATCH_WATCHERS.get((chan, index)) or {}).values())


#: WARNING: RETRACTED 2026-09-03, SAME DAY: the watcher DOES read cmd 9/10/12 -- but
#: only AFTER it has entered the play scene. The CLIENT TRACE (shim-CASPC,
#: `Recv=WATCHCARD` -> `GamePlayInit`, and the player trace `Recv=STARTDATA` ->
#: `GamePlayInit` -> `Recv=TURNINFO` -> `Recv=PUTCARD` each turn) settles what
#: the arm scan got wrong: `@WFCard` (cmd 38) is what MOVES the watcher into the
#: play scene (its `GamePlayInit`), and once there it renders the board from the
#: SAME `@TurnData`/`@PutCard`/`@BattleData` a player reads. Dropping them left
#: the observer in an inited-but-empty play scene with nothing to draw. So the
#: watcher now gets @WFCard (the GamePlayInit trigger) AND the cmd 9/10/12 stream
#: (the render). `POL_TM_WATCH_DROP_STREAM=1` restores the drop for an A/B; a
#: brief pre-GamePlayInit pile is the tradeoff, and @WFCard fires on turn 0 so it
#: is small.
_WATCHER_DEAF_CMDS = frozenset((TURNDATA_CMD, PUTCARD_CMD, BATTLEDATA_CMD))


#: The per-turn stream a `@WatchInfo` SNAPSHOT supersedes. Every one of these
#: carries state the snapshot itself re-states -- the board, the turn, whose
#: move it is, the hands -- so a copy still sitting in the queue when the
#: snapshot goes out is BY CONSTRUCTION older than what the client is about to
#: be told. A queued `@Quit`/`@GameML`/table push is not per-turn state and is
#: NOT in here: the purge must not become "empty the queue".
_WATCH_SNAPSHOT_SUPERSEDES = frozenset((
    STARTDATA_CMD, TURNDATA_CMD, PUTCARD_CMD, BATTLEDATA_CMD,
    RESULTDATA_CMD, WFCARD_CMD))


def _watch_stream_purge(member_id, why=""):
    """Drop a watcher's QUEUED per-turn stream. Returns how many went.

    WARNING: A SNAPSHOT DOES NOT CATCH THE QUEUE UP -- IT ONLY OUTRUNS IT (fixed
    2026-09-09, live report "invalid card data detected" while watching a COM
    game). `@WatchInfo` sh=1 hands the observer the board AS OF turn T and the
    client starts drawing from there. Nothing dropped what was already queued
    for turns BEFORE T -- and after a rematch that is an entire PREVIOUS match,
    because a watcher stays registered in `_MATCH_WATCHERS` across the game
    boundary while `_PUSHES` keeps filling.

    Measured, member 3 watching table 1:

        04:07:27.745..746  TWENTY-ONE bodies in one millisecond -- the previous
                           game's turns 3..10, its @ResultData, and then the NEW
                           game's turn-0 @TurnData
        04:07:33.789       the client handshakes; we answer a snapshot at /T=1
        04:07:35.793       turn 0's @BattleData + @PutCard arrive BEHIND it
        04:07:39.796       turn 1's @TurnData

    The client filed turn 0's `@PutCard` into msgid slot 0x0a and never consumed
    it -- its scene was already past turn 0 -- then read turn 1's `@TurnData`,
    advanced (`GamePlayLink:Turn=1`), polled its put slot and found that stale
    card:

        Errot!!Turn1!=0  ->  (0x43, 40) @DataError, fixed text
                             "Invalid card data detected. Exiting Tetra Master."

    KEY: THE GAP CANNOT FIX THIS AND NEVER COULD. `POL_TM_WATCH_PUT_GAP` orders a
    render against the @TurnData of its OWN turn; it says nothing about a body
    from a turn the client has already been told it is past. And once a watcher
    falls behind, `_idle_pushes_locked` hands over EVERY entry at counter <= 0 in
    one batch (the 21 above), so `after=N` stops separating anything at all.
    The snapshot has to CLEAR what it supersedes rather than race it.

    `POL_TM_WATCH_SNAPSHOT_PURGE=0` restores the racing behaviour for an A/B.
    """
    if _env_int("POL_TM_WATCH_SNAPSHOT_PURGE", 1) != 1:
        return 0
    key = _push_key(member_id)
    if key is None:
        return 0
    dropped = []
    with _PUSH_LOCK:
        q = _PUSHES.get(key)
        if not q:
            return 0
        keep = []
        for entry in q:
            try:
                cmd = (int(entry[1][:8], 16) >> 8) & 0xFF
            except (ValueError, TypeError, IndexError):
                cmd = -1
            if cmd in _WATCH_SNAPSHOT_SUPERSEDES:
                dropped.append(entry[1])
            else:
                keep.append(entry)
        if keep:
            _PUSHES[key] = keep
        else:
            _PUSHES.pop(key, None)
    if dropped:
        _say("tm: 🔭 cleared %d queued per-turn push(es) for member %s -- the "
             "@WatchInfo snapshot supersedes them%s"
             % (len(dropped), member_id, (" (%s)" % why) if why else ""))
        for body in dropped[:24]:
            _say("tm:   ...superseded: %s" % describe_line(body))
        if len(dropped) > 24:
            _say("tm:   ...and %d more" % (len(dropped) - 24))
    return len(dropped)


#: The `after` a watcher's purgeable render messages (@PutCard/@BattleData/
#: @WFCard) ride on. See `_watch_push`. It is a TWO-SIDED constraint, not "as
#: late as possible" -- the render must land AFTER the @TurnData that enters its
#: own turn N (queued a cycle earlier, so further decremented) and BEFORE the
#: @TurnData that enters turn N+1 (queued right after it, same cycle). Both
#: @TurnData ride `after=1`, so the render must ride `after=1` too: it then
#: delivers behind turn N's @TurnData (which is ahead of it in the queue) and
#: ahead of turn N+1's @TurnData (which is behind it, same drain, list order).
#: WARNING: after=0 overtakes turn N's own @TurnData -> Errot!!Turn(N-1)!=N (the
#: original crash); after=2 overshoots turn N+1's @TurnData -> Errot!!Turn(N+1)
#: !=N (a crash this gap CAUSED at 2, live 2026-09-06). 0 disables the floor
#: entirely (restores the racing after=0) for an A/B.
def _watch_render_gap():
    return max(0, _env_int("POL_TM_WATCH_PUT_GAP", 1))


def _watch_push(chan, index, body, why, after=0):
    """One body to every watcher of this match, pinned to the table peer.

    Relays the in-match stream (@TurnData/@PutCard/@BattleData) the watcher reads
    once its play scene is up -- @WFCard (`_watch_wfcard`) is what starts that
    scene. `POL_TM_WATCH_DROP_STREAM=1` reverts to dropping the stream.

    WARNING: THE OBSERVER PURGE-RACE GAP (2026-09-06). A watcher's play scene has no
    ack, so it cannot tell the server it has entered a turn. GamePlayEnd
    (`0xC7642`) purges the receive store of EVERY per-turn message --
    `@WFCard`/`@Start`/`@PutCard`/`@BattleData`, msgids {0x25,0x26,7,8,0xA,0xC,
    0x28} -- and keeps ONLY `@TurnData` (9). So a render message that lands in
    the same drain as the `@TurnData` that enters its turn is DELETED, the client
    reaches the turn with no card, reads the NEXT turn's put, and prints
    `Errot!!Turn` -> "invalid card data detected" (live, watching a COM game).
    A player never hits this because the ack gate holds each turn's put until the
    player has acked past the purge; a passive watcher can't ack, so its
    purgeable render rides a fixed GAP behind the @TurnData that leads it.
    `@TurnData` itself is exempt -- it survives the purge and MUST lead.
    `POL_TM_WATCH_PUT_GAP=0` restores the racing behaviour."""
    try:
        # The 8-char header is little-endian bytes [0x43, 00, cmd, turn]
        # (`_turn_code`/`encode_code`), so the command is byte 2 -> >>8.
        cmd = (int(body[:8], 16) >> 8) & 0xFF
    except (ValueError, TypeError):
        cmd = -1
    if _env_int("POL_TM_WATCH_DROP_STREAM", 0) and cmd in _WATCHER_DEAF_CMDS:
        return
    if cmd in (PUTCARD_CMD, BATTLEDATA_CMD):
        after = max(int(after), _watch_render_gap())
    for wmid, wpeer in _watchers_of(chan, index):
        _queue_push(wmid, body, "watcher: " + why, after=after, peer=wpeer)


def _wfcard_body(tile, row, turn):
    """`@WFCard=` (cmd 38, arm 0x1020F4) for ONE board tile. Decoded 2026-09-03:
    `/T=` is the TURN ([obj+0x18d]); the tile is named by an `@<tile>` SECTION
    marker; `/D=` is the same 6-value row `@PutCard` carries --
    `id|attack|type|pdef|mdef|power` (arrows/flag are DERIVED from the card DB by
    id, not sent). WARNING: The arm writes NO owner -- a flip's colour change is not
    expressible here; owners come only from the initial @WatchInfo `/F=`."""
    d = b"|".join(b"%d" % int(row[f]) for f in tmbattle.ROW_FIELDS[:6])
    return b"@WFCard=/T=%d@%d/D=" % (int(turn), int(tile)) + d


def _watch_wfcard(chan, index, turn, tiles, board):
    """Push one @WFCard per named tile to every watcher -- THE watcher's only
    live board-update channel (`_watch_push` drops the cmd 9/10/12 stream it
    cannot read). WARNING: `@<tile>` is matched as a raw substring by the client
    (0xaa920), so a multi-digit tile (>=10) may collide with a single-digit one.

    VERIFIED: RE-ENABLED 2026-09-03: @WFCard is what TRANSITIONS the watch scene into
    the play scene (`Recv=WATCHCARD` -> `GamePlayInit`, measured shim-CASPC).
    Without it the dealt @StartData/@TurnData/@PutCard just FILE unread (the
    watch scene polls only cmd 38). The turn-desync it caused before was a
    MISSING BASELINE, not @WFCard itself -- the watcher now also gets the deal's
    @StartData + turn-0 @TurnData (see "DEAL THE WATCHERS TOO"), so the play
    scene consumes cmd 8 -> 9 -> 10 in order. `POL_TM_WATCH_WFCARD_LIVE=0`
    disables it for an A/B."""
    if not _env_int("POL_TM_WATCH_WFCARD_LIVE", 1):
        return
    watchers = _watchers_of(chan, index)
    if not watchers:
        return
    for t in tiles:
        card = board.get(int(t))
        if card is None:
            continue
        body = _turn_code(WFCARD_CMD, turn) + _wfcard_body(t, card.row, turn)
        for wmid, wpeer in watchers:
            # @WFCard (0x26) is purged at GamePlayEnd too, so it rides the same
            # observer gap as @PutCard/@BattleData -- behind the @TurnData that
            # enters the turn (see `_watch_push`, the purge-race note).
            _queue_push(wmid, body, "watcher: @WFCard tile %d (card %d)"
                        % (t, int(card.row["id"])),
                        after=_watch_render_gap(), peer=wpeer)
        _say("tm:   ...watcher board: @WFCard tile %d = card %d, turn %d"
             % (t, int(card.row["id"]), turn))


def _watchinfo_body(names_hex, rules, n, mid_game=None):
    """`@WatchInfo=` -- the observer's state snapshot (arm 0x101B15).

    `names_hex` is one UPPERCASE-hex name per ABSOLUTE seat (occ order --
    the arm applies no rotation; a watcher lives in the absolute frame).
    `mid_game` is None for the pre-game form (sh=0) or a dict with
    turn/active/board/hand counts for the sh=1 form -- the arm reads
    /H /T /S /A /F /CH only when the envelope sh is 1.

    WARNING: /F= and /CH= per-tile encodings are the arm's WRITES read backwards
    (low 6 bits -> [obj+0x1F5+i], bits 6-7 -> the tile record's owner slot,
    /CH= -> the tile record's first byte); which values render correctly is
    for the first live observe to measure -- serve (owner+1)<<6 and the
    card id, logged as the experiment they are.

    VERIFIED: THE LOW SIX BITS NOW CARRY SOMETHING REAL. `[obj+0x1F5+i]` is the
    OBJECT CODE array (`tmbattle.roll_board`), so an unplayed block or special
    tile is sent as its own code -- the same byte `@StartData` puts there --
    while a tile a player holds keeps the owner-bit form. A CAPTURED block
    renders as its captor's tile and carries no card face, because a pseudo
    card id (0x8000+) is not something `/CH=` can name.
    """
    body = bytearray(b"@WatchInfo=/N=%d" % n)
    body += b"/CN=" + b"|".join(names_hex)
    body += b"/R=" + b"|".join(b"%d" % v for v in list(rules)[:7])
    body += b"/Q=" + b"|".join([b"0"] * n)
    body += b"/C=" + b"|".join([b"0"] * n)
    if mid_game:
        counts = mid_game.get("counts") or [0] * n
        body += b"/H=" + b"|".join(b"%d" % v for v in counts[:n])
        body += b"/T=%d" % int(mid_game.get("turn") or 0)
        body += b"/S=%d" % int(mid_game.get("s") or 0)
        body += b"/A=%d" % int(mid_game.get("active") or 0)
        tiles = int(mid_game.get("tiles") or 16)
        board = mid_game.get("board") or {}
        objects = mid_game.get("objects") or []
        fvals, chvals = [], []
        for t in range(tiles):
            card = board.get(t)
            code = int(objects[t]) if t < len(objects) else 0
            held = (card is not None and card.owner is not None
                    and int(card.owner) < n)
            if held:
                fvals.append(b"%d" % (((int(card.owner) + 1) & 3) << 6))
                # A pseudo card (a captured block) has no face to name.
                cid = int(card.row["id"])
                chvals.append(b"%d" % (cid if cid < 250 else 0))
            elif code:
                fvals.append(b"%d" % (code & 0x3F))
                chvals.append(b"0")
            else:
                fvals.append(b"0")
                chvals.append(b"0")
        body += b"/F=" + b"|".join(fvals)
        body += b"/CH=" + b"|".join(chvals)
    body += b"/L=" + b"|".join([b"0"] * n)
    return bytes(body)


def _take_frame(pools, me):
    """The take list in the RECIPIENT's frame: its own pool first.

    WARNING: MEASURED 2026-09-07T15:28Z (41c): the Deck won from ABSOLUTE seat 1 and
    its take screen showed the pools SWAPPED -- its own cards offered as the
    loser's -- so it picked blind (the server still moved the right card,
    the winner's slot 3). The client reads /H= occurrence i and the flat /D<k>= as
    "player i in MY frame" (me = 0), exactly like @VsGameInit and @ResultData;
    a winner in seat 0 never showed it. `POL_TM_TCLIST_ROTATE=0` restores the
    absolute order."""
    if not _env_int("POL_TM_TCLIST_ROTATE", 1) or not pools:
        return list(pools)
    me = int(me) % len(pools)
    return list(pools[me:]) + list(pools[:me])


def _losers_from(win, pools):
    """The losing seats in the order the WINNER's take screen walks them: the
    other seats in rotated order after itself (identity for two players)."""
    n = len(pools)
    if _env_int("POL_TM_TCLIST_ROTATE", 1):
        return [(win + k) % n for k in range(1, n) if pools[(win + k) % n]]
    return [s2 for s2 in range(n) if s2 != win and pools[s2]]


def _take_occ0(pools, me, win):
    """Occurrence 0 of every take-list row, per pool, in the RECIPIENT's frame.

    WARNING: IT IS THE SEAT ENTITLED TO TAKE THE CARD. The trade scene's state 0x3A
    (0xC91B9, read 2026-09-07) accepts a target seat only if one of its cards
    carries this byte (hand-slot record +0x1D, where `@TCList /D<k>=` occ 0
    lands) EQUAL to the picker's seat index in the viewer's frame. We sent 0 =
    "the viewer itself": the winner's own client found its targets, every
    LOSER's client (winner = index 1 in its frame) found none, printed "NEXT
    ENEMY" and left the take scene before the pick -- the 2026-09-07 "the loser
    never sees which card was taken". The 09-02 "occ 0 = id makes the cards
    invisible" measurement is the same byte: an id is no seat. `pools` is
    already rotated to `me` first; the winner's own pool keeps 0.
    """
    n = len(pools)
    if n == 0 or not _env_int("POL_TM_TCLIST_TAKER", 1):
        return [0] * n
    w_r = (int(win) - int(me)) % n if _env_int("POL_TM_TCLIST_ROTATE", 1) \
        else int(win) % n
    return [0 if p == w_r else w_r for p in range(n)]


def _tclist_body(pools, occ0=None):
    """`@TCList=` -- the win screen's take list. Arm 0x104648, `Recv=TRADELIST`.

    VERIFIED: MEASURED LIVE 2026-08-22T16:49Z, ring clean (store_count=2): a WINNER's
    post-@Ready screen polls exactly (0x43, 26) and (0x43, 35) and hangs
    without them -- a DRAW skips both and flows straight to @Continue. This
    message re-supplies every player's cards for the take screen (the play
    hands were popped as played), into the 156-byte hand-slot records.

    Shape, off the arm:
        /H=      one count PER PLAYER (occurrence i -> player i's slot count,
                 written to [esi+0x19F+48k] at the rotated slot)
        /D<k>=   ONE CARD each, k FLAT across players (player*5+card), each a
                 pipe list read by occurrence:
                   occ 0 -> +0x22A/+0x229   the take screen's NAME field --
                                            see below; was served 0
                   occ 1 -> +0x21A word     the CARD ID (names the ART cad%05d)
                   occ 2 -> +0x214          attack     -+ the arm's own
                   occ 3 -> &3 -> +0x215    TYPE        | CardPrm baseline
                   occ 4 -> +0x216          p. defence  | math at 0x104960
                   occ 5 -> +0x217          m. defence -+ pins these three
                   occ 6 -> +0x219          the arrows

    WARNING: occ 0 IS NOT THE NAME -- it is a VALIDITY/flag field, and a large value
    INVALIDATES the card. Falsified live 2026-09-02: the take screen showed the
    RIGHT card (correct art from occ 1, correct card transferred by @GetSelect)
    under a WRONG name (Chocobo->"Cerberus", Frog->"Fat Chocobo"), so occ 0 = id
    was tried as the name -- and the cards went **INVISIBLE** at the select
    prompt (id 88 etc. is out of this field's valid range). So: occ 0 must stay
    small (0 renders the card), and the WRONG NAME comes from somewhere else
    (occ 1 drives the ART and is correct; the name is NOT occ 1 either, since
    art is right while name is wrong, and the wrong names have no fixed offset
    from the id -- not a shifted string table). The real name field is
    UNIDENTIFIED and needs the TM.dll dump (arm 0x104648), which is not in this
    checkout. `POL_TM_TCLIST_NAME=1` re-enables the falsified occ-0 = id ONLY as
    the A/B that proved it invalid -- do not turn it on.

    `pools` is one list of wire rows (the 8-value `id|atk|type|pdef|mdef|
    power|arrows|x` format everything else uses) per ABSOLUTE seat.
    """
    name_id = _env_int("POL_TM_TCLIST_NAME", 0)
    body = bytearray(b"@TCList=/H="
                     + b"|".join(b"%d" % len(p) for p in pools))
    k = 0
    for pi, pool in enumerate(pools):
        taker = int((occ0 or [0] * len(pools))[pi])
        for row in pool:
            v = row.split(b"|")
            if len(v) < 8:
                continue
            o0 = v[0] if name_id else b"%d" % taker
            body += b"/D%d=" % k + b"|".join(
                (o0, v[0], v[1], v[2], v[3], v[4], v[6]))
            k += 1
    return bytes(body)


def _placements(scores):
    """`[place x100]` per seat from `[score]` -- **FRACTIONAL** ranking.

    KEY: **THE TIE RULE IS THE CLIENT'S OWN, AND IT IS NOT THE RANKING LISTS'.**
    Measured 2026-08-24 off the result path's average-rank recomputation
    (`0xCC814`), which is the arithmetic this feeds:

        edx = 0
        for i in 0..31:                       ; a 32-slot ring at struct +0x8C
            eax = ring[i] + 2
            eax = eax * 5 * 5                 ; two `lea eax,[eax+eax*4]`
            edx += eax * 2                    ; `lea edx,[edx+eax*2]`
        struct+0x04 = edx >> 5                ; / 32  -> AVERAGE RANK

    i.e. `avg = mean((ring[i] + 2) * 50)`. Read that scale off its own ends:
    `ring[i] == 0` gives **100 = 1.00** and `== 2` gives **200 = 2.00**, so the
    stored byte is `2 * (place - 1)` -- **each unit is HALF a place**, and the
    odd values exist precisely so a DRAW can be recorded. A two-player draw is
    `ring[i] == 1` -> **1.50**, not a shared 1st.

    WARNING: So this is FRACTIONAL (mean) ranking, while `tmrank`'s LISTS use standard
    COMPETITION ranking (`0x175417`: a row whose value equals the previous row's
    inherits its rank). Both are the client's, on two different screens, and an
    earlier version of this function used the ladder's rule here -- which scored
    every draw as a win and made the average, and therefore the TITLE, better
    than it should be.

    Returned **x100** so nothing has to carry halves: seat i's contribution is
    already in the save's own fixed point.
    """
    n = len(scores)
    vals = [int(s or 0) for s in scores]
    out = [100] * n
    for i, v in enumerate(vals):
        better = sum(1 for w in vals if w > v)
        tied = sum(1 for w in vals if w == v)
        # Fractional rank: the mean of the places this seat's tie group spans,
        # i.e. better+1 .. better+tied. x100, and exact for any tie size
        # because (tied - 1) * 50 is a whole number of hundredths.
        out[i] = (better + 1) * 100 + (tied - 1) * 50
    return out


def _result_code(place_x100):
    """The client's own per-match RESULT byte from a fractional place x100.

    VERIFIED: MEASURED at `0xCC5C7..0xCC62E`, the per-opponent accumulator, and it is
    the cleanest confirmation this whole area has: the client walks the other
    seats and adds **0 for a win, 1 for a draw, 2 for a loss** --

        0xCC5CD  cmp cl, [esi]        ; my score vs this opponent's
        0xCC5D1  inc byte [+0x1A1]    ;   EQUAL   -> += 1
        0xCC5DF  jae 0xCC610          ;   >=      -> nothing more (a win is 0)
        0xCC5E7  add cl, 2            ;   <       -> += 2

    -- so the byte is exactly `2 * (fractional place - 1)`, which is what
    `_placements` computes independently from the scores. Two derivations of the
    same number from different ends of the same function.

    That byte is what lands in the result ring (`0xCC806`) and what the streak
    machine branches on (`0xCC64C`), so both consumers take it from here.
    """
    return max(0, (int(place_x100) - 100) // 50)


def _streak_next(current, result):
    """The client's Consecutive Wins counter after one match. SIGNED.

    Ported from `0xCC643..0xCC69C`. One field carries both Playdat labels: a win
    run is positive (`Consecutive Wins`), a loss run negative
    (`Consecutive Losses`), and the screen picks by sign.

        result == 2  ->  0                      0xCC651 `mov word [+0x88], bp`
                                                (ebp is 0 -- 0xCC62C gates the
                                                whole block on `test ebp,ebp`)
        result <  2  ->  1 if current < 0       0xCC663 `test cx,cx / jge`
                         else current + 1       0xCC675 `inc cx`
        result >  2  ->  -1 if current > 0      0xCC68A `mov word [...], 0xFFFF`
                         else current - 1       0xCC695 `dec word [...]`

    WARNING: In a TWO-player match `result == 2` is a loss, and it RESETS to zero
    rather than starting a loss run -- a loss run needs `result > 2`, which only
    a 3-player board can produce. That is the client's behaviour, not a
    simplification of it, and it is why `Consecutive Losses` is nearly dead on a
    two-player server.

    WARNING: Three gates on the update are UNMEASURED (`ebp`, `[ebx+0x197]`,
    `[0x52464CA]` must all be zero). They plausibly exclude observers and
    abandoned matches. We apply the update unconditionally, which can only
    over-count in cases we do not yet know how to detect.
    """
    cur = int(current or 0)
    if result == 2:
        return 0
    if result < 2:
        return 1 if cur < 0 else cur + 1
    return -1 if cur > 0 else cur - 1


def _bump_result_stats(member_id, score, place=None, opponents=None,
                       combo=0):
    """One finished match for `member_id`: advance the career counters and
    return `(games, score_total)`.

    The first two are the numbers `@ResultData`'s own-section `/D=` occ 5/6
    carry, and occ 5 is a DIVISOR (see `_resultdata_body`) -- this function
    never returns a games count below 1. Persisted in the collection's `rank`
    block, beside the ranking stats `tools/tmrank.py` reads, because they are
    the same kind of thing: numbers only the server can know.

    `place` is this member's finishing POSITION in the match (1 = won), from
    `_placements`. It feeds `avg_rank`, and passing None simply leaves the
    average where it was -- a caller that cannot say where somebody finished
    must not be able to move it.

    WARNING: **THE SAVE IS NOT REWRITTEN FROM HERE** (`sync_save=False`, which exists
    to avoid rebuilding the card block for a header change). `_save_sync_stats`
    delivers these into the save's own stat fields instead, and the caller runs
    it once per member per match -- see the banner there for why a stat that
    only reaches the JSON is a stat the player never sees.
    """
    score = max(0, int(score or 0))
    if not _env_int("POL_TM_RESULT_STATS", 1):
        return 1, score
    try:
        data = _collection_load(member_id)
        block = data.setdefault("rank", {})
        import tmrank as _tr
        # Read BEFORE `games` moves: a block from before `tiled_games` existed
        # derives it from the counts it already holds (tmrank.tiled_games_of).
        tiled_before = _tr.tiled_games_of(block)
        games = max(1, int(block.get("games") or 0) + 1)
        total = max(0, int(block.get("score_total") or 0) + score)
        block["games"], block["score_total"] = games, total
        # WHEN they last played: Top 30 and Best Rookies are "last week's"
        # lists and only rank members who played in the tallied week
        # (tmrank._eligible). Every result counts, COM games included.
        block["last_played"] = int(time.time())
        # ...AND THE VS. RATING, which is what the ranking lists sort on
        # (tmrank.LISTS: menus 0/1/2 read it, x100 fixed point). SE's formula
        # is unrecoverable; the average score x100 is ours by construction
        # (the csid precedent), matches the career screen's own average
        # (score_total / games -- the same division the client runs at
        # 0x10F6A5), and lands on the scale of the one measured example
        # ("2.96"). POL_TM_RANK_RATING=0 stops writing it.
        if _env_int("POL_TM_RANK_RATING", 1):
            # WARNING: RESCALED 2026-09-07 (live testing): the
            # client's own Technical Rating (0xCC72F) is written for ratings
            # well UNDER 4.00 -- it multiplies (400 - avg opponent rating) --
            # and average tiles x100 (300..800) printed 0.00 on every result
            # screen. `tmrank.rating_of` is the ONE formula: 1.00 + 3.00 x the
            # share of the board held, over the career (`tiles_total`).
            block["tiles_total"] = (max(0, int(block.get("tiles_total") or 0))
                                    + _board_tiles(1 + max(0, int(opponents or 0))))
            # ...and HOW MANY games those tiles cover, so rating_of can count
            # the rest at 16 each instead of dividing a career's score by a
            # few games' tiles (one tester's flat 4.00, 2026-09-13).
            block["tiled_games"] = tiled_before + 1
            block["rating"] = max(0, int(_tr.rating_of(block)))
        # ...AND THE AVERAGE RANK, the mean finishing position x100. This is
        # the stat `CoPrm.BIN` gates every TITLE on and the one the Player Data
        # screen calls 平均順位 / `Average Rank`, and it has never had a
        # producer -- which is why the save's +0x30 has always been 0 and why
        # the client's own title has always been computed against a free pass
        # (`tm_title.py`, and `<=` against zero matches every threshold).
        #
        # VERIFIED: **AND THE DEFINITION IS NOW MEASURED, NOT INFERRED** (2026-08-24).
        # It was reasoned from `CoPrm.BIN`'s 300..100 thresholds, `PlPrm.BIN`'s
        # COM opponents and the guidebook's prize formula; then the client's own
        # recomputation at `0xCC814` was read, and it is
        # `mean((ring[i] + 2) * 50)` over a 32-slot result ring -- i.e. **mean
        # finishing place x100**, exactly the scale that was guessed, with
        # draws on the half-steps. See `_placements`.
        #
        # WARNING: **100 IS THE FLOOR, NOT 0.** The client's arithmetic cannot produce
        # below 1.00, so a 0 here is out of domain -- which is what an unwritten
        # save `+0x30` has always been.
        #
        # `place_total` is kept alongside (already x100) so the average stays
        # exact across restarts; storing only the rounded average would make a
        # later correction impossible.
        if place is not None and _env_int("POL_TM_AVG_RANK", 1):
            # WARNING: THE DIVISOR IS GAMES THAT PLACED, NOT GAMES PLAYED. `games`
            # counts EVERY result; `place_total` only accumulates when the
            # caller could say where the member finished (`place is not None`
            # -- the guard right above). Dividing the one by the other
            # deflates the average by however many results carried no place,
            # and on real data it went far enough under 1.00 to be pinned by
            # the `max(100, ...)` floor: member 3, 2026-08-25, place_total
            # 1150 over games 18 = 63 -> floored to 100, i.e. "a perfect 1.00
            # average" for a player whose every recorded placement was a 2nd
            # or a draw. The floor was hiding the arithmetic, so the number
            # looked plausible and was not.
            #
            # WARNING: This matters beyond the Status screen: the title picker
            # (`tm_title.py`, rva 0x110B10) tests `avg_rank <= threshold`, so
            # a too-low average clears every row and INFLATES the title.
            places = max(0, int(block.get("place_total") or 0)) + max(100, int(place))
            placed = int(block.get("placed") or 0)
            if not placed and block.get("place_total"):
                # MIGRATION, once, for a block written before `placed` existed.
                # Derived from what is already stored -- `place_total` and the
                # average we currently believe -- so it reproduces today's
                # number rather than inventing a different one. The count is
                # unrecoverable exactly (nothing recorded which results
                # carried a place); this is the closest consistent pair.
                prev_avg = max(100, int(block.get("avg_rank") or 100))
                placed = max(1, round(int(block["place_total"]) / prev_avg))
                _say("tm:   ...member %s: seeding rank.placed = %d from the "
                     "stored place_total %d / avg_rank %d (one-time; the old "
                     "average divided by GAMES PLAYED, not games placed)"
                     % (member_id, placed, block["place_total"], prev_avg))
            placed += 1
            block["place_total"], block["placed"] = places, placed
            block["avg_rank"] = max(100, places // placed)
            # ...AND THE STREAK PAIR, off the same result byte the client uses.
            # `consec_wins` is SIGNED: negative is a losing run, which is how one
            # field serves both Playdat labels. `streak` is its running max, and
            # the client only reconsiders the max on a non-losing result
            # (0xCC673/0xCC67E fall into 0xCC6AA; the loss arm jumps past it).
            res = _result_code(place)
            consec = _streak_next(block.get("consec_wins"), res)
            block["consec_wins"] = max(-32768, min(32767, consec))
            if res < 2:
                block["streak"] = max(int(block.get("streak") or 0),
                                      block["consec_wins"])
        # ...AND OPPONENTS FACED, which is NOT the games count. `0xCC7AC` adds
        # `N - 1` per match and `0xCC7E4` divides Grand Total by it, so Average
        # Prize is per OPPONENT. Authoring it also keeps that division away from
        # zero -- it is the result-screen divide-by-zero's divisor.
        if opponents is not None and _env_int("POL_TM_AVG_RANK", 1):
            block["opponents"] = (max(0, int(block.get("opponents") or 0))
                                  + max(0, int(opponents)))
        # MOST COMBOS -- a career MAX, exactly as `0xD029B` keeps it. Tallied
        # unconditionally so the history exists; only the SAVE write is gated
        # (see `_note_combo` for what is inferred and what is measured).
        if combo:
            block["most_combos"] = max(int(block.get("most_combos") or 0),
                                       int(combo))
        _collection_store(member_id, data, sync_save=False)
        return games, total
    except Exception as e:
        _say("tm: result stats NOT recorded for member %s (%s) -- serving the "
             "one-game floor" % (member_id, e))
        return 1, score


def _grow_used_cards(member_id, used_rows):
    """Grow the cards `member_id` USED this match toward their CardPrm ceilings,
    and persist -- "the cards you use will grow too" (SE guidebook).

    Growth is server-authored: BOTH clients only store/render the absolute stats
    we send (via the save / `@Card`) and compute no growth themselves (PC
    0xB2CF0-only, PS2 0x2b1c80-only). So the server raises the stat and the save
    rewrite (inside `_collection_store`) carries it to the client on its next
    reload. The AMOUNT is `tmroll`'s tunable policy; the mechanism (grow toward
    the ceiling, never past) is measured.

    `used_rows` are the member's selected-hand wire rows (b"id|atk|...", up to 8
    pipe values). Each is matched to a COLLECTION instance by (id + the four
    combat stats) -- the erase/sell identity -- preferring an exact match so a
    duplicate id grows the right copy; a used card we hold no record of is
    skipped (drift), never invented. Returns the number of cards that grew.
    `POL_TM_CARD_GROW=0` disables growth.
    """
    if member_id is None or tmroll is None or not _env_int("POL_TM_CARD_GROW", 1):
        return 0
    used = []
    for r in used_rows or []:
        try:
            c = [int(x) for x in bytes(r).split(b"|") if x != b""]
        except (ValueError, TypeError):
            continue
        if len(c) >= 5:
            used.append(c)
    if not used:
        return 0
    try:
        data = _collection_load(member_id)
        have = data.get("cards")
        have = [list(c) for c in have] if isinstance(have, list) else []
        rnd = random.Random()        # per-instance growth; no reproducibility needed
        consumed, grown = set(), []
        for c in used:
            cid, stats = c[0], c[1:5]
            idx = next((i for i, row in enumerate(have)
                        if i not in consumed and row and row[0] == cid
                        and list(row[1:5]) == stats), None)
            if idx is None:          # fall back to any same-id instance
                idx = next((i for i, row in enumerate(have)
                            if i not in consumed and row and row[0] == cid), None)
            if idx is None:
                continue             # a used card we have no record of
            row = have[idx]
            while len(row) < 8:
                row.append(0)
            new_row, deltas = tmroll.grow_card(rnd, row)
            consumed.add(idx)
            if deltas:
                have[idx] = new_row
                grown.append((cid, deltas))
        if grown:
            data["cards"] = have
            _collection_store(member_id, data)      # rewrites the save
            _say("tm: member %s -- %d used card(s) GREW toward their ceiling: %s"
                 % (member_id, len(grown),
                    "; ".join("#%d %s" % (c, d) for c, d in grown)))
        return len(grown)
    except Exception as e:                                   # never fail a match
        _say("tm: card growth NOT applied for member %s (%s)" % (member_id, e))
        return 0


def _save_sync_stats(member_id):
    """Deliver the career stats we hold into the SAVE the client reads.

    WARNING: **THE GAP THIS CLOSES: WE HAVE BEEN COMPUTING STATISTICS AND NEVER
    HANDING THEM OVER.** `_bump_result_stats` and `_bump_prize` maintain games,
    score, VS. Rating, average rank and the prize-money tallies in the
    collection's `rank` block; `tmprize` maintains the prize-point balance.
    `tools/tmrank.py` reads the first set once a week for the ranking screens --
    and NOTHING read either of them for `U/g/TM0DataFile`, which is the only
    place Player Data -> Status gets its numbers from. So the screen showed
    zeros for figures this server had on disk, which is a delivery bug, not a
    missing feature.

    Offsets are `tmsave.STAT_FIELDS`, measured by the self-locating save
    probe. Writes go through `_save_patch_fields`, so this is
    the same atomic header patch `@Opt=` and the guild write already use -- the
    card block is untouched and no rebuild happens.

    WARNING: **ONLY FIELDS WE ACTUALLY PRODUCE ARE WRITTEN.** `Biggest Prize`,
    `Average Prize` and `Consecutive Wins` have offsets but no producer, and a
    zero written into them is indistinguishable from the zero already there --
    so they are named in `tmsave.STAT_FIELDS` and deliberately absent below.
    Average rank is written only once `games > 0`: a player who has never
    played has no average, and writing one anyway would be inventing the single
    number every title in the game is gated on.

    `POL_TM_SAVE_STATS=0` disables the whole write-through.

    KEY: **AND THE TWO 17-BYTE NAME SLOTS, WHICH ARE OURS TO FILL AND WHICH WE
    HAVE ALWAYS SHIPPED AS ZEROS.** Reported live, 2026-08-25: "'Memorable Win'
    just shows my own name". It is not coming from the save: both
    slots read ZERO in every member's file on prod.

    Measured in `TM.dll.unpacked` the same night: `0x5224CD4` (save +0x104) and
    `0x5224CE5` (save +0x115) have **exactly one reference each image-wide**,
    `0x101508` and `0x10151E`, and both are a `push` feeding the `0x1E2060`
    memcpy that copies **save -> object** (`[esi+0x104]`, `[esi+0x116]`, 0x11
    bytes each). Nothing in the client ever writes them back. So they are
    SERVER-AUTHORED strings, exactly like the stat dwords above -- the same
    delivery gap this function exists to close, one tier along.

    WARNING: WHICH SLOT IS "Memorable Win" IS NOT SETTLED. `tmsave.STRING_FIELDS`
    calls it a *candidate* for +0x104 and names no owner for +0x115, and after
    the `/Rule=` occ-0 retraction this file is not going to guess a field from
    position again. `POL_TM_SAVE_STR_104` / `POL_TM_SAVE_STR_115` write a
    literal into each slot so ONE login settles it: set them to two
    distinguishable strings, open Player Data -> Status, and read which label
    shows which. Unset (the default) writes nothing and leaves today's zeros.
    Once the mapping is measured, the producer replaces the knob.
    """
    if member_id is None or tmsave is None:
        return {}
    _str_probe = {}
    for _off, _env in ((0x104, "POL_TM_SAVE_STR_104"),
                       (0x115, "POL_TM_SAVE_STR_115")):
        _val = os.environ.get(_env)
        if _val:
            # 16 characters + the NUL the 17-byte slot holds. latin1 because
            # the encoding of these slots is unmeasured too -- a probe should
            # put ASCII in and read ASCII out, not add a variable.
            _str_probe[_off] = (17, _val.encode("latin1", "replace")[:16]
                                + b"\x00")
    if not _env_int("POL_TM_SAVE_STATS", 1):
        return {}
    try:
        block = (_collection_load(member_id) or {}).get("rank") or {}

        def _n(key):
            try:
                return max(0, int(block.get(key) or 0))
            except (TypeError, ValueError):
                return 0

        def _sn(key):
            """...and the SIGNED reader, for the one field that runs negative."""
            try:
                return max(-32768, min(32767, int(block.get(key) or 0)))
            except (TypeError, ValueError):
                return 0

        games = _n("games")
        prize_total = _n("prize_total")
        opponents = _n("opponents")
        rating = _n("rating")
        if games:
            # A block written before 2026-08-22 carries games/score_total and no
            # stored rating. ONE derivation, and it is `tmrank.stats_of`'s --
            # the ranking lists and this save must not disagree about a player's
            # rating, and they would the moment two copies of this formula
            # drifted. See that function's own note.
            try:
                import tmrank as _tr
                rating = max(0, int(_tr.stats_of({"rank": block})["rating"]))
            except Exception:
                rating = 0
        # WARNING: WIDTHS ARE THE PARSER'S, and the word fields are written AS WORDS
        # (`write_field` grew width 2 for exactly this): a dword at +0x78 would
        # destroy Winning Streak at +0x7A. `tmsave.STAT_FIELDS_WORD`.
        changes = {
            tmsave.RATING_OFF: (4, rating),
            tmsave.PRIZE_TOTAL_OFF: (4, prize_total),
            tmsave.VS_GAMES_OFF: (2, min(games, 0xFFFF)),
        }
        if games and _n("avg_rank"):
            changes[tmsave.AVG_RANK_OFF] = (4, _n("avg_rank"))
        if _n("biggest_prize"):
            changes[tmsave.BIGGEST_PRIZE_OFF] = (4, _n("biggest_prize"))
        if opponents:
            # AVERAGE PRIZE is the client's own division -- Grand Total over
            # OPPONENTS FACED, not over matches (0xCC7E4). Writing the divisor
            # as well as the quotient keeps the result screen's own recompute
            # (0x10F6A5) off a zero, which is the result-screen crash.
            changes[tmsave.OPPONENTS_OFF] = (2, min(opponents, 0xFFFF))
            changes[tmsave.AVERAGE_PRIZE_OFF] = (4, prize_total // opponents)
        # THE STREAK PAIR. `consec_wins` is SIGNED -- `write_field` masks, so a
        # losing run stores as two's complement exactly as `0xCC68A` writes it.
        # Both are written whenever either is non-zero, because 0 is a real
        # value for this pair (a reset) and skipping it would strand the old one.
        consec, streak = _sn("consec_wins"), _n("streak")
        if consec or streak:
            changes[tmsave.CONSEC_WINS_OFF] = (2, consec)
            changes[tmsave.STREAK_OFF] = (2, min(streak, 0xFFFF))
        # MOST COMBOS -- OFF BY DEFAULT. The field is measured, the UNIT is not
        # (`_note_combo`). `POL_TM_MOST_COMBOS=1` puts it on the screen, which
        # is how the unit gets settled: play a match with a known chain and read
        # the number back.
        if _n("most_combos") and _env_int("POL_TM_MOST_COMBOS", 0):
            changes[tmsave.MOST_COMBOS_OFF] = (1, min(_n("most_combos"), 0xFF))
        # The prize-point BALANCE is the closest thing we hold to `Prize Points
        # Acquired`, and it is not the same number -- ours drifts UP relative to
        # the client's the moment a player spends, because the Prize Center's
        # debit is client-side and no message reports it (`tmprize`'s own
        # banner). Written anyway, because a balance that is right until the
        # first purchase beats a zero that is never right, and named as the
        # approximation it is rather than quietly labelled "acquired".
        if tmprize is not None and _env_int("POL_TM_SAVE_PRIZE_POINTS", 1):
            try:
                pts = int((tmprize.load(member_id) or {}).get("points") or 0)
                changes[tmsave.PRIZE_POINTS_OFF] = (4, max(0, pts))
            except Exception:
                pass
        # THE NAME-SLOT PROBE, if it is armed. See this function's banner:
        # +0x104 and +0x115 are server-authored and we have only ever written
        # zeros, which is why "Memorable Win" has nothing of ours to show.
        # Which slot carries which label is UNMEASURED -- set the two env vars
        # to different strings and one login settles it.
        # THE DECK SET NAMES ride every sync too, so a stale stamp in a
        # save (the 2026-09-06 champion write) is cleared on the next login
        # without waiting for a collection change. An armed probe wins.
        for _off, _v in _deck_name_fields(member_id).items():
            changes.setdefault(_off, _v)
        changes.update(_str_probe)
        if _str_probe:
            _say("tm: member %s save STRING PROBE armed -> %s. Open Player "
                 "Data -> Status and read which label shows which; that is the "
                 "measurement (tmsave.STRING_FIELDS calls +0x104 a CANDIDATE "
                 "for Memorable Win and names no owner for +0x115)."
                 % (member_id, ", ".join(
                     "+0x%03X=%r" % (o, v[1]) for o, v in
                     sorted(_str_probe.items()))))
        moved = _save_patch_fields(member_id, changes)
        if moved:
            def _shown(v):
                return (repr(bytes(v).rstrip(b"\x00").decode("latin1"))
                        if isinstance(v, (bytes, bytearray)) else v)
            _say("tm: member %s save stats -> %s"
                 % (member_id, ", ".join(
                     "%s %s->%s" % (
                         {0x104: "deck tab 1", 0x115: "deck tab 2"}.get(
                             o, tmsave.STAT_FIELDS.get(o, hex(o))),
                         _shown(a), _shown(b))
                     for o, (a, b) in sorted(moved.items()))))
        return moved
    except Exception as e:
        _say("tm: save stats NOT delivered for member %s (%s) -- the Player "
             "Data screen will keep showing the old numbers" % (member_id, e))
        return {}


def _bump_prize(member_id, amount, why=""):
    """Credit winnings to the ranking prize counters (rank block
    `prize_total` / `prize_week` -- what the Grand Total and Weekly Total
    lists sort on). The weekly job resets `prize_week` at each publish.
    Money itself moves in `_set_money`; this is only the tally."""
    amount = int(amount or 0)
    if amount <= 0 or member_id is None \
            or not _env_int("POL_TM_RANK_PRIZE", 1):
        return
    try:
        data = _collection_load(member_id)
        block = data.setdefault("rank", {})
        block["prize_total"] = max(0, int(block.get("prize_total") or 0)
                                   + amount)
        block["prize_week"] = max(0, int(block.get("prize_week") or 0)
                                  + amount)
        # BIGGEST PRIZE -- `0xCC7BD cmp / 0xCC7C5 mov`, a plain running max over
        # the SAME per-match amount that feeds Grand Total. This is the whole
        # formula; there is nothing else to it.
        block["biggest_prize"] = max(int(block.get("biggest_prize") or 0),
                                     amount)
        _collection_store(member_id, data, sync_save=False)
        _say("tm: member %s prize tally +%d%s (total %d, week %d)"
             % (member_id, amount, (" -- " + why) if why else "",
                block["prize_total"], block["prize_week"]))
        # `prize_total` IS a save field (+0x50, Grand Total). The result path
        # syncs before this settles, so without this call the screen would lag
        # the ledger by exactly one match -- every time.
        _save_sync_stats(member_id)
    except Exception as e:
        _say("tm: prize tally NOT recorded for member %s (%s)"
             % (member_id, e))


def _resultdata_body(rand, seats=None, me=0, scores=None, stats=None,
                     prizes=None):
    """`@ResultData=` -- the end of the match. Arm 0x104141, `Recv=RESULTINFO`.

    WARNING: `/P=0` IS FATAL, NOT LEGAL -- RETRACTED 2026-08-21, by a live crash.
    The first end-to-end match delivered `/P=0` and BOTH clients died on the
    result screen. The old reading ("0x104205 `cmp eax,0 / jle 0x105174`
    completes the message") was true of the PARSER and said nothing about the
    scene: the early-out at 0x105174 sets the success flag, and the moment the
    command-13 wait is satisfied the client runs

        0x10F6A5   div ecx      ; [0x5246460] / (word [0x5246464] & 0xFFFF)

    and 0xCBD80 -- called at the scene transition, 0xC7A02 -- repeats the same
    division at 0xCBF69 for non-watchers. The divisor 0x5246464 is [obj+0x84],
    written by exactly ONE producer in the image: the recipient's own `@No`
    section below. On a fresh session it is BSS zero, so `/P=0` is an integer
    divide-by-zero pushed to everyone at the table at once. A message being
    parsed is not a scene surviving it.

    Field map, measured off the arm (own-section writes 0x104282..0x10435B):

        /R= x8   the random table again, into the SAME [0x2B63C4]+0x1A8+i
                 slots `@TurnData` fills (0x1041BE, `cmp edi, 8` at 0x1041D8).
        /P=      the number of `@No<i>=` sections; the loop bound (0x1044EC).
        @No<i>=  i is the seat in the RECIPIENT's own rotated numbering
                 (k = (N - own + i) % N at 0x104254; own = 0 in our copies,
                 same convention as `_vsgame_body`). `/D=` is ONE pipe list,
                 read by occurrence:

                   occ 0 -> [obj+0x04]        dead store; occ 2 overwrites it
                   occ 1 -> [obj+0xCC]        dword; 0xCBD80 max-updates it
                                              into [obj+0x78] -- the Biggest
                                              Prize `max` pattern, so this is
                                              read as THE MATCH PRIZE. Served
                                              from `prizes` (the settled net
                                              gain, computed before the body
                                              is built); reported live 2026-09-02:
                                              "the COM result screen shows the
                                              wager as 0" while the wallet
                                              moved -- delivery, not
                                              computation, one more time.
                                              INFERRED from the max-update; the
                                              first game served a real value
                                              here is the measurement.
                                              POL_TM_RESULT_CC != 0 overrides
                                              (the sweep apparatus it always
                                              was); POL_TM_RESULT_PRIZE=0
                                              restores the flat 0.
                   occ 2 -> [obj+0x180+48k]   the displayed rank slot, AND
                                              [obj+0x04] (0x1042CE / 0x1042E6)
                   occ 3 -> [obj+0x88]        word; career screen prints it
                                              only when > 0 (0x1389D6)
                   occ 4 -> [obj+0x86]        word; no reader found yet
                   occ 5 -> [obj+0x84]        word; **THE DIVISOR -- >= 1**
                   occ 6 -> [obj+0x80]        dword; the dividend

                 Every OTHER player's section is read for occ 2 ONLY
                 (0x10448F). `/T=` is optional (0x104394 skips it over
                 pre-zeroed defaults); `/RN=` is a hex :str (`_str_field`)
                 copied to [obj+0x128].

    WARNING: ALWAYS SEVEN VALUES IN `/D=`. None of the seven parses checks its
    result before storing (the `@VsGameInit` `/R=` trap, one message over): a
    short pipe list stores stack garbage into the remaining slots, the divisor
    included.

    The career screen (STATUS module, 0x138761..0x1389D6) prints [obj+0x78],
    the quotient [obj+0x7C], [obj+0x80], and [obj+0x88]-when-positive -- so
    occ 5/6 read as GAMES PLAYED and a career score total, quotient = the
    average. The NAMES are inferred from those readers; the NUMBERS are ours
    by construction (the csid precedent -- we author the field, the client
    only stores and divides): `_bump_result_stats` tracks real games and the
    real tile totals per member. Rank (occ 0/2) stays the same 0 every other
    message serves (`POL_TM_VSGAME_RA`, `_SHOPINIT_FIELDS`) until rank is a
    real thing on this server; `POL_TM_RESULT_RA` overrides.

    WARNING: The server still does not decide who WON on this message: 0xCBD80
    computes win/lose from the per-seat tile counts (block 0x5246580, stride
    0x30) the match itself filled. What a win PAYS is still the take flow's
    open question -- nothing here invents a prize.

    `POL_TM_RESULT_P=0` restores the pre-crash one-liner, kept ONLY as
    measurement apparatus for the divide itself.
    """
    body = b"@ResultData=/R=" + b"|".join(b"%d" % v for v in rand[:8])
    if not _env_int("POL_TM_RESULT_P", 1) or not seats:
        return body + b"/P=0"
    seats = list(seats)
    n = len(seats)
    scores = (list(scores or []) + [0] * n)[:n]
    # Same per-recipient rotation as @VsGameInit: the recipient IS seat 0 in
    # its own copy, so its section is @No0 and k = i for every other seat.
    if _env_int("POL_TM_VSGAME_SEAT0", 1) and me:
        seats = seats[me:] + seats[:me]
        scores = scores[me:] + scores[:me]
        me = 0
    ra_env = _env_int("POL_TM_RESULT_RA", 0)

    def _seat_rating(mid):
        # occ 0/2 are the result screen's per-player RATING rows ("a bunch
        # of rating scores that are all 0" -- reported live 2026-08-22, first
        # game after the tally went live). Serve each member's real VS.
        # Rating (the same avg-score x100 the ranking lists sort on); a COM
        # seat or a member with no record stays 0. POL_TM_RESULT_RA != 0
        # overrides everyone, kept as the sweep apparatus it always was.
        if ra_env:
            return ra_env
        try:
            import tmrank as _tr
            return int(_tr.stats_of(_collection_load(mid)).get("rating") or 0)
        except Exception:
            return 0

    body += b"/P=%d" % n
    for i, (mid, _ident) in enumerate(seats):
        ra = _seat_rating(mid)
        if i == me:
            games, total = (stats or {}).get(_push_key(mid),
                                             (1, scores[i]))
            cc = _env_int("POL_TM_RESULT_CC", 0)
            if not cc and _env_int("POL_TM_RESULT_PRIZE", 1):
                cc = int((prizes or {}).get(_push_key(mid), 0))
            d = (ra, cc, ra, 0, 0,
                 max(1, games), max(0, total))
            body += b"@No%d=/D=" % i + b"|".join(b"%d" % v for v in d)
            body += b"/RN=" + _vsgame_player_name(mid, i)
        else:
            d = (ra, 0, ra, 0, 0, 1, 0)
            body += b"@No%d=/D=" % i + b"|".join(b"%d" % v for v in d)
    return body


CONTINUE_CMD = 17               #: code 0x43 command 17 -- `@Continue`, arm 0x104EE1

#: The `/Ans=` ladder, read off the panel itself.
#:
#: `@Continue` is the post-game **"Play again?"** panel (`Playmes.BIN` 93; ctor
#: 0x10B780, built from 0xC2406, 13 states over table 0x10DCB8). Its constructor
#: at 0x10B7AB presets a player's slot to 2 when that player CANNOT play again,
#: which is what names 1 and 2; 3 has no writer anywhere in the image except the
#: receive arm itself, so it is a value only a server can produce.
CONTINUE_UNANSWERED = 0         #: has not answered
CONTINUE_AGAIN = 1              #: playing again      (sent from 0x10D27B / 0x10D375)
CONTINUE_DECLINE = 2            #: not playing again  (sent from 0x10D92B)
CONTINUE_FINAL = 3              #: SERVER ONLY -- "stop asking, tear the panel down"

#: (chan, index) -> {push_key(member): the highest answer that member has given}
#:
#: WARNING: MAX, NOT ASSIGN -- because that is what the client does. 0x104F8F/0x104F93
#: is `if (stored < received) stored = received`, so a slot only ever climbs. A
#: tally that could go back down would disagree with the panel it is driving.
_MATCH_CONTINUE = {}

#: (chan, index) -> push keys whose "Play again?" PANEL IS OPEN (their @Ready=
#: arrived). WARNING: A live-status update (sh>=2) delivered before the panel exists
#: sits in the client's store and is READ AFTER the seat answers -- which
#: bounces that seat back to the interactive panel (the rule the vote handler
#: states below). Measured 2026-09-07T04:37Z on the Deck: the status was pushed
#: at :19.56 while it was still on the result screen, its @Ready came :19.77,
#: its Yes :21.75, then TWO Recv=CONTINUE and no GameContinue -- a greyed panel;
#: the PC, which got only the tally, continued. So the status now waits for
#: @Ready (`POL_TM_CONTINUE_READY_GATE=0` restores the immediate push) and a
#: panel opening with a vote already in gets it one reply behind the @Ready
#: answer (`POL_TM_CONTINUE_READY_GAP`).
_CONTINUE_READY = {}


def _continue_body(answers, bval=0):
    """`@Continue=` -- the "Play again?" tally. Arm 0x104EE1, `Recv=CONTINUE`.

    `answers` is already in the RECIPIENT'S frame (occurrence 0 = the recipient);
    `_continue_frame` does that rotation.

    Two shapes here are load-bearing and both are hazards of the arm:

    * **One key, `|`-separated, exactly N values.** The same encoding `/ID=` and
      `/CN=` use -- see `_vsgame_body`'s banner. The loop at 0x104F40 runs
      `[obj+0xE5]` times (N, from GameInit's `/N=`) and **0x104F71 reads the out
      slot without checking the parse return**, so a short list maxes STACK
      GARBAGE into the client's per-player records. That is `/R=`'s bug one
      field over, and here a garbage value >= 3 tears the panel down instantly.
    * **`/B=` is repeated too.** It is read at occurrence `[obj+0x19B]` -- the
      recipient's own seat -- not at 0. Under the recipient-first convention
      that is 0 and one value would do; emitting N copies is correct under both
      conventions and costs nothing. (`/B=` is not really a `@Continue` field at
      all: it is the shared scene-exit byte 0x2B648E that `@Quit`(0x43,5),
      `@Quit`(0xA2,30) and SHOPQUIT(0xB2,23) also write. Meaning UNMEASURED --
      `POL_TM_CONTINUE_B` sweeps it, exactly as
      `POL_TM_MATCHQUIT_B` does for the same byte on the match quit.)
    """
    vals = [int(v) for v in answers]
    return (b"@Continue=/Ans=" + b"|".join(b"%d" % v for v in vals)
            + b"/B=" + b"|".join(b"%d" % int(bval) for _ in vals))


def _continue_frame(vals, me):
    """`vals` in ABSOLUTE seat order -> the frame the recipient at seat `me` reads.

    The client maps occurrence `i` to its own slot `(N - me + i) % N`
    (0x104F59..0x104F71), so with `POL_TM_VSGAME_SEAT0` making every recipient
    seat 0 -- the rule `_vsgame_body` established and everything downstream
    already follows -- the list is simply rotated recipient-first. Flip that knob
    and this rotation has to come off with it, which is why it reads the same
    env var rather than assuming.
    """
    vals = [int(v) for v in vals]
    if _env_int("POL_TM_VSGAME_SEAT0", 1) and me:
        return vals[me:] + vals[:me]
    return vals


def _reset_for_rematch(chan, index):
    """Everything a fresh game needs cleared, when there is no fresh announcement.

    WARNING: A REMATCH DOES NOT GO THROUGH `@VsGameInit`, SO `_remember_match` NEVER
    RUNS. 0xC243E takes the panel's `0x4000054` straight back to the deal scene
    (`[obj+0x16F] = 0x18`) and the client re-picks its five cards. Without this
    the second game inherits the first one's hands, board and turn counter --
    `/H=` would index the wrong card, and `_CARD_READY` still holding both
    players would fire the deal on the first `@CardSelect=` of the pair.
    """
    _CARD_READY.pop((chan, index), None)
    _MATCH_HANDS.pop((chan, index), None)
    _MATCH_TURN.pop((chan, index), None)
    _MATCH_BOARD.pop((chan, index), None)
    _MATCH_OBJECTS.pop((chan, index), None)
    _MATCH_COMBO.pop((chan, index), None)
    _TURN_RAND.pop((chan, index), None)
    _MATCH_BOTS.pop((chan, index), None)
    _BOT_SYNTH.discard((chan, index))
    _PENDING_BATTLE.pop((chan, index), None)
    # An unsettled PvP escrow is dropped WITHOUT refunding here -- each
    # member's refund rides their own durable staked_wager (@GameExit /
    # _stake_recover), and refunding both places would pay twice.
    _PVP_STAKES.pop((chan, index), None)
    # WARNING: A REMATCH IS A NEW GAME TO THE MATCH-HOLD CLOCK TOO. `_MATCH_BEGAN`
    # is stamped at the FIRST `@GameReady=` and a rematch sends no new one, so
    # after enough consecutive rematches the epoch aged past
    # POL_TM_MATCH_HOLD_S and `expire_silent_seats` released the seats UNDER
    # the live game -- the table republished as free, and a stranger reserving
    # it re-opens the stale-roster-hijack class (09-02 review M2).
    if (chan, index) in _MATCH_BEGAN:
        _MATCH_BEGAN[(chan, index)] = time.time()
    _live_matches_write()
    _MATCH_CONTINUE.pop((chan, index), None)
    _CONTINUE_READY.pop((chan, index), None)


#: --------------------------------------------------------------------------
#: A PLAYER WHO LEAVES A RUNNING MATCH IS REPLACED BY THE SERVER (2026-09-04)
#: --------------------------------------------------------------------------
#: THE HOLE: `@GameExit=` cleaned the leaver's own state and told the other
#: players NOTHING. The client has no in-match receive path for "a player
#: left" -- its board scene polls exactly one slot at a time ((0x43, 9) for
#: the turn, (0x43, 10) for the opponent's card) and `@Dead` (cmd 39) is
#: polled only by WAITING scenes (shop init, VS. COM init, trade: 26 sites,
#: none in 0xC0000-0xD3000). So the opponent sat on (0x43, 10) until their
#: own timeout, and seat expiry could not help either: it is suspended for
#: POL_TM_MATCH_HOLD_S (45 min) while a match is "running".
#:
#: THE FIX is the one Janhourou already uses for a mid-hand quit (`116ce73c`):
#: the server plays the empty seat. Network mode makes the server the
#: authority for every seat (0xCEE51 skips the client's own resolver), and
#: the VS. COM engine (`_queue_com_turns` / `_com_play_acked`) already plays
#: a seat out of a server-held hand -- this reuses its move choice, its
#: ack-gate (the GamePlayEnd purge race) and the PvP per-recipient framing.
#: The departed seat keeps its OWN hand (learned at `@CardSelect=`), so the
#: take flow at the end moves a card the leaver really held.
#:
#: Trigger: `@Break=` (0x43 msgid 39, `/ID=0` -- measured on prod 09-03, sent
#: ~2 s BEFORE `@GameExit=`) or `@GameExit=` from a seat in a running match.
#: A leaver during CARD SELECT gets a synthetic hand (a COM deck row set) and
#: the deal proceeds; that match's take flow is disabled (`bot_synth`) so no
#: card the leaver never owned changes hands. Pushes to a departed seat are
#: skipped everywhere in the match flow (`_is_bot`); its stake stays in the
#: PvP pot and cannot win it. `POL_TM_BOT_SEATS=0` restores the old
#: behaviour (the opponent hangs).
CARDSELECT_SLOT = 7             #: code 0x43 command 7 -- `@CardSelect`, arm 0x1030C9
DATAERROR_CMD = 40              #: code 0x43 command 40 -- `@DataError=/No=`, arm 0x103481


def _roster_at(chan, index):
    """The announced roster of the match at this table, or []."""
    for v in _MATCH_ROSTER.values():
        if v[0] == chan and v[1] == index:
            return list(v[2])
    return []


def _is_bot(chan, index, member_id):
    """Is this seat played by the server because its player left?"""
    return _push_key(member_id) in (_MATCH_BOTS.get((chan, index)) or set())


def _humans(chan, index, roster):
    return [(m, v) for m, v in roster if not _is_bot(chan, index, m)]


def _seat_departed(member_id, why):
    """A seated player is leaving; if their match is running, take the seat.

    Idempotent: `@Break=` and `@GameExit=` both arrive for one leaver."""
    if not _env_int("POL_TM_BOT_SEATS", 1) or member_id is None:
        return
    chan, index, seats = _match_of(member_id)
    if not seats or len(seats) < 2 or _in_com_game(member_id):
        return
    key = (chan, index)
    seat = next((i for i, (m, _v) in enumerate(seats)
                 if _push_key(m) == _push_key(member_id)), None)
    if seat is None:
        return
    st = _MATCH_TURN.get(key) or {}
    dealt = st.get("turn") is not None
    if st.get("result_sent"):
        return                              # the game is over; nothing to play
    if not dealt and key not in _CARD_READY and key not in _MATCH_STARTED:
        return                              # never started; the room path owns it
    bots = _MATCH_BOTS.setdefault(key, set())
    if _push_key(member_id) in bots:
        return
    if len(_humans(chan, index, seats)) <= 1:
        # The last human is leaving too: nothing is left to play for.
        return
    bots.add(_push_key(member_id))
    n = len(seats)
    _say("tm: 🤖 member %s (seat %d) LEFT %s table %d mid-match (%s) -- the "
         "server plays that seat from here on so %d other player(s) are not "
         "stranded on (0x43, 10)"
         % (member_id, seat, chan, index, why, len(seats) - len(bots)))
    _live_matches_write()
    if not dealt:
        # WARNING: A LEAVE DURING CARD SELECT WITH ONE HUMAN LEFT: TELL THEM.
        # Measured 2026-09-07T15:28Z: the Deck quit at card select, the
        # server dealt its seat a synthetic hand and the tester "received
        # no notification at all" -- a game against a nameless bot with the
        # take flow off. Card select is the one match scene that polls
        # (0x43, 40) @DataError, and on it the client shows "the match is
        # gone", sends @Quit= and leaves cleanly. Three or more humans keep
        # the bot path below. `POL_TM_CARDSELECT_LEAVE=bot` restores it.
        # REPORTED LIVE 2026-09-07 20:00Z: `dataerror` is NOT a clean exit -- its
        # dialog is "Invalid card data detected. Exiting Tetra Master." and it
        # throws the survivor OUT of the title (fired live 18:22Z, right after
        # a clean game's rematch vote). Default is `bot` again until the
        # client's own leave path (@GetAway announced after the pick)
        # is measured; `dataerror` stays selectable.
        _mode = (os.environ.get("POL_TM_CARDSELECT_LEAVE") or "bot") \
            .strip().lower()
        if _mode == "dataerror" and len(_humans(chan, index, seats)) <= 1:
            for _om, _ov in seats:
                if _is_bot(chan, index, _om):
                    continue
                _queue_push(_om, _turn_code(DATAERROR_CMD, 0)
                            + b"@DataError=/No=%d" % _env_int("POL_TM_DATAERROR_NO", 0),
                            "the match is gone (a seat left during card select)",
                            after=0, peer=_MATCH_PEER.get(_push_key(_om)))
                _say("tm:   ...seat %d left during CARD SELECT and member %s is "
                     "the last human: pushing (0x43, %d) @DataError so its "
                     "card-select scene says the match is gone and leaves "
                     "(no synthetic deal; POL_TM_CARDSELECT_LEAVE=bot restores)"
                     % (seat, _om, DATAERROR_CMD))
            return
        if _mode == "getaway" and len(_humans(chan, index, seats)) <= 1:
            # THE CLIENT'S OWN LEAVE PATH (static, 0xC0AB0 read 2026-09-07):
            # a @GetAway received during card select marks the seat (arm
            # 0x104FF6, +0x1A7 = 1); the announcer runs only while the board
            # scene is null, from the state 8 -> 9 transition right after the
            # pick is accepted: "%s" + notice 0x4000041 ("<name> has left"),
            # the seat's hand zeroed, and [obj+0x16D] = 1 when one seat is
            # left -- whose ONLY two readers (0xC3FC0, 0xDA359) skip the turn
            # timer. So the match continues, timer off, and the empty seat is
            # played by the bot path below. /P= is ABSOLUTE (the arm rotates).
            # UNMEASURED LIVE; `bot` (silent) is the default until it is.
            for _om, _ov in seats:
                if _is_bot(chan, index, _om):
                    continue
                _queue_push(_om, _turn_code(GETAWAY_CMD, 0)
                            + b"@GetAway=/P=%d" % seat,
                            "seat %d left during card select (@GetAway)" % seat,
                            after=0, peer=_MATCH_PEER.get(_push_key(_om)))
                _say("tm:   ...seat %d left during CARD SELECT: pushing (0x43, "
                     "%d) @GetAway=/P=%d to member %s -- the client announces "
                     "'<name> has left' after its pick and turns the turn "
                     "timer off; the seat is played from here "
                     "(POL_TM_CARDSELECT_LEAVE=getaway)"
                     % (seat, GETAWAY_CMD, seat, _om))
        # Card select: give the seat a hand and let the deal fire through the
        # SAME arm the client's own pick takes (state, pushes, first turn).
        hands = _MATCH_HANDS.setdefault(key, {})
        if not hands.get(_push_key(member_id)):
            rows = _com_deck_rows(seat)
            hands[_push_key(member_id)] = list(rows)
            hands[("deck", seat)] = list(rows)
            _BOT_SYNTH.add(key)
            _say("tm:   ...seat %d had not chosen yet: dealt it a synthetic "
                 "hand (%s); the take flow is OFF for this match"
                 % (seat, ", ".join(r.split(b"|")[0].decode() for r in rows)))
        if _push_key(member_id) not in (_CARD_READY.get(key) or set()):
            try:
                _handle_line(encode_code(IN_MATCH_CODE | (CARDSELECT_SLOT << 16))
                             + b"@CardSelect=/C=0", peer="bot",
                             member_id=member_id)
            except Exception as exc:
                _say("tm:   ...synthetic @CardSelect= for seat %d failed (%r)"
                     % (seat, exc))
        st = _MATCH_TURN.get(key) or {}
        if (st.get("turn") is not None and st.get("active") == seat
                and st.get("await_ack") is None):
            # The deal announces turn 0 inline (not via `_queue_next_turn`),
            # so when the departed seat was drawn to start, arm the gate here:
            # the human's turn-0 ack releases the server's put.
            st["await_ack"] = int(st.get("turn") or 0)
            st["bot_seat"] = seat
            _say("tm:   ...the departed seat starts: holding its card for a "
                 "human @TurnData= ack")
        return
    pend = _PENDING_BATTLE.get(key)
    if pend and pend.get("actor") == seat:
        pend["bot"] = True
        _say("tm:   ...seat %d was parked on its own @BattleSelect -- "
             "auto-selecting" % seat)
        _advance_placement(chan, index)
        return
    if st.get("active") == seat and st.get("await_ack") is None:
        _bot_move(chan, index, int(st.get("turn") or 0), seat)


def _bot_move(chan, index, turn, active, direct_to=None):
    """Play the departed seat `active` on `turn`.

    Returns the `@PutCard=` body framed for `direct_to` (the acker, whose
    reply carries it -- `_com_play_acked`'s shape) or None. Everyone else
    gets the put on their next reply, battles one reply later, and the next
    turn behind those -- the human `@PutData=` path, seat for seat."""
    key = (chan, index)
    seats = _roster_at(chan, index)
    n = len(seats)
    st = _MATCH_TURN.setdefault(key, {})
    if not seats or not 0 <= active < n:
        return None
    if st.get("bot_played") == turn:
        return None                          # a second acker; already played
    st["bot_played"] = turn
    mid_bot = seats[active][0]
    hands = _MATCH_HANDS.setdefault(key, {})
    hand = hands.get(_push_key(mid_bot)) or []
    board = _MATCH_BOARD.get(key) or {}
    tiles = _board_tiles(n)
    empty = [t for t in range(tiles) if t not in board]
    limit = _env_int("POL_TM_TURN_LIMIT", 5 * n)
    if not hand or not empty:
        _say("tm: 🤖 seat %d has %d card(s) and %d empty tile(s) on turn %d "
             "-- passing the turn" % (active, len(hand), len(empty), turn))
        _queue_next_turn(chan, index, seats, min(turn + 1, limit),
                         (active + 1) % n, roster=seats)
        return None
    rnd_moves = random.Random()
    hand_idx = rnd_moves.randrange(len(hand))
    row = hand.pop(hand_idx)
    tile = rnd_moves.choice(empty)
    st["turn"], st["active"], st["n"] = turn, active, n
    _say("tm: 🤖 departed seat %d (turn %d) plays hand slot %d -- card %s -- "
         "onto tile %d%s. Watch for '---->Recv=PUTCARD' on the other client(s)."
         % (active, turn, hand_idx, row.split(b"|")[0].decode(), tile,
            "" if direct_to is None else " (answering member %s's ack directly)"
            % direct_to))
    direct = None
    for mid, _v in seats:
        if _is_bot(chan, index, mid):
            continue
        seat = next((i for i, (m, _x) in enumerate(seats)
                     if _push_key(m) == _push_key(mid)), 0)
        body = (_turn_code(PUTCARD_CMD, turn)
                + _putcard_body(seats, seat, active, hand_idx, tile, row))
        if direct_to is not None and _push_key(mid) == _push_key(direct_to):
            direct = body
            continue
        _queue_push(mid, body, "the departed seat's card on tile %d" % tile,
                    after=_env_int("POL_TM_PUTCARD_GAP", 0),
                    peer=_MATCH_PEER.get(_push_key(mid)))
    _watch_push(chan, index,
                _turn_code(PUTCARD_CMD, turn)
                + _putcard_body(seats, 0, active, hand_idx, tile, row),
                "the departed seat's card on tile %d" % tile)
    if _env_int("POL_TM_BATTLE", 1):
        if _begin_placement(chan, index, n, active, tile, row, turn, seats,
                            None, mid_bot, seats):
            _PENDING_BATTLE[key]["bot"] = True
            _advance_placement(chan, index)
    elif _env_int("POL_TM_TURNDATA", 1):
        _queue_next_turn(chan, index, seats, turn + 1, (active + 1) % n,
                         roster=seats)
    return direct


def _queue_next_turn(chan, index, seats, turn, active, roster=None, n_solo=None,
                     after=None):
    """Announce turn `turn` to everyone in the match, per recipient.

    WARNING: THE TURN NUMBER IS NOT OURS TO PICK. The client counts it itself at the
    end of every turn (0xC76A2 `inc al` into [obj+0x18D]) and then re-polls
    command 9; `@TurnData` OVERWRITES that counter with our third envelope byte,
    and the next `@PutCard` is compared against it. Send the value the client
    has already reached -- that is `previous + 1` -- or the placement after it
    is refused with `Errot!!Turn%d!=%d`.

    The match ends after `5 * N` turns (0xC7701), so there is nothing to
    announce past that: the client prints `End` and moves to the result scene
    instead of polling 9 again.

    `roster`/`n_solo` are the VS. COM frame: `seats` is EMPTY in a COM game
    (the sixth and seventh seat gates lived in this function -- turn 2+ and
    the result were never announced to a COM game at all), so the caller
    passes who to actually push to (the one human) and the real player count.
    The math stays in `n`; the loops walk `roster`. `after` overrides the
    queue delay -- the COM turn chain needs strictly increasing values so its
    messages arrive in order, one per reply.
    """
    n = len(seats) or int(n_solo or 2)
    roster = list(seats) or list(roster or [])
    limit = _env_int("POL_TM_TURN_LIMIT", 5 * n)
    if turn >= limit:
        # THE MATCH IS OVER, AND THE CLIENT ASKS FOR ONE MORE THING. 0xC7701
        # prints `End` instead of `Next` here and the post-game scene polls
        # (0x43, 13); measured live, both clients sat on that poll for ever.
        _say("tm:   ...that was the last turn (%d of %d = 5 x %d players): the "
             "client prints 'End' and polls (0x43, %d) for @ResultData"
             % (turn, limit, n, RESULTDATA_CMD))
        # ONCE PER MATCH. This branch is reached off the final @PutData, and a
        # re-sent placement must not double-count a game, re-tally a lucky
        # card, or re-push the result. `_remember_match` pops this key, so a
        # new match at the same table re-arms it.
        st = _MATCH_TURN.setdefault((chan, index), {})
        if st.get("result_sent"):
            return
        st["result_sent"] = True
        # THE MATCH IS FINAL HERE, AND THE BOARD IS STILL IN MEMORY. Tally
        # before the result goes out: `_MATCH_BOARD` is popped when the table
        # breaks up, and a lucky card counted after that is counted never.
        _tally_lucky(chan, index, roster, n=n)
        if not _env_int("POL_TM_RESULTDATA", 1):
            return
        rnd = _turn_rand((chan, index))
        scores = _board_scores(chan, index, n)
        stats = {}
        # WHERE EACH SEAT FINISHED, which is the only input `Average Rank` has.
        # Computed over the FULL score vector, not the roster, so a COM game
        # ranks the human against the machine rather than against nobody.
        places = _placements(scores) if scores else []
        for i, (mid, _v) in enumerate(roster):
            stats[_push_key(mid)] = _bump_result_stats(
                mid, scores[i] if i < len(scores) else 0,
                place=places[i] if i < len(places) else None,
                # `n`, not len(roster): a COM game has one human on the roster
                # and the client still counts the machine as an opponent
                # (0xCC79D reads the PLAYER COUNT, which is what /N= carried).
                opponents=max(0, n - 1),
                # This seat's biggest chain this match, keyed by the ABSOLUTE
                # seat -- `_note_combo` records against the actor the placement
                # names, which is the same numbering `roster` is indexed by.
                combo=(_MATCH_COMBO.get((chan, index)) or {}).get(i, 0))
            # ...AND HAND THEM TO THE CLIENT. The stats above land in the
            # collection JSON; the save is what Player Data -> Status reads,
            # and until 2026-08-24 nothing carried them across.
            _save_sync_stats(mid)
            # ...AND GROW THE CARDS THIS SEAT USED (guidebook: "the cards you use
            # will grow too"). The ("deck", seat) copy is the full five that
            # survives play (the per-member hand is popped as cards land); seat i
            # is this roster index. A COM seat owns no collection, so it no-ops.
            _grow_used_cards(
                mid, (_MATCH_HANDS.get((chan, index)) or {}).get(("deck", i)))
        # THE WAGER SETTLES HERE (the wager money -- see the @ComGame= handler
        # for the client-side map), BEFORE the result bodies are built: the
        # settled net gain rides `/D=` occ 1 (`_resultdata_body`'s prize
        # slot), and until 2026-09-02 the settlement ran AFTER the pushes,
        # so the result screen's wager row read the 0 we served -- the
        # wallet moved, the screen did not (the stats bug one message
        # over). Mirror of the client's own arithmetic: decisive win = the
        # pot doubles (0x10BC54, cap 2000) and pays out, so net +wager;
        # decisive loss = the stake stays gone; a draw returns it
        # (INFERRED -- flagged in the handler). Settled once: `staked` is
        # cleared, and @GameExit refunds only what is still staked.
        prizes = {}
        if n_solo and _com_wager_enabled():
            _wentry = _COM_GAME.get(_push_key(roster[0][0])) if roster else None
            _wstk = (_wentry or {}).get("staked")
            if _wstk:
                _wwin = max(range(len(scores)), key=lambda i: scores[i]) \
                    if scores else 0
                _wlose = min(range(len(scores)), key=lambda i: scores[i]) \
                    if scores else 0
                _wmid = roster[0][0]
                _wcap = _env_int("POL_TM_WAGER_CAP", 2000)
                if scores and scores[_wwin] == scores[_wlose]:
                    _set_money(_wmid, money_of(_wmid) + _wstk,
                               "VS. COM draw -- stake returned")
                elif _wwin == 0:
                    _pay = min(2 * _wstk, _wcap)
                    _set_money(_wmid, money_of(_wmid) + _pay,
                               "VS. COM WIN -- pot doubled to %d (cap %d) "
                               "and paid out; net +%d"
                               % (_pay, _wcap, _pay - _wstk))
                    _bump_prize(_wmid, _pay - _wstk, "VS. COM win")
                    prizes[_push_key(_wmid)] = _pay - _wstk
                else:
                    _say("tm:   ...VS. COM LOSS -- the %d stake is forfeit "
                         "(the client's own pot is not repaid on a loss)"
                         % _wstk)
                _wentry["staked"] = None
                _stake_clear(_wmid)
        # ...AND THE PvP POT. Escrow filled at the accept quorum
        # (`_queue_vsgameinit`): a UNIQUE top score takes every stake; a tie
        # at the top refunds each player their own. Settled once (the escrow
        # is popped); the winner's NET gain feeds the prize tally the
        # ranking money lists sort on.
        if not n_solo and _env_int("POL_TM_WAGER", 1) \
                and _env_int("POL_TM_WAGER_PVP", 1):
            _pstakes = _PVP_STAKES.pop((chan, index), None)
            if _pstakes:
                # A departed seat's stake stays in the pot and cannot win it:
                # the top score is taken over the seats still playing.
                _hs = [i for i in range(len(scores))
                       if not (i < len(roster)
                               and _is_bot(chan, index, roster[i][0]))] \
                    or list(range(len(scores)))
                _ptop = max(scores[i] for i in _hs) if scores else 0
                _pwinners = [i for i in _hs if scores[i] == _ptop]
                _pot = sum(a for _m, a in _pstakes.values())
                if len(_pwinners) == 1 and _pot > 0:
                    _pw = _pwinners[0]
                    _pwmid = roster[_pw][0] if _pw < len(roster) else None
                    _pwkey = _push_key(_pwmid)
                    _own = (_pstakes.get(_pwkey) or (None, 0))[1]
                    if _pwmid is not None:
                        _set_money(_pwmid, money_of(_pwmid) + _pot,
                                   "PvP WIN -- takes the %d pot (own stake "
                                   "%d back + %d winnings)"
                                   % (_pot, _own, _pot - _own))
                        _bump_prize(_pwmid, _pot - _own, "PvP win")
                        prizes[_pwkey] = _pot - _own
                    for _k2, (_m2, _a2) in _pstakes.items():
                        _stake_clear(_m2)
                        if _k2 != _pwkey:
                            _say("tm:   ...PvP stake of %d forfeited by "
                                 "member %s" % (_a2, _m2))
                else:
                    for _m2, _a2 in _pstakes.values():
                        if _is_bot(chan, index, _m2):
                            _stake_clear(_m2)
                            _say("tm:   ...PvP stake of %d forfeited by member "
                                 "%s (left the match)" % (_a2, _m2))
                            continue
                        _set_money(_m2, money_of(_m2) + _a2,
                                   "PvP draw/tie -- stake returned")
                        _stake_clear(_m2)
        # WARNING: `roster`, never `seats`: `_resultdata_body` answers `/P=0` for an
        # empty seat list, and /P=0 is the MEASURED divide-by-zero result-screen
        # crash (0x10F6A5). A COM game gets /P=1 -- only the human's own @No0
        # section, which is the one that carries the divisor.
        _a0 = after if after is not None else _env_int("POL_TM_RESULT_GAP", 1)
        _watch_push(chan, index,
                    _turn_code(RESULTDATA_CMD, turn)
                    + _resultdata_body(rnd, roster, 0, scores, stats, prizes),
                    "the result", after=_a0)
        for me, (mid, _v) in enumerate(roster):
            if _is_bot(chan, index, mid):
                continue                # a departed seat reads nothing
            _queue_push(mid,
                        _turn_code(RESULTDATA_CMD, turn)
                        + _resultdata_body(rnd, roster, me, scores, stats,
                                           prizes),
                        "the result",
                        after=_a0,
                        peer=_MATCH_PEER.get(_push_key(mid)))
            _say("tm:   ...result: (0x43, %d) @ResultData= /P=%d to member %s "
                 "(games divisor %d -- 0x10F6A5 divides by it, never 0). Watch "
                 "for '---->Recv=RESULTINFO' and a result screen that LIVES."
                 % (RESULTDATA_CMD, len(roster),
                    mid, stats.get(_push_key(mid), (1, 0))[0]))
        # A MATCH THAT ENDED WITH WATCHERS PRESENT: the players' post-game
        # screen polls (0x43, 36) @Change -- the player-replacement
        # negotiation an observer's channel JOIN arms (measured live
        # 2026-08-22T21:15Z: both players parked on 36+26 after a draw, the
        # first game ever observed). The arm (0x1050A2) sets its success
        # flag on EVERY path, own-seat /P= included, so a no-op
        # @Change=/P=0 (own seat in each recipient's frame) releases the
        # wait without swapping anyone. POL_TM_CHANGE_RELEASE=0 disables.
        if _watchers_of(chan, index) and _env_int("POL_TM_CHANGE_RELEASE", 1):
            for mid, _v in roster:
                _queue_push(mid,
                            _turn_code(CHANGE_CMD, turn) + b"@Change=/P=0/C=0",
                            "post-game @Change release (watchers present)",
                            after=_a0 + 4,
                            peer=_MATCH_PEER.get(_push_key(mid)))
            _say("tm:   ...watchers present: pushing the no-op @Change "
                 "release to %d player(s)" % len(roster))
        # THE TAKE FLOW, decisive games only. Measured 16:49Z: a WINNER's
        # post-@Ready screen polls (0x43,26) @TCList and (0x43,35) @GetAway
        # and hangs without them; a DRAW polls neither and flows to @Continue
        # by itself. /P= is refused when it names the recipient's own seat
        # (0x105060), so it carries the seat being taken FROM -- the loser.
        # The pools are the ("deck", seat) copies kept at @CardSelect time.
        # PvP now included (POL_TM_TAKE_PVP=0 restores the COM-only flow --
        # a decisive PvP game used to park on exactly these polls): the same
        # messages go to every roster member, and the winner's @GetSelect
        # transfers between two REAL collections. POL_TM_TCLIST=0 disables.
        _take_pvp = (not n_solo and roster and len(roster) > 1
                     and _env_int("POL_TM_TAKE_PVP", 1)
                     and (chan, index) not in _BOT_SYNTH)
        if not n_solo and (chan, index) in _BOT_SYNTH:
            _say("tm:   ...no take flow: a departed seat played a SYNTHETIC "
                 "hand, so no card it never owned may change hands")
        if _env_int("POL_TM_TCLIST", 1) and (n_solo or _take_pvp):
            win = max(range(len(scores)), key=lambda i: scores[i]) \
                if scores else 0
            lose = min(range(len(scores)), key=lambda i: scores[i]) \
                if scores else 0
            _bot_win = (not n_solo and win < len(roster)
                        and _is_bot(chan, index, roster[win][0]))
            if _bot_win:
                _say("tm:   ...no take flow: the winning seat %d LEFT the "
                     "match, and a departed seat takes nothing" % win)
            if scores and scores[win] != scores[lose] and not _bot_win:
                hands = _MATCH_HANDS.get((chan, index)) or {}
                pools = [hands.get(("deck", s2)) or [] for s2 in range(n)]
                if all(pools):
                    # The pick needs this after the board is gone: who won,
                    # who lost, every seat's cards by slot, and (for PvP)
                    # WHICH MEMBER sits in each seat -- the loser's card
                    # leaves a real collection. `@GetSelect=` and the COM's
                    # own pick (below) both commit against it, once per
                    # losing seat -- a real transfer, no longer display
                    # only.
                    st["take"] = {"win": win, "lose": lose,
                                  "pools": [list(p) for p in pools],
                                  "roster": [m for m, _v in roster]}
                    ga = (_turn_code(GETAWAY_CMD, turn)
                          + b"@GetAway=/P=%d" % lose)
                    # WHO GETS @GetAway. The arm (0x104FF6, read 2026-09-07)
                    # rotates /P= itself, then MARKS that seat's byte +0x1A7
                    # -- the byte the "Play again?" ctor (0x10B7AB) presets to
                    # DECLINED -- and refuses to mark the recipient's OWN seat
                    # unless its +0x32 == 4. So every recipient that accepts
                    # it sees the loser as "not playing again"; the loser's
                    # own copy is the auto-decline of 2026-09-03 (41d/41e).
                    # `POL_TM_GETAWAY_TO=winner` sends it to the winning seat
                    # only (an A/B: does the loser's take scene need it at
                    # all?); default `all` is the measured-working take.
                    # DEFAULT `none` since 2026-09-07T15:13Z: the winner's take
                    # screen opens and picks without it (measured), and to a
                    # recipient it means "seat P LEFT".
                    _ga_to = (os.environ.get("POL_TM_GETAWAY_TO") or "none").strip().lower()
                    for _si, (mid, _v) in enumerate(roster):
                        if _is_bot(chan, index, mid):
                            continue
                        pk = _MATCH_PEER.get(_push_key(mid))
                        # THE TAKE LIST IN THE RECIPIENT'S OWN FRAME (41c).
                        _fp = _take_frame(pools, _si)
                        tcl = (_turn_code(TCLIST_CMD, turn)
                               + _tclist_body(_fp, _take_occ0(_fp, _si, win)))
                        _queue_push(mid, tcl, "the take list (@TCList)",
                                    after=_a0 + 1, peer=pk)
                        if _ga_to == "none" or (_ga_to == "winner"
                                                 and _si != win):
                            # WARNING: @GetAway IS THE "PLAYER LEFT" NOTICE, not a take
                            # message (2026-09-07 14:59Z): sent to the winner it
                            # showed "Deck left the game", emptied the take
                            # ("NEXT ENEMY", no @GetSelect) and auto-DECLINED
                            # the panel; the loser's scene ran without it.
                            _say("tm:   ...@GetAway withheld from seat %d "
                                 "(POL_TM_GETAWAY_TO=%s)" % (_si, _ga_to))
                            continue
                        _queue_push(mid, ga, "the take seat (@GetAway)",
                                    after=_a0 + 2, peer=pk)
                    _say("tm:   ...take flow: @TCList (%s, rotated per "
                         "recipient) + @GetAway=/P=%d to %s. Watch for "
                         "'---->Recv=TRADELIST'."
                         % ("|".join(str(len(p)) for p in pools), lose, _ga_to))
                    if n_solo and win != 0 and _env_int("POL_TM_TAKE_COM", 1):
                        # THE COM WON: the picks are the server's to make --
                        # the loser's screen polls (0x43, 15) (0xCA481) and
                        # parks without them. ONE PICK PER LOSING SEAT
                        # (mirror of the human-win rule, measured off the
                        # first 3-player win), random slot each, in seat
                        # order; sh is the picker's SEAT (0x107C6F). The
                        # human's card (seat 0 lost) leaves the collection
                        # here -- the push is the commitment.
                        _lsrs = [s2 for s2 in range(n)
                                 if s2 != win and pools[s2]]
                        _gap = _a0 + 3
                        for _sL in _lsrs:
                            slot = random.randrange(len(pools[_sL]))
                            gsel = (_turn_code(GETSELECT_CMD, win)
                                    + b"@GetSelect=/E=%d/H=%d"
                                    % (_env_int("POL_TM_GETSEL_E", 0), slot))
                            for mid, _v in roster:
                                _queue_push(mid, gsel,
                                            "the COM's pick from seat %d"
                                            % _sL,
                                            after=_gap,
                                            peer=_MATCH_PEER.get(
                                                _push_key(mid)))
                            _gap += 1
                            _say("tm:   ...THE COM (seat %d) WON -- picking "
                                 "seat %d's slot %d: card %s"
                                 % (win, _sL, slot,
                                    pools[_sL][slot].split(b"|")[0].decode()))
                            if _sL == 0 and _env_int("POL_TM_TAKE_LOSS", 1):
                                row = pools[_sL][slot]
                                for mid, _v in roster:
                                    _collection_remove_card(
                                        mid, int(row.split(b"|")[0]),
                                        row=row)
                        st["take"]["taken"] = True
                else:
                    _say("tm:   ...no take flow: deck copies missing for %s"
                         % [s2 for s2 in range(n) if not pools[s2]])
        return
    st = _MATCH_TURN.setdefault((chan, index), {})
    st["turn"], st["active"], st["n"] = turn, active, n
    rnd = _turn_rand((chan, index))          # SAME table all match -- see _TURN_RAND
    # /S= is the SCORE (board tile count, recipient-rotated) -- see
    # `_turndata_body` for the measurement. Same knob as the COM chain.
    _scores = (_board_scores(chan, index, n)
               if _env_int("POL_TM_TURN_SCORES", 1) == 1 else None)
    _watch_push(chan, index,
                _turn_code(TURNDATA_CMD, turn)
                + _turndata_body(seats, 0, active, rnd, scores=_scores,
                                 n_solo=n_solo),
                "turn %d" % turn,
                after=(after if after is not None
                       else _env_int("POL_TM_TURNDATA_GAP", 1)))
    for mid, _v in roster:
        if _is_bot(chan, index, mid):
            continue                    # a departed seat reads nothing
        seat = next((i for i, (m, _x) in enumerate(roster)
                     if _push_key(m) == _push_key(mid)), 0)
        _say("tm:   ...turn %d: (0x43, %d) @TurnData= to member %s (seat %d, "
             "/A=%d). Watch for '---->Recv=TURNINFO' then "
             "'GamePlayLink:Turn=%d,Active=%d'."
             % (turn, TURNDATA_CMD, mid, seat, (active - seat) % n,
                turn, (active - seat) % n))
        _rot = (_scores[seat:] + _scores[:seat]) if _scores else None
        _queue_push(mid,
                    _turn_code(TURNDATA_CMD, turn)
                    + _turndata_body(seats, seat, active, rnd, scores=_rot,
                                     n_solo=n_solo),
                    "turn %d" % turn,
                    after=(after if after is not None
                           else _env_int("POL_TM_TURNDATA_GAP", 1)),
                    peer=_MATCH_PEER.get(_push_key(mid)))
    # A DEPARTED SEAT'S TURN: the server plays it. Ack-gated like the COM
    # chain (the GamePlayEnd purge race, see `_queue_com_turns`): the first
    # human `@TurnData=` ack for this turn releases the put as its direct
    # reply (`_bot_move` via the ack arm). `POL_TM_COM_ACK_GATE=0` plays it
    # straight away instead.
    if (not n_solo and roster and active < len(roster)
            and _is_bot(chan, index, roster[active][0])):
        if _env_int("POL_TM_COM_ACK_GATE", 1):
            st["await_ack"] = turn
            st["bot_seat"] = active
            _say("tm:   ...turn %d belongs to departed seat %d -- holding its "
                 "card for a human @TurnData= ack" % (turn, active))
        else:
            _bot_move(chan, index, turn, active)


def _is_com_deck(rec):
    """True for a PackPrm record that is a COM opponent's DECK.

    A deck is kind 0, never sold, and carries a real draw distribution (at
    least one firing row -- a `(level, group, cat, cum)` tuple with cum > 0).
    That is what separates it from a Prize Center record (kind 5, price > 0,
    one named card, all-zero cum) and from a buyable booster (kind 1, priced).
    The distinction matters because `_draw_pack` on a single-card prize record
    deals that ONE card five times.
    """
    return bool(rec) and rec.get("kind") == 0 and not rec.get("price") \
        and any(d[3] > 0 for d in (rec.get("draw") or []))


def _com_deck_ladder():
    """The contiguous run of COM-deck records starting at the base rung.

    25..44 in the shipped file -- twenty decks; record 45 on is the Prize
    Center (kind 5). Computed, not hardcoded, so a different PackPrm.BIN can
    only shorten or lengthen it, never smuggle a prize record in.
    """
    base = _env_int("POL_TM_COM_DECK_BASE", 25)
    ladder = []
    no = base
    while _is_com_deck(_pack_rec(no)):
        ladder.append(no)
        no += 1
    return ladder or [base]


def _com_deck_record(char_index):
    """The PackPrm record number for one COM character's deck.

    WARNING: THE `/Com=` INDEX -> RECORD MAPPING IS UNMEASURED. The character-select
    screen's per-entry table ([obj+0x1CA], builder 0x107239) presumably names
    each character's deck record, but nobody has read it. Until then the guess
    is `POL_TM_COM_DECK_BASE` (default 25, the ladder's first rung) + the
    character index -- but that plain offset walked PAST the deck ladder for
    any index >= 21 and landed on the single-card Prize Center records, so the
    COM was dealt five copies of one card (reported live 2026-09-02: Trade King
    Gohn, `/Com=23` -> record 47 = Maechen x5). We now WRAP within the real
    deck ladder, which guarantees a varied deck for every index. The FLAVOUR
    is still a guess -- a wrong deck is visible, not fatal, and the real fix is
    reading the select table -- but five identical cards is not a flavour, it
    is a broken deck.
    """
    ladder = _com_deck_ladder()
    return ladder[max(0, int(char_index) - 1) % len(ladder)]


def _com_deck_rows(char_index):
    """Five card rows (wire format) for one COM opponent.

    `PackPrm.BIN` carries the COM decks -- kind 0, price 0, never sold:
    record 12 is Cid's (category 4, airships), 27 Quina's (group 3 category 5,
    mascots), and 25..44 read as a difficulty ladder (level bands rising).
    See `_com_deck_record` for the (unmeasured) index -> record mapping.
    """
    rows = _draw_pack(_com_deck_record(char_index), n=5)
    return [b"|".join(b"%d" % v for v in r) for r in rows]


def _queue_com_turns(chan, index, member_id, n, turn, active, after=None):
    """Play every consecutive COM turn from (turn, active), then hand back.

    VERIFIED: THE CLIENT DOES NOT PLAY THE COM -- MEASURED LIVE 2026-08-22T14:31Z.
    With the deal and turn 0 finally landing (`bc3f4d8c`), the human placed a
    card, we announced turn 1 `/A=1`, the client ACKED the announcement and
    then sat polling (0x43, 10) -- it never sends `@PutData=` for a COM seat.
    In network mode the server is the authority for every seat, COM included
    (0xCEE51 skips the client's own resolver outright). So this function is
    the COM player: announce the COM's turn, pick a move from the COM hand
    dealt at @CardSelect time, push `@PutCard=` into the slot the client is
    parked on, battles behind it, and repeat until the turn is the human's
    again -- or the match is over, where `_queue_next_turn` takes the result.

    Every message gets a strictly increasing `after` so it rides its own
    reply, in order -- the drain law, chained. Move choice is uniform random
    over the empty tiles and the COM's remaining hand; `POL_TM_COM_PLAY=0`
    disables the whole player (restoring the parked-scene behaviour for an
    A/B).

    WARNING: THE CHAIN IS ACK-GATED NOW, AND THE 3-PLAYER CRASH IS WHY (measured
    2026-08-22T17:03Z, shim-CASPC.1.log:201272). The client PURGES the store
    at every GamePlayEnd -- 0xC7642 hands (0x43, 0x28/8/0xA/0xC) to 0x101830
    before it advances the turn -- so a `@PutCard` that arrives while the
    PREVIOUS turn's battles are still animating is deleted as a stale record:
    the trace shows the turn-13 put popped with no `Recv=PUTCARD` at
    `GamePlayEnd:Turn=12`, the client then polling (0x43,10) for a message
    that no longer exists, and the turn-14 put failing `0x103876`'s compare
    (`Errot!!Turn13!=14` -> return -1 -> 0xC72A6's "invalid card data
    detected" dialog -> CardGameOutClose). It is a RACE, not a 3-player
    fault -- two chained COM turns just double the puts in flight.

    The gate is the client's own per-turn `@TurnData=` ack (measured: every
    turn, COM turns included, 17:01-17:02Z session): announce the COM's turn
    here, then hold its put/battles until the ack proves the client has
    ENTERED that turn -- past the purge, parked on (0x43,10). @TurnData
    itself (cmd 9) is not in the purge list, so the announcement may arrive
    any time. `POL_TM_COM_ACK_GATE=0` restores the old one-shot chain.
    """
    if not _env_int("POL_TM_COM_PLAY", 1):
        return
    key = (chan, index)
    limit = _env_int("POL_TM_TURN_LIMIT", 5 * n)
    rnd_moves = random.Random()
    a = _env_int("POL_TM_TURNDATA_GAP", 1) if after is None else int(after)
    rnd = _turn_rand(key)
    peer = _MATCH_PEER.get(_push_key(member_id))
    guard = 0
    while guard < 32:
        guard += 1
        if turn >= limit:
            _queue_next_turn(chan, index, [], turn, active,
                             roster=[(member_id, 0)], n_solo=n, after=a)
            return
        st = _MATCH_TURN.setdefault(key, {})
        st["turn"], st["active"], st["n"] = turn, active, n
        # /S= IS THE SCORE -- the per-seat BOARD TILE COUNT (measured static
        # 2026-08-22: 0x103799 stores it into 0x5246580+48k, the field the
        # client's own turn-end tally rebuilds from tile owners; see
        # `_turndata_body`). The hand-remaining counts served before were the
        # live report "score fluctuated, 1 point with 6 cards on the field";
        # the constant 5 before that was the "COM hand refilled" report.
        # POL_TM_TURN_SCORES=0 falls back to the hand counts, =-1 to the
        # constant, both kept as A/B apparatus.
        counts = None
        _sc_mode = _env_int("POL_TM_TURN_SCORES", 1)
        if _sc_mode == 1:
            counts = _board_scores(chan, index, n)
        elif _sc_mode == 0 and _env_int("POL_TM_COM_TURN_COUNTS", 1):
            _h = _MATCH_HANDS.get(key) or {}
            counts = [len(_h.get(_push_key(member_id) if s2 == 0
                                 else ("com", s2)) or [])
                      for s2 in range(n)]
        td = _turn_code(TURNDATA_CMD, turn) + _turndata_body(
            [], 0, active, rnd, scores=counts, n_solo=n)
        _say("tm:   ...turn %d: (0x43, %d) @TurnData= /A=%d /S=%s%s"
             % (turn, TURNDATA_CMD, active % n,
                "|".join(str(v) for v in counts) if counts else "(const)",
                "" if active % n else " -- the human's turn, COM chain done"))
        _queue_push(member_id, td, "turn %d" % turn, after=a, peer=peer)
        # ...AND TO THE WATCHERS. A COM game's turns/puts/battles are authored
        # HERE, not relayed from a client, so without these three pushes an
        # observer of a COM table would see the human's own placements
        # (relayed by the `@PutData=` arm) and nothing the machine does.
        _watch_push(chan, index, td, "turn %d" % turn, after=a)
        a += 1
        if active % n == 0:
            return
        if _env_int("POL_TM_COM_ACK_GATE", 1):
            # Hold the COM's put until the client acks THIS turn -- see the
            # docstring. `_com_play_acked` (called from the @TurnData= ack
            # handler) plays it and re-enters this function for the next turn.
            st["await_ack"] = turn
            _say("tm:   ...COM turn %d announced -- holding its @PutCard for "
                 "the client's own @TurnData= ack (the GamePlayEnd purge race)"
                 % turn)
            return
        hands = _MATCH_HANDS.setdefault(key, {})
        hand = hands.get(("com", active)) or []
        if not hand:
            # The client skips cardless players itself (0xC76A2's advance),
            # so announce-and-move-on mirrors it. With 5 cards each over 5*N
            # turns this should never fire; say so if it does.
            _say("tm: WARNING: COM seat %d has no cards left on turn %d -- "
                 "announced the turn and moved on" % (active, turn))
            turn, active = turn + 1, (active + 1) % n
            continue
        board = _MATCH_BOARD.get(key) or {}
        tiles = _board_tiles(n)
        empty = [t for t in range(tiles) if t not in board]
        if not empty:
            _say("tm: WARNING: no empty tile for COM seat %d on turn %d -- the "
                 "match should already be over; announcing the result"
                 % (active, turn))
            _queue_next_turn(chan, index, [], limit, active,
                             roster=[(member_id, 0)], n_solo=n, after=a)
            return
        hand_idx = rnd_moves.randrange(len(hand))
        row = hand.pop(hand_idx)
        tile = rnd_moves.choice(empty)
        _say("tm: 🤖 COM seat %d (turn %d) plays hand slot %d -- card %s -- "
             "onto tile %d. Watch for '---->Recv=PUTCARD'."
             % (active, turn, hand_idx, row.split(b"|")[0].decode(), tile))
        pc = _turn_code(PUTCARD_CMD, turn) + _putcard_body(
            [], 0, active, hand_idx, tile, row, n_solo=n)
        _queue_push(member_id, pc, "COM seat %d's card" % active,
                    after=a, peer=peer)
        _watch_push(chan, index, pc, "the COM's card on tile %d" % tile,
                    after=a)
        a += 1
        if _env_int("POL_TM_BATTLE", 1):
            fights, _marks = _apply_placement(chan, index, n, active, tile, row)
            rnd8 = _turn_rand(key)
            for att, dfn, res, verdict in fights:
                _say("tm:   ...COM battle: tile %d vs tile %d -- %s takes it"
                     % (att, dfn,
                        {tmbattle.ATTACKER: "the ATTACKER",
                         tmbattle.DEFENDER: "the DEFENDER",
                         tmbattle.DRAW: "NOBODY (a draw)"}[verdict]))
                _bd = (_turn_code(BATTLEDATA_CMD, turn)
                       + _battledata_body(res, att, dfn, rnd8))
                _queue_push(member_id, _bd,
                            "the COM's battle on tile %d" % dfn,
                            after=a, peer=peer)
                _watch_push(chan, index, _bd,
                            "the COM's battle on tile %d" % dfn, after=a)
                a += 1
        turn, active = turn + 1, (active + 1) % n
    _say("tm: WARNING: COM turn chain guard tripped (32 iterations) -- stopped")


def _com_play_acked(chan, index, member_id, n, turn, active):
    """Play ONE ack-released COM turn; return its `@PutCard=` as the DIRECT
    REPLY to the ack that released it.

    The other half of `_queue_com_turns`'s ack gate. The client has just
    acked `turn`, so it is past its GamePlayEnd purge and parked on
    (0x43, 10) -- the put can answer the ack itself (the cmd-9 reader never
    touches a (0x43, 10) body, so the "answer nothing on command 9" rule is
    about the SHAPE of the reply, not its existence). Battles ride the next
    replies (`POL_TM_BATTLE_GAP` -- same law as the human's placement: the
    put MOVES the scene, its own reply must not carry more), and the next
    turn's announcement queues behind them, which re-arms the gate or emits
    the result.
    """
    key = (chan, index)
    peer = _MATCH_PEER.get(_push_key(member_id))
    hands = _MATCH_HANDS.setdefault(key, {})
    hand = hands.get(("com", active)) or []
    limit = _env_int("POL_TM_TURN_LIMIT", 5 * n)
    if not hand:
        _say("tm: WARNING: COM seat %d has no cards left on acked turn %d -- "
             "announcing the next turn instead" % (active, turn))
        _queue_com_turns(chan, index, member_id, n, turn + 1, (active + 1) % n)
        return None
    board = _MATCH_BOARD.get(key) or {}
    tiles = _board_tiles(n)
    empty = [t for t in range(tiles) if t not in board]
    if not empty:
        _say("tm: WARNING: no empty tile for COM seat %d on acked turn %d -- the "
             "match should already be over; announcing the result"
             % (active, turn))
        _queue_next_turn(chan, index, [], limit, active,
                         roster=[(member_id, 0)], n_solo=n)
        return None
    rnd_moves = random.Random()
    hand_idx = rnd_moves.randrange(len(hand))
    row = hand.pop(hand_idx)
    tile = rnd_moves.choice(empty)
    _say("tm: 🤖 COM seat %d (turn %d, ack-released) plays hand slot %d -- "
         "card %s -- onto tile %d, answering the ack directly. Watch for "
         "'---->Recv=PUTCARD'."
         % (active, turn, hand_idx, row.split(b"|")[0].decode(), tile))
    a = _env_int("POL_TM_BATTLE_GAP", 1)
    if _env_int("POL_TM_BATTLE", 1):
        fights, _marks = _apply_placement(chan, index, n, active, tile, row)
        rnd8 = _turn_rand(key)
        for att, dfn, res, verdict in fights:
            _say("tm:   ...COM battle: tile %d vs tile %d -- %s takes it"
                 % (att, dfn,
                    {tmbattle.ATTACKER: "the ATTACKER",
                     tmbattle.DEFENDER: "the DEFENDER",
                     tmbattle.DRAW: "NOBODY (a draw)"}[verdict]))
            _bd = (_turn_code(BATTLEDATA_CMD, turn)
                   + _battledata_body(res, att, dfn, rnd8))
            _queue_push(member_id, _bd,
                        "the COM's battle on tile %d" % dfn,
                        after=a, peer=peer)
            _watch_push(chan, index, _bd,
                        "the COM's battle on tile %d" % dfn, after=a)
            a += 1
    _pc = _turn_code(PUTCARD_CMD, turn) + _putcard_body(
        [], 0, active, hand_idx, tile, row, n_solo=n)
    # The put ANSWERS the ack for the player, so a watcher -- who sent no ack
    # -- can only get it as a push. Ahead of the battles it rides with
    # (after=0): same order the player sees, the put first.
    _watch_push(chan, index, _pc, "the COM's card on tile %d" % tile)
    _queue_com_turns(chan, index, member_id, n, turn + 1, (active + 1) % n,
                     after=a)
    return _pc


COMLIST_CMD = 4                 #: code 0x43 command 4 -- `@ComList`, arm 0x102F74


def _comlist_body(values):
    """`@ComList=` -- the COM opponent list. Arm 0x102F74, `Recv=COMLIST`.

    WARNING: THE MESSAGE THE "CHANGE SETTINGS" BUTTON WAITS FOR, and until
    2026-09-07 this server had never sent one. `@Continue=`'s `/Conf=1` is that
    button (the arm's own comment called it "...but change the settings first"
    and marked the `@ComList` wait INFERRED); a tester took that path three
    times in one night and the client froze every time, sending nothing at all
    afterwards -- because a slot poll is a LOCAL lookup and makes no traffic.

    The format is measured off the parser, not invented:

        0x102FC3  /L=  parsed first  -> [obj+0x18C], the ROW COUNT
        0x103003  /D=  one occurrence per row, `|`-separated like /ID= and /CN=
        0x10300D  each parsed as a DWORD into the table at [0x524B630],
        0x10301C  row i at + i*0x14 + 0x18

    WARNING: WHAT A ROW'S DWORD MEANS IS NOT MEASURED. It is served as the COM
    character index -- the same index space `@ComGame=`'s `/Com=` already
    sends back to us -- because that is the only COM identifier on this wire.
    `POL_TM_COMLIST_VALUES` sweeps it (a comma list) and `POL_TM_COMLIST=0`
    stops sending it at all. The client says whether it landed:
    `---->Recv=COMLIST` (0x51C4AA0).
    """
    vals = list(values)
    return (b"@ComList=/L=%d" % len(vals)
            + b"/D=" + b"|".join(b"%d" % int(v) for v in vals))


def _comlist_values():
    """The opponent indices to advertise. Defaults to our own COM roster."""
    raw = os.environ.get("POL_TM_COMLIST_VALUES")
    if raw:
        out = []
        for part in raw.replace("|", ",").split(","):
            try:
                out.append(int(part.strip(), 0))
            except ValueError:
                pass
        return out or [0]
    return list(range(len(_com_deck_ladder())))


def _comgame_body(member_id, rules):
    """`@ComGameInit=` -- the vs-COM opener, arm 0x102D21.

    One player, no seats and no `/ID=` search: the fields are this player's own
    numbers plus the same seven-value ruleset.

        /RA= -> dword [obj+0x04]      /M=  -> dword [obj+0xC8] (clamped >= 0)
        /CP= -> word  [obj+0x00]      /S=  -> word  [obj+0xEC]

    WARNING: `/CP=` IS THE CARD LEVEL SLOT, AND SENDING 0 LOCKED THE OPPONENT LIST
    (2026-09-08). `0x102DCA mov word [esi], ax` puts this field into struct
    +0x00 -- the SAME word the save parser derives the card level into
    (0x10147F) and the same word the opponent-unlock routine reads
    (`0x101E47`/`0x105160` load it and pass it to `0x110A70`, which marks an
    entry playable only when it is >= that opponent's `PlPrm.BIN` +0x02
    threshold, and otherwise substitutes record 0's placeholder). We sent a
    hardcoded 0, so every VS. COM open told the client its card level was zero
    and every opponent past the first two locked until something recomputed it.
    The live symptom was exactly that: no new opponent appears while you
    play, and one or two are suddenly available after a relaunch (the save
    parse re-derives the real number).

    WARNING: The field is NAMED Card Points and that name is what hid this -- the
    value it lands in is the card level. `POL_TM_VSGAME_CP` still forces a
    number for an A/B; unset, it is now `card_level_of(member)`.
        /R= x7 -> [obj+0xDC] dword, [obj+0xE0..0xE4] bytes, [obj+0xD9] byte
    """
    # WARNING: `/CP=` is CARD POINTS (struct +0x00 <- file +0x38), not money. `/M=` is
    # the wallet (struct +0xC8 <- file +0x34). They are adjacent in the save and
    # were conflated for a while; do not let the balance back into /CP=.
    _cp = os.environ.get("POL_TM_VSGAME_CP")
    if _cp is None or _cp == "":
        _cp_val = card_level_of(member_id)
    else:
        _cp_val = _env_int("POL_TM_VSGAME_CP", 0)
    _say("tm:   ...VS. COM card level = %d (0xB9040 ported; it gates which "
         "opponents unlock against PlPrm.BIN +0x02, and this server sent 0 "
         "until 2026-09-08)" % (_cp_val,))
    return (b"@ComGameInit=/RA=%d/M=%d/CP=%d/S=%d"
            % (_env_int("POL_TM_VSGAME_RA", 0), money_of(member_id),
               _cp_val, _env_int("POL_TM_COMGAME_S", 1))
            + b"/R=" + b"|".join(b"%d" % v for v in rules))


def _queue_vsgameinit(member_id):
    """Hand this player the board, once they have accepted the match.

    WARNING: IT MUST NOT RIDE THE `@GameWa=` THAT TRIGGERED IT. That reply is what
    lets the client leave the reservation scene and BUILD the match scene, and
    the match scene is what polls `(0x43, 1)`; a message delivered before its
    poller exists sits unread in a store that the scene change then drains. Same
    fault and same fix as `POL_TM_MATCH41_GAP` -- see the banner above
    `MATCH_41_MSGID`. `POL_TM_VSGAME_GAP=0` restores the same-batch behaviour
    for an A/B.
    """
    if not _env_int("POL_TM_VSGAME", 1):
        return
    try:
        import tmroom
        chan = tmroom.room_of(member_id) if member_id is not None else None
        # WARNING: `chan` is allowed to be None here and must NOT be an early return:
        # the player has just LEFT the room to enter the match, so `room_of`
        # legitimately answers nothing. `_table_of_member` falls back to the
        # announced-match snapshot, which carries the room with it.
        index, seats = _table_of_member(chan, member_id)
        if index is None or not seats:
            _say("tm:   no @VsGameInit -- member %s is at no table and no "
                 "announced match remembers them" % (member_id,))
            return
        chan = chan or _MATCH_ROSTER.get(_push_key(member_id), (None,))[0]
        if len(seats) < 2:
            # 0x1024D5's own refusal, raised here rather than on the client where
            # it costs a `StartAloneERROR` and a scene that never starts.
            _say("tm:   no @VsGameInit -- table %d has %d seat(s) and /N= must be"
                 " > 1 (0x1024D5 -> StartAloneERROR)" % (index, len(seats)))
            return
        # SHARED STATE, not the process cache: a match started after a restart
        # would otherwise be played under default rules instead of the table's.
        # `_vsgame_rule_seven`, NOT `_rule_seven`: @VsGameInit's parser reads
        # `/R=` in a different slot order and `_rule_seven`'s @Tet order put the
        # timer in the wrong slot -> every PvP game ran at 0:30 (see
        # VSGAME_RULE_KEYS_PARSED).
        rules = _vsgame_rule_seven(_table_rules_get(chan, index))
        # WARNING: NOT just this member. The board goes to everyone, once everyone has
        # accepted -- see `_MATCH_ACCEPTS` for the measurement that forced it.
        _pvp_staked = False
        for mid in _note_accept(chan, index, member_id, seats):
            seat = next((i for i, (m, _v) in enumerate(seats)
                         if _push_key(m) == _push_key(mid)), None)
            if seat is None:
                continue
            body = encode_code(VSGAME_MSGID) + _vsgame_body(seats, seat, rules)
            _say("tm: pushing (0x43, 1) @VsGameInit to member %s -- %s table %d, "
                 "seat %d of %d, /R=%s. The client's OWN trace is the "
                 "measurement: '---->Recv=PLGAMEINIT' then 'ID=%d'."
                 % (mid, chan, index, seat, len(seats),
                    ",".join(str(v) for v in rules), seat))
            # WARNING: NO HOLD-BACK, AND THE QUORUM IS WHY. `POL_TM_MATCH41_GAP`'s
            # rule is "do not arrive before the scene exists"; waiting for the
            # LAST accept already guarantees every player is in that scene, so
            # a further reply of delay buys nothing and costs a lot. Measured
            # 2026-08-20T11:04Z: with a hold-back of 1 the two boards went out
            # **19 s apart** (11:04:09 and 11:04:28) because each rides its own
            # member's next reply and this band only speaks on `@Pong=`. The
            # guest sat on a black screen for those 19 s and then could not
            # start. `POL_TM_VSGAME_GAP=1` restores the old delay.
            band = _MATCH_PEER.get(_push_key(mid))                 if _env_int("POL_TM_VSGAME_PIN_PEER", 1) else None
            _queue_push(mid, body, "the board at table %d" % index,
                        after=_env_int("POL_TM_VSGAME_GAP", 0), peer=band)
            _pvp_staked = True
        # THE PvP WAGER STAKES IN HERE -- once, on the accept quorum (the
        # loop above runs only when the LAST player accepts). `/R=` occ 0 is
        # the table wager (block[30..33], the table-settings panel) and the
        # clients already receive it -- the pot at [obj+0xDC] -- but the
        # server never moved the money, so a PvP win displayed winnings the
        # next save sync confiscated (the last money gap). ESCROW: each
        # member stakes min(wager, balance); the result block hands the whole
        # pot to a UNIQUE winner (net + the losers' stakes), refunds everyone
        # on a tie at the top; @GameExit before a result refunds via the
        # durable staked_wager, and a restart's orphan is refunded by
        # `_stake_recover` -- the same machinery the COM wager proved.
        # /M= in the bodies above is PRE-stake (built before this runs), the
        # same order the COM path uses. POL_TM_WAGER_PVP=0 disables; rematches
        # are UNSTAKED (no new @VsGameInit -- logged when one begins).
        if _pvp_staked and _env_int("POL_TM_WAGER", 1) \
                and _env_int("POL_TM_WAGER_PVP", 1):
            # KEY: THE POT IS NOW A TABLE-LIVED FIGURE, not `rules[0]` read
            # afresh: Double Up moves it between games (`_double_up_pot`) and
            # `@Continue=` carries no field that reports the new one back, so
            # this server has to keep its own copy or the client's panel and
            # our escrow drift apart on the first rematch. A fresh
            # @VsGameInit is a fresh MATCH, so the escalation resets here.
            _pot = _table_pot(chan, index, {"bm": rules[0]}, reset=True)
            _stake_pvp_pot(chan, index, seats, _pot, "wager")
    except Exception as exc:
        # `_say` must never raise from a reply path, and neither must this.
        _say("tm:   @VsGameInit not queued (%r)" % (exc,))

def audit_room_tables(chan, seats_by_index=None):
    """Every way `chan`'s published table state can disagree with the truth --
    READ-ONLY, the checker half of the invariant.

    WARNING: THIS LIST IS THE INVARIANT'S DEFINITION, IN ONE PLACE: *table state must
    not outlive the presence that created it*, extended to the DERIVED row
    fields the client actually renders from (`row[5]` seat count, `block[4..5]`
    count, `block[14..29]` occupancy id) -- the fields `reconcile_room_tables`'
    stored-row heal has never checked, and per the 0x674C4 switch the exact
    fields that draw "Setting Up" (state 0, count != 0) and Join/owned (state
    1, foreign id) on a table nobody is at. `services/tmtables.py` prints these
    verdicts from inside the container; Phase 2's reconcile is to HEAL from this
    same list. A check that lives anywhere else lets the checker and the healer
    disagree, which is how six one-producer "fixes" happened.

    Returns `[{"table": idx, "kind": slug, "severity": "bad"|"info",
    "detail": line}, ...]`; empty means CLEAN. Never raises -- an unreadable
    room returns a single `audit-failed` entry, because silence is what let
    every earlier producer hide.

    `seats_by_index` defaults to the SHARED store (`tmroom.seats`), so a
    standalone audit reads the same truth both containers share; in-process
    callers may pass `_seats_of(chan)` to audit what THIS process believes.
    Presence evidence (live sessions) is only consulted when `responders` is
    already loaded -- i.e. inside a serving band. A standalone process has an
    EMPTY presence registry, and judging seats "unbacked" against it would
    manufacture exactly the false absence the d1bd5100 bounce guard exists to
    prevent; standalone, those checks degrade to `severity: "info"`.
    """
    out = []

    def note(idx, kind, detail, severity="bad"):
        out.append({"table": idx, "kind": kind, "severity": severity,
                    "detail": detail})
    try:
        import tmroom
        if seats_by_index is None:
            seats_by_index = tmroom.seats(chan)
        seats_by_index = {int(i): [(int(m), int(x or 0)) for m, x in (s or [])]
                          for i, s in (seats_by_index or {}).items()}
        recorded = {int(m) for m, _v in tmroom.members(chan)}
        rows = dict(tmroom.tables(chan))
        try:
            import titles
            sessions_known = titles.core.bound
        except ImportError:
            sessions_known = False

        # --- seats vs presence, and seats vs the published rows -------------
        for idx, seats in sorted(seats_by_index.items()):
            if seats and ("#TM0T%03d" % idx) not in rows:
                note(idx, "seats-without-row",
                     "the server holds %d seat(s) here but publishes NO row -- "
                     "every client sees the authored free tile while the next "
                     "@GameEN= is refused" % len(seats))
            for mid, ident in seats:
                place = tmroom.room_of(mid)
                has_record = mid in recorded
                alive = None
                if sessions_known:
                    try:
                        alive = bool(titles.core.PRESENCE.sessions_for(mid))
                    except Exception:
                        alive = None
                if place and place != chan:
                    # The one release_seats_elsewhere exists for -- if it shows
                    # up here, that path missed an event.
                    note(idx, "seat-holder-elsewhere",
                         "member %s holds a seat here but their record says "
                         "they are in %s" % (mid, place))
                elif not has_record and alive is False:
                    note(idx, "seat-unbacked",
                         "member %s holds a seat with no room record AND no "
                         "live session -- the reconcile sweep's own definition "
                         "of absent" % mid)
                elif not has_record:
                    note(idx, "seat-off-room",
                         "member %s holds a seat with no record in this room; "
                         "presence %s -- legitimate if they are mid-navigation "
                         "(reservation persistence), stale otherwise"
                         % (mid, "is live" if alive else
                            "is unknown from this process"),
                         severity="info")

        # --- the member records' seated letters vs the seat list ------------
        # block[8]/block[9] of a record's status block are what the client's
        # Start-Game gate reads (+0x48/+0x4C); a 'D' with no seat behind it is
        # drift. Measured on prod 2026-08-21: release_seats re-marked BEFORE
        # saving the seat list, so every release left the letters seated.
        seated_members = {int(m) for s in seats_by_index.values() for m, _i in s}
        for mid, vals in tmroom.members(chan):
            v10 = vals[10] if len(vals) > 10 else None
            if not isinstance(v10, str):
                continue
            if tmroom._mark_status(v10, int(mid) in seated_members) != v10:
                note(None, "member-letters-drift",
                     "member %s's record letters disagree with their seat "
                     "(seated=%s) -- the Start-Game gate reads these"
                     % (mid, int(mid) in seated_members))

        # --- the published rows vs the seat list ----------------------------
        for name, row in sorted(rows.items()):
            if not name.startswith("#TM0T"):
                continue
            try:
                idx = int(name[5:])
            except ValueError:
                note(None, "unparseable-name",
                     "stored row %r cannot be indexed" % name)
                continue
            seats = seats_by_index.get(idx) or []
            authored = tmroom.fixture_table(idx)
            authored_state = (tmroom._as_int(authored[3]) if authored
                              else tmroom.EMPTY_TABLE_STATE)
            state = tmroom._as_int(row[3])
            if tmroom._as_int(row[2]) < tmroom.TABLE_ID_MIN:
                note(idx, "colliding-id",
                     "row id %s is below TABLE_ID_MIN -- collides with member "
                     "ids on the client" % row[2])
            elif (tmroom._room_no(chan)
                  and tmroom._as_int(row[2])
                  == tmroom.canonical_table_id(idx - 1, 0)):
                # Measured live 2026-08-21: the client's my-reservation global
                # matched table 1 in EVERY room because all rooms shared the
                # pre-fold id -- the yellow tile that follows a player around.
                note(idx, "room-shared-id",
                     "row id %s is the PRE-FOLD canonical -- identical in "
                     "every room, so one client's my-reservation matches this "
                     "table everywhere" % row[2])
            if not seats and state != authored_state:
                note(idx, "row-occupied-nobody",
                     "state %s with no seat behind it (authored state is %s) "
                     "-- the classic phantom" % (state, authored_state))
            if seats and state == authored_state:
                note(idx, "row-free-somebody",
                     "%d seat(s) held but the row still says the authored "
                     "state %s -- no client can see the reservation"
                     % (len(seats), authored_state))
            row_count = tmroom._as_int(row[5])
            if row_count != len(seats):
                note(idx, "seat-count-drift",
                     "row[5] says %d seated, the seat list holds %d"
                     % (row_count, len(seats)))
            blk = row[6] or ""
            if len(blk) >= 30:
                cnt = occ = None
                try:
                    cnt = tmroom.unletters(blk[4:6])
                    occ = tmroom.unletters(blk[14:30])
                except Exception:
                    note(idx, "block-undecodable",
                         "block %r does not letter-decode" % blk[:30])
                if cnt is not None and cnt != len(seats):
                    note(idx, "block-count-drift",
                         "block[4..5] says %d seated, the seat list holds %d "
                         "-- this byte pair gates EVERY arm of the tile "
                         "switch (0x671DD)" % (cnt, len(seats)))
                if occ is not None:
                    want = next((i for _m, i in seats if i), 0)
                    if not seats and occ:
                        note(idx, "ghost-occupancy-id",
                             "block[14..29] carries id 0x%X with nobody "
                             "seated -- renders as somebody's table" % occ)
                    elif seats and want and occ != (want & 0xFFFFFFFFFFFFFFFF):
                        note(idx, "occupancy-id-drift",
                             "block id 0x%X but seat 0's ident is 0x%X -- "
                             "ownership will render wrong" % (occ, want))
                    elif seats and not want and occ:
                        note(idx, "occupancy-id-unbacked",
                             "block id 0x%X but no seat carries an ident "
                             "(the /NN=-less case) -- id kept from a previous "
                             "occupant?" % occ)
            else:
                note(idx, "short-block",
                     "block is %d chars; the client reads 30+ (count at 4..5, "
                     "id at 14..29)" % len(blk))
            try:
                if tmroom.table_confirmed(chan, idx) and not seats:
                    note(idx, "confirm-without-seats",
                         "table is marked rules-confirmed with nobody seated "
                         "-- a future owner inherits a confirm they never "
                         "gave")
            except Exception:
                pass
    except Exception as exc:
        note(None, "audit-failed", repr(exc))
    return out


def reconcile_room_tables(chan):
    """Heal `chan`'s seats and table rows against the roster. Runs on every <DE>.

    WARNING: THE INVARIANT THIS ENFORCES IS THE ONE THE WHOLE DAY KEPT BREAKING:
    **table state must not outlive the presence that created it.** Every stale
    variant live testing reported -- a reservation surviving a restart, a
    "Setting up" tile in a room empty for an hour, a row carrying a state the
    client cannot render or an id from before a fix -- is this invariant broken
    somewhere, and every previous fix repaired ONE producer. This repairs the
    STATE, on an event that is guaranteed to precede anybody looking at it: the
    `<DE>` a client sends on entering the room, before it fetches `b/g/PTL`.

    Three repairs, all idempotent, all published only when something changed:

      * a seat whose member has NO RECORD in the room is released -- the seat
        was created by presence, and the record is presence's source of truth
        (`release_seats` exists for the event-driven case; this is the sweep
        for every event that never fired: dead sockets during downtime, ghost
        expiry, any release path that missed);
      * a table whose seats emptied is republished free, through the same
        `_republish_table` the event path uses, so the TD and the snapshot say
        the same thing;
      * a stored row still carrying a pre-fix defect -- a state the client has
        no arm for, an id small enough to collide -- is republished normalised.
        `note_table` already rewrites the id on the way in; the state is
        clamped here for exactly the row measured live at 22:07 ("#TM0T001",
        state 7, id 0x1, untouched since before both fixes deployed).

    Returns the number of tables republished. `POL_TM_RECONCILE=0` disables.
    """
    if os.environ.get("POL_TM_RECONCILE", "1") != "1":
        return 0
    try:
        import tmroom
        # WARNING: LIVE-SESSION-BACKED, NOT ROOM-RECORD-BACKED (2026-08-20 pm). The
        # first cut swept seats whose member had no record IN THE ROOM -- which
        # contradicts reservation persistence: the client keeps its table across
        # a room exit (it has a Cancel menu for the deliberate case), so walking
        # out retires your room record and the old sweep then freed your table
        # behind your back. A seat is now released here only when its member has
        # no session anywhere on the service -- the same evidence the stale-
        # record retire trusts. Standalone (no responders importable, i.e. the
        # selftests), nobody is judged absent.
        # WARNING: ABSENT = NO SESSION **AND** NO ROOM RECORD, a conjunction -- and the
        # second half went in ten minutes after the first shipped without it.
        # Measured 23:47:59: an authsess recreate dropped every session, the
        # first <DE> in ran this sweep before the other player's client had
        # re-ponged, and his seat was released as "absent" while he sat there.
        # His room RECORD had survived in the file the whole time. A session
        # can blip (deploy bounce, relay reconnect); a session AND record both
        # gone is somebody who actually left the service.
        recorded = {int(m) for m, _v in tmroom.members(chan)}

        def _absent(mid):
            if int(mid) in recorded:
                return False
            # A record that MOVED to another room is absence FROM THIS room:
            # `release_seats_elsewhere` frees the seat on their <DE> into the
            # new room, but that event can land on the other container or be
            # lost to a restart -- the audit's `seat-holder-elsewhere` kind.
            # Records only move on the member's own <DE>, so this cannot fire
            # on a session blip (the d1bd5100 case keeps the record HERE).
            place = tmroom.room_of(mid)
            if place and place != chan:
                return True
            try:
                import titles
                if not titles.core.bound:
                    raise RuntimeError("no core bound (standalone)")
                return not titles.core.PRESENCE.sessions_for(mid)
            except Exception as exc:
                # WARNING: LOUD ON PURPOSE (was a silent `return False`): an import
                # failure here disables the whole sweep while looking exactly
                # like "everyone is present" -- the success-only-log trap.
                _say("tm: WARNING: reconcile %s cannot read presence (%r) -- "
                     "judging member %s PRESENT by default; the absent-seat "
                     "sweep is NOT running" % (chan, exc, mid))
                return False
        # The heartbeat sweep first, so a cancelled/silent holder's seat is
        # already gone by the time the row heal below recomputes -- and so an
        # idle room heals on the next entry even if no table @Pong ever
        # triggers the sweep again.
        expire_silent_seats()
        # ...and the COM tables on the same tick, for the same reason: a room
        # nobody pongs would otherwise keep a dead COM game's "Playing" tile
        # until somebody happened to beat.
        expire_com_tables()
        rooms = _seats_of(chan)
        touched = 0
        freed_any = False
        for idx, seats in sorted(rooms.items()):
            before = list(seats)
            seats[:] = [(m, i) for (m, i) in seats if not _absent(m)]
            if seats == before:
                continue
            gone = [m for m, _i in before if _absent(m)]
            _say("tm: reconcile %s table %d: seat(s) held by departed member(s) "
                 "%s released -- no live session backs them"
                 % (chan, idx, ", ".join(str(m) for m in gone)))
            _republish_table(tmroom, chan, idx, seats)
            heir = _owner_changed(before, seats)
            if heir is not None:
                _push_owner_change(tmroom, chan, idx, heir, list(seats))
            touched += 1
            freed_any = True
        if freed_any:
            _save_seats(chan)
        # A READY TABLE ANNOUNCES ITSELF ON THE NEXT ROOM ENTRY. The launch
        # freeze means the seated clients can produce NO event to retrigger the
        # match after a hold (measured: every control is locked), so a hold that
        # resolves server-side -- a confirm flag restored from stored rules, a
        # deploy interrupting the announce -- needs a trigger that does not come
        # from them. The <DE> this sweep already rides is exactly that: anybody
        # entering the room re-runs the announce, which dedups its own pushes
        # and re-checks its own gates, so a table that already launched or is
        # still unconfirmed is a no-op.
        try:
            need = _env_int("POL_TM_MATCH_AT", 2)
            if need > 1:
                for idx, seats in sorted(rooms.items()):
                    if len(seats) >= need:
                        _announce_match(tmroom, chan, idx, list(seats))
        except Exception as exc:
            _say("tm:   reconcile announce skipped (%r)" % (exc,))
        # THE STORED-ROW HEAL -- driven by `audit_room_tables`, THE definition
        # of a violation, so the checker and this healer cannot disagree
        # (the first slice of the table-state plan). This replaces the
        # old state/id-only check, which was BLIND to the derived fields the
        # client actually renders from: a free table keeping a stale seat
        # count in `block[4..5]` draws display code 2 -- "Setting Up" -- with
        # a clean state, and a stale `block[14..29]` id draws Join/owned. The
        # heal is a republish through `_republish_table`, which recomputes
        # state, row[5] and the block from the seat list (the closest thing
        # to a renderer until Phase 2b lands) -- and note_table inside it
        # rewrites a colliding id and logs the decoded result. It also matters
        # CLIENT-side: the reservation scene's own tick (0x8F6C0) clears the
        # client-local reservation global when our served row stops saying
        # "reserved by me", so healing the row is what releases a parked
        # client -- the only server-reachable clear that exists (the
        # cross-room case has none; that is the shim's job, Phase 3).
        seat_kinds = ("seat-unbacked", "seat-holder-elsewhere", "seat-off-room")
        flagged = {}
        drifted_members = []
        for v in audit_room_tables(chan, rooms):
            if v["severity"] != "bad" or v["kind"] in seat_kinds:
                continue                    # seats were repaired above
            if v["kind"] == "member-letters-drift":
                drifted_members.append(v)
                continue
            idx = v.get("table")
            if idx is None:
                continue
            flagged.setdefault(idx, []).append(v["kind"])
        # Seated-letter drift heals by re-marking the record from the (just
        # saved) seat list -- remark_seated no-ops when nothing changes.
        if drifted_members:
            seated_now = {int(m) for s in rooms.values() for m, _i in s}
            for mid, vals in tmroom.members(chan):
                v10 = vals[10] if len(vals) > 10 else None
                if (isinstance(v10, str)
                        and tmroom._mark_status(v10, int(mid) in seated_now)
                        != v10):
                    if tmroom.remark_seated(mid):
                        _say("tm: reconcile %s: member %s's seated letters "
                             "re-marked to match their seat" % (chan, mid))
                        touched += 1
        for idx, kinds in sorted(flagged.items()):
            _republish_table(tmroom, chan, idx, rooms.get(idx) or [])
            _say("tm: reconcile %s table %d: republished for %s"
                 % (chan, idx, ", ".join(sorted(set(kinds)))))
            touched += 1
        return touched
    except Exception as exc:
        _say("tm:   reconcile of %s failed (%r) -- state stands as it was"
             % (chan, exc))
        return 0


def void_all_seats(member_id, why=""):
    """Drop `member_id` from EVERY table in every room, and republish.

    WARNING: A SEAT MUST NOT OUTLIVE THE SESSION THAT MADE IT -- and a crash plus a
    fast re-login slips past `_roster_retire_on_close`'s "still present" guard
    (the new session exists before the dead one is processed), so the seat
    persists into the fresh session. Measured 2026-08-21T00:39: member 6 seated
    at #TM0R001 table 1 from a crashed match, both accounts re-logged, and the
    stale reservation rendered on both screens with nobody having reserved.

    Called on `@Init=` -- the game-service login, which fires once per session
    and always before any reservation in it. Room-to-room navigation does not
    re-init, so a legitimate "reserve, leave the room, come back" keeps its
    seat; only a fresh session voids it. `POL_TM_INIT_VOIDS_SEATS=0` disables.
    """
    if member_id is None:
        return
    try:
        import tmroom
    except Exception:
        return
    touched_any = False
    for chan in list(_SEATED):
        rooms = _seats_of(chan)
        freed = []
        for idx, seats in rooms.items():
            before = list(seats)
            seats[:] = [(m, i) for (m, i) in seats if m != member_id]
            if seats != before:
                freed.append((idx, before))
        for idx, before in freed:
            _republish_table(tmroom, chan, idx, rooms.get(idx) or [])
            gone = _owner_changed(before, rooms.get(idx) or [])
            if gone is not None:
                _push_owner_change(tmroom, chan, idx, gone,
                                   list(rooms.get(idx) or []))
        if freed:
            _save_seats(chan)
            touched_any = True
            _say("tm: voided member %s's seat(s) in %s%s -- table(s) %s"
                 % (member_id, chan, (" (" + why + ")") if why else "",
                    ", ".join(str(i) for i, _b in freed)))
    if touched_any:
        # This path never re-marked at all -- the voided member's record kept
        # its seated letters (block[8]='D'), which feeds the Start-Game gate.
        try:
            tmroom.remark_seated(member_id)
        except Exception:
            pass
    return touched_any


def release_seats(member_id, chan=None):
    """Take `member_id` off every table in `chan` and republish what that frees.

    WARNING: A SEAT AND A ROOM RECORD ARE DIFFERENT STATE AND THEY WERE DRIFTING.
    Reported 2026-08-20: a tester backed out of the room, came back, and the
    client told them they ALREADY HAD A RESERVATION on table 1 -- while player 2
    could not see it. That is exactly the two halves disagreeing: the PART path
    retires the member RECORD (`tmroom.forget_member`), but the seat list lives
    in this process and the published table row kept their id, so the table said
    "occupied by you" to one client and the roster said "not here" to the other.

    Called from the PART handler BEFORE the record is retired, so `room_of` can
    still place them.
    """
    if member_id is None:
        return
    try:
        import tmroom
        chan = chan or tmroom.room_of(member_id)
        if not chan:
            return
        rooms = _seats_of(chan)
        freed = []
        inherit = []
        for idx, seats in rooms.items():
            before = list(seats)
            seats[:] = [(m, i) for (m, i) in seats if m != member_id]
            if seats == before:
                continue
            freed.append(idx)
            gone = _owner_changed(before, seats)
            if gone is not None:
                inherit.append((idx, gone, list(seats)))
        for idx in sorted(freed):
            _republish_table(tmroom, chan, idx, rooms.get(idx) or [])
        if freed:
            _say("tm: member %s left %s -- released table(s) %s"
                 % (member_id, chan, ", ".join(str(i) for i in freed)))
            _save_seats(chan)
            # WARNING: AFTER _save_seats, not before -- remark_seated recomputes from
            # the SHARED seat list, so re-marking first read the STALE list and
            # left block[8]='D' on the record. Measured on prod 2026-08-21
            # 06:06:10Z: the release ran, the member record kept its seated
            # letters (the audit's member-letters-drift kind).
            try:
                tmroom.remark_seated(member_id)   # back to state 0, emits a PD
            except Exception:
                pass
        for idx, gone, seats in inherit:
            _push_owner_change(tmroom, chan, idx, gone, seats)
        # A SEAT RELEASED UNDER A MATCH IS THE CRASH SIGNAL. Measured
        # 2026-09-06T23:30:45Z: member 18's connection closed a minute into
        # the 3-player freeze, this path freed table 1 ("CHANGED HANDS") --
        # and `_MATCH_ROSTER[18]` stood, so every later game inherited the
        # dead match (the COM-never-plays bug). See `_match_abandoned`.
        _got = _MATCH_ROSTER.get(_push_key(member_id))
        if freed and _got and _got[0] == chan and _got[1] in freed:
            _match_abandoned(member_id, "seat released at %s table %s"
                             % (chan, _got[1]))
    except Exception as exc:
        _say("tm:   seats not released for %s (%r)" % (member_id, exc))


def _match_abandoned(member_id, why):
    """This member is OUT of the PvP match `_match_of` still names them in --
    a crash, a dropped connection, a seat released under them, a new game
    started elsewhere -- and no `@Break=`/`@GameExit=` is coming. Hand the
    seat to the server if the others are still playing, then FORGET the
    member's roster entry; when no human is left, wipe the table's match
    state so the next game there (and this member's next game anywhere)
    starts clean. Returns True when a roster entry was retired.

    WARNING: THE COM-NEVER-PLAYS BUG (2026-09-06T23:35Z).
    After the 3-player freeze member 18's client died; nothing polite ever
    arrived, so `_MATCH_ROSTER[18]` kept the 3-seat lineup for ever (only
    `_remember_match` writes it and nothing popped it). Their VS. COM game
    then read a 3-seat PvP roster at every handler: `@CardSelect=` saw
    through it and dealt, but the `@TurnData=` ack derived `_cn` from the
    stale seats and swallowed the COM's put -- and it persisted across a
    client restart because the state was HERE. The 08-25 case (reserve a
    table, then walk into VS. COM) is the same hole from the front door.

    Three producers call this: `release_seats` when a seat is actually freed
    under the member's match (the drop path), `@GameENC=` (a new COM game),
    and `_remember_match` when the member is seated at a DIFFERENT table.
    `POL_TM_ABANDON_CLEANUP=0` disables all three (and the COM-binding
    release in `_remember_match`).
    """
    if not _env_int("POL_TM_ABANDON_CLEANUP", 1) or member_id is None:
        return False
    key = _push_key(member_id)
    got = _MATCH_ROSTER.get(key)
    if not got:
        return False
    chan, index, seats = got
    try:
        # The others are still in it: the server plays this seat (the same
        # arm `@Break=`/`@GameExit=` take). Needs `_match_of` intact, so
        # BEFORE the pop.
        _seat_departed(member_id, why)
    except Exception as exc:
        _say("tm:   ...seat takeover for member %s failed (%r) -- retiring "
             "the roster entry anyway" % (member_id, exc))
    _MATCH_ROSTER.pop(key, None)
    _run = _MATCH_STARTED.get((chan, index))
    if _run:
        _run.discard(key)
    left = [m for m, _v in seats
            if _push_key(m) != key
            and (_MATCH_ROSTER.get(_push_key(m)) or (None, None))[:2]
            == (chan, index)
            and not _is_bot(chan, index, m)]
    _say("tm: 🧹 member %s is OUT of %s table %s (%s) -- roster entry retired; "
         "%s" % (member_id, chan, index, why,
                 "%d other player(s) still hold the match" % len(left) if left
                 else "no human is left, wiping the table's match state"))
    if left:
        _live_matches_write()
        return True
    for m, _v in seats:
        _MATCH_ROSTER.pop(_push_key(m), None)      # departed/bot entries too
    _reset_for_rematch(chan, index)
    _MATCH_STARTED.pop((chan, index), None)
    _MATCH_BEGAN.pop((chan, index), None)
    _MATCH_ACCEPTS.pop((chan, index), None)
    _accepts_forget(chan, index)
    _MATCH_WATCHERS.pop((chan, index), None)
    _live_matches_write()
    try:
        import tmroom
        _republish_table(tmroom, chan, index, _seats_of(chan).get(index) or [],
                         why="match abandoned (%s), Playing withdrawn" % why)
    except Exception as exc:
        _say("tm:   table %s not returned from Playing (%r)" % (index, exc))
    return True


def release_seats_elsewhere(member_id, keep_chan):
    """Drop any seat this member still holds in a room they are NOT in now.

    WARNING: THE ORPHAN reconcile CANNOT SEE, and the recurring "phantom reserved
    table in a room I just walked into" (measured 2026-08-21: a tester recorded in
    #TM0R003 while still seated at #TM0R002 table 1). A seat is created by
    presence and is meant to die with it, but `reconcile_room_tables` frees a
    seat only when its holder has NO session ANYWHERE -- so a member who walks
    from room A to room B keeps their seat in A (they still have a session), and
    nobody entering A frees it either, because to that sweep the holder is merely
    "in another room", not "gone". Presence-in-the-SERVICE was standing in for
    presence-in-the-ROOM.

    A member can only be seated in the room they are in, so ENTERING `keep_chan`
    is exactly the event that retires every OTHER seat they hold. Driven from the
    `<DE>` room-entry handler AFTER the record is moved to the new room, so this
    and `reconcile_room_tables` ride the one event. Idempotent; `release_seats`
    already republishes each freed table and hands off ownership. Enumerates the
    SHARED seat map (`tmroom`), not this process's lazily-seeded `_SEATED`, so a
    seat in a room this worker has not touched yet is still caught.
    """
    if member_id is None:
        return
    try:
        import tmroom
        keep_id = tmroom.room_id_for(keep_chan) if keep_chan else None
        targets = []
        for room_hex, tbls in (tmroom._live_seats() or {}).items():
            try:
                rid = int(room_hex, 16)
            except (TypeError, ValueError):
                continue
            if keep_id is not None and rid == keep_id:
                continue
            if any(int(m) == int(member_id)
                   for rows in (tbls or {}).values()
                   for m, _i in (rows or [])):
                targets.append("#TM0R%03d" % (rid & 0xFFFFFFFF))
        for chan in targets:
            release_seats(member_id, chan)
        if targets:
            _say("tm: member %s entered %s -- released stale seat(s) left in %s"
                 % (member_id, keep_chan, ", ".join(targets)))
    except Exception as exc:
        _say("tm:   release_seats_elsewhere failed for %s (%r)" % (member_id, exc))


def _confirmed_flag(tmroom, chan, index):
    """Has this table's owner pressed rules-Confirm? Process set first, shared
    flag second -- the same two sources `_announce_match`'s gate reads, so the
    served STATE and the match hold can never disagree about the phase."""
    if (chan, index) in _TABLE_CONFIRMED:
        return True
    try:
        return bool(tmroom.table_confirmed(chan, index))
    except Exception:
        return False


def _table_owner(seats):
    """The member whose id the tile is drawing, i.e. the table's owner, or None.

    The SAME PICK AS `_occupancy_block`, on purpose. That function writes the
    first non-zero id in seat order into `block[14..29]` and the client compares
    it against its own guid; if "the owner" here were computed any other way the
    two could disagree, and a disagreement about who owns a table is invisible
    until somebody is stuck in front of it.
    """
    for mid, ident in seats or ():
        if ident:
            return mid
    return None


def _owner_changed(before, after):
    """The member who owned this table and no longer does, or None.

    THE OWNER IS SEAT 0, BY THE SAME RULE THE TILE DRAWS BY. `_occupancy_block`
    publishes the FIRST seat with a non-zero id and nothing else, so ownership is
    not a separate field we could disagree with, and the transfer itself is
    already implicit in any republish. What is never implicit is TELLING the
    people still sitting there -- they are in a scene polling for a message, not
    re-reading a tile.

    None covers all three no-op cases in one test, and each of them matters:
    the owner did not move; nobody is left to inherit (the table simply empties
    and goes back to its authored state); or the heir carried no `/NN=`, which
    `_occupancy_block` already refuses to fabricate an id for -- so there would
    be nothing to put in the message either.
    """
    was, now = _table_owner(before), _table_owner(after)
    return was if (now is not None and was is not None and was != now) else None


def _push_owner_change(tmroom, chan, index, left, seats):
    """Tell whoever is still at table `index` that they have inherited it.

    `@GameOwner` IS AN OWNERSHIP TRANSFER, AND THIS IS THE CASE IT WAS MEASURED
    FOR. See `OWNER_PUSH_CODE`: pushed on a match by mistake, the client rendered
    "Since <blank> has cancelled their game you are now the owner" -- which is
    wrong for a match and exactly right here. It is code 0x1A, and the
    reservation scene the survivors are sitting in polls for it itself
    (`0x84D69: 0x825A0(buf, 0x1A, -1)`), so unlike most of this family there is
    no question about whether anything is listening.

    `/CN=` is the DISPLAY NAME OF THE PLAYER WHO LEFT -- the blank in that
    measured line was our own empty `/CN=`, which is how the field was
    identified. So it is filled from the roster here rather than left empty, and
    a member we hold no name for degrades to the blank the message already
    survives.

    `/NN=` IS THE NEW OWNER'S TETRA MASTER ID, NOT THE LOBBY-BAND GUID -- the
    same 64-bit id `@GameEN=/NN=` carried and `_occupancy_block` writes into the
    tile, which is the one the receiving client compares against itself (see the
    three-ids banner in `tmroom`). Taking it from the seat list is what keeps
    those two the same number by construction.

    Default ON, unlike `POL_TM_OWNER_PUSH` (which gates the match-announce misuse
    and stays off): a switch that has always shipped off is untested code, and
    this is the one place the message is known to be correct.
    `POL_TM_OWNER_HANDOFF=0` restores the silence.
    """
    # WARNING: THE CONFIRM BELONGS TO THE PERSON WHO PRESSED IT (live testing,
    # 2026-08-21 ~13:5xZ: owner left, the heir inherited a state-2 "Playing"
    # table with Observe-only menus on every client -- the departed owner's
    # confirm kept the recruiting state alive with nobody entitled to it).
    # Clearing it here, in the ONE helper every hands-changed path calls,
    # drops the row back to state 1: the heir sees my-table, opens the rules,
    # and confirms for THEMSELVES -- the same rule _republish_table already
    # applies when a table empties. The republish below re-derives the state
    # AFTER the clear, so the flip rides the same TD as the handoff.
    _TABLE_CONFIRMED.discard((chan, index))
    try:
        tmroom.clear_table_confirmed(chan, index)
    except Exception:
        pass
    _republish_table(tmroom, chan, index, seats)
    if not _env_int("POL_TM_OWNER_HANDOFF", 1):
        _say("tm:   POL_TM_OWNER_HANDOFF=0 -- table %d changed hands and nobody "
             "is being told" % index)
        return
    heir = _table_owner(seats)
    ident = next((i for m, i in seats if m == heir), 0)
    name = (tmroom.name_of(left) or "").strip()
    # WARNING: `/CN=` AND `/QN=` ARE `:str` FIELDS -- HEX-ENCODED ASCII. The arm
    # (0x84DB8 / 0x84DEF, read 2026-09-07) decodes both with 0xAAD60, the
    # same hex-pair reader as @VsGameInit's `/CN=`; a raw name decodes to
    # garbage and drew the "<blank> has cancelled" notice.
    body = (encode_code(OWNER_PUSH_CODE)
            + b"@GameOwner=/NN=%016X/CN=%s/QN=%s"
            % (ident & 0xFFFFFFFFFFFFFFFF, _str_field(name),
               _str_field(os.environ.get("POL_TM_OWNER_QN", ""))))
    _say("tm: %s table %d CHANGED HANDS -- member %s (%r) left, member %s "
         "inherits it (/NN=%016X). Pushing @GameOwner to %s."
         % (chan, index, left, name or "?", heir, ident & 0xFFFFFFFFFFFFFFFF,
            ", ".join(str(m) for m, _i in seats)))
    for mid, _ident in seats:
        _queue_push(mid, body, "table %d is theirs now" % index,
                    after=_env_int("POL_TM_OWNER_HANDOFF_GAP", 0))


def _republish_table(tmroom, chan, index, who, why="released"):
    """Push one table's row after its seat list changed underneath us.

    Used when seating somewhere ELSE releases a seat here -- without it the
    vacated table keeps whatever it was last published with, which is the stuck
    "still reserved" row from the live report. Also the row refresh the
    match-start/match-end paths ride (`why` keeps the log honest about which).
    """
    try:
        row, authored = _table_rows(tmroom, chan, index)
        if row is None:
            return
        # A VS. COM game occupies its table without a lobby SEAT, so an empty
        # seat list is not an empty table -- see `_COM_AT`. Substituted HERE
        # rather than in the callers so that every republish path keeps the
        # tile, including one triggered by somebody else's seat change
        # elsewhere in the room.
        who = who or _com_who(chan, index)
        cap = int(row[4] or 0)
        row[5] = str(min(len(who), cap) if cap else len(who))
        row[6] = _occupancy_block(row[6], who)
        row[3] = _occupancy_state(who, (authored or row)[3],
                                  confirmed=_confirmed_flag(tmroom, chan, index),
                                  inplay=True if _match_running(chan, index)
                                  else None)
        if not who:
            # The next owner must confirm for THEMSELVES -- a confirm inherited
            # from the previous owner announces their guests into an open
            # settings dialog, which is the @GameNG= strand the gate prevents.
            _TABLE_CONFIRMED.discard((chan, index))
            # ...and an emptied table's match is OVER, whatever the exit path
            # was (clean @GameExit=, crash, TTL reap). Without this the
            # process-local flag would republish the NEXT group's reservation
            # as "Playing". _occupancy_state already served the authored state
            # above (who is empty), so this cannot change THIS row.
            _MATCH_STARTED.pop((chan, index), None)
            _MATCH_BEGAN.pop((chan, index), None)
            try:
                tmroom.clear_table_confirmed(chan, index)
            except Exception:
                pass
        if tmroom.note_table(chan, row):
            _say("tm:   %s table %d %s -> %s seated"
                 % (chan, index, why, row[5]))
    except Exception as exc:
        _say("tm:   table %d not %s (%r)" % (index, why, exc))


def _table_rows(tmroom, chan, index):
    """`(base, authored)` for one table -- the row to EDIT, and the row to fall
    back to.

    WARNING: EDITING THE FIXTURE IS WHY A RESERVATION VANISHED. Measured live
    2026-08-20: `@GameEN=` seated the player at sequence 3 (state 1 + the id, and
    the other player DID see "Setting Up"), then the `@Tet=`/`@Tab=` pair arrived
    and this module rebuilt the row from `fixture_table()` -- the AUTHORED row --
    publishing state 7 and id 0 at sequence 5. Our own settings handler wiped our
    own occupancy, which is exactly "once it's done, it goes back to showing as
    empty for the other person".

    So `base` is the LIVE row when one exists. `authored` is still returned
    because the state has to be restorable: an empty table must go back to what
    the fixture says, not stay on whatever the last occupant set.
    """
    authored = tmroom.fixture_table(index)
    live = dict(tmroom.tables(chan)).get("#TM0T%03d" % index)
    return (list(live) if live else (list(authored) if authored else None),
            authored)


def _occupancy_state(who, authored, confirmed=False, inplay=None):
    """`+0x14` for a table with `who` at it.

    WARNING: STATE 1 IS A REASONED PICK, NOT A MEASUREMENT, and it is the only part of
    this that is. It is the one arm in the 0x674C4 switch that READS the block's
    id (0x67265), so it is the only state under which an occupancy id can render
    at all; states 3/4/5 return the empty code 2 no matter what we write. The
    authored state is restored the moment the table empties, so a wrong guess
    costs one screen and not a stuck table. `POL_TM_TABLE_STATE` overrides.

    WARNING: THE PHASE MODEL, RESTORED 2026-08-21 (live testing caught the regression):
    a two-part disassembly reconciles the tile arm and the menu arm --

      * TILE arm 0x674C4: state 1's OWNER branch (0x672E4) reads the reservation
        count [0x51def2c] (= screenobj+0x108) -- 0 -> **code 2 (LOCKED)**; state
        2's arm (0x67344) reads the seat COUNT directly -> (n<<4)|3 = 0x23, a
        CLICKABLE two-pawns tile with the my-table frame. So an owner sitting on
        a state-1 table with an empty reservation slot is LOCKED OUT; on state 2
        they are not.
      * MENU arm 0x45ABC: states 1/3/4/5 -> VAR2 (JOIN); state 2/6 -> VAR4 (no
        join). The OWNER gets VAR1 (Start Game) at any state.

    Reconciled phase model, which is the flow that worked live (confirm->state 2,
    7b740142) before it was wrongly reverted (27a9a470):
      * occupied, NOT confirmed -> **state 1** (recruiting): foreign players get
        the JOIN menu, the owner is in the reserve/rules dialogs (not looking at
        the tile), so the state-1 owner-tile lock never bites.
      * occupied, CONFIRMED     -> **state 2** (ready): the owner's tile is the
        clickable two-pawns (reads the count, NOT +0x108) and Start Game shows
        via VAR1; further joins close (the owner confirms once everyone is in).
      * The owner confirms LAST, so 3-player works by joining BEFORE confirm.

    `POL_TM_TABLE_STATE` overrides the recruiting value; `POL_TM_TABLE_STATE_
    CONFIRMED` (default 2) the confirmed one. Start Game still ungreys only when
    the reservation slot fills (gate 0x5C0D0) -- state 2 makes the tile
    reachable; +0x108 makes the button pressable.
    """
    if not who:
        # THE AUTHORED VALUE, VERBATIM. The clamp that stood here is RETRACTED
        # with 6151fa3c (see tmroom.EMPTY_TABLE_STATE): authored 7 IS the
        # client's deterministic empty tile -- display code 0 is pre-stored and
        # `state > 6` skips the only `mov ecx, 2` -- while clamping to 0 handed
        # free tables the state-0 arm's `cmp eax, ebp`, a render that varies
        # with client state. Measured 22:18: the heal TD flipped 7 -> 0 and
        # "Setting up" appeared on a free table moments later.
        try:
            return str(int(os.environ.get("POL_TM_TABLE_EMPTY_STATE",
                                          str(authored)), 0) & 0xFF)
        except (TypeError, ValueError):
            return str(authored)
    # VERIFIED: CONFIRMED -> STAY AT STATE 1 (default flipped 2->1, 2026-08-22). The
    # state-2 flip was a WORKAROUND for +0x108 never filling: at state 1 the
    # owner tile arm 0x672E4 reads +0x108 and LOCKS when it is 0, so state 2 (arm
    # reads the seat count instead) was used to make the tile clickable after
    # confirm. But state 2 gives foreign players VAR4 = NO JOIN -- the guest
    # lockout a tester hit. Now that +0x108 FILLS (proven live 2026-08-22 via
    # tm_store_probe: the msgid-4 re-feed drains into +0x108 once +0x41c arms),
    # the workaround is obsolete: state 1 unlocks the owner tile from +0x108 AND
    # keeps joins open (VAR2), so a guest can still seat and drive +0x108 to >=2,
    # which is what ungreys Start Game (gate 0x5C0D0 needs >=2). Set
    # POL_TM_TABLE_STATE_CONFIRMED=2 to restore the old flip. inplay (>=3 seated,
    # OR a running match -- callers pass _match_running(), set on @GameReady= and
    # cleared on the last @GameExit=) goes to state 2 -- a table mid-game shows
    # "Playing"/Observe and joins close.
    if inplay is None:
        inplay = len(who) >= _env_int("POL_TM_INPLAY_AT", 3)
    if inplay:
        return "2"
    if confirmed:
        try:
            return str(int(os.environ.get("POL_TM_TABLE_STATE_CONFIRMED",
                                          "1"), 0) & 0xFF)
        except ValueError:
            return "1"
    try:
        return str(int(os.environ.get("POL_TM_TABLE_STATE", "1"), 0) & 0xFF)
    except ValueError:
        return "1"


def _occupancy_block(current, who):
    """block[14..29] := the occupant's 64-bit id, or zeros when nobody is there.

    THE ORDER IS MEASURED, not assumed: `letters(value, 16)`, most significant
    nibble first, proved against a verbatim model of the client's own four-group
    unpack at 0x66DBB over 2,004 values. See `tools/tmptl.py`'s banner.

    WARNING: ONE id, one field. Sixteen nibbles is exactly 64 bits; there is no second
    slot here for a second player, whatever a table seats.
    """
    import tmroom                       # services/, so it is always mounted
    block = _block_fit(current, 30).encode("latin1", "replace")
    ident = 0
    for _mid, i in who:
        if i:
            ident = i
            break
    if not who:
        out = bytearray(block)
        out[4:6] = tmroom.letters(0, 2)
        out[14:30] = tmroom.letters(0, 16)
        return bytes(out).decode("latin1")
    if not ident:
        # The client compares this against ITS OWN guid. A member we hold no
        # guid for would get a fabricated one, which is the "an id fed to a
        # consumer is worse than a blank tile" case -- so leave it empty and say
        # so, rather than publish a number nobody will ever match.
        _say("tm:   ...this message carried no /NN= -- occupancy id left EMPTY "
             "(a wrong id is worse than none)")
        return current
    out = bytearray(block)
    out[14:30] = tmroom.letters(ident & 0xFFFFFFFFFFFFFFFF, 16)
    # VERIFIED: block[4..5] IS THE COUNT, AND IT GATES EVERY ARM OF THE SWITCH.
    # Measured 2026-08-20 at 0x671DD after live testing reported the tile still
    # said "Setting Up" with occupancy published:
    #
    #     mov dl, [esi-0x22]      ; block[4]
    #     mov cl, [esi-0x21]      ; block[5]
    #     shl eax, 4 ; add eax, ecx
    #     -> eax = ((block[4]-'A') << 4) + (block[5]-'A')
    #
    # and every arm opens by testing it: `cmp eax, ebp; je 0x67361` -> display
    # code 2, which is the code states 3/4/5 give unconditionally and is what
    # renders "Setting Up". We author 'AA' = 0, so the occupancy branch fell
    # through to the default however good the id was. With the count present,
    # state 1 + a foreign id reaches 0x6730D: 1 -> code 0x11, n -> (n << 4) | 5.
    #
    # WARNING: SAFE TO WRITE ONLY [4..5]. `tools/tmptl.py` warns that a table's label
    # writes [4..13] and that **[6..13] is the PASSWORD field** -- readable text
    # there sets a password nobody can type. The count is the two bytes before
    # it and nothing else moves.
    out[4:6] = tmroom.letters(min(len(who), 0xFF), 2)
    return bytes(out).decode("latin1")


def _settings_block(fields, current):
    """The 64-byte `+0x28` block with the fields we can PLACE written in.

    WARNING: ONLY `lu`/`ll` MOVE, AND THAT IS DELIBERATE. An earlier wire reading
    established that `au`/`al` -- the band that actually decides whether a
    reservation is accepted -- have NO home in `block[30..48]`: its only
    rank-shaped field is `block[38]`, bounded 0..8 by `0x6708C`, which cannot
    hold 100 or 300. The instruction is to settle where they live BEFORE
    authoring, so everything else is carried in the stored set and left out of
    the blob rather than written to a plausible-looking wrong offset.
    """
    import tmroom                       # NOT tools/tmptl -- see its banner
    block = _block_fit(current, 49).encode("latin1", "replace")
    out_pre = bytearray(block)
    # VERIFIED: THE WAGER, 2026-08-22 -- and it is the whole of "the other player sees
    # Wager 0". THE VIEW PANEL DOES NOT READ OUR 0x13 AT ALL. Measured live with
    # `tm_store_probe` while that panel was OPEN: every `0x521ef78[i]+0x30` was
    # 0 and our pushed 0x13 was still sitting unread in the store. The panel is
    # the loop at `0x66D10`, which walks the TABLE RECORD (`0x521BB70 + 0x4E` =
    # `block[0x26]`, `add esi, 0x68` per row) -- so everything it draws comes
    # from THIS block and nothing else. `0x13`/`@Rule=` feeds the OWNER's config
    # panel; they are two scenes over two different channels, and three fixes to
    # the push channel could never have moved this screen.
    # `tools/tmptl.py` decoded the block on 2026-08-17: the wager is
    # `block[30..33]`, masked to 16 bits by `0x66FCC`. We have never written it,
    # so it read as the authored padding -- 0. (Historically it read 48047, the
    # NUL-padding artefact that banner explains; 0 is the same hole, one
    # generation later.)
    bet = fields.get("bm")
    if bet is not None:
        try:
            bet = int(bet)
        except (TypeError, ValueError):
            bet = None
    if bet is not None and 0 <= bet <= 0xFFFF:
        out_pre[30:34] = tmroom.letters(bet, 4)
    elif bet is not None:
        _say("tm:   ...wager %s does not fit the 16 bits 0x66FCC masks it to; "
             "left as authored" % bet)
    # THE TIME LIMIT, `block[35]`, an index into `tmptl.TIME_LIMITS`
    # ("0:15" "0:30" "1:00" "3:00" "5:00" "10:00"); `0x6701F` reads anything
    # above 4 as 5. Shipped on the same evidence that carried `bm`, which is
    # now confirmed live: the NAME, the observed RANGE (every `@Tet=` ever
    # captured carries tl=0, 3 or 5 -- inside 0..5 and nothing else), and the
    # fact that it is the only index-shaped field in the message against the
    # only index-shaped slot in the block. Out of range is left alone rather
    # than clamped, so a wrong reading shows up as "unchanged" rather than as a
    # plausible wrong time.
    lim = fields.get("tl")
    if lim is not None:
        try:
            lim = int(lim)
        except (TypeError, ValueError):
            lim = None
    if lim is not None and 0 <= lim <= 5:
        out_pre[35:36] = tmroom.letters(lim, 1)
    elif lim is not None:
        _say("tm:   ...time limit %s is not an index into TIME_LIMITS (0..5); "
             "left as authored" % lim)
    # THE FOUR RULE BITS, `block[34]` -- MEASURED ONE TOGGLE AT A TIME against
    # the wire with a tester narrating each control (2026-08-22), which is
    # the only reason this is right: the message order (`bm du st cb ca gs tl`)
    # is NOT the bit order, so anything positional would have shuffled three of
    # the four. `tmptl.RULE_BITS` is the bit order and the NAMES line up with
    # the field names once you stop reading the message left to right:
    #     bit 0  double_up       <- du   ("Double Up" on, du 0 -> 1)
    #     bit 1  chance_block    <- cb   ("Chance Blocks" off, cb 1 -> 0)
    #     bit 2  special_tile    <- st   ("Special Tiles" off, st 1 -> 0)
    #     bit 3  rotating_block  <- ca   ("Rotating Blocks" off, ca 1 -> 0)
    # WARNING: AND `gs` IS NOT A RULE BIT. Four toggles accounted for all four bits
    # without it, so the fifth boolean belongs to something else and writing it
    # into this byte would corrupt a rule. It stays unmapped until measured.
    _RULE_FIELD_BIT = (("du", 0), ("cb", 1), ("st", 2), ("ca", 3))
    have = [(k, b) for k, b in _RULE_FIELD_BIT if fields.get(k) is not None]
    if have:
        rules = 0
        for key, bit in _RULE_FIELD_BIT:
            try:
                if int(fields.get(key, 0)):
                    rules |= 1 << bit
            except (TypeError, ValueError):
                pass
        out_pre[34:35] = tmroom.letters(rules & 0x0F, 1)

    # QUITTING, `block[36]` -- and `gs` is the field, which reads like a rule
    # and is not one. Measured at BOTH ENDS of the scale (2026-08-22): the
    # tester set "Substitute" and `gs` went 1 -> 0; set "Loss" and it went
    # 0 -> 2. `tmptl.QUITTING` is ["Substitute", "Invalid", "Loss"], so index 0
    # and index 2 are pinned by measurement and 1 is the only slot left.
    # WARNING: STORED ONE-BASED. `0x67053` does `(v - 1) & 3`, so a stored 0 reads as
    # "Loss" -- writing the raw index would put every table one step wrong AND
    # make "Substitute" read as "Loss", the worst possible off-by-one here.
    quit_v = fields.get("gs")
    if quit_v is not None:
        try:
            quit_v = int(quit_v)
        except (TypeError, ValueError):
            quit_v = None
    if quit_v is not None and 0 <= quit_v <= 2:
        out_pre[36:37] = tmroom.letters(quit_v + 1, 1)
    elif quit_v is not None:
        _say("tm:   ...quitting %s is not an index into QUITTING (0..2); left "
             "as authored" % quit_v)

    # PASSWORD, `block[37]` bit 2. Measured 2026-08-22: the tester turned
    # Password on and `pa` went 0 -> 1 (with the secret itself arriving
    # separately as `pw`). `block[37]` is `(password << 2) | (observe & 3)`, so
    # ONLY BIT 2 IS TOUCHED -- the observe half stays as authored because `in`
    # has never been seen at anything but 0 and is still unmeasured. Writing the
    # whole byte would blank an Observe setting we cannot yet read.
    pw_on, obs = fields.get("pa"), fields.get("in")
    if pw_on is not None or obs is not None:
        try:
            cur37 = (ord(block[37:38]) - 0x41) & 0x0F if len(block) > 37 else 0
        except (TypeError, ValueError):
            cur37 = 0
        # OBSERVE, bits 0-1. Measured: "No Comments Allowed" moved `in` 0 -> 2,
        # and `0x66F7B` reads 3 as 0, so the field is 0..2 and maps straight
        # across. Each half is written ONLY if its field is present, so a
        # message carrying one and not the other cannot blank the other.
        if obs is not None:
            try:
                v = int(obs)
            except (TypeError, ValueError):
                v = None
            if v is not None and 0 <= v <= 2:
                cur37 = (cur37 & ~0x03) | v
            elif v is not None:
                _say("tm:   ...observe %s is out of 0..2 (0x66F7B reads 3 as 0)"
                     "; left as authored" % obs)
        if pw_on is not None:
            cur37 &= ~0x04
            try:
                if int(pw_on or 0):
                    cur37 |= 0x04
            except (TypeError, ValueError):
                pass
        out_pre[37:38] = tmroom.letters(cur37 & 0x0F, 1)

    # VERIFIED: THE COMMENT, `block[38]` -- the "[38] is rank / co has no home" reading
    # is RETRACTED, this time by opening the consumer instead of fitting ranges
    # (2026-08-22, static, TM.dll.unpacked). The earlier field map called [38] "a 0..8
    # field, Average Rank's shape" because 0x6708C clamps it -- but the clamp's
    # consumer is the COMMENT line of the table-info dialog (ctor 0x601B0,
    # labels 'Master' 441 / 'Comment' 451), read end to end:
    #
    #     0x66F5D  cx = (block[38]<<4) | block[37]        the pair build
    #     0x6708C  cmp dl, 8; ja -> 0                     row+0x40 = block[38]
    #     0x60561  id = u16[0x51B2F7C + row[0x40]*2]      the dialog's value
    #              -> Lobby.BIN 385..393 = 'No Comment', 'Tetra buddies
    #                 wanted!', ... 'Flexible rules!'     NINE presets = 0..8
    #
    # `co` is that index: the tester picked "Tetra buddies wanted!" (preset 1)
    # and `co` went 0 -> 1. The 0..8 bound IS the preset count, not a rank. We
    # never wrote [38], so every table drew index 0 = "No Comment" -- the
    # live report "comments don't persist". Out of range is left alone (the
    # client would clamp it to 0 anyway, but "unchanged" beats "silently No
    # Comment").
    com = fields.get("co")
    if com is not None:
        try:
            com = int(com)
        except (TypeError, ValueError):
            com = None
    if com is not None and 0 <= com <= 8:
        out_pre[38:39] = tmroom.letters(com, 1)
    elif com is not None:
        _say("tm:   ...comment %s is not a preset index (0..8); left as "
             "authored" % com)
    # WARNING: `au`/`al` (the rank band, 300/100) STILL have no home in the block --
    # with [38] now taken by the comment there is no rank-shaped slot at all;
    # they are carried in the stored set only (the wire reading stands for them).
    block = bytes(out_pre)
    current = block.decode("latin1")
    lo, hi = fields.get("ll"), fields.get("lu")
    if lo is None or hi is None:
        return current
    if not (0 <= lo <= 0xFFFFF and 0 <= hi <= 0xFFFFF and lo <= hi):
        _say("tm:   ...card level %s-%s is not a range this block can hold; "
             "left as authored" % (lo, hi))
        return current
    out = bytearray(block)
    # The ENCODER's order, which is `tmptl.table_settings`'s: card_max occupies
    # block[39..43] and card_min block[44..48]. The panel draws them the other
    # way round ("Card Level min - max"), which is the trap the earlier field map got
    # backwards once already -- so this follows the writer, not the screen.
    out[39:44] = tmroom.letters(hi, 5)
    out[44:49] = tmroom.letters(lo, 5)
    return bytes(out).decode("latin1")


def _reply_delay(name, default_ms):
    """Hold a reply back before sending it -- see the banner at `@GameM=`."""
    ms = _env_int(name, default_ms)
    if ms > 0:
        time.sleep(min(ms, 30000) / 1000.0)


def _env_int(name, default):
    """An integer knob, tolerant of junk -- a bad env must not kill a reply."""
    try:
        return int(os.environ.get(name, str(default)), 0)
    except ValueError:
        return default


def _gameeca_enabled():
    """`POL_TM_GAMEECA=0` restores the silent control for an A/B.

    Kept for the same reason `POL_TM_GAMEEA` has one: this reply is built from a
    string-pool NEIGHBOURHOOD and an INFERRED msgid, so the ability to put the
    old behaviour back without a redeploy is part of shipping it.
    """
    return os.environ.get("POL_TM_GAMEECA", "1") == "1"


def _gameeca_msgid():
    """The `@GameECA` message code. `POL_TM_GAMEECA_MSGID` overrides the msgid.

    WARNING: INFERRED, NOT MEASURED -- request msgid + 1, which is the rule every
    documented pair in this protocol follows. On this band a wrong msgid is
    silently ignored, exactly like no reply at all, so this knob is the FIRST
    sweep if "Preparing game" does not move: try 0x13, 0x02, 0x12.
    """
    try:
        mid = int(os.environ.get("POL_TM_GAMEECA_MSGID", "0x13"), 0) & 0xFF
    except (TypeError, ValueError):
        mid = 0x13
    return 0x00000041 | (mid << 16)


def _gameeca_en():
    """`/EN=` for `@GameECA`, and it MUST be > 0.

    VERIFIED: READ OFF THE PARSER, after two wrong guesses cost two live rounds
    (TM.dll `0x8538D`):

        push "@GameECA" ; call 0xAA920      ; match the command
        push "/EN="     ; call 0xAB080      ; parse the integer
        cmp  eax, 0
        je   -> [esp+0xc] = 0xFFFFFF9D      ; -99, the FAILURE arm
        jle  -> skip
        jg   -> [0x5228200] = 0xF           ; success, the session advances

    So `@GameECA` is the exact twin of `@GameEA`: same `/EN=` field, same
    "must be positive" rule. `MSG_GAMEEA`'s note that 1 is the value TM.dll
    `0x90114` singles out applies here too, so 1 is the default.

    WARNING: WHAT WAS READ WRONG TWICE, because it is the instructive part: `/TblId=`
    and `/TblNo=` are NOT `@GameECA`'s fields. They belong to **`@MuchMake`**,
    a LATER message in the same session -- the parser at `0x85511` matches
    `"@MuchMake"` (VA 0x51B27DC) and only then reads them, storing TblNo ->
    `[0x52223A8]` and TblId -> `[0x521BB58/5C]`. The first cut read the string POOL's
    neighbourhood (`@GameECA` `/TblId=` `/TblNo=` `@MuchMake` ...) and assumed
    adjacency meant ownership. It does not: the pool is just a pool. Both live
    failures followed from putting the right fields on the wrong message, which
    left `/EN=` absent -> parsed as 0 -> the -99 arm -> Lobby.BIN 448.
    **Adjacency in a string table is a hint about a SUBSYSTEM, never about a
    message's fields. Open the parser.**
    """
    return _env_int("POL_TM_GAMEECA_EN", 1)


def _queue_com_match(member_id, peer_nick=None):
    """Hand a VS. COM player the table, then the start -- the `@MuchMake` pair.

    VERIFIED: WHY THIS EXISTS SEPARATELY FROM `_push_muchmake`. That one is the PvP
    path and fires on SEAT COUNT at a lobby table; a COM game has one human and
    no seated peers, so it can never fire. Live 2026-08-22: with `@GameECA=/EN=1`
    accepted the client runs the rest of the handshake by itself
    (`@CheckJoinTable=` -> `_Ans=/Join=1/Mem=1`, `@Req=` -> `@PLAYACK=/EN=1`)
    and then sits in the COM scene on a bare UI background, drawing nothing.
    Nothing was left to send it.

    WARNING: AND `POL_TM_MUCHMAKE_PUSH` IS DEFAULT-OFF ON PURPOSE -- do not "fix" that.
    It was measured: in the MATCH scene the client never polls 0xD2, so pushing
    there only fills a store nothing drains. That measurement is about a
    DIFFERENT SCENE. `@MuchMake`'s receivers were always known to be real but
    unplaced -- msgid 0 at `0x854B0` (called from `0x6D780` and `0x7B770`) and
    msgid 1 at `0x85790`, reached through the state-machine jump table at
    `0x985F3` -- and the COM scene is the strongest candidate yet for being one
    of them, because it is a scene that demonstrably waits for something. So
    this path gets its own switch (`POL_TM_COM_MUCHMAKE`) and leaves the PvP
    default exactly where its measurement put it.

    WARNING: THE STAGGER IS THE SAME LAW AS EVERYWHERE ELSE HERE: msgid 0 gives the
    client the table, msgid 1 is the start it answers, and msgid 1's poller only
    exists after the client has ACTED on the table. Same batch = the start lands
    in a store before anything looks for it, and a scene change drains it. See
    `_queue_push`'s banner and `POL_TM_MATCH41_GAP`.
    """
    # *** THE BOARD FIRST -- `@ComGameInit` INTO THE SLOT THE CLIENT POLLS. ***
    #
    # VERIFIED: MEASURED, and it is the first thing on this feature that was not a
    # guess. `probe_lookup` on the live client (shim `[tmlog] probe_lookup=1`,
    # the 0x825A0 store-lookup probe) shows the client polling
    # **`code=0x43 msgid=2`** 645 times, "not found", every time -- and
    # `COMGAME_MSGID` IS (0x43, 2). It is asking for `@ComGameInit` and we have
    # never put one there.
    #
    # We already BUILD it (`_comgame_body`, arm 0x102D21) and already answer it
    # when the client sends `@ComGame=` -- but in a VS. COM session the client
    # never sends `@ComGame=`, so the builder was only ever reachable down a
    # path this flow does not take. Hence a push, into the slot it polls.
    #
    # WARNING: AND `@ComGameInit` IS THE RIGHT SHAPE FOR THIS: "one player, no seats
    # and no `/ID=` search", per its own banner -- no second player, no table,
    # no room. That is exactly a COM game, and it is why `_queue_vsgameinit`
    # (the PvP board, `/N=` must be > 1 or `0x1024D5` raises StartAloneERROR)
    # is NOT what a COM game needs.
    if _env_int("POL_TM_COM_GAMEINIT", 1):
        # WARNING: COM ORDER, NOT @Tet ORDER. This board seeds the VS. COM rules screen,
        # and the client parses its /R= as bm|st|cb|ca|tl|du (0x102D21), so the
        # @Tet-order `_rule_seven` served Special Tile OFF / Time Limit 0:30
        # instead of the factory On / 3:00 (confirmed live 2026-09-02). See
        # `_com_rule_seven`.
        rules = _com_rule_seven(None)
        # `peer=` matters: whichever @ComGameInit the client reads LAST is the
        # one whose delivering sender becomes the session pair (0x102D3C). A
        # copy framed from the recipient's own nick would set the pair to the
        # OWN guid and every later in-match push would fail the equality gates.
        _queue_push(member_id,
                    encode_code(COMGAME_MSGID)
                    + _comgame_body(member_id, rules),
                    why="VS. COM: the board (@ComGameInit)",
                    after=_env_int("POL_TM_COM_GAMEINIT_GAP", 1),
                    peer=peer_nick)
        _say("tm: member %s VS. COM -- queued @ComGameInit into (0x43, 2), the "
             "slot probe_lookup measured the client polling 645x and never "
             "finding. /R=%s (POL_TM_COM_GAMEINIT=0 disables)"
             % (member_id, ",".join(str(v) for v in rules)))

    # \U0001f534 AND PRE-LOAD THE LEAVE ACK INTO (0x43, msgid 5), BECAUSE THE PS2
    # NEVER ASKS FOR IT.
    #
    # MEASURED 2026-09-08 off the leave-hang savestate. `CQMComConfig` (the VS.
    # COM settings screen, tick `0x002f3cb0`) leaves like this: at `0x002f4514`
    # it puts a progress dialog on the parent `CQMCardGame` (`+296`), sets
    # percent 0 / ceiling 80, enters its state 4, and then polls
    # `0x003edbe0(0x005D6550, 5)` = **(code 67, msgid 5) = `@Quit`**. On timeout
    # it raises selector 339 -> **`-1-37339`** and parks on that dialog, which is
    # exactly the 80/80 read off the savestate.
    #
    # \U0001f534 THE CLIENT NEVER SENDS `@Quit=` ON THIS PATH. The string
    # `@Quit` (`0x00489CB8`) has exactly ONE reference in `TMaster.pex` and it is
    # the RECEIVE arm `0x003eedd8`; the console only ever parses one. So the
    # `@Quit=` handler in `_handle_line` -- which answers correctly, in the slot
    # the message arrived on -- can never fire here. It has to be a PUSH, the
    # same shape as `@ComGameInit` above.
    #
    # WARNING: msgid 5 is polled by THREE screens (`0x002f3cb0` here,
    # `0x002faa10`, `0x00365280`) and all three raise 339 on timeout, so this is
    # the console's general "leave acknowledged" and the same gap very likely
    # hangs the exit of those screens too.
    #
    # WARNING: Staggered BEHIND the board on purpose: same-batch delivery lands it
    # in the store before anything looks for it, and the client only reads msgid
    # 5 while it is in a leave-wait. `POL_TM_COM_QUIT_PRELOAD=0` disables.
    if _env_int("POL_TM_COM_QUIT_PRELOAD", 1):
        _no = _env_int("POL_TM_MATCHQUIT_NO", 0)
        _b = _env_int("POL_TM_MATCHQUIT_B", 0)
        _queue_push(member_id,
                    encode_code(COMQUIT_MSGID)
                    + (b"@Quit=/No=%d/B=%d" % (_no, _b)),
                    why="VS. COM: the leave ack (@Quit)",
                    after=_env_int("POL_TM_COM_QUIT_GAP", 2),
                    peer=peer_nick)
        _say("tm: member %s VS. COM -- pre-loaded @Quit=/No=%d/B=%d into "
             "(0x43, 5), the slot CQMComConfig state 4 polls when you leave. "
             "The console never SENDS @Quit (one xref in TMaster.pex, the "
             "receive arm), so without this the leave times out with -1-37339. "
             "(POL_TM_COM_QUIT_PRELOAD=0 disables)" % (member_id, _no, _b))

    # \U0001f534 AND THE STEP AFTER THAT: `@GameEA=/Exit=1` INTO (0x41, 0x0E).
    #
    # MEASURED off the second leave-hang savestate, once the `@Quit` above had
    # moved `CQMComConfig` from state 4 to state **6** (proved live: `[+301]`
    # read 5 before the fix and 6 after). State 6 only raises the parent's
    # progress bar 80 -> 100 when event **`0x03001f57`** arrives, and that event
    # is posted by **`CComPart`** (`0x00424600`, the leave counterpart of
    # `CComJoin`) from its state 7.
    #
    # `CComPart` was parked in state **100** with selector **182** in its dialog
    # (`[obj+320]`), raised from its state 1: `0x003b0c70` fetches
    # **(code 0x41, msgid 0x0E)**, matches **`@GameEA`** (`0x00486BD0`) and reads
    # **`/Exit=`** (`0x00486C80`) -- and returns 0 when the message is absent.
    #
    # We already BUILD exactly that (`GAMEEXIT_ANS_MSGID` + `@GameEA=/Exit=1`),
    # but only as a REPLY to `@GameExit=` -- and the console never sent one
    # (authserv.log 00:17: nothing at all after `@PLAYACK`). Its `CComPart`
    # state 0 does reference `@GameExit=` (`0x003b09a4`) and then advances
    # unconditionally, so state 1 polls whether or not anything reached us.
    # Third instance of the same console pattern: it polls a slot instead of
    # asking. `POL_TM_COM_EXIT_PRELOAD=0` disables.
    if _env_int("POL_TM_COM_EXIT_PRELOAD", 1):
        _ex = _env_int("POL_TM_GAMEEXIT_EN", 1)
        _queue_push(member_id,
                    encode_code(GAMEEXIT_ANS_MSGID)
                    + (b"@GameEA=/Exit=%d" % _ex),
                    why="VS. COM: the part ack (@GameEA)",
                    after=_env_int("POL_TM_COM_EXIT_GAP", 3),
                    peer=peer_nick)
        _say("tm: member %s VS. COM -- pre-loaded @GameEA=/Exit=%d into "
             "(0x41, 0x0E), the slot CComPart state 1 polls while leaving. "
             "Without it CComPart parks at selector 182 and never posts "
             "0x03001f57, so the progress bar stays clamped at 80. "
             "(POL_TM_COM_EXIT_PRELOAD=0 disables)" % (member_id, _ex))

    # WARNING: DEFAULT OFF AS OF THE LIVE READ. The `@MuchMake` pair below was
    # delivered cleanly (log 06:32:38/06:32:40, correctly folded TblId) and the
    # scene did not move -- and `probe_lookup` then showed why: the COM scene
    # polls NOTHING but the ambient set, 0xD2 among the codes it never asks for.
    # Kept switchable rather than deleted because the receivers at 0x854B0 /
    # 0x85790 are real and still unplaced, but it is a no-op here and must not
    # read as load-bearing.
    if not _env_int("POL_TM_COM_MUCHMAKE", 0):
        return
    tid_s, no = _gameeca_table(member_id)
    try:
        tblid = int(tid_s, 16)
    except (TypeError, ValueError):
        _say("tm: no COM @MuchMake -- table id %r is not hex" % (tid_s,))
        return
    gap = _env_int("POL_TM_COM_MUCHMAKE_GAP", 2)
    _queue_push(member_id,
                encode_code(MATCH_PUSH_CODE)
                + b"@MuchMake=/TblNo=%d/TblId=%016X" % (no, tblid),
                # after=1, NOT 0. "0 = the very next one" INCLUDES the reply
                # being built right now -- measured on the bench: after=0 put
                # @MuchMake in the same batch as @GameECA, which is precisely
                # the scene-change drain this stagger exists to avoid.
                why="VS. COM: the table", after=1)
    if _env_int("POL_TM_COM_MATCH_START", 1):
        _queue_push(member_id,
                    encode_code(MATCH_START_CODE)
                    + b"@MuchMake=/Start=%d"
                      % _env_int("POL_TM_COM_MATCH_START_N", 1),
                    why="VS. COM: the start", after=1 + gap)
    _say("tm: member %s VS. COM -- queued @MuchMake /TblNo=%d /TblId=%016X "
         "then /Start= %d reply(ies) later (POL_TM_COM_MUCHMAKE=0 disables)"
         % (member_id, no, tblid, gap))


def _gameeca_table(member_id):
    """`(TblId, TblNo)` for **`@MuchMake`** -- NOT for `@GameECA`, see
    `_gameeca_en`. Unused until `@MuchMake` is implemented; kept because the
    room-fold reasoning below is the part that was hard to get right.

    A REAL table in the player's own room, named exactly as that room's
    `b/g/PTL` names it.

    WARNING: THE FIRST VERSION OF THIS WAS WRONG TWICE, and the client said so:
    `@GameECA` was accepted (the `@GameENC=` retry loop stopped) and then the
    session died with Lobby.BIN 448 "Could not play against the computer."
    Both defects point the same way -- we named a table that does not exist:

      1. **`TblNo` was 17**, deliberately "past the 16 playable rows so it
         cannot collide". But the client only ever hears about tables 1..16;
         17 is not a tile it holds. Sibling string 447, "Could not play against
         computer as this table is not yet ready", says this family of error is
         ABOUT THE TABLE.
      2. **`TblId` was the PRE-FOLD id.** `tmroom.canonical_table_id(n, 0)` is
         not what any room serves -- ids are ROOM-FOLDED
         (`canonical_table_id(n, room_no)`) precisely so one room's table 1
         cannot match another's. So even the id for a valid number named a
         table in no room at all. This is the same fold that
         `_normalise_table_ids` exists to apply, and the same class of bug as
         the my-reservation collision it fixed.

    So resolve the member's ACTUAL room and hand back that room's table, with
    the fold applied. `POL_TM_GAMEECA_TBLNO` picks the table number (1-based,
    default 1) and `POL_TM_GAMEECA_TBLID` still force-overrides the id for a
    sweep.

    WARNING: Still unmeasured: whether a COM game wants a lobby table at all, rather
    than a slot on the `'E'` server (whose capacity check at `0x76338` is
    `>= 8`, so a slot index would be 0..7). If a real, correctly-folded table
    still fails, THAT is the next hypothesis -- and it is why the override
    survives.
    """
    no = _env_int("POL_TM_GAMEECA_TBLNO", 1)
    tid = os.environ.get("POL_TM_GAMEECA_TBLID")
    if tid:
        return tid, no
    room_no = 0
    try:
        import tmroom
        chan = tmroom.room_of(member_id) if member_id is not None else None
        room_no = tmroom._room_no(chan) if chan else 0
        if not room_no:
            # SAY SO. room_no 0 falls back to the PRE-FOLD id, which is exactly
            # the value that just failed live -- an id no room serves. If this
            # line appears, the reply is wrong and the member placement is the
            # bug, not the table number.
            _say("tm: \U0001f534 member %s @GameENC= but no room record places "
                 "them -- /TblId= falls back to the PRE-FOLD id, which names a "
                 "table in NO room and is what Lobby.BIN 448 rejected before. "
                 "Fix placement, not the table number." % (member_id,))
        return "0x%016X" % tmroom.canonical_table_id(no - 1, room_no), no
    except Exception:
        # Never let a lookup failure turn into silence -- an unanswered
        # `@GameENC=` is the hang this whole branch exists to end.
        return "0x%016X" % (0x0000002100000000 | (no << 12) | no), no


def encode_code(code):
    """A TM message code -> its 8-char wire header (u32, little-endian, hex)."""
    return struct.pack("<I", code & 0xFFFFFFFF).hex().upper().encode("ascii")


def decode_ebody(body):
    """Split a TM E-body into (code, command_bytes), or (None, body) if it has
    no leading 8-hex-char header. `body` is the payload with the 'G' class char
    already stripped (as _game_notice_reply hands it over)."""
    if len(body) >= 8 and all(c in b"0123456789ABCDEFabcdef" for c in body[:8]):
        code = struct.unpack("<I", bytes.fromhex(body[:8].decode("ascii")))[0]
        return code, body[8:]
    return None, body


class TMState(Enum):
    """Placeholder match lifecycle -- rename/renumber once the protocol is known."""
    CONNECT = 0        # client just dialed the world address
    HELLO = 1          # expected crypto/session handshake (may reuse login K)
    MATCHMAKING = 2    # lobby-of-players / opponent select
    MATCH = 3          # board + card-play exchange
    RESULT = 4         # win/loss, card award
    CLOSED = 5


def describe():
    """One-liner for the harness log."""
    return ("TM0 text protocol: zones, rooms, tables, VS. COM and player matches, "
            "trade, shop, auction, rankings, events.")


def describe_line(body):
    """Human-readable summary of one TM E-body, for the capture log.

    The 8-hex header is a u32 with THREE meaningful bytes, not one -- byte 0 is
    the code, byte 2 the msgid a waiter matches on, byte 3 the shop/channel
    number (see the banner above `MSG_SHOPINIT`). Printing the u32 whole gave
    lines like `code=18088114` for what is really code 178 msgid 0x14.
    """
    code, cmd = decode_ebody(body)
    if code is None:
        return "no-header cmd=%r" % (cmd[:120],)
    codestr = "code=%d" % (code & 0xFF)
    if (code >> 16) & 0xFF:
        codestr += " msgid=0x%02x" % ((code >> 16) & 0xFF)
    if (code >> 24) & 0xFF:
        codestr += " sh=%d" % ((code >> 24) & 0xFF)
    if (code >> 8) & 0xFF:
        codestr += " b1=0x%02x" % ((code >> 8) & 0xFF)
    return "%s cmd=%r" % (codestr, cmd[:120])


def handle_line(body, peer="-", peer_nick=None, member_id=None):
    """Decode one TM0 E-body and return the reply, with any PENDING PUSH behind it.

    VERIFIED: THE PUSH IS WHY THIS WRAPPER EXISTS. `@MuchMake` is server-initiated
    (the command table: `@MuchMakeAns=` is an ANSWER, the only inverted command in the family)
    and this band has no spontaneous send -- every byte we emit is a reply to a
    line the client sent. So a push RIDES one, exactly as `@GameQA` rides the
    `@GameM=` reply and SHOPINIT rides `@ShEnter`. The client drains its message
    store by (code, msgid), not by request/response pairing, which is what makes
    that legal rather than a trick.

    WARNING: `@Pong=` IS THE CARRIER OF LAST RESORT, and it is a good one: the client
    sends one every ~2 s (76 in one measured session) and we answer it with
    silence today, so a match can be announced within two seconds of becoming
    ready without inventing a channel.
    """
    reply = _handle_line(body, peer, peer_nick, member_id)
    pushes = _pending_pushes(member_id, peer_nick)
    if not pushes:
        return reply
    out = ([] if reply is None
           else list(reply) if isinstance(reply, (list, tuple)) else [reply])
    return out + pushes


def _handle_line(body, peer="-", peer_nick=None, member_id=None):
    """Decode one TM0 E-body and return the reply E-body, or None to stay silent.

    `body` is the payload with the leading 'G' class char stripped, mirroring
    janhourou.handle_line. Silence is deliberate for anything we cannot yet
    speak: a logged unknown is a lead, a wrong answer is noise.

    `peer_nick` is the NICK the client addressed its NOTICE to -- the TM0 service
    peer. It is what `/Shm=` is derived from; see `_teach_shm`. Optional, so the
    selftest and any other caller keep working without it.
    """
    code, cmd = decode_ebody(body)

    if b"@Pong=" in cmd:
        # THE RESERVATION KEEP-ALIVE. A @Pong addressed to a TABLE's own peer
        # is the client saying "I still hold this table" (see the _SEAT_ALIVE
        # banner -- measured live 2026-08-22, and its SILENCE is the only wire
        # expression of Cancel Reservation). Stamp it, then let the expiry
        # sweep reap whoever has gone quiet.
        if member_id is not None:
            _hb_chan, _hb_idx = _peer_table(peer_nick)
            if _hb_chan:
                # ...stamped against THE TABLE THIS BEAT NAMED. See _alive_key.
                _SEAT_ALIVE[_alive_key(_hb_chan, member_id,
                                       _hb_idx)] = time.time()
        expire_silent_seats()
        # ...and the same silence frees a VS. COM table whose player died
        # without an `@GameExit=`. See `expire_com_tables`.
        expire_com_tables()
        # VERIFIED: RE-FEED THE RESERVATION LIST every heartbeat for a SEATED player.
        # RE 2026-08-21: the client's reservation list (+0x108/+0x110)
        # and the "table members" sidebar are fed ONLY by a (code 0x41, msgid
        # 0xC) `@GameML` that the reservation branch 0xA940C->0x83B80 polls
        # every frame while +0x41c is armed; the store holds few entries so a
        # one-shot reply is evicted before that branch runs. `@Pong=` arrives
        # ~every 2s on the game band and we answered it with SILENCE, which
        # makes it the perfect carrier: a msgid-0xC reply rides it ALONE (no
        # competing msgid-4 to be grabbed first), lands in the store, and the
        # branch copies it -- filling the sidebar and letting the Start-Game
        # gate see the reserved players. A looker (not seated) gets None, so
        # their slot stays empty and Reserve stays available.
        # `POL_TM_RESV_PONG=0` restores the old silence.
        out = []
        # EVENT SHOP item re-feed: if this member just opened the event shop, its
        # item list (@Data + @Card) is PACED onto the @Pong heartbeat -- the door
        # batch does not take (measured: item count stayed 0). Push here, after the
        # scene has armed its item wait, for the configured number of heartbeats.
        # TRADE-BOARD RE-SERVE: while a trade accept is armed, re-push the
        # entry lines on the heartbeat -- the client's one-shot @TrAns purge
        # (measured 2026-08-24, [tmlog]: RecvCLEAR pops (0xA2,*)) eats any
        # copy delivered before it; the first post-purge copy sticks. Queued
        # via _queue_push, so it rides this very reply through the wrapper.
        _trade_reserve_tick(member_id)
        left = _EVENTSHOP_PENDING.get(member_id, 0)
        if left > 0:
            _EVENTSHOP_PENDING[member_id] = left - 1
            if left - 1 <= 0:
                _EVENTSHOP_PENDING.pop(member_id, None)
            items = _eventshop_item_lines(member_id)
            if items:
                out.extend(items)
                _say("tm: member %s -- event shop items pushed on @Pong (%d push(es) "
                     "left); watch the shim [tmevent] EVENTSHOP itemE field" % (member_id, left - 1))
        if os.environ.get("POL_TM_RESV_PONG", "1") == "1":
            resv = _reservation_reply(member_id)
            if resv:
                out.extend(resv if isinstance(resv, list) else [resv])
        return out or None

    if b"@TeachDV=" in cmd:
        # The opener. Teach the client its default-data version so the
        # "retrieving default data" wait resolves. See the module header.
        if not _teach_enabled():
            return None
        # HOLD THE REPLY. Measured 2026-08-15 from a live pol.exe minidump: the PC
        # client LOGS our answer (`sqMg:recv(...)<E2000000@TeachDVAns/D=1/V=1/...>`)
        # and then leaves every parsed field at its sentinel -- LN D/V still 255,
        # AC/RK/Shm still 0 -- so the message was consumed by the message layer and
        # the handler at TM.dll 0x053c134d never applied it. Same signature as the
        # POLpro race in responders.py: the client marks the operation pending
        # AFTER its send returns, and the result handler drops anything that lands
        # while the state is still 0. We were answering in ~3 ms.
        #
        # `delay=<ms>` in the teach control file, re-read per line, so this is
        # tunable to zero without a restart once the shape is known.
        _teach_delay()
        d, v = _teach_d(), _teach_v()
        return encode_code(MSG_TEACHDVANS) + _teachdvans_body(d, v, peer_nick)

    if b"@Init=" in cmd:
        # THE STEP AFTER THE MANIFEST, and the one that was timing out.
        #
        # Sequence, measured live 2026-08-15T00:10 (authserv.log + lobby.log):
        #   we answer @TeachDVAns  ->  the client fetches b/g/TM0IML  ->  two
        #   seconds later it sends `@Init=/NN=<u64 hex>/HID=0/Tm0=0/Tm1=0/Dm=0/
        #   Vol=0/CN=<content id, hex ASCII>/GID=<content id>/L=0`  ->  we were
        #   silent  ->  it timed out. `@Init=` appears exactly ONCE in the whole
        #   of authserv.log, in the first run where the manifest carried a
        #   NON-ZERO entry u64 -- runs with count=1 but a zero u64 never sent it.
        #   So the manifest chain (count -> flag -> u64) is what unlocks this.
        #
        # Reply shape, read off the parser at TMaster.pex 0x003a9240:
        #   * 0x0029a880(msg, buf, 129, 0) -- the message must present code
        #     **129** (0x81), the usual request+1 (128 -> 129, as 225 -> 226);
        #   * 0x0029c920(buf, "@InitAns", ...) -- the body must contain
        #     `@InitAns`;
        #   * 0x0029ce50(buf, "/Ans=", 0, &out) -- an integer field `/Ans=`,
        #     which the function RETURNS directly.
        # Its caller (0x003b7448 in the default-data state machine, state at
        # object+0x160) does `blez` on that return, so **/Ans= must be > 0** to
        # continue. 1 is the least-committal positive answer; the knob exists
        # because the value may turn out to mean something (a session/table id)
        # rather than a plain OK.
        if not _init_enabled():
            return None
        # PERSIST THE GUILD. The second `@Init=` of a launch carries `/GLD=<n>`
        # -- the choice the player just confirmed -- and it is the ONLY command
        # that can (the getter feeding /GLD= has three callers, the two
        # @Init senders and the gate). Write it to save +0x3B and the guild
        # screen stops drawing, because 0x14C866 reads exactly that byte.
        # `/GLD=0` means "not chosen yet", so it is never written back.
        # VERIFIED: THE PLAYER'S GAME-ID, AND THEY HAND IT TO US THEMSELVES.
        # See `_GAME_IDS`: `/GID=` here is exactly what `@VsGameInit`'s `/ID=`
        # list is matched against, so capturing it once at login is all the
        # board ever needs.
        _note_game_id(member_id, cmd)
        # ...AND THE **POL-ID**, WHICH IS A DIFFERENT ID AND THE ONE THE CHAT
        # ROSTER IS KEYED BY. `@Init=/NN=` is formatted from TM.dll's
        # [0x5242918]/[0x524291C]; 0x14BC50 copies that pair to
        # [0x5245308]/[0x524530C]; and the chat sidebar compares every roster
        # row's id against those two and skips the match as "me" (0x0AFE3).
        # So the row a client recognises as itself is the one carrying exactly
        # the characters it sent here -- which is why `note_pol_id` stores the
        # string, not a parsed integer. See `_chat_roster_body`.
        #
        # WARNING: An earlier capture of this same field lived in `responders.py` and
        # was REMOVED, correctly: it was being fed to `tmroom.note_guid`, whose
        # value is the lobby-band guid+slot the member RECORD's +0x00 carries,
        # and this is not that id. Both are true of the same person at once.
        _note_pol_id(member_id, cmd)
        # A FRESH @Init IS A FRESH SESSION -- void any seat this member still
        # holds from a dead one. See `void_all_seats`: this is the session-death
        # release that a crash + fast re-login slips past the retire guard.
        if os.environ.get("POL_TM_INIT_VOIDS_SEATS", "1") == "1":
            try:
                void_all_seats(member_id, "fresh @Init")
            except Exception as exc:
                _say("tm:   @Init seat void failed (%r)" % (exc,))
        if _guild_persist_enabled():
            m = re.search(rb"/GLD=(\d+)", cmd)
            guild = int(m.group(1)) if m else 0
            if guild and member_id is not None:
                if _save_patch(member_id, SAVE_OFF_GUILD, guild):
                    _say("tm: member %s guild -> %d (save +0x%02X)"
                          % (member_id, guild, SAVE_OFF_GUILD))
        # The `=` after the name is the PC container-parser's key delimiter --
        # see the banner above `_teachdvans_body`. `/Ans=` is unchanged, and the
        # PS2's strstr of `@InitAns` still matches.
        ans = encode_code(MSG_INITANS) + (b"@InitAns=/Ans=%d" % _init_ans())
        # A PENDING TRADE IS SERVED HERE, in the same batch: a trade game-band
        # connection's whole life is handshake -> ~15s idle -> client close,
        # so the @InitAns reply is the ONE carrier guaranteed to ride that
        # socket while the board it feeds still exists. See note_trade_accept.
        serve = _trade_pending_serve(member_id)
        return [ans] + serve if serve else ans

    if b"@Chat=" in cmd:
        # WHO IS IN THIS CHAT? -- msgid 1, asked twenty times in one measured
        # session and answered zero. See the `MSG_CHAT` banner: the msgid is the
        # arm, `1` is the request and `4` is the member list it is waiting for.
        #
        # Every other msgid stays silent HERE and says so, because the other two
        # arms are not ours to answer: msgid 8 is a chat LINE, and lines are
        # relayed as a room NOTICE (a `#`-target, which `_game_notice_reply`
        # hands straight back), never through this dispatcher.
        # `code` is None for a body with no 8-hex header, and a headerless
        # `@Chat=` is not the request -- the sender at 0x07F540 always stamps
        # one. Fold it to "some other arm" rather than raising.
        msgid = ((code or 0) >> 16) & 0xFF
        if msgid != CHAT_MSGID_WHO:
            _say("tm: member %s sent @Chat= msgid 0x%02X -- not the roster "
                 "request (msgid %d); captured, silent"
                 % (member_id, msgid, CHAT_MSGID_WHO))
            return None
        if not _chat_roster_enabled():
            return None
        return _chat_roster_body(member_id)

    if b"@Opt=" in cmd:
        # THE OPTION WRITE-BACK. The client has no save-write API, so this is how
        # a setting the player changed ever reaches storage -- see the banner
        # above `OPT_FIELD_OFFSETS`. Two things happen here and they are
        # independent: we PERSIST the twelve bytes, and we ANSWER, because the
        # sending scene blocks on `@OptAns` and an unanswered `@Opt=` is the same
        # 75-second dead wait that `@Quit=` was.
        if not _opt_enabled():
            return None
        if _opt_persist_enabled():
            moved = _save_patch_many(member_id, _opt_fields(cmd))
            for off, (old, new) in sorted(moved.items()):
                _say("tm: member %s save +0x%03X %d -> %d%s"
                      % (member_id, off, old, new,
                         "  (skip handle linking)" if off == SAVE_OFF_HNSS else ""))
        return encode_code(MSG_OPTANS) + (b"@OptAns=/Ans=%d" % _opt_ans())

    if b"@ShReq=" in cmd:
        # Player Data -> Cards. One field, `/EN=`, and it must be > 0; the
        # client then takes OUR message's sender id as the card-shop endpoint.
        # See the banner above `_sh_enabled`.
        if not _sh_enabled():
            return None
        lines = [encode_code(_sh_code()) + (b"@ShEnter=/EN=%d" % _sh_en())]
        # RESTORE THE COLLECTION, if we are asked to. See the banner above
        # `_COLLECTION_SUFFIX`: recording is free and always on, giving the
        # cards BACK is the unproven half, so it is opt-in.
        if _card_restore_enabled():
            saved = _collection_cards(member_id)
            if saved:
                _say("tm: member %s restoring %d saved card(s)"
                      % (member_id, len(saved)))
                lines.append(_card_body(encode_code(MSG_SHOPCARD), saved))
        # ...and PUSH the shop's opening message straight after it. The client
        # sends nothing more once it has the endpoint -- it drops into
        # `0x101A10(0x13)` and waits -- so SHOPINIT has to be unsolicited. The
        # store is a queue, so arriving before the scene starts waiting is fine;
        # the record simply sits there until the matcher picks it up.
        #
        # WHICH DOOR is decided here rather than below, because it now picks the
        # INIT BODY as well as the AUCMONEY amount -- sending the card shop's
        # `@Init=` to Check Out is what ran the pack-name setup on a screen with
        # no packs. See the banner above `_INIT_ARM_FIELDS`.
        msgid = (code >> 16) & 0xFF
        checkout = msgid == SHREQ_MSGID_CHECKOUT
        arm = _checkout_init_arm() if checkout else "Init"
        amount = _checkout_money_for(member_id) if checkout else None
        if _shopinit_enabled():
            if checkout:
                _say("tm: member %s @ShReq= msgid 0x%02X = auction Check Out -- "
                      "SHOPINIT body is @%s= (POL_TM_CHECKOUT_INIT). Read the "
                      "client's own '---->Recv=' line to see which arm it ran; "
                      "@Init= is the CARD SHOP arm and is what printed "
                      "Now1stPackName='s Pack on this screen."
                      % (member_id, msgid, arm))
            lines.append(encode_code(MSG_SHOPINIT)
                         + _init_body(arm, member_id, amount))
        # ...AND THE WALLET, BUT ONLY AT THE CARD SHOP. `AUCMONEY` is the only
        # message that carries money; SHOPINIT parses `/M=` and discards it. See
        # `_aucmoney_line`. It has to follow SHOPINIT, because SHOPINIT is what
        # sets the shop number this message's gate compares against.
        #
        # msgid 0x01 is the auction Check Out door, where this same `/M=` is
        # announced as the proceeds of a card sale -- see the banner above
        # `SHREQ_MSGID_CHECKOUT` for the two live measurements that establish it.
        if checkout and _aucmoney_shop_only():
            # The reproduction path for the measurement, NOT a fallback: this is
            # what hung the load. See the banner above `SHREQ_MSGID_CHECKOUT`.
            _say("tm: member %s @ShReq= msgid 0x%02X = Check Out -- WITHHOLDING "
                  "AUCMONEY (POL_TM_AUCMONEY_SHOP_ONLY=1). This is measured to "
                  "hang the screen before it renders; unset it to serve /M=%d."
                  % (member_id, msgid, _checkout_money()))
        else:
            if checkout:
                _say("tm: member %s @ShReq= msgid 0x%02X = auction Check Out -- "
                      "serving /M=%d (proceeds awaiting collection), NOT the "
                      "wallet (%d). The wallet here is what announced a card "
                      "sale that never happened."
                      % (member_id, msgid, amount, money_of(member_id)))
            money_line = _aucmoney_line(member_id, amount)
            if money_line:
                lines.append(money_line)
            if checkout:
                # AND THE CARDS. The Check Out scene waits on TWO commands --
                # 0x20 for the money and 0x21 for the cards (TM.dll 0x13498B and
                # 0x134F97) -- so serving only AUCMONEY answers half the screen.
                # After AUCMONEY because that is the order the scene's own
                # `Recv=` trace shows, and because SHOPINIT sets the shop number
                # both messages' gates compare against.
                # SETTLE FIRST. The sweep runs off the class-A path, and
                # Check Out arrives on class G -- so a player who opens Check
                # Out without touching the auction list would be told they are
                # owed nothing by a store that had never been brought up to
                # date. Late import: tetramaster is imported BY tmtitle.
                try:
                    import tmtitle as _r
                    _r._auction_sweep()
                except Exception as _e:                      # pragma: no cover
                    _say("tm: member %s Check Out -- sweep failed (%s); "
                          "delivering whatever is already pending"
                          % (member_id, _e))
                # WARNING: RE-READ THE MONEY. `amount` was computed before this block,
                # so it predates the sweep -- which is exactly when a sale gets
                # credited. Measured: a seller credited 20 was served /M=0 in
                # the same reply that queued their cards correctly.
                amount = _checkout_money_for(member_id)
                if money_line and amount:
                    lines[lines.index(money_line)] = _aucmoney_line(
                        member_id, amount)
                # *** THE FOUR-SECTION STREAM (default). The scene runs four
                # sub-scenes -- money A, cards A, cards B, money B -- each
                # consuming its own 0x20/0x21 record from the store queue, and
                # it has NO base UI: an unanswered section is a black screen.
                # NO @Dead, EVER: 0x27 is the scene's error channel -- an
                # answered @Dead in any state raises dialog 221 ("Failed to
                # connect to server.", TRM-37221) and parks the scene in
                # state 0x80. The exit is the client's own @Quit= -> our
                # @EQuit=/D=0 on msgid 23. Full machine: the MSG_AUCDEAD
                # banner, `_checkout_stream_enabled`.
                # OK: RENDER CONFIRMED LIVE 2026-08-21T03:13:42Z (m3): the
                # sweep-returned card rode cards A and the screen said the
                # card "was returned (for being unsold)" -- so CARDS A WEARS
                # THE 'RETURNED CARD' LABEL. WARNING: Pending cards carry no
                # won-vs-returned kind yet, so a WON card would ALSO render
                # as "returned" here; if won cards belong in cards B, the
                # pending store needs a kind field first. Exit path (money B
                # consumed after @EQuit) not yet exercised live.
                if _checkout_stream_enabled():
                    # WARNING: STRIP EVERY 0x20 LINE, NOT `money_line` BY IDENTITY.
                    # The re-read block above REPLACES the object in `lines`
                    # when the sweep just credited money, so remove(money_line)
                    # raises ValueError -- which ate the ENTIRE door reply on
                    # the first real settlement (17:16:34Z, a tester's 'Payment:
                    # 100' visit: "list.remove(x): x not in list -- silent",
                    # client timed out with the credit safely still pending).
                    lines = [l for l in lines
                             if not (l[:2] == b"B2" and l[4:6] == b"20")]
                    stream = _checkout_stream_lines(member_id, amount)
                    lines.extend(stream)
                    # WARNING: THE REMAINING SECTIONS PACE ON TIME, NOT ON @Get.
                    # Measured 17:28Z (m9, the first WON-card delivery): the
                    # NON-EMPTY cards path never sends @Get -- the pacer only
                    # fires on the quiet/empty branch (state 6 sends 0x15 only
                    # when [subobj+0x14C]==1; the add-with-UI branch clears
                    # it) -- so every @Get-gated release starved and the
                    # client timed out in cards B's wait. The real constraint
                    # The door batch (money A + cards A = returned + terminator)
                    # is in `lines`; cards B (won) rides @Get #1 and money B +
                    # @EQuit ride @Get #2 (see _checkout_pull_lines). Read
                    # pending for the log and the clear.
                    pend = _pending_of(member_id)
                    refund = int(pend.get("refund") or 0)
                    _say("tm: member %s Check Out -- money A /M=%d (proceeds) + "
                          "cards A (returned %r) + cards B (won %r, on @Get #1) "
                          "+ money B /M=%d (bid refund, on @Get #2); @EQuit on "
                          "@Get #2. Clearing pending; recover from this line if "
                          "the client never took them."
                          % (member_id, amount, pend["cards"], pend.get("won"),
                             refund))
                    if tmauction is not None and (pend["cards"] or pend.get("won")
                                                  or amount or refund):
                        _collected = [tmauction.card_row_from_ii(ii)
                                      for ii in (pend["cards"]
                                                 + (pend.get("won") or []))]
                        try:
                            tmauction.clear_pending(
                                member_id, money=bool(amount), cards=True,
                                won=True, refund=bool(refund))
                        except OSError as e:
                            _say("tm: member %s Check Out -- CANNOT clear "
                                  "the pending store (%s). The next visit "
                                  "will re-deliver; fix the store first."
                                  % (member_id, e))
                        else:
                            # PERSIST THE CARDS SERVER-SIDE, after a SUCCESSFUL
                            # clear (so a re-delivery cannot also re-add them --
                            # duplicates compound silently). The client adds each
                            # collected card to ITS OWN deck, but our collection
                            # authors the save the client reloads, so without
                            # this a won/returned card is confiscated on the next
                            # sync -- the wallet-credit trap, one field over.
                            if _collected:
                                _collection_add_cards(member_id, _collected)
                            # CREDIT THE SERVER-SIDE WALLET by BOTH money
                            # sections. Each adds its /M= to the CLIENT's wallet
                            # ([0x2B64A8] += /M=) -- money A the sale proceeds
                            # (Collecting Payment), money B the bid refund --
                            # so without the mirror the next wallet sync
                            # (/M=money_of()) confiscates them (found 17:22Z:
                            # the tester collected 100 on screen while money_of read
                            # the old balance). WARNING: money B's wallet credit is
                            # NOT yet confirmed live -- it may be delivery-only.
                            _credit = (amount or 0) + refund
                            if _credit:
                                _set_money(member_id,
                                           money_of(member_id) + _credit,
                                           "auction collected (proceeds %s + "
                                           "refund %s)" % (amount or 0, refund))
                    probes = _checkout_probe_lines()
                    if probes:
                        lines.extend(probes)
                    return lines
                # --- LEGACY SHAPE (POL_TM_CHECKOUT_STREAM=0) below. ---
                # WARNING: 2026-08-21: THE "FALSIFIED" VERDICT BELOW IS ITSELF
                # RETRACTED. authserv.log shows the 01:5x test served /M=0 (the
                # env never reached the container) WITH AUCCARDS+@Dead still on
                # (fa4fbe60 deployed ~01:45Z, after the 01:34/01:37 runs) -- it
                # reproduced nothing. The 08-19 render was @Init + /M=777; no
                # run since has matched it. Kept verbatim below as history.
                # WARNING: OFF BY DEFAULT -- THESE TWO REGRESSED A WORKING SCREEN.
                # Check Out RENDERED on 2026-08-19 with nothing but @EcmInit and
                # AUCMONEY(/M=777): "You sold your card! ... Payment: 777".
                # Tonight AUCCARDS and @Dead were added and every attempt since
                # has hung, including one carrying a real /M=20.
                #
                # The suspect is @Dead's own arm (0x105DB3), which does
                # `mov byte [esi+0xea], 0` -- it CLEARS a flag. If that byte is
                # what tells the scene it has something to draw, sending @Dead
                # unconditionally kills the screen, and the working run never
                # got one.
                #
                # WARNING: AND THAT THEORY IS FALSIFIED -- TESTED 2026-08-21T01:5xZ.
                # POL_TM_CHECKOUT_M=777 was set on prod, reproducing the 08-19
                # run exactly (same value, same @EcmInit + AUCMONEY, these two
                # gated OFF) and the screen STILL did not render. So AUCCARDS
                # and @Dead are NOT what broke Check Out, and neither is /M=0:
                # something else changed between 08-19 and 08-20.
                #
                # They stay off anyway -- unproven, not guilty -- so the wire
                # matches the last shape known to have worked. Turning them on
                # is a bisect step (=1 AUCCARDS, =2 adds @Dead), not a fix.
                #
                # WARNING: WHERE TO START NEXT, and it is NOT here: diff what else
                # moved in the Check Out path between 08-19 and 08-20 --
                # @EcmInit's own body, _checkout_init_arm, the probe lines, the
                # shop byte. Three deploys were spent adding messages to a
                # screen whose problem was never the messages, and the 777 test
                # is what finally proved that. Get `Recv=` tracing on a client
                # before spending another.
                _cards_mode = 0
                try:
                    _cards_mode = int(os.environ.get("POL_TM_AUCCARDS", "0"), 0)
                except ValueError:
                    _cards_mode = 0
                cards_line = _auccards_line(member_id) if _cards_mode else None
                if cards_line:
                    lines.append(cards_line)
                    if _cards_mode >= 2:
                        lines.append(_aucdead_line())
                    # WARNING: CLEAR ON SEND, AND LOG THE CONTENTS. There is no ack --
                    # this arm stores its payload and sets a flag, it does not
                    # reply -- so "delivered" is unobservable and one of two
                    # risks has to be taken. Not clearing re-sends the same
                    # cards on EVERY Check Out visit, and the client ADDS them
                    # to the collection each time: an item dupe that compounds
                    # silently. Clearing risks losing one delivery if the
                    # message never lands.
                    #
                    # Dupe is the worse failure, so we clear -- and the full
                    # payload goes in the log so a lost one is RECOVERABLE by
                    # hand rather than gone. Same reasoning as the sweep: prefer
                    # the failure a human can see and undo.
                    pend = _pending_of(member_id)
                    if not pend["cards"] and not amount:
                        _say("tm: member %s Check Out -- nothing owed; serving "
                              "the EMPTY AUCCARDS (/E=0) so the scene's 0x21 "
                              "wait is answered rather than left hanging."
                              % (member_id,))
                    else:
                        _say("tm: member %s Check Out -- serving AUCCARDS %r "
                              "and AUCMONEY /M=%d, then clearing. Recover from "
                              "this line if the client never took them."
                              % (member_id, pend["cards"], amount))
                        if tmauction is not None:
                            try:
                                tmauction.clear_pending(
                                    member_id, money=bool(amount), cards=True)
                            except OSError as e:
                                _say("tm: member %s Check Out -- CANNOT clear "
                                      "the pending store (%s). The next visit "
                                      "will re-deliver these cards; fix the "
                                      "store before the player opens it again."
                                      % (member_id, e))
        if checkout:
            # ...AND POKE THE DRAIN LOOP. Last, so nothing above it changes, and
            # only on this door. See the banner above `_CHECKOUT_PROBE_DEFAULT`.
            probes = _checkout_probe_lines()
            if probes:
                _say("tm: member %s Check Out -- PROBING the drain loop with "
                      "%s. Read the client trace for `store lookup ... FOUND` "
                      "and the `find(\"@Key\")` lines that follow it: those name "
                      "the arm's vocabulary. POL_TM_CHECKOUT_PROBE= to stop."
                      % (member_id,
                         ", ".join("0x%02X msgid %d" % (c, m)
                                   for c, m in _checkout_probe_codes())))
                lines.extend(probes)
        return lines

    if b"@Sell=" in cmd:
        # THE SALE. `@Sell=/C=<n>@<i>=/D=<card>` -- the player sold cards at the
        # card shop. Fire-and-forget: there is no `@ESell` in TM.dll, so the
        # client has already priced it, credited itself and moved on, and the
        # only thing that can go wrong here is our record drifting from its.
        # See `_sell_cards`, and the price banner above it.
        return _sell_cards(member_id, cmd)

    if b"@Erase=" in cmd:
        # VERIFIED: THE DISCARD. Captured 2026-08-20T15:40:38Z, the first one ever seen:
        #
        #     code=178 msgid=0x10 sh=1  @Erase=/Mode=1/C=1@0=/D=201|69|0|55|56|16
        #
        # so it arrives on **(0xB2, 0x10)** with a `/Mode=` and then the same
        # `/C=<n>@<i>=/D=<card>` list `@Sell=` uses. FIRE-AND-FORGET: the sender
        # at 0x1080E4 does NOT drain a reply slot first (compare `@GameExit=`,
        # whose 0x8416C drains (0x41, 0x0E) before sending), so nothing is
        # waiting on us -- but the card must still leave our collection, or the
        # next `_collection_to_save` writes it back and the discard undoes
        # itself on the following load.
        #
        # WARNING: `/Mode=` is UNREAD. The one capture carried `Mode=1`; the client has
        # `@Erase=` senders on codes 0x43 and 0xA2 as well, so other modes and
        # other screens very likely exist. Log it rather than branch on a value
        # seen exactly once.
        return _erase_cards(member_id, cmd)

    if b"@Buy=" in cmd:
        # THE CARD SHOP PURCHASE. `/No=` is the pack index the player picked.
        if not _shopbuy_enabled():
            return None
        # VERIFIED: AND IT COSTS SOMETHING NOW. See the banner above `_pack_price`:
        # `/No=` indexes `PackPrm.BIN` directly, validated against a tester's
        # own purchases (`/No=3` -> 9000, `/No=0` -> the free Pauper's Pack).
        # The client debits ITSELF, so this only has to agree with it; a wrong
        # debit desyncs the wallet and `_collection_to_save` then writes our
        # number into the save the client loads.
        _no = re.search(rb"/No=(\d+)", cmd or b"")
        _pack_no = _no.group(1) if _no else None
        if _env_int("POL_TM_BUY_DEBIT", 1):
            _cost = _pack_price(_pack_no)
            if _cost is None:
                _say("tm: member %s @Buy= %s -- NO PRICE for that pack index; "
                     "not debiting (this will desync if the client charged)"
                     % (member_id, cmd[:40]))
            elif _cost and member_id is not None:
                _bal = money_of(member_id)
                if _bal < _cost:
                    # WARNING: DO NOT DEBIT WHAT WE CANNOT PRICE. This used to be an
                    # unconditional `max(0, bal - cost)`, and the clamp ATE A
                    # WHOLE BALANCE: 2026-08-25T04:25:54Z, member 3 held 39980,
                    # bought pack /No=4 which our table prices at 40000, and
                    # went to **0** -- 39980 gold destroyed by a 20-gold
                    # shortfall. The warning below was already printed, one
                    # line AFTER the write it was warning about.
                    #
                    # And the client disagreed with us in the same breath: it
                    # went on to hand over the pack (3 cards ACQUIRED at
                    # 04:26:03), then took a second one at /No=3 while our
                    # balance said 0. A client that could not afford a pack
                    # does not deliver it -- it prices and decides for itself,
                    # which this file has said since `_sell_price`. So when our
                    # number says "cannot afford" and the client proceeds
                    # anyway, the number that is wrong is OURS.
                    #
                    # Refusing to move money is the only safe arm. A missed
                    # debit leaves us a bit rich and self-corrects at the next
                    # measured price; a clamp to zero is unrecoverable and
                    # `@ComGameInit=/M=` then ASSIGNS that zero into the live
                    # wallet, destroying the client's own correct figure too.
                    _say("tm:   WARNING: member %s @Buy= /No=%s costs %d and we think "
                         "they hold %d -- NOT DEBITING. Our price or our "
                         "balance is wrong (the client prices and decides for "
                         "itself); a clamp here would destroy %d gold to cover "
                         "a %d shortfall. POL_TM_BUY_DEBIT=0 disables debiting "
                         "entirely."
                         % (member_id, _no.group(1).decode(), _cost, _bal,
                            _bal, _cost - _bal))
                else:
                    _set_money(member_id, max(0, _bal - _cost),
                               "pack /No=%s" % _no.group(1).decode())
            else:
                _say("tm: member %s bought pack /No=%s -- FREE (%s)"
                     % (member_id, _no.group(1).decode() if _no else "?",
                        "the Pauper's Pack" if _no and _no.group(1) == b"0"
                        else "price 0 in PackPrm"))
        # Echo the request's own header: it already carries code 0xB2, msgid
        # 0x14 and the shop number, which is exactly what the handler's two
        # equality gates compare against.
        return _shopbuy_body(body[:8], member_id, _pack_no)

    if b"@Get=" in cmd:
        # THE ACQUIRE. `@Get=/No=1` on (0xB2, 0x15) is the client TELLING us it
        # took a card -- it has already moved staging slot 0 into its collection
        # and zeroed the rest of the pack before the message goes out (TM.dll
        # 0x17961D..0x179657), so there is nothing to answer and answering is
        # not what makes it work. What it is good for is TRUTH: this is the only
        # moment we can know a grant became an acquisition.
        if not _record_on_grant():
            _collection_commit_offer(member_id)
        # ...AND ON THE CHECK OUT DOOR IT IS THE SECTION PACER. A cards
        # sub-scene sends it as it completes (state 6, 0x13538C), and the
        # NEXT section's record must arrive only now -- pre-queued same-msgid
        # records are drained together (measured 03:29Z). See _CHECKOUT_PULL.
        pull = _checkout_pull_lines(member_id, peer_nick)
        if pull:
            _say("tm: member %s Check Out @Get= -- releasing %s (the section "
                  "pacer; pre-queuing is measured to fail)"
                  % (member_id,
                     " + ".join(l[8:l.index(b"=", 8) + 1].decode("ascii",
                                "replace") for l in pull)))
            # WARNING: NO closing @Dead here either -- the 04:37Z double-@Dead
            # model is retracted (see the MSG_AUCDEAD banner): 0x133BEB is
            # state 0x0D's ABORT poll, and a @Dead there raises dialog 221
            # ("Failed to connect to server.") instead of any closing
            # notice. The exit is the client's own @Quit= -> @EQuit=/D=0.
            return pull
        return None

    if b"@Decks=" in cmd:
        # THE CLIENT REPORTING ITS DECK, unprompted. The static read established msgid 0x22
        # has no arm, so this wants RECORDING, not answering -- and it is the
        # only place the player's own arrangement of their cards is ever stated.
        _collection_set_deck(member_id, cmd, sh=((code or 0) >> 24) & 0xFF)
        return None

    if b"@CvReq=" in cmd:
        # RANKINGS -> PRIZE CENTER. See the banner above `_cv_en`.
        if not _cv_enabled():
            return None
        lines = [encode_code(MSG_CVENTER) + (b"@CvEnter=/EN=%d" % _cv_en())]
        # ...AND THE SAME UNSOLICITED (0xB2, 0x13) PUSH THE SHOP GETS. Measured
        # live 2026-08-16: `@CvEnter=/EN=1` alone is ACCEPTED -- the client takes
        # the endpoint and goes quiet, sending only `@Pong` -- and then hangs,
        # because the scene's poll (TM.dll rva 0x8B410) has two accept paths and
        # only one of them reports completion:
        #
        #   0x8B43F  lookup (0xB2, 0x13)  -> sender id, and [esp+0xc] = 1
        #   0x8B47E  lookup code 0x71     -> parse @CvEnter + /EN=, sender id,
        #                                    but [esp+0xc] is left at 0
        #   0x8B520  mov edi, [esp+0xc]   <- the return value
        #
        # So `@CvEnter` hands the endpoint over and SHOPINIT is what actually
        # advances the scene. The first path never reads the body -- it takes
        # only the sender id -- so ANY (0xB2, 0x13) satisfies it.
        #
        # WARNING: ...AND FOR A YEAR THAT WAS THE CARD SHOP'S `@Init=`, WHICH IS THE
        # SAME MISTAKE THE AUCTION CHECK OUT DOOR MADE. msgid 0x13 dispatches on
        # the BODY COMMAND (see `_INIT_ARM_FIELDS`): `@Init` is the card shop,
        # and the Prize Center's own arm is **`@ExcInit`** (rva 0x1054EE) -- the
        # EXCHANGE, 賞品交換所, whose `/PP=` carries the point balance and the
        # three pending awards and whose `/LC=` carries the week's lucky cards.
        # Sending `@Init` here initialises the exchange as a card shop, which is
        # exactly what printed `Now1stPackName='s Pack` on the Check Out screen.
        #
        # `POL_TM_CV_INIT=excinit|init|off` picks, per request, so the wrong one
        # can be reproduced on demand -- the client's own `---->Recv=` line is
        # what arbitrates, not this comment.
        arm = os.environ.get("POL_TM_CV_INIT", "excinit").strip().lower()
        if arm == "off":
            pass
        elif arm == "einit":
            # EVENT-SHOP EXPERIMENT (2026-08-22). The orphaned Event Shop SCENE
            # has no reachable entry, BUT @EInit -- the event shop's opener -- rides
            # the SAME (0xB2, 0x13) SHOPINIT slot as @ExcInit and is parsed by the
            # SAME shop scene (arm 0x10563A), told apart only by the body name. So
            # feeding @EInit down the REACHABLE Prize Center door should flip that
            # scene into its event-shop variant with no client patch. @EInit is
            # @Init with /RT= in place of /SP=/SN=; structurally
            # it is the card shop's opener (implemented, safe) + one field, so it
            # cannot hit @EcmInit's /C= stack-garbage loop. Fields ini-tunable for
            # live bisection. WARNING: UNMEASURED -- watch the client's ---->Recv= line.
            n  = _env_int("POL_TM_SHOP_N", 1)
            rt = _env_int("POL_TM_EINIT_RT", 1)
            # /M= is the member's OWN balance -- see `_eventshop_item_lines`.
            # A hardcoded 10000 here would announce a wallet nobody earned.
            # /CP= is the CARD LEVEL slot (struct +0x00, stored at
            # 0x1056D8 in this arm), not a spare zero -- see `card_level`.
            body = (b"@EInit=/N=%d/RA=0/M=%d/CP=%d/S=1/RT=%d"
                    % (n, money_of(member_id), card_level_of(member_id), rt))
            _stat = os.environ.get("POL_TM_EINIT_STAT")
            if _stat is not None:
                try:
                    body += b"/Stat=%d" % int(_stat, 0)
                except ValueError:
                    pass
            _say("tm: member %s -- Prize Center door sending @EInit (EVENT SHOP "
                 "variant experiment): %s" % (member_id, body.decode("latin-1")))
            lines.append(encode_code(MSG_SHOPINIT) + body)
            # @EInit opens the scene but it stays "Now Loading" until its item list
            # (money @Data 0xB2/0x20 + cards @Card 0xB2/0x21) arrives. MEASURED
            # via the in-process @EInit object hook 2026-08-22: pushed in the SAME
            # door batch, the items DO NOT take -- the captured object showed item
            # count esi+0xf0 == 0 -- because the shop-family scene drains the entry
            # batch before it arms its item wait (the exact "sections can't be
            # pre-queued" rule the auction Check Out hit). So PACE them: push on the
            # member's next @Pong(s), after the scene has armed, the same carrier the
            # reservation re-feed uses. POL_TM_EINIT_PACE=0 = old door-batch push
            # (for A/B); POL_TM_EINIT_PONGS = how many heartbeats to (re-)push on.
            if os.environ.get("POL_TM_EINIT_PACE", "1") == "1":
                _EVENTSHOP_PENDING[member_id] = _env_int("POL_TM_EINIT_PONGS", 2)
                _say("tm: member %s -- event shop items PACED onto the next %d "
                     "@Pong(s) (door-batch push does not take -- item count stayed 0)"
                     % (member_id, _EVENTSHOP_PENDING[member_id]))
            else:
                lines.extend(_eventshop_item_lines(member_id))
        elif arm == "excinit" and tmprize is not None:
            body = tmprize.excinit_body(
                member_id, shop_no=_env_int("POL_TM_SHOP_N", 1), say=_say)
            _say("tm: member %s opened the PRIZE CENTER -- %s"
                 % (member_id, body.decode("latin-1")))
            lines.append(encode_code(MSG_SHOPINIT) + body)
            # `excinit_body` runs `announce()`, which PAYS -- so the balance
            # this member holds has just moved. Carry it into the save's
            # `Prize Points Acquired` while we are the ones who changed it.
            _save_sync_stats(member_id)
        elif _shopinit_enabled():
            if arm == "excinit":                             # pragma: no cover
                _say("tm: POL_TM_CV_INIT=excinit but tmprize did not import -- "
                      "falling back to the CARD SHOP's @Init=, which is the "
                      "wrong arm for this door")
            lines.append(encode_code(MSG_SHOPINIT)
                         + _shopinit_body(member_id))
        return lines

    if b"@CardReq=" in cmd:
        # The card-service endpoint handshake. See the banner above MSG_CARDREQ.
        if not _cardent_enabled():
            return None
        return encode_code(MSG_CARDENT) + (b"@CardEnt=/Ans=%d" % _cardent_ans())

    if b"@EventTimeReq=" in cmd:
        # THE EVENT COUNTDOWN. Both event pumps block here. See the banner above
        # `_eventtime_enabled` for why -1/-1 is the right default.
        if not _eventtime_enabled():
            return None
        start, end, now = _eventtime_fields()
        return (encode_code(MSG_EVENTTIMEANS)
                + (b"@EventTimeReqCheck=/Start=%d/End=%d/Now=%d" % (start, end, now)))

    if b"@EInitReq=" in cmd:
        # THE EVENT SHOP'S CARD DOOR -- card machine state 3. See the banner
        # above `_einitent_enabled`; this cannot fire before the scene opens.
        if not _einitent_enabled():
            return None
        card, ans = _einitent_fields()
        _say("tm: member %s @EInitReq -- the EVENT SHOP is open (first time ever "
              "if so); answering /Card=%d/Ans=%d" % (member_id, card, ans))
        return (encode_code(MSG_EINITENT)
                + (b"@EInitEnt=/Card=%d/Ans=%d" % (card, ans)))

    if b"@GameQT=" in cmd:
        # CONFIRM, on the table settings screen. See the banner above
        # `MSG_GAMEQA`: the client asks for this now, and without it `0x841E0`
        # returns 0 and the screen says Lobby.BIN 337, "Reservations cannot be
        # made with current table restrictions". Same value as the push.
        #
        # WARNING: TWO SENDERS, ONE NAME (static read 2026-08-22).
        # The BARE `@GameQT=` is the client's generic dialog-confirm --
        # rules-Confirm, a guest's rules-accept AND the tile-menu Cancel
        # Reservation are byte-identical (measured 08-21, confirmed static:
        # sender 0x83FC0 takes the table id as an argument but uses it only for
        # its local wait bookkeeping; nothing reaches the wire). Sender 0x84050
        # appends `/ID=<16-hex table id>` -- a table-scoped form fired from the
        # table-screen scene (sites 0x4BF68/0x4C01A; exact menu attribution
        # unpinned). Both senders drain and wait on (0x41, 6), so the reply is
        # the same @GameQA either way.
        #
        # Until 2026-08-22 BOTH forms were answered as confirm and nothing was
        # released -- which is the live report "cancel frees the table on my
        # screen and it comes back occupied": the ack satisfied the cancel's
        # waiter, the scene tore down and cleared the client-local reservation
        # global, and the next TD/PTL repainted the tile from the served row,
        # which still carried the seat. A `/ID=`-bearing `@GameQT=` now
        # RELEASES the sender's seat in their room (`release_seats` republishes
        # the freed row and hands off ownership; an emptied table drops its
        # confirmed flag in `_republish_table`). The bare form stays a pure
        # confirm -- distinguishing bare cancel from bare confirm needs the
        # scripted capture that has not been done; do not guess it here.
        # `POL_TM_GAMEQT_CANCEL=0` restores confirm-for-everything. Both arms
        # log the raw body, so a live session shows which form each UI action
        # sends.
        if not _gameqt_enabled():
            return None
        qt = _gameqa_qt()
        _idm = re.search(rb"/ID=([0-9A-Fa-f]{1,16})", cmd or b"")
        if _idm and _env_int("POL_TM_GAMEQT_CANCEL", 1):
            wire_id = int(_idm.group(1), 16)
            chan = None
            try:
                import tmroom
                chan = tmroom.room_of(member_id)
            except Exception as exc:
                _say("tm:   ...cannot place member %s in a room (%r)"
                     % (member_id, exc))
            _say("tm: member %s @GameQT=/ID=%016X -- the table-scoped form: "
                 "releasing their seat%s (raw: %r)"
                 % (member_id, wire_id,
                    (" in %s" % chan) if chan
                    else " -- NO room record, nothing to release",
                    cmd[:96]))
            if chan:
                release_seats(member_id, chan)
            return encode_code(MSG_GAMEQA) + (b"@GameQA=/QT=%d" % qt)
        _say("tm: member %s @GameQT= -- the bare dialog-confirm (confirm / "
             "accept / cancel are byte-identical); /QT=%d (raw: %r)"
             % (member_id, qt, cmd[:96]))
        return encode_code(MSG_GAMEQA) + (b"@GameQA=/QT=%d" % qt)

    if b"@GameENC=" in cmd:
        # *** CHECKED BEFORE `@GameEN=`, AND THAT ORDER IS LOAD-BEARING. ***
        # `b"@GameEN=" in cmd` is a SUBSTRING test and `@GameENC=` does not
        # contain it -- but only because of the `=`. The two literals sit 0x104
        # apart in TM.dll and the selftest below pins them apart; testing the
        # longer key first means a future edit to either key cannot silently
        # route the COM game into the reservation branch.
        if not _gameeca_enabled():
            return None
        en = _gameeca_en()
        _say("tm: member %s @GameENC= -- VS. COM game request; answering "
             "@GameECA=/EN=%d (msgid 0x%02X). /EN= MUST be > 0: TM.dll 0x853AF "
             "parses it and 0 takes the -99 arm = Lobby.BIN 448."
             % (member_id, en, (_gameeca_msgid() >> 16) & 0xFF))
        # WARNING: THE GAME PEER IS THE COM MATCH'S SESSION IDENTITY -- CAPTURE IT
        # HERE. A COM flow never sends `@GameOK=` (the PvP hook that fills
        # `_MATCH_PEER`), so every in-match push used to go out framed from the
        # recipient's OWN nick -- and the match object's session-pair gates
        # ([obj+0x28]/[0x2C], stored by the @ComGameInit arm 0x102D3C from the
        # DELIVERING record's sender u64) silently discard a message framed
        # from anyone else. That is where the dealt-but-dead @StartData went.
        # The peer this request rides is the peer the whole COM session lives
        # on (the client @Pongs it with /GM=), so pin pushes to it: right
        # socket AND right envelope in one.
        key = _push_key(member_id)
        if key is not None:
            # A NEW GAME ENDS THE OLD ONE. A PvP roster still naming this
            # member is a match they are demonstrably not in any more (the COM-never-plays bug:
            # the crashed 3-player game that made every later COM game deal
            # and then never play). See `_match_abandoned`.
            if _MATCH_ROSTER.get(key):
                _match_abandoned(member_id, "a new VS. COM game (@GameENC=)")
            # A durable stake nothing in this process claims = a restart
            # orphaned it mid-match. Refund BEFORE the fresh game overwrites
            # the entry (the fourth restart casualty of 2026-08-22).
            _stake_recover(member_id, "new @GameENC=")
            _COM_GAME[key] = {"n": None, "coms": [], "peer": peer_nick}
            if peer_nick:
                _MATCH_PEER[key] = peer_nick
            # AND TELL THE ROOM. The peer this request rides IS the table
            # the player picked VS. COM on (`_peer_table` decodes it; measured
            # live 2026-08-25), and `/NN=` is the occupancy id the tile
            # renders. Without this the table stayed free for everyone else
            # and no Observe entry existed -- the reported bug.
            _enc_nn = re.search(rb"/NN=([0-9A-Fa-f]{1,16})", cmd or b"")
            _bind_com_table(member_id, peer_nick,
                            int(_enc_nn.group(1), 16) if _enc_nn else None)
        # AND HAND THEM THE TABLE. The reply below is what lets the client
        # BUILD the COM scene; the `@MuchMake` pair rides later replies, never
        # this one, for the same reason `_queue_vsgameinit` does not ride the
        # `@GameWa=` that triggers it.
        _queue_com_match(member_id, peer_nick)
        return encode_code(_gameeca_msgid()) + (b"@GameECA=/EN=%d" % en)

    if b"@GameEN=" in cmd:
        # THE RESERVATION. See the banner above `MSG_GAMEEA`: the waiter reads
        # one field out of one message, and until now got neither. Checked
        # BEFORE `@GameM=` only for readability -- the two keys cannot collide,
        # and `@GameENC=` does not contain `@GameEN=` either.
        if not _gameea_enabled():
            return None
        # SEAT THEM, AND TELL THE REST OF THE ROOM. This is the reported bug:
        # "when a game is reserved by one player, it doesn't show as reserved for
        # everyone else -- it still believes it's empty". `@GameEN=` arrives on
        # the TABLE's peer (measured 00:26:34Z on UE7QN1N9G, table 1), so it can
        # be placed, and `+0x0C` is the seated count every other client reads out
        # of `b/g/PTL`.
        #
        # WARNING: AND THIS DOES NOT SHOW ON ANYBODY'S SCREEN. Measured 2026-08-20
        # in `TM.dll.unpacked` after live testing reported "no really observable
        # changes at all": the room's table screen walks the records at 0x66D7A
        # and reads +0x00, +0x04, +0x14, block[0], block[4..5], block[14..29] and
        # block[48] -- and NOT +0x0C. A whole-image scan finds **one** reference
        # to +0x0C in the entire client, the auto-pick's ">= 8" refusal at
        # 0x7633A, and **zero** to +0x08 and +0x10.
        #
        # The seat count is kept anyway because it is TRUE and it does feed that
        # one gate, but the visible status is the STATE BYTE (+0x14) through the
        # jump table at 0x674C4, and WHO is sitting there is the two 64-bit ids
        # packed into block[14..29]. See the measurement in `tools/tmptl.py`.
        # Neither is written here: the state values are a 7-way vocabulary we
        # have only partly decoded, and the nibble order of those ids is not
        # pinned -- and an id fed to a consumer is worse than a blank tile.
        n = _seat_at_table(member_id, peer_nick, seated=True, cmd=cmd)
        # Start the heartbeat clock AT the reservation: the first table-peer
        # @Pong lands ~6 s later (measured), and a stampless seat would
        # otherwise ride the sweep's first-sight grace instead of the real TTL.
        _en_chan, _en_idx = _peer_table(peer_nick)
        if _en_chan and member_id is not None:
            _SEAT_ALIVE[_alive_key(_en_chan, member_id,
                                   _en_idx)] = time.time()
        en = _gameea_en() if n is None else max(n, _gameea_en())
        _say("tm: member %s @GameEN= -- the RESERVATION; /EN=%d%s"
             % (member_id, en,
                "" if n is None else " (%d at the table)" % n))
        return encode_code(MSG_GAMEEA) + (b"@GameEA=/EN=%d" % en)

    if b"@Break=" in cmd:
        # THE IN-MATCH LEAVE NOTICE. (0x43, msgid 39, `/ID=0`), sent from the
        # board scene's state 3 at 0x109B0C, measured on prod 2026-09-03 ~2 s
        # BEFORE the same client's `@GameExit=`. Nothing waits on a reply
        # (the sender's scene is on its way out), so none is sent -- but the
        # OTHER players' seats are what it is about: see `_seat_departed`.
        _say("tm: member %s @Break= -- leaving the match (no reply is read; "
             "the empty seat is played by the server from here)" % (member_id,))
        _seat_departed(member_id, "@Break=")
        return None

    if b"@ExitCh=" in cmd or b"@Viewer=" in cmd or b"@Mode=" in cmd:
        # Fire-and-forget senders, read off TM.dll 2026-09-04: `@ExitCh=`
        # (code 0x20, fn 0x86460) builds, appends and calls the plain send
        # 0x82060 -- no store lookup, no wait; it precedes the IRC PART.
        # `@Viewer=` (0x41 msgid 0x11, fn 0x84F70) loops on 0x821D0, the SEND
        # completion, not a reply. `@Mode=` (0xB2, 0x1088AA) has never been
        # seen on a wire. Logged so they stop reading as "no answer".
        _say("tm: member %s %s -- a fire-and-forget notice (no reply is "
             "polled for it)" % (member_id, cmd.split(b"=")[0].decode("latin1")))
        return None

    if b"@GameExit=" in cmd:
        # A PLAYER LEAVING A RUNNING MATCH: hand the seat to the server BEFORE
        # the roster bookkeeping below forgets who was playing.
        _seat_departed(member_id, "@GameExit=")
        # ...AND UNSEAT THEM. Without this the first reservation of a session
        # marks the table forever: the row is server-side state now, so somebody
        # has to take it back down, and a table that says 1 seated with nobody
        # there is worse than the bug this fixes.
        _seat_at_table(member_id, peer_nick, seated=False, cmd=cmd)
        # ...AND END THEIR MATCH, both kinds. The "match is RUNNING" signals
        # now gate the @Pong reservation re-feed (see `_reservation_reply` --
        # the ring-flood fix), so they must CLEAR on the room-return signal or
        # the re-feed stays suppressed at the table for ever, which is the
        # +0x108 bug reborn. `_reset_for_rematch` on the COM key is a no-op
        # for anyone not in a COM match.
        _gx_key = _push_key(member_id)
        if _gx_key is not None:
            # A departing member stops being a watcher of anything.
            for _wset in _MATCH_WATCHERS.values():
                _wset.pop(_gx_key, None)
            # ...AND GIVE THE TABLE BACK. A VS. COM game holds its tile at
            # Playing (`_COM_AT`); the room-return `@GameExit=` is what ends
            # it, exactly as it ends a PvP match below. The flag must
            # not outlive the game.
            _gx_cc, _gx_ci = _com_table_of(member_id)
            if _gx_ci is not None:
                _release_com_table(_gx_cc, _gx_ci, why="VS. COM over")
            _reset_for_rematch(None, _gx_key)
            # ...AND REFUND AN UNSETTLED STAKE. The client's own abort path
            # (0xBC701) does `wallet += pot; pot = 0` when a COM game ends
            # without a result; mirror it or the save sync eats the stake.
            _gx_e = _COM_GAME.get(_gx_key)
            if _gx_e and _gx_e.get("staked") and _env_int("POL_TM_WAGER", 1):
                _set_money(member_id, money_of(member_id) + _gx_e["staked"],
                           "VS. COM aborted -- stake refunded (mirror of "
                           "0xBC701)")
                _gx_e["staked"] = None
                _stake_clear(member_id)
            else:
                # ...and a stake only the COLLECTION remembers (the restart
                # wiped the match) is refunded here too.
                _stake_recover(member_id, "@GameExit=")
        _gx_c, _gx_i, _gx_s = _match_of(member_id)
        if _gx_i is not None:
            _gx_run = _MATCH_STARTED.get((_gx_c, _gx_i)) or set()
            _gx_run.discard(_gx_key)
            if not _gx_run:
                # THE LAST PLAYER IS BACK IN THE ROOM: the match is over and
                # "Playing" must not outlive it. Pop BOTH flags --
                # leaving _MATCH_BEGAN would hand a rematch-without-reconfirm
                # the old epoch and shrink its TTL hold -- then republish so
                # the tile falls back to whatever the seats justify (the
                # authored free state once everyone's @GameExit= has unseated
                # them via _seat_at_table above).
                _MATCH_STARTED.pop((_gx_c, _gx_i), None)
                _MATCH_BEGAN.pop((_gx_c, _gx_i), None)
                try:
                    import tmroom
                    _republish_table(tmroom, _gx_c, _gx_i,
                                     _seats_of(_gx_c).get(_gx_i) or [],
                                     why="match over, Playing withdrawn")
                except Exception as exc:
                    _say("tm:   table %s not returned from Playing (%r)"
                         % (_gx_i, exc))
        # WARNING: AND ANSWER IT, OR THE CLIENT HANGS ON THE LOADING SCREEN. Reported live,
        # 2026-08-20T13:31Z: "framework is still stalled at the loading screen
        # when quitting" -- after a clean `@Quit=` exchange and `@GameExit=`,
        # with nothing further on the wire.
        #
        # `@GameExit=` follows this family's drain-before-you-wait idiom exactly,
        # and the sender shows the slot it will read:
        #
        #     0x8416C  push 0xe / push 0x41 / call 0x828E0   DRAIN (0x41, 0x0E)
        #     0x8417B  push 0xd / push 0x41
        #     0x8417F  "@GameExit=" / 0x84188 call 0xA97C0   SEND  (0x41, 0x0D)
        #
        # so the answer is request+1, and the waiter at 0x84601
        # (`0x827F0(buf, 0x41, ...)`) parses it at 0x8462D..0x8464F:
        #
        #     0xAA920(msg, "@GameEA")     the name -- the reservation reply's
        #                                 name, REUSED on a different msgid
        #     0xAB080("/Exit=", ..)       one int; 0x8465B tests `> 0` before
        #                                 clearing the global at 0x298200
        #
        # WARNING: The literal is `@GameEA` with no `=`; the wire needs the `=` like
        # every other key on this channel.
        if not _env_int("POL_TM_GAMEEXIT_ANS", 1):
            return None
        ex = _env_int("POL_TM_GAMEEXIT", 1)
        _say("tm: member %s sent @GameExit= -- answering (0x41, 0x0E) "
             "@GameEA=/Exit=%d (0x8416C drains that slot immediately before "
             "sending, so it is waiting for it; 0x8465B wants > 0)"
             % (member_id, ex))
        return encode_code(GAMEEXIT_ANS_MSGID) + (b"@GameEA=/Exit=%d" % ex)

    if b"@GameM=" in cmd:
        # THE TABLE'S MEMBER LIST. See the banner above `MSG_GAMEML`.
        if not _gameml_enabled():
            return None
        # WARNING: WE ANSWER TOO FAST, AND THIS CLIENT DROPS A REPLY THAT BEATS ITS
        # OWN BOOKKEEPING. Reported live 2026-08-20: Table Members renders EMPTY on a
        # table two people are standing at -- and the reply is demonstrably on
        # the wire and correct (`@GameML=/ID=../Num=2/MLID=..` with both rows,
        # authserv.log 02:49:39). The gap between the request and our answer was
        # **0.57 ms**.
        #
        # That is the `@TeachDVAns` race, one command over, and it is measured
        # on THIS client: "the client marks the operation pending AFTER its send
        # returns, and the result handler drops anything that lands while the
        # state is still 0. We were answering in ~3 ms." `_teach_delay` exists
        # for exactly that, and POL_POLPRO_DELAY_MS is the same fault a third
        # time on the lobby band.
        #
        # WARNING: THIS IS A HYPOTHESIS WITH A CHEAP TEST, NOT A MEASUREMENT. If the
        # pane is still empty with a delay in place, the delay is NOT the fault
        # and `POL_TM_GAMEML_DELAY_MS=0` puts it back rather than leaving an
        # unexplained pause in the path -- the same discipline `_teach_delay`
        # asks for.
        _reply_delay("POL_TM_GAMEML_DELAY_MS", 1500)
        body = _gameml_body(cmd, member_id, peer_nick)
        lines = [encode_code(MSG_GAMEML) + body]
        # WARNING: THE RESERVATION-SLOT COPY (msgid 0xC) ONLY WHEN THE REQUESTER IS
        # SEATED. @GameM= is the only request the client sends (log 2026-08-21:
        # it sends just @GameEN=/@GameM=/@GameQT=), so the reservation slot
        # (+0x108) fills from THIS response, on msgid 0xC. But a NON-SEATED
        # player sends @GameM= just to LOOK, and filling their +0x108 with the
        # seated players made their client think THEY were reserved and grey
        # Reserve ("viewing table members makes reservation impossible").
        # So: a SEATED requester gets the reservation list (their tile shows
        # pawns + Start Game -- the tester's "State A"); a looker gets only
        # the members pane and their Reserve stays available. POL_TM_GAMEML_RESV=0
        # disables.
        # WARNING: On the @GameM= reply frame the members flag +0x414 IS armed, so the
        # members arm (0xA918F) pops the FIRST matching (0x41,4) and the
        # reservation arm (0xA940C) sees nothing -- this copy cannot fill +0x108
        # on this frame. With POL_TM_RESV_MSGID=4 (default) it emits a SECOND
        # msgid-4 @GameML that lingers in the store for the next (non-View) frame,
        # where +0x414 is disarmed and the reservation arm can take it. The @Pong
        # re-feed is the primary carrier; this is belt-and-braces. See
        # _resv_gameml_code() for why 0xC never worked for the owner.
        if os.environ.get("POL_TM_GAMEML_RESV", "1") == "1" and body \
                and _is_seated_here(member_id, peer_nick):
            lines.append(encode_code(_resv_gameml_code()) + body)
        # ...and PUSH `@GameQA` behind it. See the banner above `MSG_GAMEQA`:
        # the reservation reads its verdict out of a message the client never
        # requests, so it has to arrive unsolicited. `@GameM=` is the one
        # table-context command the client does send, which makes it the
        # earliest moment we know the player is looking at a table.
        if _gameqa_enabled():
            lines.append(encode_code(MSG_GAMEQA) + (b"@GameQA=/QT=%d" % _gameqa_qt()))
        return lines

    if b"@Req=" in cmd and b"@ShReq=" not in cmd and b"@CvReq=" not in cmd:
        # THE LAST UNANSWERED MESSAGE IN THE CHAIN. See MSG_PLAYACK.
        # WARNING: The key test is anchored so `@ShReq=` (the card shop) and `@CvReq=`
        # (the Prize Center) cannot fall in here -- both CONTAIN "@Req=" as a
        # substring and both already have their own handlers above.
        if not _env_int("POL_TM_PLAYACK", 1):
            return None
        en = _env_int("POL_TM_PLAYACK_EN", 1)
        _say("tm: member %s @Req= -- answering code 2 @PLAYACK=/EN=%d "
             "(0x86A7F needs > 0; 0 becomes error 0xFFFF7FFD)" % (member_id, en))
        # \U0001f534 ON THE PS2 THIS IS ALSO THE COM-BOARD TRIGGER, BECAUSE THE
        # CONSOLE'S ENTRY HANDSHAKE DOES NOT EXIST.
        #
        # MEASURED 2026-09-08. `_queue_com_match` (the only thing that ever
        # queues `@ComGameInit` into (0x43, 2)) hangs off the `@GameENC=`
        # handler -- and **`@GameENC`, `@GameECA` and `@CheckJoinTable` are all
        # ABSENT from `TMaster.pex`**, so that trigger can never fire on the
        # console. The PS2's VS. COM entry is
        #
        #     @Init= -> @InitAns=/Ans=1 ; <CR> profile -> <CS> ; @Req= -> @PLAYACK
        #
        # and then it polls (code 0x43, msgid 2) for `@ComGameInit` until it
        # gives up with **`-1-37338`** ("timed out while connecting to the
        # server", selector 338 at `0x0031a84c`, the only site that raises it).
        # The client-side names came out of the module: `0x003edbe0(obj, N)`
        # fetches (code 67, msgid N) and its msgid ladder maps 2 ->
        # `@ComGameInit` (`0x00489C58`) and 39 -> `@Dead` (`0x00489D18`).
        # Same build-split trap as `@EQuit`/`@Quit`.
        #
        # WARNING: THE GATE IS MATCH CONTEXT, NOT CLIENT KIND, because there is no
        # build tell in scope here (same problem `POL_TM_SHOPQUIT_PS2` has) and
        # the PC sends `@Req=` too -- in PvP, after `@GameReady=`. So fire ONLY
        # when this `@Req=` arrives with NO match context at all: no COM game
        # already set up (the PC's `@GameENC=` sets one), no PvP roster, and not
        # seated at a table. On the PC both COM and PvP fail at least one of
        # those, so this is a no-op there.
        #
        # WARNING: AND IT IS FAIL-SAFE ON PURPOSE. `@Req=` -> `@PLAYACK` ALREADY
        # WORKS on both builds; this trigger is additive, so anything it raises
        # (`_is_seated_here` does a bare `int(member_id)`, for one) must not be
        # allowed to take the working reply down with it.
        if _env_int("POL_TM_REQ_COMGAMEINIT", 1) and member_id is not None:
            try:
                _rk = _push_key(member_id)
                _bare = (_rk is not None and not _COM_GAME.get(_rk)
                         and not _MATCH_ROSTER.get(_rk)
                         and not _is_seated_here(member_id, peer_nick))
            except Exception as _exc:
                _bare, _rk = False, None
                _say("tm: member %s @Req= -- COM-board gate skipped (%r); "
                     "the @PLAYACK reply below is unaffected"
                     % (member_id, _exc))
            if _bare:
                _stake_recover(member_id, "bare @Req= (PS2 VS. COM entry)")
                _COM_GAME[_rk] = {"n": None, "coms": [], "peer": peer_nick}
                if peer_nick:
                    _MATCH_PEER[_rk] = peer_nick
                _say("tm: member %s bare @Req= with no match context -- treating "
                     "it as the PS2 VS. COM entry and queueing @ComGameInit into "
                     "(0x43, 2). @GameENC=/@GameECA=/@CheckJoinTable are absent "
                     "from TMaster.pex, so the PC trigger cannot fire here; "
                     "without this the client times out with -1-37338. "
                     "(POL_TM_REQ_COMGAMEINIT=0 disables)" % (member_id,))
                _queue_com_match(member_id, peer_nick)
        return encode_code(MSG_PLAYACK) + (b"@PLAYACK=/EN=%d" % en)

    if b"@GameReady=" in cmd:
        # VERIFIED: THE OWNER-ACTION MATCH TRIGGER, captured live 2026-08-22. When the
        # host presses "Start Game" it walks @CheckJoinTable= -> PART -> this
        # @GameReady= (0x41 msgid 0x0A, no reply slot -- see the banner above
        # MSG_GAMEEA/GAMEWA) -> @Req= -> @GameOK=, and enters the match. The GUEST
        # meanwhile sits at the table polling @Pong, because it never received the
        # wake-up @GameStart= (0x41 msgid 7 = MATCH_41_MSGID) that starts its OWN
        # copy of that chain. This is the trigger the old headcount _announce_match
        # got wrong (it fired on SEAT COUNT before confirm, so the wake-up hit an
        # open rules dialog and the host answered @GameNG= -- see the 12:43 trace
        # by _TABLE_CONFIRMED). @GameReady= is post-confirm, post-PART: an
        # unambiguous "the host is going now", so wake the OTHER seated players.
        #
        # Delivered on their NEXT @Pong (after=lead), never appended to this reply
        # -- the wake-up must land on a frame AFTER the scene that polls (0x41,7)
        # exists (POL_TM_MATCH41_GAP / the _announce_match banner). The host is NOT
        # woken: it initiated on its own and is already past this scene.
        # POL_TM_GAMEREADY_WAKE=0 disables.
        if not _env_int("POL_TM_GAMEREADY_WAKE", 1):
            return None
        chan, index, seats = _match_of(member_id)
        if not seats:
            _say("tm: member %s sent @GameReady= but no match roster is known -- "
                 "cannot wake the other players (was the seat path reached?)"
                 % (member_id,))
            return None
        # This member has now STARTED. Track it so we never wake a player who is
        # already going: without this, the guest's own @GameReady= (sent after it
        # is woken) would push @GameStart= back at the host, who is already in the
        # match -- a ping-pong, and a stray (0x41,7) into a scene that no longer
        # polls it. Wake only seated players who have NOT started.
        started = _MATCH_STARTED.setdefault((chan, index), set())
        started.add(_push_key(member_id))
        # The heartbeat-expiry match gate reads this: in-match players stop
        # ponging the table (the reservation is consumed client-side), so the
        # TTL must not reap a running game's seats. See `_MATCH_BEGAN`.
        _MATCH_BEGAN.setdefault((chan, index), time.time())
        # ...AND TELL THE DEPLOY GATE NOW. The marker used to be first written
        # at `@CardSelect=`, so Start Game -> deal reported 0 live matches and
        # a restart in that window wiped the accept quorum (2026-09-03T22:57Z).
        _live_matches_write()
        # ...AND TELL THE ROOM. The row sat at recruiting (state 1) for the
        # whole match, so everyone else kept seeing Join on a table mid-game.
        # _match_running is True as of the add() above, so this republish
        # serves state 2 -- "Playing", with Observe on the menu. Idempotent on
        # the guests' later @GameReady= (note_table no-ops an identical row).
        try:
            import tmroom
            _republish_table(tmroom, chan, index,
                             _seats_of(chan).get(index) or list(seats),
                             why="match started, now Playing")
        except Exception as exc:
            _say("tm:   table %s not flipped to Playing (%r) -- the room "
                 "keeps seeing the recruiting tile" % (index, exc))
        others = [(m, i) for (m, i) in seats
                  if _push_key(m) != _push_key(member_id)
                  and _push_key(m) not in started]
        if not others:
            _say("tm: member %s @GameReady= -- no un-started OTHER seated player "
                 "to wake (roster %r, started %r)"
                 % (member_id, [m for m, _ in seats], sorted(started)))
            return None
        body = encode_code(MATCH_41_MSGID) + os.environ.get(
            "POL_TM_MATCH41_BODY", "@GameStart=").encode("latin1", "replace")
        lead = _env_int("POL_TM_MATCH41_GAP", 1)
        _say("tm: member %s @GameReady= (host starting) -> waking %d other seated "
             "player(s) with @GameStart= (0x41 msgid 7) on their next @Pong: %s"
             % (member_id, len(others), ", ".join(str(m) for m, _ in others)))
        for mid, _ident in others:
            _queue_push(mid, body, "match wake-up at table %d" % index, after=lead)
        return None

    if b"@GameOK=" in cmd or b"@GameNG=" in cmd:
        # THE TWO MESSAGES THE HOST AND GUEST SENT THE MOMENT (0x41,7) LANDED,
        # both of which we met with silence and both of which stalled. `@GameNG=`
        # is msgid 0x0F, so `request+1` puts its answer in the 0x10 slot the
        # client is already polling -- and that slot's waiter names it `@GameWa`
        # with `/Error=`. See GAMEWA_MSGID.
        if not _env_int("POL_TM_GAMEWA", 1):
            return None
        err = _env_int("POL_TM_GAMEWA_ERROR", 0)
        ok = b"@GameOK=" in cmd
        code = GAMEWA_OK_MSGID if ok else GAMEWA_NG_MSGID
        _say("tm: member %s sent %s -- answering (0x41, %#x) @GameWa=/Error=%d"
             % (member_id, "@GameOK=" if ok else "@GameNG=",
                (code >> 16) & 0xFF, err))
        if ok and err == 0:
            _live_matches_write()          # the accept window is live time too
            # THE BAND, captured from the message itself. `@GameOK=` is sent by
            # the match scene, so the peer it arrives on is the peer that scene
            # is listening to -- and a board delivered anywhere else is unread
            # (see `_pending_pushes`).
            if peer_nick:
                _MATCH_PEER[_push_key(member_id)] = peer_nick
            # AND THIS IS WHERE THE BLACK SCREEN STARTS. `@GameWa=/Error=0` is
            # the last thing either client is told; the scene it then builds
            # polls `(0x43, 1)` and we have never answered it. Queued, not
            # appended -- see `_queue_vsgameinit` for why it must not ride this
            # reply. `@GameNG=` gets nothing: that player declined.
            _queue_vsgameinit(member_id)
        return encode_code(code) + (b"@GameWa=/Error=%d" % err)

    if b"@CardSelect=" in cmd:
        # VERIFIED: THE HOST IS ON THE BOARD AND THIS IS WHAT IT ASKS NEXT. Measured
        # live 2026-08-20T10:55:55Z, seconds after the first `@VsGameInit` was
        # read: `code=67 msgid=0x07 @CardSelect=/C=5@N0=/D=161|100|2|90|...`,
        # i.e. the player's five chosen cards. Unanswered, the screen sits on
        # "selecting" for ever (reported live, same minute).
        #
        # THE ANSWER, off arm 0x1030C9 -- and note it is gated like every other
        # in-match message on `[esi+0x28]/[esi+0x2c]`, the session pair that
        # `@VsGameInit` established, so this can only work after the board:
        #
        #   /Ans= once PER SEAT (occurrence i, i in 0..N-1), each written to
        #         `[esi + 0x19D + 48k]` where k = (i - me) mod N is the same
        #         display slot the board uses. `0x10317E` treats the value 1
        #         specially -- a seat whose byte becomes 1 having been 0 gets
        #         `[esi + 0x19F + 48k] = 5` -- which reads as "this player has
        #         confirmed", so 1 per seat is "everybody has chosen".
        #   /Ok=  occurrence 0. `0x1031B5` loads it and `0x1031BC` **increments**
        #         it into the result slot, and every caller tests `cmp eax, 1`.
        #         So **`/Ok=0` is the success value**, not 1.
        #
        # WARNING: NOT MEASURED. The widths and the arithmetic are read off the parser;
        # the VALUES are inferred from that `== 1` arm. `POL_TM_CARDSELECT=0`
        # restores the silence for an A/B, and `_ANS` / `_OK` sweep the two
        # numbers without a rebuild. The client says whether it read it:
        # `---->Recv=CARDSELECT` (0x103107) is in the traced sink.
        if not _env_int("POL_TM_CARDSELECT", 1):
            _say("tm: member %s sent @CardSelect= -- POL_TM_CARDSELECT=0, "
                 "staying silent" % (member_id,))
            return None
        msgid = ((code or 0) >> 16) & 0xFF
        chan, index, seats = _match_of(member_id)
        # A LIVE COM BINDING BEATS A STALE LOBBY ROSTER -- see `_in_com_game`.
        if seats and _in_com_game(member_id):
            _say("tm:   ...member %s is in a VS. COM game but `_match_of` still "
                 "holds a %d-seat lobby roster from a reservation -- IGNORING "
                 "it. Left standing it deals no COM hand and pins the active "
                 "seat at 0 for ever." % (member_id, len(seats)))
            chan, index, seats = None, None, []
        # THE VS. COM PLAYER COUNT, AND THE MATCH KEY. `seats` is empty in a
        # COM game; the real N arrived on `@ComGame=/Com=...` (1 + one entry
        # per COM opponent -- 2 was hardcoded before that field was parsed).
        # And a COM match is keyed PER MEMBER: `_match_of` answers
        # (None, None) for it, so two members in simultaneous COM games would
        # share one board, one hand store and one turn counter.
        com_n = None if seats else _com_n(member_id)
        if com_n:
            index = _push_key(member_id)
        picked = re.search(rb"/C=(\d+)", cmd or b"")
        # THE HAND, AND THIS IS THE ONLY TIME WE SEE IT. `@PutData=` names a
        # card by hand index alone, so `@PutCard`'s `/D=` can only be built from
        # what was captured here. See `_MATCH_HANDS`.
        _hand = _hands_from_cardselect(cmd)
        if _hand:
            _hands_cs = _MATCH_HANDS.setdefault((chan, index), {})
            _hands_cs[_push_key(member_id)] = _hand
            # ...AND THE DECK COPY, keyed by SEAT -- the win screen's take
            # list re-supplies every player's five (the play hands get popped
            # as played), and PvP needs it exactly like the COM path: a
            # decisive PvP game parked on the take polls without it.
            _seat_cs = next((i for i, (m, _v) in enumerate(seats)
                             if _push_key(m) == _push_key(member_id)), 0)
            _hands_cs[("deck", _seat_cs)] = list(_hand)
            _say("tm:   ...member %s is holding %d card(s): %s"
                 % (member_id, len(_hand),
                    b", ".join(r.split(b"|")[0] for r in _hand).decode()))
        # WARNING: AND IT MUST BE TRUE. The first version answered `/Ans=1` for EVERY
        # seat on the first player's selection -- i.e. "everybody has chosen"
        # when only one had. Measured 2026-08-20T11:04Z: the host went on, the
        # guest (whose board was still 19 s away) arrived to a game that had
        # already moved and said it could not start. `/Ans=` occurrence i is
        # ABSOLUTE seat i (0x103150's `(N - me + i) % N` is only the DISPLAY
        # slot it writes to), so the vector has to say who has actually picked.
        ready = _CARD_READY.setdefault((chan, index), set())
        ready.add(_push_key(member_id))
        # CARD SELECT IS PART OF THE MATCH as far as the deploy gate is
        # concerned -- stamp it here, not only at the deal. See
        # `_live_matches_write`.
        _live_matches_write()
        others = [m for m, _i in seats if _push_key(m) not in ready]
        # WARNING: AN EMPTY ROSTER IS NOT "EVERYONE HAS CHOSEN". `others` is a filter
        # OVER `seats`, so it is VACUOUSLY empty when we hold no seats -- and
        # `not others` is the deal trigger. A PvP match whose roster this
        # process never saw therefore DEALS on the first `@CardSelect=` to
        # arrive, and `deal_to = seats or [(member_id, 0)]` deals it to the
        # SENDER ALONE.
        #
        # Measured 2026-08-25T03:01:13Z, 8s after authsess was recreated
        # mid-card-select: "member 16 @CardSelect= ... 1 of 2 have chosen"
        # followed immediately by "...dealing: (0x43, 8) @StartData= to member
        # 16" -- one seat dealt, `_MATCH_TURN` created, and member 3 left in
        # card select for ever because THIS BAND NEVER RESENDS (see below).
        # The restart did not merely lose the match, it MANUFACTURED a corrupt
        # one and published it to the deploy gate as live.
        #
        # A VS. COM game legitimately has no seats -- `com_n` is what says so,
        # and it is the only case `deal_to`'s solo fallback was written for.
        # `POL_TM_DEAL_ROSTER_GATE=0` restores the old vacuous behaviour.
        roster_known = bool(seats) or bool(com_n)
        if not _env_int("POL_TM_DEAL_ROSTER_GATE", 1):
            roster_known = True
        n_eff = len(seats) or com_n or 2
        _say("tm: member %s @CardSelect= (0x43, msgid %d) picked %s card(s) -- "
             "%d of %d have chosen%s. /Ok=0 is the success value (0x1031BC "
             "increments it). Watch for '---->Recv=CARDSELECT'."
             % (member_id, msgid, picked.group(1).decode() if picked else "?",
                len(ready), n_eff,
                "" if not others else "; still waiting on "
                + ", ".join(str(m) for m in others)))
        _mine = next((i for i, (m, _v) in enumerate(seats)
                      if _push_key(m) == _push_key(member_id)), 0)
        body_now = _cardselect_body(seats, ready, msgid, _mine, n_solo=com_n)
        # When the LAST player picks, everyone else is still holding the vector
        # from before and has no way to ask again -- this band never resends. So
        # the completed vector is pushed to them, the same shape as the board.
        if not others and not roster_known:
            _say("tm: WARNING: member %s completed card select for a match this "
                 "process has NO ROSTER for (no seats, not a COM game) -- "
                 "REFUSING to deal. Dealing here would start a one-sided game "
                 "for the sender alone (deal_to's solo fallback) and strand "
                 "everyone else in card select. Almost always: login/authsess "
                 "was recreated mid-match -- check the container's uptime."
                 % (member_id,))
            # ...AND TELL THE CLIENT THE MATCH IS GONE, in the one message its
            # card-select scene reads for exactly that. Read off TM.dll
            # 2026-09-04: the scene at 0xD7280 polls (0x43, 7) for the picks
            # and then (0x43, 40) `@DataError`; on a hit (0xD7317..0xD737C)
            # it sends `@Quit=` (cmd 5, which the arm below answers), shows
            # message base+0x5DC and leaves (state 0x11). The arm 0x103481
            # parses `/No=` into a stack local and discards it. Without this
            # the client sat in card select until its own timeout -- the
            # "a fresh process cannot tell a client its match is gone" hole.
            # Card select is the ONLY match scene that polls cmd 40 (the
            # board scene does not), so this is sent here and nowhere else.
            if _env_int("POL_TM_DATAERROR_ON_LOST_MATCH", 1):
                _no = _env_int("POL_TM_DATAERROR_NO", 0)
                _queue_push(member_id,
                            _turn_code(DATAERROR_CMD, 0) + b"@DataError=/No=%d" % _no,
                            "the match is gone (@DataError -> the client quits)",
                            after=0)
                _say("tm:   ...pushing (0x43, %d) @DataError=/No=%d so the "
                     "client leaves card select cleanly (it answers with "
                     "@Quit=). POL_TM_DATAERROR_ON_LOST_MATCH=0 disables."
                     % (DATAERROR_CMD, _no))
        if not others and roster_known and _env_int("POL_TM_STARTDATA", 1):
            # THE DEAL. 0xC28C8 asks for command 8 the moment card select is
            # released, and nothing else will move the scene. Per recipient,
            # because `/S=` is in the recipient's own seat frame.
            # WHO LEADS. Retail spun the turn-order roulette to choose the
            # starting seat at RANDOM; we hardcoded seat 0, so the human led
            # every game (the roulette played but always landed on the player --
            # reported live 2026-09-02). The client is server-authoritative
            # here: `@StartData`'s `/S=` is consumed at TM.dll 0x10354E with NO
            # validation, no clamp and no "human first" branch (disassembled
            # 2026-09-02), so any seat in [0, N) is honoured. `/S=` and
            # `@TurnData`'s `/A=` both derive from this one `starter`, so drawing
            # it once keeps them consistent, and a COM seat winning the toss is
            # already handled by the `starter % n_eff != 0` COM-leads path below.
            # WARNING: UNIFORM IS AN INFERENCE, NOT A MEASUREMENT: the roulette is
            # cosmetic (it animates AFTER /S= is read), so the client holds no
            # record of retail's distribution. Uniform across the seats is the
            # least-committal randomisation and is client-consistent. Setting
            # `POL_TM_START_SEAT` still forces a fixed seat for repro/tests (=0
            # restores human-first if a COM-leads-from-turn-0 game misbehaves
            # live -- that path was never exercised while starter was pinned 0).
            if os.environ.get("POL_TM_START_SEAT") is not None:
                starter = _env_int("POL_TM_START_SEAT", 0)
            else:
                starter = random.randrange(max(1, n_eff))
            # THE TURN COUNTER STARTS HERE. `@StartData`'s own third envelope
            # byte is stored as the turn (0x103536), and the client counts up
            # from it to `5 * N` before it prints `End` -- see `_turn_code`.
            first = _env_int("POL_TM_FIRST_TURN", 0)
            _MATCH_TURN[(chan, index)] = {"turn": first, "active": starter,
                                          "n": n_eff}
            _live_matches_write()
            # The WATCHER deal (@StartData + turn-0 @TurnData) is sent AFTER the
            # players' deal below, so it can share the players' turn-0 `rnd`.
            # See "DEAL THE WATCHERS TOO".
            if com_n:
                # A NEW COM GAME AT THE SAME KEY: the previous game's board
                # would leave its tiles occupied (PvP gets this cleanup in
                # `_remember_match`; a COM game has no board handshake).
                _MATCH_BOARD.pop((chan, index), None)
                _MATCH_OBJECTS.pop((chan, index), None)
                _MATCH_COMBO.pop((chan, index), None)
                # ...AND DEAL THE COM HANDS. The server plays the COM
                # (measured -- see `_queue_com_turns`), so each COM seat
                # needs five cards it can put down. Deck per character from
                # `_com_deck_rows`; the mapping knob is documented there.
                _hands = _MATCH_HANDS.setdefault((chan, index), {})
                _coms = (_COM_GAME.get(_push_key(member_id)) or {}).get("coms") or []
                # ...AND KEEP THE ORIGINALS. The play hands get popped card by
                # card, but the WIN screen's take list (@TCList, measured
                # 16:49Z: the winner polls (0x43,26)+(0x43,35) and nothing
                # else) carries every player's FULL five -- see _tclist_body.
                if _hand:
                    _hands[("deck", 0)] = list(_hand)
                for _s in range(1, n_eff):
                    _ci = _coms[_s - 1] if _s - 1 < len(_coms) else _s
                    _hands[("com", _s)] = _com_deck_rows(_ci)
                    _hands[("deck", _s)] = list(_hands[("com", _s)])
                    _say("tm:   ...COM seat %d (character %s) holds: %s"
                         % (_s, _ci,
                            ", ".join(r.split(b"|")[0].decode()
                                      for r in _hands[("com", _s)])))
            # WARNING: FOURTH SEAT GATE, AND THE ONE THAT HELD THE DEAL. This loop
            # iterates `seats`, which is EMPTY in a VS. COM game -- so the body
            # never ran and @StartData was never dealt, leaving the scene parked
            # at 0xC28C8 polling command 8 for ever. Live 2026-08-22: /Ok=1
            # released card select exactly as measured, and then nothing came.
            # `_startdata_body` already falls back to N=2 when seats is empty,
            # which is the right frame for human + COM, so the deal itself
            # needed no change -- only somebody to deal it to.
            # See the sibling gates in `_push_muchmake`, `_queue_vsgameinit` and
            # `_cardselect_body`: SEAT COUNT IS THE FIRST THING TO CHECK when a
            # COM feature stalls.
            deal_to = seats or [(member_id, 0)]
            # THE BOARD, ROLLED ONCE FOR THE WHOLE MATCH and seeded into
            # `_MATCH_BOARD` before anybody is dealt -- see `_match_objects`.
            # It has to happen AFTER the COM path's `_MATCH_BOARD.pop` above
            # or the pop would take the blocks straight back out again.
            _codes = _match_objects(chan, index, n_eff, fresh=True)
            for mid in (m for m, _i in deal_to):
                seat = next((i for i, (m, _v) in enumerate(deal_to)
                             if _push_key(m) == _push_key(mid)), 0)
                sd = (_turn_code(STARTDATA_CMD, first)
                      + _startdata_body(seats, seat, starter, n_solo=com_n,
                                        codes=_codes))
                _say("tm:   ...dealing: (0x43, %d) @StartData= to member %s "
                     "(seat %d, /S=%d). Watch for '---->Recv=STARTDATA'."
                     % (STARTDATA_CMD, mid, seat, (starter - seat) % n_eff))
                # WARNING: IT MUST NOT SHARE A BATCH WITH THE CARD-SELECT ANSWER.
                # Measured 2026-08-20T14:19:12Z: both went out in ONE reply
                # ("answered with 2 line(s)"), (0x43, 8) flipped to **FOUND** --
                # so it was retrieved -- and `---->Recv=STARTDATA` never
                # printed. `@CardSelect=/Ok=1` is the message that CHANGES THE
                # STATE (0x17 -> 0x18 -> 0x19), and the deal rode along with the
                # thing that creates the state meant to read it. Same law as
                # `POL_TM_MATCH41_GAP` and `@MuchMake`'s two halves.
                _queue_push(mid, sd, "the deal",
                            after=_env_int("POL_TM_STARTDATA_GAP", 1),
                            peer=_MATCH_PEER.get(_push_key(mid)))
            # ...AND THE FIRST TURN, behind the deal. Both clients poll
            # command 9 the moment @StartData lands. One random table for the
            # whole match -- see `_TURN_RAND`.
            if _env_int("POL_TM_TURNDATA", 1):
                rnd = _turn_rand((chan, index), fresh=True)
                if com_n and (starter % n_eff) != 0:
                    # THE COM LEADS: announcing turn 0 is not enough, the
                    # server has to PLAY it -- the chain owns both.
                    _queue_com_turns(chan, index, member_id, n_eff,
                                     first, starter)
                    return body_now
                # WARNING: FIFTH SEAT GATE (found 2026-08-22, one loop below the
                # fourth): this iterated `seats`, so a VS. COM game got its
                # deal and then NO turn 0 -- the scene reads @StartData and
                # immediately polls command 9, for ever. `deal_to` is the
                # roster that already carries the solo fallback.
                for mid in (m for m, _i in deal_to):
                    seat = next((i for i, (m, _v) in enumerate(deal_to)
                                 if _push_key(m) == _push_key(mid)), 0)
                    # Turn 0: the board is empty, so the score /S= is all
                    # zeros (the client's own tally would write the same
                    # after the first placement).
                    _sc0 = ([0] * n_eff
                            if _env_int("POL_TM_TURN_SCORES", 1) == 1
                            else None)
                    td = (_turn_code(TURNDATA_CMD, first)
                          + _turndata_body(seats, seat, starter, rnd,
                                           scores=_sc0, n_solo=com_n))
                    _say("tm:   ...turn %d: (0x43, %d) @TurnData= to member %s "
                         "(seat %d, /A=%d, RandTbl=%s). Watch for "
                         "'---->Recv=TURNINFO'."
                         % (first, TURNDATA_CMD, mid, seat,
                            (starter - seat) % n_eff,
                            ",".join(str(v) for v in rnd)))
                    _queue_push(mid, td, "turn %d" % first,
                                after=_env_int("POL_TM_TURNDATA_GAP", 1),
                                peer=_MATCH_PEER.get(_push_key(mid)))
                # WARNING: DEAL THE WATCHERS TOO -- the play scene a watcher's @WFCard
                # opens needs the SAME @StartData + turn-0 @TurnData a player
                # gets, or it has no turn-0 baseline: the client polls cmd 9
                # (@TurnData) before cmd 10 (@PutCard), so with only the NEXT
                # turn's @TurnData filed it advances to turn 1 and the first
                # @PutCard(0) mismatches (`Errot!!Turn` -> "invalid card data",
                # the observer quits -- measured shim-CASPC 2026-09-03). Sent in
                # the ABSOLUTE frame (seat 0; a watcher is never rotated) with
                # the players' own turn-0 `rnd`, framed from the table peer that
                # delivered @WatchInfo. They FILE until @WFCard opens the play
                # scene, which then consumes @StartData -> @TurnData(0) ->
                # @PutCard(0) -> @TurnData(1) ... in order.
                # `POL_TM_WATCH_STARTDATA=0` turns the watcher deal off.
                if _watchers_of(chan, index) and _env_int("POL_TM_WATCH_STARTDATA", 1):
                    _wsd = (_turn_code(STARTDATA_CMD, first)
                            + _startdata_body(seats, 0, starter, n_solo=com_n))
                    _wtd0 = (_turn_code(TURNDATA_CMD, first)
                             + _turndata_body(seats, 0, starter, rnd,
                                              scores=([0] * n_eff
                                                      if _env_int("POL_TM_TURN_SCORES", 1) == 1
                                                      else None),
                                              n_solo=com_n))
                    for _wmid, _wpeer in _watchers_of(chan, index):
                        _queue_push(_wmid, _wsd, "watcher: the deal (@StartData)",
                                    after=_env_int("POL_TM_STARTDATA_GAP", 1),
                                    peer=_wpeer)
                        _queue_push(_wmid, _wtd0,
                                    "watcher: turn 0 (deal baseline)",
                                    after=_env_int("POL_TM_TURNDATA_GAP", 1),
                                    peer=_wpeer)
                    _say("tm:   ...watcher deal: @StartData + turn-0 @TurnData "
                         "(absolute frame) to %d watcher(s) -- the play scene's "
                         "turn baseline" % len(_watchers_of(chan, index)))
        if not others and _env_int("POL_TM_CARDSELECT_BROADCAST", 1):
            for mid in (m for m, _i in seats if _push_key(m) != _push_key(member_id)):
                _say("tm:   ...that completes it -- pushing the same "
                     "@CardSelect= vector to member %s" % (mid,))
                _seat = next((i for i, (m, _v) in enumerate(seats)
                              if _push_key(m) == _push_key(mid)), 0)
                _queue_push(mid, _cardselect_body(seats, ready, msgid, _seat),
                            "card select complete",
                            after=_env_int("POL_TM_CARDSELECT_GAP", 0),
                            peer=_MATCH_PEER.get(_push_key(mid)))
        return body_now

    if b"@Data=" in cmd and (code or 0) & 0xFF == IN_MATCH_CODE \
            and ((code or 0) >> 16) & 0xFF == WATCHINFO_CMD:
        # THE OBSERVER. Measured live 2026-08-22T20:07Z:
        # the client JOINs the table's own
        # channel and sends `@Data=/Watch=<n>` on (0x43, 37); the answer is
        # `@WatchInfo` on the same slot -- the arm 0x101B15's full state
        # snapshot, with the envelope sh selecting the mid-game form (sh=1
        # reads /H /T /S /A /F /CH; the exit state is sh+0xE). After it, the
        # watcher consumes the same in-match pushes the players get.
        if not _env_int("POL_TM_WATCH", 1):
            return None
        _wchan, _widx = _peer_table(peer_nick)
        if _widx is None:
            # The selftest (and any odd peer) fall back to the only PvP
            # match running, loudly.
            _cands = [k for k in _MATCH_TURN
                      if k[0] is not None or isinstance(k[1], int)]
            if len(_cands) == 1:
                _wchan, _widx = _cands[0]
                _say("tm:   ...watch request peer unresolved -- falling back "
                     "to the only running match %r" % ((_wchan, _widx),))
        if _widx is None:
            _say("tm: 🔭 member %s sent @Data=/Watch= but the peer %r "
                 "resolves to no table and no single match is running -- "
                 "silent" % (member_id, peer_nick))
            return None
        # THE STATE KEY, AND IT IS NOT ALWAYS THE TABLE. A PvP match is
        # keyed by (chan, index); a VS. COM match is keyed `(None, push_key)`
        # -- per member, because `_match_of` cannot place it (see the
        # `@CardSelect=` handler). `_COM_AT` is what turns the table the
        # observer clicked back into that key, and registering the watcher
        # UNDER it is what makes every COM `_watch_push` reach them unchanged.
        _wkey = (_wchan, _widx)
        _wcom = _com_key_at(_wchan, _widx)
        _wseats = next((v[2] for v in _MATCH_ROSTER.values()
                        if v[0] == _wchan and v[1] == _widx), [])
        _wcoms = []
        if not _wseats and _wcom is not None:
            _wgot = _COM_GAME.get(_wcom[1]) or {}
            _wme = _wgot.get("member")
            if _wme is None:
                _say("tm: 🔭 member %s wants to watch the VS. COM game at %s "
                     "table %s but its binding names no member -- silent"
                     % (member_id, _wchan, _widx))
                return None
            _wkey = _wcom
            # The human is ABSOLUTE seat 0 in a COM game (the whole COM path
            # builds its bodies with me=0), so the roster is one real member
            # followed by one entry per chosen COM character.
            _wseats = [(_wme, 0)]
            _wcoms = list(_wgot.get("coms") or [])
        if not _wseats:
            _say("tm: 🔭 member %s wants to watch %s table %s but no match "
                 "roster is there -- silent (the game may not have started)"
                 % (member_id, _wchan, _widx))
            return None
        _wn = len(_wseats) + len(_wcoms)
        # WARNING: NEVER SERVE `/N=` < 2, AND A COM GAME CAN ASK BEFORE ITS ROSTER
        # EXISTS. `/N=` is not a display field: arm 0x101B15 writes it to
        # [esi+0xE5] and then **[esi+0xE6] = N - 2** as the BOARD-SIZE index
        # into the tile-count table at 0x51C359E. N=1 indexes that table at
        # **-1** -- a garbage board size the whole snapshot is then built on,
        # and no later correction undoes the seeding.
        #
        # Measured live 2026-08-25: member 6 opened Observe on a COM table at
        # 04:08:39Z, BEFORE that game's `@ComGame=/Com=` had arrived (04:09:06Z)
        # -- so `_com_n` was still None, `_wcoms` was empty, and we answered
        # "1 player(s)". The client re-asked 66 s later and got a correct N=2
        # sh=1 snapshot on top of the poisoned one, then the live card stream,
        # and threw "invalid card data detected, exiting Tetra Master".
        #
        # Silence is the right answer: the observer demonstrably RE-ASKS, and a
        # watcher that arrives a beat early then gets a snapshot built on a real
        # roster. `POL_TM_WATCH_MIN_N=0` restores the old behaviour for an A/B.
        if _wn < max(2, _env_int("POL_TM_WATCH_MIN_N", 2)):
            _say("tm: 🔭 member %s wants to watch %s table %s but the roster is "
                 "only %d player(s) -- STAYING SILENT rather than seeding the "
                 "observer with /N=%d (arm 0x101B15 derives the board size as "
                 "N-2, so N<2 indexes the tile table out of range and poisons "
                 "the whole scene). %s"
                 % (member_id, _wchan, _widx, _wn, _wn,
                    "The COM roster (@ComGame=/Com=) has not arrived yet; the "
                    "client re-asks." if _wcom is not None
                    else "The match roster is incomplete."))
            return None
        _wnames = [_vsgame_player_name(m, i)
                   for i, (m, _v) in enumerate(_wseats)]
        _wnames += [_com_player_name(_ci, len(_wseats) + _k)
                    for _k, _ci in enumerate(_wcoms)]
        # @WatchInfo /R= parses on the @VsGameInit slots (not @Tet order).
        _wrules = _vsgame_rule_seven(_table_rules_get(_wchan, _widx))
        _wst = _MATCH_TURN.get(_wkey) or {}
        _wboard = _MATCH_BOARD.get(_wkey) or {}
        _whands = _MATCH_HANDS.get(_wkey) or {}
        # VERIFIED: ALWAYS THE BOARD FORM (sh=1), even before the deal. Measured
        # 2026-09-03 (disasm of the watch scene): `@WatchInfo` sh=1 routes the
        # watch scene to PHASE 5 (deal-start, arm 0xC1329), which draws the
        # board and self-advances phase 5 -> 2 -> 3 (0xC1549 -> 0xC12CD) with NO
        # further network input -- so the observer renders the live board
        # exactly like a player, then fills in from the @PutCard/@TurnData
        # pushes. sh=0 routes to PHASE 1 (0xC0D37), which NEVER writes a phase
        # and so never advances -- THE "stuck on selecting cards" DEAD-END a
        # card-select joiner hit live (and the reason the deal-time sh=1
        # *refresh* is inert: by then the scene's super-state is already 4 and
        # the 0x0F state it lands on is outside the play machine's range). A
        # pre-deal join therefore gets an EMPTY board (turn 0, full 5-card
        # hands); a mid-game join gets the real board; a post-result join gets
        # the frozen final board -- all of them a board, none of them the
        # dead-end. `POL_TM_WATCH_SH0=1` restores the old pre-deal sh=0 form.
        _dealt = _wst.get("turn") is not None
        counts = []
        for m, _v in _wseats:
            counts.append(len(_whands.get(_push_key(m)) or []) if _dealt else 5)
        # A COM seat's hand is held under ("com", seat) -- the server is the COM
        # player (see `_queue_com_turns`), so these counts are the real
        # remaining cards, not a guess.
        for _k in range(len(_wseats), _wn):
            counts.append(len(_whands.get(("com", _k)) or []) if _dealt else 5)
        if not _dealt and _env_int("POL_TM_WATCH_SH0", 0):
            sh, mid_game = 0, None            # opt-in to the old dead-end form
        else:
            sh = 1
            mid_game = {"turn": _wst.get("turn") or 0,
                        "active": _wst.get("active") or 0,
                        "counts": counts, "board": _wboard,
                        "objects": _MATCH_OBJECTS.get(_wkey) or [],
                        "tiles": _board_tiles(_wn), "s": 0}
        # WARNING: THE SNAPSHOT SUPERSEDES THE QUEUE, SO CLEAR IT FIRST. Anything
        # already queued for this member is state from BEFORE the board we are
        # about to hand them -- up to and including a whole previous match a
        # still-registered watcher never drained. Delivered behind the
        # snapshot it reads as a card for a turn the client has passed:
        # `Errot!!Turn1!=0` -> "Invalid card data detected." See
        # `_watch_stream_purge` for the live trace.
        _watch_stream_purge(member_id,
                            "observing %s table %s at turn %s"
                            % (_wchan, _widx,
                               (mid_game or {}).get("turn", "pre-game")))
        _MATCH_WATCHERS.setdefault(_wkey, {})[
            _push_key(member_id)] = (member_id, peer_nick)
        _say("tm: 🔭 member %s OBSERVES %s table %s%s -- answering @WatchInfo "
             "sh=%d (%s), %d player(s), %d watcher(s) now. WARNING: the sh=1 /F=/CH= "
             "tile encoding is an EXPERIMENT -- watch the observer's screen."
             % (member_id, _wchan, _widx,
                " (a VS. COM game, key %r)" % (_wkey,) if _wcom else "", sh,
                "mid-game snapshot" if mid_game else "pre-game",
                _wn, len(_MATCH_WATCHERS.get(_wkey) or {})))
        _wreply = (_turn_code(WATCHINFO_CMD, sh)
                   + _watchinfo_body(_wnames, _wrules, _wn, mid_game))
        if mid_game and _env_int("POL_TM_WATCH_WFCARD", 1):
            # The board's card FACES ride behind as @WFCard, one per occupied
            # tile. FORMAT CORRECTED 2026-09-03 off arm 0x1020F4 (see
            # `_wfcard_body`): /T= is the TURN, the tile is an `@<tile>` marker,
            # /D= is the 6-value row -- the old `@WFCard=/T=<tile>/D=<8 vals>`
            # put the tile in the turn slot and named no tile at all.
            _wturn = int((_wst.get("turn") or 0))
            _wa = 1
            for _t in sorted(_wboard):
                if _wboard[_t].placer == tmbattle.OWNER_BLOCK:
                    continue      # a block has no card face (`@WFCard` /D=)
                _queue_push(member_id,
                            _turn_code(WFCARD_CMD, _wturn)
                            + _wfcard_body(_t, _wboard[_t].row, _wturn),
                            "watcher: tile %d's card" % _t,
                            after=_wa, peer=peer_nick)
                _wa += 1
        return _wreply

    if b"@TurnData=" in cmd:
        # THE CLIENT'S OWN ACK. 0x103801: after `@TurnData` is read, the arm
        # tests `[esi+0x32]` and, when it is 0, calls the sender at 0x106E30 for
        # command 9 -- `@TurnData=/Ans=<own seat>` (builder 0x10794A). It is a
        # receipt, not a request.
        #
        # WARNING: NEVER ANSWER WITH A (0x43, 9) BODY. Command 9 is the slot the
        # client READS `@TurnData` from, so a turn-shaped reply would be
        # re-read as another announcement. But in an ack-gated COM game this
        # ack is also the RELEASE: it proves the client has entered the turn
        # (past the 0xC7642 GamePlayEnd purge that eats an early @PutCard --
        # the 3-player "invalid card data" crash), so the pending COM put
        # answers it directly on its own (0x43, 10) slot. See
        # `_queue_com_turns` / `_com_play_acked`.
        _t = ((code or 0) >> 24) & 0xFF
        _say("tm: member %s acked turn %d (@TurnData=)" % (member_id, _t))
        _live_matches_write()
        if _env_int("POL_TM_COM_ACK_GATE", 1):
            _chan, _index, _seats = _match_of(member_id)
            if _seats and _in_com_game(member_id):
                # WARNING: THE SWALLOWED ACK (2026-09-06T23:35:47Z). A stale
                # PvP roster made `_seats` truthy, `_cn` was derived from it
                # BEFORE the COM override, and the COM's ack-released put
                # never went out -- "no answer for this command -- captured,
                # silent", COM never plays. Same override as `@CardSelect=`.
                _chan, _index, _seats = None, None, []
            _cn = None if _seats else _com_n(member_id)
            if _seats:
                # A DEPARTED SEAT'S TURN in a PvP match: this ack proves the
                # client is parked on (0x43, 10), so the server's put for
                # that seat answers it directly (see `_seat_departed`).
                _st = _MATCH_TURN.get((_chan, _index)) or {}
                _pending = _st.get("await_ack")
                _bseat = _st.get("bot_seat")
                if (_pending is not None and _bseat is not None
                        and 0 <= _bseat < len(_seats)
                        and _is_bot(_chan, _index, _seats[_bseat][0])):
                    if _pending != _t:
                        _say("tm: WARNING: member %s acked turn %d but the departed "
                             "seat's pending turn is %d -- following the client"
                             % (member_id, _t, _pending))
                    _st.pop("await_ack", None)
                    _st.pop("bot_seat", None)
                    return _bot_move(_chan, _index, _t, _bseat,
                                     direct_to=member_id)
            if _cn:
                _index = _push_key(member_id)
                _st = _MATCH_TURN.get((_chan, _index)) or {}
                _pending = _st.get("await_ack")
                _n = _st.get("n") or _cn
                if _pending is not None and _pending == _t:
                    _st.pop("await_ack", None)
                    return _com_play_acked(_chan, _index, member_id, _n, _t,
                                           _st.get("active", _t % _n))
                if _pending is not None and _pending != _t:
                    # The CLIENT owns the counter (0xC76A2, incl. its cardless
                    # skip) -- follow it, loudly.
                    _say("tm: WARNING: member %s acked turn %d but the pending COM "
                         "turn is %d -- following the client"
                         % (member_id, _t, _pending))
                    _st.pop("await_ack", None)
                    if _t % _n != 0:
                        return _com_play_acked(_chan, _index, member_id, _n,
                                               _t, _t % _n)
        return None

    if b"@PutData=" in cmd:
        # VERIFIED: A CARD IS PLAYED. Builder 0x1079EE (send table index cmd-5 = 10),
        # so the client asks on the same slot it will read the answer from:
        #
        #     /P= -> byte [ebp+0x19B]  the sender's own seat, which is ALWAYS 0
        #            in its own copy of the board -- so it does not
        #            identify anybody and the actor comes from the connection.
        #     /H= -> byte [obj+0x1B2]  the hand slot
        #     /F= -> byte [obj+0x1B1]  the board tile
        #     sh  -> byte [obj+0x18D]  the TURN, which `@PutCard` must echo
        #
        # The answer is `@PutCard` on the same (0x43, 10), and 0xC6FC7 is the
        # `cmp eax, 1` that both players -- placer and watcher alike -- are
        # parked on. The watcher gets it as a push on its own band.
        if not _env_int("POL_TM_PUTCARD", 1):
            _say("tm: member %s sent @PutData= -- POL_TM_PUTCARD=0, staying "
                 "silent" % (member_id,))
            return None
        turn = ((code or 0) >> 24) & 0xFF
        chan, index, seats = _match_of(member_id)
        # ...and the same stale-roster override as `@CardSelect=`, or the
        # placement lands under the PvP key the deal never used.
        if seats and _in_com_game(member_id):
            chan, index, seats = None, None, []
        com_n = None if seats else _com_n(member_id)
        if com_n:
            index = _push_key(member_id)    # per-member COM match key
        n = len(seats) or com_n or 2
        actor = next((i for i, (m, _v) in enumerate(seats)
                      if _push_key(m) == _push_key(member_id)), 0)
        _h = re.search(rb"/H=(-?\d+)", cmd or b"")
        _f = re.search(rb"/F=(-?\d+)", cmd or b"")
        _p = re.search(rb"/P=(-?\d+)", cmd or b"")
        hand_idx = int(_h.group(1)) if _h else 0
        tile = int(_f.group(1)) if _f else 0
        # WARNING: VS. COM, UNMEASURED TERRITORY: in a COM game the human is seat 0
        # in its own (and only) frame, so `/P=` nonzero can only be the client
        # reporting a COM seat's move -- a message shape no capture has shown
        # yet. We hold no hand for a COM seat (only `@CardSelect=` fills
        # `_MATCH_HANDS`, and only the human sends it), so don't guess a card:
        # log the whole thing where the next reading will look.
        if com_n and _p and int(_p.group(1)) != 0:
            _say("tm: WARNING: member %s @PutData= with /P=%s in a VS. COM game -- "
                 "the client is reporting a COM seat's move and we hold no "
                 "hand for it. NOT answered. Full body: %r. Decide from this "
                 "capture whether the client carries the card (/D=?) or "
                 "expects the server to play the COM."
                 % (member_id, _p.group(1).decode(), cmd))
            return None
        hand = (_MATCH_HANDS.get((chan, index)) or {}).get(_push_key(member_id)) or []
        if not 0 <= hand_idx < len(hand):
            # WARNING: DO NOT INVENT A CARD. A wrong `/D=` is a different card on the
            # board for both players and nothing anywhere reports it; an id at
            # or above the client's own limit ([0x2FF4FC]) or a type >= 4 is
            # `Error:CardNum=%d` and the arm bails. Say what is missing instead.
            _say("tm: WARNING: member %s played hand slot %d but we hold %d card(s) "
                 "for them -- no @PutCard sent. The hand is only ever learned "
                 "from @CardSelect= (see _MATCH_HANDS); if that message was "
                 "missed this match cannot be relayed."
                 % (member_id, hand_idx, len(hand)))
            return None
        row = hand[hand_idx]
        # WARNING: AND THE HAND COMPACTS. Measured off `0xC70F4`, the arm that runs
        # once a placement is applied:
        #
        #     edi = [obj+0x17A]                 the hand slot just played
        #     while edi < [0x2B657F + 48*p]:    the (already decremented) count
        #         copy slot[edi+1] -> slot[edi]      156 bytes, 0x2B65EC + 156*i
        #
        # so the five per-player slots at `0x2B65EC + 156*(player*5 + h)` are
        # LEFT-SHIFTED over the played one. `/H=` is therefore an index into the
        # REMAINING hand, and a server list that never shrinks answers the wrong
        # card from the second play onwards.
        #
        # Measured live 2026-08-20T16:36Z, and reported off the screen before it
        # was found ("it wound up putting a copy of the previous card"):
        # member 6 held 42,47,58,59,87 and played /H=0, /H=1, /H=1 -- three
        # different cards to the client, and this server sent 42, 47, **47**.
        del hand[hand_idx]
        st = _MATCH_TURN.setdefault((chan, index),
                                    {"turn": turn, "active": actor, "n": n})
        if turn != (st.get("turn") or 0):
            # Not fatal -- the CLIENT owns the counter between turns (0xC76A2)
            # and it is the authority. Follow it, and log the disagreement so a
            # desync is visible instead of silent.
            _say("tm: WARNING: member %s is on turn %d, we had %d -- following the "
                 "client (0xC76A2 increments it; @PutCard must echo its value "
                 "or the arm prints Errot!!Turn)"
                 % (member_id, turn, st.get("turn")))
        st["turn"], st["active"], st["n"] = turn, actor, n
        _live_matches_write()
        _say("tm: VERIFIED: member %s (seat %d) plays hand slot %d -- card %s -- onto "
             "tile %d, turn %d. Answering (0x43, %d) @PutCard= and pushing the "
             "same board to %d other player(s). Watch for "
             "'---->Recv=PUTCARD' then 'GamePlayPutCard=%d'."
             % (member_id, actor, hand_idx, row.split(b"|")[0].decode(), tile,
                turn, PUTCARD_CMD, n - 1, tile))
        for mid, _v in seats:
            if _push_key(mid) == _push_key(member_id):
                continue
            if _is_bot(chan, index, mid):
                continue                # a departed seat reads nothing
            seat = next((i for i, (m, _x) in enumerate(seats)
                         if _push_key(m) == _push_key(mid)), 0)
            _queue_push(mid,
                        _turn_code(PUTCARD_CMD, turn)
                        + _putcard_body(seats, seat, actor, hand_idx, tile, row),
                        "the card played on tile %d" % tile,
                        after=_env_int("POL_TM_PUTCARD_GAP", 0),
                        peer=_MATCH_PEER.get(_push_key(mid)))
        # ...AND TO THE WATCHERS, in the absolute frame (an observer was
        # never rotated -- @WatchInfo's /CN= occurrences are absolute).
        _watch_push(chan, index,
                    _turn_code(PUTCARD_CMD, turn)
                    + _putcard_body(seats, 0, actor, hand_idx, tile, row,
                                    n_solo=com_n),
                    "the card on tile %d" % tile)
        # The one-human roster for the loops below: battles and the next turn
        # go to the placer too, and `seats` is empty in a VS. COM game.
        _roster = seats or [(member_id, 0)]
        # VERIFIED: AND NOW THE BOARD. The placement goes through `_apply_placement`,
        # which is the server BEING THE RESOLVER -- the one job 0xCEE51 leaves
        # it: the client's own resolver is skipped outright when the network
        # flag is set, so a contested placement waits on (0x43, 12) for ever
        # unless this runs.
        #
        # WARNING: THE BATTLE GOES TO EVERY SEAT INCLUDING THE PLACER. Both clients
        # run the same battle scene off the same `@PutCard` -- measured, both
        # printed the whole `-Battle*` sequence and both parked -- so this is
        # not a watcher-only push.
        # WARNING: THE RESOLVER IS NOW RESUMABLE. A placement that can attack more than
        # one defender STOPS here and waits for the player's `@BattleSelect=/B=`
        # (0x43, 11) -- the multi-defender path that HUNG live 2026-09-03 because
        # the server auto-resolved every battle in its own order and never
        # handled command 11. `_advance_placement` resolves the 0- and 1-battle
        # cases immediately (byte-identical to the old inline path) and finishes
        # the turn; on a >1 case it parks in `_PENDING_BATTLE` and the
        # `@BattleSelect=` arm resumes it. The next-turn announcement therefore
        # rides `_advance_placement`'s own `_finish()`, NOT this reply.
        if _env_int("POL_TM_BATTLE", 1):
            if _begin_placement(chan, index, n, actor, tile, row, turn, seats,
                                com_n, member_id, _roster):
                _advance_placement(chan, index)
        elif _env_int("POL_TM_TURNDATA", 1):
            # POL_TM_BATTLE=0: no board resolution, but the turn still advances
            # exactly as before (the deal/turn machinery is independent).
            if com_n:
                _queue_com_turns(chan, index, member_id, n, turn + 1,
                                 (actor + 1) % n)
            else:
                _queue_next_turn(chan, index, seats, turn + 1, (actor + 1) % n,
                                 roster=_roster, n_solo=com_n)
        return _turn_code(PUTCARD_CMD, turn) + _putcard_body(
            seats, actor, actor, hand_idx, tile, row, n_solo=com_n)

    if b"@BattleSelect=" in cmd:
        # VERIFIED: THE MULTI-DEFENDER TARGET CHOICE. When a placed card can attack more
        # than one back-pointing defender, the client's picker (scene state 3,
        # 0xCE558) sends ONE `@BattleSelect=/B=<tile>` (0x43, cmd 11) naming the
        # ABSOLUTE tile it chose, then parks in state 8 polling (0x43, 12) for
        # the `@BattleData` we push -- it does NOT expect an echo of this
        # message. Combos are client-driven: after each @BattleData it
        # re-contests and, if >1 targets still remain, sends another
        # @BattleSelect (a single remaining target it auto-resolves silently).
        # So we resolve the named tile, push its @BattleData, and let
        # `_advance_placement` re-check for the next pause or the turn's end.
        # Unhandled, this was a ~75 s dead wait -> the live hang.
        if not _env_int("POL_TM_BATTLE", 1):
            _say("tm: member %s sent @BattleSelect= -- POL_TM_BATTLE=0, silent"
                 % (member_id,))
            return None
        _b = re.search(rb"/B=(-?\d+)", cmd or b"")
        chosen = int(_b.group(1)) if _b else None
        chan, index, seats = _match_of(member_id)
        if seats and _in_com_game(member_id):
            chan, index, seats = None, None, []
        if not seats and _com_n(member_id):
            index = _push_key(member_id)
        if (chan, index) not in _PENDING_BATTLE:
            _say("tm: member %s sent @BattleSelect=/B=%s but no placement is "
                 "awaiting a battle choice at %s table %s -- ignoring (stale, "
                 "already resolved, or a lost roster)"
                 % (member_id, chosen, chan, index))
            return None
        _say("tm: VERIFIED: member %s chose battle target tile %s (@BattleSelect=/B=) "
             "-- resolving it and any single follow-ups; another >1 choice will "
             "park again." % (member_id, chosen))
        _advance_placement(chan, index, chosen=chosen)
        return None

    if b"@ComGame=" in cmd:
        # VERIFIED: VS. COM, AND IT IS A REQUEST/REPLY ON ONE SLOT -- NOT "server
        # initiated", and not a UI gate either. Both readings in
        # the earlier in-game notes come from matching the wrong literal:
        # TM.dll carries TWO, `@ComGameInit` at rva 0x234AD8 (the parser at
        # 0x102D2A, which is what that reading found) and **`@ComGame=` at rva
        # 0x234D10**, whose only xref is a BUILDER --
        #
        #     0x106F52  0xA97C0(buf, "@ComGame=", 0x43, esi, cmd & 0xFF, 0)
        #
        # so the client SENDS `@ComGame=` on code 0x43 with the command id in the
        # msgid slot, then blocks in `0x101A10(2)` waiting for the answer to come
        # back in that same slot. Section 11e was right all along.
        #
        # Answer where it asked: same code, same msgid. `0x101A10` looks the
        # message up by the id it was called with, so a reply parked anywhere
        # else is a reply nothing drains -- this file's oldest lesson.
        if not _env_int("POL_TM_COMGAME", 1):
            _say("tm: member %s sent @ComGame= -- POL_TM_COMGAME=0, staying "
                 "silent (the client will raise Lobby.BIN 428)" % (member_id,))
            return None
        msgid = ((code or 0) >> 16) & 0xFF
        # KEY: `/Com=` IS THE ROSTER, AND THIS IS THE ONLY MESSAGE THAT CARRIES
        # IT. The COM character-select screen sends (msgid 6, live 13:57:26Z)
        #
        #     @ComGame=/Rule=100|0|1|1|1|0|0|0/Com=1|2
        #
        # one pipe entry per chosen COM opponent (builder 0x107239: [obj+0xE5]
        # count over the per-entry table at [obj+0x1CA]). The player count of
        # the match is 1 + len(/Com=); discarding it was the N=2 hardcode that
        # answered a 3-player game with `/Ans=1|1` and `/L=0|0`. The character
        # INDEXES are kept too -- the client owns the COM decks (they are
        # client-side data), but whoever ends up playing the COM's turns will
        # need to know which characters were picked.
        _com_m = re.search(rb"/Com=([0-9|]+)", cmd or b"")
        if _com_m and _push_key(member_id) is not None:
            coms = [int(v) for v in _com_m.group(1).split(b"|") if v]
            key = _push_key(member_id)
            entry = _COM_GAME.setdefault(
                key, {"n": None, "coms": [], "peer": peer_nick})
            entry["coms"], entry["n"] = coms, 1 + len(coms)
            if peer_nick:
                entry["peer"] = peer_nick
                _MATCH_PEER[key] = peer_nick
            _say("tm:   ...VS. COM roster: /Com=%s -> %d player(s) (1 human + "
                 "%d COM)." % ("|".join(str(c) for c in coms), entry["n"],
                               len(coms)))
        # VERIFIED: ECHO THE PLAYER'S OWN RULES. MEASURED BOTH WAYS 2026-08-25, and
        # this is the live report "the VS. COM settings I pick are ignored".
        #
        # The two messages use the SAME field order, and it is not `@Tet=`'s:
        #
        #   `@ComGame=/Rule=`  builder 0x106F87..0x107223, eight values --
        #       [+0xDC] (clamped 0..0x7D0), +0xE0, +0xE1, +0xE2, +0xE3, +0xE4,
        #       +0xD9, +0xE9
        #   `@ComGameInit=/R=` parser  0x102DE3..0x102ECB, SEVEN occurrences --
        #       0x102E05 -> [esi+0xDC], then +0xE0, +0xE1, +0xE2, +0xE3, +0xE4,
        #       and 0x102ECB -> [esi+0xD9]. There is no eighth: `+0xE9` is sent
        #       by the client and never read back.
        #
        # So the first seven positions line up exactly and the correct answer
        # is the client's own list, verbatim. What we were sending instead was
        # `_rule_seven`, which is the **`@Tet=`** order (bm, du, st, cb, ca,
        # gs, tl) -- a different mapping onto different slots. Live proof of
        # the damage, member 3 2026-08-25T04:09:06Z:
        #
        #     client  /Rule=135|1|1|1|3|0|0|0   -> DC=135 E0=1 E1=1 E2=1 E3=3
        #     we sent /R=135|0|1|1|1|1|3        -> DC=135 E0=0 E1=1 E2=1 E3=1
        #
        # every rule slot overwritten with a default the player did not pick.
        #
        # `_rule_seven` is untouched and still the fallback here: it also feeds
        # `@VsGameInit`, whose `/R=` lands on a DIFFERENT slot set
        # ([esi+0xE0..E4, E9, D9], no +0xDC), so this correction must not be
        # generalised to it without reading that parser too.
        # `POL_TM_COM_ECHO_RULES=0` restores the old defaults-only reply.
        rules = list(_rule_seven(None))
        _rl_all = re.search(rb"/Rule=(-?\d+(?:\|-?\d+)*)", cmd or b"")
        if _rl_all and _env_int("POL_TM_COM_ECHO_RULES", 1):
            _picked = [int(v) for v in _rl_all.group(1).split(b"|")]
            if len(_picked) >= 7:
                rules = _picked[:7]
                _say("tm:   ...VS. COM rules: echoing the player's own "
                     "/Rule= (%s) -- the first seven positions of /Rule= and "
                     "/R= are the same slots (0x106F87 builder, 0x102DE3 "
                     "parser)" % ",".join(str(v) for v in rules))
            else:
                _say("tm: WARNING: member %s sent /Rule= with only %d value(s) -- "
                     "expected >= 7 (DC,E0..E4,D9). Serving defaults; capture "
                     "this body, the builder emits eight."
                     % (member_id, len(_picked)))
        # THE WAGER (the wager's money half, measured static 2026-08-22). /Rule=
        # occ 0 is the stake the player chose on the COM select screen, and
        # our /R= occ 0 lands in [obj+0xDC] = 0x52464BC -- THE POT: the match
        # build (0xEC1CA) does `wallet -= pot` (floored 0), the win path
        # doubles it (0x10BC54, capped 0x7D0 = 2000; 0x10CF82 clears the
        # double when the wallet cannot cover it) and the mode-1 settlement
        # arms (0xD88E5 / 0xD8B1D / 0x10F947...) do `wallet += pot; pot = 0`.
        # 0x10C67B is the "%d,%03d" prize print. Serving /R=0 is why every
        # win paid 0. The server mirrors the client's own arithmetic below
        # (stake in here, settle at the result, refund on @GameExit) so the
        # save sync agrees with the local wallet. POL_TM_WAGER=0 restores
        # /R=0 and no server-side movement. WARNING: Per-outcome client arithmetic
        # beyond the win-double is INFERRED -- the check is the client's own
        # wallet display matching `money_of` after each outcome.
        wager = 0
        if not _com_wager_enabled():
            _say("tm:   ...VS. COM stake NOT taken: `/Rule=` occ 0 is not "
                 "measured to be the wager (see `_com_wager_enabled`) -- "
                 "POL_TM_WAGER_COM=1 to charge it anyway.")
        if _com_wager_enabled() and _push_key(member_id) is not None:
            _rl = re.search(rb"/Rule=(-?\d+)", cmd or b"")
            cap = _env_int("POL_TM_WAGER_CAP", 2000)
            wager = max(0, min(int(_rl.group(1)) if _rl else 0, cap))
            wager = min(wager, money_of(member_id))   # 0x10CF82's own rule
            rules[0] = wager
        _say("tm: member %s @ComGame= (0x43, msgid %d) -- answering IN THAT SLOT "
             "with @ComGameInit=, /R=%s. Look for '---->Recv=COMGAMEINIT' in the "
             "client's own trace." % (member_id, msgid,
                                      ",".join(str(v) for v in rules)))
        _reply = (encode_code(IN_MATCH_CODE | (msgid << 16))
                  + _comgame_body(member_id, rules))
        if wager > 0:
            # AFTER the body: /M= must carry the pre-stake balance -- the
            # client subtracts the pot itself at match build (0xEC1CA).
            _entry = _COM_GAME.setdefault(
                _push_key(member_id), {"n": None, "coms": [],
                                       "peer": peer_nick})
            _entry["staked"] = wager
            _set_money(member_id, money_of(member_id) - wager,
                       "VS. COM stake in (/R=%d; the client subtracts the "
                       "same pot at 0xEC1CA)" % wager)
            _stake_persist(member_id, wager)
            # WARNING: THE POT STILL DOES NOT REACH THE CLIENT -- OPEN (#7). The client
            # reads the pot from [obj+0xDC], written only by the @ComGameInit
            # parser from the (0x43,2) board it consumes. A re-push of the
            # wager-bearing board into (0x43,2) was tried 2026-09-02 and did NOT
            # work: the client read @ComGameInit a second time but the win screen
            # still printed prize 0, so the pot was already latched (as 0) before
            # that read. The client does not re-poll (0x43,2) after committing the
            # wager. Cracking this needs a live memory probe on [obj+0xDC]
            # (0x52464BC) to see exactly when it is zeroed -- not another blind
            # push. Server settlement is correct regardless (the win pays out);
            # only the client's on-screen prize/pot is wrong.
        return _reply

    if b"@Rule=" in cmd:
        # THE TABLE-RULES REQUEST. Answer with the same 0x13 body the push
        # sends. The table is identified by the message's own u64 (0x86ED0 reads
        # it from the envelope, not from a field), so the answer goes back on the
        # connection it arrived on and needs no id of ours.
        if not _env_int("POL_TM_TABLE_INFO", 1):
            return None
        idx = None
        try:
            import tmroom
            chan = tmroom.room_of(member_id) if member_id is not None else None
            if chan:
                idx, why = table_index_for_peer(peer_nick, tmroom.room_peer(chan), chan=chan)
                _say("tm: member %s @Rule= -- table %s [%s]"
                     % (member_id, idx if idx else "UNRESOLVED", why))
                fields = _table_rules_get(chan, idx)
                if fields:
                    return encode_code(TABLE_INFO_CODE) + table_info_body(fields)
        except Exception as exc:
            _say("tm:   @Rule= lookup failed (%r)" % (exc,))
        _say("tm:   ...no stored settings for that table -- staying silent "
             "rather than inventing a ruleset the owner never chose")
        return None

    if b"@MuchMakeAns=" in cmd:
        # THE CLIENT ACCEPTING A MATCH. Nothing to answer (yet) -- what matters
        # is that it ARRIVED, because that is the proof the push landed and was
        # understood. Log it loudly; it is the measurement this whole path needs.
        m = re.search(rb"/Ans=(-?\d+)", cmd)
        _say("tm: member %s ANSWERED THE MATCH PUSH -- @MuchMakeAns=/Ans=%s"
             % (member_id, m.group(1).decode() if m else "?"))
        return None

    if b"@CheckJoinTable=" in cmd:
        # THE SECOND PLAYER TRYING TO SIT DOWN. Unanswered this is a dead wait,
        # which is the whole of "nothing else can be done". See MSG_CHECKJOIN.
        if os.environ.get("POL_TM_CHECKJOIN", "1") != "1":
            return None
        join = _env_int("POL_TM_JOIN", 1) & 1
        mem = _env_int("POL_TM_JOIN_MEM", 1) & 1
        _say("tm: member %s @CheckJoinTable= (peer %s) -- answering /Join=%d"
             "/Mem=%d; the client packs these as Join | (Mem << 1)"
             % (member_id, peer_nick or b"-", join, mem))
        return (encode_code(MSG_CHECKJOINANS)
                + (b"@CheckJoinTable_Ans=/Join=%d/Mem=%d" % (join, mem)))

    if b"@Tab=" in cmd or b"@Tet=" in cmd:
        # THE TABLE SETTINGS, AND THE SERVER HAS NEVER LISTENED. The wire
        # reading found this on 2026-08-18 and it is still true: every one of these
        # met "no answer for this command -- captured, silent", which is the whole
        # of "the Save option does nothing" and half of "the restrictions are not
        # the defaults".
        #
        # Two carriers, both measured:
        #   code 0x24  `@Save=/Tbl=<n>/Game=<n>` + the pair -- the SAVE button,
        #              and the ONLY message that names a table.
        #   code 0x14  the bare pair -- sent on Confirm. NAMES NO TABLE, which is
        #              exactly why this is capture-and-log before it is anything
        #              else: `/Tbl=` is the one field that would let us publish
        #              the reservation, and the common path does not carry it.
        #
        # WARNING: NOTHING IS ANSWERED HERE ON PURPOSE. Whether `@Save=` has a reply
        # the client waits for is UNMEASURED, and inventing one is the habit this
        # subsystem has been burned by ten times. What this does is store the set
        # and say so, so the next reservation is evidence instead of a shrug.
        _note_table_settings(cmd, member_id, code, peer_nick)
        # ...and if it is the PRESET write, store it. This is the half of the
        # settings work that was still open: `@Save=` is "save my custom default table
        # values", the client re-reads them from the SAVE at +0xB8..+0xF0 on the
        # next login (`0x14D9F0`), and we had never written a byte there.
        if (code or 0) & 0xFF == TABLE_SETTINGS_PRESET:
            _persist_table_preset(cmd, member_id)
        # WARNING: `POL_TM_SAVE_ANS` -- THE SWEEP KNOB FOR THE UNMEASURED QUESTION ABOVE.
        #
        # DEFAULT 0, i.e. the silence this arm has always kept. Nothing about the
        # shipped behaviour changes; do not read this block as a decision that
        # `@Save=` has a reply. It does not invent one, it makes measuring
        # whether it needs one cost ONE ENV VAR instead of a code change and a
        # redeploy -- and this file already records what that costs: the VS. COM
        # session that "spun for 116 retries" was read only after exactly that
        # round trip (see the no-handler note in responders.py).
        #
        # 2026-08-26 completeness sweep: `@Tet=`/`@Save=` are the ONLY commands
        # in the whole measured window (`logs/authserv.log`, 2026-08-17..08-19)
        # that were unanswered then and are STILL unanswered now -- `@Get=`,
        # `@GameEN=`, `@Sell=`, `@GameQT=` have all since grown handlers, and
        # `@Pong=`'s silence is deliberate and correct (it is the push carrier).
        # That makes this the highest-value single measurement left outside the
        # match itself.
        #
        # THE PRECEDENT IS DIRECTLY BELOW: `@Ready=` sat unanswered too, hung the
        # win screen on the first COM game ever completed, and was released by a
        # well-formed echo in the same slot. If `@Save=` behaves the same way,
        # this knob is the one-line confirmation; if it does not, the silence was
        # right and that is worth knowing just as much.
        if _env_int("POL_TM_SAVE_ANS", 0):
            _say("tm: member %s @Tab=/@Tet=/@Save= (code 0x%02x) -- "
                 "POL_TM_SAVE_ANS=1, echoing the message back in the same slot. "
                 "WARNING: THIS SHAPE IS UNMEASURED. Watch for the Save/Confirm dialog "
                 "closing (success) or the client sitting on it anyway (the echo "
                 "is not what it wants)." % (member_id, (code or 0) & 0xFF))
            return body
        _say("tm: member %s @Tab=/@Tet=/@Save= (code 0x%02x) -- stored, "
             "answering NOTHING (the shipped default). Set POL_TM_SAVE_ANS=1 to "
             "echo it back and find out whether this client is waiting on a "
             "reply; that question is still UNMEASURED."
             % (member_id, (code or 0) & 0xFF))
        return None

    if b"@Ready=" in cmd and b"@GameReady=" not in cmd \
            and (code or 0) & 0xFF == IN_MATCH_CODE:
        # WARNING: the guards matter: `@GameReady=` CONTAINS `@Ready=` (its own
        # handler sits earlier, but substring order must not be load-bearing
        # twice), and the READY arm exists only in the 0x43 table.
        # THE POST-RESULT HANDSHAKE -- THE WIN-SCREEN HANG. Measured live
        # 2026-08-22T14:51:56Z, the first COM game ever played to completion:
        # @ResultData was read, the win screen drew, and the client sent
        #
        #     code=67 msgid=0x0e sh=10  @Ready=/Ans=0
        #
        # and hung, because nothing here answered it. The receive arm
        # (0x1045E7, Recv=READY, command 14) is the simplest in the family:
        # name match, the session-pair gate, parse `/Go=`, result = 1
        # unconditionally (0x105174) -- so a well-formed echo with /Go=
        # present releases the scene. /Go='s consumer is not yet read;
        # 1 is the natural "go" and POL_TM_READY_GO sweeps it.
        if not _env_int("POL_TM_READY", 1):
            _say("tm: member %s sent @Ready= -- POL_TM_READY=0, staying "
                 "silent (the result screen will hang)" % (member_id,))
            return None
        go = _env_int("POL_TM_READY_GO", 1)
        _live_matches_write()
        _say("tm: member %s @Ready= (0x43, 14) -- the post-result handshake; "
             "answering /Go=%d in the same slot. Watch for '---->Recv=READY'."
             % (member_id, go))
        # THE PANEL EXISTS FROM HERE. Mark it, and if the other seat(s) have
        # already voted, hand this one the live status one reply behind this
        # answer -- see `_CONTINUE_READY`.
        _rc, _ri, _rs = _match_of(member_id)
        if _rs and not _in_com_game(member_id):
            # WARNING: A DECISIVE GAME SENDS @Ready= TWICE (measured 2026-09-07
            # 14:45Z): once off the result screen, once when the take scene
            # ends. Only the second one opens the panel. And the LOSER's second
            # comes SIX SECONDS after the take list, before the winner has
            # picked -- its scene ends on our /Go= answer and the winner sees
            # "Player has left the game" (41e). `POL_TM_READY_HOLD_LOSER=1`
            # HOLDS that answer until the winner's @GetSelect lands; the
            # release rides one reply behind the @GetSelect push, so the
            # loser's scene shows the taken card first.
            _st_r = _MATCH_TURN.get((_rc, _ri)) or {}
            _take_r = _st_r.get("take") or {}
            _k_r = _push_key(member_id)
            _rn = _st_r.setdefault("ready_n", {}) if _st_r else {}
            _rn[_k_r] = int(_rn.get(_k_r) or 0) + 1
            _decisive = bool(_take_r)
            _pools_r = _take_r.get("pools") or []
            _losers_r = [s2 for s2 in range(len(_pools_r))
                         if s2 != _take_r.get("win") and _pools_r[s2]]
            _pending_r = (_decisive and
                          len(_take_r.get("picked") or []) < len(_losers_r))
            _tros_r = _take_r.get("roster") or []
            _my_r = next((i for i, m in enumerate(_tros_r)
                          if _push_key(m) == _k_r), None)
            _is_winner = _decisive and _my_r == _take_r.get("win")
            _hold_mode = _env_int("POL_TM_READY_HOLD_LOSER", 0)
            if _pending_r and not _is_winner and _rn[_k_r] >= 2 and _hold_mode:
                _take_r.setdefault("held_ready", {})[_k_r] = body[:8]
                if _hold_mode == 2:
                    # MODE 2 (measured 14:59Z that mode 1's held reply changes
                    # nothing: the scene ends before any reply): answer NOW
                    # with /Go=0 and push /Go=1 once the winner has picked --
                    # does the scene wait on the VALUE?
                    _say("tm:   ...member %s's 2nd @Ready= (the take scene): "
                         "answering /Go=0 now, /Go=1 follows the winner's pick "
                         "(POL_TM_READY_HOLD_LOSER=2)" % (member_id,))
                    return body[:8] + b"@Ready=/Go=0"
                _say("tm:   ...member %s's 2nd @Ready= (the take scene) is HELD "
                     "until the winner picks (POL_TM_READY_HOLD_LOSER=1)"
                     % (member_id,))
                return None
            if _decisive and _rn[_k_r] < 2:
                _say("tm:   ...member %s's 1st @Ready= of a decisive game -- the "
                     "take scene is next, not the panel; not marking the "
                     "panel open" % (member_id,))
                return body[:8] + (b"@Ready=/Go=%d" % go)
            _CONTINUE_READY.setdefault((_rc, _ri), set()).add(_k_r)
            _tl = _MATCH_CONTINUE.get((_rc, _ri)) or {}
            _vt = _tl.get("votes") or {}
            _seats_r = _tl.get("seats") or _rs
            _ans_r = [_vt.get(_push_key(m), CONTINUE_UNANSWERED)
                      for m, _v in _seats_r]
            _me_r = next((i for i, (m, _v) in enumerate(_seats_r)
                          if _push_key(m) == _push_key(member_id)), None)
            if (_me_r is not None and _ans_r[_me_r] == CONTINUE_UNANSWERED
                    and any(a != CONTINUE_UNANSWERED for a in _ans_r)
                    and _env_int("POL_TM_CONTINUE_PARTIAL", 0)
                    and _env_int("POL_TM_CONTINUE_READY_GATE", 1)):
                _queue_push(member_id,
                            _turn_code(CONTINUE_CMD,
                                       _env_int("POL_TM_CONTINUE_SH", 2))
                            + _continue_body(_continue_frame(list(_ans_r), _me_r),
                                             _env_int("POL_TM_CONTINUE_B", 0)),
                            "the @Continue live status",
                            after=_env_int("POL_TM_CONTINUE_READY_GAP", 1),
                            peer=_MATCH_PEER.get(_push_key(member_id)))
                _say("tm:   ...the panel opens with a vote already in (%s): "
                     "pushing the live status one reply behind the @Ready "
                     "answer" % "|".join(str(v) for v in _ans_r))
        return body[:8] + (b"@Ready=/Go=%d" % go)

    if b"@GetSelect=" in cmd:
        # THE WINNER TAKES A CARD. Sender 0x107C6F: `@GetSelect=/H=<slot>`,
        # sh = the sender's own seat -- the winner naming which of the loser's
        # cards it takes. The receive arm (0x104E5B, Recv=GETSELECT, command
        # 15) parses `/E=` and `/H=` and stores /H='s byte into
        # [0x52463C4]+0x1B2 -- the same hand-slot global @PutData reads -- so
        # the answer is the confirmation the scene acts on. /E='s consumer is
        # unread; 0 as the least-committal value, POL_TM_GETSEL_E sweeps it.
        if not _env_int("POL_TM_GETSELECT", 1):
            return None
        _h = re.search(rb"/H=(-?\d+)", cmd or b"")
        hsel = int(_h.group(1)) if _h else 0
        ev = _env_int("POL_TM_GETSEL_E", 0)
        _live_matches_write()
        chan, index, seats = _match_of(member_id)
        _say("tm: member %s @GetSelect= (0x43, 15) picked hand slot %d -- "
             "echoing /E=%d/H=%d" % (member_id, hsel, ev, hsel))
        # The transfer half of the take: the pick is real now. The take state was
        # stashed by the result block (win/lose seats, every pool by slot,
        # the seat->member roster); the picked card joins the winner's
        # COLLECTION and -- when the losing seat is a REAL member (PvP) --
        # leaves the loser's. ONE PICK PER LOSING SEAT (reported live 2026-08-22,
        # first 3-player win: "it lets me take one card from each com" --
        # two @GetSelects, nothing on the wire naming the pool), consumed in
        # ascending seat order: the take screen walks the losers one panel
        # at a time, so pick k indexes losers[k]'s pool. In a COM game the
        # losing seat has no member behind it and nothing is removed.
        # POL_TM_TAKE_WIN=0 restores the display-only behaviour;
        # POL_TM_TAKE_PVP=0 keeps PvP out of the take flow entirely.
        _tkey = None
        if _env_int("POL_TM_TAKE_WIN", 1):
            if not seats and _com_n(member_id):
                _tkey = (chan, _push_key(member_id))
            elif seats:
                _tkey = (chan, index)
        if _tkey is not None:
            _tst = _MATCH_TURN.get(_tkey) or {}
            _take = _tst.get("take")
            _tros = (_take or {}).get("roster") or []
            _my_seat = next((i for i, m in enumerate(_tros)
                             if _push_key(m) == _push_key(member_id)), 0)
            if _take and _take.get("win") == _my_seat:
                _pools = _take.get("pools") or []
                _losers = _losers_from(_my_seat, _pools)
                _picked = _take.setdefault("picked", [])
                if len(_picked) < len(_losers):
                    _seatL = _losers[len(_picked)]
                    _pool = _pools[_seatL]
                    if 0 <= hsel < len(_pool):
                        _row = _pool[hsel]
                        _card = [int(v) for v in _row.split(b"|")]
                        _picked.append(_card[0])
                        _collection_add_cards(member_id, [_card])
                        _lmid = (_tros[_seatL]
                                 if _seatL < len(_tros) else None)
                        if _lmid is not None \
                                and _push_key(_lmid) != _push_key(member_id) \
                                and _env_int("POL_TM_TAKE_LOSS", 1):
                            _collection_remove_card(_lmid, _card[0],
                                                    row=_row)
                        _say("tm: VERIFIED: member %s TAKES card %d (slot %d of "
                             "seat %d's pool%s; pick %d of %d)"
                             % (member_id, _card[0], hsel, _seatL,
                                (" = member %s" % _lmid)
                                if _lmid is not None else " (COM)",
                                len(_picked), len(_losers)))
                    else:
                        _say("tm: WARNING: member %s @GetSelect=/H=%d is outside "
                             "seat %d's %d-card pool -- nothing transferred"
                             % (member_id, hsel, _seatL, len(_pool)))
                else:
                    _say("tm:   ...all %d picks already committed for this "
                         "match -- echo only" % len(_losers))
            elif _take:
                _say("tm:   ...the winner is seat %d, sender is seat %d -- "
                     "echo only" % (_take.get("win"), _my_seat))
        # In PvP the other seats watch the take on the same slot; a COM game
        # has nobody else to tell.
        for mid, _v in seats:
            if _push_key(mid) == _push_key(member_id):
                continue
            _queue_push(mid, body[:8] + (b"@GetSelect=/E=%d/H=%d" % (ev, hsel)),
                        "the winner's take",
                        after=_env_int("POL_TM_GETSEL_GAP", 1),
                        peer=_MATCH_PEER.get(_push_key(mid)))
        # RELEASE the loser(s) whose take-scene @Ready= was held (see the
        # @Ready= arm): one reply behind the @GetSelect push, so their scene
        # renders the pick and then ends; their panel opens after that.
        if seats:
            _tk2 = (chan, index)
            _take2 = (_MATCH_TURN.get(_tk2) or {}).get("take") or {}
            _held = _take2.get("held_ready") or {}
            _pools2 = _take2.get("pools") or []
            _losers2 = [s2 for s2 in range(len(_pools2))
                        if s2 != _take2.get("win") and _pools2[s2]]
            _done2 = len(_take2.get("picked") or []) >= len(_losers2)
            if _held and (_done2 or not _env_int("POL_TM_TAKE_WIN", 1)):
                for _hk, _hdr in list(_held.items()):
                    _hm = next((m for m, _v in seats if _push_key(m) == _hk),
                               None)
                    if _hm is None:
                        continue
                    _queue_push(_hm, _hdr + (b"@Ready=/Go=%d"
                                             % _env_int("POL_TM_READY_GO", 1)),
                                "the held @Ready (the take is done)",
                                after=_env_int("POL_TM_GETSEL_GAP", 1) + 1,
                                peer=_MATCH_PEER.get(_hk))
                    _CONTINUE_READY.setdefault(_tk2, set()).add(_hk)
                    _say("tm:   ...releasing member %s's held @Ready= one reply "
                         "behind the @GetSelect push; its panel opens next"
                         % (_hm,))
                _take2["held_ready"] = {}
        return body[:8] + (b"@GetSelect=/E=%d/H=%d" % (ev, hsel))

    if b"@Continue=" in cmd:
        # WARNING: THE POST-GAME "PLAY AGAIN?" PANEL -- A QUORUM VOTE, AND THE ONE
        # MESSAGE IN THIS FAMILY WHOSE REPLY CAN BE PARSED AND STILL NOT COUNT.
        #
        # Read statically off arm 0x104EE1 on 2026-08-21.
        # NOTHING HERE HAS BEEN ON A WIRE. The client says in one line whether
        # it landed: `---->Recv=CONTINUE` (0x104F1A) plus `probe_lookup`'s
        # `(0x43, 17, n, found)`.
        #
        # What the client sends (builder 0x107CEC, through 0x106E30's table):
        #     /Ans=  its own answer      1 = play again, 2 = decline
        #     /Conf= 0, or 1 for the "...but change the settings first" button
        #            (that path then waits on command 4, `@ComList`) -- INFERRED
        #     /PR=   a persisted option, inferred to be the prize-ranking name
        #            display (0xCBB22/0xCBC05 write it beside the save byte
        #            0x294C86). We log it; we do not persist it yet.
        #     sh   = the sender's own seat, which is ALWAYS 0 in its own frame.
        #
        # WARNING: AND THAT LAST LINE IS THE TRAP. `0x104FAC` is the ONLY site in the
        # arm that sets 0x101A10's return value, and it fires only when the
        # reply's third envelope byte is non-zero:
        #
        #     if ([obj+0xEB] != 0) [esp+0x24] = 1
        #
        # So a reply with sh = 0 is parsed, applied, logged as
        # `---->Recv=CONTINUE`, and THEN reported as "no answer" -- the message
        # is gone from the store and the panel waits for ever. Answering this
        # the way `@Quit=` does, by echoing `body[:8]`, would echo the client's
        # own sh = 0 and swallow every reply. `_turn_code` puts `sh` in the
        # third byte; the assertion below refuses to send a zero.
        #
        # The contract, from the four waiters (0x10CA50 / 0x10D66A / 0x10D996 /
        # 0x10E07E):
        #     sh = 1     "the rematch is ON", and nothing else may use it --
        #                state 6 checks only the recipient's own slot before it
        #                re-deals (0x4000054 -> 0xC243E).
        #     sh >= 2    a status update: own slot 3 => leave (0x4000053), own
        #                slot 0/1 => redraw and keep waiting, own slot 2 =>
        #                ignored.
        # WARNING: A CLIENT THAT ANSWERED 2 LEAVES THE PANEL ONLY BY HAVING ITS OWN
        # SLOT RAISED TO 3 WITH sh >= 2. State 7 tests nothing else.
        if not _env_int("POL_TM_CONTINUE", 1):
            _say("tm: member %s sent @Continue= -- POL_TM_CONTINUE=0, staying "
                 "silent (the panel will sit on 'Thinking...')" % (member_id,))
            return None
        _a = re.search(rb"/Ans=(-?\d+)", cmd or b"")
        _c = re.search(rb"/Conf=(-?\d+)", cmd or b"")
        _p = re.search(rb"/PR=(-?\d+)", cmd or b"")
        ans = int(_a.group(1)) if _a else CONTINUE_UNANSWERED
        _live_matches_write()
        chan, index, seats = _match_of(member_id)
        _say("tm: member %s @Continue= /Ans=%d%s%s -- the post-game "
             "'Play again?' panel"
             % (member_id, ans,
                "" if not _c else " /Conf=%s" % _c.group(1).decode(),
                "" if not _p else " /PR=%s" % _p.group(1).decode()))
        if not seats:
            com_n2 = _com_n(member_id)
            if com_n2:
                # THE EIGHTH SEAT GATE. A VS. COM game has no roster and this
                # branch stayed silent -- the "Play again?" panel would think
                # for ever. The COM seats' answers are OURS: the COM always
                # agrees, so the human's answer IS the vote. /Ans=1 -> sh=1,
                # everyone 1, rematch ON (the client re-picks cards; the deal
                # block re-deals the COM hands and pops the board). /Ans=2 ->
                # sh=2 with the human's own slot 3, which is the only test
                # state 7 makes before tearing the panel down.
                bval2 = _env_int("POL_TM_CONTINUE_B", 0)
                ckey = (None, _push_key(member_id))
                if ans == CONTINUE_AGAIN:
                    sh2 = 1
                    vals2 = [CONTINUE_AGAIN] * com_n2
                    _reset_for_rematch(*ckey)
                    _say("tm:   ...VS. COM: the COM always plays again -- "
                         "sh=1, rematch ON, per-match state reset")
                    # WARNING: AND /Conf=1 IS THE "CHANGE THE SETTINGS FIRST"
                    # BUTTON. That path waits on (0x43, 4) `@ComList`, which
                    # this server had never sent -- so the screen froze and the
                    # client went SILENT, because a slot poll is a local lookup
                    # and puts nothing on the wire. Measured three times live
                    # 2026-09-07 (Conf=1 froze, Conf=0 did not).
                    _conf = int(_c.group(1)) if _c else 0
                    if _conf == 1 and _env_int("POL_TM_COMLIST", 1):
                        _cl = (_turn_code(COMLIST_CMD,
                                          _env_int("POL_TM_COMLIST_SH", 0))
                               + _comlist_body(_comlist_values()))
                        _queue_push(member_id, _cl,
                                    "the COM opponent list (@ComList)",
                                    after=_env_int("POL_TM_COMLIST_GAP", 1),
                                    peer=_MATCH_PEER.get(_push_key(member_id)))
                        _say("tm:   ...and /Conf=1 is CHANGE SETTINGS -- "
                             "queueing (0x43, %d) @ComList=/L=%d. Watch for "
                             "'---->Recv=COMLIST'. POL_TM_COMLIST=0 disables, "
                             "POL_TM_COMLIST_VALUES sweeps the row values "
                             "(their meaning is UNMEASURED)."
                             % (COMLIST_CMD, len(_comlist_values())))
                else:
                    sh2 = _env_int("POL_TM_CONTINUE_SH", 2)
                    vals2 = [CONTINUE_FINAL] + [CONTINUE_DECLINE] * (com_n2 - 1)
                    _say("tm:   ...VS. COM: human declined -- own slot %d, "
                         "sh=%d tears the panel down"
                         % (CONTINUE_FINAL, sh2))
                return (_turn_code(CONTINUE_CMD, sh2)
                        + _continue_body(_continue_frame(vals2, 0), bval2))
            # THE LIVE ROSTER IS GONE -- but a PENDING VOTE may still know it.
            # Measured live 2026-08-22T19:43Z: member 11 voted, a deploy
            # restart wiped _MATCH_ROSTER (and the other vote) 13s before
            # member 6's, and both clients parked on "Now loading" behind a
            # reply that never came. The vote tally now snapshots its seats,
            # so an expiry/roster race inside one process resumes here...
            for _k2, _t2 in list(_MATCH_CONTINUE.items()):
                _snap = _t2.get("seats") if isinstance(_t2, dict) else None
                if _snap and any(_push_key(m) == _push_key(member_id)
                                 for m, _v in _snap):
                    chan, index = _k2
                    seats = list(_snap)
                    _say("tm:   ...live roster gone, but a pending vote at "
                         "%r remembers this member -- resuming that quorum"
                         % (_k2,))
                    break
        if not seats:
            # ...and a vote NOTHING remembers (the restart case: both the
            # roster and the other player's vote died) gets the ORPHAN
            # teardown instead of a hang: sh=2 with a FULL-WIDTH all-3 list.
            # Slot 3 under sh>=2 is the one test state 7 makes, the rotation
            # is identity on a uniform list, and a LONGER-than-N list is safe
            # (the arm reads exactly its own N occurrences; the measured hazard was
            # a SHORT list). POL_TM_CONTINUE_ORPHAN=0 restores the silence.
            if not _env_int("POL_TM_CONTINUE_ORPHAN", 1):
                _say("tm:   ...no roster, no pending vote -- staying silent "
                     "(POL_TM_CONTINUE_ORPHAN=0)")
                return None
            _say("tm:   ...no roster and no pending vote (a restart raced "
                 "the panel) -- answering the orphan teardown: sh=%d, "
                 "/Ans=3|3|3|3" % _env_int("POL_TM_CONTINUE_SH", 2))
            return (_turn_code(CONTINUE_CMD, _env_int("POL_TM_CONTINUE_SH", 2))
                    + _continue_body([CONTINUE_FINAL] * 4,
                                     _env_int("POL_TM_CONTINUE_B", 0)))
        n = len(seats)
        me = next((i for i, (m, _v) in enumerate(seats)
                   if _push_key(m) == _push_key(member_id)), 0)
        key = _push_key(member_id)
        tally = _MATCH_CONTINUE.setdefault((chan, index), {})
        # The seats snapshot is what the resume path above searches; votes
        # moved under their own key so the snapshot cannot collide with a
        # member key.
        tally.setdefault("seats", [(m, v) for m, v in seats])
        votes = tally.setdefault("votes", {})
        # A departed seat can never answer: it declined when it left.
        for _bm, _bv in seats:
            if _is_bot(chan, index, _bm):
                votes[_push_key(_bm)] = max(votes.get(_push_key(_bm),
                                                      CONTINUE_UNANSWERED),
                                            CONTINUE_DECLINE)
        votes[key] = max(votes.get(key, CONTINUE_UNANSWERED), ans)
        answers = [votes.get(_push_key(mid), CONTINUE_UNANSWERED)
                   for mid, _v in seats]
        waiting = [mid for (mid, _v), v in zip(seats, answers)
                   if v == CONTINUE_UNANSWERED]
        bval = _env_int("POL_TM_CONTINUE_B", 0)
        if waiting:
            # VERIFIED: LIVE STATUS on the "Play again?" panel (reported live 2026-09-03:
            # "neither can see if the one has selected something"). Push each
            # STILL-WAITING seat an sh>=2 partial tally so its panel redraws
            # (0x10D996: own slot 0 => redraw and keep waiting) now showing who
            # has already answered. Two safety rules make this the RIGHT refresh,
            # not the "answer thrown away" hazard the old silence avoided:
            #   * only UNANSWERED seats are pushed -- a seat that answered 1 gets
            #     bounced back to the interactive panel by an sh>=2 update, so we
            #     never send one to an answered seat;
            #   * the VOTER gets NO reply (its own client already shows its own
            #     choice; a reply carrying its own slot would reset it too).
            # The quorum resolution below still sends everyone the final tally.
            # In 2-player this is exactly right (the lone other seat is always
            # the unanswered one). `POL_TM_CONTINUE_PARTIAL=0` restores silence.
            # WARNING: OFF BY DEFAULT since 2026-09-07T15:14Z. The sh>=2 partial
            # tally delivered to a panel that has NOT answered greys its Yes:
            # the winner's panel opened 15:14:02, the loser voted :07, the
            # status landed :08 and the winner's Yes greyed -- 41d exactly,
            # reproduced with every other cause removed (@GetAway to nobody,
            # the panel gated on the 2nd @Ready). The client has no way to
            # show "the other one chose" without that side effect.
            if _env_int("POL_TM_CONTINUE_PARTIAL", 0):
                _psh = _env_int("POL_TM_CONTINUE_SH", 2)
                _pushed = 0
                for i, (mid, _v) in enumerate(seats):
                    if i == me or answers[i] != CONTINUE_UNANSWERED:
                        continue
                    if (_env_int("POL_TM_CONTINUE_READY_GATE", 1)
                            and _push_key(mid) not in
                            (_CONTINUE_READY.get((chan, index)) or set())):
                        _say("tm:   ...seat %d (member %s) has not sent @Ready= "
                             "-- its panel does not exist yet; the live status "
                             "waits for it (read after the seat answers, it "
                             "bounces the panel -- the 04:37Z greyed Deck)"
                             % (i, mid))
                        continue
                    _queue_push(mid,
                                _turn_code(CONTINUE_CMD, _psh)
                                + _continue_body(_continue_frame(list(answers), i),
                                                 bval),
                                "the @Continue live status",
                                after=_env_int("POL_TM_CONTINUE_GAP", 0),
                                peer=_MATCH_PEER.get(_push_key(mid)))
                    _pushed += 1
                _say("tm:   ...%d of %d answered (%s); pushed live status to %d "
                     "waiting seat(s), holding the resolution -- still on %s"
                     % (n - len(waiting), n, "|".join(str(v) for v in answers),
                        _pushed, ", ".join(str(m) for m in waiting)))
            else:
                _say("tm:   ...%d of %d have answered (%s); holding the reply "
                     "(POL_TM_CONTINUE_PARTIAL=0) -- still waiting on %s"
                     % (n - len(waiting), n,
                        "|".join(str(v) for v in answers),
                        ", ".join(str(m) for m in waiting)))
            return None
        if not waiting and all(v == CONTINUE_AGAIN for v in answers):
            sh = 1
            outcome = "EVERYONE said yes -- the rematch is ON"
            final = list(answers)
            _reset_for_rematch(chan, index)
            _say("tm:   ...and the per-match state is reset: a rematch never "
                 "sends @VsGameInit again, so nothing else would have cleared "
                 "the hands, the board or the turn counter.")
            # WARNING: ...AND NEITHER WOULD ANYTHING HAVE STAKED IT. A rematch sends
            # no @VsGameInit, so the accept-quorum stake above never runs and
            # every rematch used to be played for nothing while the client's
            # own "Play again?" panel showed a pot -- and, under `du`, a
            # DOUBLED one. The panel is the client half of this; see
            # `_double_up_pot` for the five instructions it is built on.
            # POL_TM_WAGER_REMATCH=0 restores the unstaked behaviour.
            if _env_int("POL_TM_WAGER", 1)                     and _env_int("POL_TM_WAGER_PVP", 1)                     and _env_int("POL_TM_WAGER_REMATCH", 1):
                _rpot = _double_up_pot(chan, index, seats)
                if _rpot > 0:
                    _stake_pvp_pot(chan, index, seats, _rpot, "rematch pot")
                    _say("tm:   ...the rematch is staked at %d a seat "
                         "(du=%s)"
                         % (_rpot,
                            (_table_rules_get(chan, index) or {}).get(
                                "du", TET_DEFAULTS["du"])))
        elif not waiting:
            sh = _env_int("POL_TM_CONTINUE_SH", 2)
            outcome = ("somebody declined (%s) -- everyone leaves"
                       % "|".join(str(v) for v in answers))
            final = None                     # per recipient: own slot -> 3
        else:
            sh = _env_int("POL_TM_CONTINUE_SH", 2)
            outcome = "a partial tally (%s) -- refresh only" % "|".join(
                str(v) for v in answers)
            final = list(answers)
        if not (sh & 0xFF):
            # THE ONE ASSERTION THIS PATH MUST CARRY. See the banner: sh = 0 is
            # parsed and then discarded, which looks in every log exactly like a
            # reply that worked.
            _say("tm:   WARNING: REFUSING TO SEND @Continue= WITH sh=0 -- 0x104FAC "
                 "would parse it, apply it, and report 'no answer'. Fix "
                 "POL_TM_CONTINUE_SH.")
            return None
        _say("tm:   ...%s: answering (0x43, %d) sh=%d. Watch for "
             "'---->Recv=CONTINUE' on every client."
             % (outcome, CONTINUE_CMD, sh))
        mine = None
        for i, (mid, _v) in enumerate(seats):
            vals = list(final) if final is not None else list(answers)
            if final is None:
                vals[i] = CONTINUE_FINAL     # only OUR slot 3 releases state 7
            body_i = (_turn_code(CONTINUE_CMD, sh)
                      + _continue_body(_continue_frame(vals, i), bval))
            if i == me:
                mine = body_i
                continue
            _queue_push(mid, body_i, "the @Continue tally",
                        after=_env_int("POL_TM_CONTINUE_GAP", 0),
                        peer=_MATCH_PEER.get(_push_key(mid)))
        return mine

    if b"@Quit=" in cmd:
        # Leaving the card shop. Unanswered, this hangs the client and the
        # session dies ~75 s later -- see the banner above `CARDPRM_RECORDS`.
        if not _shopquit_enabled():
            return None
        # WARNING: BUT `@Quit=` IS SENT ON THREE DIFFERENT CODES AND THE ANSWER IS NOT
        # THE SAME NAME ON EACH. Caught live 2026-08-20T10:58:54Z, quitting a
        # MATCH from the host's end:
        #
        #     m3 <- code=67 msgid=0x05 sh=8  @Quit=/No=0
        #     m3 -> code=67 msgid=0x05 sh=8  @EQuit=/D=0      <- unreadable
        #
        # `@EQuit` is the CARD SHOP's name -- it belongs to 0xB2 command 23
        # (arm 0x105A97, `Recv=SHOPQUIT`). On code 0x43 the arm is **0x103039**,
        # which matches the literal `@Quit` and reads `/No=` and `/B=`; a body
        # named `@EQuit` fails its `0xAA920` name match, falls through to the
        # 0xB2 table -- whose commands start at 7, so msgid 5 is out of range --
        # and lands on the default arm, which returns 0. That is the exact input
        # to Lobby.BIN 428, "This command cannot be executed".
        #
        # Same message, same field, three codes: answer in the vocabulary of the
        # code it arrived on. `0x1030A3` sets the result to 1 unconditionally, so
        # the fields only have to be present and parseable.
        if (code or 0) & 0xFF == IN_MATCH_CODE:
            no = _env_int("POL_TM_MATCHQUIT_NO", 0)
            b = _env_int("POL_TM_MATCHQUIT_B", 0)
            _say("tm: member %s @Quit= on code 0x43 -- that is the MATCH quit, "
                 "not the shop's: answering @Quit=/No=%d/B=%d (arm 0x103039 "
                 "matches the name `@Quit`; `@EQuit` is 0xB2's and would be "
                 "unreadable here)" % (member_id, no, b))
            return body[:8] + (b"@Quit=/No=%d/B=%d" % (no, b))
        # ...AND THE NAME ALSO DIFFERS BY CLIENT BUILD, not just by code.
        #
        # WARNING: `@EQuit` DOES NOT EXIST ANYWHERE IN THE PS2 MODULE. Grepped the
        # decrypted `TMaster.pex` (2026-09-08, load base 0x280000): `@Quit`,
        # `@Quit=` and `---->Recv=SHOPQUIT` are all present, `@EQuit` is not,
        # at any offset. The PS2's SHOPQUIT arm compares the body against
        # **`@Quit`** (`0x003f0fd0` -> `0x00489CB8`), passes the same two
        # equality gates the PC's does (sender id at s6+40, shop number at
        # s6+48), logs `Recv=SHOPQUIT` at `0x003f1010`, and then parses TWO
        # fields -- `/N=` (`0x003f1020`) and `/D=` (`0x003f103c`).
        #
        # So answering `@EQuit=/D=0` to a PS2 console is a name its dispatcher
        # can never match: the waiter never wakes and the shop screen hangs with
        # the client heartbeating `@Pong=/Cnt=N/SM=0` and asking for nothing.
        # Measured live 2026-09-08T19:46:22Z -- the exchange was otherwise
        # textbook (header echoed, `/D=0`), which is what made it look right.
        #
        # WARNING: POL_TM_SHOPQUIT_PS2 defaults to 1 because the PS2 is the build that
        # is played; set it to 0 to restore the PC vocabulary. There is no
        # client-kind signal in scope here to pick automatically -- `@Pong=`'s
        # `/SM=` vs `/RM=` is shop-vs-room STATE, not build -- and this module
        # already handles build splits with a knob (`_teachdvans` `ext`).
        _CHECKOUT_PULL.pop(member_id, None)
        if os.environ.get("POL_TM_SHOPQUIT_PS2", "1") == "1":
            return body[:8] + (b"@Quit=/N=%d/D=%d" % (_shop_n(), _shopquit_d()))
        return body[:8] + (b"@EQuit=/D=%d" % _shopquit_d())

    # Nothing above matched.
    #
    # WARNING: THIS ARM USED TO CLAIM the @Opt / @StartData / @TurnData / @BattleData
    # family was "not decoded yet". THAT WAS FALSE and ~17,000 lines stale: all
    # four are decoded and answered ABOVE (`_opt_body`, `_startdata_body`,
    # `_turndata_body`, `_battledata_body`, with the client's own dispatch arms
    # cited at each). A reviewer read this comment, plus the vestigial
    # frame()/parse()/build() stubs just below, and reported the entire TM match
    # protocol as unimplemented. Corrected 2026-08-26.
    #
    # As of 2026-09-04 every one of the client's 52 send keys has an arm above
    # (see the module docstring's COVERAGE list). Anything reaching here is a
    # key this server has never seen on a wire. Capture it; do not invent a
    # reply.
    return None


# --- stubs to be implemented from RE (kept as explicit NotImplemented so a caller
#     that reaches for them fails loudly instead of sending fabricated bytes) ---
def frame(sock):
    raise NotImplementedError("TM world framing not reversed yet (capture first)")


def parse(frame_bytes):
    raise NotImplementedError("TM world message set not reversed yet")


def build(msg):
    raise NotImplementedError("TM world message set not reversed yet")


def _selftest_blocks():
    """The block writers must never grow the field past its authored length.

    This is the assertion the crash of 2026-08-20 did not have. A 64-character
    block fills `+0x28` to the end of the 104-byte record and leaves the client
    an unterminated string; both players' clients died on the first one.
    """
    ok = True
    authored = "BAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAFBAABIGJPAAAAA"      # 49
    for name, got in (
            ("occupancy", _occupancy_block(authored, [(1, 0xAB12CD56EB0F5932)])),
            ("occupancy/empty", _occupancy_block(authored, [])),
            ("settings", _settings_block({"ll": 10, "lu": 50}, authored))):
        if len(got) > len(authored):
            _say("FAIL: %s grew the block %d -> %d; +0x28 runs to the end of "
                  "the record and an unterminated block CRASHES THE CLIENT"
                  % (name, len(authored), len(got)))
            ok = False
    # ...and the id must be readable back exactly as the client reads it.
    import tmroom
    blk = _occupancy_block(authored, [(1, 0xAB12CD56EB0F5932)])
    if tmroom.client_reads_id(blk[14:30]) != 0xAB12CD56EB0F5932:
        _say("FAIL: the occupancy id does not survive the block write")
        ok = False
    if tmroom.client_reads_id(_occupancy_block(authored, [])[14:30]) != 0:
        _say("FAIL: an empty table must publish id 0 -- non-zero is what the "
              "state-1 arm treats as 'somebody is here'")
        ok = False
    return ok


def _selftest_packs():
    """THE PACK CONTENTS, against `PackPrm.BIN` itself.

    This is the guard the uniform-draw bug did not have: a Beginner's Pack that
    yields a level-19 card is what live testing reported on 2026-08-20, and the
    only reason it went unnoticed is that nothing asserted a pack's cards come
    from the pack's own draw table. See the banner above `_draw_pack`.
    """
    ok = True
    table = _packprm()
    # 78 packs in the PC build's table, 77 in the PS2's -- the count is the
    # table's own header word, so either is a whole decode.
    if len(table) not in (77, 78):
        _say("FAIL: PackPrm.BIN must decode to 77 or 78 records -- got %d"
             % len(table))
        return False

    # The four fields the shop actually reads, on a record a tester bought.
    beg = table[1]
    if (beg["price"], beg["cards"], beg["kind"]) != (1000, 10, 1):
        _say("FAIL: /No=1 must be price 1000, 10 cards, kind 1 -- got %r"
             % ((beg["price"], beg["cards"], beg["kind"]),))
        ok = False
    if [r[0] for r in beg["draw"]] != [1, 2, 3, 5]:
        _say("FAIL: /No=1 draws levels 1/2/3/5 -- got %r"
             % ([r[0] for r in beg["draw"]],))
        ok = False

    # EVERY ROW BUT ONE RESOLVES TO A REAL POOL. This is what makes the
    # (level, group, category) reading a decode rather than a shape that fits:
    # 308 rows over 77 records, and 307 of them select real cards. The one
    # exception is SE's own data -- record 27, Quina's COM deck, kind 0, never
    # sold -- and it is pinned here so that a wider break cannot hide behind it.
    empty = []
    for i, rec in enumerate(table):
        prev = -1
        for level, group, cat, cum in rec["draw"]:
            if cum <= prev:          # unreachable filler after the table ends
                continue
            prev = cum
            if not _pack_pool(level, group, cat):
                empty.append((i, rec["kind"], (level, group, cat)))
    # With the PC build's tables every row resolves; the PS2 build's has one
    # exception, record 27 (Quina's COM deck, kind 0, never sold).
    if empty not in ([], [(27, 0, (1, 3, 5))]):
        _say("FAIL: every draw row must select real cards (the PS2 table's one "
             "exception is record 27, level 1 / group 3 / category 5) -- got %r"
             % (empty,))
        ok = False

    # And the draw obeys it. Levels first -- the reported symptom.
    lv = set()
    for _ in range(120):
        for card in _draw_pack(1):
            lv.add(tm_cardprm.row(card[0])[5])
    if not lv <= {1, 2, 3, 5}:
        _say("FAIL: a Beginner's Pack yielded levels %r; the file says 1/2/3/5"
             % (sorted(lv),))
        ok = False

    # Then the themed packs, whose names state their own answer.
    for no, cat, what in ((21, 2, "Summoner's"), (22, 3, "Warrior's"),
                          (23, 4, "Voyager's"), (24, 7, "FFIX Celebrity")):
        got = set()
        for _ in range(60):
            for card in _draw_pack(no):
                got.add(tm_cardprm.row(card[0])[7])
        if got != {cat}:
            _say("FAIL: the %s Pack must draw category %d only -- got %r"
                 % (what, cat, sorted(got)))
            ok = False

    # The Prize Center card id, off the same record at +0x20.
    if table[74]["card"] != 152 or table[74]["price"] != 10000:
        _say("FAIL: the dearest Prize Center record is card 152 at 10000 -- "
             "got %r at %r" % (table[74]["card"], table[74]["price"]))
        ok = False

    # A COM opponent's deck is FIVE VARIED cards from a real deck record, never
    # a single Prize Center card five times over (reported live 2026-09-02: /Com=23
    # -> record 47 = Maechen x5, because the base+offset mapping walked past
    # the deck ladder into the kind-5 records). Every character index -- high
    # ones included -- must resolve to a real deck.
    ladder = _com_deck_ladder()
    if len(ladder) < 2 or not all(_is_com_deck(_pack_rec(n)) for n in ladder):
        _say("FAIL: the COM deck ladder must be real deck records, got %r"
             % (ladder,))
        ok = False
    for ci in (1, 6, 11, 21, 22, 23, 40):
        rec_no = _com_deck_record(ci)
        if not _is_com_deck(_pack_rec(rec_no)):
            _say("FAIL: /Com=%d resolved to record %d, which is NOT a deck "
                 "(five identical cards is the bug this guards)" % (ci, rec_no))
            ok = False
        deck = [r.split(b"|")[0] for r in _com_deck_rows(ci)]
        if len(deck) != 5:
            _say("FAIL: a COM deck is five cards, /Com=%d gave %d"
                 % (ci, len(deck)))
            ok = False
        # A real deck record's pool is many cards, so five draws are not all
        # one id -- the degenerate single-card deck the tester saw.
        if len(set(deck)) == 1:
            _say("FAIL: /Com=%d dealt five identical cards (id %r) from record "
                 "%d -- the single-card Prize Center bug" % (ci, deck[0], rec_no))
            ok = False

    if ok:
        _say("packs: 77 records, 307 of 308 draw rows select real cards, "
             "/No=1 yields only levels 1/2/3/5, and every COM deck is a real "
             "%d-record ladder (no single-card decks)" % len(ladder))
    return ok


def _selftest_seating():
    """A settings publish must NOT un-seat anybody.

    Measured live 2026-08-20: `@GameEN=` seated the player and the other client
    correctly drew "Setting Up", then the `@Tet=`/`@Tab=` pair rebuilt the row
    from the AUTHORED fixture and published state 7 / id 0 -- "once it's done it
    goes back to showing as empty for the other person". This replays that exact
    order.

    WARNING: Sandboxed like `tmroom.selftest`: `note_table` publishes, and a test that
    can reach the snapshot two containers share has already cost this project one
    prod incident.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ))
    saved_seated = dict(_SEATED)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-seat-"), "r.json")
        tmroom._OWNER[0] = True
        tmroom._TABLES.clear(); tmroom._PEERS.clear()
        tmroom._RECORDS.clear(); tmroom._ROOMS_SEQ.clear(); _SEATED.clear()
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer("#TM0R001", peer_guid("UKXDBA266"))

        def row():
            live = dict(tmroom.tables("#TM0R001")).get("#TM0T001")
            return live and (live[3], tmroom.client_reads_id(live[6][14:30]))

        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        if row() != ("1", ident):
            _say("FAIL: @GameEN= must seat and publish the id, got %r" % (row(),))
            ok = False
        handle_line(b"14000000@Tet=/tl=0@Tab=/in=0/lu=99999/ll=0/au=300/al=100",
                    peer_nick="UE7QN1N9G", member_id=mid)
        # 0x14 is the CONFIRM. Since 2026-08-22 confirm KEEPS state 1 (default
        # POL_TM_TABLE_STATE_CONFIRMED=1): +0x108 now fills (proven live), so the
        # state-1 owner tile unlocks from the reservation slot WITHOUT the
        # state-2 flip that closed joins and locked out guests. It must NOT
        # un-seat, and the id must survive. (<3 seated, so inplay is False.)
        if row() != ("1", ident):
            _say("FAIL: the confirm must keep state 1 (owner tile unlocks from "
                  "+0x108; joins stay open) WITHOUT un-seating -- got %r "
                  "(want state 1, same id)." % (row(),))
            ok = False
        handle_line(b"41000000@GameExit=", peer_nick="UE7QN1N9G", member_id=mid)
        # THE AUTHORED 7 AGAIN -- 6151fa3c's clamp is RETRACTED (measured:
        # its heal TD put "Setting up" on a free table at 22:18; state 7 is the
        # client's deterministic empty tile, display code 0 pre-stored). This
        # assertion has now flip-flopped once; it checks the ONE constant,
        # tmroom.EMPTY_TABLE_STATE, whose banner carries the whole history.
        import tmroom as _tr
        if row() != (str(_tr.EMPTY_TABLE_STATE), 0):
            _say("FAIL: leaving must restore the AUTHORED state and clear the "
                  "id, got %r" % (row(),))
            ok = False
    except Exception as exc:
        _say("FAIL: seating selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (_SEATED, saved_seated)):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_cancel():
    """`@GameQT=/ID=` must RELEASE the sender's seat; bare `@GameQT=` must not.

    Static read 2026-08-22: sender 0x84050 appends
    `/ID=<16-hex table id>`; the bare form (0x83FC0) is the generic
    dialog-confirm shared by rules-Confirm, rules-accept AND the tile-menu
    cancel -- byte-identical, so the bare form must stay a pure confirm. Until
    the cancel arm existed, a `/ID=` cancel was answered as a confirm and the
    freed-on-screen table came back occupied on the next TD -- the live
    2026-08-22 report.

    WARNING: Sandboxed like `_selftest_seating`: the release republishes through the
    shared registry.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ))
    saved_seated = dict(_SEATED)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-cancel-"),
                                     "r.json")
        tmroom._OWNER[0] = True
        tmroom._TABLES.clear(); tmroom._PEERS.clear()
        tmroom._RECORDS.clear(); tmroom._ROOMS_SEQ.clear(); _SEATED.clear()
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer("#TM0R001", peer_guid("UKXDBA266"))

        def row():
            live = dict(tmroom.tables("#TM0R001")).get("#TM0T001")
            return live and (live[3], tmroom.client_reads_id(live[6][14:30]))

        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        if row() != ("1", ident):
            _say("FAIL: @GameEN= must seat first, got %r" % (row(),)); ok = False
        # A BARE @GameQT= is the dialog-confirm and must NOT touch the seat.
        handle_line(b"41000500@GameQT=", peer_nick="UE7QN1N9G", member_id=mid)
        if row() != ("1", ident):
            _say("FAIL: a bare @GameQT= (confirm) must NOT release the seat, "
                 "got %r" % (row(),)); ok = False
        # The `/ID=` form frees the table AND still answers @GameQA -- both
        # senders wait on (0x41, 6), so a cancel that releases but does not
        # answer would hang the client exactly like the unanswered confirm did.
        wire = tmroom.canonical_table_id(0, 1)
        out = handle_line(b"41000500@GameQT=/ID=%016X" % wire,
                          peer_nick="UE7QN1N9G", member_id=mid)
        if not out or not out.startswith(b"41000600") or b"@GameQA=" not in out:
            _say("FAIL: the /ID= cancel must still answer @GameQA on "
                 "(0x41, 6), got %r" % (out,)); ok = False
        if row() != (str(tmroom.EMPTY_TABLE_STATE), 0):
            _say("FAIL: @GameQT=/ID= must release the seat -- authored state "
                 "and id 0, got %r" % (row(),)); ok = False
    except Exception as exc:
        _say("FAIL: cancel selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (_SEATED, saved_seated)):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_heartbeat():
    """The reservation heartbeat TTL, red-path first. See `_SEAT_ALIVE`.

    Measured live 2026-08-22: Cancel Reservation sends ZERO bytes -- the
    table-peer @Pong just stops -- so silence-past-TTL is the only server-side
    cancel there is. Assert: a fresh stamp survives the sweep; a silent one is
    freed and the row republishes empty; a running match's table is exempt; an
    ANCIENT match is reclaimed (the post-match leak); TTL 0 disables.

    WARNING: Sandboxed like `_selftest_seating`; the sweep walks the shared seat map.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ))
    saved_seated = dict(_SEATED)
    saved_alive, saved_began = dict(_SEAT_ALIVE), dict(_MATCH_BEGAN)
    saved_started = dict(_MATCH_STARTED)
    saved_sweep, saved_ttl = _SEAT_SWEEP[0], _os.environ.get("POL_TM_SEAT_TTL_S")
    chan = "#TM0R001"
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-hb-"), "r.json")
        tmroom._OWNER[0] = True
        for d in (tmroom._TABLES, tmroom._PEERS, tmroom._RECORDS,
                  tmroom._ROOMS_SEQ, _SEATED, _SEAT_ALIVE, _MATCH_BEGAN,
                  _MATCH_STARTED):
            d.clear() if isinstance(d, dict) else None
        _os.environ.pop("POL_TM_SEAT_TTL_S", None)
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer(chan, peer_guid("UKXDBA266"))

        # The peer decode that feeds the stamp: our own published table id must
        # round-trip nick -> (chan, index), and a member's nick must NOT.
        tnick = table_peer_nick(tmroom, chan, 1)
        if tnick and _peer_table(tnick) != (chan, 1):
            _say("FAIL: _peer_table(%r) = %r, want (%r, 1)"
                 % (tnick, _peer_table(tnick), chan)); ok = False
        if _peer_table(b"UGRAWG3GT") != (chan, 1):
            _say("FAIL: the live-measured table-1 peer must decode, got %r"
                 % (_peer_table(b"UGRAWG3GT"),)); ok = False

        def seated():
            return any(m == mid for m, _i in _seats_of(chan).get(1, []))

        def seat():
            handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                        peer_nick="UE7QN1N9G", member_id=mid)
            if not seated():
                _say("FAIL: heartbeat selftest could not seat its member")
                return False
            return True

        def sweep(age=None):
            if age is not None:
                _SEAT_ALIVE[_alive_key(chan, mid, 1)] = time.time() - age
            _SEAT_SWEEP[0] = 0.0
            return expire_silent_seats()

        # Fresh stamp: the sweep must not touch the seat.
        if seat():
            _SEAT_ALIVE[_alive_key(chan, mid, 1)] = time.time()
            sweep()
            if not seated():
                _say("FAIL: a FRESH heartbeat's seat was freed"); ok = False
        # WARNING: AND ONE TABLE'S BEAT MUST NOT KEEP ANOTHER TABLE'S SEAT ALIVE.
        # The bug this replaces: `_SEAT_ALIVE` keyed `(chan, member)`, so a
        # player reserved at table 1 who then pongs table 2 (a VS. COM game
        # lives on the table peer for its whole length) re-stamped the table-1
        # reservation on every beat and the TTL could never reap it.
        if seated() or seat():
            _SEAT_ALIVE[_alive_key(chan, mid, 1)] = time.time() - 120
            _SEAT_ALIVE[_alive_key(chan, mid, 2)] = time.time()
            _SEAT_SWEEP[0] = 0.0
            expire_silent_seats()
            if seated():
                _say("FAIL: a table-1 seat silent 120s was kept alive by a "
                     "FRESH beat on table 2 -- _SEAT_ALIVE must key by TABLE, "
                     "or a COM game at one table pins a reservation at another")
                ok = False
            _SEAT_ALIVE.pop(_alive_key(chan, mid, 2), None)
        # Silent past TTL: freed.
        if seated() or seat():
            sweep(age=120)
            if seated():
                _say("FAIL: a seat silent 120s (TTL 45) must be freed"); ok = False
        # A running match holds the table even in silence.
        if seat():
            _MATCH_STARTED[(chan, 1)] = {_push_key(mid)}
            _MATCH_BEGAN[(chan, 1)] = time.time()
            sweep(age=120)
            if not seated():
                _say("FAIL: a running match's seat was reaped by the TTL"); ok = False
            # ...but an ancient match is reclaimed (the post-match leak).
            _MATCH_BEGAN[(chan, 1)] = time.time() - 100000
            sweep(age=120)
            if seated():
                _say("FAIL: a match begun 100000s ago must not hold the table")
                ok = False
            _MATCH_STARTED.pop((chan, 1), None)
            _MATCH_BEGAN.pop((chan, 1), None)
        # TTL 0 disables the whole mechanism.
        if seat():
            _os.environ["POL_TM_SEAT_TTL_S"] = "0"
            if sweep(age=120) != 0 or not seated():
                _say("FAIL: POL_TM_SEAT_TTL_S=0 must disable the sweep"); ok = False
    except Exception as exc:
        _say("FAIL: heartbeat selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (_SEATED, saved_seated), (_SEAT_ALIVE, saved_alive),
                     (_MATCH_BEGAN, saved_began), (_MATCH_STARTED, saved_started)):
            d.clear(); d.update(v)
        _SEAT_SWEEP[0] = saved_sweep
        if saved_ttl is None:
            _os.environ.pop("POL_TM_SEAT_TTL_S", None)
        else:
            _os.environ["POL_TM_SEAT_TTL_S"] = saved_ttl
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_seat_move():
    """Entering a new room must release a seat left behind in the old one.

    The orphan `reconcile_room_tables` cannot see (see
    `release_seats_elsewhere`): a member seated in room A who moves to room B
    keeps their room-A seat, because the sweep frees only seats of members with
    NO session at all. Reserve in R001, walk into R002, assert R001 is freed --
    and that the seat in R002 (where they now are) would be untouched.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0], dict(tmroom._TABLES),
             dict(tmroom._PEERS), dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ),
             dict(tmroom._SEATS))
    saved_seated = dict(_SEATED)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-move-"), "r.json")
        tmroom._OWNER[0] = True
        for d in (tmroom._TABLES, tmroom._PEERS, tmroom._RECORDS,
                  tmroom._ROOMS_SEQ, tmroom._SEATS):
            d.clear()
        _SEATED.clear()
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932

        def seated_in(chan):
            return [m for rows in _seats_of(chan).values() for m, _i in rows
                    if m == mid]

        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer("#TM0R001", peer_guid("UKXDBA266"))
        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        if not seated_in("#TM0R001"):
            _say("FAIL: @GameEN= did not seat the player in R001"); ok = False
        # The member walks into R002: the record moves first (as the <DE> handler
        # does), then the entry sweep runs. R001's seat must go; a hypothetical
        # R002 seat must be spared.
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000002", "T"])
        release_seats_elsewhere(mid, "#TM0R002")
        if seated_in("#TM0R001"):
            _say("FAIL: a move to R002 left the R001 seat orphaned -- the "
                 "recurring phantom this fixes"); ok = False
    except Exception as exc:
        _say("FAIL: seat-move selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (tmroom._SEATS, saved[6]), (_SEATED, saved_seated)):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_audit():
    """The invariant checker must pass the REAL seating path and catch planted
    defects.

    Two directions, both load-bearing:
      * a table seated through the real `@GameEN=` handler must audit CLEAN --
        if the production path itself trips the auditor, one of them is wrong
        and that disagreement is exactly what to investigate;
      * a planted phantom row (state 1, count 1, occupancy id, NO seats -- the
        tester's literal bug) must produce the four kinds that describe it.
        A checker that cannot go red is the dead-assertion class the ledger
        already records twice.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ),
             json.loads(json.dumps(tmroom._SEATS)),
             json.loads(json.dumps(tmroom._CONFIRMED)))
    saved_seated = dict(_SEATED)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-audit-"),
                                     "r.json")
        tmroom._OWNER[0] = True
        tmroom._TABLES.clear(); tmroom._PEERS.clear()
        tmroom._RECORDS.clear(); tmroom._ROOMS_SEQ.clear()
        tmroom._SEATS.clear(); tmroom._CONFIRMED.clear(); _SEATED.clear()
        chan = "#TM0R001"
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer(chan, peer_guid("UKXDBA266"))
        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        bad = [v for v in audit_room_tables(chan, _seats_of(chan))
               if v["severity"] == "bad"]
        if bad:
            _say("FAIL: the real seating path must audit CLEAN, got %r" % bad)
            ok = False
        # THE PLANTED PHANTOM -- the live report: a row claiming
        # state 1 / 1 seated / an occupancy id, with no seat behind it.
        blk = bytearray(b"A" * 49)
        blk[4:6] = tmroom.letters(1, 2)
        blk[14:30] = tmroom.letters(0xDEAD, 16)
        room = "0x%016X" % tmroom.room_id_for(chan)
        tmroom._TABLES.setdefault(room, {})["#TM0T002"] = [
            "#TM0T002", "0", "0x0021000100063063", "1", "8", "1",
            blk.decode("latin1")]
        kinds = {v["kind"] for v in audit_room_tables(chan, _seats_of(chan))
                 if v.get("table") == 2 and v["severity"] == "bad"}
        want = {"row-occupied-nobody", "seat-count-drift",
                "block-count-drift", "ghost-occupancy-id"}
        if not want <= kinds:
            _say("FAIL: the planted phantom must trip %r, tripped only %r"
                 % (sorted(want), sorted(kinds)))
            ok = False
    except Exception as exc:
        _say("FAIL: audit selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (tmroom._SEATS, saved[6]), (tmroom._CONFIRMED, saved[7]),
                     (_SEATED, saved_seated)):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_reconcile():
    """`reconcile_room_tables` heals what the audit flags -- the FIRST test
    this function has ever had (the coverage map found
    zero). Three legs: a planted phantom row is republished clean; a member
    whose record MOVED rooms loses the seat left behind (the new `_absent`
    branch); and the audit reads CLEAN afterwards -- checker and healer
    agreeing is the whole design.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ),
             json.loads(json.dumps(tmroom._SEATS)),
             json.loads(json.dumps(tmroom._CONFIRMED)))
    saved_seated = dict(_SEATED)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-recon-"),
                                     "r.json")
        tmroom._OWNER[0] = True
        tmroom._TABLES.clear(); tmroom._PEERS.clear()
        tmroom._RECORDS.clear(); tmroom._ROOMS_SEQ.clear()
        tmroom._SEATS.clear(); tmroom._CONFIRMED.clear(); _SEATED.clear()
        chan = "#TM0R001"
        mid, ident = 0x860FB3E2A2, 0xAB12CD56EB0F5932
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        tmroom.note_room_peer(chan, peer_guid("UKXDBA266"))
        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        # Leg 1: the planted phantom (state 1 / count 1 / occ id, no seats).
        blk = bytearray(b"A" * 49)
        blk[4:6] = tmroom.letters(1, 2)
        blk[14:30] = tmroom.letters(0xDEAD, 16)
        room = "0x%016X" % tmroom.room_id_for(chan)
        tmroom._TABLES.setdefault(room, {})["#TM0T002"] = [
            "#TM0T002", "0", "0x0021000100063063", "1", "8", "1",
            blk.decode("latin1")]
        if reconcile_room_tables(chan) < 1:
            _say("FAIL: reconcile must republish the planted phantom")
            ok = False
        row2 = dict(tmroom.tables(chan)).get("#TM0T002")
        if row2 and (row2[3] != str(tmroom.EMPTY_TABLE_STATE)
                     or tmroom._as_int(row2[5]) != 0
                     or tmroom.unletters(row2[6][14:30]) != 0):
            _say("FAIL: the phantom row must heal to authored state/0/0, "
                 "got state=%s seated=%s occ=%X"
                 % (row2[3], row2[5], tmroom.unletters(row2[6][14:30])))
            ok = False
        # Leg 2: the record moves to R002 -> the R001 seat is absent now.
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000002", "T"])
        reconcile_room_tables(chan)
        if any(m == mid for s in _seats_of(chan).values() for m, _i in s):
            _say("FAIL: a record that moved rooms must free the seat it "
                 "left behind")
            ok = False
        # Leg 3: checker and healer agree -- the room audits CLEAN.
        bad = [v for v in audit_room_tables(chan, _seats_of(chan))
               if v["severity"] == "bad"]
        if bad:
            _say("FAIL: after reconcile the audit must be clean, got %r" % bad)
            ok = False
        # Leg 4: the seated letters revert on release -- prod 2026-08-21
        # 06:06:10Z: release_seats re-marked BEFORE saving the seat list, so
        # block[8] stayed 'D' on the record after every release.
        v10 = "T              LAA0AAABABAABIAB"
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", v10])
        handle_line(b"41000100@GameEN=/NN=%016X/L=1" % ident,
                    peer_nick="UE7QN1N9G", member_id=mid)
        rec = tmroom._RECORDS[str(mid)][10]
        if rec[-16:][8] != "D":
            _say("FAIL: seating must mark block[8]='D', record reads %r"
                 % rec[-16:])
            ok = False
        release_seats(mid, chan)
        rec = tmroom._RECORDS[str(mid)][10]
        if rec[-16:][8] != "A":
            _say("FAIL: release must revert block[8] to 'A' -- the "
                 "remark-before-save ordering bug (record reads %r)"
                 % rec[-16:])
            ok = False
        # Leg 4b: an ownership change clears the departed owner's confirm --
        # the heir must NOT inherit a state-2 "Playing" table (measured live
        # 2026-08-21: Observe-only menus on every client after the owner left).
        mid2 = mid + 1
        tmroom.note_member(mid2, ["0", "0", "0", "0", "T2", "2", "0", "0",
                                  "0", "0x0000002000000001", "T2"])
        _seats_of(chan)[1] = [(mid, ident), (mid2, ident + 1)]
        _save_seats(chan)
        _TABLE_CONFIRMED.add((chan, 1))
        tmroom.note_table_confirmed(chan, 1)
        release_seats(mid, chan)          # the owner (seat 0) departs
        if (chan, 1) in _TABLE_CONFIRMED or tmroom.table_confirmed(chan, 1):
            _say("FAIL: the departed owner's confirm must not survive the "
                 "handoff")
            ok = False
        row1 = dict(tmroom.tables(chan)).get("#TM0T001")
        if row1 and row1[3] == "2":
            _say("FAIL: the heir's table must drop out of state 2 -- they "
                 "have not confirmed anything")
            ok = False
        # Leg 5: a stored row carrying the PRE-FOLD (room-shared) id heals to
        # the room-folded id -- the yellow-tile-in-every-room carrier,
        # measured live 2026-08-21 (my-reservation == every room's table 1).
        shared = tmroom.canonical_table_id(2, 0)          # table 3, pre-fold
        tmroom._TABLES.setdefault(room, {})["#TM0T003"] = [
            "#TM0T003", "0", "0x%016X" % shared, "7", "8", "0",
            ("A" * 49)]
        kinds = {v["kind"] for v in audit_room_tables(chan, _seats_of(chan))
                 if v.get("table") == 3 and v["severity"] == "bad"}
        if "room-shared-id" not in kinds:
            _say("FAIL: the pre-fold id must be flagged room-shared-id, "
                 "got %r" % sorted(kinds))
            ok = False
        reconcile_room_tables(chan)
        row3 = dict(tmroom.tables(chan)).get("#TM0T003")
        want = tmroom.canonical_table_id(2, tmroom._room_no(chan))
        if not row3 or tmroom._as_int(row3[2]) != want:
            _say("FAIL: reconcile must fold the room into the id -- want "
                 "0x%016X, row holds %r" % (want, row3 and row3[2]))
            ok = False
    except Exception as exc:
        _say("FAIL: reconcile selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (tmroom._SEATS, saved[6]), (tmroom._CONFIRMED, saved[7]),
                     (_SEATED, saved_seated)):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_gameml_format():
    """`@GameML` must be COLUMNAR, pipe-delimited, with HEX-encoded names.

    The client's row extractors (0xAAD60 string / 0xAB470 u64 / 0xAB080 int) are
    columnar: each key appears ONCE and its value is a `|`-list indexed per
    member; names are read two-hex-chars-per-byte, so plaintext "Fox" decodes to
    the single byte 0xEA -- the empty-pane sentinel the dump showed. RE + minidump
    2026-08-21.
    """
    import tmroom
    ok = True
    saved = {k: dict(getattr(tmroom, k)) for k in
             ("_RECORDS", "_POLIDS", "_ROOMS_SEQ", "_GUIDS", "_NAMES",
              "_DELTAS", "_SEATS")}
    saved_owner = tmroom._OWNER[0]
    try:
        for k in saved:
            getattr(tmroom, k).clear()
        tmroom._OWNER[0] = True
        m1, m2 = 111, 222
        # index 9 = room, index 10 = 15-wide name + 16-letter status block.
        tmroom.note_member(m1, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                "0x0000002000000001",
                                "Fox            DAA0AABFDBBADIAB"])
        tmroom.note_member(m2, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                "0x0000002000000001",
                                "LaptopTest2    LAA0AAALDBBACIAB"])
        tmroom.note_pol_id(m1, "AB12CD31DC3F63B6")
        tmroom.note_pol_id(m2, "AB12CD56EB0F5932")
        # TABLE-SCOPED (the 13:38Z pseudo-add): only SEATED members may appear.
        # Both are seated at table 1 here; the room-roster shortcut is the bug.
        tmroom._SEATS["0x%016X" % tmroom.room_id_for("#TM0R001")] = {
            "1": [[m1, 1], [m2, 2]]}
        body = _gameml_body(b"@GameM=/ID=AB12CD31DC3F63B6", member_id=m1)
        if not body:
            _say("FAIL: @GameML produced nothing"); return False
        # ...and an asker who is NOT seated must not conjure themselves in.
        m3_unseated = 333
        tmroom.note_member(m3_unseated,
                           ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                            "0x0000002000000001",
                            "Bystander      LAA0AAALDBBACIAB"])
        tmroom.note_pol_id(m3_unseated, "AB12CE9A544769E8")
        body3 = _gameml_body(b"@GameM=/ID=AB12CE9A544769E8",
                             member_id=m3_unseated)
        if body3 is None or b"/Num=0" not in body3 or b"/MLID=/" not in body3:
            _say("FAIL: an unseated room-joiner must get /Num=0 and must NOT "
                 "appear in the table list (the 13:38Z pseudo-add), got %r"
                 % body3)
            ok = False
        # THE RESERVATION-SLOT COPY rides msgid 0xC, same body, distinct header.
        # code 0x41 msgid 4 -> "41000400"; msgid 0xC -> "41000C00". The gate's
        # +0x108 only fills from the msgid-0xC channel (parser 0x83B80).
        if encode_code(MSG_GAMEML) != b"41000400":
            _say("FAIL: MSG_GAMEML header changed: %r" % encode_code(MSG_GAMEML))
            ok = False
        if encode_code(MSG_GAMEML_RESV) != b"41000C00":
            _say("FAIL: the reservation reply must be code 0x41 msgid 0xC "
                 "(41000C00), got %r" % encode_code(MSG_GAMEML_RESV)); ok = False
        # COLUMNAR: exactly one of each key (a grouped/repeated key is the bug).
        for key in (b"/MLID=", b"/MLNN=", b"/Po=", b"/Lv=", b"/Rk=", b"/HN="):
            if body.count(key) != 1:
                _say("FAIL: @GameML must carry ONE %r (columnar), got %d in %r"
                     % (key, body.count(key), body)); ok = False
        if b"/Num=2" not in body:
            _say("FAIL: @GameML /Num=2 missing: %r" % body); ok = False
        # PIPE-joined hex columns, member order.
        if b"/MLID=AB12CD31DC3F63B6|AB12CD56EB0F5932" not in body:
            _say("FAIL: /MLID= must be pipe-joined hex POL-IDs: %r" % body); ok = False
        # hex of the two fixture names: 436173 and 4C6170746F705465737432.
        if b"/MLNN=466F78|4C6170746F705465737432" not in body:
            _say("FAIL: /MLNN= must be pipe-joined UPPERCASE hex names: %r"
                 % body); ok = False
        if b"/HN=466F78|4C6170746F705465737432" not in body:
            _say("FAIL: /HN= must match /MLNN= hex encoding: %r" % body); ok = False
        # NO plaintext name may leak -- 0xAAD60 would hex-decode it to garbage.
        if b"Fox" in body or b"LaptopTest2" in body:
            _say("FAIL: @GameML leaked a plaintext name (would decode to 0xEA): "
                 "%r" % body); ok = False
        # values must never contain a raw delimiter.
        for col in body.split(b"/")[1:]:
            v = col.split(b"=", 1)[1] if b"=" in col else b""
            if b"@" in v:
                _say("FAIL: @GameML value carries a stray '@': %r" % body); ok = False
    except Exception as exc:
        _say("FAIL: gameml-format selftest raised %r" % (exc,)); ok = False
    finally:
        for k, v in saved.items():
            getattr(tmroom, k).clear(); getattr(tmroom, k).update(v)
        tmroom._OWNER[0] = saved_owner
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_peer_index():
    """Table indexing MUST survive an authsess restart.

    The peer-block base drifts by 0x1000 on every client re-allocation, and a
    restart forces one (the client resumes on a new allocation while the server
    keeps the old room peer). The computed table index must not depend on WHICH
    drift era it is. This resolves one table peer against a room peer taken from
    k=1, 2 and 3 blocks up and asserts the SAME index every time -- the property
    that was broken 2026-08-21 (room 3 table 2 dropped as "out of range").
    """
    ok = True
    nick = "UKXDBA266"
    guid = peer_guid(nick)
    if guid is None:
        _say("FAIL: peer-index selftest could not fold the test nick"); return False
    tlow = guid & 0xFFFFFFFF
    room_cls = (peer_class(guid) - 1) & 0xFF   # so the class gate sees room+1
    for room_no in (1, 3):
        for idx in (1, 2, 4):
            got = {}
            for k in (1, 2, 3):                # three allocation eras / restarts
                room1_low = (tlow - (idx - 1) + k * 0x1000) & 0xFFFFFFFF
                room_low = (room1_low + (room_no - 1)) & 0xFFFFFFFF
                room_peer = (room_cls << 32) | room_low
                res, _why = table_index_for_peer(
                    nick, room_peer, chan=("#TM0R%03d" % room_no).encode())
                got[k] = res
            if set(got.values()) != {idx}:
                _say("FAIL: table index not restart-stable for room %d table %d "
                     "-- got %r across drift eras k=1,2,3 (all must be %d)"
                     % (room_no, idx, got, idx)); ok = False
    return ok


def _selftest_value0():
    """The roster key id: snapshot AND delta stream serve the POL-ID
    (POL_TM_VALUE0=polid, the Start-Game-gate/members-pane unlock), and a
    <PC> removal still rewrites after the record is popped."""
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0], dict(tmroom._RECORDS),
             dict(tmroom._ROOMS_SEQ), dict(tmroom._GUIDS),
             dict(tmroom._POLIDS),
             json.loads(json.dumps(tmroom._DELTAS)))
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-v0-"),
                                     "r.json")
        tmroom._OWNER[0] = True
        tmroom._RECORDS.clear(); tmroom._ROOMS_SEQ.clear()
        tmroom._GUIDS.clear(); tmroom._POLIDS.clear(); tmroom._DELTAS.clear()
        chan, mid, guid = "#TM0R001", 6, 0x860FB3E2A2
        polid = "AB12CD56EB0F5932"
        tmroom.note_guid(mid, guid)
        tmroom.note_pol_id(mid, polid)
        tmroom.note_member(mid, ["0x%016X" % guid, "0", "0", "0", "T", "2",
                                 "0", "0", "0", "0x0000002000000001",
                                 "T              LAA0AAABABAABIAB"])
        blob = tmroom.build_ptl(chan, b"")
        row0 = tmroom.decode_member(
            blob[tmroom.MEMBER_OFF:tmroom.MEMBER_OFF + tmroom.MEMBER_REC])
        if tmroom._as_int(row0[0]) != int(polid, 16):
            _say("FAIL: the snapshot must key the row by the POL-ID, got %r"
                 % row0[0])
            ok = False
        tmroom.forget_member(mid)
        deltas = tmroom.rewrite_value0_deltas(tmroom.deltas_after(chan, 0))
        pc = [d for d in deltas if d[1] == "PC"]
        if not pc or tmroom._as_int(pc[-1][2][0]) != int(polid, 16):
            _say("FAIL: a <PC> after the record is popped must still carry "
                 "the POL-ID (client removes by id), got %r"
                 % (pc and pc[-1][2]))
            ok = False
        pd = [d for d in deltas if d[1] in ("PD", "DE")]
        if pd and tmroom._as_int(pd[-1][2][0]) != int(polid, 16):
            _say("FAIL: the delta stream must agree with the snapshot, got %r"
                 % pd[-1][2][0])
            ok = False
    except Exception as exc:
        _say("FAIL: value0 selftest raised %r" % (exc,)); ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._RECORDS, saved[2]), (tmroom._ROOMS_SEQ, saved[3]),
                     (tmroom._GUIDS, saved[4]), (tmroom._POLIDS, saved[5]),
                     (tmroom._DELTAS, saved[6])):
            d.clear(); d.update(v)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_peer_class():
    """The table-class gate must accept every era measured live and refuse the
    room family -- each constant here cost a night of dropped reservations.
    The rule is: class LOW NIBBLE == room number (era-invariant high nibble)."""
    ok = True
    for cls, room_cls, room_no, want, name in (
            (0xD0, None, None, True, "legacy absolute"),
            (0xF1, 0xF0, 1, True, "2026-08-20 room1 era F"),
            (0xE1, 0xF0, 1, True, "room1 one drift step"),
            (0xD1, 0xF0, 1, True, "2026-08-21T12:33 room1 two steps"),
            (0xD3, 0xF0, 3, True, "2026-08-21T16:24 ROOM 3 live reject (0xD3)"),
            (0xF3, None, 3, True, "room 3 era F"),
            (0xD1, None, None, True, "drifted, no room known: family-bit x1"),
            (0xF0, 0xF0, 1, False, "a ROOM (low 0) is not a table"),
            (0xD1, 0xF0, 3, False, "room-1 class in a room-3 lookup"),
            (0xA2, 0xF0, 2, False, "foreign high nibble"),
            (None, 0xF0, 1, False, "no class")):
        if _is_table_class(cls, room_cls, room_no) != want:
            _say("FAIL: _is_table_class(%r, %r, room=%r) must be %s (%s)"
                 % (cls, room_cls, room_no, want, name))
            ok = False
    return ok


def _pipe_vals(body, key):
    """The `|`-separated values under one key -- the shape 0xAB080 indexes.

    `key` includes its `/` and `=`. The value ends at the next `/` or `@`,
    which is the find-first-of set at 0x51B3950.
    """
    i = body.find(key)
    if i < 0:
        return []
    v = body[i + len(key):]
    end = len(v)
    for ch in (b"/", b"@"):
        j = v.find(ch)
        if 0 <= j < end:
            end = j
    return v[:end].split(b"|")


def _selftest_chat():
    """The `@Chat=` roster: the two `/Num=` numbers, the shop-byte gate, and the
    ids going back exactly as they came in.

    Asserted against the PARSER -- nothing here has been on a wire yet -- so
    every check names the instruction that would reject the message.

    WARNING: Sandboxed like `_selftest_ingame`: this writes members and POL-IDs into
    `tmroom`, and a test that can reach the snapshot two containers share has
    already cost this project one incident.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0])
    saved_maps = [(d, dict(d)) for d in (tmroom._RECORDS, tmroom._ROOMS_SEQ,
                                         tmroom._POLIDS, tmroom._NAMES,
                                         tmroom._GUIDS, _CHAT_SERIAL)]
    saved_deltas = dict(tmroom._DELTAS)
    # THE DEFAULT IS OFF AND MUST STAY OFF until arm A's gate is proven -- see
    # the banner on `_chat_roster_enabled`. Assert that here, so flipping it back
    # trips a test rather than a live client's chat indicator, then force it on
    # for this run: the BODY still has to be correct whenever it is enabled.
    _chat_env = _os.environ.pop("POL_TM_CHAT_ROSTER", None)
    if _chat_roster_enabled():
        _say("FAIL: POL_TM_CHAT_ROSTER must default OFF -- an un-erased msgid-4 "
             "parks in the store and blocks every later code-0x42 message, which "
             "is a measured, live-confirmed loss of the chat indicator")
        ok = False
    _os.environ["POL_TM_CHAT_ROSTER"] = "1"
    # THE LIVE TOGGLE. Point it at a path of our own first: a real
    # /data/tm_chat_roster.txt on the box running the test would otherwise decide
    # these assertions, and a test that reads production state is not a test.
    global _CHAT_CONTROL_FILE
    _chat_file_keep = _CHAT_CONTROL_FILE
    _CHAT_CONTROL_FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-toggle-"),
                                       "tm_chat_roster.txt")
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-chat-"), "r.json")
        tmroom._OWNER[0] = True
        for d, _ in saved_maps:
            d.clear()
        tmroom._DELTAS.clear()

        chan = "#TM0R001"
        # Two players in one room. `A` has told us its POL-ID; `C` has not, and
        # must therefore be LEFT OUT rather than given a made-up one.
        for mid, name in ((41, "Fox"), (42, "Framework"), (43, "Quiet")):
            tmroom.note_member(mid, tmroom.synth_member(chan, mid, name), chan)
        tmroom.note_pol_id(41, b"AB12CD56EB0F5932")
        tmroom.note_pol_id(42, b"AB12CEb20d9067c4")           # case is normalised

        # --- what the client actually sends, off the 2026-08-20 capture -------
        req = (encode_code(MSG_CHAT | (CHAT_MSGID_WHO << 16))
               + b"@Chat=/NN=0000000000000000/CN=/HID=0/Dm=2/Vol=0")
        reply = _handle_line(req, member_id=41)
        _say("chat roster reply:", reply)
        if not reply:
            _say("FAIL: the msgid-1 request must be answered"); return False

        head, body = reply[:8], reply[8:]
        # code 0x42, msgid 4 -- the arm at 0x07E42F. `0x07DFD0(0x42, 4)` is what
        # the screen pump polls for; any other msgid is never looked at.
        if head != b"42000400":
            _say("FAIL: header %r -- the roster is code 0x42 msgid 4" % head)
            ok = False
        rcode, rcmd = decode_ebody(reply)
        if (rcode & 0xFF) != MSG_CHAT or ((rcode >> 16) & 0xFF) != CHAT_MSGID_ROSTER:
            _say("FAIL: decoded header %08X" % rcode); ok = False
        # The `=` straight after the name: 0xAA920 is a lookup in a map keyed on
        # `name=`, so `@Chat` without it becomes the key `@Chat/Num` and every
        # field read misses. Same byte, same lesson as @TeachDVAns.
        if not rcmd.startswith(b"@Chat="):
            _say("FAIL: the PC key delimiter `=` is missing after @Chat"); ok = False

        # --- `/Num=` is TWO numbers under one key ---------------------------
        nums = _pipe_vals(body, b"/Num=")
        if nums != [b"0", b"2"]:
            _say("FAIL: /Num= must be <last part>|<entries>, got %r" % (nums,))
            ok = False
        # 0x07E4B0: `cmp a9780(6), /Num=[0]` and `jne` bails without touching the
        # list. The shop byte IS the part index, so a one-message roster needs
        # both to be 0 -- this is the gate the whole reply hangs on.
        if (rcode >> 24) & 0xFF != int(nums[0] or b"0"):
            _say("FAIL: the header's shop byte (%d) must equal /Num= occurrence "
                 "0 (%s) or 0x07E4B0 drops the message"
                 % ((rcode >> 24) & 0xFF, nums[0])); ok = False

        # --- the rows, in the same order under both keys ---------------------
        ids, names = _pipe_vals(body, b"/NN="), _pipe_vals(body, b"/CN=")
        if ids != [b"AB12CD56EB0F5932", b"AB12CEB20D9067C4"]:
            _say("FAIL: /NN= must echo @Init=/NN= verbatim (upper-cased), got %r"
                 % (ids,)); ok = False
        if names != [b"Fox", b"Framework"]:
            _say("FAIL: /CN= must be the names in the same order, got %r"
                 % (names,)); ok = False
        if len(ids) != int(nums[1] or b"0") or len(names) != len(ids):
            _say("FAIL: the entry loop at 0x07E6A1 runs /Num=[1] times and reads "
                 "one of each key per pass -- the three counts must agree")
            ok = False
        if b"|" in b"".join(names):
            _say("FAIL: a `|` inside a name would split one row into two"); ok = False
        # Member 43 has no POL-ID. A row for it would be a row its owner does not
        # recognise as itself (0x0AFE3) and nobody else can resolve.
        if b"Quiet" in body:
            _say("FAIL: a member with no POL-ID must be omitted, not invented")
            ok = False

        # --- `/C=` identifies the snapshot and only ever rises ---------------
        first = _pipe_vals(body, b"/C=")
        again = _pipe_vals(_handle_line(req, member_id=41)[8:], b"/C=")
        if not first or not again or int(again[0]) <= int(first[0]):
            _say("FAIL: /C= must rise per snapshot (0x07E5C4 erases a lesser one "
                 "unread), got %r then %r" % (first, again)); ok = False

        # --- the other two arms are not ours to answer ----------------------
        line = (encode_code(MSG_CHAT | (CHAT_MSGID_LINE << 16))
                + b"@Chat=/La=1/Dt=#CHAT#\thello")
        if _handle_line(line, member_id=41) is not None:
            _say("FAIL: msgid 8 is a chat LINE and is relayed, not answered here")
            ok = False

        # --- and the store refuses an id of the wrong kind -------------------
        if tmroom.note_pol_id(44, b"0000000000000000"):
            _say("FAIL: all-zeros is the client's `I do not know`, not an id")
            ok = False
        if tmroom.note_pol_id(44, b"not-hex"):
            _say("FAIL: a non-hex POL-ID must be refused"); ok = False
        if tmroom.pol_id_of(44) != "":
            _say("FAIL: an unknown member's POL-ID must be empty, not a guess")
            ok = False
        # ...and it takes the OTHER source's wire form too. Class-L `<PC>` comes
        # through polpro as `(0xAB12CD56EB0F5932)`, parentheses and all, and
        # `responders._roster_note` hands that value straight over.
        import polpro as _polpro
        _pc = _polpro.parse(b"<PC>(0xAB12CD56EB0F5932)")[0][1][0]
        if not tmroom.note_pol_id(45, _pc) or                 tmroom.pol_id_of(45) != "AB12CD56EB0F5932":
            _say("FAIL: <PC>'s %r must normalise to the same 16 hex digits, "
                 "got %r" % (_pc, tmroom.pol_id_of(45))); ok = False

        # --- the control file, which is what aborts a live test -------------
        # WARNING: ABSENT MUST MEAN "NO OPINION", NEVER "ON". This file is the abort
        # handle for a reply carrying a measured hazard; if a missing or
        # unreadable file could enable it, the abort would be the thing that
        # armed it.
        if _chat_control_says() is not None:
            _say("FAIL: a missing control file must have no opinion, got %r"
                 % (_chat_control_says(),)); ok = False
        _os.environ["POL_TM_CHAT_ROSTER"] = "0"
        for text, want, why in (("1", True, "1 enables"),
                                ("0", False, "0 disables"),
                                ("off", False, "a word disables"),
                                ("", None, "empty = no opinion"),
                                (" \n ", None, "whitespace = no opinion")):
            with open(_CHAT_CONTROL_FILE, "w") as _f:
                _f.write(text)
            if _chat_control_says() is not want:
                _say("FAIL: control file %r -> %r, expected %r (%s)"
                     % (text, _chat_control_says(), want, why)); ok = False
            # and the file must actually GOVERN, not just parse
            if want is not None and _chat_roster_enabled() is not want:
                _say("FAIL: control file %r did not override the env (%s)"
                     % (text, why)); ok = False
        _os.unlink(_CHAT_CONTROL_FILE)
        if _chat_roster_enabled():
            _say("FAIL: with the file gone, POL_TM_CHAT_ROSTER=0 must govern")
            ok = False
        _os.environ["POL_TM_CHAT_ROSTER"] = "1"    # restore for any later check
    finally:
        tmroom._FILE, tmroom._OWNER[0] = saved
        for d, was in saved_maps:
            d.clear()
            d.update(was)
        tmroom._DELTAS.clear()
        tmroom._DELTAS.update(saved_deltas)
        _CHAT_CONTROL_FILE = _chat_file_keep
        if _chat_env is None:
            _os.environ.pop("POL_TM_CHAT_ROSTER", None)
        else:
            _os.environ["POL_TM_CHAT_ROSTER"] = _chat_env
    return ok


def _selftest_ingame():
    """The code-0x43 family: the slot, the seat search and the alone guard.

    Asserted against the PARSER, since nothing here has been on a wire yet --
    every check below names the instruction that would reject the message.

    WARNING: Sandboxed exactly like `_selftest_seating`: `_queue_vsgameinit` reads the
    live room registry and writes the push queue, and a test that can reach the
    snapshot two containers share has already cost this project one incident.
    """
    import os as _os, tempfile, tmroom
    ok = True
    saved = (tmroom._FILE, tmroom._OWNER[0],
             dict(tmroom._TABLES), dict(tmroom._PEERS),
             dict(tmroom._RECORDS), dict(tmroom._ROOMS_SEQ))
    saved_seated, saved_pushes = dict(_SEATED), dict(_PUSHES)
    saved_roster, saved_accepts = dict(_MATCH_ROSTER), dict(_MATCH_ACCEPTS)
    saved_ready, saved_peer = dict(_CARD_READY), dict(_MATCH_PEER)
    saved_conf = set(_TABLE_CONFIRMED)
    saved_hands, saved_turn = dict(_MATCH_HANDS), dict(_MATCH_TURN)
    saved_board, saved_rand = dict(_MATCH_BOARD), dict(_TURN_RAND)
    try:
        tmroom._FILE = _os.path.join(tempfile.mkdtemp(prefix="tm-game-"), "r.json")
        tmroom._OWNER[0] = True
        for d in (tmroom._TABLES, tmroom._PEERS, tmroom._RECORDS,
                  tmroom._ROOMS_SEQ, _SEATED, _PUSHES, _MATCH_ROSTER,
                  _MATCH_ACCEPTS, _CARD_READY, _MATCH_PEER,
                  _MATCH_HANDS, _MATCH_TURN, _MATCH_BOARD, _TURN_RAND,
                  _TABLE_CONFIRMED):
            d.clear()

        # --- the body, built straight from a two-seat table ------------------
        seats = [(0x860FB3E2A2, 0xAB12CD56EB0F5932), (0x860FB3E2A3, 0xAB12CEB20D9067C4)]
        rules = _rule_seven({"bm": 1, "du": 2, "st": 3,
                             "cb": 4, "ca": 5, "gs": 6, "tl": 7})
        if rules != [1, 2, 3, 4, 5, 6, 7]:
            _say("FAIL: the seven /R= values must come from @Tet= in "
                 "TABLE_INFO_TET order, got %r" % (rules,)); ok = False
        for me in (0, 1):
            body = _vsgame_body(seats, me, rules)
            # The `=` after the name. 0xAA920 is a lookup in a map keyed on
            # `name=`; this is the delimiter whose absence cost ~10 retracted
            # TRM-0-37088 conclusions (see the module header).
            if not body.startswith(b"@VsGameInit=/N=2"):
                _say("FAIL: @VsGameInit needs the `=` delimiter and /N= first, "
                     "got %r" % (body[:40],)); ok = False
            # /N= must be > 1 or 0x1024D5 goes to StartAloneERROR.
            ids = _pipe_vals(body, b"/ID=")
            if len(ids) != 2:
                _say("FAIL: one /ID= per seat, got %r" % (ids,)); ok = False
            # THE SEAT SEARCH. 0x1024F9 takes the FIRST /ID= equal to the
            # client's own GAME-ID; the recipient's must sit at their index and
            # no other occurrence may collide with it.
            # THE RECIPIENT IS ALWAYS OCCURRENCE 0 -- the client can only ever
            # match there (see `_vsgame_body`), so its own id leads the list
            # whatever seat it holds at the table.
            elif ids[0] != b"%d" % _env_int("POL_TM_VSGAME_SELFID", 0):
                _say("FAIL: the recipient's own /ID= must be FIRST, got %r"
                     % (ids,)); ok = False
            elif ids[1] == ids[0]:
                _say("FAIL: a peer's /ID= must differ from the recipient's, or "
                     "0x1024F9 stops on the wrong player -- got %r" % (ids,))
                ok = False
            if len(_pipe_vals(body, b"/CN=")) != 2 or                     len(_pipe_vals(body, b"/R=")) != 7:
                _say("FAIL: /CN= must carry one name per seat and /R= seven "
                     "values, PIPE-separated under one key -- got %r / %r"
                     % (_pipe_vals(body, b"/CN="), _pipe_vals(body, b"/R=")))
                ok = False
            # One `@No<i>=` per ABSOLUTE seat, named by sprintf("@No%d") at
            # 0x10282F -- and `=`-delimited for the same reason as the header.
            for i in range(2):
                if b"@No%d=" % i not in body:
                    _say("FAIL: @VsGameInit needs an @No%d= section" % i)
                    ok = False
            # /M= is read only out of the recipient's own section (0x1028BE),
            # but every section carries it: the client picks, we do not.
            if body.count(b"/M=") != 2:
                _say("FAIL: each @No section carries its own /M=, got %d"
                     % body.count(b"/M=")); ok = False

        # --- the slot ---------------------------------------------------------
        # code 0x43 in header byte 0, command 1 in byte 2. `0x825A0(buf, 0x43,
        # 1)` will not look at anything else, and `0xA9780(rec, 4)` is what reads
        # the msgid out of the 8-hex header.
        if encode_code(VSGAME_MSGID) != b"43000100":
            _say("FAIL: @VsGameInit must be addressed (0x43, 1), got %r"
                 % (encode_code(VSGAME_MSGID),)); ok = False
        if encode_code(COMGAME_MSGID) != b"43000200":
            _say("FAIL: @ComGameInit must be addressed (0x43, 2), got %r"
                 % (encode_code(COMGAME_MSGID),)); ok = False

        # --- vs-COM answers in the slot it was asked in -----------------------
        ans = handle_line(b"43000200@ComGame=/No=0", member_id=None)
        if not ans or not ans.startswith(b"43000200"):
            _say("FAIL: @ComGame= must be answered on its OWN (code, msgid) -- "
                 "0x101A10 looks the answer up by the id it was called with; "
                 "got %r" % (ans,)); ok = False
        if not ans or b"@ComGameInit=" not in ans:
            _say("FAIL: the vs-COM answer is @ComGameInit= (arm 0x102D21), "
                 "got %r" % (ans,)); ok = False
        if ans and len(_pipe_vals(ans, b"/R=")) != 7:
            _say("FAIL: @ComGameInit carries seven PIPE-separated /R= values, "
                 "got %r" % (_pipe_vals(ans, b"/R="),)); ok = False
        # A msgid we did not choose must be echoed, not normalised to 2.
        ans5 = handle_line(b"43000500@ComGame=", member_id=None)
        if not ans5 or not ans5.startswith(b"43000500"):
            _say("FAIL: @ComGame= must be answered in the msgid it arrived on, "
                 "got %r" % (ans5,)); ok = False

        # --- the VS. COM roster: /Com= is the player count, and every -------
        # --- in-match push must be framed from the game peer ----------------
        # Live 2026-08-22T13:57Z: a 3-player COM game (@ComGame=/Com=1|2) was
        # answered `/Ans=1|1` and `/L=0|0` because N=2 was hardcoded with the
        # /Com= field discarded; the deal went out UNPINNED, framed from the
        # recipient's own nick, and the session-pair gate (0x1034FD compares
        # the delivering record's sender u64 against [obj+0x28]/[0x2C], which
        # the @ComGameInit arm 0x102D3C filled with the GAME PEER's guid) threw
        # it away without a log line anywhere; and turn 0 was never queued at
        # all (the fifth seat gate). This chain is the whole COM start path.
        _PUSHES.pop(_push_key("comtest"), None)
        _svcom, _svpeer = dict(_COM_GAME), dict(_MATCH_PEER)
        _COM_GAME.clear()
        handle_line(b"41001200@GameENC=/NN=0", member_id="comtest",
                    peer_nick=b"UTESTPEER")
        if _MATCH_PEER.get(_push_key("comtest")) != b"UTESTPEER":
            _say("FAIL: @GameENC= must record the game peer in _MATCH_PEER -- "
                 "a COM game never sends @GameOK=, and unframed pushes die on "
                 "the session-pair gate"); ok = False
        _q0 = _PUSHES.get(_push_key("comtest")) or []
        if not any(e[2] == b"UTESTPEER" for e in _q0
                   if b"@ComGameInit=" in e[1]):
            _say("FAIL: the queued (0x43,2) @ComGameInit must be PINNED to the "
                 "game peer -- delivered from any other nick it would set the "
                 "session pair to the wrong guid"); ok = False
        handle_line(b"43000603@ComGame=/Rule=100|0|1|1|1|0|0|0/Com=1|2",
                    member_id="comtest", peer_nick=b"UTESTPEER")
        if _com_n("comtest") != 3:
            _say("FAIL: /Com=1|2 is TWO COM opponents -> 3 players, got %r"
                 % (_com_n("comtest"),)); ok = False
        _PUSHES.pop(_push_key("comtest"), None)
        _cs3 = handle_line(b"43000700@CardSelect=/C=5"
                           b"@N0=/D=42|43|1|20|18|7|4|255"
                           b"@N1=/D=47|38|1|28|11|7|3|255"
                           b"@N2=/D=58|56|1|56|45|14|7|2"
                           b"@N3=/D=59|45|1|45|56|14|7|2"
                           b"@N4=/D=87|60|2|60|87|21|12|4",
                           member_id="comtest", peer_nick=b"UTESTPEER")
        if not _cs3 or b"/Ans=1|1|1" not in _cs3 or b"/Ok=1" not in _cs3:
            _say("FAIL: a 3-player COM card select answers /Ans=1|1|1/Ok=1 "
                 "(one occurrence per seat, arm 0x1030C9), got %r"
                 % (_cs3,)); ok = False
        _qs = _PUSHES.get(_push_key("comtest")) or []
        _sd3 = next((e for e in _qs if b"@StartData=" in e[1]), None)
        _td3 = next((e for e in _qs if b"@TurnData=" in e[1]), None)
        if _sd3 is None:
            _say("FAIL: the COM deal must queue @StartData"); ok = False
        else:
            if len(_pipe_vals(_sd3[1], b"/L=")) != 3:
                _say("FAIL: a 3-player deal carries three /L= values "
                     "(0x103689 zeroes a failed parse -- the count must be "
                     "exact), got %r" % (_pipe_vals(_sd3[1], b"/L="),))
                ok = False
            if len(_pipe_vals(_sd3[1], b"/F=")) != 25:
                _say("FAIL: a 3-player board is 25 tiles (TILES_BY_PLAYERS[1]),"
                     " got %d" % len(_pipe_vals(_sd3[1], b"/F="))); ok = False
            if _sd3[2] != b"UTESTPEER" or _sd3[4] != b"UTESTPEER":
                _say("FAIL: the COM deal must be pinned AND framed to the game "
                     "peer (session-pair gate 0x1034FD), got peer=%r source=%r"
                     % (_sd3[2], _sd3[4])); ok = False
        if _td3 is None:
            _say("FAIL: the COM game must queue turn 0's @TurnData -- the "
                 "FIFTH seat gate: the loop iterated `seats`, which is empty "
                 "in a COM game, so the scene polled command 9 for ever")
            ok = False
        else:
            if len(_pipe_vals(_td3[1], b"/S=")) != 3:
                _say("FAIL: a 3-player @TurnData carries three /S= values, "
                     "got %r" % (_pipe_vals(_td3[1], b"/S="),)); ok = False
            if _td3[2] != b"UTESTPEER":
                _say("FAIL: turn 0 must be pinned to the game peer too, "
                     "got %r" % (_td3[2],)); ok = False
        # The COM hands were dealt at CardSelect time -- the server plays the
        # COM (measured 14:31Z: the client acks the COM's turn announcement
        # and then just polls (0x43, 10) -- it never sends @PutData for a COM
        # seat).
        _ckey = (None, _push_key("comtest"))
        _chands = _MATCH_HANDS.get(_ckey) or {}
        if len(_chands.get(("com", 1)) or []) != 5 \
                or len(_chands.get(("com", 2)) or []) != 5:
            _say("FAIL: both COM seats must be dealt five cards at the deal, "
                 "got %r" % ({k: len(v) for k, v in _chands.items()},))
            ok = False
        # ...and the human's placement starts the ACK-GATED chain: announce
        # the COM's turn 1, but hold its @PutCard until the client's own
        # @TurnData= ack proves it is past the GamePlayEnd purge. THE CRASH
        # THIS ENCODES (2026-08-22T17:03Z, 3-player): 0xC7642 purges
        # (0x43, 8/0xA/0xC/0x28) from the store at every GamePlayEnd, so a
        # COM put delivered while the previous turn still animates is
        # DELETED (popped, no Recv=PUTCARD), and the next put the client
        # sees is a turn ahead -- Errot!!Turn -> -1 -> 0xC72A6's "invalid
        # card data detected" -> CardGameOutClose.
        _prev = list(_PUSHES.get(_push_key("comtest")) or [])
        _pd = handle_line(b"43000A00@PutData=/P=0/H=0/F=12",
                          member_id="comtest", peer_nick=b"UTESTPEER")
        _pdl = _pd if isinstance(_pd, list) else ([_pd] if _pd else [])
        if not any(b"@PutCard=" in ln for ln in _pdl):
            _say("FAIL: the human's @PutData= must answer @PutCard=, got %r"
                 % (_pdl,)); ok = False
        _qp = [e for e in (_PUSHES.get(_push_key("comtest")) or [])
               if not any(e is p for p in _prev)]
        if any(b"@PutCard=" in e[1] for e in _qp):
            _say("FAIL: no COM @PutCard may be QUEUED before its turn is "
                 "acked -- an early put is purged at GamePlayEnd (0xC7642) "
                 "and the desync is the 3-player 'invalid card data' crash")
            ok = False
        if len([e for e in _qp if b"@TurnData=" in e[1]]) != 1:
            _say("FAIL: the placement announces exactly ONE turn (the next "
                 "COM turn; its put waits for the ack), got %r"
                 % ([e[1][:20] for e in _qp],)); ok = False
        _st_ack = _MATCH_TURN.get((None, _push_key("comtest"))) or {}
        if _st_ack.get("await_ack") != 1:
            _say("FAIL: the announced COM turn must arm await_ack=1, got %r"
                 % (_st_ack.get("await_ack"),)); ok = False
        # The client enters turn 1 and acks it: the pending put ANSWERS THE
        # ACK (on its own (0x43,10) slot -- the cmd-9 reader never sees it).
        _a1 = handle_line(b"43000901@TurnData=/Ans=0", member_id="comtest",
                          peer_nick=b"UTESTPEER")
        _a1l = _a1 if isinstance(_a1, list) else ([_a1] if _a1 else [])
        if not any(ln.startswith(b"43000A01") and b"@PutCard=/A=1" in ln
                   for ln in _a1l):
            _say("FAIL: acking COM turn 1 must release its @PutCard as the "
                 "direct reply (header 43000A01, /A=1), got %r" % (_a1l,))
            ok = False
        if (_MATCH_TURN.get((None, _push_key("comtest"))) or {}) \
                .get("await_ack") != 2:
            _say("FAIL: releasing turn 1 must announce turn 2 and re-arm "
                 "await_ack=2"); ok = False
        _a2 = handle_line(b"43000902@TurnData=/Ans=0", member_id="comtest",
                          peer_nick=b"UTESTPEER")
        _a2l = _a2 if isinstance(_a2, list) else ([_a2] if _a2 else [])
        if not any(ln.startswith(b"43000A02") and b"@PutCard=/A=2" in ln
                   for ln in _a2l):
            _say("FAIL: acking COM turn 2 must release the second COM seat's "
                 "@PutCard (43000A02, /A=2), got %r" % (_a2l,)); ok = False
        _qp2 = _PUSHES.get(_push_key("comtest")) or []
        if not any(b"@TurnData=/A=0" in e[1] and e[1].startswith(b"43000903")
                   for e in _qp2):
            _say("FAIL: after the last COM seat the human's turn 3 must be "
                 "announced (43000903, /A=0)"); ok = False
        if (_MATCH_TURN.get((None, _push_key("comtest"))) or {}) \
                .get("await_ack") is not None:
            _say("FAIL: a human turn arms no await_ack"); ok = False
        # A duplicate ack must not double-play the turn. (The reply may still
        # carry DRAINED pushes -- handle_line attaches anything due -- so the
        # assertion is "no put released", not "no reply".)
        _dup = handle_line(b"43000902@TurnData=/Ans=0", member_id="comtest",
                           peer_nick=b"UTESTPEER")
        _dupl = _dup if isinstance(_dup, list) else ([_dup] if _dup else [])
        if any(b"@PutCard=" in ln for ln in _dupl):
            _say("FAIL: a duplicate turn ack must release no @PutCard "
                 "(await_ack was consumed)"); ok = False
        _afters = [e[0] for e in _qp]
        if _afters != sorted(_afters):
            _say("FAIL: the COM chain's queued pushes must be in "
                 "non-decreasing after order, got %r" % (_afters,))
            ok = False
        if any(e[2] != b"UTESTPEER" for e in _qp):
            _say("FAIL: every COM chain push must be pinned to the game peer")
            ok = False
        # --- the post-game family: @Ready, @GetSelect, @Continue (COM) ------
        # The win-screen hang, live 14:51:56Z: @ResultData read, the win
        # screen drew, the client sent (0x43,14) @Ready=/Ans=0 and nothing
        # answered. Arm 0x1045E7: any well-formed echo with /Go= releases it.
        _rd = handle_line(b"43000E0A@Ready=/Ans=0", member_id="comtest",
                          peer_nick=b"UTESTPEER")
        _rdl = _rd if isinstance(_rd, list) else ([_rd] if _rd else [])
        if not any(ln.startswith(b"43000E0A") and b"@Ready=/Go=1" in ln
                   for ln in _rdl):
            _say("FAIL: (0x43,14) @Ready= must be echoed with /Go= (arm "
                 "0x1045E7 parses /Go= and sets result 1), got %r" % (_rdl,))
            ok = False
        # ...and @GameReady= (0x41) must NOT fall into the @Ready arm.
        if handle_line(b"43000E0A@GameReady=", member_id=None) is not None:
            _say("FAIL: @GameReady= contains '@Ready=' as a substring and "
                 "must not reach the READY echo"); ok = False
        _gs = handle_line(b"43000F00@GetSelect=/H=2", member_id="comtest",
                          peer_nick=b"UTESTPEER")
        _gsl = _gs if isinstance(_gs, list) else ([_gs] if _gs else [])
        if not any(ln.startswith(b"43000F00") and b"/E=0/H=2" in ln
                   for ln in _gsl):
            _say("FAIL: (0x43,15) @GetSelect=/H=2 must echo /E=/H= (arm "
                 "0x104E5B stores /H= into [obj+0x1B2]), got %r" % (_gsl,))
            ok = False
        # @Continue in a COM game: the COM always agrees, the human's answer
        # is the vote. /Ans=1 -> sh=1 everyone-1 (rematch ON); /Ans=2 -> the
        # human's own slot 3 under sh>=2 (the only test state 7 makes).
        _cy = handle_line(b"43001100@Continue=/Ans=1/Conf=0/PR=0",
                          member_id="comtest", peer_nick=b"UTESTPEER")
        _cyl = _cy if isinstance(_cy, list) else ([_cy] if _cy else [])
        if not any(ln.startswith(b"43001101") and b"/Ans=1|1|1" in ln
                   for ln in _cyl):
            _say("FAIL: COM @Continue=/Ans=1 must answer sh=1 with every "
                 "slot 1 (rematch ON), got %r" % (_cyl,)); ok = False
        # WARNING: /Conf=1 IS "CHANGE THE SETTINGS FIRST", and that path waits on
        # (0x43, 4) @ComList. Without it the screen froze and the client went
        # SILENT -- measured three times live 2026-09-07, and Conf=0 never did.
        _PUSHES.pop(_push_key("comtest"), None)
        handle_line(b"43001100@Continue=/Ans=1/Conf=0/PR=0",
                    member_id="comtest", peer_nick=b"UTESTPEER")
        if any(b"@ComList=" in e[1]
               for e in (_PUSHES.get(_push_key("comtest")) or [])):
            _say("FAIL: /Conf=0 is a plain rematch and must NOT send @ComList")
            ok = False
        _PUSHES.pop(_push_key("comtest"), None)
        handle_line(b"43001100@Continue=/Ans=1/Conf=1/PR=0",
                    member_id="comtest", peer_nick=b"UTESTPEER")
        _clq = [e[1] for e in (_PUSHES.get(_push_key("comtest")) or [])
                if b"@ComList=" in e[1]]
        if len(_clq) != 1:
            _say("FAIL: /Conf=1 must queue exactly one @ComList, got %r"
                 % (_clq,)); ok = False
        elif not _clq[0].startswith(b"43000400"):
            _say("FAIL: @ComList rides (0x43, 4) -- arm 0x102F74; got %r"
                 % (_clq[0][:8],)); ok = False
        elif _clq[0].count(b"|") + 1 != int(
                re.search(rb"/L=(\d+)", _clq[0]).group(1)):
            _say("FAIL: /L= is the ROW COUNT and must match the number of /D= "
                 "occurrences (0x102FDC reads it as the loop bound) -- %r"
                 % (_clq[0],)); ok = False
        _PUSHES.pop(_push_key("comtest"), None)
        _cn = handle_line(b"43001100@Continue=/Ans=2/Conf=0/PR=0",
                          member_id="comtest", peer_nick=b"UTESTPEER")
        _cnl = _cn if isinstance(_cn, list) else ([_cn] if _cn else [])
        if not any(ln.startswith(b"43001102") and b"/Ans=3|" in ln
                   for ln in _cnl):
            _say("FAIL: COM @Continue=/Ans=2 must answer sh>=2 with the "
                 "human's own slot 3 (CONTINUE_FINAL tears the panel down), "
                 "got %r" % (_cnl,)); ok = False
        # A vote whose live roster died mid-quorum RESUMES from the tally's
        # own seats snapshot (2026-08-22T19:43Z: a deploy restart between the
        # two votes hung both clients on "Now loading")...
        _MATCH_CONTINUE[(None, "q-test")] = {
            "seats": [("qva", 0), ("qvb", 0)],
            "votes": {_push_key("qva"): 2}}
        _qv = handle_line(b"43001100@Continue=/Ans=2/Conf=0/PR=0",
                          member_id="qvb")
        _qvl = _qv if isinstance(_qv, list) else ([_qv] if _qv else [])
        if not any(ln.startswith(b"43001102") for ln in _qvl):
            _say("FAIL: a vote with no live roster must resume from the "
                 "pending tally's seats snapshot and complete the quorum, "
                 "got %r" % (_qvl,)); ok = False
        _MATCH_CONTINUE.pop((None, "q-test"), None)
        # ...and a vote NOTHING remembers gets the orphan teardown, not a
        # hang: sh=2, full-width all-3 (longer than N is safe; short is the
        # stack-garbage hazard).
        _qo = handle_line(b"43001100@Continue=/Ans=2/Conf=0/PR=0",
                          member_id="q-orphan")
        _qol = _qo if isinstance(_qo, list) else ([_qo] if _qo else [])
        if not any(ln.startswith(b"43001102") and b"/Ans=3|3|3|3" in ln
                   for ln in _qol):
            _say("FAIL: an orphaned @Continue (restart raced the vote) must "
                 "answer the all-3 teardown, got %r" % (_qol,)); ok = False
        # --- the take flow: @TCList row shape and decisive-only --------------
        _rowA = b"42|43|1|20|18|7|4|255"
        _tb = _tclist_body([[_rowA], [_rowA, _rowA]])
        if not _tb.startswith(b"@TCList=/H=1|2"):
            _say("FAIL: @TCList /H= is one count per player, got %r"
                 % (_tb[:24],)); ok = False
        # ...in the RECIPIENT's frame (41c, measured 15:28Z): seat 1's copy
        # lists its own pool first.
        _tb1 = _tclist_body(_take_frame([[_rowA], [_rowA, _rowA]], 1))
        if not _tb1.startswith(b"@TCList=/H=2|1"):
            _say("FAIL: the take list must be rotated so the recipient's own "
                 "pool is occurrence 0 (the Deck picked blind from seat 1), "
                 "got %r" % (_tb1[:24],)); ok = False
        # occ 0 = the seat ENTITLED to take the row, in the recipient's frame
        # (state 0x3A, 0xC91B9): winner seat 0's copy keeps 0 everywhere; the
        # loser (seat 1) sees its own rows tagged 1 (the winner in its frame)
        # and the winner's rows 0 -- without it the loser's scene finds no
        # target and leaves before the pick (2026-09-07).
        _p2 = [[_rowA], [_rowA]]
        if _take_occ0(_take_frame(_p2, 0), 0, 0) != [0, 0]:
            _say("FAIL: the winner's own copy keeps occ0 = 0 (its targets)"); ok = False
        if _take_occ0(_take_frame(_p2, 1), 1, 0) != [1, 0]:
            _say("FAIL: the loser's copy must tag its own rows with the winner's "
                 "index (1) and the winner's rows 0, got %r"
                 % (_take_occ0(_take_frame(_p2, 1), 1, 0),)); ok = False
        _tbl = _tclist_body(_take_frame(_p2, 1), _take_occ0(_take_frame(_p2, 1), 1, 0))
        if b"/D0=1|" not in _tbl or b"/D1=0|" not in _tbl:
            _say("FAIL: the loser's take list rows must carry the taker byte, "
                 "got %r" % (_tbl,)); ok = False
        if _losers_from(1, [[_rowA], [_rowA], [_rowA]]) != [2, 0]:
            _say("FAIL: the winner's take screen walks the losers in rotated "
                 "order after itself, got %r"
                 % (_losers_from(1, [[_rowA], [_rowA], [_rowA]]),)); ok = False
        # occ 0 must stay 0 (small): it is a validity flag, not the name -- occ
        # 0 = id made the take cards INVISIBLE live (2026-09-02). Row shape is
        # 0|id|atk|type|pdef|mdef|arrows, flat-indexed across players.
        if b"/D0=0|42|43|1|20|18|4" not in _tb \
                or b"/D2=0|42|43|1|20|18|4" not in _tb:
            _say("FAIL: @TCList /D<k>= is flat-indexed, occurrences "
                 "0|id|atk|type|pdef|mdef|arrows (arm 0x104648); occ 0 must be "
                 "0 or the card goes invisible, got %r" % (_tb,)); ok = False
        _tk = (None, "tcl-test")
        _MATCH_HANDS[_tk] = {("deck", 0): [_rowA], ("deck", 1): [_rowA]}
        _MATCH_BOARD[_tk] = {0: tmbattle.Card(_rowA, 0)}   # 1-0: decisive
        _PUSHES.pop(_push_key("tclm"), None)
        _queue_next_turn(None, "tcl-test", [], 10, 0,
                         roster=[("tclm", 0)], n_solo=2)
        _qt = _PUSHES.get(_push_key("tclm")) or []
        if not any(b"@TCList=" in e[1] for e in _qt) \
                or any(b"@GetAway=" in e[1] for e in _qt):
            _say("FAIL: a decisive COM result must queue @TCList and NO "
                 "@GetAway -- the win screen picks without it and to a "
                 "recipient it means 'seat P LEFT' (measured 2026-09-07 "
                 "15:13Z; the 16:49Z 'hangs without it' read is retracted)")
            ok = False
        _MATCH_BOARD[_tk] = {}                              # 0-0: a draw
        _MATCH_TURN.pop(_tk, None)
        _PUSHES.pop(_push_key("tclm"), None)
        _queue_next_turn(None, "tcl-test", [], 10, 0,
                         roster=[("tclm", 0)], n_solo=2)
        if any(b"@TCList=" in e[1]
               for e in (_PUSHES.get(_push_key("tclm")) or [])):
            _say("FAIL: a DRAW must queue no take flow -- the draw screen "
                 "polls neither 26 nor 35 and flows to @Continue by itself")
            ok = False
        _PUSHES.pop(_push_key("tclm"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND,
                   _MATCH_CONTINUE, _CARD_READY):
            _d.pop(_tk, None)

        # --- the take is a TRANSFER now -------------------------------
        # (1) HUMAN WON: @GetSelect=/H=<slot> adds the loser's card to the
        # winner's collection, once. The fixture key must be the real COM key
        # (chan from _match_of, index = push_key) or the handler cannot find
        # the stashed take state -- which is itself part of the contract.
        _tkm = (None, _push_key("tclm"))
        _COM_GAME[_push_key("tclm")] = {"n": 2, "coms": [1]}
        _MATCH_HANDS[_tkm] = {("deck", 0): [_rowA], ("deck", 1): [_rowA]}
        _MATCH_BOARD[_tkm] = {0: tmbattle.Card(_rowA, 0)}   # human wins 1-0
        _PUSHES.pop(_push_key("tclm"), None)
        _collection_store("tclm", {"cards": []}, sync_save=False)
        _queue_next_turn(None, _push_key("tclm"), [], 10, 0,
                         roster=[("tclm", 0)], n_solo=2)
        _gsw = handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
        _gswl = _gsw if isinstance(_gsw, list) else ([_gsw] if _gsw else [])
        _col = (_collection_load("tclm").get("cards")) or []
        if _col != [[42, 43, 1, 20, 18, 7, 4, 255]]:
            _say("FAIL: a winning @GetSelect must ADD the picked card to the "
                 "collection (-- the take was display only), got %r"
                 % (_col,)); ok = False
        _gsw2 = handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
        if len((_collection_load("tclm").get("cards")) or []) != 1:
            _say("FAIL: a 2-player win has ONE loser -- a re-sent @GetSelect "
                 "must not take twice"); ok = False
        _PUSHES.pop(_push_key("tclm"), None)
        # (1b) a 3-PLAYER win takes ONE CARD PER LOSING SEAT (reported live,
        # 2026-08-22: "it lets me take one card from each com") -- two picks
        # land, a third is refused.
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND):
            _d.pop(_tkm, None)
        _COM_GAME[_push_key("tclm")] = {"n": 3, "coms": [1, 2]}
        _MATCH_HANDS[_tkm] = {("deck", 0): [_rowA], ("deck", 1): [_rowA],
                              ("deck", 2): [_rowA]}
        _MATCH_BOARD[_tkm] = {0: tmbattle.Card(_rowA, 0)}   # human wins 1-0-0
        _collection_store("tclm", {"cards": []}, sync_save=False)
        _queue_next_turn(None, _push_key("tclm"), [], 15, 0,
                         roster=[("tclm", 0)], n_solo=3)
        handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
        handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
        handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
        if len((_collection_load("tclm").get("cards")) or []) != 2:
            _say("FAIL: a 3-player win takes ONE card per losing seat (two "
                 "picks land, the third is refused), got %r"
                 % (_collection_load("tclm").get("cards"),)); ok = False
        _PUSHES.pop(_push_key("tclm"), None)
        _COM_GAME[_push_key("tclm")] = {"n": 2, "coms": [1]}
        # (2) COM WON: the server makes the pick itself -- the loser's screen
        # polls (0x43, 15) (0xCA481) and parks without it -- and the human's
        # card leaves the collection.
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND):
            _d.pop(_tkm, None)
        _MATCH_HANDS[_tkm] = {("deck", 0): [_rowA], ("deck", 1): [_rowA]}
        _MATCH_BOARD[_tkm] = {0: tmbattle.Card(_rowA, 1)}   # the COM wins 1-0
        _collection_store("tclm", {"cards": [[42, 43, 1, 20, 18, 7, 4, 255]]},
                          sync_save=False)
        _queue_next_turn(None, _push_key("tclm"), [], 10, 0,
                         roster=[("tclm", 0)], n_solo=2)
        _qt3 = _PUSHES.get(_push_key("tclm")) or []
        _gsel = next((e for e in _qt3 if b"@GetSelect=" in e[1]), None)
        if _gsel is None or not _gsel[1].startswith(b"43000F01"):
            _say("FAIL: a COM win must push the COM's own @GetSelect (header "
                 "43000F01 -- sh is the winner's seat), got %r"
                 % (_gsel[1][:20] if _gsel else None)); ok = False
        if (_collection_load("tclm").get("cards")) or []:
            _say("FAIL: the COM's take must remove the picked card from the "
                 "human's collection (POL_TM_TAKE_LOSS)"); ok = False
        _PUSHES.pop(_push_key("tclm"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND,
                   _MATCH_CONTINUE, _CARD_READY):
            _d.pop(_tkm, None)

        # --- the PvP take: flow queued for BOTH, transfer moves a card ------
        # between two REAL collections (winner gains, loser loses), gated on
        # the take-roster seat so a loser's own @GetSelect stays echo-only.
        _rowB = b"47|38|1|28|11|7|3|255"
        _tkp = (None, "pvp-take-test")
        _MATCH_HANDS[_tkp] = {("deck", 0): [_rowA], ("deck", 1): [_rowB]}
        _MATCH_BOARD[_tkp] = {0: tmbattle.Card(_rowA, 1)}   # seat 1 wins 0-1
        _collection_store("tclm", {"cards": []}, sync_save=False)
        _collection_store("tclo", {"cards": [[42, 43, 1, 20, 18, 7, 4, 255]]},
                          sync_save=False)
        _PUSHES.pop(_push_key("tclm"), None)
        _PUSHES.pop(_push_key("tclo"), None)
        _pvp_seats = [("tclo", 0), ("tclm", 0)]     # tclo seat 0, tclm seat 1
        _queue_next_turn(None, "pvp-take-test", _pvp_seats, 10, 0,
                         roster=_pvp_seats)
        for _pm in ("tclm", "tclo"):
            _qv = _PUSHES.get(_push_key(_pm)) or []
            if not any(b"@TCList=" in e[1] for e in _qv) \
                    or any(b"@GetAway=" in e[1] for e in _qv):
                _say("FAIL: a decisive PvP result must queue @TCList and NO "
                     "@GetAway to member %s (@GetAway = 'seat P LEFT', "
                     "measured 2026-09-07 15:13Z)" % _pm)
                ok = False
        # the LOSER's pick is echo-only...
        _sv_mof = _match_of
        globals()["_match_of"] = lambda m: (None, "pvp-take-test", _pvp_seats)
        try:
            handle_line(b"43000F00@GetSelect=/H=0", member_id="tclo")
            if (_collection_load("tclo").get("cards") or []) == []:
                _say("FAIL: a LOSER's @GetSelect must transfer nothing")
                ok = False
            # ...the WINNER's pick moves the card between the collections.
            handle_line(b"43000F00@GetSelect=/H=0", member_id="tclm")
            if [c[0] for c in (_collection_load("tclm").get("cards") or [])] \
                    != [42]:
                _say("FAIL: the PvP winner's @GetSelect must ADD the loser's "
                     "picked card, got %r"
                     % (_collection_load("tclm").get("cards"),)); ok = False
            if (_collection_load("tclo").get("cards") or []):
                _say("FAIL: the PvP loser must LOSE the picked card "
                     "(POL_TM_TAKE_LOSS), got %r"
                     % (_collection_load("tclo").get("cards"),)); ok = False
        finally:
            globals()["_match_of"] = _sv_mof
        _PUSHES.pop(_push_key("tclm"), None)
        _PUSHES.pop(_push_key("tclo"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND,
                   _MATCH_CONTINUE, _CARD_READY):
            _d.pop(_tkp, None)
        for _pth in (_collection_file("tclo"), _save_resource_file("tclo")):
            try:
                if _pth and os.path.exists(_pth):
                    os.remove(_pth)
            except OSError:
                pass

        # --- the PvP WAGER: escrow at the result ----------------------------
        # (The stake-in half rides the accept quorum in _queue_vsgameinit;
        # here the settlement is exercised directly: a unique winner takes
        # the pot, a tie refunds each player their own stake.)
        _pk = ("#TM0RTEST", 8)
        _collection_store("tclm", {"cards": [], "money": 500}, sync_save=False)
        _collection_store("tclo", {"cards": [], "money": 300}, sync_save=False)
        _PVP_STAKES[_pk] = {_push_key("tclm"): ("tclm", 200),
                            _push_key("tclo"): ("tclo", 200)}
        _stake_persist("tclm", 200); _stake_persist("tclo", 200)
        _MATCH_BOARD[_pk] = {0: tmbattle.Card(_rowA, 0)}    # tclm wins 1-0
        _MATCH_TURN.pop(_pk, None)
        _queue_next_turn("#TM0RTEST", 8, [("tclm", 0), ("tclo", 0)], 10, 0,
                         roster=[("tclm", 0), ("tclo", 0)])
        if money_of("tclm") != 500 + 400:
            _say("FAIL: a unique PvP winner takes the whole pot (own 200 "
                 "back + 200 winnings), got %d" % money_of("tclm")); ok = False
        if money_of("tclo") != 300:
            _say("FAIL: the PvP loser's stake stays forfeited, got %d"
                 % money_of("tclo")); ok = False
        if (_collection_load("tclo").get("staked_wager")) is not None:
            _say("FAIL: PvP settlement must clear BOTH durable stakes")
            ok = False
        _rk2 = (_collection_load("tclm").get("rank")) or {}
        if _rk2.get("prize_total", 0) < 200:
            _say("FAIL: the PvP winner's net gain must feed the prize tally, "
                 "got %r" % (_rk2,)); ok = False
        # ...and the winner's own @No0 section DELIVERS the settled prize on
        # /D= occ 1 ([obj+0xCC] -- 0xCBD80 max-updates it into [obj+0x78],
        # the Biggest Prize `max`). Reported live 2026-09-02: the result screen's
        # wager row read 0 for ever because the settlement ran after the
        # bodies were built and nothing carried the figure.
        _prb = next((e[1] for e in (_PUSHES.get(_push_key("tclm")) or [])
                     if b"@ResultData=" in e[1]), None)
        _prd = _pipe_vals(_prb or b"", b"/D=")
        if len(_prd) < 2 or _prd[1] != b"200":
            _say("FAIL: the winner's @ResultData /D= occ 1 must carry the "
                 "settled net prize (200), got %r" % (_prb,)); ok = False
        # ...a TIE refunds each player their own stake.
        _PVP_STAKES[_pk] = {_push_key("tclm"): ("tclm", 100),
                            _push_key("tclo"): ("tclo", 100)}
        _MATCH_BOARD[_pk] = {}                              # 0-0
        _MATCH_TURN.pop(_pk, None)
        _wm_before = money_of("tclm")
        _wo_before = money_of("tclo")
        _queue_next_turn("#TM0RTEST", 8, [("tclm", 0), ("tclo", 0)], 10, 0,
                         roster=[("tclm", 0), ("tclo", 0)])
        if money_of("tclm") != _wm_before + 100 \
                or money_of("tclo") != _wo_before + 100:
            _say("FAIL: a PvP tie must refund each stake, got %d/%d"
                 % (money_of("tclm"), money_of("tclo"))); ok = False
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_BOARD, _MATCH_TURN, _TURN_RAND, _MATCH_HANDS,
                   _MATCH_CONTINUE, _CARD_READY):
            _d.pop(_pk, None)
        _PVP_STAKES.pop(_pk, None)

        # --- DOUBLE UP (`du`): a POST-GAME STAKE ESCALATOR ------------------
        # KEY: NOT a pre-game multiplier, which is the answer to "why not just
        # wager double". Both `du` read sites sit inside 0x10B780 -- the "Play
        # again?" panel constructor (`Playmes.BIN` 93, accessors all index
        # 0x11 = @Continue) -- so the rule doubles the pot the REMATCH is
        # played for (0x10BF5C, clamped to 0x7D0) and switches ITSELF off when
        # the wallet can no longer cover it (0x10D9C9). @Continue= carries no
        # field that reports the new figure, so this server has to run the
        # same arithmetic or the panel and the escrow disagree from the first
        # rematch -- the gold divergence flagged earlier and left open.
        _dk = ("#TM0RDU", 3)
        _sv_trg = globals()["_table_rules_get"]
        try:
            _du_rules = {"bm": 300, "du": 1}
            globals()["_table_rules_get"] = lambda _c, _i: dict(_du_rules)
            _TABLE_POT.pop(_dk, None)
            _collection_store("duA", {"cards": [], "money": 9000},
                              sync_save=False)
            _collection_store("duB", {"cards": [], "money": 9000},
                              sync_save=False)
            _duseats = [("duA", 0), ("duB", 0)]
            if _table_pot(_dk[0], _dk[1]) != 300:
                _say("FAIL: the table pot is seeded from the `bm` rule, got %r"
                     % (_table_pot(_dk[0], _dk[1]),)); ok = False
            # ...and it COMPOUNDS across rematches, up to the 2000 clamp. That
            # clamp is the point of the rule: the pot is capped everywhere, so
            # doubling is the only way to climb to the ceiling from below --
            # and it does nothing at all for a wager above 1000.
            _ladder = [_double_up_pot(_dk[0], _dk[1], _duseats)
                       for _ in range(4)]
            if _ladder != [600, 1200, 2000, 2000]:
                _say("FAIL: du doubles the pot each rematch and clamps at "
                     "%d (0x10BF5C/0x10BF69) -- got %r"
                     % (_wager_cap(), _ladder)); ok = False
            # `du = 0` never escalates, which is why the rule was inert.
            _du_rules["du"] = 0
            _TABLE_POT[_dk] = 300
            if _double_up_pot(_dk[0], _dk[1], _duseats) != 300:
                _say("FAIL: with du=0 a rematch is played for the same pot")
                ok = False
            # WARNING: THE WALLET GATE, 0x10D9C9. A seat that cannot cover the
            # DOUBLED stake turns the rule off -- the client raises its own
            # "can't afford it" dialog on the same compare, so escalating
            # anyway would stake a player into an overdraft their own screen
            # already refused them.
            _du_rules["du"] = 1
            _TABLE_POT[_dk] = 300
            _collection_store("duB", {"cards": [], "money": 500},
                              sync_save=False)
            if _double_up_pot(_dk[0], _dk[1], _duseats) != 300:
                _say("FAIL: du switches itself off when a seat cannot cover "
                     "the doubled pot (0x10D9C9), got %r"
                     % (_TABLE_POT.get(_dk),)); ok = False
            # ...and the escrow takes the ESCALATED pot off every seat, which
            # is the half that used to be missing: a rematch sends no
            # @VsGameInit, so nothing staked it at all.
            _collection_store("duB", {"cards": [], "money": 9000},
                              sync_save=False)
            _TABLE_POT[_dk] = 300
            _dpot = _double_up_pot(_dk[0], _dk[1], _duseats)
            _stake_pvp_pot(_dk[0], _dk[1], _duseats, _dpot, "rematch pot")
            if _dpot != 600 or money_of("duA") != 8400                     or money_of("duB") != 8400:
                _say("FAIL: the rematch escrow takes the doubled pot (600) "
                     "off each seat, got pot %r and %d/%d"
                     % (_dpot, money_of("duA"), money_of("duB"))); ok = False
            if sorted(a for _m, a in (_PVP_STAKES.get(_dk) or {}).values())                     != [600, 600]:
                _say("FAIL: ...and it lands in the same escrow the result "
                     "settles, got %r" % (_PVP_STAKES.get(_dk),)); ok = False
            # ...and a FRESH @VsGameInit resets the escalation: du compounds
            # across rematches at one table session, not for ever.
            if _table_pot(_dk[0], _dk[1], {"bm": 300}, reset=True) != 300:
                _say("FAIL: a fresh announcement re-seeds the pot from `bm`")
                ok = False
        finally:
            globals()["_table_rules_get"] = _sv_trg
            _PVP_STAKES.pop(_dk, None)
            _TABLE_POT.pop(_dk, None)
            _stake_clear("duA"); _stake_clear("duB")
            for _pth in (_collection_file("duA"), _collection_file("duB")):
                try:
                    if _pth and os.path.exists(_pth):
                        os.remove(_pth)
                except OSError:
                    pass

        # --- the OBSERVER: @Data=/Watch= answers a @WatchInfo snapshot and
        # --- joins the push relay -------------------------------------------
        _wk = ("#TM0RTEST", 9)
        _MATCH_ROSTER[_push_key("wplA")] = ("#TM0RTEST", 9,
                                            [("wplA", 0), ("wplB", 0)])
        _MATCH_TURN[_wk] = {"turn": 3, "active": 1, "n": 2}
        _MATCH_BOARD[_wk] = {5: tmbattle.Card(_rowA, 0)}
        _MATCH_HANDS[_wk] = {_push_key("wplA"): [_rowA],
                             _push_key("wplB"): [_rowB]}
        _PUSHES.pop(_push_key("wobs"), None)
        _wr = handle_line(b"43002500@Data=/Watch=0", member_id="wobs",
                          peer_nick=b"UNOTATABLE")
        _wrl = _wr if isinstance(_wr, list) else ([_wr] if _wr else [])
        _wsnap = next((ln for ln in _wrl if b"@WatchInfo=" in ln), None)
        if _wsnap is None or not _wsnap.startswith(b"43002501"):
            _say("FAIL: @Data=/Watch= must answer @WatchInfo with sh=1 for a "
                 "running match (the arm reads the board only under sh=1), "
                 "got %r" % (_wsnap[:30] if _wsnap else _wrl,)); ok = False
        if _wsnap is not None and (b"/T=3" not in _wsnap
                                   or b"/F=" not in _wsnap):
            _say("FAIL: the sh=1 snapshot carries the turn and the board "
                 "(/T=, /F=), got %r" % (_wsnap,)); ok = False
        if _push_key("wobs") not in (_MATCH_WATCHERS.get(_wk) or {}):
            _say("FAIL: the observer must join the match's watcher set")
            ok = False
        # ...and the watcher gets BOTH the in-match stream (it reads cmd 9/10/12
        # once its play scene is up) AND @WFCard (the play-scene trigger).
        _PUSHES.pop(_push_key("wobs"), None)
        _watch_push("#TM0RTEST", 9, b"43000903@TurnData=/A=1", "relay probe")
        if not any(b"@TurnData=" in e[1]
                   for e in (_PUSHES.get(_push_key("wobs")) or [])):
            _say("FAIL: _watch_push must relay the in-match stream to a watcher")
            ok = False
        _watch_push("#TM0RTEST", 9, b"43002600@WFCard=/T=0@5/D=1|1|0|1|1|0",
                    "wfcard probe")
        if not any(b"@WFCard=" in e[1]
                   for e in (_PUSHES.get(_push_key("wobs")) or [])):
            _say("FAIL: @WFCard (the play-scene trigger) must reach a watcher")
            ok = False
        # ...and @GameExit= removes the watcher.
        handle_line(b"41000D00@GameExit=", member_id="wobs",
                    peer_nick=b"UNOTATABLE")
        if _push_key("wobs") in (_MATCH_WATCHERS.get(_wk) or {}):
            _say("FAIL: a departing watcher must leave the watcher set")
            ok = False
        _MATCH_ROSTER.pop(_push_key("wplA"), None)
        _MATCH_WATCHERS.pop(_wk, None)
        _PUSHES.pop(_push_key("wobs"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND):
            _d.pop(_wk, None)

        # --- a VS. COM game is AT A TABLE: the room sees Playing, and an
        # --- observer of that table gets the COM match's snapshot ----------
        #
        # The peer is built the way the client builds one -- the table id
        # xor'd with the client key, base-36'd, scrambled -- so this exercises
        # the real `_peer_table` decode rather than a stub. See `_COM_AT`.
        import polnick as _pn
        _ct_room, _ct_no = 1, 5
        _ct_id = tmroom.canonical_table_id(_ct_no - 1, _ct_room)
        _ct_v = (_ct_id ^ tmroom.client_key()) & 0xFFFFFFFFFF
        _ct_pid = ""
        for _i in range(8):
            _ct_v, _d8 = divmod(_ct_v, 36)
            _ct_pid = _pn.ALPHA[_d8] + _ct_pid
        _ct_peer = _pn.nick_for_polid(_ct_pid).encode("ascii")
        _ct_chan = "#TM0R%03d" % _ct_room
        if _peer_table(_ct_peer) != (_ct_chan, _ct_no):
            _say("FAIL: the selftest's own table peer must decode to "
                 "(%s, %d), got %r"
                 % (_ct_chan, _ct_no, (_peer_table(_ct_peer),)))
            ok = False
        _COM_AT.clear()
        _cr = handle_line(
            b"41001200@GameENC=/NN=AB12CD31DC3F63B6/HID=0/Dm=0/Vol=0/CN=/HN=43",
            member_id="ctcom", peer_nick=_ct_peer)
        if not _cr or b"@GameECA=" not in (
                _cr if isinstance(_cr, bytes) else b"".join(_cr)):
            _say("FAIL: @GameENC= must still answer @GameECA=, got %r" % (_cr,))
            ok = False
        if _COM_AT.get((_ct_chan, _ct_no)) != _push_key("ctcom"):
            _say("FAIL: @GameENC= must bind the COM game to the table its "
                 "peer names (_COM_AT = %r)" % (_COM_AT,))
            ok = False
        if not _match_running(_ct_chan, _ct_no):
            _say("FAIL: a COM game at a table must read as a RUNNING match -- "
                 "that is what publishes state 2 (Playing/Observe)")
            ok = False
        if _occupancy_state(_com_who(_ct_chan, _ct_no), 7, inplay=True) != "2":
            _say("FAIL: the COM table's row must publish state 2")
            ok = False
        # ...and the observer of THAT table reaches the COM match's state.
        _COM_GAME[_push_key("ctcom")]["n"] = 2
        _COM_GAME[_push_key("ctcom")]["coms"] = [3]
        _ck = (None, _push_key("ctcom"))
        _MATCH_TURN[_ck] = {"turn": 2, "active": 1, "n": 2}
        _MATCH_BOARD[_ck] = {4: tmbattle.Card(_rowA, 0)}
        _MATCH_HANDS[_ck] = {_push_key("ctcom"): [_rowA],
                             ("com", 1): [_rowB, _rowB]}
        _PUSHES.pop(_push_key("cobs"), None)
        _cw = handle_line(b"43002500@Data=/Watch=0", member_id="cobs",
                          peer_nick=_ct_peer)
        _cwl = _cw if isinstance(_cw, list) else ([_cw] if _cw else [])
        _csnap = next((ln for ln in _cwl if b"@WatchInfo=" in ln), None)
        if _csnap is None or b"/N=2" not in _csnap or b"/T=2" not in _csnap:
            _say("FAIL: watching a COM table must answer a two-player sh=1 "
                 "@WatchInfo built from the COM key, got %r"
                 % (_csnap[:60] if _csnap else _cwl,))
            ok = False
        if _csnap is not None and b"/H=1|2" not in _csnap:
            _say("FAIL: the COM seat's hand count comes from ('com', seat) -- "
                 "expected /H=1|2, got %r" % (_csnap,))
            ok = False
        if _push_key("cobs") not in (_MATCH_WATCHERS.get(_ck) or {}):
            _say("FAIL: a COM table's watcher must be registered under the "
                 "COM MATCH KEY, or no COM push ever reaches them")
            ok = False
        # ...and the machine's own turns relay to that watcher.
        _PUSHES.pop(_push_key("cobs"), None)
        _watch_push(None, _push_key("ctcom"), b"43000902@TurnData=/A=1",
                    "COM relay probe")
        if not any(b"@TurnData=" in e[1]
                   for e in (_PUSHES.get(_push_key("cobs")) or [])):
            _say("FAIL: a COM turn must relay to the table's watchers")
            ok = False
        # ...and @GameExit= gives the table back.
        handle_line(b"41000D00@GameExit=", member_id="ctcom",
                    peer_nick=_ct_peer)
        if (_ct_chan, _ct_no) in _COM_AT or _match_running(_ct_chan, _ct_no):
            _say("FAIL: @GameExit= must release the COM table -- §15c, the "
                 "tile must not outlive the game")
            ok = False
        _COM_AT.clear()
        _COM_GAME.pop(_push_key("ctcom"), None)
        _MATCH_WATCHERS.pop(_ck, None)
        _PUSHES.pop(_push_key("cobs"), None)
        _PUSHES.pop(_push_key("ctcom"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND):
            _d.pop(_ck, None)

        # --- the player's OWN rules ride /R=, and the kill switch works -----
        #
        # `/Rule=` and `/R=` share their first seven slots (builder 0x106F87,
        # parser 0x102DE3 -- DC, E0..E4, D9), so the reply is the client's own
        # list verbatim. Serving `_rule_seven` there was the `@Tet=` order on
        # the wrong slots and overwrote every rule the player picked.
        _collection_store("tclmoff", {"cards": [], "money": 1000},
                          sync_save=False)
        _wsaved_com = os.environ.pop("POL_TM_WAGER_COM", None)
        _wecho = handle_line(b"43000600@ComGame=/Rule=250|7|6|5|4|3|2|1/Com=1",
                             member_id="tclmoff", peer_nick=b"UTESTPEER")
        _wecho = _wecho if isinstance(_wecho, bytes) else b"".join(_wecho or [])
        if b"/R=250|7|6|5|4|3|2" not in _wecho:
            _say("FAIL: @ComGameInit= must echo the player's own /Rule= in its "
                 "first seven positions (they are the same slots) -- got %r"
                 % (_wecho,)); ok = False
        _COM_GAME.pop(_push_key("tclmoff"), None)
        _PUSHES.pop(_push_key("tclmoff"), None)

        # ...and POL_TM_WAGER_COM=0 stops the stake moving at all.
        _collection_store("tclmoff", {"cards": [], "money": 1000},
                          sync_save=False)
        os.environ["POL_TM_WAGER_COM"] = "0"
        handle_line(b"43000600@ComGame=/Rule=250|0|1|1|1|0|0|0/Com=1",
                    member_id="tclmoff", peer_nick=b"UTESTPEER")
        if money_of("tclmoff") != 1000:
            _say("FAIL: POL_TM_WAGER_COM=0 must move NO gold, got %d"
                 % money_of("tclmoff")); ok = False
        _COM_GAME.pop(_push_key("tclmoff"), None)
        _PUSHES.pop(_push_key("tclmoff"), None)
        _collection_store("tclmoff", {"cards": [], "money": 0},
                          sync_save=False)

        # --- the wager money: the WAGER rides /R= occ 0 and settles server-side -
        # WARNING: RUN WITH THE KNOB ON. The mechanism is unchanged and still has to
        # work; only the default moved.
        os.environ["POL_TM_WAGER_COM"] = "1"
        _collection_store("tclm", {"cards": [], "money": 1000},
                          sync_save=False)
        _wg = handle_line(b"43000600@ComGame=/Rule=250|0|1|1|1|0|0|0/Com=1",
                          member_id="tclm", peer_nick=b"UTESTPEER")
        if not _wg or b"/R=250|" not in _wg:
            _say("FAIL: the client's /Rule= occ 0 (the wager) must ride /R= "
                 "occ 0 -- it is the POT at 0x52464BC, and /R=0 is why every "
                 "win paid 0; got %r" % (_wg,)); ok = False
        if money_of("tclm") != 750:
            _say("FAIL: serving a wager must stake it in (the client runs "
                 "wallet -= pot at 0xEC1CA), got %d" % money_of("tclm"))
            ok = False
        _MATCH_HANDS[_tkm] = {("deck", 0): [_rowA], ("deck", 1): [_rowA]}
        _MATCH_BOARD[_tkm] = {0: tmbattle.Card(_rowA, 0)}   # human wins
        _queue_next_turn(None, _push_key("tclm"), [], 10, 0,
                         roster=[("tclm", 0)], n_solo=2)
        if money_of("tclm") != 750 + 500:
            _say("FAIL: a decisive COM win pays min(2*wager, 2000) -- the "
                 "client doubles the pot at 0x10BC54 -- got %d"
                 % money_of("tclm")); ok = False
        if (_COM_GAME.get(_push_key("tclm")) or {}).get("staked") is not None:
            _say("FAIL: the settlement must clear `staked` (or @GameExit "
                 "double-refunds)"); ok = False
        # ...and the RANKING TALLIES advanced: the result wrote the VS.
        # Rating (average score x100 -- what menus 0/1/2 sort on) and the
        # win's net gil landed in the prize counters (menus 4/5).
        _rk = (_collection_load("tclm").get("rank")) or {}
        import tmrank as _trk
        if _rk.get("rating") != _trk.rating_of(_rk):
            _say("FAIL: a result must write rank.rating = tmrank.rating_of "
                 "(the ranking lists sort on it), got %r" % (_rk,))
            ok = False
        if (_rk.get("tiled_games") != _trk.tiled_games_of(dict(_rk, tiled_games=None))
                or not _rk.get("last_played")):
            _say("FAIL: a result must stamp tiled_games (the games tiles_total "
                 "covers -- rating_of's denominator) and last_played (the Top "
                 "30 activity rule), got %r" % (_rk,)); ok = False
        if _rk.get("prize_total") != 250 or _rk.get("prize_week") != 250:
            _say("FAIL: a wager win must add the net winnings to "
                 "prize_total AND prize_week (the Grand/Weekly Total "
                 "lists), got %r" % (_rk,)); ok = False
        if (_collection_load("tclm").get("staked_wager")) is not None:
            _say("FAIL: the settlement must clear the DURABLE stake record "
                 "too (staked_wager -- the restart-orphan ledger)"); ok = False
        # ...and the AVERAGE RANK moved, which is the stat every TITLE in the
        # game is gated on and which had no producer at all before 2026-08-24.
        # A decisive win is a 1st place, so one game must read exactly 1.00.
        if _rk.get("games") == 1 and _rk.get("avg_rank") != 100:
            _say("FAIL: winning the only game played is an average rank of "
                 "1.00 -- x100, so 100 -- got %r" % (_rk.get("avg_rank"),))
            ok = False
        if _rk.get("place_total") != 100 * (_rk.get("placed") or 0):
            _say("FAIL: place_total accumulates places ALREADY x100 (one win "
                 "== 100), so the average stays exact across restarts -- "
                 "got %r" % (_rk,)); ok = False
        # WARNING: AND THE DIVISOR IS `placed`, NOT `games`. A result that carries no
        # placement must move neither, or the average is deflated by it -- the
        # bug that put a floored 1.00 on a player who had never placed 1st.
        _rk_av = dict(_rk)
        _av_before = (_collection_load("tclm").get("rank") or {}).get("avg_rank")
        _bump_result_stats("tclm", place=None, score=0)
        _rk_after = (_collection_load("tclm").get("rank")) or {}
        if _rk_after.get("avg_rank") != _av_before:
            _say("FAIL: a result with NO placement must leave the average "
                 "alone -- it moved %r -> %r"
                 % (_av_before, _rk_after.get("avg_rank"))); ok = False
        if _rk_after.get("placed") != _rk_av.get("placed"):
            _say("FAIL: a result with NO placement must not advance `placed`, "
                 "got %r" % (_rk_after.get("placed"),)); ok = False
        # *** AND IT REACHED THE SAVE. *** The stats above live in the
        # collection JSON, which the CLIENT NEVER READS: Player Data -> Status
        # is drawn from `U/g/TM0DataFile`, and every one of these fields was
        # zero there for as long as this server has existed. This is the
        # delivery assertion, not another ledger one.
        try:
            with open(_save_file("tclm"), "rb") as _sf:
                _sb = _sf.read()
            # The DWORD fields, read as dwords. VS. Player Games is NOT among
            # them -- it is a word, and it is checked in the word block below.
            _got = {o: struct.unpack_from("<I", _sb, o)[0]
                    for o in (tmsave.RATING_OFF, tmsave.AVG_RANK_OFF,
                              tmsave.PRIZE_TOTAL_OFF,
                              tmsave.BIGGEST_PRIZE_OFF)}
            if struct.unpack_from("<H", _sb, tmsave.VS_GAMES_OFF)[0] \
                    != _rk.get("games"):
                _say("FAIL: the save's VS. Player Games (+0x%X) must carry the "
                     "career count -- got %r for %r games"
                     % (tmsave.VS_GAMES_OFF,
                        struct.unpack_from("<H", _sb, tmsave.VS_GAMES_OFF)[0],
                        _rk.get("games"))); ok = False
            if _got[tmsave.RATING_OFF] != _rk.get("rating"):
                _say("FAIL: the save's VS. Rating (+0x128) must carry the "
                     "rating the ranking lists sort on -- got %r vs %r"
                     % (_got[tmsave.RATING_OFF], _rk.get("rating")))
                ok = False
            if _got[tmsave.AVG_RANK_OFF] != _rk.get("avg_rank"):
                _say("FAIL: the save's Average Rank (+0x30) must carry the "
                     "computed average -- got %r vs %r"
                     % (_got[tmsave.AVG_RANK_OFF], _rk.get("avg_rank")))
                ok = False
            if _got[tmsave.PRIZE_TOTAL_OFF] != _rk.get("prize_total"):
                _say("FAIL: the save's Grand Total (+0x50) must carry the "
                     "prize-money tally -- got %r vs %r"
                     % (_got[tmsave.PRIZE_TOTAL_OFF], _rk.get("prize_total")))
                ok = False
            # ...and the WORD block, which is the half a dword write would
            # have corrupted. Read as words, because that is how the client
            # reads them.
            _w = {o: struct.unpack_from("<H", _sb, o)[0]
                  for o in (tmsave.OPPONENTS_OFF, tmsave.CONSEC_WINS_OFF,
                            tmsave.STREAK_OFF, tmsave.VS_GAMES_OFF)}
            if _w[tmsave.OPPONENTS_OFF] != _rk.get("opponents"):
                _say("FAIL: the save's opponents-faced (+0x40) must carry the "
                     "tally Average Prize divides by -- got %r vs %r"
                     % (_w[tmsave.OPPONENTS_OFF], _rk.get("opponents")))
                ok = False
            if _rk.get("opponents"):
                _ap = struct.unpack_from("<I", _sb,
                                         tmsave.AVERAGE_PRIZE_OFF)[0]
                _want_ap = _rk.get("prize_total", 0) // _rk["opponents"]
                if _ap != _want_ap:
                    _say("FAIL: Average Prize (+0x4C) is Grand Total over "
                         "OPPONENTS FACED (0xCC7E4), not over matches -- "
                         "got %r, want %r" % (_ap, _want_ap)); ok = False
            if _w[tmsave.CONSEC_WINS_OFF] != (_rk.get("consec_wins", 0)
                                              & 0xFFFF):
                _say("FAIL: Consecutive Wins (+0x78) must store SIGNED, two's "
                     "complement, as 0xCC68A writes it -- got %#x vs %r"
                     % (_w[tmsave.CONSEC_WINS_OFF], _rk.get("consec_wins")))
                ok = False
            # WARNING: THE ONE THAT CATCHES THE OLD BUG: a dword write at +0x78 would
            # have zeroed this. It must survive a save the streak also touched.
            if _w[tmsave.STREAK_OFF] != _rk.get("streak"):
                _say("FAIL: Winning Streak (+0x7A) must SURVIVE the write to "
                     "its neighbour +0x78 -- a dword write there destroys it "
                     "-- got %r vs %r"
                     % (_w[tmsave.STREAK_OFF], _rk.get("streak"))); ok = False
            if _got[tmsave.BIGGEST_PRIZE_OFF] != _rk.get("biggest_prize"):
                _say("FAIL: Biggest Prize (+0x48) is a running max over the "
                     "same amount Grand Total sums -- got %r vs %r"
                     % (_got[tmsave.BIGGEST_PRIZE_OFF],
                        _rk.get("biggest_prize"))); ok = False
        except OSError as _se:
            _say("FAIL: the result path must WRITE the save's stat block "
                 "(%r) -- the numbers exist only where the client cannot see "
                 "them otherwise" % (_se,)); ok = False
        # THE TIE RULE, measured off the client's own average-rank arithmetic at
        # 0xCC814: `mean((ring[i] + 2) * 50)`, whose scale makes the stored byte
        # `2 * (place - 1)` -- so the odd values are DRAWS and a two-player draw
        # is 1.50, not a shared 1st. FRACTIONAL ranking, x100.
        if _placements([3, 3]) != [150, 150]:
            _say("FAIL: a two-player draw is 1.50 -- the client's ring stores "
                 "the odd value for it (0xCC81E) -- got %r"
                 % (_placements([3, 3]),)); ok = False
        if _placements([5, 1]) != [100, 200]:
            _say("FAIL: a decisive two-player match is 1.00 / 2.00, got %r"
                 % (_placements([5, 1]),)); ok = False
        if _placements([1, 5, 3]) != [300, 100, 200]:
            _say("FAIL: places are 1-based, x100, ordered by score, got %r"
                 % (_placements([1, 5, 3]),)); ok = False
        if _placements([5, 5, 1]) != [150, 150, 300]:
            _say("FAIL: a shared 1st/2nd is 1.50 each and the loser is 3.00 "
                 "(FRACTIONAL ranking -- NOT the ranking lists' competition "
                 "rule), got %r" % (_placements([5, 5, 1]),)); ok = False
        # ...and the floor. The client's own arithmetic cannot go below 1.00,
        # so nothing here may either -- 0 is what an unwritten save reads as.
        if min(_placements([9, 9, 9])) < 100:
            _say("FAIL: 1.00 is the floor of this scale, got %r"
                 % (_placements([9, 9, 9]),)); ok = False
        # MOST COMBOS: the chain size one placement took. Counted by owner
        # DIFF, so it cannot drift from however the arms above apply flips.
        _cb = ("#TM0RCOMBO", 1)
        _MATCH_COMBO.pop(_cb, None)
        # The board AFTER: tiles 0 and 1 now belong to seat 0, and `before` says
        # they belonged to seat 1 -- i.e. this placement turned both.
        _bd = {0: tmbattle.Card(_rowA, 0), 1: tmbattle.Card(_rowA, 0),
               2: tmbattle.Card(_rowA, 0)}
        _note_combo(_cb[0], _cb[1], 0, _bd, {0: 1, 1: 1}, 2)
        if (_MATCH_COMBO.get(_cb) or {}).get(0) != 2:
            _say("FAIL: a placement that turned TWO enemy cards is a chain of "
                 "2 -- got %r" % ((_MATCH_COMBO.get(_cb) or {}).get(0),))
            ok = False
        # ...and it is a MAX, not a sum: a later, smaller chain must not lower it.
        _note_combo(_cb[0], _cb[1], 0, {0: tmbattle.Card(_rowA, 0)}, {0: 1}, 9)
        if (_MATCH_COMBO.get(_cb) or {}).get(0) != 2:
            _say("FAIL: Most Combos is a career/match MAX (0xD029B), so a "
                 "smaller later chain must not lower it -- got %r"
                 % ((_MATCH_COMBO.get(_cb) or {}).get(0),)); ok = False
        # ...and the PLACED card is never counted as part of its own chain.
        _MATCH_COMBO.pop(_cb, None)
        _note_combo(_cb[0], _cb[1], 0, {5: tmbattle.Card(_rowA, 0)}, {}, 5)
        if _MATCH_COMBO.get(_cb):
            _say("FAIL: a placement that took NOTHING is not a combo -- the "
                 "placed card must never count as its own chain, got %r"
                 % (_MATCH_COMBO.get(_cb),)); ok = False
        _MATCH_COMBO.pop(_cb, None)
        # THE RESULT BYTE, from the OTHER end: `_placements` derives the place
        # from the scores, `_result_code` turns it back into the byte the client
        # builds by summing 0/1/2 per opponent (0xCC5C7). The two must agree, or
        # one of the two derivations is wrong.
        for _sc, _want in (([5, 1], [0, 2]), ([3, 3], [1, 1]),
                           ([5, 5, 1], [1, 1, 4]), ([9, 9, 9], [2, 2, 2]),
                           ([1, 5, 3], [4, 0, 2])):
            _got = [_result_code(p) for p in _placements(_sc)]
            if _got != _want:
                _say("FAIL: scores %r must give result bytes %r (win 0 / draw 1 "
                     "/ loss 2 PER OPPONENT, 0xCC5C7) -- got %r"
                     % (_sc, _want, _got)); ok = False
        # THE STREAK MACHINE, arm by arm, against 0xCC643..0xCC69C.
        for _cur, _res, _want, _why in (
                (0, 0, 1, "a win from nothing starts a run"),
                (3, 0, 4, "a win extends a run"),
                (-2, 0, 1, "a win BREAKS a losing run and restarts at 1 "
                           "(0xCC668 `mov cx,1`), it does not resume at -1"),
                (5, 2, 0, "result 2 RESETS to zero (0xCC651), it does not "
                          "start a losing run -- in a 2-player match that is "
                          "the loss case"),
                (0, 4, -1, "a heavy loss from nothing goes to -1"),
                (4, 4, -1, "...and breaks a winning run to -1 (0xCC68A "
                           "`mov word, 0xFFFF`), not to 3"),
                (-2, 4, -3, "...and deepens an existing losing run")):
            _got = _streak_next(_cur, _res)
            if _got != _want:
                _say("FAIL: streak(%r, result=%r) must be %r -- %s -- got %r"
                     % (_cur, _res, _want, _why, _got)); ok = False
        # ...and an ABORT refunds an unsettled stake, once.
        handle_line(b"43000600@ComGame=/Rule=250|0|1|1|1|0|0|0/Com=1",
                    member_id="tclm", peer_nick=b"UTESTPEER")
        if (_collection_load("tclm").get("staked_wager")) != 250:
            _say("FAIL: a stake-in must persist staked_wager in the "
                 "collection (restarts ate two stakes on 2026-08-22)")
            ok = False
        _wab = money_of("tclm")                              # staked again
        handle_line(b"41000D00@GameExit=", member_id="tclm",
                    peer_nick=b"UTESTPEER")
        if money_of("tclm") != _wab + 250:
            _say("FAIL: @GameExit before a result must refund the stake "
                 "(mirror of 0xBC701), got %d" % money_of("tclm")); ok = False
        if (_collection_load("tclm").get("staked_wager")) is not None:
            _say("FAIL: the abort refund must clear staked_wager"); ok = False
        # ...and a stake only the COLLECTION remembers (the restart case) is
        # refunded when the next game begins.
        _COM_GAME.pop(_push_key("tclm"), None)
        _cd = _collection_load("tclm")
        _cd["staked_wager"] = 77
        _collection_store("tclm", _cd, sync_save=False)
        _worph = money_of("tclm")
        handle_line(b"41001200@GameENC=/NN=0", member_id="tclm",
                    peer_nick=b"UTESTPEER")
        if money_of("tclm") != _worph + 77:
            _say("FAIL: a new @GameENC= must refund an ORPHANED stake (a "
                 "restart wiped the match that held it), got %d vs %d+77"
                 % (money_of("tclm"), _worph)); ok = False
        if (_collection_load("tclm").get("staked_wager")) is not None:
            _say("FAIL: the orphan refund must clear staked_wager"); ok = False
        _PUSHES.pop(_push_key("tclm"), None)
        _PUSHES.pop(_push_key("tclm"), None)
        _MATCH_OBJECTS.clear()
        for _d in (_MATCH_HANDS, _MATCH_BOARD, _MATCH_TURN, _TURN_RAND,
                   _MATCH_CONTINUE, _CARD_READY):
            _d.pop(_tkm, None)
        _COM_GAME.pop(_push_key("tclm"), None)
        for _pth in (_collection_file("tclm"), _save_resource_file("tclm")):
            try:
                if _pth and os.path.exists(_pth):
                    os.remove(_pth)
            except OSError:
                pass

        # ...and put the COM-wager knob back the way it was found.
        if _wsaved_com is None:
            os.environ.pop("POL_TM_WAGER_COM", None)
        else:
            os.environ["POL_TM_WAGER_COM"] = _wsaved_com

        # --- the ring-flood gate: no reservation re-feed into a running match,
        # --- and @GameExit= releases the gate ---------------------------------
        # Measured 16:16Z: one re-feed per @Pong filled the client's global
        # receive ring mid-COM-game (store_count=24) and the COM's @PutCard
        # was dropped on arrival -- "the com seems to have stopped".
        _MATCH_TURN[(None, _push_key("comtest"))] = {"turn": 1, "active": 1,
                                                     "n": 2}
        if _reservation_reply("comtest") is not None:
            _say("FAIL: the @Pong reservation re-feed must be SUPPRESSED while "
                 "a VS. COM match is running -- each one eats a ring slot the "
                 "match scene never frees"); ok = False
        handle_line(b"41000D00@GameExit=", member_id="comtest",
                    peer_nick=b"UTESTPEER")
        if (None, _push_key("comtest")) in _MATCH_TURN:
            _say("FAIL: @GameExit= must clear the COM match key, or the "
                 "re-feed gate stays closed at the table for ever (+0x108 "
                 "reborn)"); ok = False
        _PUSHES.pop(_push_key("comtest"), None)
        for _k in ((None, None), _ckey):
            _CARD_READY.pop(_k, None)
            _MATCH_HANDS.pop(_k, None)
            _MATCH_TURN.pop(_k, None)
            _TURN_RAND.pop(_k, None)
            _MATCH_BOARD.pop(_k, None)
            _MATCH_OBJECTS.pop(_k, None)
            _MATCH_CONTINUE.pop(_k, None)
        # --- the PS2 VS. COM entry: a bare `@Req=` must seed the board -------
        # `@GameENC=` does not exist in TMaster.pex, so `@Req=` is the console's
        # only trigger for `@ComGameInit`. Without this the client polls
        # (0x43, 2) and dies with -1-37338. The gate is MATCH CONTEXT: with a
        # COM game already set up (what the PC's `@GameENC=` does) it must NOT
        # fire again.
        _rq_key = _push_key(987654321)
        _COM_GAME.pop(_rq_key, None)
        _MATCH_ROSTER.pop(_rq_key, None)
        _PUSHES.pop(_rq_key, None)
        _rq = handle_line(b"43000101@Req=/NN=0000000000000000/HID=0/Dm=0/Vol=0"
                          b"/CN=/HN=", member_id=987654321, peer_nick=b"UTESTPEER")
        if not _rq or b"@PLAYACK=/EN=1" not in _rq:
            _say("FAIL: @Req= must still answer @PLAYACK=/EN=1, got %r" % (_rq,))
            ok = False
        _rq_q = [e[1] for e in (_PUSHES.get(_rq_key) or [])
                 if b"@ComGameInit=" in e[1]]
        if not _rq_q:
            _say("FAIL: a bare @Req= must queue @ComGameInit -- the PS2 has no "
                 "@GameENC= and polls (0x43, 2) until -1-37338. Queue was %r"
                 % ([e[1] for e in (_PUSHES.get(_rq_key) or [])],)); ok = False
        elif not _rq_q[0].startswith(encode_code(COMGAME_MSGID)):
            _say("FAIL: the @Req=-triggered @ComGameInit must be addressed "
                 "(0x43, 2); got %r" % (_rq_q[0][:8],)); ok = False
        # ...and the leave ack must be pre-loaded into (0x43, 5). The console
        # never SENDS @Quit (one xref in TMaster.pex, the receive arm), so
        # without this the leave times out with -1-37339.
        _rq_v = [e[1] for e in (_PUSHES.get(_rq_key) or []) if b"@Quit=" in e[1]]
        if not _rq_v:
            _say("FAIL: VS. COM entry must also pre-load @Quit -- CQMComConfig "
                 "state 4 polls (0x43, 5) on leave and raises 339 on timeout. "
                 "Queue was %r" % ([e[1] for e in (_PUSHES.get(_rq_key) or [])],))
            ok = False
        elif not _rq_v[0].startswith(encode_code(COMQUIT_MSGID)):
            _say("FAIL: the leave ack must be addressed (0x43, 5); got %r"
                 % (_rq_v[0][:8],)); ok = False
        elif b"/No=" not in _rq_v[0] or b"/B=" not in _rq_v[0]:
            _say("FAIL: arm 0x103039 reads /No= and /B=; got %r" % (_rq_v[0],))
            ok = False
        # ...and the part ack into (0x41, 0x0E): CComPart state 1 polls it while
        # leaving and parks at selector 182 without it, so 0x03001f57 is never
        # posted and the parent's progress bar stays clamped at 80.
        _rq_e = [e[1] for e in (_PUSHES.get(_rq_key) or []) if b"@GameEA=" in e[1]]
        if not _rq_e:
            _say("FAIL: VS. COM entry must also pre-load @GameEA -- CComPart "
                 "state 1 polls (0x41, 0x0E). Queue was %r"
                 % ([e[1] for e in (_PUSHES.get(_rq_key) or [])],)); ok = False
        elif not _rq_e[0].startswith(encode_code(GAMEEXIT_ANS_MSGID)):
            _say("FAIL: the part ack must be addressed (0x41, 0x0E); got %r"
                 % (_rq_e[0][:8],)); ok = False
        elif b"/Exit=" not in _rq_e[0]:
            _say("FAIL: 0x003b0cdc reads /Exit= (0x00486C80), not /EN=; got %r"
                 % (_rq_e[0],)); ok = False
        # ...and it must NOT fire a second time now a COM game exists.
        _PUSHES.pop(_rq_key, None)
        handle_line(b"43000101@Req=/NN=0/HID=0/Dm=0/Vol=0/CN=/HN=",
                    member_id=987654321, peer_nick=b"UTESTPEER")
        if any(b"@ComGameInit=" in e[1] for e in (_PUSHES.get(_rq_key) or [])):
            _say("FAIL: @Req= must not re-queue @ComGameInit once a COM game is "
                 "set up -- that is the PC path, where @GameENC= already did it")
            ok = False
        _PUSHES.pop(_rq_key, None)
        _COM_GAME.pop(_rq_key, None)
        _MATCH_PEER.pop(_rq_key, None)

        _COM_GAME.clear(); _COM_GAME.update(_svcom)
        _MATCH_PEER.clear(); _MATCH_PEER.update(_svpeer)
        # comtest staked a wager in the roster test and the @GameExit= above
        # refunded it -- both wrote a collection file for a synthetic member;
        # remove it AFTER the last handle_line that can re-create it.
        for _pth in (_collection_file("comtest"),
                     _save_resource_file("comtest")):
            try:
                if _pth and os.path.exists(_pth):
                    os.remove(_pth)
            except OSError:
                pass

        # --- @Quit= answers in the vocabulary of the code it arrived on -------
        # The live 10:58:54Z bug: a MATCH quit was answered with the CARD SHOP's
        # `@EQuit`, which arm 0x103039 cannot name-match.
        mq = handle_line(b"43000508@Quit=/No=0", member_id=None)
        if not mq or not mq.startswith(b"43000508"):
            _say("FAIL: a match @Quit= must be answered in its own slot, got %r"
                 % (mq,)); ok = False
        if not mq or b"@Quit=" not in mq or b"@EQuit=" in mq:
            _say("FAIL: code 0x43's quit answer is `@Quit` (0x103039); `@EQuit` "
                 "is 0xB2's and cannot name-match here. Got %r" % (mq,))
            ok = False
        if not mq or b"/No=" not in mq or b"/B=" not in mq:
            _say("FAIL: 0x103039 reads /No= and /B=, got %r" % (mq,)); ok = False
        # ...and the card shop's is a BUILD SPLIT, so pin BOTH sides of the
        # knob. `POL_TM_SHOPQUIT_PS2` defaults to 1 (the console vocabulary,
        # `@Quit=/N=/D=`); 0 restores the PC's `@EQuit=`. Asserting only the PC
        # shape left this selftest RED from 9e3f4ba3 until 2026-09-08, which is
        # exactly as good as having no gate at all.
        os.environ["POL_TM_SHOPQUIT_PS2"] = "0"
        sq = handle_line(b"B2001701@Quit=/No=1", member_id=None)
        del os.environ["POL_TM_SHOPQUIT_PS2"]
        if not sq or b"@EQuit=" not in sq:
            _say("FAIL: with POL_TM_SHOPQUIT_PS2=0 the SHOP quit must answer "
                 "@EQuit=, got %r" % (sq,))
            ok = False
        sq2 = handle_line(b"B2001701@Quit=/No=1", member_id=None)
        if not sq2 or b"@Quit=/N=" not in sq2 or b"@EQuit=" in sq2:
            _say("FAIL: the DEFAULT shop quit is the PS2's @Quit=/N=/D= -- "
                 "@EQuit is absent from TMaster.pex and hangs the shop. "
                 "Got %r" % (sq2,))
            ok = False

        # --- the alone guard, and it must be OURS, not the client's -----------
        mid = 0x860FB3E2A2
        tmroom.note_member(mid, ["0", "0", "0", "0", "T", "2", "0", "0", "0",
                                 "0x0000002000000001", "T"])
        _SEATED["#TM0R001"] = {1: [(mid, 1)]}
        _PUSHES.clear()
        _queue_vsgameinit(mid)
        if _PUSHES:
            _say("FAIL: a one-seat table must not be sent @VsGameInit -- /N=1 "
                 "hits 0x1024D5 and prints StartAloneERROR"); ok = False
        _SEATED["#TM0R001"] = {1: [(mid, 1), (mid + 1, 2)]}
        _remember_match("#TM0R001", 1, [(mid, 1), (mid + 1, 2)])
        # WARNING: ONE ACCEPT IS NOT A GAME. Measured 2026-08-20T10:55Z: the guest
        # accepted first, was handed the board 2 s later while the host had not
        # accepted, and never opened it -- see `_MATCH_ACCEPTS`.
        _queue_vsgameinit(mid)
        if _PUSHES:
            _say("FAIL: the board must NOT go out on the first accept -- it "
                 "lands in a scene that does not exist yet. Got %r" % (_PUSHES,))
            ok = False
        # ...and the last accept starts it for EVERYONE, each from their own seat.
        _queue_vsgameinit(mid + 1)
        for who, seat in ((mid, 0), (mid + 1, 1)):
            queued = [e[1] for e in _PUSHES.get(who, [])]
            body = next((b for b in queued if b.startswith(b"43000100")), None)
            if body is None:
                _say("FAIL: the last accept must hand the board to every seat; "
                     "member %s got %r" % (who, queued)); ok = False
                continue
            ids = _pipe_vals(body, b"/ID=")
            if ids[0] != b"%d" % _env_int("POL_TM_VSGAME_SELFID", 0):
                _say("FAIL: member %s (table seat %d) must still lead its OWN "
                     "copy -- got %r" % (who, seat, ids)); ok = False
        # WARNING: THE LIVE FAILURE OF 2026-08-20T10:43Z, AS A TEST. Entering a match
        # PARTs the room, so `release_seats` empties `_SEATED` BEFORE `@GameOK=`
        # arrives and both players got "seated at no table". The announced-match
        # snapshot is what has to carry it; assert against the state as it
        # actually is at that moment -- seats gone, room gone.
        _remember_match("#TM0R001", 1, [(mid, 1), (mid + 1, 2)])
        _SEATED.clear()
        _PUSHES.clear()
        tmroom.forget_member(mid)
        _queue_vsgameinit(mid)
        _queue_vsgameinit(mid + 1)
        after_part = [e[1] for e in _PUSHES.get(mid, [])]
        if not any(b.startswith(b"43000100") for b in after_part):
            _say("FAIL: @VsGameInit must survive the PART that entering a match "
                 "performs -- _SEATED is empty and room_of() is None by the time "
                 "@GameOK= lands. Got %r" % (after_part,)); ok = False
        # And it must still be built from THIS member's point of view.
        body = next((b for b in after_part if b.startswith(b"43000100")), b"")
        if b"/N=2" not in body:
            _say("FAIL: the remembered roster must keep both seats, got %r"
                 % (body,)); ok = False
        # WARNING: THE HOLD-BACK ASSERTION USED TO LIVE HERE AND IS NOW WRONG ON
        # PURPOSE. "Do not arrive before the scene exists" is satisfied by
        # waiting for the last accept, and a reply of extra delay measurably
        # cost 19 s between the two boards (2026-08-20T11:04Z). What has to hold
        # instead is that the board never goes out on anything BUT the last
        # accept -- asserted above -- and that every seat is queued together.
        for _who in (mid, mid + 1):
            if not _PUSHES.get(_who):
                _say("FAIL: both seats must be queued at the same moment, "
                     "member %s has nothing" % (_who,)); ok = False

        # --- a push must ride a reply on the RIGHT BAND -------------------
        # WARNING: Measured 2026-08-20: the SAME board to the SAME member was read
        # when it rode a reply to the TABLE peer (11:22:40, UE7QN1N9G) and
        # invisible when it rode one to the ROOM peer (11:30:54, UKXDBA266) --
        # 130,000 further trace lines with no `---->Recv=` at all. A correct
        # message on the wrong peer is as unread as no message.
        _PUSHES.clear()
        _queue_push(mid, b"43000100@Board", "band test", after=0,
                    peer=b"UE7QN1N9G")
        if _pending_pushes(mid, b"UKXDBA266"):
            _say("FAIL: a board pinned to the table peer must NOT ride a reply "
                 "on the room peer"); ok = False
        if _pending_pushes(mid, b"UE7QN1N9G") != [b"43000100@Board"]:
            _say("FAIL: it MUST ride a reply on its own peer"); ok = False
        # ...and it must not be lost by the miss: it is still queued.
        _PUSHES.clear()
        _queue_push(mid, b"43000100@Board", "band test", after=0,
                    peer=b"UE7QN1N9G")
        _pending_pushes(mid, b"UKXDBA266")
        if not _PUSHES.get(_push_key(mid)):
            _say("FAIL: a wrong-band reply must LEAVE the push queued, not "
                 "drop it"); ok = False
        # An unpinned push still rides anything, which is every other caller.
        _PUSHES.clear()
        _queue_push(mid, b"13000000@Info", "unpinned", after=0)
        if _pending_pushes(mid, b"ANYPEER") != [b"13000000@Info"]:
            _say("FAIL: an unpinned push must ride any reply"); ok = False
        _PUSHES.clear()
        # --- requeue: a pop is not a delivery (09-02 review H3) ------------
        # The consumer hands back what it could not send; the entries must
        # come out again on the next drain, in order, without duplicating.
        _queue_push(mid, b"43000800@StartData=/S=0", "requeue test", after=0)
        _queue_push(mid, b"43000900@TurnData=/A=0", "requeue test", after=0)
        _rq = idle_pushes(mid, None)
        if len(_rq) != 2:
            _say("FAIL: requeue test setup expected 2 due, got %r" % (_rq,))
            ok = False
        requeue_pushes(mid, _rq[1:], why="selftest simulated send failure")
        _rq2 = idle_pushes(mid, None)
        if [b for _p, b in _rq2] != [b"43000900@TurnData=/A=0"]:
            _say("FAIL: an undelivered push must come back out on the next "
                 "drain; got %r" % (_rq2,)); ok = False
        if idle_pushes(mid, None):
            _say("FAIL: a requeued push must not duplicate"); ok = False
        # And with POL_TM_PUSH_REQUEUE=0 the old drop-on-failure returns.
        os.environ["POL_TM_PUSH_REQUEUE"] = "0"
        requeue_pushes(mid, _rq, why="disabled")
        if idle_pushes(mid, None):
            _say("FAIL: POL_TM_PUSH_REQUEUE=0 must restore drop-on-failure")
            ok = False
        os.environ.pop("POL_TM_PUSH_REQUEUE", None)
        _PUSHES.clear()

        # --- @StartData: the deal, and /S= must be per recipient -----------
        # Measured 2026-08-20T13:38Z: with /Ok=1 the scene reached state 0x19
        # and began polling 7 then 8 (0xC28C8). Command 8 is @StartData.
        _seatsD = [(mid, 1), (mid + 1, 2)]
        _sd0 = _startdata_body(_seatsD, 0, 0)
        _sd1 = _startdata_body(_seatsD, 1, 0)
        if _pipe_vals(_sd0, b"/F=") != [b"0"] * 16:
            _say("FAIL: @StartData carries one /F= per tile, 16 for two players "
                 "(0x23359E[N-2]); got %r" % (_pipe_vals(_sd0, b"/F="),))
            ok = False
        if len(_pipe_vals(_sd0, b"/L=")) != 2:
            _say("FAIL: /L= is per player, got %r" % (_pipe_vals(_sd0, b"/L="),))
            ok = False
        # WARNING: /S= is run through the seat rotation at 0x10354E, and our copies are
        # recipient-first -- so the SAME starter must be a different number in
        # each copy, or both clients believe they lead.
        if _pipe_vals(_sd0, b"/S=") != [b"0"] or _pipe_vals(_sd1, b"/S=") != [b"1"]:
            _say("FAIL: with table seat 0 starting, its own copy must say /S=0 "
                 "and the other /S=1 -- got %r / %r"
                 % (_pipe_vals(_sd0, b"/S="), _pipe_vals(_sd1, b"/S=")))
            ok = False

        # --- THE BOARD OBJECTS ---------------------------------------------
        # VERIFIED: 2026-09-06. `/F=`, `/B=` and `/E=` are the board: blocks, chance
        # blocks, rotating blocks and special tiles, all three rules at once.
        # See `tmbattle.roll_board` for where every number comes from.
        _bk = ("#TM0OBJ", 77)
        # The suite pins the board OFF (see `selftest`); this is the one test
        # that is about it, so it owns the knob for its own duration.
        os.environ["POL_TM_BOARD_OBJECTS"] = "1"
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS.pop(_bk, None)
        _TABLE_SETTINGS[_bk] = {"st": 1, "cb": 1, "ca": 1}
        _codesB = _match_objects(_bk[0], _bk[1], 2, fresh=True)
        if len(_codesB) != 16:
            _say("FAIL: a two-player board is 16 tiles (0x23359E), got %d"
                 % len(_codesB)); ok = False
        _sdB = _startdata_body(_seatsD, 0, 0, codes=_codesB)
        if [int(v) for v in _pipe_vals(_sdB, b"/F=")] != list(_codesB):
            _say("FAIL: @StartData /F= is the object-code array VERBATIM -- "
                 "0x10360F stores the byte with no mask; got %r for %r"
                 % (_pipe_vals(_sdB, b"/F="), _codesB)); ok = False
        _bB = int((_pipe_vals(_sdB, b"/B=") or [b"-1"])[0])
        _eB = int((_pipe_vals(_sdB, b"/E=") or [b"-1"])[0])
        if (_bB, _eB) != tmbattle.board_counts(_codesB):
            _say("FAIL: /B= and /E= are DERIVED from /F=, got (%d, %d) for %r"
                 % (_bB, _eB, _codesB)); ok = False
        if _bB + _eB != sum(1 for c in _codesB if c):
            # WARNING: THE CRASH GUARD. The client's reveal loop divides by the
            # number of tiles still carrying a code; a count that outruns the
            # array reaches `idiv ecx` with ecx = 0 and kills the client.
            _say("FAIL: /B= + /E= must account for EVERY non-empty tile or the "
                 "client's reveal loop divides by zero -- %d + %d vs %r"
                 % (_bB, _eB, _codesB)); ok = False
        # A BLOCK OCCUPIES ITS TILE; A SPECIAL TILE DOES NOT. That asymmetry is
        # the client's (0xC7015/0xC703C plays a card ON a special tile), and it
        # is what keeps the COM's `t not in board` pick honest.
        _boardB = _MATCH_BOARD.get(_bk) or {}
        for _t, _c in enumerate(_codesB):
            _kind = tmbattle.object_kind(_c)
            if _kind in ("plain", "chance", "rotating") and _t not in _boardB:
                _say("FAIL: a %s block must occupy tile %d" % (_kind, _t))
                ok = False
                break
            if _kind in (None, "special") and _t in _boardB:
                _say("FAIL: tile %d is %r and must stay PLAYABLE -- a special "
                     "tile is a property of a free tile, not an occupant"
                     % (_t, _kind))
                ok = False
                break
        if _board_scores(_bk[0], _bk[1], 2) != [0, 0]:
            _say("FAIL: a block belongs to nobody (owner 4) and must score for "
                 "nobody -- got %r" % (_board_scores(_bk[0], _bk[1], 2),))
            ok = False
        # ...and with every rule OFF the same table deals plain blocks only.
        _TABLE_SETTINGS[_bk] = {"st": 0, "cb": 0, "ca": 0}
        _plainB = set()
        for _ in range(60):
            _plainB |= {c for c in _match_objects(_bk[0], _bk[1], 2, fresh=True)
                        if c}
        if not _plainB <= set(tmbattle.PLAIN_CODES):
            _say("FAIL: st=cb=ca=0 must deal plain blocks only (0xC0F40 / "
                 "0xC0F72 / 0xC1051 each gate their own family) -- got %r"
                 % (sorted(_plainB),)); ok = False
        # WARNING: POL_TM_START_F AND `_MATCH_BOARD` MUST BE THE SAME BOARD. This is
        # the regression test for 2026-09-07T00:16Z, the first live game with
        # objects on: the knob was read in `_startdata_body` only, so `/F=` went
        # out as the explicitly authored board while `_match_objects` rolled a
        # DIFFERENT one and seeded the server's model from it. Blocks on tiles
        # 1/4/6/9/11/14 on screen, 4/8/9/11/13 in the server -- and every
        # consumer downstream answering about a board nobody could see.
        os.environ["POL_TM_START_F"] = "0|0|6|0|8|0|9|3"
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS.pop(_bk, None)
        _codesF = _match_objects(_bk[0], _bk[1], 2, fresh=True)
        _sdF = _startdata_body(_seatsD, 0, 0, codes=_codesF)
        os.environ.pop("POL_TM_START_F", None)
        _wantF = [0, 0, 6, 0, 8, 0, 9, 3] + [0] * 8
        if [int(v) for v in _pipe_vals(_sdF, b"/F=")] != _wantF:
            _say("FAIL: POL_TM_START_F must serve the board it names, padded "
                 "to the tile count -- got %r" % (_pipe_vals(_sdF, b"/F="),))
            ok = False
        if list(_codesF) != _wantF:
            _say("FAIL: the FORCED board must also be the match's own board, "
                 "got %r" % (_codesF,))
            ok = False
        # ...and the seeded board must agree with the wire, tile for tile.
        _bdF = _MATCH_BOARD.get(_bk) or {}
        for _t, _cF in enumerate(_wantF):
            _occ = tmbattle.object_card(_cF) is not None
            if _occ != (_t in _bdF):
                _say("FAIL: THE DIVERGENCE. Tile %d is code %d (%s) on the wire "
                     "but %s in `_MATCH_BOARD` -- the server would be playing a "
                     "different board than the client can see"
                     % (_t, _cF, tmbattle.object_kind(_cF) or "empty",
                        "occupied" if _t in _bdF else "free"))
                ok = False
                break
        if _tile_ability(_bk[0], _bk[1], 7) != 1:
            _say("FAIL: the forced board's special tile (code 3 -> ability 1) "
                 "must reach `_tile_ability` too")
            ok = False
        if (int((_pipe_vals(_sdF, b"/B=") or [b"-1"])[0]),
                int((_pipe_vals(_sdF, b"/E=") or [b"-1"])[0])) != (3, 1):
            _say("FAIL: an explicit board's counts are still derived -- three "
                 "blocks (6, 8, 9) and one special (3)")
            ok = False

        # WARNING: AND AN OVERRIDE MUST NOT SMUGGLE PAST THE DEAL A CODE THE DEAL
        # ITSELF IS NOT ALLOWED TO PRODUCE. Regression for 2026-09-07T04:20Z:
        # `roll_board` had already stopped dealing 17, but prod still pinned
        # one on tile 3 through this very knob, so the withdrawal protected
        # nobody and the match hung on the first battle against it. 17 is
        # modelled again, so the knob under test here is `POL_TM_SCRAMBLE=0`
        # -- the switch that withdraws it -- and the assertion is that BOTH
        # halves move together.
        os.environ["POL_TM_SCRAMBLE"] = "0"
        os.environ["POL_TM_START_F"] = "0|17|0|6|0|0|0|0"
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS.pop(_bk, None)
        _codesU = _match_objects(_bk[0], _bk[1], 2, fresh=True)
        if list(_codesU)[:4] != [0, 0, 0, 6]:
            _say("FAIL: with POL_TM_SCRAMBLE=0 a forced board must blank every "
                 "code the deal may not produce and keep the rest -- got %r"
                 % (list(_codesU)[:4],))
            ok = False
        if 1 in (_MATCH_BOARD.get(_bk) or {}):
            _say("FAIL: a blanked tile must be FREE in `_MATCH_BOARD` too, not "
                 "seeded from the code the environment asked for")
            ok = False
        if 17 in _dealt_chance_codes():
            _say("FAIL: POL_TM_SCRAMBLE=0 must take 17 out of the deal ladder "
                 "too -- got %r" % (_dealt_chance_codes(),))
            ok = False
        # ...and with the knob at its default the SAME board keeps its 17,
        # because the code is modelled. A blanking that cannot be turned off
        # is a withdrawal nobody can undo without a deploy.
        os.environ.pop("POL_TM_SCRAMBLE", None)
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS.pop(_bk, None)
        _codesU2 = _match_objects(_bk[0], _bk[1], 2, fresh=True)
        os.environ.pop("POL_TM_START_F", None)
        if list(_codesU2)[:4] != [0, 17, 0, 6]:
            _say("FAIL: 17 is modelled, so a forced board keeps it -- got %r"
                 % (list(_codesU2)[:4],))
            ok = False
        if 17 not in _dealt_chance_codes():
            _say("FAIL: the default deal ladder is the client's own "
                 "(6/7/8/17 at 0xC106C) -- got %r" % (_dealt_chance_codes(),))
            ok = False

        # ...and POL_TM_BOARD_OBJECTS=0 deals the pre-2026-09-06 empty board.
        # That also restores the suite-wide pin for everything after this.
        os.environ["POL_TM_BOARD_OBJECTS"] = "0"
        if any(_match_objects(_bk[0], _bk[1], 2, fresh=True)):
            _say("FAIL: POL_TM_BOARD_OBJECTS=0 must deal an empty board")
            ok = False

        # --- A SPECIAL TILE ACTUALLY DOES SOMETHING ------------------------
        # The card played on tile 5 must inherit ability 2 (0xC7015 saves
        # [tile.card+0x24], 0xB3200 zeroes it, 0xC703C puts it back), and the
        # chance block on tile 6 must be FOUGHT -- the neutral arm at 0xD386B,
        # which has no reverse-arrow test at all.
        os.environ["POL_TM_BOARD_OBJECTS"] = "1"
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][5] = 4                     # special tile, ability 2
        _MATCH_OBJECTS[_bk][6] = 6                     # a chance block next door
        _MATCH_BOARD[_bk] = {6: tmbattle.object_card(6)}
        if _tile_ability(_bk[0], _bk[1], 5) != 2:
            _say("FAIL: special tile code 4 carries ability 2 (0xB303C)")
            ok = False
        _fightsB, _ = _apply_placement(_bk[0], _bk[1], 2, 0, 5,
                                       b"9|40|0|10|10|0|4|0")   # one EAST arrow
        _placedB = (_MATCH_BOARD.get(_bk) or {}).get(5)
        if _placedB is None or _placedB.ability != 2:
            _say("FAIL: a card played onto a special tile INHERITS its ability "
                 "-- got %r" % (_placedB and _placedB.ability,))
            ok = False

        # KEY: ...AND ABILITY 1 IS SPENT ON THE BATTLE IT POWERED, while 2 and 3
        # are not. 0xD30EE reads the ability of the tile just played on
        # (`[obj+0x179]`) at -BattleEnd and zeroes it only when it is EXACTLY 1.
        # The three special tiles are not symmetric and this is the asymmetry.
        for _ab, _code, _want in ((1, 3, 0), (2, 4, 2), (3, 5, 3)):
            _MATCH_BOARD.pop(_bk, None)
            _MATCH_OBJECTS[_bk] = [0] * 16
            _MATCH_OBJECTS[_bk][5] = _code
            # A weak defender on tile 6 so tile 5 has something to fight.
            _MATCH_BOARD[_bk] = {6: tmbattle.Card(b"9|1|0|1|1|0|255|0", 1)}
            _bA, _ = _apply_placement(_bk[0], _bk[1], 2, 0, 5,
                                      b"9|60|0|60|60|0|4|0")   # arrows 4 = E
            if not _bA:
                _say("FAIL: the ability-burn probe needs a battle to burn on "
                     "(code %d fought nothing)" % _code)
                ok = False
                break
            _after = (_MATCH_BOARD.get(_bk) or {}).get(5)
            if _after is None or int(_after.ability) != _want:
                _say("FAIL: special tile code %d (ability %d) must read %d "
                     "after its first battle -- 0xD30EE burns 1 and leaves 2 "
                     "and 3 alone; got %r"
                     % (_code, _ab, _want, _after and _after.ability))
                ok = False
                break
        # ...and a placement that never fights keeps even an ability 1, because
        # a flip-only move does not reach -BattleEnd at all.
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][5] = 3                     # ability 1
        _MATCH_BOARD[_bk] = {}
        _bN, _ = _apply_placement(_bk[0], _bk[1], 2, 0, 5,
                                  b"9|60|0|60|60|0|4|0")
        _keptN = (_MATCH_BOARD.get(_bk) or {}).get(5)
        if _bN or _keptN is None or int(_keptN.ability) != 1:
            _say("FAIL: with nothing to fight, the attack boost is NOT spent "
                 "-- got battles=%r ability=%r"
                 % (_bN, _keptN and _keptN.ability))
            ok = False
        if not _fightsB or _fightsB[0][1] != 6:
            _say("FAIL: an EAST arrow into a chance block must start a battle "
                 "on tile 6 -- 0xD386B needs no reverse arrow, only a non-zero "
                 "mask; got %r" % ([(b[0], b[1], b[3]) for b in _fightsB],))
            ok = False
        # A PLAIN block, by contrast, has no arrows at all and is inert.
        _MATCH_BOARD[_bk] = {6: tmbattle.object_card(1)}
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][6] = 1
        _fightsP, _marksP = _apply_placement(_bk[0], _bk[1], 2, 0, 5,
                                             b"9|40|0|10|10|0|4|0")
        if _fightsP or _marksP:
            _say("FAIL: a PLAIN block is a dead tile -- 0xB30DF zeroes its "
                 "arrow mask and 0xD387E drops it; got %r / %r"
                 % (_fightsP, _marksP)); ok = False
        # ...and neither block ever reaches the take list as a card.
        if any(_board_takes(_bk[0], _bk[1], 2)):
            _say("FAIL: a block is a tile, not a card -- it must never feed "
                 "the lucky-card take list a pseudo id")
            ok = False

        # --- A WHOLE MATCH WITH A BLOCKED BOARD ----------------------------
        # WARNING: THE INVARIANT THE SHIPPED TABLES IMPLY: 16 tiles - 6 blocks = 10 =
        # two players x five cards, and 6 is the MOST LIKELY block count
        # (50%). So a full board must still take every card, and the blocks
        # must score for nobody. This is the interaction the rest of the suite
        # can no longer see, because the legacy scripted flows are pinned off.
        for _codesW, _label in (
                ([1, 0, 0, 0, 6, 0, 0, 2, 0, 9, 0, 0, 17, 0, 0, 1],
                 "six blocks (the modal roll)"),
                ([1, 3, 0, 0, 6, 4, 0, 2, 0, 9, 5, 0, 17, 0, 0, 1],
                 "six blocks and three special tiles")):
            _MATCH_BOARD.pop(_bk, None)
            _MATCH_OBJECTS[_bk] = list(_codesW)
            _bd = _MATCH_BOARD.setdefault(_bk, {})
            for _t, _c in enumerate(_codesW):
                _oc = tmbattle.object_card(_c)
                if _oc is not None:
                    _bd[_t] = _oc
            _freeW = [_t for _t in range(16) if _t not in _bd]
            if len(_freeW) != 10:
                _say("FAIL: %s must leave exactly ten playable tiles (two "
                     "hands of five), got %d" % (_label, len(_freeW)))
                ok = False
            # WARNING: THE FREE LIST IS RECOMPUTED EVERY PLACEMENT. It used to be
            # taken once up front, which was fine until code 17 (scramble)
            # started MOVING cards mid-match -- a precomputed tile could be
            # occupied by the time its turn came, the occupied-tile guard
            # refused it, and the test failed on its own stale assumption.
            for _i in range(len(_freeW)):
                _bdN = _MATCH_BOARD.get(_bk) or {}
                _t = next((x for x in range(16) if x not in _bdN), None)
                if _t is None:
                    _say("FAIL: %s -- ran out of board on card %d"
                         % (_label, _i))
                    ok = False
                    break
                _fW, _mW = _apply_placement(
                    _bk[0], _bk[1], 2, _i % 2, _t, b"9|30|0|30|30|0|255|0")
                if not (_MATCH_BOARD.get(_bk) or {}):
                    _say("FAIL: %s -- the board emptied on card %d"
                         % (_label, _i))
                    ok = False
                    break
            _bdW = _MATCH_BOARD.get(_bk) or {}
            # WARNING: THE BOARD NO LONGER HAS TO FILL, and that is the chance blocks
            # doing their job: one that triggers is CONSUMED and its tile goes
            # back to empty (state 0x1D). This assertion used to be `== 16` and
            # it was right until 2026-09-07; it is kept as an inequality so the
            # ten cards are still proved to have landed somewhere.
            # A consumed block frees its tile; a SCRAMBLE also relocates every
            # card, so the only thing still guaranteed is that no card is lost.
            _consumable = sum(1 for _c in _codesW
                              if _c in tmbattle.CONSUMED_CHANCE_CODES)
            _cards_on = sum(1 for _cW in _bdW.values()
                            if int(_cW.row["id"]) < 0x8000)
            if _cards_on != len(_freeW):
                _say("FAIL: %s -- every card played must still be ON the board "
                     "somewhere; played %d, found %d"
                     % (_label, len(_freeW), _cards_on))
                ok = False
            # A PLAIN block can never change hands: it has no arrows, so
            # 0xD387E drops it and nothing ever contests it.
            for _t, _c in enumerate(_codesW):
                if tmbattle.object_kind(_c) == "plain"                         and _bdW[_t].owner != tmbattle.OWNER_BLOCK:
                    _say("FAIL: %s -- plain block on tile %d changed hands; it "
                         "has no arrow mask and must never be contested"
                         % (_label, _t))
                    ok = False
                    break
            # ...but a CHANCE or ROTATING block can be, and a captured one
            # scores for its captor -- which is what makes fighting one worth
            # doing, and is why the total is NOT capped at the ten cards
            # played. What must hold is that the accounting is complete.
            _scW = _board_scores(_bk[0], _bk[1], 2)
            _neutralW = sum(1 for _c in _bdW.values()
                            if _c.owner == tmbattle.OWNER_BLOCK)
            _emptyW = 16 - len(_bdW)
            if sum(_scW) + _neutralW + _emptyW != 16:
                _say("FAIL: %s -- every tile is a seat's, a block's, or EMPTY; "
                     "got scores %r + %d neutral + %d empty on a 16-tile board"
                     % (_label, _scW, _neutralW, _emptyW))
                ok = False
            if _emptyW > _consumable:
                _say("FAIL: %s -- a tile can only end up empty because a chance "
                     "block was consumed off it; %d empty vs %d consumable"
                     % (_label, _emptyW, _consumable))
                ok = False
            if sum(_scW) < 1:
                _say("FAIL: %s -- ten cards were played and nobody scored"
                     % (_label,))
                ok = False

        # --- THE CHANCE-BLOCK EFFECT, END TO END ---------------------------
        # VERIFIED: Decoded 2026-09-07 off the high-nibble dispatch at 0xD07F5 and
        # cross-checked against a tester's live commentary four kinds for
        # four. A battle resolving against a code-7 block powers up every card
        # the ACTOR holds by +10 on rec+0x26 -- the field `resolve` has always
        # taken as `att_mod`/`dfn_mod` and which nothing had ever moved.
        for _code, _want in ((7, 10), (6, -10)):
            _MATCH_BOARD.pop(_bk, None)
            _MATCH_OBJECTS[_bk] = [0] * 16
            _MATCH_OBJECTS[_bk][6] = _code
            _MATCH_BOARD[_bk] = {6: tmbattle.object_card(_code),
                                 0: tmbattle.Card(b"9|30|0|30|30|0|255|0", 0)}
            # Tile 5 places with an EAST arrow (bit 2) into the block on 6.
            _fx, _ = _apply_placement(_bk[0], _bk[1], 2, 0, 5,
                                      b"9|40|0|10|10|0|4|0")
            _bdx = _MATCH_BOARD.get(_bk) or {}
            if not _fx:
                _say("FAIL: a chance block must be BATTLED (its mask is 0xFF), "
                     "code %d" % _code); ok = False; break
            # WARNING: AND THE BLOCK IS GONE. The tester's screen showed all four
            # triggered tiles BLANK while this server still held them as cards,
            # and three of them were in the score we served.
            if 6 in _bdx:
                _say("FAIL: a chance block is CONSUMED by its own effect -- "
                     "state 0x1D rebuilds the tile as card id 0xFFFF, so tile 6 "
                     "must be EMPTY and playable, not captured (code %d)"
                     % _code); ok = False; break
            if _board_scores(_bk[0], _bk[1], 2)[0] != 2:
                _say("FAIL: a consumed block must not count toward anyone's "
                     "score -- seat 0 holds tiles 0 and 5 and nothing else, "
                     "got %r" % (_board_scores(_bk[0], _bk[1], 2),))
                ok = False; break
            if _bdx[0].modifier != _want:
                _say("FAIL: code %d must move the actor's OTHER card on tile 0 "
                     "by %+d (rec+0x26), got %r"
                     % (_code, _want, _bdx[0].modifier)); ok = False; break
            # ...and the block itself is nobody's card, so it must not move.
            if 6 in _bdx and _bdx[6].placer == tmbattle.OWNER_BLOCK                     and _bdx[6].modifier != 0:
                _say("FAIL: the block is not the actor's card and must not be "
                     "powered by its own effect"); ok = False; break
        # --- CODE 17 = SCRAMBLE, and the CANDIDATE LIST is `card+0x1C` -----
        # WARNING: THIS ASSERTION HAS FLIPPED THREE TIMES AND EVERY FLIP WAS A LIVE
        # MATCH: "changes nothing" while 17 was undealt, "scrambles and
        # consumes" for the few hours it was first dealt, back to "changes
        # nothing" on the 2026-09-07T04:20Z hang, and now here. What is
        # different this time is that the list the redeal indexes is the
        # CLIENT'S (0xD3A60 arg2=1 -> `card+0x1C`), not "every tile we hold no
        # card on" -- see `_unplayed_specials`.
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][6] = 17                    # SCRAMBLE
        _MATCH_OBJECTS[_bk][9] = 4                     # an UNPLAYED special
        _MATCH_BOARD[_bk] = {6: tmbattle.object_card(17),
                             0: tmbattle.Card(b"9|30|0|30|30|0|255|0", 0)}
        _apply_placement(_bk[0], _bk[1], 2, 0, 5, b"9|40|0|10|10|0|4|0")
        _bdS = _MATCH_BOARD.get(_bk) or {}
        _cardsS = sorted(t for t, c in _bdS.items()
                         if int(c.row["id"]) < 0x8000)
        if len(_cardsS) != 2:
            _say("FAIL: a scramble moves the two cards, it does not lose or "
                 "duplicate one -- got %r" % (_cardsS,))
            ok = False
        elif 6 in _bdS:
            _say("FAIL: a scrambled block is CONSUMED like every other chance "
                 "block -- state 0x1A rebuilds tile 6 as card id 0xFFFF with "
                 "+0x1C/+0x1D/+0x1E all 8 (0xD205A..0xD20B5), so it must be "
                 "empty and playable")
            ok = False
        elif 9 in _cardsS:
            # WARNING: THE ONE THING 04:20Z GOT WRONG. Tile 9 carries an unplayed
            # special, which the client stamps `+0x1C` = 4 (0xC1976) while
            # leaving its owner byte 8 -- so 0xD3A60's arg2=1 arm never
            # collects it and the redeal can never land there.
            _say("FAIL: an UNPLAYED SPECIAL TILE is not a scramble candidate "
                 "(card+0x1C is 4, not 8) -- a card landed on tile 9")
            ok = False
        elif _board_scores(_bk[0], _bk[1], 2) != [2, 0]:
            # Two: the card that was on 0 and the card just played, wherever
            # the redeal put them. The block is consumed, not captured, so it
            # scores for nobody -- and OWNERSHIP TRAVELS WITH EACH CARD, which
            # is why a score that agrees proves nothing about the board.
            _say("FAIL: a scramble moves cards without changing owners, and "
                 "the block is consumed rather than captured; got %r"
                 % (_board_scores(_bk[0], _bk[1], 2),))
            ok = False
        # ...and the SPECIAL is spent by the placement that lands on it, so it
        # stops being a candidate-blocker (and stops paying its ability twice).
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][9] = 4                     # ability 2, defence up
        _MATCH_BOARD[_bk] = {}
        if _unplayed_specials(_bk[0], _bk[1]) != [9]:
            _say("FAIL: a dealt special tile nobody has played on is the one "
                 "square the scramble may not use -- got %r"
                 % (_unplayed_specials(_bk[0], _bk[1]),))
            ok = False
        _apply_placement(_bk[0], _bk[1], 2, 0, 9, b"9|40|0|10|10|0|4|0")
        _cardS = (_MATCH_BOARD.get(_bk) or {}).get(9)
        if _cardS is None or int(_cardS.ability) != 2:
            _say("FAIL: the tile's ability still belongs to the card played on "
                 "it (0xC7015/0xC703C) -- got %r"
                 % (_cardS and _cardS.ability,))
            ok = False
        if _unplayed_specials(_bk[0], _bk[1]) != []:
            _say("FAIL: a special tile that has been PLAYED ON is no longer "
                 "one -- 0xC7098 overwrites its +0x1C with the owner, so a "
                 "scramble that lifts the card off may redeal there")
            ok = False
        if _tile_ability(_bk[0], _bk[1], 9) != 0:
            _say("FAIL: ...and it must not pay its ability a second time")
            ok = False

        # --- A ROTATING BLOCK IS NEITHER CAPTURED NOR LOST TO --------------
        # WARNING: REGRESSION, 2026-09-07T03:19Z. Written as
        # `verdict == ATTACKER and not _rot`, a rotating battle the attacker
        # WON fell into the `else` -- the LOSS path -- and handed the player's
        # own winning card to owner 4. The log read "board map after tile 10
        # ... 10:4" with the score 0-0. Both invariants hold whoever wins,
        # because neither arm applies at all.
        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS[_bk] = [0] * 16
        _MATCH_OBJECTS[_bk][6] = 9                     # rotating, fires S
        _MATCH_BOARD[_bk] = {6: tmbattle.object_card(9)}
        # Tile 10 places pointing N (bit 0) at the block on tile 6.
        _fR, _ = _apply_placement(_bk[0], _bk[1], 2, 0, 10,
                                  b"9|40|0|10|10|0|1|0")
        _bdR = _MATCH_BOARD.get(_bk) or {}
        if not _fR:
            _say("FAIL: a rotating block has one arrow and a non-zero mask, so "
                 "0xD386B fights it"); ok = False
        elif _bdR.get(10) is None or _bdR[10].owner != 0:
            _say("FAIL: THE REGRESSION -- the placed card must stay its "
                 "owner's whoever won; a rotating battle applies NEITHER the "
                 "capture arm nor the loss arm. Got owner %r"
                 % (_bdR.get(10) and _bdR[10].owner,))
            ok = False
        elif _bdR.get(6) is None or _bdR[6].owner != tmbattle.OWNER_BLOCK:
            _say("FAIL: the block itself is never captured -- nothing in "
                 "states 0x1E..0x22 writes its owner (got %r)"
                 % (_bdR.get(6) and _bdR[6].owner,))
            ok = False
        elif _board_scores(_bk[0], _bk[1], 2) != [1, 0]:
            _say("FAIL: after the placement seat 0 holds exactly its own card, "
                 "got %r" % (_board_scores(_bk[0], _bk[1], 2),))
            ok = False

        _MATCH_BOARD.pop(_bk, None)
        _MATCH_OBJECTS.pop(_bk, None)
        _TABLE_SETTINGS.pop(_bk, None)
        os.environ["POL_TM_BOARD_OBJECTS"] = "0"       # back to the suite pin

        # --- the TURN BYTE, the hand, and the card that lands on the board --
        # WARNING: The third envelope byte is the TURN and `@PutCard` is COMPARED
        # against it (0x103876 -> `Errot!!Turn%d!=%d`), so a header that drops
        # it is a message the client reads and then throws away.
        # (the u32 is 0x070A0043 and the wire is its little-endian hex, which
        # is why a live line reads `43000A07` -- same shape as the measured
        # `GTM0GB2001701@Quit=`, code 0xB2 command 0x17 shop 1.)
        if _turn_code(PUTCARD_CMD, 7) != b"43000A07":
            _say("FAIL: an in-match header is code | cmd<<16 | turn<<24; "
                 "got %r" % (_turn_code(PUTCARD_CMD, 7),)); ok = False
        if ((struct.unpack("<I", bytes.fromhex(
                _turn_code(TURNDATA_CMD, 3).decode()))[0] >> 24) & 0xFF) != 3:
            _say("FAIL: @TurnData must carry the turn in the sh byte -- that is "
                 "the store the client reads it from (0x1036EC)"); ok = False
        # The hand is parsed by its @N<i>= NUMBER, not by order of appearance:
        # /H= indexes this list, and an off-by-one puts a different card down.
        _cs = (b"@CardSelect=/C=3@N1=/D=100|84|0|99|75|23|20|0"
               b"@N0=/D=161|100|2|90|99|31|21|255"
               b"@N2=/D=42|43|1|20|18|7|4|255")
        _hand = _hands_from_cardselect(_cs)
        if [r.split(b"|")[0] for r in _hand] != [b"161", b"100", b"42"]:
            _say("FAIL: the hand must come back in @N<i>= order, got %r"
                 % ([r.split(b"|")[0] for r in _hand],)); ok = False
        # ...and the card that goes out is the one at that slot, with the seat
        # rotated per recipient (the actor is 0 in its own copy, 1 in the other).
        _pc_self = _putcard_body(_seatsD, 0, 0, 2, 5, _hand[2])
        _pc_other = _putcard_body(_seatsD, 1, 0, 2, 5, _hand[2])
        if b"/A=0/H=2/F=5/D=42|43|1|20|18|7|4|255" not in _pc_self:
            _say("FAIL: the placer's own @PutCard is %r" % (_pc_self,)); ok = False
        if b"/A=1/" not in _pc_other:
            _say("FAIL: the watcher must see the actor one seat away, got %r"
                 % (_pc_other,)); ok = False
        # WARNING: AND THE CARD TYPE MUST SURVIVE: 0x1039D9 refuses `type >= 4` with
        # `Error:CardNum=%d`, so a mangled row is a bailed-out arm, not a wrong
        # picture. This is the same eight-field row @CardSelect= carries.
        if len(_pipe_vals(_pc_self, b"/D=")) != 8:
            _say("FAIL: /D= is eight values (occ0 word, occ1..7 bytes), got %r"
                 % (_pipe_vals(_pc_self, b"/D="),)); ok = False
        if int(_pipe_vals(_pc_self, b"/D=")[2]) >= 4:
            _say("FAIL: card type must stay < 4 (0x1039D9 Error:CardNum)")
            ok = False
        # --- AN UNPROMPTED PUSH MUST NOT LEAVE ON THE WRONG SOCKET ---------
        # The regression that black-screened a live player: a member has a room
        # connection AND a table connection, and the first `idle_pushes` drained
        # whichever ticked first. Measured on ports 50478 / 50512.
        _PUSHES.clear()
        _queue_push(mid, b"43000100@VsGameInit=/N=2", "board", after=0,
                    peer=b"UE7QN1N9G")
        if idle_pushes(mid, {b"UKXDBA266"}):
            _say("FAIL: a board pinned to the TABLE peer must not go out on a "
                 "socket that only carries the ROOM peer"); ok = False
        if not _PUSHES.get(_push_key(mid)):
            _say("FAIL: ...and the wrong socket must LEAVE it queued"); ok = False
        if idle_pushes(mid, set()):
            _say("FAIL: a socket carrying no game traffic at all is not a band")
            ok = False
        _got = idle_pushes(mid, {b"UE7QN1N9G", b"UKXDBA266"})
        if [b for _p, b in _got] != [b"43000100@VsGameInit=/N=2"]:
            _say("FAIL: the socket that carries the table peer must get it, "
                 "got %r" % (_got,)); ok = False
        _PUSHES.clear()

        # --- `:str` FIELDS ARE HEX. Asserted against the CLIENT'S OWN SEND ---
        # `@GameEN=/CN=/HN=4C6170746F705465737432/L=1` is captured in this file's
        # `@GameEN=` banner: the fixture name in the encoding 0xAAD60 decodes. If our
        # /CN= does not round-trip to the same bytes, the name is a row of squares.
        if _str_field("LaptopTest2") != b"4C6170746F705465737432":
            _say("FAIL: a :str field is UPPERCASE hex of the cp932 bytes -- the "
                 "client's own @GameEN=/HN= says so; got %r"
                 % (_str_field("LaptopTest2"),)); ok = False
        # ...and the decoder, run over the PLAIN text, must produce exactly the
        # garbage a tester photographed: 'z' then squares.
        def _aad60(s):
            o = bytearray()
            for i in range(0, len(s) - 1, 2):
                h, l = ord(s[i]), ord(s[i + 1])
                h = h - 0x37 if h > 0x39 else h - 0x30
                l = l - 0x37 if l > 0x39 else l - 0x30
                o.append((((h << 4) & 0xFF) + l) & 0xFF)
            return bytes(o)
        if _aad60("LaptopTest2") != bytes((0x7A, 0xCD, 0xB9, 0xFE, 0xFD)):
            _say("FAIL: the 0xAAD60 model no longer reproduces the measured "
                 "garbage 7A CD B9 FE FD"); ok = False
        if _aad60(_str_field("LaptopTest2").decode()) != b"LaptopTest2":
            _say("FAIL: /CN= must round-trip through the client's own decoder")
            ok = False

        # WARNING: THE HAND COMPACTS -- `/H=` indexes what is LEFT, not the deal.
        # 0xC70F4 left-shifts the 156-byte slots at 0x2B65EC over the played one.
        # This is the live sequence that found it: member 6 held 42,47,58,59,87
        # and played slots 0, 1, 1 -- which is 42, 58, 59, and the server sent
        # 42, 47, 47 until this was fixed.
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _PUSHES.clear()
        _six = _hands_from_cardselect(
            b"@CardSelect=/C=5@N0=/D=42|1|0|1|1|1|1|0@N1=/D=47|1|0|1|1|1|1|0"
            b"@N2=/D=58|1|0|1|1|1|1|0@N3=/D=59|1|0|1|1|1|1|0"
            b"@N4=/D=87|1|0|1|1|1|1|0")
        # WARNING: AND IT MUST RUN THE HANDLER. The first version of this check did the
        # `del` on a local list and passed cleanly with the real one removed --
        # the same dead-assertion class as `ok = _selftest_blocks(); ok = True`.
        _remember_match("#TM0R001", 1, _seatsD)
        for _m, _v in _seatsD:
            _MATCH_PEER[_push_key(_m)] = b"UE7QN1N9G"
            handle_line(encode_code(IN_MATCH_CODE | (7 << 16))
                        + b"@CardSelect=/C=5@N0=/D=42|1|0|1|1|1|1|0"
                          b"@N1=/D=47|1|0|1|1|1|1|0@N2=/D=58|1|0|1|1|1|1|0"
                          b"@N3=/D=59|1|0|1|1|1|1|0@N4=/D=87|1|0|1|1|1|1|0",
                        peer=b"auth-band", peer_nick=b"UE7QN1N9G", member_id=_m)
        _played = []
        for _turn, _h in ((0, 0), (2, 1), (4, 1)):
            _r = handle_line(_turn_code(PUTDATA_CMD, _turn)
                             + b"@PutData=/P=0/H=%d/F=%d" % (_h, _turn),
                             peer=b"auth-band", peer_nick=b"UE7QN1N9G",
                             member_id=_seatsD[0][0])
            _lines = _r if isinstance(_r, (list, tuple)) else [_r or b""]
            _pcl = next((l for l in _lines if b"@PutCard=" in l), b"")
            _played.append((_pipe_vals(_pcl, b"/D=") or [b"?"])[0])
        if _played != [b"42", b"58", b"59"]:
            _say("FAIL: slots 0,1,1 out of 42,47,58,59,87 are cards 42,58,59 -- "
                 "a hand that does not compact answers %r" % (_played,)); ok = False
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _PUSHES.clear()

        # The last turn must not be announced: the client prints `End` at
        # 5 * N (0xC7701) and stops polling command 9.
        _PUSHES.clear()
        _MATCH_TURN.clear()
        # The stats bump persists games/score_total into the collection dir;
        # sandbox it so a selftest never writes member files into the live
        # resource tree.
        _rsave = os.environ.get("POL_RESOURCE_DIR")
        _rtmp = tempfile.mkdtemp(prefix="tm-result-")
        os.environ["POL_RESOURCE_DIR"] = _rtmp
        try:
            _queue_next_turn("#TM0R001", 1, _seatsD, 10, 0)
            # Past 5xN there is no turn 10 -- but there IS a result, and the
            # client polls (0x43, 13) for it for ever if it does not come
            # (measured live: both stopped exactly there after
            # GamePlayEnd:Turn=9).
            _past = [e[1] for v in _PUSHES.values() for e in v]
            if any(b"@TurnData=" in b for b in _past):
                _say("FAIL: turn 10 of a two-player match is past the 5xN limit "
                     "and must not be announced -- got %r" % (_past,)); ok = False
            if len(_past) != 2 or not all(b"@ResultData=" in b for b in _past):
                _say("FAIL: the last turn must hand both players @ResultData on "
                     "(0x43, 13) -- got %r" % (_past,)); ok = False
            # WARNING: /P=0 CRASHED BOTH CLIENTS (2026-08-21, live): 0x10F6A5 divides
            # by [obj+0x84], and only the recipient's own @No section writes
            # it. One section per seat, and the first /D= in each copy is the
            # recipient's own (@No0 -- recipient-first rotation).
            for _b in _past:
                if b"/P=2" not in _b:
                    _say("FAIL: @ResultData must carry one @No section per "
                         "seat -- /P=0 is the measured divide-by-zero crash; "
                         "got %r" % (_b,)); ok = False
                _d = _pipe_vals(_b, b"/D=")
                if len(_d) != 7:
                    _say("FAIL: /D= is read by occurrence, SEVEN slots, none "
                         "checked -- a short list stores stack garbage, the "
                         "divisor included; got %r" % (_d,)); ok = False
                elif int(_d[5] or 0) < 1:
                    _say("FAIL: /D= occ 5 is the divisor at 0x10F6A5 and must "
                         "be >= 1; got %r" % (_d,)); ok = False
                if b"/RN=" not in _b:
                    _say("FAIL: the own section carries /RN= (hex :str, "
                         "[obj+0x128]); got %r" % (_b,)); ok = False
            # ...and the counters must actually persist, or every match is
            # for ever the player's first.
            _blk = (_collection_load(_seatsD[0][0]) or {}).get("rank") or {}
            if _blk.get("games") != 1:
                _say("FAIL: a finished match must record games=1 in the rank "
                     "block; got %r" % (_blk,)); ok = False
            # THE ONCE-GUARD: a re-sent final placement must not double
            # anything -- not the pushes, not the games count.
            _n0 = sum(len(v) for v in _PUSHES.values())
            _queue_next_turn("#TM0R001", 1, _seatsD, 10, 0)
            if sum(len(v) for v in _PUSHES.values()) != _n0:
                _say("FAIL: the end branch must fire ONCE per match -- a "
                     "re-sent @PutData re-pushed the result"); ok = False
            _blk = (_collection_load(_seatsD[0][0]) or {}).get("rank") or {}
            if _blk.get("games") != 1:
                _say("FAIL: a re-fired end branch double-counted games; got "
                     "%r" % (_blk,)); ok = False
        finally:
            if _rsave is None:
                os.environ.pop("POL_RESOURCE_DIR", None)
            else:
                os.environ["POL_RESOURCE_DIR"] = _rsave
            shutil.rmtree(_rtmp, ignore_errors=True)
        _PUSHES.clear()
        _MATCH_TURN.clear()
        _queue_next_turn("#TM0R001", 1, _seatsD, 9, 1)
        if len(_PUSHES) != 2:
            _say("FAIL: turn 9 IS in range and must reach both players, got %r"
                 % (list(_PUSHES),)); ok = False
        _PUSHES.clear(); _MATCH_TURN.clear()

        # --- the sell price is a LADDER LOOKUP, against three real prices ---
        # Measured 2026-08-20 off the shop screen, with the cards from the wire.
        # A flat price desyncs the wallet silently: the CLIENT prices the card
        # and only tells us which one went.
        _lad = _cardpri_table()
        if _lad[:6] != [0, 25, 50, 100, 250, 500]:
            _say("FAIL: CardPri.BIN must ship and decode -- got %r" % (_lad[:6],))
            ok = False
        # All six measured cards, spanning every card type. The multiplier is
        # the TYPE in eighths -- (7 + max(0, type-1))/8 -- and the STATS do not
        # enter it: 15 and 102 differ wildly but are both type 0 and both 7/8.
        for _c, _want in (([15, 14, 0, 10, 12, 2], 43),
                          ([102, 65, 0, 56, 40, 6], 656),
                          ([49, 54, 1, 43, 13, 5], 437),
                          ([234, 5, 1, 50, 100, 15], 437),
                          ([161, 100, 2, 90, 99, 21], 10000),
                          ([158, 97, 3, 89, 95, 24], 19687),
                          # the wire's 6th value is the ARROW MASK since
                          # 422bc0c4 -- a Fat Chocobo (table level 6) with
                          # arrows 20 must still sell at ladder[6]*7/8:
                          ([89, 34, 0, 76, 32, 20], 656)):
            _got = _sell_price(_c)
            if _got != _want:
                _say("FAIL: type %d level %d must sell for %d, got %d"
                     % (_c[2], _c[5], _want, _got)); ok = False
        # THE CARD TABLE: an id alone must be enough to price a card, or a wire
        # row missing its level silently desyncs the wallet.
        if _cardprm(158) != (3, 24) or _cardprm(161) != (2, 21):
            _say("FAIL: CardPrm.BIN must ship and decode (12-byte records from "
                 "+16; byte 1 = type, byte 5 = level) -- got %r / %r"
                 % (_cardprm(158), _cardprm(161))); ok = False
        if _sell_price([158]) != 19687 or _sell_price([161]) != 10000:
            _say("FAIL: a card id alone must price correctly, got %d / %d"
                 % (_sell_price([158]), _sell_price([161]))); ok = False
        # There is no type 4 in the real records; the rule covers every card.
        _types = {t for t, _l in (_cardprm(i) for i in range(480)) if t is not None}
        if _types != {0, 1, 2, 3}:
            _say("FAIL: the card table should carry types 0..3 only, got %r"
                 % (sorted(_types),)); ok = False

        # The type must MOVE the price -- same level and stats, different type.
        if _sell_price([1, 9, 3, 9, 9, 21]) <= _sell_price([1, 9, 0, 9, 9, 21]):
            _say("FAIL: a type-3 card must sell for more than a type-0 one at "
                 "the same level"); ok = False
        # A card with no level must NOT silently take the ladder's level 0.
        if _sell_price([1, 2, 3]) == _lad[0]:
            _say("FAIL: a level-less card must fall back loudly, not price at "
                 "ladder[0]"); ok = False

        # --- `/CP=` is the CARD LEVEL, and must not carry the wallet -------
        # WARNING: Money is file +0x34 -> struct +0xC8 (`/M=`); struct +0x00 is the
        # CARD LEVEL, which the VS. COM opponent unlock compares against each
        # `PlPrm.BIN` +0x02 threshold (0x110AE4 and four more). Sending the
        # balance here wrote the player's gil into their card level; sending 0
        # locked every opponent. Both shipped. See `_init_body`.
        #
        # WARNING: THE OLD FIXTURE COULD NOT FAIL: it built the body for a member with
        # no money, so the `/CP= == /M=` compare was `0 == 0` and skipped. This
        # one uses a member who holds both cards and gold.
        _cl_rows = [[89, 34, 0, 76, 32, 8, 20, 255],
                    [158, 97, 3, 89, 95, 36, 65, 255]]
        _collection_store("cpm", {"cards": _cl_rows, "money": 4321})
        _shopb = _init_body("Init", "cpm")
        _cp = re.search(rb"/CP=(\d+)", _shopb)
        _m = re.search(rb"/M=(\d+)", _shopb)
        _want_cl = card_level(_cl_rows)
        if not _cp or not _m:
            _say("FAIL: the shop body needs both /M= and /CP=, got %r" % (_shopb,))
            ok = False
        else:
            if int(_cp.group(1)) != _want_cl:
                _say("FAIL: /CP= must carry the CARD LEVEL (%d) -- it lands in "
                     "struct +0x00, the VS. COM opponent gate. Got %r"
                     % (_want_cl, _shopb)); ok = False
            if _cp.group(1) == _m.group(1) and _m.group(1) != b"0":
                _say("FAIL: /CP= must not carry the wallet -- money is /M= "
                     "(struct +0xC8). Got %r" % (_shopb,)); ok = False

        # --- @GameExit= must be ANSWERED or the client hangs on Loading ------
        # Reported live 2026-08-20T13:31Z: stalled on the loading screen after a
        # clean quit. 0x8416C drains (0x41, 0x0E) immediately before sending
        # `@GameExit=` on (0x41, 0x0D), so it is waiting for request+1.
        _ex = handle_line(b"41000D00@GameExit=", member_id=None)
        if not _ex or not _ex.startswith(b"41000E00"):
            _say("FAIL: @GameExit= must be answered on (0x41, 0x0E) -- the "
                 "sender drains exactly that slot first. Got %r" % (_ex,))
            ok = False
        if not _ex or b"@GameEA=" not in _ex:
            _say("FAIL: the @GameExit= answer is named `@GameEA` (0x8462D) and "
                 "needs the `=` delimiter, got %r" % (_ex,)); ok = False
        _exv = re.search(rb"/Exit=(-?\d+)", _ex or b"")
        if not _exv or int(_exv.group(1)) <= 0:
            _say("FAIL: /Exit= must be > 0 -- 0x8465B tests it before clearing "
                 "the global at 0x298200. Got %r" % (_ex,)); ok = False

        # --- a match must NOT start while the owner is still configuring ----
        # WARNING: Measured 2026-08-20T12:43Z: two seats were reached while the host
        # was mid-dialog, the wake-up went out, and the host answered @GameNG=
        # -- stranding the guest on a black screen. See `_TABLE_CONFIRMED`.
        _PUSHES.clear(); _MATCH_ACCEPTS.clear(); _TABLE_CONFIRMED.clear(); tmroom._CONFIRMED.clear()
        _SEATED["#TM0R001"] = {1: [(mid, 1), (mid + 1, 2)]}
        _announce_match(tmroom, "#TM0R001", 1, [(mid, 1), (mid + 1, 2)])
        if _PUSHES:
            _say("FAIL: no match may be announced before the owner confirms -- "
                 "a wake-up into an open dialog is answered @GameNG=. Got %r"
                 % (_PUSHES,)); ok = False
        # ...and 0x24 (@Save=, the preset) must NOT count as confirmation: the
        # client sends it repeatedly while the dialog is still open.
        _note_table_settings(b"@Save=/Tbl=1/Game=1@Tet=/tl=0@Tab=/in=0",
                             mid, 0x24, b"UE7QN1N9G")
        # ...and `/Tbl=` must NOT be read as a table number.
        # WARNING: Measured on prod 2026-08-20T19:41:47Z: a real save made AT TABLE 2
        # carried `/Tbl=0`, the hint beat the peer, and the settings were filed
        # against table 0 -- which does not exist:
        #
        #     table -> UNRESOLVED [peer 0xd0e4bcbb92 indexes to 2 but /Tbl= says
        #                          0 -- TAKING /Tbl=, ...]
        #     ...table 0 is not in the shipped fixture -- nothing to update
        #
        # `0x87410` takes `/Tbl=` and `/Game=` as PAGE selectors; the peer
        # is the table. Asserted on the ARITHMETIC and on the CALL SITE, because
        # by this point in the fixture no room peer is registered and a
        # behavioural check would pass for the wrong reason (it would answer
        # None either way -- which is exactly how a green test can hide this).
        _room_peer = 0xf0e4bcbb91                  # UKXDBA266, off the wire
        if table_index_for_peer(b"UE7QN1N9N", _room_peer, None)[0] != 2:
            _say("FAIL: UE7QN1N9N (0xd0e4bcbb92) is table 2 against room "
                 "%#x" % (_room_peer,)); ok = False
        if table_index_for_peer(b"UE7QN1N9N", _room_peer, 0)[0] != 0:
            _say("FAIL: this test is inert -- a `/Tbl=` hint is supposed to win "
                 "when one is passed, which is why the call site must not pass "
                 "one for @Save=."); ok = False
        _src = inspect.getsource(_note_table_settings)
        if "table_index_for_peer(peer_nick, room, None, chan=chan)" not in _src:
            _say("FAIL: _note_table_settings must pass NO /Tbl= hint -- it is "
                 "the page selector, not a table number (0x87410)."); ok = False

        # --- a table's rules must OUTLIVE THIS PROCESS -------------------
        # WARNING: They used to live only in `_TABLE_SETTINGS`, so a restarted
        # container answered "no rules" for every table until somebody
        # reconfigured one. The dialog sites that hard-code "seed from the
        # table's rules" then find the info record unpopulated, `0x873A0`
        # fails its `+0x30 == 2` gate, and `0x6BC61` shows a message instead
        # of opening the dialog.
        _rules = {"bm": 166, "du": 0, "st": 1, "cb": 1, "ca": 1, "gs": 1,
                  "tl": 3, "in": 0, "lu": 99999, "ll": 0, "au": 0, "al": 0,
                  "co": 0, "pa": 0, "Tbl": 0, "Game": 1}
        _table_rules_put("#TM0R001", 2, _rules)
        _TABLE_SETTINGS.clear()                 # <- the restart
        _back = _table_rules_get("#TM0R001", 2)
        if not _back:
            _say("FAIL: a table's rules must survive an empty process cache -- "
                 "they are what code 0x13 is built from."); ok = False
        elif _back.get("tl") != 3 or _back.get("lu") != 99999:
            _say("FAIL: the recovered rules must be the ones stored, got %r"
                 % (_back,)); ok = False
        # ...and the PAGE SELECTORS are not rules; storing them would put
        # `/Tbl=` and `/Game=` into a 0x13 body that has no such fields.
        elif "Tbl" in _back or "Game" in _back:
            _say("FAIL: /Tbl= and /Game= are page selectors and must not be "
                 "stored as table rules. Got %r" % (sorted(_back),)); ok = False
        _TABLE_SETTINGS.clear()
        _announce_match(tmroom, "#TM0R001", 1, [(mid, 1), (mid + 1, 2)])
        if _PUSHES:
            _say("FAIL: @Save= (0x24) is the preset write, not Confirm -- it "
                 "must not start a match. Got %r" % (_PUSHES,)); ok = False
        # ...but the Confirm (0x14) must, and on a table that filled up while
        # the dialog was open it must start it THERE, since @GameEN= is gone.
        _TABLE_CONFIRMED.add(("#TM0R001", 1))
        _announce_match(tmroom, "#TM0R001", 1, [(mid, 1), (mid + 1, 2)])
        if not _PUSHES:
            _say("FAIL: once the owner has confirmed, a full table must start")
            ok = False
        _PUSHES.clear(); _MATCH_ACCEPTS.clear(); _TABLE_CONFIRMED.clear(); tmroom._CONFIRMED.clear()
        _SEATED.clear()

        # --- the PRESET write: ONE HALF, and the dwords stay dwords --------
        # WARNING: The failure this guards is worse than the bug it fixes. `@Save=`
        # carries all fourteen fields but only the SENDING PAGE filled its half
        # in; storing both halves would blank the other page every time. See
        # `table_preset_fields`, and `logs/authserv.log` 2026-08-18T06:45:57Z
        # for a real `/Game=1` whose whole `@Tab=` half was zeros.
        if tmsave is not None:
            _rules = (b"@Save=/Tbl=0/Game=1"
                      b"@Tet=/bm=0/du=0/st=1/cb=1/ca=1/gs=1/tl=3"
                      b"@Tab=/in=0/lu=0/ll=0/au=0/al=0/co=0/pa=0")
            _ch, _half = table_preset_fields(_rules)
            _tab_offs = {tmsave.TABLE_PRESET_BY_NAME[n][0]
                         for n in tmsave.TABLE_PRESET_TAB}
            if _tab_offs & set(_ch):
                _say("FAIL: a /Game=1 (rules page) @Save= must store ONLY the "
                     "@Tet= half -- its @Tab= zeros are untouched globals, not "
                     "the player's choices. Got %r" % (sorted(_ch),)); ok = False
            if _ch.get(0x0D0) != (1, 3):
                _say("FAIL: /tl=3 belongs at save +0x0D0 as one byte "
                     "(0x14DB44 reads it with `mov byte`). Got %r"
                     % (_ch.get(0x0D0),)); ok = False

            _restr = (b"@Save=/Tbl=1/Game=0"
                      b"@Tet=/bm=0/du=0/st=0/cb=0/ca=0/gs=0/tl=0"
                      b"@Tab=/in=0/lu=99999/ll=0/au=300/al=100/co=0/pa=0")
            _ch2, _half2 = table_preset_fields(_restr)
            _tet_offs = {tmsave.TABLE_PRESET_BY_NAME[n][0]
                         for n in tmsave.TABLE_PRESET_TET}
            if _tet_offs & set(_ch2):
                _say("FAIL: a /Tbl=1 (restrictions page) @Save= must store ONLY "
                     "the @Tab= half. Got %r" % (sorted(_ch2),)); ok = False
            # WARNING: THE WIDTH IS THE WHOLE POINT for these four. `au=300` truncated
            # to a byte is 44, and a 0..44 rank band refuses everybody the same
            # way 0..0 did -- which is the measured refusal, re-manufactured.
            for _n, _want in ((b"au", 300), (b"al", 100), (b"lu", 99999)):
                _o, _w = tmsave.TABLE_PRESET_BY_NAME[_n]
                if _ch2.get(_o) != (4, _want):
                    _say("FAIL: /%s=%d must be stored as a DWORD at +0x%03X. "
                         "Got %r" % (_n.decode(), _want, _o, _ch2.get(_o)))
                    ok = False

            # ...and a message naming NEITHER page is not a preset write.
            _ch3, _half3 = table_preset_fields(
                b"@Tet=/tl=0@Tab=/in=0")
            if _ch3 or _half3:
                _say("FAIL: an @Save= with /Tbl=0 and /Game=0 names no page -- "
                     "0x87410 would have built the bare 0x14 pair instead. "
                     "Got %r" % (_ch3,)); ok = False

            # The factory header must carry what the client's own Default sends,
            # and the DWORD fields must be dwords.
            # WARNING: `au`/`al` WERE ASSERTED AT 300/100 HERE AND THAT WAS WRONG --
            # carried over from a code 0x14 Confirm, i.e. the TABLE tier. Page 2's
            # Default on the PRESET tier sends `lu=99999` and every other
            # restriction ZERO. See `tmsave.DEFAULT_TABLE_PRESET_RETRACTED`; do
            # not put the band back without settling which tier owns it.
            _minted = bytearray(tmsave.build([]))
            for _n, _want in ((b"lu", 99999), (b"au", 0), (b"al", 0)):
                _o, _w = tmsave.TABLE_PRESET_BY_NAME[_n]
                _got = struct.unpack_from("<I", _minted, _o)[0]
                if _got != _want:
                    _say("FAIL: a minted save must carry /%s=%d at +0x%03X "
                         "(the client's own Default). Got %d"
                         % (_n.decode(), _want, _o, _got)); ok = False

        # --- the PIPE format itself, which is what 0xAB080 indexes ---------
        # WARNING: We emitted `/ID=0/ID=1` for weeks of session time. `0xAB080` finds
        # `name=` then steps past `index` copies of the separator at 0x51B2B78,
        # which is `|`; the value ends at the next `/` or `@`. A repeated KEY
        # gives occurrence >= 1 nothing to step to, and NONE of the call sites
        # checks the return before storing -- hence NOT_FOUND ids, squares for
        # names, and rules read from stack garbage (the auto-selecting timer).
        _pb = _vsgame_body([(mid, 1), (mid + 1, 2)], 0, [4, 5, 6, 7, 8, 9, 10])
        if b"/ID=0/ID=" in _pb or b"/R=4/R=" in _pb:
            _say("FAIL: repeated KEYS -- values must be `|`-separated under ONE "
                 "key (0xAB15A steps over 0x51B2B78 = '|'). Got %r" % (_pb,))
            ok = False
        if _pipe_vals(_pb, b"/R=") != [b"4", b"5", b"6", b"7", b"8", b"9", b"10"]:
            _say("FAIL: the seven rules must survive as a pipe list in order, "
                 "got %r" % (_pipe_vals(_pb, b"/R="),)); ok = False
        # The value must STOP at the next key, not swallow it.
        if _pipe_vals(_pb, b"/N=") != [b"2"]:
            _say("FAIL: a value ends at the next `/` or `@` (0x51B3950), got %r"
                 % (_pipe_vals(_pb, b"/N="),)); ok = False

        # --- the recipient leads its own copy, whatever seat it holds ------
        # WARNING: Three runs, one rule (see `_vsgame_body`): the client matched its
        # id ONLY at occurrence 0, and answered ID=NOT_FOUND whenever its id was
        # later in the list -- including when the list carried the real content
        # ids. `/GID=` is NOT what `/ID=` is compared against: that is the global
        # at 0x294BD4 (`GAME-ID`, which reads 0), while `gameID = 1000001602`
        # comes from 0x2B52A0/A4, the pair feeding `/GID=`. Different variables.
        _note_game_id(mid, b"@Init=/NN=X/CN=Y/GID=000000003B9ACB2E/L=1")
        if _game_id_of(mid) != 1000000302:
            _say("FAIL: /GID= is hex ASCII and must decode, got %r"
                 % (_game_id_of(mid),)); ok = False
        _seatsG = [(mid, 1), (mid + 1, 2)]
        _self = b"%d" % _env_int("POL_TM_VSGAME_SELFID", 0)
        for _me in (0, 1):
            _bd = _vsgame_body(_seatsG, _me, [0] * 7)
            _ids = _pipe_vals(_bd, b"/ID=")
            if _ids[0] != _self:
                _say("FAIL: the recipient at table seat %d must still be /ID= "
                     "occurrence 0 in its own copy -- got %r" % (_me, _ids))
                ok = False
            if _self in _ids[1:]:
                _say("FAIL: no peer may carry the recipient's own id, or the "
                     "match stops on the wrong player -- got %r" % (_ids,))
                ok = False
        # ...and the reordering itself: the recipient's own NAME leads its own
        # copy. Asserted with names this test sets, so it cannot pass on
        # ambient state -- an earlier version did, and a negative run that
        # should have failed came back green.
        tmroom.note_name(mid, "AAAHOST")
        tmroom.note_name(mid + 1, "ZZZGUEST")
        _n0 = [_aad60(v.decode()) for v in
               _pipe_vals(_vsgame_body(_seatsG, 0, [0] * 7), b"/CN=")]
        _n1 = [_aad60(v.decode()) for v in
               _pipe_vals(_vsgame_body(_seatsG, 1, [0] * 7), b"/CN=")]
        if _n0[:2] != [b"AAAHOST", b"ZZZGUEST"]:
            _say("FAIL: seat 0's copy must lead with its own name, got %r"
                 % (_n0,)); ok = False
        if _n1[:2] != [b"ZZZGUEST", b"AAAHOST"]:
            _say("FAIL: seat 1's copy must ALSO lead with its own name -- the "
                 "client only ever matches itself at occurrence 0. Got %r"
                 % (_n1,)); ok = False
        _GAME_IDS.clear()

        # --- an unconfigured table must serve the CLIENT'S defaults, not 0 ----
        # Measured off the wire 2026-08-20; see TET_DEFAULTS. Zeros are not
        # merely un-default -- the wire reading measured au=0/al=0 as always refused.
        if _rule_seven(None) != [0, 0, 1, 1, 1, 1, 3]:
            _say("FAIL: a table nobody has configured must serve the client's "
                 "own defaults, got %r" % (_rule_seven(None),)); ok = False
        # ...and a table that HAS been configured still wins.
        if _rule_seven({"tl": 9}) != [0, 0, 1, 1, 1, 1, 9]:
            _say("FAIL: stored settings must override the defaults, got %r"
                 % (_rule_seven({"tl": 9}),)); ok = False
        # @VsGameInit/@WatchInfo use the PARSER's slot order, tl at occ 4 (the
        # match engine's timer slot [0xE3]), NOT @Tet's occ 6 -- the PvP 0:30 bug.
        if _vsgame_rule_seven(None) != [0, 1, 1, 1, 3, 0, 1]:
            _say("FAIL: @VsGameInit defaults must put tl=3 at occ 4 (Special "
                 "Tile On, 3:00), got %r" % (_vsgame_rule_seven(None),)); ok = False
        if _vsgame_rule_seven({"tl": 9})[4] != 9:
            _say("FAIL: the owner's timer must land at /R= occ 4 for @VsGameInit,"
                 " got %r" % (_vsgame_rule_seven({"tl": 9}),)); ok = False

        # --- @CardSelect=: the vector must be TRUE ----------------------------
        # The 2026-08-20T11:04Z fault: answering `/Ans=1` for every seat on the
        # FIRST player's pick told the host both had chosen; it went on, and the
        # guest arrived to a game that had moved and could not start.
        _seats2 = [(mid, 1), (mid + 1, 2)]
        _one = _cardselect_body(_seats2, {_push_key(mid)}, 7, 0)
        if _one != b"43000700@CardSelect=/Ans=1|0/Ok=0":
            _say("FAIL: with only seat 0 chosen the vector must be "
                 "/Ans=1/Ans=0, got %r" % (_one,)); ok = False
        # WARNING: AND IT IS RECIPIENT-FIRST. The player who just picked must read
        # `1` at occurrence 0 -- telling it `0` is what made P2's client
        # auto-select (measured 11:47:33Z, `/Ans=0/Ans=1` to the picker).
        _mineone = _cardselect_body(_seats2, {_push_key(mid + 1)}, 7, 1)
        if _mineone != b"43000700@CardSelect=/Ans=1|0/Ok=0":
            _say("FAIL: the picker must see its OWN readiness at occurrence 0, "
                 "got %r" % (_mineone,)); ok = False
        _both = _cardselect_body(_seats2, {_push_key(mid), _push_key(mid + 1)}, 7, 0)
        if _both != b"43000700@CardSelect=/Ans=1|1/Ok=1":
            _say("FAIL: with both chosen the vector must be /Ans=1/Ans=1, "
                 "got %r" % (_both,)); ok = False
        # WARNING: /Ok= IS THE GO SIGNAL. 0x1031BC returns `/Ok= + 1`, and the
        # card-select STATE (0x17, arm 0xC282E) tests `cmp eax, 2` -- so only
        # `/Ok=1` advances it. 0 while anyone is still choosing keeps the scene
        # parked, which is exactly what it is for.
        if b"/Ok=1" not in _both:
            _say("FAIL: with everyone chosen /Ok= must be 1 -- 0xC283A tests "
                 "`cmp eax, 2` and 0x1031BC returns /Ok=+1, so /Ok=0 parks the "
                 "scene for ever. Got %r" % (_both,)); ok = False
        if b"/Ok=0" not in _one:
            _say("FAIL: while someone is still choosing /Ok= must be 0, got %r"
                 % (_one,)); ok = False

        # --- AN EMPTY ROSTER IS NOT "EVERYONE HAS CHOSEN" ---------------------
        # 2026-08-25T03:01:05Z: pol-git-sync recreated login/authsess while a
        # 2-player match was in CARD SELECT. 8s later the fresh process took
        # member 16's `@CardSelect=` with `seats == []`, and because `others`
        # filters OVER `seats` it came out vacuously empty -- "1 of 2 have
        # chosen" and then "...dealing: @StartData= to member 16". `deal_to =
        # seats or [(member_id, 0)]` dealt the sender a ONE-SIDED game; the
        # other player was never dealt in (this band never resends) and the
        # table's observer stayed on "players are selecting their cards".
        _CARD_READY.clear(); _MATCH_TURN.clear(); _PUSHES.clear()
        _MATCH_HANDS.clear()
        _orph = handle_line(b"43000700@CardSelect=/C=5"
                            b"@N0=/D=42|43|1|20|18|7|4|255"
                            b"@N1=/D=47|38|1|28|11|7|3|255"
                            b"@N2=/D=58|56|1|56|45|14|7|2"
                            b"@N3=/D=59|45|1|45|56|14|7|2"
                            b"@N4=/D=87|60|2|60|87|21|12|4",
                            member_id="orphanmatch", peer_nick=b"UTESTPEER")
        if _MATCH_TURN:
            _say("FAIL: a @CardSelect= for a match with NO ROSTER (no seats, "
                 "not a COM game) must NOT deal -- it created %d turn "
                 "entry/entries, i.e. a one-sided game"
                 % (len(_MATCH_TURN),)); ok = False
        if any(b"@StartData=" in e[1] for e in _PUSHES.get(_push_key("orphanmatch"), ())):
            _say("FAIL: a rosterless @CardSelect= must not queue @StartData= -- "
                 "that is the one-sided deal reaching the wire"); ok = False
        # ...and a VS. COM game, which legitimately has NO seats, must still
        # deal. This is the case `deal_to`'s solo fallback was written for, and
        # the gate above must not take it with it.
        _CARD_READY.clear(); _MATCH_TURN.clear(); _PUSHES.clear()
        handle_line(b"43000603@ComGame=/Rule=100|0|1|1|1|0|0|0/Com=1",
                    member_id="orphancom", peer_nick=b"UTESTPEER")
        _PUSHES.pop(_push_key("orphancom"), None)
        handle_line(b"43000700@CardSelect=/C=5"
                    b"@N0=/D=42|43|1|20|18|7|4|255"
                    b"@N1=/D=47|38|1|28|11|7|3|255"
                    b"@N2=/D=58|56|1|56|45|14|7|2"
                    b"@N3=/D=59|45|1|45|56|14|7|2"
                    b"@N4=/D=87|60|2|60|87|21|12|4",
                    member_id="orphancom", peer_nick=b"UTESTPEER")
        if not _MATCH_TURN:
            _say("FAIL: a VS. COM game has no seats BY CONSTRUCTION and must "
                 "still deal -- the rosterless gate must key on com_n, not on "
                 "`seats` alone"); ok = False

        # --- THE DEPLOY GATE MUST SEE A CARD-SELECT MATCH ---------------------
        # `count` was `len(_MATCH_TURN)`, which is not created until the deal,
        # so a match in card select reported 0 live matches and pol-git-sync /
        # pol-stale-check recreated login+authsess straight through it. That is
        # the restart above.
        _lmp = os.path.join(os.environ.get("POL_DATA_DIR", "/data"),
                            "tm-matches-live.json")
        _CARD_READY.clear(); _MATCH_TURN.clear(); _MATCH_STARTED.clear()
        _live_matches_write()
        try:
            if json.load(open(_lmp)).get("count") != 0:
                _say("FAIL: with no match anywhere the live marker must "
                     "report 0"); ok = False
        except Exception as _e:
            _say("FAIL: the live marker was not written (%s)" % (_e,)); ok = False
        # A match that has STARTED but not yet dealt -- the exact blind spot.
        _MATCH_STARTED[("#TM0RTEST", 4)] = True
        _live_matches_write()
        try:
            if json.load(open(_lmp)).get("count") != 1:
                _say("FAIL: a match that has STARTED but not yet DEALT must "
                     "count as live -- this is the card-select blind spot that "
                     "let a deploy restart eat a match on 2026-08-25"); ok = False
        except Exception as _e:
            _say("FAIL: the live marker was not written (%s)" % (_e,)); ok = False
        _MATCH_STARTED.clear(); _CARD_READY.clear(); _MATCH_TURN.clear()
        _live_matches_write()
        # ==== THE BATTLE, DRIVEN THROUGH THE REAL HANDLER =================
        #
        # WARNING: THROUGH `handle_line`, NOT THROUGH `_apply_placement`. The hand
        # compaction check in this file's history did its `del` on a local list
        # and passed green with the real fix removed; a battle test that calls
        # the resolver directly would do the same thing, because the whole
        # question is whether a PLACEMENT reaches it.
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
        _TURN_RAND.clear(); _PUSHES.clear()
        _remember_match("#TM0R001", 1, _seatsD)
        for _m, _v in _seatsD:
            _MATCH_PEER[_push_key(_m)] = b"UE7QN1N9G"
        # Two cards that MUST fight: tile 5 points EAST (bit 2 = 4), tile 6
        # points WEST (bit 6 = 64), so each has an arrow into the other.
        # Fields: id|attack|type|pdef|mdef|power|ARROWS|flag.
        _east, _west = b"100|50|0|10|10|0|4|0", b"101|50|0|10|10|0|64|0"
        # ...and one that cannot: tile 6 pointing NORTH is a free flip.
        _north = b"102|50|0|10|10|0|1|0"

        def _play(seat, turn, tile, rows):
            """One player's whole turn: deal them `rows`, then place slot 0."""
            _MATCH_HANDS.setdefault(("#TM0R001", 1), {})[
                _push_key(_seatsD[seat][0])] = [bytearray(r) and r for r in rows]
            return handle_line(_turn_code(PUTDATA_CMD, turn)
                               + b"@PutData=/P=0/H=0/F=%d" % tile,
                               peer=b"auth-band", peer_nick=b"UE7QN1N9G",
                               member_id=_seatsD[seat][0])

        def _battles():
            return [e[1] for v in _PUSHES.values() for e in v
                    if b"@BattleData=" in e[1]]

        _play(0, 0, 5, [_east])
        if _battles():
            _say("FAIL: the first card on an empty board fights nobody -- got "
                 "%r" % (_battles(),)); ok = False
        if (_MATCH_BOARD.get(("#TM0R001", 1)) or {}).get(5) is None:
            _say("FAIL: a relayed placement must be REMEMBERED -- without a "
                 "board model nothing can tell a battle from a flip"); ok = False
        _PUSHES.clear()
        _play(1, 1, 6, [_west])
        _bd = _battles()
        # One battle, and it must reach BOTH seats: measured 2026-08-20T17:34Z,
        # placer and watcher alike printed -BattleStart:8 and parked in
        # 0x101A10(0xC).
        if len(_bd) != 2 or len(set(_bd)) != 1:
            _say("FAIL: a contested placement owes EVERY seat the same "
                 "@BattleData on (0x43, %d) -- got %r"
                 % (BATTLEDATA_CMD, _bd)); ok = False
        elif not _bd[0].startswith(b"43000C01"):
            # 0x103C0D compares the third envelope byte against [obj+0x18D] and
            # bails with `Errot!!Turn%d!=%d`. Turn 1 is the turn it was played
            # on -- the client does not increment until GamePlayEnd.
            #
            # WARNING: THE HEADER IS A LITERAL ON PURPOSE. Written as
            # `_turn_code(BATTLEDATA_CMD, 1)` this assertion compares the
            # builder against ITSELF and passes with the turn byte hardcoded to
            # zero -- checked, it did. Code 0x43, command 0x0C, turn 0x01.
            _say("FAIL: @BattleData must be 43 00 0C <turn> -- code 0x43, "
                 "command %d, and the turn it was FOUGHT on. Got %r"
                 % (BATTLEDATA_CMD, _bd[0][:8])); ok = False
        else:
            _a = _pipe_vals(_bd[0], b"/A=")
            _d = _pipe_vals(_bd[0], b"/D=")
            _r = _pipe_vals(_bd[0], b"/R=")
            if len(_a) != 5 or len(_d) != 5:
                _say("FAIL: @BattleData is FIVE values a side -- tile, raw "
                     "stat, roll, selector, multiplier -- got /A=%r /D=%r"
                     % (_a, _d)); ok = False
            if len(_r) != 8:
                _say("FAIL: /R= is exactly eight (0x103E52 `cmp esi, 8`), got "
                     "%r" % (_r,)); ok = False
            if (_a[:1], _d[:1]) != ([b"6"], [b"5"]):
                _say("FAIL: occ0 is the TILE, attacker first -- the card just "
                     "placed attacks. Got /A=%r /D=%r" % (_a[:1], _d[:1]))
                ok = False
            if _a[3:] != [b"1", b"1"] or _d[3:] != [b"4", b"1"]:
                _say("FAIL: a type-P attacker selects ATTACK (1) against "
                     "PHYSICAL defence (4), and a card with no ability has "
                     "multiplier 1 -- got /A=%r /D=%r" % (_a[3:], _d[3:]))
                ok = False
            if _a[1] != b"50" or _d[1] != b"10":
                _say("FAIL: the raw stat is the SELECTED stat -- attack 50 vs "
                     "phys def 10, got %r / %r" % (_a[1], _d[1])); ok = False
            if not 0 <= int(_a[2]) <= 50 or not 0 <= int(_d[2]) <= 10:
                _say("FAIL: a roll is bounded by its own raw stat, got %r / %r"
                     % (_a[2], _d[2])); ok = False
            # WARNING: AND THE SERVER'S BOARD MUST AGREE WITH THE NUMBERS IT SENT.
            # This is the one assertion that catches a desync: the clients do
            # not take our word for who won, they compute it themselves from
            # these two rolls (0xD34D0, the higher one takes the tile), so a
            # board that resolved it differently is two players looking at
            # different games with nothing on the wire to say so.
            _own = _MATCH_BOARD.get(("#TM0R001", 1)) or {}
            _hi = int(_a[2]) > int(_d[2])
            _want = (5, 1) if _hi else (6, 0)
            if (_own.get(_want[0]) or tmbattle.Card(_east, None)).owner != _want[1]:
                _say("FAIL: /A= rolled %s and /D= rolled %s, so tile %d belongs "
                     "to seat %d -- the client works that out for itself from "
                     "the same two numbers (0xD34D0). Our board says %r"
                     % (_a[2], _d[2], _want[0], _want[1],
                        {t: c.owner for t, c in sorted(_own.items())}))
                ok = False
        # The board must have moved: one of the two tiles changed hands, and
        # every tile is owned by somebody.
        _sc = _board_scores("#TM0R001", 1, 2)
        if sum(_sc) != 2 or _sc not in ([2, 0], [0, 2], [1, 1]):
            _say("FAIL: two placements make two owned tiles, got %r" % (_sc,))
            ok = False
        if _sc == [1, 1]:
            _say("FAIL: a battle always changes a tile -- 0xD34D0 has no "
                 "outcome that leaves both cards where they were (a DRAW "
                 "re-fights). Got %r" % (_sc,)); ok = False

        # ...AND THE SAME BATTLE THE OTHER WAY UP. The fixture above has a
        # 50-attack card against a 10-defence one, so the attacker wins on
        # essentially every roll -- which means it cannot tell a correct
        # resolver from one that always says ATTACKER. (Checked: it does not.)
        # This one is the mirror: attack 1 into physical defence 100.
        _weak, _wall = b"103|1|0|1|1|0|4|0", b"104|1|0|100|100|0|64|0"
        for _round in range(6):
            _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
            _TURN_RAND.clear(); _PUSHES.clear()
            _remember_match("#TM0R001", 1, _seatsD)
            for _m, _v in _seatsD:
                _MATCH_PEER[_push_key(_m)] = b"UE7QN1N9G"
            _play(1, 0, 6, [_wall])
            _PUSHES.clear()
            _play(0, 1, 5, [_weak])
            _bd2 = _battles()
            if not _bd2:
                _say("FAIL: attack 1 into a wall is still a BATTLE -- both "
                     "cards point at each other, and the arrows are what "
                     "decide that, not the stats"); ok = False
                break
            _a2 = _pipe_vals(_bd2[0], b"/A=")
            _d2 = _pipe_vals(_bd2[0], b"/D=")
            if _a2[1] != b"1" or _d2[1] != b"100":
                _say("FAIL: the raw stats here are attack 1 vs phys def 100, "
                     "got %r / %r" % (_a2[1], _d2[1])); ok = False
                break
            _own2 = _MATCH_BOARD.get(("#TM0R001", 1)) or {}
            _hi2 = int(_a2[2]) > int(_d2[2])
            _t2, _s2 = (6, 0) if _hi2 else (5, 1)
            if (_own2.get(_t2) or tmbattle.Card(_weak, None)).owner != _s2:
                _say("FAIL: /A= rolled %s and /D= rolled %s, so tile %d belongs "
                     "to seat %d -- the losing side is the one that changes "
                     "hands. Our board says %r"
                     % (_a2[2], _d2[2], _t2, _s2,
                        {t: c.owner for t, c in sorted(_own2.items())}))
                ok = False
                break

        # A FREE FLIP IS NOT A BATTLE. The same geometry with the second card
        # pointing away takes the tile with no message at all -- and a server
        # that sent @BattleData here would put the client into a battle scene
        # its own arrow engine never entered.
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
        _TURN_RAND.clear(); _PUSHES.clear()
        _remember_match("#TM0R001", 1, _seatsD)
        for _m, _v in _seatsD:
            _MATCH_PEER[_push_key(_m)] = b"UE7QN1N9G"
        _play(0, 0, 5, [_east])
        _PUSHES.clear()
        _play(1, 1, 6, [_north])
        if _battles():
            _say("FAIL: a card that does NOT point back is taken with no "
                 "battle (0xD3862 writes flag 2, not 1) -- got %r"
                 % (_battles(),)); ok = False
        if _board_scores("#TM0R001", 1, 2) != [1, 1]:
            _say("FAIL: tile 5 points east at tile 6, and tile 6 points north "
                 "-- neither takes the other, so the score is 1/1. Got %r"
                 % (_board_scores("#TM0R001", 1, 2),)); ok = False
        # ...but the placer's own arrow DOES flip: put the north card down
        # first and then attack it from tile 5 with an east arrow.
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
        _TURN_RAND.clear(); _PUSHES.clear()
        _remember_match("#TM0R001", 1, _seatsD)
        for _m, _v in _seatsD:
            _MATCH_PEER[_push_key(_m)] = b"UE7QN1N9G"
        _play(1, 0, 6, [_north])
        _PUSHES.clear()
        _play(0, 1, 5, [_east])
        if _battles():
            _say("FAIL: attacking a card that does not point back is a FLIP")
            ok = False
        if _board_scores("#TM0R001", 1, 2) != [2, 0]:
            _say("FAIL: a free flip hands the tile to the placer -- seat 0 "
                 "should hold both, got %r"
                 % (_board_scores("#TM0R001", 1, 2),)); ok = False

        # --- LUCKY CARDS: the tally, and the Prize Center's own opener ------
        #
        # The board is built by hand rather than played, because `_play` rolls
        # dice and this has to assert an exact take. `placer` is what makes a
        # take legible: tile 6 was PLAYED by seat 1 and is OWNED by seat 0.
        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
        _TURN_RAND.clear(); _PUSHES.clear()
        _remember_match("#TM0R001", 1, _seatsD)
        _bd = _MATCH_BOARD.setdefault(("#TM0R001", 1), {})
        _bd[5] = tmbattle.Card(_east, 0)              # seat 0 played and kept
        _bd[6] = tmbattle.Card(_north, 1)             # seat 1 played it...
        _bd[6].owner = 0                              # ...and seat 0 took it
        _tk = _board_takes("#TM0R001", 1, 2)
        if _tk != [[102], []]:
            _say("FAIL: a TAKE is owner != placer -- seat 0 took card 102 off "
                 "seat 1 and seat 1 took nothing. Got %r" % (_tk,)); ok = False
        if any(c.placer != p for c, p in ((_bd[5], 0), (_bd[6], 1))):
            _say("FAIL: `placer` must not move when `owner` does -- it is the "
                 "only thing that can tell a take from a play"); ok = False

        if tmprize is None:
            _say("FAIL: tmprize did not import -- the Prize Center cannot be "
                 "served and no lucky card can be counted"); ok = False
        else:
            import shutil as _shutil
            import tempfile as _tempfile
            _root = _tempfile.mkdtemp(prefix="tm-selftest-prize-")
            _envsave = {k: os.environ.get(k) for k in
                        ("POL_RESOURCE_DIR", "POL_TM_LUCKY_POOL",
                         "POL_TM_CV_INIT")}
            try:
                os.environ["POL_RESOURCE_DIR"] = _root
                # Force the week's draw to a pool that CONTAINS the taken card,
                # so the tally is deterministic without waiting for a Sunday.
                os.environ["POL_TM_LUCKY_POOL"] = "102,105,106"
                _tally_lucky("#TM0R001", 1, _seatsD)
                _mid0 = _seatsD[0][0]
                if tmprize.pending_lucky(_mid0) != 10:
                    _say("FAIL: taking one of the week's lucky cards is worth "
                         "one card's points, got %r"
                         % (tmprize.pending_lucky(_mid0),)); ok = False
                if tmprize.pending_lucky(_seatsD[1][0]) != 0:
                    _say("FAIL: the player who LOST a lucky card is owed "
                         "nothing"); ok = False

                # ...and the door hands over `@ExcInit`, not the card shop's
                # `@Init`. THIS is the assertion that would have caught the
                # Check Out bug a year earlier.
                os.environ["POL_TM_CV_INIT"] = "excinit"
                _cv = _handle_line(encode_code(MSG_CVREQ)
                                   + b"@CvReq=/NN=0000000000000000/HID=0"
                                     b"/Dm=0/Vol=0/CN=", member_id=_mid0)
                _cv = _cv if isinstance(_cv, list) else [_cv]
                if len(_cv) != 2 or b"@CvEnter=" not in _cv[0]:
                    _say("FAIL: the Prize Center needs BOTH the endpoint "
                         "handshake and a (0xB2, 0x13) push, got %r"
                         % (_cv,)); ok = False
                elif b"@ExcInit=" not in _cv[1]:
                    _say("FAIL: WARNING: the PRIZE CENTER's arm is @ExcInit (rva "
                         "0x1054EE), not the CARD SHOP's @Init -- that is the "
                         "same mistake the Check Out door made. Got %r"
                         % (_cv[1],)); ok = False
                elif _cv[1].count(b"/PP=") != 1 or _cv[1].count(b"/LC=") != 1:
                    _say("FAIL: /PP= and /LC= are PIPE-separated single fields "
                         "-- repeated keys make every indexed read return the "
                         "first value. Got %r" % (_cv[1],)); ok = False
                # The award was PAID by that message, so a second visit in the
                # same week must offer nothing -- announcing twice pays twice.
                if tmprize.pending_lucky(_mid0) != 0:
                    _say("FAIL: opening the Prize Center PAYS the pending "
                         "award; the same week must not still be owed"); ok = False
                _cv2 = _handle_line(encode_code(MSG_CVREQ)
                                    + b"@CvReq=/NN=0000000000000000/HID=0"
                                      b"/Dm=0/Vol=0/CN=", member_id=_mid0)
                _pp2 = (_cv2[1] if isinstance(_cv2, list) else _cv2)
                _pp2 = _pp2.split(b"/PP=")[1].split(b"/")[0].split(b"|")
                if [int(x) for x in _pp2] != [10, 0, 0, 0]:
                    _say("FAIL: the SECOND visit shows the credited balance "
                         "and no pending award -- got %r" % (_pp2,)); ok = False
                # And `off` really is off, so the old behaviour is reachable.
                os.environ["POL_TM_CV_INIT"] = "off"
                _cv3 = _handle_line(encode_code(MSG_CVREQ)
                                    + b"@CvReq=/NN=0000000000000000/HID=0"
                                      b"/Dm=0/Vol=0/CN=", member_id=_mid0)
                if len(_cv3 if isinstance(_cv3, list) else [_cv3]) != 1:
                    _say("FAIL: POL_TM_CV_INIT=off must send the handshake "
                         "alone"); ok = False
            finally:
                for k, v in _envsave.items():
                    if v is None:
                        os.environ.pop(k, None)
                    else:
                        os.environ[k] = v
                _shutil.rmtree(_root, ignore_errors=True)

        _MATCH_HANDS.clear(); _MATCH_TURN.clear(); _MATCH_BOARD.clear()
        _TURN_RAND.clear(); _PUSHES.clear()

    except Exception as exc:
        import traceback as _tb
        _say("FAIL: in-game selftest raised %r -- %s" % (exc, _tb.format_exc()))
        ok = False
    finally:
        (tmroom._FILE, tmroom._OWNER[0]) = saved[0], saved[1]
        for d, v in ((tmroom._TABLES, saved[2]), (tmroom._PEERS, saved[3]),
                     (tmroom._RECORDS, saved[4]), (tmroom._ROOMS_SEQ, saved[5]),
                     (_SEATED, saved_seated), (_PUSHES, saved_pushes),
                     (_MATCH_ROSTER, saved_roster),
                     (_MATCH_ACCEPTS, saved_accepts),
                     (_CARD_READY, saved_ready), (_MATCH_PEER, saved_peer),
                     (_MATCH_HANDS, saved_hands), (_MATCH_TURN, saved_turn),
                     (_MATCH_BOARD, saved_board), (_TURN_RAND, saved_rand)):
            d.clear(); d.update(v)
        _TABLE_CONFIRMED.clear(); tmroom._CONFIRMED.clear(); _TABLE_CONFIRMED.update(saved_conf)
        tmroom._CACHE["mtime"] = -1.0
    return ok


def _selftest_continue():
    """`@Continue=` -- the quorum reply, against the three rules that decide it.

    Not "does it look right": each assertion is a branch in arm 0x104EE1 or in
    the panel that polls it, and each has a failure mode that is invisible on
    the wire.
    """
    ok = True
    seats = [(101, 1), (102, 2)]
    chan, index = "#TMSELF", 1
    saved = dict(_MATCH_CONTINUE), dict(_MATCH_ROSTER), dict(_PUSHES)
    try:
        _MATCH_CONTINUE.clear(); _MATCH_ROSTER.clear(); _PUSHES.clear()
        for mid, _v in seats:
            _MATCH_ROSTER[_push_key(mid)] = (chan, index, seats)

        # Cases 0/0b exercise the LIVE STATUS, which is OFF by default since
        # 15:14Z (it greys the unanswered seat's Yes); pin it under its knob.
        _saved_partial = os.environ.get("POL_TM_CONTINUE_PARTIAL")
        os.environ["POL_TM_CONTINUE_PARTIAL"] = "1"
        # 0. THE LIVE STATUS WAITS FOR THE PANEL (2026-09-07T04:37Z, the Deck's
        #    greyed panel): a vote before the other seat's @Ready pushes it
        #    NOTHING; that seat's @Ready then gets the status once, one reply
        #    behind, framed recipient-first.
        _CONTINUE_READY.clear()
        handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=1/PR=0/Conf=0",
                    member_id=101)
        if any(b"@Continue=" in e[1] for e in (_PUSHES.get(_push_key(102)) or [])):
            _say("FAIL: no live status may be pushed to a seat whose @Ready= has "
                 "not arrived (read after it answers, it bounces the panel)")
            ok = False
        _rdy = handle_line(b"43000E0A@Ready=/Ans=0", member_id=102)
        _rdyl = _rdy if isinstance(_rdy, list) else ([_rdy] if _rdy else [])
        if any(b"@Continue=" in b for b in _rdyl):
            _say("FAIL: the live status must not ride the @Ready= answer itself "
                 "(the panel is built from that reply)"); ok = False
        _q102 = [e for e in (_PUSHES.get(_push_key(102)) or [])
                 if b"@Continue=" in e[1]]
        if len(_q102) != 1 or b"/Ans=0|1" not in _q102[0][1]:
            _say("FAIL: after @Ready= the pending vote's status is queued once for "
                 "the NEXT reply, recipient-first (/Ans=0|1); got %r"
                 % ([(e[0], e[1]) for e in _q102],)); ok = False
        _MATCH_CONTINUE.clear(); _PUSHES.clear(); _CONTINUE_READY.clear()

        # 0b. A DECISIVE GAME: the 1st @Ready= does not open the panel, the
        #     loser's 2nd is HELD during the take and released one reply behind
        #     the @GetSelect push, and the winner's 2nd opens its panel.
        import tempfile as _tf
        _saved_res = os.environ.get("POL_RESOURCE_DIR")
        _saved_hold = os.environ.get("POL_TM_READY_HOLD_LOSER")
        _saved_turn = dict(_MATCH_TURN)
        try:
            os.environ["POL_RESOURCE_DIR"] = _tf.mkdtemp(prefix="tm-rdy-")
            os.environ["POL_TM_READY_HOLD_LOSER"] = "1"
            _row = b"220|55|0|78|80|18|7|255"
            _MATCH_TURN[(chan, index)] = {
                "turn": 9, "active": 1, "n": 2,
                "take": {"win": 0, "lose": 1, "pools": [[_row], [_row]],
                         "roster": [101, 102], "picked": []}}
            handle_line(b"43000E0A@Ready=/Ans=0", member_id=101)   # winner #1
            handle_line(b"43000E0A@Ready=/Ans=0", member_id=102)   # loser #1
            _r2 = handle_line(b"43000E0A@Ready=/Ans=0", member_id=102)  # loser #2
            if _r2 is not None:
                _say("FAIL: the loser's 2nd @Ready= must be HELD during the take, "
                     "got %r" % (_r2,)); ok = False
            if _push_key(101) in (_CONTINUE_READY.get((chan, index)) or set()):
                _say("FAIL: the winner's 1st @Ready= must not open its panel (it "
                     "is going into the take scene)"); ok = False
            handle_line(b"43000F00@GetSelect=/H=0", member_id=101)
            _q102 = [e for e in (_PUSHES.get(_push_key(102)) or [])
                     if b"@Ready=/Go=" in e[1]]
            if len(_q102) != 1 or _q102[0][0] < 1:
                _say("FAIL: the winner's pick must release the loser's held "
                     "@Ready= one reply behind the @GetSelect push, got %r"
                     % ([(e[0], e[1]) for e in _q102],)); ok = False
            handle_line(b"43000E0A@Ready=/Ans=0", member_id=101)   # winner #2
            if _push_key(101) not in (_CONTINUE_READY.get((chan, index)) or set()):
                _say("FAIL: the winner's 2nd @Ready= (after the take) opens its "
                     "panel"); ok = False
        finally:
            _MATCH_TURN.clear(); _MATCH_TURN.update(_saved_turn)
            for _k, _v in (("POL_RESOURCE_DIR", _saved_res),
                           ("POL_TM_READY_HOLD_LOSER", _saved_hold)):
                if _v is None:
                    os.environ.pop(_k, None)
                else:
                    os.environ[_k] = _v
        _MATCH_CONTINUE.clear(); _PUSHES.clear(); _CONTINUE_READY.clear()
        if _saved_partial is None:
            os.environ.pop("POL_TM_CONTINUE_PARTIAL", None)
        else:
            os.environ["POL_TM_CONTINUE_PARTIAL"] = _saved_partial
        # ...and OFF (the default): a vote pushes the other seat NOTHING.
        handle_line(b"43000E0A@Ready=/Ans=0", member_id=102)
        handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=1/PR=0/Conf=0",
                    member_id=101)
        if any(b"@Continue=" in e[1] for e in (_PUSHES.get(_push_key(102)) or [])):
            _say("FAIL: with the live status OFF (default) a vote must push the "
                 "other seat nothing -- the sh>=2 update greys its Yes (41d)")
            ok = False
        _MATCH_CONTINUE.clear(); _PUSHES.clear(); _CONTINUE_READY.clear()

        # 1. THE THIRD ENVELOPE BYTE IS NEVER 0. 0x104FAC is the only site that
        #    sets 0x101A10's return value and it tests exactly this.
        for who, ans in ((101, 1), (102, 1)):
            out = handle_line(_turn_code(CONTINUE_CMD, 0)
                              + b"@Continue=/Ans=%d/PR=0/Conf=0" % ans,
                              member_id=who)
        # The reply may be a LIST: seat 102's vote drains the live-status partial
        # tally we pushed it on 101's vote, alongside the resolution. Find the
        # resolution body (the yes/yes tally, /Ans=1|1) among whatever came back.
        _outl = out if isinstance(out, list) else ([out] if out else [])
        _res = next((b for b in _outl if b"@Continue=" in b and b"/Ans=1|1" in b),
                    None)
        if not _outl:
            _say("FAIL: a completed yes/yes vote produced no reply at all")
            ok = False
        elif _res is None:
            _say("FAIL: the resolution tally (/Ans=1|1) was not in the reply, "
                 "got %r" % (_outl,)); ok = False
        else:
            sh = (struct.unpack("<I", bytes.fromhex(_res[:8].decode()))[0] >> 24) & 0xFF
            if sh == 0:
                _say("FAIL: @Continue= answered with sh=0 -- 0x104FAC parses "
                     "that and then reports 'no answer'"); ok = False
            elif sh != 1:
                _say("FAIL: everyone said yes, so sh must be 1 (state 6's only "
                     "route to the re-deal); got %d" % sh); ok = False

        # 2. THE LIST IS ALWAYS N LONG. 0x104F71 stores without checking the
        #    parse return, so a short list maxes stack garbage into the records.
        _MATCH_CONTINUE.clear(); _PUSHES.clear()
        handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=1",
                    member_id=101)
        out = handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=2",
                          member_id=102)
        # The decliner's reply may be a LIST (its live-status partial from vote 1
        # drains alongside the resolution); the resolution is the sh=2 teardown
        # whose own slot 0 is CONTINUE_FINAL (3).
        _outl = out if isinstance(out, list) else ([out] if out else [])
        out = next((b for b in _outl if b"@Continue=" in b
                    and b.split(b"/Ans=")[1].split(b"/B=")[0].split(b"|")[0]
                    == b"%d" % CONTINUE_FINAL), None)
        if out is None or out.count(b"|") < len(seats) - 1:
            _say("FAIL: the reply must carry one /Ans= per player; got %r"
                 % (_outl,)); ok = False

        # 3. A DECLINER LEAVES ONLY VIA ITS OWN SLOT == 3 (state 7, 0x10D9AD),
        #    and the recipient's own slot is occurrence 0.
        if out is not None:
            vals = out.split(b"/Ans=")[1].split(b"/B=")[0].split(b"|")
            if vals[0] != b"%d" % CONTINUE_FINAL:
                _say("FAIL: the decliner's own slot must be raised to %d or it "
                     "sits on 'Play again?' for ever; got %r"
                     % (CONTINUE_FINAL, vals)); ok = False
            other = _PUSHES.get(_push_key(101), [])
            if not other:
                _say("FAIL: the other player was never told the vote closed")
                ok = False
            else:
                ovals = (other[-1][1].split(b"/Ans=")[1]
                         .split(b"/B=")[0].split(b"|"))
                if ovals[0] != b"%d" % CONTINUE_FINAL:
                    _say("FAIL: every recipient's OWN slot carries the 3, not "
                         "just the decliner's; got %r" % (ovals,)); ok = False

        # 4. A REMATCH RESETS THE GAME STATE -- it never sends @VsGameInit, so
        #    nothing else would.
        _MATCH_CONTINUE.clear(); _PUSHES.clear()
        _MATCH_HANDS[(chan, index)] = {"stale": [b"1|1|1|1|1|1|1|1"]}
        _CARD_READY[(chan, index)] = {_push_key(101), _push_key(102)}
        for who in (101, 102):
            handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=1",
                        member_id=who)
        if (chan, index) in _MATCH_HANDS or (chan, index) in _CARD_READY:
            _say("FAIL: the rematch left the previous game's hands/readiness "
                 "behind -- the next @CardSelect= would deal on one player")
            ok = False
    finally:
        _MATCH_CONTINUE.clear(); _MATCH_CONTINUE.update(saved[0])
        _MATCH_ROSTER.clear(); _MATCH_ROSTER.update(saved[1])
        _PUSHES.clear(); _PUSHES.update(saved[2])
        _MATCH_HANDS.pop((chan, index), None)
        _CARD_READY.pop((chan, index), None)
        _MATCH_TURN.pop((chan, index), None)
        _MATCH_BOARD.pop((chan, index), None)
        _MATCH_OBJECTS.pop((chan, index), None)
        _MATCH_COMBO.pop((chan, index), None)
        _TURN_RAND.pop((chan, index), None)
    return ok


def _selftest_loss_combo():
    """The loss-side combo, pinned to the MEASURED game (2026-08-22T21:14Z).

    The fixture is the live board verbatim: tiles 8/9/10 the COM's, tile 4 the
    human's other card, and the human places tile 5 (card 10, arrows 0x7E)
    into three back-pointing enemies. The engine fights tile 10 (last by
    index, exactly as it did live), the placer LOSES, and the client's
    measured behaviour is the assertion: the placed card flips to the
    defender AND tile 4 -- reached by the beaten card's W arrow -- is
    captured with it (`board3p.log` 17:14:13). The old arm left tile 4 alone,
    which was THE board divergence.
    """
    ok = True
    key = ("#TMSELFTEST", 99)
    saved_board = dict(_MATCH_BOARD)
    try:
        _MATCH_BOARD.clear()
        _MATCH_BOARD[key] = {
            4: tmbattle.Card([21, 5, 0, 5, 5, 0, 0x03, 0], 0),
            8: tmbattle.Card([12, 5, 0, 5, 5, 0, 0x02, 0], 1),
            9: tmbattle.Card([58, 5, 0, 5, 5, 0, 0x07, 0], 1),
            10: tmbattle.Card([3, 90, 0, 90, 90, 0, 0x82, 0], 1),
        }

        class _Seq(object):
            # resolve() draws exactly four randrange(0x8000) values (two per
            # side); 0,0 rolls the 1-stat attacker to 0 and 0x4000,0x4000 the
            # 90-stat defender to 4, so the verdict is DEFENDER by arithmetic,
            # not by luck.
            def __init__(self, vals):
                self.vals = list(vals)

            def randrange(self, bound):
                return self.vals.pop(0) % bound

        battles, _marks = _apply_placement(
            key[0], key[1], 2, 0, 5, b"10|1|0|1|1|0|126|0",
            rnd=_Seq([0, 0, 0x4000, 0x4000]))
        board = _MATCH_BOARD.get(key) or {}
        if len(battles) != 1 or battles[0][1] != 10:
            _say("FAIL: the engine must fight exactly the live game's battle "
                 "-- tile 10, last by index -- got %r"
                 % ([(b[0], b[1], b[3]) for b in battles],))
            ok = False
        elif battles[0][3] != tmbattle.DEFENDER:
            _say("FAIL: the rigged rolls must lose the battle for the placer, "
                 "got verdict %r" % (battles[0][3],))
            ok = False
        if board.get(5) is None or board[5].owner != 1:
            _say("FAIL: the beaten placed card must flip to the defender "
                 "(0xCFAB4)")
            ok = False
        if board.get(4) is None or board[4].owner != 1:
            _say("FAIL: THE LOSS COMBO -- tile 4, reached by the beaten "
                 "card's W arrow, must be captured by the winner (measured "
                 "live 2026-08-22T21:14Z; the old stop-dead arm was the "
                 "board divergence)")
            ok = False
        for t in (8, 9, 10):
            if board.get(t) is not None and board[t].owner != 1:
                _say("FAIL: tile %d was already the winner's and must not "
                     "move" % t)
                ok = False
    finally:
        _MATCH_BOARD.clear()
        _MATCH_BOARD.update(saved_board)
    return ok


def _selftest_deck_names():
    """The two 17-byte save strings are the DECK SET NAMES (2026-09-07).

    A stored `@DN1=` report lands on tab 1 (+0x104) with tab 2 zeroed; the
    base knob moves it; the champion's name must NOT be stamped into +0x104
    by default (a tester's tab read the champion's name) but still is under
    `POL_TM_CHAMPION_SAVE_SLOT=1`; and a stats sync clears a stale stamp --
    the repair path for the saves written on 2026-09-06.
    """
    import tempfile
    ok = True
    m = "dnames"
    env_keys = ("POL_RESOURCE_DIR", "POL_TM_DECK_NAME_BASE",
                "POL_TM_CHAMPION_SAVE_SLOT", "POL_TM_CHAMPION_NAME",
                "POL_TM_SAVE_WRITE", "POL_TM_DECK_NAMES", "POL_TM_DECK_SLOTS")
    saved_env = {k: os.environ.get(k) for k in env_keys}
    if tmsave is None:
        return ok
    try:
        os.environ["POL_RESOURCE_DIR"] = tempfile.mkdtemp(prefix="tm-dn-")
        os.environ["POL_TM_SAVE_WRITE"] = "1"
        os.environ["POL_TM_CHAMPION_NAME"] = "laplacier"
        for k in ("POL_TM_DECK_NAME_BASE", "POL_TM_CHAMPION_SAVE_SLOT",
                  "POL_TM_DECK_NAMES"):
            os.environ.pop(k, None)
        path = _save_file(m)

        def slots():
            b = open(path, "rb").read()
            return (bytes(b[0x104:0x115]).rstrip(b"\x00"),
                    bytes(b[0x115:0x126]).rstrip(b"\x00"))

        _collection_store(m, {"cards": [], "deck_names":
                              "@Decks=/C=0@DN1=/S=434F4F4C204445434B53"})
        if slots() != (b"COOL DECKS", b""):
            _say("FAIL: a stored @DN1= name must land on deck tab 1 (+0x104) "
                 "with tab 2 zeroed, got %r" % (slots(),)); ok = False
        if b"laplacier" in open(path, "rb").read():
            _say("FAIL: the champion's name must NOT be stamped into the save "
                 "by default -- it is the first deck tab (decided 2026-09-07)")
            ok = False
        os.environ["POL_TM_DECK_NAME_BASE"] = "0"
        _collection_to_save(m, [])
        if slots() != (b"", b"COOL DECKS"):
            _say("FAIL: POL_TM_DECK_NAME_BASE=0 must move @DN1= to tab 2, got "
                 "%r" % (slots(),)); ok = False
        os.environ.pop("POL_TM_DECK_NAME_BASE", None)
        # The old behaviour, behind its knob (the positive control).
        os.environ["POL_TM_CHAMPION_SAVE_SLOT"] = "1"
        _collection_to_save(m, [])
        if slots()[0] != b"laplacier":
            _say("FAIL: POL_TM_CHAMPION_SAVE_SLOT=1 must restore the champion "
                 "stamp, got %r" % (slots(),)); ok = False
        os.environ.pop("POL_TM_CHAMPION_SAVE_SLOT", None)
        # The repair: a stats sync alone (no collection change) clears it.
        moved = _save_sync_stats(m)
        if slots() != (b"COOL DECKS", b"") or 0x104 not in (moved or {}):
            _say("FAIL: _save_sync_stats must put the deck names back over a "
                 "stale stamp (the 2026-09-06 repair), got %r moved=%r"
                 % (slots(), sorted((moved or {}).keys()))); ok = False
        # A 17+ byte name is cut to 16 so the NUL survives.
        _collection_store(m, {"cards": [], "deck_names":
                              "@Decks=/C=0@DN1=/S=" + "41" * 20 + "@DN2=/S=42"})
        b = open(path, "rb").read()
        if b[0x104:0x115] != b"A" * 16 + b"\x00" or slots()[1] != b"B":
            _say("FAIL: a long name must be cut to 16 + NUL and @DN2= must be "
                 "tab 2, got %r" % (b[0x104:0x126],)); ok = False
        # THE DECK SLOTS. Cards stored with the old CardPrm column in field 8
        # must land in NO deck; a card-form report places them by its own
        # 7th value (absolute slot); a later report for another deck keeps
        # them; a re-report of the same deck replaces that deck only.
        _collection_store(m, {"cards": [[220, 55, 0, 78, 80, 18, 7, 2],
                                        [215, 90, 2, 100, 100, 23, 1, 3],
                                        [100, 10, 0, 10, 10, 5, 3, 0]]})

        def slot8():
            b = open(path, "rb").read()
            return [b[tmsave.CARDS_OFF + tmsave.REC * i + 8] for i in range(3)]

        if slot8() != [255, 255, 255]:
            _say("FAIL: cards with no reported deck slot must save byte 8 = 255 "
                 "(the CardPrm column scattered them into decks), got %r"
                 % (slot8(),)); ok = False
        if _new_card_slot(2) != 255:
            _say("FAIL: a granted card carries no deck slot"); ok = False
        _collection_set_deck(m, b"@Decks=/C=2@0=/D=220|55|0|78|80|7|3"
                                b"@1=/D=215|90|2|100|100|1|29", sh=1)
        if slot8() != [3, 29, 255]:
            _say("FAIL: a card-form @Decks= report must place cards by its 7th "
                 "value (absolute slot), got %r" % (slot8(),)); ok = False
        _collection_set_deck(m, b"@Decks=/C=1@0=/D=220|55|0|78|80|7|1", sh=1)
        if slot8() != [1, 29, 255]:
            _say("FAIL: re-reporting a card MOVES it (its old slot goes) and "
                 "leaves other decks alone, got %r" % (slot8(),)); ok = False
        # A second card joining the SAME deck must not evict the first (live
        # reports carry one card each).
        _collection_set_deck(m, b"@Decks=/C=1@0=/D=100|10|0|10|10|3|2", sh=1)
        if slot8() != [1, 29, 2]:
            _say("FAIL: placing a second card in a deck must keep the first, "
                 "got %r" % (slot8(),)); ok = False
        _collection_set_deck(m, b"@Decks=/C=1@0=/D=100|10|0|10|10|3|255", sh=1)
        # A first map is SEEDED from the pre-slot verbatim report (03:05Z loss).
        _collection_store(m, {"cards": [[220, 55, 0, 78, 80, 18, 7, 2],
                                        [215, 90, 2, 100, 100, 23, 1, 3],
                                        [100, 10, 0, 10, 10, 5, 3, 0]],
                              "deck": "@Decks=/C=1@0=/D=100|10|0|10|10|3|4"})
        _collection_set_deck(m, b"@Decks=/C=1@0=/D=220|55|0|78|80|7|3", sh=1)
        if slot8() != [3, 255, 4]:
            _say("FAIL: the first slot map must keep the pre-slot report's card "
                 "(slot 4) beside the new placement, got %r" % (slot8(),))
            ok = False
        _collection_set_deck(m, b"@Decks=/C=2@0=/D=220|55|0|78|80|7|3"
                                b"@1=/D=215|90|2|100|100|1|29", sh=1)
        _collection_set_deck(m, b"@Decks=/C=1@0=/D=100|10|0|10|10|3|255", sh=1)
        # A removal is the card reported with slot 255 (measured 03:17:51Z).
        _collection_set_deck(m, b"@Decks=/C=2@0=/D=220|55|0|78|80|7|255"
                                b"@1=/D=215|90|2|100|100|1|255", sh=1)
        if slot8() != [255, 255, 255]:
            _say("FAIL: a card reported with slot 255 must leave its deck, got "
                 "%r" % (slot8(),)); ok = False
        os.environ["POL_TM_DECK_SLOTS"] = "0"
        if _new_card_slot(2) != 2:
            _say("FAIL: POL_TM_DECK_SLOTS=0 restores the old column"); ok = False
        os.environ.pop("POL_TM_DECK_SLOTS", None)
        # A per-tab report MERGES: naming tab 2 must not lose tab 1 (the
        # 02:42Z TEST/RAWR loss).
        _collection_set_deck(m, b"@Decks=/C=0@DN1=/S=54455354")
        _collection_set_deck(m, b"@Decks=/C=0@DN2=/S=52415752")
        if slots() != (b"TEST", b"RAWR"):
            _say("FAIL: a later @DN2= report must merge with the stored @DN1=, "
                 "got %r" % (slots(),)); ok = False
        os.environ["POL_TM_DECK_NAMES"] = "0"
        if _deck_name_fields(m):
            _say("FAIL: POL_TM_DECK_NAMES=0 must author nothing"); ok = False
    except Exception as exc:
        import traceback
        _say("FAIL: deck-names selftest raised %r\n%s"
             % (exc, traceback.format_exc())); ok = False
    finally:
        for k, v in saved_env.items():
            if v is None:
                os.environ.pop(k, None)
            else:
                os.environ[k] = v
    return ok


def _selftest_owner_name():
    """The @GameOwner handoff names the leaver, HEX-ENCODED (0xAAD60)."""
    import tmroom
    ok = True
    chan, index = "#TMOWNER", 1
    left, heir = 7101, 7102
    saved_push = dict(_PUSHES)
    try:
        _PUSHES.clear()
        tmroom.note_name(left, "DeckTestNew")
        _push_owner_change(tmroom, chan, index, left, [(heir, 0x22)])
        body = next((e[1] for e in (_PUSHES.get(_push_key(heir)) or [])
                     if b"@GameOwner=" in e[1]), None)
        want = b"/CN=" + "DeckTestNew".encode("cp932").hex().upper().encode()
        if body is None or want not in body:
            _say("FAIL: @GameOwner must carry the leaver's name as a hex :str "
                 "(0xAAD60 decodes /CN= by hex pairs; raw text drew a blank), "
                 "got %r" % (body,)); ok = False
        if body is not None and b"/CN=DeckTestNew" in body:
            _say("FAIL: /CN= must not be raw text"); ok = False
    finally:
        _PUSHES.clear(); _PUSHES.update(saved_push)
    return ok


def _selftest_card_level():
    """THE CARD LEVEL, and that @ComGameInit no longer sends 0 (2026-09-08).

    The client's 0xB9040: the best grade of each DISTINCT card id, plus
    `type - 1` per row for types above 1, plus 5 per distinct arrow mask.
    """
    ok = True
    rows = [[89, 34, 0, 76, 32, 8, 20, 255],     # two copies, grades 8 and 13
            [89, 34, 0, 76, 32, 13, 20, 255],    # only the 13 counts
            [158, 97, 3, 89, 95, 36, 65, 255],   # type 3 -> +2, new mask -> +5
            [15, 14, 0, 10, 12, 3, 20, 255]]     # mask 20 already seen
    want = 13 + 36 + 3 + 2 + 5 * 2
    got = card_level(rows)
    if got != want:
        _say("FAIL: card_level must be the best grade per DISTINCT id + "
             "(type-1) per row + 5 per distinct arrow mask = %d, got %d"
             % (want, got)); ok = False
    if card_level([[89, 34, 0, 76, 32, 8, 20, 255]] * 9) != 8 + 5:
        _say("FAIL: duplicates of one card must not stack -- the dedupe is by "
             "CARD ID (0xB90FE)"); ok = False
    if card_level([[99999, 1, 0, 1, 1, 200, 1, 255]]) != 0:
        _say("FAIL: a card id past the CardPrm table is skipped whole "
             "(0xB90F6), grade and arrows included"); ok = False
    # ...and the message carries it. A 0 here LOCKS every VS. COM opponent
    # above the first threshold (PlPrm.BIN +0x02 vs struct +0x00).
    saved = os.environ.get("POL_TM_VSGAME_CP")
    saved_dir = os.environ.get("POL_RESOURCE_DIR")
    try:
        os.environ.pop("POL_TM_VSGAME_CP", None)
        import tempfile
        _d = tempfile.mkdtemp(prefix="tmcl")
        os.environ["POL_RESOURCE_DIR"] = _d
        _collection_store("clvl", {"cards": rows, "money": 500})
        body = _comgame_body("clvl", [0] * 7)
        if b"/CP=%d" % want not in body:
            _say("FAIL: @ComGameInit must carry the player's CARD LEVEL in "
                 "/CP= (it lands in struct +0x00, the opponent-unlock gate) "
                 "-- got %r" % (body,)); ok = False
        os.environ["POL_TM_VSGAME_CP"] = "7"
        if b"/CP=7" not in _comgame_body("clvl", [0] * 7):
            _say("FAIL: POL_TM_VSGAME_CP must still force a value"); ok = False
    finally:
        if saved is None:
            os.environ.pop("POL_TM_VSGAME_CP", None)
        else:
            os.environ["POL_TM_VSGAME_CP"] = saved
        if saved_dir is None:
            os.environ.pop("POL_RESOURCE_DIR", None)
        else:
            os.environ["POL_RESOURCE_DIR"] = saved_dir
    return ok



def _selftest_cardselect_leave():
    """A 2-player leave during card select tells the survivor (2026-09-07
    15:28Z): a (0x43, 40) @DataError is pushed to the remaining human and no
    synthetic deal happens."""
    ok = True
    chan, index = "#TMCSLEAVE", 1
    key = (chan, index)
    m0, m1 = "cslA", "cslB"
    seats = [(m0, 0x11), (m1, 0x22)]
    stores = (_MATCH_ROSTER, _MATCH_TURN, _MATCH_HANDS, _MATCH_STARTED,
              _MATCH_BOTS, _PUSHES, _CARD_READY, _MATCH_PEER)
    saved = [(d, dict(d)) for d in stores]
    saved_synth = set(_BOT_SYNTH)
    saved_mode = os.environ.get("POL_TM_CARDSELECT_LEAVE")
    try:
        for d in stores:
            d.clear()
        os.environ["POL_TM_CARDSELECT_LEAVE"] = "dataerror"   # opt-in since 09-07
        _remember_match(chan, index, seats)
        _MATCH_STARTED[key] = {_push_key(m0), _push_key(m1)}
        _CARD_READY[key] = {_push_key(m0)}
        _seat_departed(m1, "selftest: quit at card select")
        q0 = [e[1] for e in (_PUSHES.get(_push_key(m0)) or [])]
        if not any(b"@DataError=" in b for b in q0):
            _say("FAIL: the last human must be pushed @DataError when the "
                 "other seat leaves during card select, got %r" % (q0,))
            ok = False
        if any(b"@StartData=" in b for b in q0) or key in _MATCH_TURN:
            _say("FAIL: no synthetic deal may follow a 2-player card-select "
                 "leave (the survivor is told the match is gone)"); ok = False
        # `getaway`: the survivor is told by the client's own announcer
        # (@GetAway=/P=<abs seat>, cmd 35) AND the seat is dealt and played.
        for d in (_MATCH_TURN, _MATCH_HANDS, _MATCH_BOTS, _PUSHES, _CARD_READY):
            d.clear()
        _BOT_SYNTH.clear()
        os.environ["POL_TM_CARDSELECT_LEAVE"] = "getaway"
        _remember_match(chan, index, seats)
        _MATCH_STARTED[key] = {_push_key(m0), _push_key(m1)}
        _MATCH_HANDS[key] = {_push_key(m0): [b"1|5|0|6|5|2|26|255"] * 5,
                             ("deck", 0): [b"1|5|0|6|5|2|26|255"] * 5}
        _CARD_READY[key] = {_push_key(m0)}
        _seat_departed(m1, "selftest: quit at card select (getaway)")
        q0 = [e[1] for e in (_PUSHES.get(_push_key(m0)) or [])]
        if not any(b"@GetAway=/P=1" in b for b in q0):
            _say("FAIL: getaway mode must push @GetAway=/P=<absolute seat> to "
                 "the survivor, got %r" % (q0,)); ok = False
        if any(b"@DataError=" in b for b in q0):
            _say("FAIL: getaway mode must not also push @DataError"); ok = False
        if key not in _BOT_SYNTH or not (_MATCH_HANDS.get(key) or {}).get(_push_key(m1)):
            _say("FAIL: getaway mode must still deal the departed seat a hand "
                 "(the client continues the match, timer off)"); ok = False
    finally:
        for d, v in saved:
            d.clear(); d.update(v)
        _BOT_SYNTH.clear(); _BOT_SYNTH.update(saved_synth)
        if saved_mode is None:
            os.environ.pop("POL_TM_CARDSELECT_LEAVE", None)
        else:
            os.environ["POL_TM_CARDSELECT_LEAVE"] = saved_mode
    return ok


def _selftest_rotating_turns():
    """THE ROTATING BLOCK TURNS (2026-09-07T14:23Z freeze).

    The frozen 2p game verbatim: a code-15 block on tile 6. Turn 1 the Deck's
    card 148 on tile 9 fights the block (wins; the ray fires SE at empty 11)
    then tile 12 (loses -> 9 is the human's). Turn 3 the Deck's card 24 on tile 3
    beats the block: the ray now fires SW at tile 9 and converts it to the
    Deck (both clients did; the static server fired E at empty 7). Turn 4
    the human's card 47 (N,NE) on tile 13 then points N at a Deck card that points
    back -- exactly ONE battle, which both clients found and the server did
    not. With POL_TM_ROTATING_STEP=0 the old behaviour returns and turn 4
    finds nothing (the freeze).
    """
    ok = True
    key = ("#TMSELFTEST", 96)

    class _Rig(object):
        def __init__(self, script):
            self.script, self.i = list(script), 0

        def randrange(self, bound):
            v = self.script[self.i % len(self.script)]
            self.i += 1
            return bound - 1 if v else 0

    saved_board, saved_turn = dict(_MATCH_BOARD), dict(_MATCH_TURN)
    saved_pend, saved_push = dict(_PENDING_BATTLE), dict(_PUSHES)
    saved_step = os.environ.get("POL_TM_ROTATING_STEP")
    try:
        for step, want4 in (("1", [9]), ("0", [])):
            os.environ["POL_TM_ROTATING_STEP"] = step
            _MATCH_BOARD[key] = {6: tmbattle.object_card(15)}
            _MATCH_TURN[key] = {"turn": 0, "active": 1, "n": 2}
            # turn 0: the human (seat 1) card 58 on tile 12 -- no battle
            _apply_placement(key[0], key[1], 2, 1, 12,
                             b"58|56|1|56|45|14|7|2", rnd=_Rig([1, 1, 0, 0]))
            # turn 1: Deck (seat 0) card 148 on tile 9: beats the block, loses
            # to 12 -> 9 becomes the human's.
            _MATCH_TURN[key]["turn"] = 1
            # Live the Deck CHOSE the block first (@BattleSelect=/B=6), so this
            # turn rides the resumable resolver with that choice; the second
            # battle (vs 12) is then the single follow-up, lost.
            _PENDING_BATTLE.pop(key, None)
            _begin_placement(key[0], key[1], 2, 0, 9, b"148|10|0|10|10|3|114|255",
                             1, [], None, None, [],
                             rnd=_Rig([1, 1, 0, 0, 0, 0, 1, 1]))
            _advance_placement(key[0], key[1], chosen=6)
            if key in _PENDING_BATTLE or _MATCH_BOARD[key][9].owner != 1 \
                    or 12 not in _MATCH_BOARD[key] \
                    or _MATCH_BOARD[key][12].owner != 1:
                _say("FAIL: [step %s] turn 1 (block chosen first, then 12 lost) "
                     "must hand tile 9 to seat 1; pending=%r 9 -> %r"
                     % (step, key in _PENDING_BATTLE,
                        _MATCH_BOARD[key][9].owner)); ok = False
            # turn 2: the human's card 35 on tile 0 -- quiet
            _MATCH_TURN[key]["turn"] = 2
            _apply_placement(key[0], key[1], 2, 1, 0, b"35|20|0|20|20|5|32|255",
                             rnd=_Rig([1, 1, 0, 0]))
            # turn 3: Deck card 24 on tile 3 beats the block -> the ray.
            _MATCH_TURN[key]["turn"] = 3
            _apply_placement(key[0], key[1], 2, 0, 3, b"24|18|1|6|10|2|96|2",
                             rnd=_Rig([1, 1, 0, 0]))
            own9 = _MATCH_BOARD[key][9].owner
            if (step == "1" and own9 != 0) or (step == "0" and own9 != 1):
                _say("FAIL: [step %s] after turn 3 tile 9 must be seat %s's "
                     "(the ray fires SW on turn 3 when the block turns), got %r"
                     % (step, 0 if step == "1" else 1, own9)); ok = False
            # turn 4: the human's card 47 (N, NE) on tile 13.
            _MATCH_TURN[key]["turn"] = 4
            b4, _m = _apply_placement(key[0], key[1], 2, 1, 13,
                                      b"47|21|1|20|20|4|3|255",
                                      rnd=_Rig([1, 1, 0, 0]))
            if [b[1] for b in b4] != want4:
                _say("FAIL: [step %s] turn 4 fought %r, expected %r -- %s"
                     % (step, [b[1] for b in b4], want4,
                        "THE FREEZE: both clients found the battle vs tile 9"
                        if step == "1" else
                        "the static block must reproduce the server's own log"))
                ok = False
    finally:
        _MATCH_BOARD.clear(); _MATCH_BOARD.update(saved_board)
        _MATCH_TURN.clear(); _MATCH_TURN.update(saved_turn)
        _PENDING_BATTLE.clear(); _PENDING_BATTLE.update(saved_pend)
        _PUSHES.clear(); _PUSHES.update(saved_push)
        if saved_step is None:
            os.environ.pop("POL_TM_ROTATING_STEP", None)
        else:
            os.environ["POL_TM_ROTATING_STEP"] = saved_step
    return ok


def _selftest_replay_3p():
    """THE 3-PLAYER FREEZE, replayed (2026-09-06T23:24-23:29Z).

    The fixture is the frozen game verbatim -- seats 0=member 18, 1=member 3,
    2=member 11, every `@PutCard /D=` row and every `@BattleData` verdict out
    of authserv.log -- driven through `_apply_placement` with the rolls rigged
    to the recorded outcomes. Two things are pinned:

      * `POL_TM_COMBO=placed` (the old engine) reproduces the server's own
        `board map after` lines to the tile and finds NO battle on turn 13 --
        the freeze, as the log shows it.
      * `defeated` (the default) diverges at turn 7 and finds exactly ONE
        battle on turn 13, tile 5 vs tile 11 -- which is what BOTH passive
        clients did (`-BattleCheck` -> `-Battle:Select=1` -> `-BattleStart:8`,
        then parked; shim-CASPC.log / shim-STEAMDECK.1.log). Tile 11 is seat
        1's from turn 8 on: the card taken on tile 7 (arrows N,NE,SE,S,SW)
        points SW at 11, and 11 was the loser's.
    """
    ok = True
    key = ("#TMSELFTEST", 97)
    A, D = tmbattle.ATTACKER, tmbattle.DEFENDER
    # (seat, tile, row, (verdict, defender tile) or None)
    game = [
        (2, 4, b"24|16|1|6|10|2|96|2", None),
        (0, 9, b"142|15|1|43|46|9|137|255", None),
        (1, 8, b"89|31|0|60|25|10|7|255", (A, 4)),
        (2, 2, b"26|16|1|5|9|2|40|255", None),
        (0, 3, b"89|31|0|60|25|10|7|255", (A, 4)),
        (1, 7, b"39|34|1|13|18|5|59|255", None),
        (2, 14, b"3|6|1|5|7|2|130|255", None),
        (0, 11, b"86|25|0|30|20|6|130|12", (A, 7)),
        (1, 12, b"59|45|1|45|56|13|163|255", (A, 7)),
        (2, 1, b"1|5|0|6|5|2|26|3", None),
        (0, 6, b"140|23|1|28|31|7|76|255", (D, 12)),
        (1, 0, b"86|29|0|32|11|6|40|255", None),
        (2, 10, b"11|9|1|8|14|3|8|255", None),
        (0, 5, b"46|28|1|15|30|6|104|255", None),      # the frozen turn
    ]
    # authserv.log 2026-09-06 23:24:48 .. 23:29:23, `board map after`:
    server_maps = [
        "4:2", "4:0 9:0", "4:1 8:1 9:1", "2:2 4:1 8:2 9:1",
        "2:2 3:0 4:0 8:2 9:1", "2:1 3:1 4:0 7:1 8:2 9:1",
        "2:1 3:1 4:0 7:1 8:2 9:1 14:2",
        "2:1 3:1 4:0 7:0 8:2 9:1 11:0 14:2",
        "2:1 3:1 4:0 7:1 8:1 9:1 11:0 12:1 14:2",
        "1:2 2:1 3:1 4:0 7:2 8:1 9:1 11:0 12:1 14:2",
        "1:2 2:1 3:1 4:0 6:1 7:1 8:1 9:1 11:0 12:1 14:2",
        "0:1 1:2 2:1 3:1 4:0 6:1 7:1 8:1 9:1 11:0 12:1 14:2",
        "0:1 1:2 2:1 3:1 4:0 6:1 7:1 8:1 9:1 10:2 11:0 12:1 14:2",
    ]
    # The clients' board (the defeated-card chain), turns 0..12:
    client_maps = list(server_maps[:7]) + [
        "2:0 3:0 4:0 7:0 8:2 9:1 11:0 14:2",                  # turn 7: 2,3 taken
        "2:1 3:1 4:0 7:1 8:1 9:1 11:1 12:1 14:2",             # turn 8: 2,3,11
        "1:2 2:1 3:1 4:0 7:2 8:1 9:1 11:1 12:1 14:2",
        "1:2 2:1 3:1 4:0 6:1 7:2 8:1 9:1 11:1 12:1 14:2",     # turn 10: 7 stays 2
        "0:1 1:2 2:1 3:1 4:0 6:1 7:2 8:1 9:1 11:1 12:1 14:2",
        "0:1 1:2 2:1 3:1 4:0 6:1 7:2 8:1 9:1 10:2 11:1 12:1 14:2",
    ]

    class _Rig(object):
        """resolve() draws two randrange(0x8000) per side: bound-1 for the
        side meant to win, 0 for the other (the stat moduli here never
        divide 0x7FFF, so the winner's roll is > 0)."""
        def __init__(self, script):
            self.script, self.i = list(script), 0

        def randrange(self, bound):
            v = self.script[self.i % len(self.script)]
            self.i += 1
            return bound - 1 if v else 0

    def _fmt(board):
        return " ".join("%d:%s" % (t, board[t].owner) for t in sorted(board))

    def _run(mode):
        saved_mode = os.environ.get("POL_TM_COMBO")
        os.environ["POL_TM_COMBO"] = mode
        try:
            _MATCH_BOARD[key] = {}
            out = []
            for turn, (seat, tile, row, v) in enumerate(game):
                rig = _Rig([0, 0, 1, 1] if v and v[0] == D else [1, 1, 0, 0])
                battles, _m = _apply_placement(key[0], key[1], 3, seat, tile,
                                               row, rnd=rig)
                out.append(([(b[1], b[3]) for b in battles],
                            _fmt(_MATCH_BOARD[key])))
            return out
        finally:
            if saved_mode is None:
                os.environ.pop("POL_TM_COMBO", None)
            else:
                os.environ["POL_TM_COMBO"] = saved_mode

    saved_board, saved_combo = dict(_MATCH_BOARD), dict(_MATCH_COMBO)
    try:
        for mode, maps, want13 in (("placed", server_maps, []),
                                   ("defeated", client_maps, [11])):
            got = _run(mode)
            for turn, (seat, tile, row, v) in enumerate(game[:13]):
                fights, board = got[turn]
                want = [(v[1], v[0])] if v else []
                if fights != want:
                    _say("FAIL: [%s] turn %d (seat %d, tile %d) fought %r, the "
                         "log had %r" % (mode, turn, seat, tile, fights, want))
                    ok = False
                if board != maps[turn]:
                    _say("FAIL: [%s] board after turn %d is\n  %s\nexpected\n  %s"
                         % (mode, turn, board, maps[turn])); ok = False
            f13 = [t for t, _v in got[13][0]]
            if f13 != want13:
                _say("FAIL: [%s] turn 13 (card 46, arrows 104, tile 5) fought "
                     "%r, expected %r -- %s"
                     % (mode, f13, want13,
                        "the old engine must reproduce the freeze (no battle "
                        "where every client found one)" if mode == "placed"
                        else "THE FREEZE: both clients ran -BattleCheck -> "
                        "-Battle:Select=1 on tile 5 vs tile 11 and parked for "
                        "a @BattleData the server never sent (2026-09-06T23:29Z)"))
                ok = False
    finally:
        _MATCH_BOARD.clear(); _MATCH_BOARD.update(saved_board)
        _MATCH_COMBO.clear(); _MATCH_COMBO.update(saved_combo)
    return ok


def _selftest_abandon_cleanup():
    """A CRASHED match must not poison the member's next game.

    Three shapes, each the live one: the abandon primitive seat by seat (the
    others keep the match and the departed seat is played by the server,
    the last human wipes it); the DROP path (`release_seats` freeing a seat
    under a running match retires the roster entry -- 23:30:45Z); and the
    reported case end to end: a member holding a stale 3-seat roster starts
    a VS. COM game, is dealt a COM hand, places a card, acks turn 1 and THE
    COM PLAYS. With `POL_TM_ABANDON_CLEANUP=0` the roster survives
    `@GameENC=` and this fails -- the positive control.
    """
    ok = True
    chan, index = "#TMABANDON", 1
    key = (chan, index)
    A, B, C = "abndA", "abndB", "abndC"
    seats = [(A, 0x11), (B, 0x22), (C, 0x33)]
    stores = (_MATCH_ROSTER, _MATCH_TURN, _MATCH_BOARD, _MATCH_HANDS,
              _MATCH_STARTED, _MATCH_BEGAN, _MATCH_BOTS, _PUSHES, _COM_GAME,
              _COM_AT, _MATCH_PEER, _CARD_READY, _MATCH_ACCEPTS, _TURN_RAND,
              _MATCH_COMBO, _PENDING_BATTLE, _MATCH_WATCHERS, _SEATED)
    saved = [(d, dict(d)) for d in stores]
    saved_synth = set(_BOT_SYNTH)
    saved_seat_env = os.environ.get("POL_TM_START_SEAT")

    def crashed_match():
        _remember_match(chan, index, seats)
        _MATCH_STARTED[key] = {_push_key(m) for m, _v in seats}
        _MATCH_BEGAN[key] = time.time()
        _MATCH_TURN[key] = {"turn": 13, "active": 0, "n": 3}
        _MATCH_BOARD[key] = {5: tmbattle.Card(b"46|28|1|15|30|6|104|255", 0)}
        _MATCH_HANDS[key] = {_push_key(m): [b"1|5|0|6|5|2|26|3"]
                             for m, _v in seats}

    def gone(m):
        return _match_of(m) == (None, None, [])

    try:
        for d in stores:
            d.clear()
        # --- 1. the primitive, seat by seat -------------------------------
        crashed_match()
        if not _match_abandoned(A, "selftest: connection dropped"):
            _say("FAIL: _match_abandoned must retire a held roster entry")
            ok = False
        if not gone(A):
            _say("FAIL: the crashed member's roster entry must be retired "
                 "(_match_of(18) kept answering the dead 3-seat game)"); ok = False
        if _match_of(B)[:2] != (chan, index) or _match_of(C)[:2] != (chan, index):
            _say("FAIL: the other players must keep the running match"); ok = False
        if not _is_bot(chan, index, A):
            _say("FAIL: while others remain, the departed seat is played by the "
                 "server (the @Break=/@GameExit= arm)"); ok = False
        if key not in _MATCH_TURN:
            _say("FAIL: the running match must survive the first departure")
            ok = False
        _match_abandoned(B, "selftest: connection dropped")
        _match_abandoned(C, "selftest: connection dropped")
        if (key in _MATCH_TURN or key in _MATCH_BOARD or key in _MATCH_STARTED
                or key in _MATCH_BEGAN or key in _MATCH_HANDS
                or any(v[:2] == key for v in _MATCH_ROSTER.values())):
            _say("FAIL: when the last human is gone the table's match state "
                 "must be wiped (turn/board/hands/started/began/roster)")
            ok = False
        # --- 2. the DROP path: a seat freed under a running match ---------
        crashed_match()
        _SEATED[chan] = {index: list(seats)}
        release_seats(A, chan)
        if not gone(A):
            _say("FAIL: release_seats freeing a seat under the member's match "
                 "must retire their roster entry (the 23:30:45Z drop)"); ok = False
        if _match_of(B)[:2] != (chan, index):
            _say("FAIL: a released seat must not wipe the others' match")
            ok = False
        # --- 3. the reported case: stale roster -> VS. COM -> the COM plays -
        for d in stores:
            d.clear()
        crashed_match()                        # the crash was never processed
        os.environ["POL_TM_START_SEAT"] = "0"
        handle_line(b"41001200@GameENC=/NN=AB12CD4A7D80FA66/HID=0/Dm=0/Vol=0"
                    b"/CN=/HN=4", member_id=A, peer_nick=b"UTESTPEER")
        if not gone(A):
            _say("FAIL: @GameENC= must retire a stale PvP roster -- left "
                 "standing it is the COM-never-plays state"); ok = False
        # The live binding `_bind_com_table` makes from a decodable peer.
        _k = _push_key(A)
        _COM_GAME.setdefault(_k, {})["table"] = (chan, 2)
        _COM_GAME[_k]["member"] = A
        _COM_AT[(chan, 2)] = _k
        handle_line(b"43000600@ComGame=/Rule=0|1|1|1|3|0|0|0/Com=9",
                    member_id=A, peer_nick=b"UTESTPEER")
        if _com_n(A) != 2:
            _say("FAIL: /Com=9 is one COM opponent -> 2 players, got %r"
                 % (_com_n(A),)); ok = False
        handle_line(b"43000700@CardSelect=/C=5"
                    b"@N0=/D=89|31|0|60|25|10|7|255"
                    b"@N1=/D=140|23|1|28|31|7|76|255"
                    b"@N2=/D=86|25|0|30|20|6|130|12"
                    b"@N3=/D=35|20|0|20|20|5|17|255"
                    b"@N4=/D=39|34|1|13|18|5|59|255",
                    member_id=A, peer_nick=b"UTESTPEER")
        ck = (None, _k)
        if len((_MATCH_HANDS.get(ck) or {}).get(("com", 1)) or []) != 5:
            _say("FAIL: the COM must be dealt a hand after a crashed PvP "
                 "match, got %r" % ({k: len(v) for k, v in
                                     (_MATCH_HANDS.get(ck) or {}).items()},))
            ok = False
        _st = _MATCH_TURN.get(ck) or {}
        if _st.get("turn") != 0 or _st.get("active") != 0:
            _say("FAIL: the COM game must deal turn 0 to the human (START_SEAT "
                 "0), got %r" % (_st,)); ok = False
        handle_line(b"43000A00@PutData=/P=0/H=0/F=15", member_id=A,
                    peer_nick=b"UTESTPEER")
        if (_MATCH_TURN.get(ck) or {}).get("await_ack") != 1:
            _say("FAIL: the human's put must announce COM turn 1 ack-gated, "
                 "got %r" % (_MATCH_TURN.get(ck),)); ok = False
        a1 = handle_line(b"43000901@TurnData=/Ans=0", member_id=A,
                         peer_nick=b"UTESTPEER")
        a1l = a1 if isinstance(a1, list) else ([a1] if a1 else [])
        if not any(ln.startswith(b"43000A01") and b"@PutCard=/A=1" in ln
                   for ln in a1l):
            _say("FAIL: THE COM MUST PLAY TURN 1 on the client's ack after an "
                 "abandoned PvP match (43000A01 @PutCard=/A=1), got %r -- the "
                 "23:35:47Z 'no answer for this command' shape" % (a1l,))
            ok = False
    except Exception as exc:
        import traceback
        _say("FAIL: abandon-cleanup selftest raised %r\n%s"
             % (exc, traceback.format_exc())); ok = False
    finally:
        for d, v in saved:
            d.clear(); d.update(v)
        _BOT_SYNTH.clear(); _BOT_SYNTH.update(saved_synth)
        if saved_seat_env is None:
            os.environ.pop("POL_TM_START_SEAT", None)
        else:
            os.environ["POL_TM_START_SEAT"] = saved_seat_env
    return ok


def _selftest_battleselect():
    """The multi-defender `@BattleSelect` handshake (the 2026-09-03 live hang).

    A placement that can attack MORE THAN ONE back-pointing defender must PARK
    the resolver (client scene state 3 -- the picker -- sends `@BattleSelect=
    /B=<tile>` and waits), then resume on the chosen tile, auto-resolve any
    single follow-up, and only THEN finish the turn. A single- or zero-battle
    placement must never park.

    Board (4x4): the placer's card on tile 5 has E+S arrows into two enemies
    that both point back -- tile 6 (W arrow) and tile 9 (N arrow) -- so contest
    yields two BATTLE marks. Rigged rolls make the placer win both.
    """
    ok = True
    key = ("#TMBSELTEST", 7)
    m0, m1 = "bsel0", "bsel1"
    seats = [(m0, 0), (m1, 1)]
    saved_board = dict(_MATCH_BOARD)
    saved_turn = dict(_MATCH_TURN)
    saved_pend = dict(_PENDING_BATTLE)
    saved_push = {k: list(v) for k, v in _PUSHES.items()}
    saved_rand = dict(_TURN_RAND)

    class _Win(object):
        # resolve() draws exactly four randrange(0x8000) values per battle (no
        # ability multiplier here, ability 0), attacker first then defender.
        # The pattern gives the attacker both HIGH draws and the defender both
        # ZERO, so the placer wins every battle by arithmetic, not luck. The
        # defender's stat is clamped to >= 1 (0xD334D), so a zeroed defender
        # would still ROLL and tie -- hence high-vs-zero, not stat tricks.
        def __init__(self):
            self.i = 0

        def randrange(self, bound):
            v = (0x7FFF, 0x7FFF, 0, 0)[self.i % 4]
            self.i += 1
            return v % bound if bound else 0

    try:
        _MATCH_BOARD.clear(); _MATCH_TURN.clear(); _PENDING_BATTLE.clear()
        _PUSHES.clear(); _TURN_RAND.clear()
        _MATCH_BOARD[key] = {
            6: tmbattle.Card(b"2|0|0|0|0|0|64|0", 1),   # W arrow (bit6)
            9: tmbattle.Card(b"3|0|0|0|0|0|1|0", 1),    # N arrow (bit0)
        }
        _MATCH_TURN[key] = {"turn": 0, "active": 0, "n": 2}

        # Placed card: tile 5, E(bit2)+S(bit4) arrows = 0x14 = 20, strong.
        placed = _begin_placement(key[0], key[1], 2, 0, 5,
                                  b"1|15|0|0|0|0|20|0", 0, seats, None, m0,
                                  seats, rnd=_Win())
        if not placed:
            _say("FAIL: @BattleSelect selftest could not place the card")
            return False
        paused = _advance_placement(key[0], key[1])
        if paused is not False:
            _say("FAIL: a 2-defender placement must PARK for @BattleSelect, "
                 "not resolve itself (got %r)" % (paused,)); ok = False
        if key not in _PENDING_BATTLE:
            _say("FAIL: a parked placement must be recorded in _PENDING_BATTLE")
            ok = False
        _bd0 = [e for e in (_PUSHES.get(_push_key(m1)) or [])
                if b"@BattleData=" in e[1]]
        if _bd0:
            _say("FAIL: no @BattleData may be pushed while parked -- the client "
                 "has not chosen yet (got %d)" % len(_bd0)); ok = False

        # The player chooses tile 6. Winning it leaves tile 9 as the lone
        # remaining battle, which auto-resolves (no second @BattleSelect), then
        # the turn finishes.
        done = _advance_placement(key[0], key[1], chosen=6)
        if done is not True:
            _say("FAIL: resolving the chosen battle (and its single follow-up) "
                 "must COMPLETE the placement, got %r" % (done,)); ok = False
        if key in _PENDING_BATTLE:
            _say("FAIL: a completed placement must clear _PENDING_BATTLE")
            ok = False
        _bd = [e for e in (_PUSHES.get(_push_key(m1)) or [])
               if b"@BattleData=" in e[1]]
        if len(_bd) != 2:
            _say("FAIL: two battles (chosen tile 6 + auto tile 9) must push two "
                 "@BattleData, got %d" % len(_bd)); ok = False
        board = _MATCH_BOARD.get(key) or {}
        if not all(board.get(t) is not None and board[t].owner == 0
                   for t in (5, 6, 9)):
            _say("FAIL: the placer won both, so tiles 5/6/9 must all be seat 0, "
                 "got %r" % ({t: (board.get(t) and board[t].owner)
                              for t in (5, 6, 9)},)); ok = False
        # A @TurnData for the next turn must have been announced by _finish().
        _td = [e for e in (_PUSHES.get(_push_key(m1)) or [])
               if b"@TurnData=" in e[1]]
        if not _td:
            _say("FAIL: finishing the placement must announce the next turn")
            ok = False

        # A ONE-defender placement must resolve without ever parking.
        _PENDING_BATTLE.clear(); _PUSHES.clear()
        _MATCH_BOARD[key] = {9: tmbattle.Card(b"3|0|0|0|0|0|1|0", 1)}
        _MATCH_TURN[key] = {"turn": 2, "active": 0, "n": 2}
        _begin_placement(key[0], key[1], 2, 0, 5, b"1|15|0|0|0|0|16|0", 2,
                         seats, None, m0, seats, rnd=_Win())   # S arrow only
        if _advance_placement(key[0], key[1]) is not True:
            _say("FAIL: a single-defender placement must resolve immediately, "
                 "never park"); ok = False
        if key in _PENDING_BATTLE:
            _say("FAIL: a single-defender placement left a pending battle")
            ok = False
    finally:
        _MATCH_BOARD.clear(); _MATCH_BOARD.update(saved_board)
        _MATCH_TURN.clear(); _MATCH_TURN.update(saved_turn)
        _PENDING_BATTLE.clear(); _PENDING_BATTLE.update(saved_pend)
        _PUSHES.clear(); _PUSHES.update(saved_push)
        _TURN_RAND.clear(); _TURN_RAND.update(saved_rand)
    return ok


def selftest():
    """Run the suite with the SHARED ROSTER SNAPSHOT out of reach.

    WARNING: A SELFTEST MUST NOT BE ABLE TO WRITE `tm-roster.json`. `tmroom.selftest`
    carries this guard already, and the reason is on the record: running it
    inside a live container overwrote prod's snapshot with fixture rows on
    2026-08-20 -- phantom tables, the real member records gone, the room
    sequence reset.

    This module had no such guard and did not need one, because nothing it
    called reached `_publish()`. `_table_rules_put` now does (a
    table's rules have to outlive a restart, so they live in the shared file),
    which puts this suite one `_note_table_settings` away from the same
    accident. So the file is redirected to a scratch path for the duration and
    the module state is put back on the way out.

    WARNING: AND THE BOARD OBJECTS ARE PINNED OFF FOR THE SUITE. Nine of the scripted
    match flows below name a HARDCODED tile (`@PutData=/P=0/H=0/F=12` and
    friends) and were written against an always-empty board; once the deal
    started rolling blocks, a placement onto one is correctly REFUSED and those
    tests failed about a third of the time. That flake is the feature working,
    not a bug in it -- so the legacy flows get the old empty board and the one
    test that is ABOUT the objects turns the knob back on for its own duration.
    Anything added here that plays a hardcoded tile inherits the same pin.
    """
    _saved_objects = os.environ.get("POL_TM_BOARD_OBJECTS")
    os.environ["POL_TM_BOARD_OBJECTS"] = "0"
    try:
        return _selftest_locked()
    finally:
        if _saved_objects is None:
            os.environ.pop("POL_TM_BOARD_OBJECTS", None)
        else:
            os.environ["POL_TM_BOARD_OBJECTS"] = _saved_objects


def _selftest_locked():
    """`selftest` with the board pinned -- see its docstring."""
    try:
        import tmroom
    except Exception:
        return _selftest_all()                 # no roster module, no hazard
    saved = (tmroom._FILE, tmroom._OWNER[0],
             json.loads(json.dumps(tmroom._RULES)))
    tmpdir = tempfile.mkdtemp(prefix="tm-selftest-")
    try:
        tmroom._FILE = os.path.join(tmpdir, "tm-roster.json")
        return _selftest_all()
    finally:
        tmroom._FILE, tmroom._OWNER[0] = saved[0], saved[1]
        tmroom._RULES.clear(); tmroom._RULES.update(saved[2])
        tmroom._CACHE["mtime"] = -1.0
        shutil.rmtree(tmpdir, ignore_errors=True)


def _selftest_accept_persist():
    """The accept quorum survives a process restart (2026-09-03T22:57Z prod).

    Two accepts with `_MATCH_ACCEPTS` wiped between them must still reach
    quorum; a persisted set for a DIFFERENT roster, or an old one, must not;
    and the deploy-gate marker must count a pending accept."""
    ok = True
    tmpdir = tempfile.mkdtemp(prefix="tm-accepts-")
    saved_env = os.environ.get("POL_DATA_DIR")
    saved = dict(_MATCH_ACCEPTS)
    chan, index = "#TMACCTEST", 3
    seats = [("acc0", 0), ("acc1", 1)]
    try:
        os.environ["POL_DATA_DIR"] = tmpdir
        _MATCH_ACCEPTS.clear()
        if _note_accept(chan, index, "acc0", seats):
            _say("FAIL: the first accept must HOLD the board"); ok = False
        try:
            with open(os.path.join(tmpdir, "tm-matches-live.json")) as f:
                cnt = int(json.load(f).get("count") or 0)
        except (OSError, ValueError):
            cnt = -1
        if cnt < 1:
            _say("FAIL: a pending accept must count as a live match for the "
                 "deploy gate (count %r)" % cnt); ok = False
        _MATCH_ACCEPTS.clear()                       # <- the restart
        got = _note_accept(chan, index, "acc1", seats)
        if sorted(got) != ["acc0", "acc1"]:
            _say("FAIL: the second accept after a restart must reach quorum "
                 "from the persisted set, got %r" % (got,)); ok = False
        if _accepts_load().get(_accepts_key(chan, index)):
            _say("FAIL: quorum must clear the persisted entry"); ok = False
        _MATCH_ACCEPTS.clear()
        _note_accept(chan, index, "acc0", seats)
        _MATCH_ACCEPTS.clear()
        if _note_accept(chan, index, "acc1", [("acc1", 1), ("acc2", 2)]):
            _say("FAIL: a persisted set for another roster must be ignored")
            ok = False
        _MATCH_ACCEPTS.clear()
        _note_accept(chan, index, "acc0", seats)
        data = _accepts_load()
        data[_accepts_key(chan, index)]["stamp"] = time.time() - 7200
        _accepts_store(data)
        _MATCH_ACCEPTS.clear()
        if _note_accept(chan, index, "acc1", seats):
            _say("FAIL: a stale persisted accept must be ignored"); ok = False
        _MATCH_ACCEPTS.clear()
        _note_accept(chan, index, "acc0", seats)
        _remember_match(chan, index, seats)          # a fresh announcement
        if _accepts_load().get(_accepts_key(chan, index)):
            _say("FAIL: a fresh announcement must forget the persisted set")
            ok = False
    finally:
        _MATCH_ACCEPTS.clear(); _MATCH_ACCEPTS.update(saved)
        if saved_env is None:
            os.environ.pop("POL_DATA_DIR", None)
        else:
            os.environ["POL_DATA_DIR"] = saved_env
        shutil.rmtree(tmpdir, ignore_errors=True)
    if ok:
        _say("selftest: accept quorum persists across a restart")
    return ok


def _selftest_bot_seat():
    """A player who leaves a running PvP match is replaced by the server.

    Mid-turn leave on the leaver's OWN turn plays at once; a later bot turn is
    ack-gated and answers the human's `@TurnData=` ack directly; the roster
    shrink from the leaver's unseat must not wipe the match; the departed
    seat pre-declines the rematch vote."""
    ok = True
    chan, index = "#TMBOTTEST", 2
    key = (chan, index)
    m0, m1 = "bot0", "bot1"
    seats = [(m0, 0), (m1, 1)]
    saved = (dict(_MATCH_ROSTER), dict(_MATCH_TURN), dict(_MATCH_HANDS),
             dict(_MATCH_BOARD), dict(_MATCH_STARTED), dict(_MATCH_BOTS),
             {k: list(v) for k, v in _PUSHES.items()}, dict(_PENDING_BATTLE),
             dict(_MATCH_CONTINUE), set(_BOT_SYNTH), dict(_TURN_RAND),
             dict(_MATCH_COMBO))
    tmpdir = tempfile.mkdtemp(prefix="tm-bot-")
    saved_env = os.environ.get("POL_DATA_DIR")
    row = b"1|15|0|0|0|0|0|0"                       # no arrows: never fights

    def _puts(mid):
        return [e[1] for e in (_PUSHES.get(_push_key(mid)) or [])]
    try:
        os.environ["POL_DATA_DIR"] = tmpdir
        for d in (_MATCH_ROSTER, _MATCH_TURN, _MATCH_HANDS, _MATCH_BOARD,
                  _MATCH_STARTED, _MATCH_BOTS, _PUSHES, _PENDING_BATTLE,
                  _MATCH_CONTINUE, _TURN_RAND, _MATCH_COMBO):
            d.clear()
        _BOT_SYNTH.clear()
        _remember_match(chan, index, seats)
        _MATCH_STARTED[key] = {_push_key(m0), _push_key(m1)}
        _MATCH_HANDS[key] = {_push_key(m0): [row] * 5, _push_key(m1): [row] * 5,
                             ("deck", 0): [row] * 5, ("deck", 1): [row] * 5}
        _MATCH_TURN[key] = {"turn": 3, "active": 1, "n": 2}
        _MATCH_BOARD[key] = {}
        # -- leave on the leaver's own turn: the put goes out at once ---------
        _seat_departed(m1, "selftest")
        if not _is_bot(chan, index, m1):
            _say("FAIL: the leaver must become a server-played seat"); ok = False
        p0 = _puts(m0)
        if not any(b"@PutCard=/A=1" in b for b in p0):
            _say("FAIL: the human must be pushed the departed seat's @PutCard "
                 "(/A=1), got %r" % (p0,)); ok = False
        if not any(b"@TurnData=" in b for b in p0):
            _say("FAIL: the departed seat's put must announce the next turn")
            ok = False
        if _puts(m1):
            _say("FAIL: nothing may be pushed to the departed seat, got %r"
                 % (_puts(m1),)); ok = False
        if len(_MATCH_BOARD.get(key) or {}) != 1:
            _say("FAIL: the bot's card must be on the board"); ok = False
        # -- the leaver's unseat republishes a shrunken roster ----------------
        _remember_match(chan, index, [(m0, 0)])
        if not _MATCH_TURN.get(key) or not _MATCH_BOTS.get(key):
            _say("FAIL: unseating the leaver must not wipe the running match")
            ok = False
        # -- a later bot turn is ack-gated and answers the ack directly -------
        _PUSHES.clear()
        _queue_next_turn(chan, index, seats, 5, 1, roster=seats)
        st = _MATCH_TURN.get(key) or {}
        if st.get("await_ack") != 5 or st.get("bot_seat") != 1:
            _say("FAIL: a departed seat's turn must arm the ack gate, got %r"
                 % (st,)); ok = False
        if any(b"@PutCard=" in b for b in _puts(m0)):
            _say("FAIL: the gated put must not go out before the ack"); ok = False
        reply = _handle_line(_turn_code(TURNDATA_CMD, 5) + b"@TurnData=/Ans=0",
                             member_id=m0)
        if not reply or not reply.startswith(_turn_code(PUTCARD_CMD, 5)) \
                or b"@PutCard=/A=1" not in reply:
            _say("FAIL: the human's ack must be answered with the departed "
                 "seat's @PutCard on (0x43, 10), got %r" % (reply,)); ok = False
        if len(_MATCH_BOARD.get(key) or {}) != 2:
            _say("FAIL: the second bot card must be on the board"); ok = False
        # -- the rematch vote: the departed seat has declined ------------------
        _MATCH_TURN[key]["result_sent"] = True
        creply = _handle_line(_turn_code(CONTINUE_CMD, 0) + b"@Continue=/Ans=1",
                              member_id=m0)
        if not creply or b"@Continue=" not in creply:
            _say("FAIL: with the departed seat pre-declined the vote must "
                 "resolve on the human's answer, got %r" % (creply,)); ok = False
        # -- a leaver DURING CARD SELECT gets a synthetic hand and the deal ---
        for d in (_MATCH_TURN, _MATCH_HANDS, _MATCH_BOARD, _MATCH_BOTS,
                  _PUSHES, _MATCH_CONTINUE, _CARD_READY, _TURN_RAND):
            d.clear()
        _BOT_SYNTH.clear()
        _remember_match(chan, index, seats)
        _MATCH_STARTED[key] = {_push_key(m0), _push_key(m1)}
        _MATCH_HANDS[key] = {_push_key(m0): [row] * 5, ("deck", 0): [row] * 5}
        _CARD_READY[key] = {_push_key(m0)}
        os.environ["POL_TM_CARDSELECT_LEAVE"] = "bot"    # the 3+-human path
        try:
            _seat_departed(m1, "selftest card select")
        finally:
            os.environ.pop("POL_TM_CARDSELECT_LEAVE", None)
        if key not in _BOT_SYNTH:
            _say("FAIL: a card-select leaver must be flagged synthetic"); ok = False
        if not (_MATCH_HANDS.get(key) or {}).get(_push_key(m1)):
            _say("FAIL: a card-select leaver must be dealt a hand"); ok = False
        if (_MATCH_TURN.get(key) or {}).get("turn") is None:
            _say("FAIL: the synthetic pick must complete card select and DEAL")
            ok = False
        if not any(b"@StartData=" in b for b in _puts(m0)):
            _say("FAIL: the human must be dealt (@StartData) after the "
                 "synthetic pick, got %r" % (_puts(m0),)); ok = False
        st = _MATCH_TURN.get(key) or {}
        if st.get("active") == 1 and st.get("await_ack") is None:
            _say("FAIL: when the departed seat starts, its turn must be "
                 "ack-gated"); ok = False
    finally:
        for d, s in ((_MATCH_ROSTER, saved[0]), (_MATCH_TURN, saved[1]),
                     (_MATCH_HANDS, saved[2]), (_MATCH_BOARD, saved[3]),
                     (_MATCH_STARTED, saved[4]), (_MATCH_BOTS, saved[5]),
                     (_PUSHES, saved[6]), (_PENDING_BATTLE, saved[7]),
                     (_MATCH_CONTINUE, saved[8]), (_TURN_RAND, saved[10]),
                     (_MATCH_COMBO, saved[11])):
            d.clear(); d.update(s)
        _BOT_SYNTH.clear(); _BOT_SYNTH.update(saved[9])
        _CARD_READY.pop(key, None)
        if saved_env is None:
            os.environ.pop("POL_DATA_DIR", None)
        else:
            os.environ["POL_DATA_DIR"] = saved_env
        shutil.rmtree(tmpdir, ignore_errors=True)
    if ok:
        _say("selftest: a departed seat is played by the server")
    return ok


def _selftest_dataerror():
    """A card select for a match this process has no roster for is answered
    with (0x43, 40) `@DataError=` -- the one message the card-select scene
    reads for "the match is gone" (it then sends `@Quit=` and leaves) -- and
    a roster-known pick is NOT."""
    ok = True
    saved_push = {k: list(v) for k, v in _PUSHES.items()}
    saved_ready = dict(_CARD_READY)
    m = "lostmatch"
    try:
        _PUSHES.pop(_push_key(m), None)
        _handle_line(encode_code(IN_MATCH_CODE | (CARDSELECT_SLOT << 16))
                     + b"@CardSelect=/C=0", member_id=m)
        got = [e[1] for e in (_PUSHES.get(_push_key(m)) or [])]
        want = _turn_code(DATAERROR_CMD, 0) + b"@DataError=/No=0"
        if want not in got:
            _say("FAIL: a no-roster card select must push %r, got %r"
                 % (want, got)); ok = False
        # Positive control: with a roster the pick must NOT be refused.
        _PUSHES.pop(_push_key(m), None)
        saved_roster = dict(_MATCH_ROSTER)
        try:
            _remember_match("#TMDETEST", 1, [(m, 0), ("lostmate", 1)])
            _handle_line(encode_code(IN_MATCH_CODE | (CARDSELECT_SLOT << 16))
                         + b"@CardSelect=/C=0", member_id=m)
            got = [e[1] for e in (_PUSHES.get(_push_key(m)) or [])]
            if any(b"@DataError=" in b for b in got):
                _say("FAIL: a card select WITH a roster must not be refused")
                ok = False
        finally:
            _MATCH_ROSTER.clear(); _MATCH_ROSTER.update(saved_roster)
            _CARD_READY.pop(("#TMDETEST", 1), None)
    finally:
        _PUSHES.clear(); _PUSHES.update(saved_push)
        _CARD_READY.clear(); _CARD_READY.update(saved_ready)
    if ok:
        _say("selftest: a lost match is told to the card-select scene")
    return ok


def _selftest_champion():
    """The rank_1 Pack is labelled after the champion: `/SN=` on the shop
    opener, absent without one, and a cp932 name never split mid-character or
    past 16 bytes. The save +0x104 write below exercises `write_field` only --
    that slot is the first DECK TAB and the rewrite path no longer stamps it
    (`_selftest_deck_names`)."""
    ok = True
    saved = os.environ.get("POL_TM_CHAMPION_NAME")
    try:
        os.environ["POL_TM_CHAMPION_NAME"] = ""
        if b"/SN=" in _shopinit_body(1):
            _say("FAIL: no champion, no /SN="); ok = False
        os.environ["POL_TM_CHAMPION_NAME"] = "Fox"
        body = _shopinit_body(1)
        if b"/SN=Fox/" not in body and not body.endswith(b"/SN=Fox"):
            _say("FAIL: the champion's name must ride @Init=/SN=, got %r"
                 % (body,)); ok = False
        if body.index(b"/SN=") > body.index(b"/PM=") if b"/PM=" in body else False:
            _say("FAIL: /SN= must precede the trailing /PM="); ok = False
        os.environ["POL_TM_CHAMPION_NAME"] = "ABCDEFGHIJKLMNOPQRSTUV"
        if len(_champion_name()) != 16:
            _say("FAIL: the name must be cut to the client's 16 bytes, got %r"
                 % (_champion_name(),)); ok = False
        os.environ["POL_TM_CHAMPION_NAME"] = "カス" * 5     # 10 kana = 20 bytes
        _n = _champion_name()
        if len(_n) != 16 or len(_n) % 2:
            _say("FAIL: a double-byte name must be cut on a character boundary, "
                 "got %d bytes" % len(_n)); ok = False
        if tmsave is not None:
            os.environ["POL_TM_CHAMPION_NAME"] = "Fox"
            buf = bytearray(tmsave.build([]))
            tmsave.write_field(buf, SAVE_OFF_CHAMPION, SAVE_CHAMPION_WIDTH,
                               _champion_name())
            got = bytes(buf[SAVE_OFF_CHAMPION:SAVE_OFF_CHAMPION + SAVE_CHAMPION_WIDTH])
            if got != b"Fox".ljust(SAVE_CHAMPION_WIDTH, b"\x00"):
                _say("FAIL: save +0x104 must carry the NUL-padded name, got %r"
                     % (got,)); ok = False
    finally:
        if saved is None:
            os.environ.pop("POL_TM_CHAMPION_NAME", None)
        else:
            os.environ["POL_TM_CHAMPION_NAME"] = saved
    if ok:
        _say("selftest: the champion names the rank_1 Pack")
    return ok


def _selftest_watch_gap():
    """The observer purge-race gap, and its TWO-SIDED constraint (2026-09-06).

    A watcher's per-turn render (@PutCard/@BattleData/@WFCard) must be DELIVERED
    after the @TurnData that enters its own turn N and before the @TurnData that
    enters turn N+1. Land it before turn N's @TurnData and the client reads a
    card for a turn it has not entered (Errot!!Turn(N-1)!=N -- the original
    after=0 crash); land it after turn N+1's @TurnData and the client has already
    advanced (Errot!!Turn(N+1)!=N -- the crash after=2 CAUSED live).

    This SIMULATES the real drain sequence -- turn N's @TurnData is queued a
    cycle earlier than the render, turn N+1's a beat later -- and asserts the
    ORDER `_pending_pushes` actually hands the watcher, which is what an
    after-value assertion alone got wrong the first time.
    """
    ok = True
    saved_pushes = dict(_PUSHES)
    saved_watchers = dict(_MATCH_WATCHERS)
    saved_env = os.environ.get("POL_TM_WATCH_PUT_GAP")
    chan, index, wmid, wpeer = "#TMWATCHGAP", 1, 4242, b"WATCHERPEER"

    def _cmd_of(body):
        try:
            return (int(body[:8], 16) >> 8) & 0xFF
        except (ValueError, TypeError):
            return -1

    def _delivery_order():
        # Drain the watcher's queue reply-by-reply, collecting the cmd order.
        order = []
        for _ in range(12):
            for body in _pending_pushes(wmid, wpeer):
                order.append(_cmd_of(body))
        return order

    def _run_one_turn():
        # The WORST case: the watcher has NOT polled since turn N's @TurnData was
        # queued, so all three of turn N's @TurnData, turn N's render, and turn
        # N+1's @TurnData are pending at once -- in that queue order (the order
        # the COM loop and the human-put handler emit them). Correct delivery
        # must be decided by LIST ORDER, which only happens when every one rides
        # the SAME `after`; a render that rides lower jumps its own turn, one
        # that rides higher falls behind the next turn. No intermediate drain,
        # precisely because a watcher's polls are decoupled from the cycle.
        _PUSHES.clear()
        _watch_push(chan, index,
                    _turn_code(TURNDATA_CMD, 3) + b"@TurnData=/A=0",
                    "turn 3 (N)", after=1)
        _watch_push(chan, index,
                    _turn_code(PUTCARD_CMD, 3) + b"@PutCard=/A=0", "card N")
        _watch_push(chan, index,
                    _turn_code(BATTLEDATA_CMD, 3) + b"@BattleData=/A=0",
                    "battle N")
        _watch_push(chan, index,
                    _turn_code(TURNDATA_CMD, 4) + b"@TurnData=/A=1",
                    "turn 4 (N+1)", after=1)
        return _delivery_order()

    try:
        _MATCH_WATCHERS.clear()
        _MATCH_WATCHERS[(chan, index)] = {_push_key(wmid): (wmid, wpeer)}

        # --- default gap: turn N's TD, then the render, then turn N+1's TD ---
        os.environ.pop("POL_TM_WATCH_PUT_GAP", None)
        order = _run_one_turn()
        # Expected: TD(N)=9, then PutCard=10 / Battle=12, then TD(N+1)=9.
        put_i = order.index(PUTCARD_CMD) if PUTCARD_CMD in order else -1
        bat_i = order.index(BATTLEDATA_CMD) if BATTLEDATA_CMD in order else -1
        td_first = order.index(TURNDATA_CMD) if TURNDATA_CMD in order else -1
        td_next = (order.index(TURNDATA_CMD, td_first + 1)
                   if order.count(TURNDATA_CMD) > 1 else -1)
        if td_first < 0 or td_next < 0 or put_i < 0 or bat_i < 0:
            _say("FAIL: watcher did not receive both @TurnData plus the render "
                 "-- order %r" % (order,)); ok = False
        else:
            if not (td_first < put_i < td_next):
                _say("FAIL: @PutCard must land AFTER turn N's @TurnData and "
                     "BEFORE turn N+1's -- order %r (td_N=%d put=%d td_N+1=%d)"
                     % (order, td_first, put_i, td_next)); ok = False
            if not (td_first < bat_i < td_next):
                _say("FAIL: @BattleData must land inside turn N's window -- "
                     "order %r" % (order,)); ok = False

        # --- after=0 (the original crash) DOES overtake turn N's @TurnData ---
        # A positive control: the bug the fix ends must still be reproducible.
        os.environ["POL_TM_WATCH_PUT_GAP"] = "0"
        order0 = _run_one_turn()
        p0 = order0.index(PUTCARD_CMD) if PUTCARD_CMD in order0 else 99
        t0 = order0.index(TURNDATA_CMD) if TURNDATA_CMD in order0 else -1
        if not (p0 < t0):
            _say("FAIL: the positive control is broken -- after=0 should race "
                 "the @PutCard AHEAD of turn N's @TurnData, order %r"
                 % (order0,)); ok = False
    finally:
        _PUSHES.clear(); _PUSHES.update(saved_pushes)
        _MATCH_WATCHERS.clear(); _MATCH_WATCHERS.update(saved_watchers)
        if saved_env is None:
            os.environ.pop("POL_TM_WATCH_PUT_GAP", None)
        else:
            os.environ["POL_TM_WATCH_PUT_GAP"] = saved_env
    return ok


def _selftest_watch_snapshot_purge():
    """A `@WatchInfo` snapshot must LEAVE NO per-turn body queued behind it.

    KEY: WITH A POSITIVE CONTROL, because an assertion whose fixture cannot
    violate it is not a test (the `/CP=` selftest sat green for nineteen days
    over the opposite behaviour). The fixture builds exactly the live backlog --
    a previous game's tail plus the new game's turn-0 bodies -- and the control
    re-runs it with `POL_TM_WATCH_SNAPSHOT_PURGE=0` and requires the stale put
    to STILL be delivered. If the purge stops working, the control is what says
    so.
    """
    ok = True
    saved_pushes = dict(_PUSHES)
    saved_env = os.environ.get("POL_TM_WATCH_SNAPSHOT_PURGE")
    wmid, wpeer = 4243, b"SNAPPEER"

    def _cmd_of(body):
        try:
            return (int(body[:8], 16) >> 8) & 0xFF
        except (ValueError, TypeError):
            return -1

    def _load_backlog():
        # The 2026-09-09 shape: a stale tail the watcher never drained, then the
        # new game's turn-0 stream -- all of it queued BEFORE the handshake.
        _PUSHES.clear()
        _queue_push(wmid, _turn_code(TURNDATA_CMD, 8) + b"@TurnData=/A=1",
                    "stale: prior game turn 8", peer=wpeer)
        _queue_push(wmid, _turn_code(RESULTDATA_CMD, 10) + b"@ResultData=/P=1",
                    "stale: prior game result", peer=wpeer)
        _queue_push(wmid, _turn_code(TURNDATA_CMD, 0) + b"@TurnData=/A=1",
                    "stale: new game turn 0", peer=wpeer)
        _queue_push(wmid, _turn_code(PUTCARD_CMD, 0) + b"@PutCard=/A=1/F=5",
                    "stale: THE turn-0 put that crashed it", peer=wpeer)
        _queue_push(wmid, _turn_code(BATTLEDATA_CMD, 0) + b"@BattleData=/A=5",
                    "stale: turn-0 battle", peer=wpeer)
        # NOT per-turn state: must SURVIVE the purge.
        _queue_push(wmid, _turn_code(5, 0) + b"@Quit=/No=0/B=0",
                    "not per-turn: @Quit", peer=wpeer)

    def _drain():
        order = []
        for _ in range(12):
            for body in _pending_pushes(wmid, wpeer):
                order.append(_cmd_of(body))
        return order

    try:
        os.environ.pop("POL_TM_WATCH_SNAPSHOT_PURGE", None)
        _load_backlog()
        went = _watch_stream_purge(wmid, "selftest")
        if went != 5:
            _say("FAIL: the snapshot purge must drop the 5 per-turn bodies, "
                 "dropped %d" % went); ok = False
        order = _drain()
        for cmd, name in ((PUTCARD_CMD, "@PutCard"), (TURNDATA_CMD, "@TurnData"),
                          (BATTLEDATA_CMD, "@BattleData"),
                          (RESULTDATA_CMD, "@ResultData")):
            if cmd in order:
                _say("FAIL: %s survived the @WatchInfo snapshot purge -- it "
                     "would reach the client behind the snapshot and be read "
                     "as a card for a turn it has passed (Errot!!Turn). "
                     "order %r" % (name, order)); ok = False
        if 5 not in order:
            _say("FAIL: the purge ate a NON per-turn push (@Quit) -- it must "
                 "only drop what the snapshot restates, order %r"
                 % (order,)); ok = False

        # --- positive control: with the purge OFF the crash is reproducible ---
        os.environ["POL_TM_WATCH_SNAPSHOT_PURGE"] = "0"
        _load_backlog()
        if _watch_stream_purge(wmid, "selftest control") != 0:
            _say("FAIL: POL_TM_WATCH_SNAPSHOT_PURGE=0 must not purge"); ok = False
        order0 = _drain()
        if PUTCARD_CMD not in order0:
            _say("FAIL: the positive control is broken -- with the purge off "
                 "the stale turn-0 @PutCard must still be delivered, order %r"
                 % (order0,)); ok = False
    finally:
        _PUSHES.clear(); _PUSHES.update(saved_pushes)
        if saved_env is None:
            os.environ.pop("POL_TM_WATCH_SNAPSHOT_PURGE", None)
        else:
            os.environ["POL_TM_WATCH_SNAPSHOT_PURGE"] = saved_env
    return ok


def _selftest_all():
    """Assert against the LIVE captured opener, not just our own output -- the
    same discipline that would have caught the janwire endianness bug."""
    ok = True
    ok = _selftest_watch_gap() and ok
    ok = _selftest_watch_snapshot_purge() and ok
    ok = _selftest_accept_persist() and ok
    ok = _selftest_bot_seat() and ok
    ok = _selftest_dataerror() and ok
    ok = _selftest_champion() and ok
    # THE BLOCK WRITERS FIRST -- this is the assertion the 2026-08-20 crash did
    # not have, and it is placed where it cannot be discarded by a later `ok =`.
    ok = _selftest_blocks() and ok
    # THE BOARD AND THE BATTLE ARITHMETIC, before anything that uses them: a
    # wrong direction order or a wrong roll is invisible on the wire and shows
    # up only as a match that plays itself wrong.
    ok = tmbattle.selftest(say=_say) and ok
    ok = _selftest_packs() and ok
    ok = _selftest_seating() and ok
    ok = _selftest_cancel() and ok
    ok = _selftest_heartbeat() and ok
    ok = _selftest_seat_move() and ok
    ok = _selftest_audit() and ok
    ok = _selftest_reconcile() and ok
    ok = _selftest_gameml_format() and ok
    ok = _selftest_peer_index() and ok
    ok = _selftest_peer_class() and ok
    ok = _selftest_value0() and ok
    ok = _selftest_ingame() and ok
    ok = _selftest_loss_combo() and ok
    ok = _selftest_replay_3p() and ok
    ok = _selftest_rotating_turns() and ok
    ok = _selftest_card_level() and ok
    ok = _selftest_cardselect_leave() and ok
    ok = _selftest_owner_name() and ok
    ok = _selftest_abandon_cleanup() and ok
    ok = _selftest_deck_names() and ok
    ok = _selftest_battleselect() and ok
    ok = _selftest_continue() and ok
    ok = _selftest_chat() and ok
    # logs/authserv.log 2026-08-13: b'GE1000000@TeachDV=' is payload (class 'G'
    # + E-body). The E-body handed to handle_line is payload[1:].
    LIVE_EBODY = b"E1000000@TeachDV="

    code, cmd = decode_ebody(LIVE_EBODY)
    _say("decode:", code, cmd)
    if code != MSG_TEACHDV:
        _say("FAIL: opener code %r, expected %d" % (code, MSG_TEACHDV)); ok = False
    if cmd != b"@TeachDV=":
        _say("FAIL: command %r" % cmd); ok = False

    # The header must round-trip the client's own bytes exactly.
    if encode_code(MSG_TEACHDV) != b"E1000000":
        _say("FAIL: encode_code(225) = %r" % encode_code(MSG_TEACHDV)); ok = False
    if encode_code(MSG_TEACHDVANS) != b"E2000000":
        _say("FAIL: encode_code(226) = %r" % encode_code(MSG_TEACHDVANS)); ok = False

    reply = handle_line(LIVE_EBODY[1:] if LIVE_EBODY[:1] == b"G" else LIVE_EBODY)
    _say("reply:", reply)
    if not reply or not reply.startswith(b"E2000000"):
        _say("FAIL: reply must carry code 226"); ok = False
    if b"@TeachDVAns" not in reply or b"/D=" not in reply or b"/V=" not in reply:
        _say("FAIL: reply must contain @TeachDVAns/D=/V="); ok = False
    # The `=` straight after the name is the PC container-parser's key delimiter
    # (banner above `_teachdvans_body`). Without it the key becomes
    # `@TeachDVAns/D`, every lookup misses, and NOTHING is parsed -- invisible on
    # the wire, and it cost this investigation a fortnight. Assert the byte.
    if b"@TeachDVAns=" not in reply:
        _say("FAIL: the PC key delimiter `=` is missing after @TeachDVAns"); ok = False

    # THE PC FIELD SET. The PC build's parser (unpacked TM.dll 0x053c134d) reads
    # `/AC=` and `/RK=` TWICE each and `/Shm=` THREE times, by occurrence index --
    # so these counts, not the field order, are what make the reply valid. Getting
    # them wrong is invisible on the wire and shows up only as TRM-0-37088
    # "Timed out while reading default data" many seconds later, so assert them.
    for field, want in ((b"/AC=", 2), (b"/RK=", 2), (b"/Shm=", 3)):
        if reply.count(field) != want:
            _say("FAIL: %r appears %d time(s), the PC parser reads %d"
                  % (field, reply.count(field), want)); ok = False

    # `ext=0` must restore the PS2-only reply exactly.
    import tempfile as _tf
    global _TEACH_FILE
    _keep = _TEACH_FILE
    try:
        _TEACH_FILE = os.path.join(_tf.gettempdir(), "tm_teach_selftest.txt")
        with open(_TEACH_FILE, "w") as f:
            f.write("0 0\next=0\n")
        plain = handle_line(LIVE_EBODY[1:])
        if plain != b"E2000000@TeachDVAns=/D=0/V=0":
            _say("FAIL: ext=0 must give the old reply, got %r" % plain); ok = False
    finally:
        _TEACH_FILE = _keep

    # Unknown commands stay silent (capture, don't invent). `@Init=` is NOT the
    # example to use here -- it grew a real handler on 2026-08-15 and this
    # assertion kept failing against it, which is why the selftest was red.
    if handle_line(b"E3000000@ThereIsNoSuchCommand=") is not None:
        _say("FAIL: undecoded command must stay silent"); ok = False
    if handle_line(b"E3000000@Init=") is None:
        _say("FAIL: @Init= is handled now and must answer"); ok = False

    # THE TRADE BOARD ENTRY (live-corrected 2026-08-22 by a tester's dump: the
    # members-list trade uses the (0xA2,1) board, not the @TrID/@Init handshake).
    # PARTIAL: LENGTH-A/B DEFAULTS (2026-08-23 late): a paired accept queues EXACTLY
    # two pushes, in order: (0xA2,1) @EventGameInit then ONE SHORT (0xA2,26)
    # @Card list (cap 10 cards -- the crash tracks logged-line length, see
    # note_trade_accept). Never a second 26, never a terminator/partner list.
    # Numeric ids: the @Pong path below runs the seat check, which int()s the
    # member id -- a string id would throw where a real member never can.
    # WARNING: THE AUTHORED SERVE IS NO LONGER THE DEFAULT (2026-08-24 -- see
    # note_trade_accept), so this block has to ARM IT EXPLICITLY. The machinery
    # is still correct and still reachable with POL_TM_TRADE_SERVE=1 for an A/B,
    # and a test that silently passed because the feature was off would be worse
    # than no test: it is exactly how a disabled path rots.
    _serve_keep = os.environ.get("POL_TM_TRADE_SERVE")
    os.environ["POL_TM_TRADE_SERVE"] = "1"
    _ta, _tb = 987001, 987002
    note_trade_accept((_ta, _tb, b"UPARTNERB", "acc"),
                      (_tb, _ta, b"UPARTNERA", "ini"))
    _due = _pending_pushes(_ta)
    _a2_1, _26 = encode_code(0xA2 | (1 << 16)), encode_code(0xA2 | (26 << 16))
    if len(_due) != 3:
        _say("FAIL: the ACCEPTER's serve must queue (0xA2,1) + own /P=0 + "
             "partner /P=1 lists (got %d push(es))" % len(_due)); ok = False
    else:
        if not _due[0].startswith(_a2_1) \
                or b"@EventGameInit=/N=2" not in _due[0]:
            _say("FAIL: first push must be (0xA2,1) @EventGameInit=/N=2, "
                 "got %r" % (_due[0][:40],)); ok = False
        if not _due[1].startswith(_26) or b"/P=0" not in _due[1]:
            _say("FAIL: second push must be the own /P=0 list, got %r"
                 % (_due[1][:40],)); ok = False
        if not _due[2].startswith(_26) or b"/P=1" not in _due[2]:
            _say("FAIL: third push must be the partner /P=1 list (the scene "
                 "waits on it after consuming /P=0 -- measured 01:11Z), "
                 "got %r" % (_due[2][:40],)); ok = False
    _dueb = _pending_pushes(_tb)
    if len(_dueb) != 2 or not all(d.startswith(_26) for d in _dueb):
        _say("FAIL: the INITIATOR's serve is the two (0xA2,26) lists ONLY "
             "(its scene never polls (0xA2,1)); got %r"
             % ([d[:20] for d in _dueb],)); ok = False
    # THE RE-SERVE: a @Pong while armed re-queues the entry (racing the
    # client's one-shot @TrAns RecvCLEAR purge); it is gap-limited, so wind
    # the clock back to simulate the next heartbeat.
    if _pending_pushes(_ta):
        _say("FAIL: nothing may be due before the re-serve gap"); ok = False
    _st = _TRADE_PENDING.get(_push_key(_ta))
    if not _st or _st.get("left") != _env_int("POL_TM_TRADE_RESERVE_N", 4):
        _say("FAIL: re-serves must be armed after the accept"); ok = False
    else:
        _st["last"] -= 10
        _pong = handle_line(b"1E000000@Pong=/Cnt=0/RM=0", member_id=_ta)
        _pp = [p for p in (_pong or []) if p.startswith(_a2_1)
               or p.startswith(_26)]
        if len(_pp) != 2 or not all(p.startswith(_26) for p in _pp):
            _say("FAIL: a heartbeat while armed must re-serve the two CARD "
                 "LISTS only (neither role polls (0xA2,1) -- measured); "
                 "got %r" % ([p[:20] for p in _pp],)); ok = False
        if _st.get("left") != _env_int("POL_TM_TRADE_RESERVE_N", 4) - 1:
            _say("FAIL: a re-serve must decrement the budget"); ok = False
        _pong2 = handle_line(b"1E000000@Pong=/Cnt=0/RM=0", member_id=_ta)
        if [p for p in (_pong2 or []) if p.startswith(_26)]:
            _say("FAIL: re-serves must respect the 3s gap"); ok = False
    note_trade_cancel(_ta, _tb)
    if _serve_keep is None:
        os.environ.pop("POL_TM_TRADE_SERVE", None)
    else:
        os.environ["POL_TM_TRADE_SERVE"] = _serve_keep
    # ...and prove the DEFAULT is off, which is what production now runs.
    note_trade_accept((_ta, _tb, b"UPARTNERB", "acc"))
    if _pending_pushes(_ta):
        _say("FAIL: with POL_TM_TRADE_SERVE unset the authored board serve "
             "must author NOTHING -- the go flag is the accept path now")
        ok = False
    note_trade_cancel(_ta, _tb)
    if _TRADE_PENDING.get(_push_key(_ta)) is not None:
        _say("FAIL: a cancel must disarm the re-serve"); ok = False

    # THE LINE-LENGTH INVARIANT (the A/B's whole point): even against an
    # oversized collection, the default cap must keep the (0xA2,26) body short
    # enough that the client's sqMg recv-log line (~50-byte prefix + body)
    # stays under the suspected ~630-byte second-stage buffer. Every crash
    # dump's line was 637 bytes (20 cards); 550 leaves margin for the prefix.
    _fake = [[166, 20, 1, 40, 45, 10, 13, 2]] * 40   # realistic digit widths
    _saved_cc = globals().get("_collection_cards")
    try:
        globals()["_collection_cards"] = lambda mid: _fake
        _line = _trade_card_line("slottest")
        import re as _re
        _hi = max((int(x) for x in _re.findall(rb"@N(\d+)=", _line)), default=-1)
        if _hi >= TRADE_BOARD_SLOTS:
            _say("FAIL: (0xA2,26) must be capped at %d cards, got @N up to %d"
                 % (TRADE_BOARD_SLOTS, _hi)); ok = False
        if len(_line) > 550:
            _say("FAIL: (0xA2,26) body must stay under 550 bytes at the "
                 "default cap (sqMg logger overflow), got %d" % len(_line))
            ok = False
        if b"/E=%d/S=0/C=%d" % (_hi + 1, _hi + 1) not in _line:
            _say("FAIL: the list must be the COMPLETE form /E=n/S=0/C=n "
                 "(S+C==E) -- the incomplete form is measured to park the "
                 "26-waiter; got %r" % (_line[:40],)); ok = False
    finally:
        if _saved_cc is not None:
            globals()["_collection_cards"] = _saved_cc

    # A bare member_id records the pair but authors nothing (no partner).
    _tmid = "selftest-trade"
    note_trade_accept(_tmid)
    if _pending_pushes(_tmid):
        _say("FAIL: a bare-member trade arm must not author any push"); ok = False
    note_trade_cancel(_tmid)
    # The legacy game-band card serve is default OFF now (a trade never opens
    # the game band).
    note_trade_accept(_tmid)
    if isinstance(handle_line(b"E3000000@Init=/NN=0", member_id=_tmid), list):
        _say("FAIL: the game-band @Init= card serve must be off by default "
             "(POL_TM_TRADE_INIT_CARDSERVE)"); ok = False
    note_trade_cancel(_tmid)

    # `@Opt=` -- the option write-back. Same delimiter rule as every other Ans,
    # and `/Ans=` must be > 0 or the sending scene reads it as "still pending"
    # and polls forever (caller 0x13F83A, `cmp edi,0 ; jg <success>`).
    opt = handle_line(b"84000000@Opt=/NN=0/HID=0/Dm=0/Vol=0/CN=1000000002"
                      b"/Ar=0/Cu=0/Vi=0/Se=0/Bgm=0/Per=0/Ran=0"
                      b"/CMD=0/CAD=0/CL=8/CT=60/HNSS=1")
    if not opt or not opt.startswith(encode_code(MSG_OPTANS)):
        _say("FAIL: @Opt= must answer with code %d, got %r"
              % (MSG_OPTANS, opt)); ok = False
    if not opt or b"@OptAns=" not in opt:
        _say("FAIL: @OptAns needs the `=` key delimiter"); ok = False
    if not opt or b"/Ans=0" in opt:
        _say("FAIL: @OptAns /Ans= must be > 0"); ok = False

    # The field map is what actually persists the two screens' settings, so
    # assert it rather than the message. `/HNSS=` is the one field with a
    # confirmed meaning; a silently-renamed key here would look like a working
    # reply and a prompt that never stops coming back.
    fields = _opt_fields(b"@Opt=/Ar=1/Cu=2/Vi=3/Se=4/Bgm=5/Per=6/Ran=7"
                         b"/CMD=1/CAD=0/CL=8/CT=60/HNSS=1")
    want = {0x0B0: 1, 0x0B1: 2, 0x0B2: 3, 0x0B3: 4, 0x0B4: 5, 0x0B5: 6,
            0x0B6: 7, 0x100: 1, 0x101: 0, 0x102: 8, 0x103: 60,
            SAVE_OFF_HNSS: 1}
    if fields != want:
        _say("FAIL: @Opt field map %r, expected %r" % (fields, want)); ok = False
    # A negative value is legal on the wire (the builder prints `-` and negates,
    # 0x18933) and must survive as the byte the client will read back.
    if _opt_fields(b"@Opt=/HNSS=-1") != {SAVE_OFF_HNSS: 0xFF}:
        _say("FAIL: a negative @Opt value must truncate to its byte"); ok = False
    # THE RESERVATION, asserted against the LIVE line off a second machine
    # (`logs/uploads/FRAMEWORK-12960-20260818T033537Z.log` line 15598, and
    # authserv.log 03:30:47). Three things have to hold at once or the client
    # goes on polling with "Retrieving table info..." on screen, which is exactly
    # what the unanswered version looked like:
    #   the code+msgid must be 0x41/2 -- the waiter is 0x827F0(buf, 0x41, 2) and
    #     nothing else in the store will be looked at;
    #   the `=` delimiter, the same rule as every other Ans on this channel;
    #   `/EN=` must be > 0 -- 0 becomes the client's own -99, "Timed out while
    #     updating table data" (tm-errors 37399).
    LIVE_GAMEEN = (b"41000100@GameEN=/NN=AB12CD56EB0F5932/HID=0/Dm=0/Vol=0"
                   b"/CN=/HN=4C6170746F705465737432/L=1")
    en = handle_line(LIVE_GAMEEN, member_id="selftest")
    if not en or not en.startswith(b"41000200"):
        _say("FAIL: @GameEN= must answer with code 0x41 msgid 2, got %r"
              % (en,)); ok = False
    if not en or b"@GameEA=" not in en:
        _say("FAIL: @GameEA needs the `=` key delimiter"); ok = False
    if not en or b"/EN=0" in en or b"/EN=" not in en:
        _say("FAIL: @GameEA /EN= must be present and > 0, got %r" % (en,)); ok = False
    # CONFIRM. Measured 04:25:07Z: `41000500@GameQT=`, no fields, unanswered ->
    # Lobby.BIN 337 on screen. The reply is code 0x41 msgid 6 and `/QT=` > 0.
    qt = handle_line(b"41000500@GameQT=", member_id="selftest")
    if not qt or not qt.startswith(b"41000600"):
        _say("FAIL: @GameQT= must answer with code 0x41 msgid 6, got %r"
              % (qt,)); ok = False
    if not qt or b"@GameQA=" not in qt or b"/QT=0" in qt:
        _say("FAIL: @GameQA needs the `=` delimiter and /QT= > 0, got %r"
              % (qt,)); ok = False

    # `@GameENC=` is a DIFFERENT command and must not be swallowed by the
    # `@GameEN=` branch -- the two literals sit 0x104 bytes apart in TM.dll and
    # only the substring test keeps them apart here. It is now ANSWERED, with
    # `@GameECA` (the VS. COM session's first reply), so the assertion is no
    # longer "stays silent" but "answers as ITSELF": right command, and NOT
    # `@GameEA`, which is what routing it into the reservation branch would
    # produce. That is the regression this test has always really been about.
    enc = handle_line(b"41001200@GameENC=/NN=0", member_id="selftest")
    if enc is None:
        _say("FAIL: @GameENC= must be answered (@GameECA) -- got silence"); ok = False
    elif b"@GameECA=" not in enc or b"@GameEA=" in enc:
        _say("FAIL: @GameENC= must answer @GameECA, never @GameEA, got %r"
              % (enc,)); ok = False
    elif b"/EN=" not in enc:
        # /EN= AND NOT /TblId=: the parser at TM.dll 0x8538D reads `/EN=` and
        # takes the -99 failure arm when it is 0 or absent. /TblId= and /TblNo=
        # belong to @MuchMake, a later message -- serving them here is what
        # produced Lobby.BIN 448 twice live.
        _say("FAIL: @GameECA needs /EN= (TM.dll 0x853AF), got %r" % (enc,))
        ok = False
    elif b"/EN=0" in enc:
        _say("FAIL: @GameECA /EN= must be > 0 -- 0 is the -99 arm = 448, got %r"
             % (enc,)); ok = False
    # AND THE SILENT CONTROL STILL WORKS -- this reply is built from an inferred
    # msgid, so the A/B that puts the old behaviour back must stay live.
    _saved_eca = os.environ.get("POL_TM_GAMEECA")
    try:
        os.environ["POL_TM_GAMEECA"] = "0"
        # WARNING: A DIFFERENT MEMBER, and that is the point rather than a dodge. The
        # assertion above already ran `@GameENC=` for "selftest", which QUEUED
        # the `@MuchMake` pair -- and a queued push drains onto whatever reply
        # comes next, so re-asking on the same member returns those pushes and
        # the control reads as "not silent" no matter what the branch does. The
        # control is about the `@GameENC=` BRANCH, so it needs a member with no
        # queue behind it. (The real-world edge is the same and is acceptable:
        # flipping the knob mid-session does not un-queue what was already
        # handed out; it is an A/B switch to set before a session, not a kill
        # switch for one in flight.)
        if handle_line(b"41001200@GameENC=/NN=0",
                       member_id="selftest-eca-off") is not None:
            _say("FAIL: POL_TM_GAMEECA=0 must restore silence"); ok = False
    finally:
        if _saved_eca is None:
            os.environ.pop("POL_TM_GAMEECA", None)
        else:
            os.environ["POL_TM_GAMEECA"] = _saved_eca

    # Persistence must be a no-op without a member -- a stateless run must never
    # write somebody's save, and `member_id` is None on any unbound session.
    if _save_patch_many(None, {SAVE_OFF_HNSS: 1}) != {}:
        _say("FAIL: _save_patch_many must do nothing without a member"); ok = False

    # --- MONEY AND THE SALE ---------------------------------------------------
    # Against the REAL bytes: member 1's stored collection and the `@Sell=` the
    # client actually sent on 2026-08-18T18:52:06Z.
    import tempfile as _tempfile
    _dir = _tempfile.mkdtemp()
    _old_res = os.environ.get("POL_RESOURCE_DIR")
    _old_wr = os.environ.get("POL_TM_SAVE_WRITE")
    os.environ["POL_RESOURCE_DIR"] = _dir
    os.environ["POL_TM_SAVE_WRITE"] = "0"       # the binary save has its own test
    try:
        _cards = [[160, 84, 0, 99, 75, 23, 20, 0], [42, 43, 1, 20, 18, 7, 4, 2],
                  [220, 55, 0, 78, 80, 19, 18, 2], [20, 18, 1, 11, 10, 4, 2, 2]]
        json.dump({"cards": _cards},
                  open(os.path.join(_dir, "1.tm_collection.json"), "w"))

        # The wire entry and the stored row DIVERGE past field 5, which is why
        # the key is five fields and not the whole row.
        parsed = _parse_card_list(b"@Sell=/C=1@0=/D=220|55|0|78|80|18")
        if parsed != [[220, 55, 0, 78, 80, 18]]:
            _say("FAIL: @Sell= did not parse, got %r" % (parsed,)); ok = False
        if parsed and parsed[0][:_CARD_KEY_FIELDS] != _cards[2][:_CARD_KEY_FIELDS]:
            _say("FAIL: the five-field key must match the stored row"); ok = False
        if parsed and parsed[0][:6] == _cards[2][:6]:
            _say("FAIL: fields 0..5 must NOT match -- if they do, the wire and "
                  "the row agree further than measured and the key can grow")
            ok = False

        # --- @Decks= STAT SYNC (A/B feature, DEFAULT OFF) --------------------
        # The merge remaps a @Decks= report's 7-field /D= (id|atk|type|pdef|mdef|
        # ARROWS|flag) onto the stored 8-field row (power at idx 5 left alone).
        # It is DEFAULT OFF (the SERVER owns card stats now -- growth would be
        # undone by taking client-reported stats), so opt in to test the mapping.
        # Own member id so it cannot perturb member 1's fixtures below.
        _saved_sync = os.environ.get("POL_TM_DECK_STAT_SYNC")
        os.environ["POL_TM_DECK_STAT_SYNC"] = "1"
        json.dump({"cards": [[220, 55, 0, 78, 80, 19, 18, 2],
                             [220, 55, 0, 78, 80, 19, 18, 2],   # a duplicate id
                             [160, 84, 0, 99, 75, 23, 20, 0]]},
                  open(os.path.join(_dir, "dm.tm_collection.json"), "w"))
        _collection_set_deck("dm", b"@Decks=/C=1@0=/D=220|99|0|78|90|31|0")
        _dmc = _collection_load("dm").get("cards", [])
        _m220 = next((c for c in _dmc if c and c[0] == 220), None)
        if not _m220 or _m220[1:5] != [99, 0, 78, 90]:
            _say("FAIL: @Decks= must merge the reported combat stats onto the "
                 "stored card by id -- got %r" % (_m220,)); ok = False
        if _m220 and _m220[6] != 31:
            _say("FAIL: @Decks= arrows (wire idx 5) must land on stored idx 6, "
                 "not be copied positionally -- got %r" % (_m220,)); ok = False
        if _m220 and (_m220[5] != 19 or _m220[7] != 2):
            _say("FAIL: @Decks= must leave the derived power (idx 5) and the flag "
                 "(idx 7) untouched -- got %r" % (_m220,)); ok = False
        if sum(1 for c in _dmc if c and c[0] == 220 and c[1] == 55) != 1:
            _say("FAIL: only ONE duplicate of id 220 must upgrade -- the other "
                 "stays nominal (no per-instance id) -- got %r" % (_dmc,)); ok = False
        if next((c for c in _dmc if c and c[0] == 160), None) != \
                [160, 84, 0, 99, 75, 23, 20, 0]:
            _say("FAIL: a card NOT in the @Decks= report must not change"); ok = False
        if _merge_deck_stats_into("dm", {"cards": list(_dmc)},
                                  b"@Decks=/C=1@0=/D=220|99|0|78|90|31|0"):
            _say("FAIL: re-reporting the same @Decks= must be idempotent"); ok = False
        if _saved_sync is None:
            os.environ.pop("POL_TM_DECK_STAT_SYNC", None)
        else:
            os.environ["POL_TM_DECK_STAT_SYNC"] = _saved_sync

        # --- CARD GROWTH: a used card grows toward its ceiling, never past ----
        json.dump({"cards": [list(roll_card_row) for roll_card_row in
                             ([160, 40, 0, 50, 40, 10, 20, 0],   # Jecht, rolled low
                              [166, 10, 1, 20, 22, 5, 13, 0])]},  # Shelinda, rolled low
                  open(os.path.join(_dir, "gm.tm_collection.json"), "w"))
        _grew = _grow_used_cards("gm", [b"160|40|0|50|40|10|20|0",
                                        b"166|10|1|20|22|5|13|0"])
        _gmc = _collection_load("gm").get("cards", [])
        _g160 = next((c for c in _gmc if c and c[0] == 160), None)
        _b160 = tm_cardprm.row(160)
        if _grew < 1:
            _say("FAIL: using a below-ceiling card must grow it"); ok = False
        if _g160 and (_g160[1] < 40 or _g160[3] < 50 or _g160[4] < 40):
            _say("FAIL: growth must not LOWER a stat -- got %r" % (_g160,)); ok = False
        if _g160 and (_g160[1] > _b160[0] or _g160[3] > _b160[2]
                      or _g160[4] > _b160[3]):
            _say("FAIL: growth must never exceed the CardPrm ceiling -- got %r "
                 "vs ceiling [%d,_,%d,%d]" % (_g160, _b160[0], _b160[2],
                                              _b160[3])); ok = False
        # a maxed card that is used must NOT grow (no room)
        json.dump({"cards": [[160, _b160[0], 0, _b160[2], _b160[3], 23, 20, 0]]},
                  open(os.path.join(_dir, "gx.tm_collection.json"), "w"))
        if _grow_used_cards("gx", [b"160|%d|0|%d|%d|23|20|0"
                                   % (_b160[0], _b160[2], _b160[3])]) != 0:
            _say("FAIL: a maxed card must not grow (no room)"); ok = False

        if money_of(1) != _start_money():
            _say("FAIL: an unstored balance must read as the start money"); ok = False
        if b"/M=%d" % _start_money() not in _shopinit_body(1):
            _say("FAIL: SHOPINIT must carry the member's money"); ok = False

        # WARNING: AND THE START MUST BE **ZERO** -- THE 2026-08-25 REGRESSION.
        # `_start_money()` defaulted to an unmeasured 10000, so every new member
        # was minted 10000 gold and then handed it, late, by whichever channel
        # they touched first: `@ComGameInit=/M=` writes straight into the live
        # wallet (0x102DA9, save struct +0xC8), and the authored save carries it
        # at `tmsave.MONEY_OFF` for the next launch. Two players reported it the
        # same minute -- "quit, lost a card, and was given 10000 gil" and "backed
        # out ... given 10000 money upon logging back in" -- and neither was a
        # payout: it was this constant arriving. SE's own ladder says 0 is right:
        # `PackPrm.BIN` opens with the FREE Pauper's Pack, which is the bootstrap
        # for a player who has nothing. See the banner above `_start_money`.
        if "POL_TM_START_MONEY" not in os.environ and _start_money() != 0:
            _say("FAIL: a new member must start with NOTHING -- _start_money() "
                 "is %d, and every gold of it gets minted and pushed into the "
                 "client's wallet" % _start_money()); ok = False

        # ...AND READING IT MUST HAVE OPENED THE ACCOUNT. WARNING: This is the free-gold
        # bug: `_start_money()` used to be returned without ever being stored, so
        # every read answered 10000 again and nothing the client spent could
        # stick. Measured 2026-08-20: a member bid 50 on an auction, their client
        # went to 9950, and `money_of` still said 10000 because their collection
        # carried no `money` key. See `money_of`.
        _acct = _collection_load(1)
        if _acct.get("money") is None:
            _say("FAIL: reading an unstored balance must OPEN THE ACCOUNT -- a "
                 "fallback that stores nothing refunds every purchase on the "
                 "next serve."); ok = False
        # ...and it must be an account, not a reset: a spend must now survive.
        # WARNING: RESTORED AFTERWARDS. Later assertions in this block are written
        # against `_start_money()`, so a probe that leaves the balance moved
        # fails THEM instead -- which is how this very check first "found" a
        # sale-pricing bug that did not exist.
        _was = money_of(1)
        _set_money(1, 9950, "selftest: the client spent 50")
        if money_of(1) != 9950:
            _say("FAIL: a stored balance must survive a re-read -- got %d"
                 % (money_of(1),)); ok = False
        _set_money(1, _was, "selftest: restoring")

        # WARNING: PRICED FROM THE CARD'S OWN LEVEL, not a flat rate. The WIRE row's
        # index 5 is the level (18 here -> CardPri[18] = 2000 -> *7/8 = 1750);
        # the STORED row's index 5 is something else, which is exactly why the
        # sell path must price from the wire row and not the identity key.
        _wire = [220, 55, 0, 78, 80, 18]
        _expect = _sell_price(_wire)
        if _expect != 1750:
            _say("FAIL: level 18 must price at floor(CardPri[18]*7/8)=1750, got "
                 "%d" % _expect); ok = False
        _sell_cards(1, b"@Sell=/C=1@0=/D=220|55|0|78|80|18")
        if money_of(1) != _start_money() + _expect:
            _say("FAIL: the sale must credit the CARD'S price (%d), money is %d"
                 % (_expect, money_of(1))); ok = False
        left = json.load(open(os.path.join(_dir, "1.tm_collection.json")))["cards"]
        if len(left) != 3 or any(c[0] == 220 for c in left):
            _say("FAIL: the sold card is still in the collection: %r" % (left,)); ok = False
        if b"/M=%d" % money_of(1) not in _shopinit_body(1):
            _say("FAIL: SHOPINIT must serve the NEW balance -- this is the whole "
                  "point; a constant here is what reset money on every visit")
            ok = False

        # --- PACK PRICES: /No= indexes PackPrm.BIN --------------------------
        # Validated against a tester's own purchases: /No=3 cost 9000 and
        # /No=0 is the free Pauper's Pack.
        # /No=4 is 30000 in the PC build's table and 40000 in the PS2's: the
        # 2026-08-25 "39980 -> 0 over a 20 shortfall, and the client delivered
        # the pack anyway" incident was a PC client priced off the PS2 table.
        _ladder = [_pack_price(i) for i in range(6)]
        if _ladder[:4] != [0, 1000, 1500, 9000] or _ladder[4] not in (30000, 40000)                 or _ladder[5] != 100000:
            _say("FAIL: PackPrm.BIN must decode (52-byte records from +16, price "
                 "at +4) to the shop's ladder -- got %r" % (_ladder,)); ok = False
        # A buy must DEBIT, and the free pack must not. WARNING: FUND THE MEMBER
        # FIRST: at the (correct) zero start, `max(0, bal - 9000)` is 0 whether
        # the debit ran or not, so the assertion would pass on a path that never
        # subtracts anything. The balance has to be able to fall.
        _set_money(1, 20000, "selftest: funding the pack-price assertions")
        _b4 = money_of(1)
        handle_line(b"B2001401@Buy=/No=3", member_id=1)
        if money_of(1) != max(0, _b4 - 9000):
            _say("FAIL: pack /No=3 must debit 9000 -- %d -> %d"
                 % (_b4, money_of(1))); ok = False
        _b5 = money_of(1)
        handle_line(b"B2001401@Buy=/No=0", member_id=1)
        if money_of(1) != _b5:
            _say("FAIL: the Pauper's Pack is FREE and must not debit -- %d -> %d"
                 % (_b5, money_of(1))); ok = False

        # WARNING: AND A PACK WE THINK THEY CANNOT AFFORD MUST NOT MOVE MONEY AT ALL.
        # The old arm was `max(0, bal - cost)` unconditionally, which on
        # 2026-08-25T04:25:54Z took member 3 from 39980 to **0** over a 20-gold
        # shortfall on a 40000 pack -- and the client then delivered that pack
        # anyway, proving OUR number was the wrong one. A clamp here is
        # unrecoverable and `@ComGameInit=/M=` assigns it into the live wallet.
        _short = _pack_price(4) - 20
        _set_money(1, _short, "selftest: the 20-gold-short case")
        handle_line(b"B2001401@Buy=/No=4", member_id=1)   # 20 short of /No=4
        if money_of(1) != _short:
            _say("FAIL: an unaffordable pack must not debit -- %d -> %d "
                 "(a clamp to 0 destroys the balance to cover a 20 shortfall)"
                 % (_short, money_of(1))); ok = False

        # --- THE DISCARD removes a card and moves NOTHING else ---------------
        # Captured live 2026-08-20T15:40:38Z on (0xB2, 0x10):
        #     @Erase=/Mode=1/C=1@0=/D=201|69|0|55|56|16
        # Fire-and-forget (0x1080E4 drains no slot), but the card must leave the
        # collection or the next save write puts it back and the discard undoes
        # itself. The pair is also a control: a discard that moves money means
        # the MONEY path is wrong, not the card path.
        _bal = money_of(1)
        _n = len(json.load(open(os.path.join(_dir, "1.tm_collection.json")))["cards"])
        _erase_cards(1, b"@Erase=/Mode=1/C=1@0=/D=99|1|0|1|1|1")
        _after = json.load(open(os.path.join(_dir, "1.tm_collection.json")))
        if money_of(1) != _bal:
            _say("FAIL: a discard must not move money -- %d -> %d"
                 % (_bal, money_of(1))); ok = False
        # (that card was not in the collection, so the count must be unchanged
        #  and the drift reported rather than silently dropping a real card)
        if len(_after["cards"]) != _n:
            _say("FAIL: discarding a card we do not hold must not remove one we "
                 "do; %d -> %d" % (_n, len(_after["cards"]))); ok = False
        # ...and one we DO hold must actually go.
        _held = list(_after["cards"][0])
        _erase_cards(1, b"@Erase=/Mode=1/C=1@0=/D=%s"
                     % "|".join(str(v) for v in _held[:5]).encode("ascii"))
        _left = json.load(open(os.path.join(_dir, "1.tm_collection.json")))["cards"]
        if len(_left) != _n - 1 or any(list(c)[:5] == _held[:5] for c in _left):
            _say("FAIL: a discarded card must leave the collection -- %r still "
                 "holds %r" % (_left, _held[:5])); ok = False

        # A card we never recorded is still a real sale: credit it, do not drop it.
        _before = money_of(1)
        # ...at ITS OWN price: level 1 -> CardPri[1] = 25 -> *7/8 = 21.
        _unrec = _sell_price([99, 1, 0, 1, 1, 1])
        _sell_cards(1, b"@Sell=/C=1@0=/D=99|1|0|1|1|1")
        if money_of(1) != _before + _unrec:
            _say("FAIL: an unrecorded card must still credit the player, at its "
                 "own price (%d); money %d -> %d" % (_unrec, _before, money_of(1)))
            ok = False

        # A `POL_TM_SHOP_M` override still wins -- that knob is how the field's
        # meaning was A/B'd and taking it away would break the experiment.
        os.environ["POL_TM_SHOP_M"] = "777"
        if b"/M=777" not in _shopinit_body(1):
            _say("FAIL: POL_TM_SHOP_M must override the stored balance"); ok = False
        del os.environ["POL_TM_SHOP_M"]

        # THE BALANCE HAS TO REACH THE SAVE, because that is where the client
        # reads it -- SHOPINIT's `/M=` does not feed the Money display (measured:
        # /M=4321 served, screen stayed at 87 across a shop exit and re-entry).
        os.environ["POL_TM_SAVE_WRITE"] = "1"
        _set_money(1, 4321, "selftest")
        _collection_to_save(1, [[220, 55, 0, 78, 80, 19, 18, 2]])
        _blob = open(os.path.join(_dir, "1.U_g_TM0DataFile.bin"), "rb").read()
        # WARNING: MONEY IS A DWORD AT +0x34, NOT A u16 AT +0x38. Corrected
        # 2026-08-20: +0x38 is Card Points (struct +0x00, where `/CP=` lands);
        # money is file +0x34 -> struct +0xC8, the field BOTH GameInits' `/M=`
        # writes with the same clamp-to-zero (0x1028CE, 0x102DA9). Every save on
        # the server read 0 at +0x34, which is why every player had 0 money.
        if struct.unpack_from("<I", _blob, tmsave.MONEY_OFF)[0] != 4321:
            _say("FAIL: the save must carry the balance as a DWORD at +0x%02X, "
                  "got %d" % (tmsave.MONEY_OFF,
                              struct.unpack_from("<I", _blob, tmsave.MONEY_OFF)[0]))
            ok = False
        # ...and it must NOT have landed in Card Points, the old wrong offset.
        if struct.unpack_from("<H", _blob, tmsave.CARDPOINTS_OFF)[0] == 4321:
            _say("FAIL: the balance was written into Card Points (+0x%02X) -- "
                  "that is the off-by-four this corrects"
                  % (tmsave.CARDPOINTS_OFF,)); ok = False
        if struct.unpack_from("<H", _blob, tmsave.COUNT_OFF)[0] != 1:
            _say("FAIL: the save's card count was disturbed"); ok = False
        # THE THREE ARE CONSECUTIVE DWORD SLOTS in the parser's copy order:
        # +0x34 money (0x101234) -> +0x38 card points (0x10124D) -> +0x3C count.
        # A gap that is not 4 apiece means one of them moved.
        if tmsave.MONEY_OFF + 4 != tmsave.CARDPOINTS_OFF or                 tmsave.CARDPOINTS_OFF + 4 != tmsave.COUNT_OFF:
            _say("FAIL: +0x38 and +0x3C are four bytes apart in the parser; a "
                  "MONEY_OFF that is not COUNT_OFF-4 means one of them moved")
            ok = False
        # A caller that does not track money must NOT zero somebody's balance.
        _keep = tmsave.build([[220, 55, 0, 78, 80, 19, 18, 2]], _blob)
        if struct.unpack_from("<H", _keep, tmsave.MONEY_OFF)[0] != 4321:
            _say("FAIL: build() without `money` must leave the balance alone")
            ok = False
        os.environ["POL_TM_SAVE_WRITE"] = "0"

        # `/PM=` IS THE MONEY ON SHOPINIT, and an absent one is an active
        # ZEROING -- the arm writes 0 into [esi+0xcc] before it parses the key.
        _set_money(1, 4321, "selftest")
        body = _shopinit_body(1)
        if b"/PM=4321" not in body:
            _say("FAIL: SHOPINIT must carry /PM= with the balance, got %r"
                  % (body,)); ok = False
        # ...and it must be a SEPARATE key from /M=, which this arm discards.
        if b"/M=" not in body:
            _say("FAIL: /M= should still be served alongside /PM="); ok = False
        if body.index(b"/PM=") <= body.index(b"/M="):
            _say("FAIL: /PM= must not be mistaken for /M= -- a parser looking "
                  "for '/M=' would match inside '/PM='; keep /M= first so the "
                  "served order matches the client's own parse order")
            ok = False

        # THE WALLET MESSAGE. AUCMONEY is the only thing that carries money;
        # SHOPINIT parses the same `/M=` key and throws it away.
        _old_n = os.environ.get("POL_TM_SHOP_N")
        os.environ["POL_TM_SHOP_N"] = "1"
        _set_money(1, 4321, "selftest")
        line = _aucmoney_line(1)
        if line != b"B2002001@Data=/M=4321":
            _say("FAIL: AUCMONEY line is %r" % (line,)); ok = False
        # The header is code | msgid<<16 | shop<<24, and the SHOP BYTE MUST
        # TRACK `/N=` -- the arm at 0x1060F9 compares it against [esi+0x30],
        # which is where SHOPINIT stored /N=. A mismatch is silently dropped.
        os.environ["POL_TM_SHOP_N"] = "11"
        if _aucmoney_line(1)[:8] != b"B200200B":
            _say("FAIL: the shop byte must follow /N=, got %r"
                  % (_aucmoney_line(1)[:8],)); ok = False
        if _shopinit_body(1)[:9] != b"@Init=/N=":
            _say("FAIL: SHOPINIT must still lead with /N="); ok = False
        if b"/N=11" not in _shopinit_body(1):
            _say("FAIL: /N= and the AUCMONEY shop byte must be the SAME value")
            ok = False
        if _old_n is None:
            os.environ.pop("POL_TM_SHOP_N", None)
        else:
            os.environ["POL_TM_SHOP_N"] = _old_n

        # BOTH DOORS GET AUCMONEY; THE AMOUNT IS WHAT DIFFERS. They send the same
        # `@ShReq=` body and are told apart ONLY by the msgid, so that is what
        # this asserts on -- see the banner above `SHREQ_MSGID_CHECKOUT`.
        # Withholding the message from Check Out is measured to hang the screen
        # before it renders, and sending the WALLET there announced a card sale
        # that never happened; the honest value is the proceeds awaiting
        # collection, which is 0 while no auction has settled.
        _set_money(1, 4321, "selftest")
        _req = b"@ShReq=/NN=0/HID=0/Dm=0/Vol=0/CN=0"
        _shop = handle_line(encode_code(MSG_SHREQ) + _req, member_id=1)
        _chk = handle_line(encode_code(MSG_SHREQ | (SHREQ_MSGID_CHECKOUT << 16))
                           + _req, member_id=1)
        if not any(b"@Data=/M=4321" in l for l in (_shop or [])):
            _say("FAIL: the CARD SHOP door (msgid 0x00) must get AUCMONEY "
                  "carrying the WALLET -- it is the only message that does")
            ok = False
        if not any(b"@Data=/M=0" in l for l in (_chk or [])):
            _say("FAIL: the Check Out door (msgid 0x%02X) must get AUCMONEY too "
                  "-- the scene blocks until it arrives -- but carrying the "
                  "proceeds awaiting collection (0), not the wallet"
                  % SHREQ_MSGID_CHECKOUT); ok = False
        if any(b"@Data=/M=4321" in l for l in (_chk or [])):
            _say("FAIL: Check Out must NOT be told the wallet -- that is what "
                  "announced a sale of the player's own balance"); ok = False
        if not any(b"@ShEnter=" in l for l in (_chk or [])):
            _say("FAIL: Check Out must still be handed its endpoint"); ok = False

        # THE DRAIN-PACED STREAM. door = money A + cards A; @Get #1 -> cards B
        # (empty, fresh 0x21 after cards A drained); @Get #2 -> money B + @EQuit
        # (fresh 0x20 after money A drained). NO @Dead (0x27 = the error). See
        # the _CHECKOUT_PULL banner.
        _msgids = [l[4:6] for l in (_chk or []) if l[:2] == b"B2"]
        if _msgids != [b"13", b"20", b"21"]:
            _say("FAIL: the Check Out door batch is money A + cards A only "
                  "(same-type records drain, so cards B / money B pace on the "
                  "@Gets); it queued %r" % _msgids)
            ok = False
        if any(l[4:6] == b"27" for l in (_chk or []) if l[:2] == b"B2"):
            _say("FAIL: @Dead (0x27) must NEVER ride the Check Out stream -- "
                  "it is the scene's error channel (dialog 221)"); ok = False
        _p1 = handle_line(b"B2001501@Get=/No=1", member_id=1)
        _p2 = handle_line(b"B2001501@Get=/No=1", member_id=1)
        _p3 = handle_line(b"B2001501@Get=/No=1", member_id=1)
        _sched = [[l[4:6] for l in (p or []) if l[:2] == b"B2"]
                  for p in (_p1, _p2, _p3)]
        if _sched != [[b"21"], [b"20", b"17"], []]:
            _say("FAIL: @Get #1 -> cards B (0x21), @Get #2 -> money B (0x20) + "
                  "@EQuit (0x17), then silence; got %r" % _sched); ok = False
        if not (_p2 and any(b"@EQuit=/D=0" in l for l in _p2)):
            _say("FAIL: @Get #2 must carry @EQuit=/D=0 -- state 0x0C polls "
                  "msgid 23; got %r" % _p2); ok = False
        os.environ["POL_TM_SHOPQUIT_PS2"] = "0"
        _q = handle_line(b"B2001701@Quit=/No=1", member_id=1)
        del os.environ["POL_TM_SHOPQUIT_PS2"]
        if isinstance(_q, list) or b"@EQuit=" not in _q:
            _say("FAIL: a @Quit= after the schedule ran must be the plain "
                  "@EQuit; got %r" % _q); ok = False
        # The knob must select the legacy shape, or the bisect needs a deploy.
        os.environ["POL_TM_CHECKOUT_STREAM"] = "0"
        _leg = handle_line(encode_code(MSG_SHREQ | (SHREQ_MSGID_CHECKOUT << 16))
                           + _req, member_id=1)
        del os.environ["POL_TM_CHECKOUT_STREAM"]
        _legids = [l[4:6] for l in (_leg or []) if l[:2] == b"B2"]
        if _legids != [b"13", b"20"]:
            _say("FAIL: POL_TM_CHECKOUT_STREAM=0 must reproduce the legacy "
                  "SHOPINIT+AUCMONEY shape; it queued %r" % _legids)
            ok = False
        _CHECKOUT_PULL.pop(1, None)

        # AND THE TWO DOORS MUST NOT BE SET UP THE SAME WAY. The client logged
        # `Now1stPackName=` at RVA 0x105390 on the Check Out screen -- inside the
        # CARD SHOP's `@Init` arm -- because that is the body we sent it. See the
        # banner above `_INIT_ARM_FIELDS`.
        if not any(b"@Init=" in l for l in (_shop or [])):
            _say("FAIL: the card shop door must still get @Init= -- that half "
                  "is not in question and must not move"); ok = False
        if any(b"@Init=" in l for l in (_chk or [])):
            _say("FAIL: Check Out must NOT be sent the card shop's @Init= -- "
                  "that is the arm that ran the pack-name setup on it"); ok = False
        if not any(b"@EcmInit=" in l for l in (_chk or [])):
            _say("FAIL: Check Out must get the arm _checkout_init_arm() picked")
            ok = False
        # Every arm serves /N= FIRST and it must equal the AUCMONEY shop byte,
        # whichever body the knob selects -- the gate at 0x1060F9 compares them.
        for _arm in sorted(_INIT_ARM_FIELDS):
            _b = _init_body(_arm, 1, 0)
            if not _b.startswith(b"@" + _arm.encode() + b"=/N="):
                _say("FAIL: @%s= must lead with /N=, got %r" % (_arm, _b))
                ok = False
            if _arm != "Init":
                # `@Init` is EXEMPT and stays so: `_shopinit_body` deliberately
                # serves six of its nine fields plus /PM=, and the card shop is
                # working to the extent it works with exactly those. The new arms
                # have no such history, so they serve their field set whole.
                for _f in _INIT_ARM_FIELDS[_arm][1]:
                    if b"/" + _f.encode() + b"=" not in _b:
                        _say("FAIL: @%s= is missing /%s=, which the arm parses"
                              % (_arm, _f)); ok = False
            # /PM= must never be mistaken for /M=, the same trap as on @Init.
            if b"/PM=" in _b and b"/M=" in _b and _b.index(b"/PM=") <= _b.index(b"/M="):
                _say("FAIL: @%s= must serve /M= before /PM=" % _arm); ok = False
        # A bad knob value must fall back, not crash and not serve `@garbage=`.
        os.environ["POL_TM_CHECKOUT_INIT"] = "nonsense"
        if _checkout_init_arm() != "EcmInit":
            _say("FAIL: an unknown POL_TM_CHECKOUT_INIT must fall back"); ok = False
        os.environ["POL_TM_CHECKOUT_INIT"] = "ExcInit"
        if b"@ExcInit=" not in _init_body(_checkout_init_arm(), 1, 0):
            _say("FAIL: POL_TM_CHECKOUT_INIT must select the arm -- if it does "
                  "not, the A/B needs a redeploy per guess and that is the whole "
                  "point of the knob"); ok = False
        del os.environ["POL_TM_CHECKOUT_INIT"]

        # Money must never go negative, whatever it is asked to store.
        # Assert on the STORED value, not on money_of() -- both clamp, so a
        # reader-side clamp would hide a writer that persisted -50.
        _set_money(1, -50)
        _stored = json.load(open(os.path.join(_dir, "1.tm_collection.json")))
        if _stored.get("money") != 0:
            _say("FAIL: a negative balance must be clamped BEFORE it is stored, "
                  "got %r" % (_stored.get("money"),)); ok = False
    finally:
        for k, v in (("POL_RESOURCE_DIR", _old_res), ("POL_TM_SAVE_WRITE", _old_wr)):
            if v is None:
                os.environ.pop(k, None)
            else:
                os.environ[k] = v

    _say("\n%s" % ("selftest OK" if ok else "SELFTEST FAILED"))
    return 0 if ok else 1


if __name__ == "__main__":
    import sys
    if "--selftest" in sys.argv:
        raise SystemExit(selftest())
    _say(f"content id {CONTENT_ID} ({CONTENT_NAME})")
    _say(describe())
    _say("states:", [s.name for s in TMState])
