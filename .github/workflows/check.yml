name: checks
on: [push, pull_request]
jobs:
  hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: scanner self-test (positive controls)
        run: python check.py --selftest
      - name: repository hygiene scan
        run: python check.py
  selftests:
    runs-on: ubuntu-latest
    steps:
      # the title runs inside the OpenLobby core, so its suites need the
      # core checked out beside it (tools/tm_testenv.py finds ../openlobby)
      - uses: actions/checkout@v4
        with:
          path: crystalmaster
      - uses: actions/checkout@v4
        with:
          repository: PrettyOpenLobby/OpenLobby
          path: openlobby
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml pycryptodome pillow
      - run: python tools/tm_run_all.py
        working-directory: crystalmaster
